# Прогресс NTSD за ночь 8–9 сентября

Оценка на коммите `ec0d5369dd5de1ad09acf46c900ea08ca6ec661c`,
2026-09-09 07:29:17 UTC+3. Последний игровой milestone в этом срезе —
`30be76e`, камера и фон. Изменения отрисовки объектов в рабочем дереве
не входят в расчёт. Это плановая метрика, не новое доказательство gameplay.

## Сравнение с вечерним срезом

База — `c2c2c91`, 2026-09-08 22:39:01. Между срезами прошло
8 часов 50 минут, добавлено 15 коммитов: 13 игровых/исследовательских
этапов и два коммита инструмента оценки/инструкций.

| Метрика | Вечером | Утром | Изменение |
| --- | ---: | ---: | ---: |
| Объединение документированных диапазонов исходного кода | 72 062 байта | 116 191 байт | +44 129 / +61,2% |
| Доля адресного пространства `.text` в объединении | 25,43% | 41,00% | +15,57 п.п. |
| Инструкции целиком внутри диапазонов | 17 874 | 28 552 | +10 678 |
| Условные переходы внутри диапазонов | 1 618 | 3 175 | +1 557 |
| Строки Swift в NTSDCore | 7 210 | 10 258 | +3 048 |
| Файлы NTSDCore | 59 | 82 | +23 |
| Строки NTSDReferenceChecks | 6 602 | 7 737 | +1 135 |
| Строки NTSDApp | 801 | 801 | 0 |

**41% — доля размеченного адресного пространства, а не готовность игры.**
Диапазоны могут содержать неподдержанные ветви, а реализованные правила могут
не иметь полных адресных аннотаций. Инструкции здесь посчитаны статически,
это не множество реально исполненных и сравненных ветвей.

Нативное ядро заметно выросло, но приложение продолжает использовать прежнюю
тренировочную основу. Отсутствие изменений NTSDApp согласуется с документами
об отсутствии интеграции; само по себе число строк этого не доказывает.

## Содержательный результат

За ночь собственное состояние оригинального запуска проведено через:

1. Повторные циклы меню с настоящими фазами ввода.
2. Выбор двух человеческих участников, готовность, VS-настройки/District и Start.
3. Пролог/подготовку/создание повтора до входа в первый игровой проход.
4. Общие распознаватели ввода и DAT-переходов, полную функцию управления Actor
   и окружающий проход World.
5. Физику Actor и World, включая восстановленные ветви деактивации,
   возрождения и превращений.
6. Границы глубины и работу с удерживаемыми объектами, сбор контактов,
   слияния, функцию разрешения попаданий и два прохода урона/предметов.
7. Cpoint-действия и размещение связанных объектов, затем камеру и фон.

Это уже основной игровой конвейер, а не только исследование главного меню.
Новые общие функции проверяются на контролируемых случаях; отдельно два
собственных запуска продолжаются через них на сохраняющемся состоянии.
Такое соединение не доказывает весь набор последовательностей исходных DAT.

Документы с доказательствами и границами:
[MATCH_LAUNCH](../research/MATCH_LAUNCH.md),
[ACTOR_CONTROL](../research/ACTOR_CONTROL.md),
[WORLD_PHYSICS](../research/WORLD_PHYSICS.md),
[WORLD_CONTACTS](../research/WORLD_CONTACTS.md),
[WORLD_HITS](../research/WORLD_HITS.md),
[WORLD_CPOINTS](../research/WORLD_CPOINTS.md),
[WORLD_CAMERA](../research/WORLD_CAMERA.md).

В последнем принятом исследовании камера прошла 4 742 контролируемых случая
и два собственных продолжения. Сохранённый отчёт фиксирует 29 успешных
регрессионных XCTest за 347,048 секунды. Эти игровые тесты в ходе оценки
повторно не запускались; текущая проверка воспроизводит только расчёт.

## Оставшаяся ближайшая работа

Собственная цепочка дошла до `41f496`, после полной камеры/фона. Дальше нужны
общая отрисовка Actor/World, остальные ветви режима, планирование кадров,
восстановление, создание/удаление и полный возврат игрового такта. Затем —
проверка непрерывных последовательностей и интеграция с приложением.

Целый матч, AI, все режимы и техники в реальном взаимодействии, Windows/device
output, файловые/сетевые границы и чистая macOS этим срезом не закрыты.
Рабочие незакоммиченные файлы показывают продолжение отрисовки, но не считаются
принятым результатом до фиксации и проверки соответствующего этапа.

## Обновление измерителя и сопоставимость

У старого списка SUPPLEMENTS не было новых исследований. Без его обновления
скрипт посчитал бы 93 057 байт / 32,84%. После просмотра положительных описаний
переноса добавлены 12 записей: Actor input/control/physics, character/match
selection, World control/physics/links/contacts/hits/cpoints/camera.

Новые записи не включают следующие ещё открытые drawing-функции из документа
камеры, будущие cpoint-вызовы из старого документа попаданий или неподключённые
тела выбора компьютерных персонажей. Как и в исходной версии, наличие нативного
модуля и соответствующего диапазона проверяется в каждом историческом срезе.
Куски с частичными доменами остаются адресными оболочками, не полной coverage.

Вся история пересчитана по новым правилам. База `c2c2c91` осталась 72 062 байта:
новые исследования появились только после неё. Для комментариев Swift без
ручных дополнений рост составляет 42 852 → 63 847 байт. Таким образом,
добавления в список измерителя раскрыли ещё 23 134 байта нынешнего среза,
но не увеличили старую базу и не подменяют игровое доказательство.

Также исправлена подпись сценария: длительность исторического окна теперь
вычисляется, а не всегда печатается как «25,9 часа».

## Скорость и обновлённая экстраполяция

За ночь прирост составил около 4,99 КБ/час. Средний темп от исследовательского
плана теперь 3,11 КБ/час за 34,73 часа. Последнее окно 5,94 часа даёт
6,31 КБ/час. В объёмной метрике перенос ускорился; это не доказательство
такого же ускорения на оставшихся сложных задачах или устройствах.

| Основа прогноза | Линейный остаток переноса |
| --- | ---: |
| Байты, вся история после плана | 54 часа |
| Инструкции, то же окно | 60 часов |
| Условные переходы, то же окно | 65 часов |
| Байты, только последние 5,94 часа | 26,5 часа |
| Только аннотации в Swift без дополнений исследований | 137 часов |

Автоматические сценарии с прежним **условным** запасом 50%:

| Сценарий | С запасом | При непрерывной работе |
| --- | ---: | ---: |
| Средний темп всей истории | 81 час | 3,4 суток |
| Темп последних шести часов | 40 часов | 1,7 суток |
| Половина темпа последних шести часов | 79 часов | 3,3 суток |

Механически выбранный вчера recent-rate сценарий изменился с 248 до 40 часов
после всего 8,84 часа работы. Это показывает **неустойчивость краткосрочной
экстраполяции**. Нельзя объявить готовую игру через двое суток на этом основании.

Более устойчивые сейчас три общие метрики дают 54–65 часов переноса или
81–98 часов с тем же предположением о запасе. Чувствительность к полноте
аннотаций остаётся большой: вариант только по Swift даёт 206 часов с запасом.
Это сопоставление сценариев, не доверительный интервал или обещанный срок.
Фактическую трудоёмкость AI, интеграции, полных режимов и Windows-проверок
по этой метрике по-прежнему нельзя надёжно ограничить сверху.

## Воспроизведение

[Полный JSON](2026-09-09-code-progress.json) включает 65 исторических срезов,
диапазоны, источники, скорости и сценарии. Старые отчёты сохранены.

```sh
python3 tools/estimate_port_progress.py --revision ec0d536 --output docs/estimates/2026-09-09-code-progress.json
```

Повторный запуск нового отчёта сверяется побайтно. Обновлённый сборщик также
воспроизводит исторический JSON `c2c2c91` побайтно в отдельный временный файл.
Проверки SHA оригинального EXE, байтов дизассемблирования и четырёх прежних
статических индексов выполняются самим скриптом. Пересечения адресов и
разбиение `.text` проверены отдельно.
