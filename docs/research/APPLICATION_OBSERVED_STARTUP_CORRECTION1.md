# Observed whole startup — correction1 passes all25 methods

Fresh build, full package verification and all25 selected comparisons passed.
Actual macOS clocks were acquired at the whole-caller request boundaries and retained
across late retry. Physical window/input/audio and full game acceptance remain open.

This candidate changes only the imports in the new observed-startup test: Foundation
is explicit and the actual NTSDReferenceChecks module is imported @testable. No
Core algorithm, test body/assertion, expected state/mask or selected method changed.
The failed build's candidate, log, partial products and archives remain immutable.
[Correction plan](APPLICATION_OBSERVED_STARTUP_CORRECTION1_PLAN.md),
[failed validation](APPLICATION_OBSERVED_STARTUP_VALIDATION.md),
[implementation](APPLICATION_OBSERVED_STARTUP.md).

Base HEAD1164fb8. Preparation97345 verified the original and failed2254-file inputs,
cloned a new regular candidate with distinct inodes, recorded the exact import-only
delta and retained all25 selected methods with their previous limits. The monitor,
exact result predicate and package checker were reused with only task identity
adaptation. The new candidate owns its fresh scratch/cache/build outputs on X5.

The consumer remains the complete WinMain/same-Host startup. One ordered typed
exchange suspends for window and16 other throwing response families, retaining
fulfilled responses/resources across retries. External clock acquisition runs only
at returned permits; prepared constants/files and aggregate sound/WAV remain declared
inputs. This removes the future-clock presampling dependency without pretending
those remaining boundaries are physical device implementations.

Original behavior remains anchored in the unchanged35-case WinMain evidence and
old auditing code.23 complete,5 original stops and7 unknown-provenance rejections
are distinct. New controls and21 retained methods keep their original full-caller,
rollback, request/cursor, resource lifetime and inspection requirements. Actual Mac
clock controls are separate from comparisons using original declared timestamps.
Their integer conversion does not modify the original recovered calendar code.

Independent contract/numeric review remains unavailable; author review and machine
comparisons are not independent review. Root1034 remains unpromoted; NTSDApp still
uses Practice. Physical window/resource identities, synchronous callbacks, actual
input/audio, nonthrowing constants/aggregate WAV, initialized loading, Windows/
clean-Mac/full match and complete game remain open. No original/emulator/capture/
refused operation is run. Source59727 remains terminal at34 Objects, not137;
existing incidents and transport/provenance dependencies remain open. EXE envelope
not recalculated. Full goal remains active.

## Fresh build, package and all25 comparisons

Build99190 exited0 in295.157seconds, sampled tree peak6,649,053,184bytes. Swift6.4
compiled Swift5/macOS14 release/native-SwiftPM with jobs2 and testability. Package
verification checked199Core/61ReferenceChecks/277test original sources, generated
accessors, fresh outputs and all1686 regular resource files385fixtures1301Core.
The65,066,472-byte test binary SHA256 is
`8ad7edc70e792294af5edfee60a0094b6356094a100ac5da48e9bb4242d2a3f6`.

Queue10821 exited0 in146.616seconds. All25 exact single-method processes passed;
full saved-output predicates and absence are checked again by closure. Four new
methods establish the declared observed-startup boundary:

- All35 original WinMain cases retain23 whole matches,5 original stops and7 distinct
  provenance rejections,6325events119WAV results.1841 once-served external replies
  comprise650 window and1191 nonwindow replies. The unchanged original audit sees
  Native stores/order/masks and produced owners; only production replies feed Core.
- Six late failures plus publication-copy failure retry on the same Host without
  reacquiring fulfilled responses. Preparation rollback, independently copied
  cursor positions and receipt-resource lifetime through retained delivery contexts
  pass. Native rollback still does not undo physical operations.
- All17 response families and272 cross-family rejections pass; a wrong type does
  not consume the valid permit. Owner/duplicate/cursor/argument-order/cancellation/
  unknown-outcome controls retain distinct boundaries.
- Integer clock epoch/floor/wrap/range checks and a whole-caller actual macOS clock
  control pass. The observed millisecond value710819298 precedes window service;
  FILETIME134349040322882730 follows all21 window responses. A late failure retries
  with those same two observations, with only two physical acquisitions. Surrounding
  host samples bound each reading. Other API/device replies remain explicit controls.
  This does not prove actual Windows clock resolution, origin or sleep behavior.

All21 prior methods passed on this corrected candidate. They retain the prepared
provider, Host inspection/concurrency and whole startup checks, plus all10 old
exchange/window/WinMain methods (including280 whole windows4574 requests). The
older82/28 broader runs remain separately dated evidence, not freshly rerun here.

No new Native comparison failure occurred. The prior test-import compilation failure
and both launcher argument errors remain preserved in the preceding frozen task.
The first correction finalizer draft had an author-identified local `result` binding
that would shadow the result-reader function. It was corrected to `applied` in a
separate unexecuted-before-launch finalizer2; both versions and pins remain. The
saved-result and terminal predicates are unchanged. No test reran for that fix.

NEXT: implement a concrete macOS window/resource request service on the now-validated
whole-startup exchange, consumed by the same Host, with Native-owned resource
identities/lifetimes and actual native-window checks. Freeze the finite AppKit/windowed
mapping and observable failure/callback boundaries before edits; use existing window
and Host evidence, not another response-queue or inventory-only study. Synchronous
callbacks, unknown fullscreen provenance, pixel/device/Windows equivalence and the
remaining nonwindow services stay explicit required dependencies; a displayed window
will not count as full startup/game acceptance.

## Verified closure and publication

Finalizer16329 terminal0/absent in71.803seconds at2026-09-26T13:50:58.529545Z.
Preparation/build/queue/all observed children are terminal and absent. Root1034,
prior2249, both original/failed2254 and current2254 candidates/source55 preserved;
only the declared test imports differ. Patch roundtrip verified every changed byte.
Candidate manifest SHA256 `431bcfd835882159c1b9e992e4aaa6d661d692cff5b50eab4ce65356eb4f5755`.

Full artifact archive: 4916regular files, 210directories,
no links, 11396837735logical bytes. Complete bodies/modes/nsmtimes/membership
and distinct clone inodes verified; manifest SHA256
`5dc8f67dc443912246096a3a2da21c08fc043efbf8894cc774810f3c8a039e9d`.
Metadata archive133members/6000640bytes, SHA256
`ff8bde7c5b0f01dbcf4b9c2af61dbda9caed3dabcc0b5a25d2701d6842a45311`. Task frozen. External free165747548160bytes,
internal58875383808bytes; observed decrease1173790720bytes,
original40/6GiB reserves plus17GiB source commitment retained.

Total child-test time79.252seconds, sampled peak
2067251200bytes; zero RSS for short methods means no sample, not
known zero use. No signal/guard/residual process. Independent review/root promotion
and actual Windows/device/game gates remain open.

[Publication](../evidence/application-observed-startup-correction1.json),
[closure](../evidence/application-observed-startup-correction1-close.json),
[import correction](../evidence/application-observed-startup-correction1.patch).
Detailed actual-clock/new-method records are archived in new-method-observations1.json.
