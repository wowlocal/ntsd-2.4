# Window exchange — Native comparison complete

All ten selected methods passed on the unchanged2244-file candidate. Fresh build,
exact package bytes, preservation and full archives are verified. Queue58993 and
finalizer62857 are terminal0 and absent; the task is frozen. Independent review,
root promotion, actual startup provider/Host integration and devices remain open.
The [plan](APPLICATION_WINDOW_EXCHANGE_VALIDATION_PLAN.md),
[publication](../evidence/application-window-exchange-validation.json) and
[closure](../evidence/application-window-exchange-validation-close.json) identify
the commands, limits, terminal records and archive manifests.

## Analysis and implementation

The [candidate](APPLICATION_WINDOW_EXCHANGE.md) lets the existing window/WinMain
consumer suspend at an unanswered request, then read the actual recorded reply
on a fresh pure cursor. Its external claim/permit boundary retains responses and
opaque resource owners without doing IO inside Core or inventing success. This
phase made no implementation change and did not promote root Native files.

Exact input manifest SHA256 is
`1e8d448c1c0889c0de25c7cfb8d486212aa4657fca0ee72ec725f02af3fa22f7`.
It includes the previously preserved dirty-baseline inputs. Only the original
Windows distribution supplies behavioral evidence; EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, with lib/CRT
pins in the plan. The existing controlled source responses and helper backings
remain explicit. No original/emulator/capture/auditor, refused operation, Windows
or host-device execution occurred. EXE envelope was not recalculated.

## Build, package and whole-caller comparison

Preparation45294 cloned2244 files with complete body/mode/nsmtime/membership and
distinct-inode checks, finishing in12.411s. Build46100 compiled fresh native-SwiftPM
release Swift5/macOS14 targets with two jobs and testability enabled; it completed
in289.979s. Sampled build process-tree peak was6,436,913,152 bytes. The package
checker verified193 Core,61 Reference and273 test sources plus generated accessors,
fresh outputs and all1686 resource files:385 fixtures and1301 Core files. The
linked XCTest binary has63,864,952 bytes, SHA256
`cd11f8f8f02dcf14613ccd33e6acf40a3f3356c04f3269df796b0bbba55c9c38`.

The same binary passed the five new methods and five retained methods:

- All280 whole-window cases compared complete requests/structures/masks, globals,
  writes, helper backings, returned values and resource output/release records.
  Every one of4574 requests suspended before its first fulfillment and was served
  once. Retried Native calculation consumed immutable replies rather than IO.
- All35 WinMain parents ran the existing complete event/store/owner checks through
  copied provider cursors. They retained23 whole successes,5 distinct original
  stops and7 unknown-provenance Native rejections, including fullscreen cursor
  provenance. The final paths compared6325 events and119 whole WAV results;650
  window replies were fulfilled. These overlap the window corpus and are not
  additional unique branches or actual Windows observations.
- All six late startup failure sites preserved the pre-attempt state, then
  completed whole startup with the same answered window journal and no re-service.
- Protocol controls rejected wrong requests, owners, revisions, duplicate/stale
  permits, competing claims and incomplete completion. String, unknown-byte and
  mask changes rejected without consuming a reply. Cancellation, late completion
  and indeterminate outcome remained distinct.
- Resource controls retained owners through snapshots/cursors, completion,
  cancellation, indeterminate failure and exchange destruction, releasing them
  after the last retained Native owner was dropped.
- The three original whole-window methods and both original WinMain methods
  passed unchanged, including the old missing-backing/output and late rollback
  controls. No old expectation or test method body was changed for this result.

Queue duration was66.584s; sum of test-process durations39.301s. Each method ran
as one complete named XCTest process. All logs have the exact selected method,
one passed result, three one-test/zero-failure summaries and Selected-tests pass;
there is no guard, signal or residual observed study process. The saved-result
predicate is unchanged from the verified earlier reader. Sampled test-tree peak
was2,068,807,680 bytes. Recorded0 for short processes means no RSS sample, not zero
memory. The prior82-method baseline is preserved separately and was not rerun or
counted as a current-candidate comparison.

## Preservation, acceptance boundary and next consumer

Finalizer62857 completed in59.869s. It verified root1034, prior2242, both copies
of candidate2244 and55 source pins. The complete regular APFS archive contains
4896 files/210 directories, no links and11,379,890,744 logical bytes; every body,
mode, nanosecond mtime and directory membership was checked, with distinct clone
inodes. Its manifest SHA256 is
`946a70ff40e224fcaa8a9befa8e6fcbad998dd476faa8247dcb733b0cfc92956`.
The67-member PAX metadata archive is5,120,000 bytes, SHA256
`70138b9c24b05d766b8be0261e6c2c920d2c54b3a8998c1965bdff0ff9d17549`, fully checked
by names/bodies/modes/nsmtimes. Closure records171,268,792,320 external and
58,915,192,832 internal free bytes; observed external decrease1,155,923,968 bytes
is below28GiB and is not a per-task allocation measurement. Original40/6GiB
reserves and17GiB source commitment remain intact.

The next concrete consumer is startup on the **same actual Host**. In this pinned
candidate, `OriginalApplicationHostSession.start` (lines161–179) has no preparation
callback: it retries from its private committed platform. Unlike `step`, it cannot
install a newly answered immutable cursor onto the staged startup platform. A
shared mutable exchange read from `stagedCopy` would violate the existing contract.
This is a Native integration gap found by source inspection, not a newly inferred
Windows rule or a reason to replace the Host after every request.

NEXT: freeze and implement finite Host startup preparation and a window-response
coordinator. Install each fresh snapshot only on an independent attempted platform,
retain the same Host/initial state/nonwindow observations across suspension, and
publish one startup batch only after the actual caller and consumption checks
succeed. Late preparation/publication-copy failures, cancellation, stale replies
and reentry must preserve prior receipts and Host state. Include whole WinMain
outcomes and the relevant existing Host startup/ownership regressions; do not
weaken the no-IO transaction rule or claim rollback of physical work.

Production nonwindow observations, physical resource mapping/callbacks, initialized
loading messages/fills, audio aggregation, independent review, root promotion,
full137 source/transport, actual input/window/audio/Windows/clean-Mac, complete
Naruto/Sasuke District match and full game remain open. Source59727 remains terminal
at its earlier34-Object publication cap. Existing safety incidents are unchanged;
this independent Native result does not resolve the affected operations.
