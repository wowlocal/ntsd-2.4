# Prepared startup platform — all eleven comparisons passed

Fresh compilation, package verification and all eleven selected methods passed on
the unchanged2249-file candidate. The production Core platform supplies the whole
startup caller with bundled original inputs and explicit prepared observations,
independently copied positions/output/cursors and retained owners. It no longer
requires an XCTest provider implementation. Physical observation acquisition and
the application backend remain missing; NTSDApp still uses Practice.

The [plan](APPLICATION_PREPARED_STARTUP_PLATFORM_VALIDATION_PLAN.md),
[implementation](APPLICATION_PREPARED_STARTUP_PLATFORM.md),
[publication](../evidence/application-prepared-startup-platform-validation.json) and
[closure](../evidence/application-prepared-startup-platform-validation-close.json)
retain separate implementation/build/package/comparison/archive/review gates.
Base HEAD is7f882b8; actual accumulated dirty input provenance is preserved.
Candidate manifest SHA256 is
`295864416a72f76e19471ece43331fc40d4a2a2ec8b6d66dbe69251c7c977eb6`.

## Analysis and runtime implementation

The Core class consumes typed prepared responses and actual original package bytes.
No reference library, expected state, fixture decoder or host IO is used by it.
Request mismatches and missing entries reject before changing their positions or
attempted panel output. The existing window cursor owns suspension; opaque resource
owners remain alive through staged copies and delayed Host delivery. Existing
callers still compute every game state, calendar result, bitmap record and PCM copy.

The value preparation does not observe a physical device. Its allocation identities
and raw backing are explicit reservations/inputs, not private source ABI. Sound/WAV
controls retain their existing aggregate boundary. Helper/format music notifications
have ignored results; terminal music requests remain bound to explicit replies.
No shared algorithm, old test, expected byte or mask was changed in this validation.
Author review remains separate from unavailable independent contract review.

## Fresh build and package

Preparation21268 completed in13.294 seconds and verified2249 regular candidate clones
by bytes, mode, nanosecond mtime, membership and distinct inodes. Its terminal summary
incorrectly retained the inherited scalar `preparedMethods:8`; the frozen selection,
method limits and runner controls each independently establish11. The separate
`preparation-count-audit1.json` preserves that reporting error and the exact pins.
No preparation or comparison was rerun to change the label. The actual queue below
selected and ran all11 methods. This metadata defect is not a Native mismatch.

Build22818 completed with exit0 in319.925 seconds; sampled tree peak6,508,265,472
bytes. Swift6.4 compiled in Swift5 language mode for macOS14, native-SwiftPM release,
two jobs with testability. Package checks verified195Core/61Reference/276test sources
plus generated accessors and fresh outputs, and all1686 resource files by complete
bytes and membership:385 fixtures and1301 Core files. The test binary has64,665,848
bytes, SHA256
`5f525dbc99e1a3fcbc28dca01b1f9c8bb519cc145e951050a93be98a01bde610`.

## Actual whole comparisons

All three new complete methods passed:

- The production platform runs all35 original WinMain parents through the same
  coordinator and Host. Source stores/order/masks, owned calendar/music/panel/WAV
  results, full Application state and ordered operations/graphics compare. Results
  retain23 successful whole chains,5 original stops and7 provenance rejections,
  6325 events,119 WAV results and650 once-served window replies. These are not35
  matches. A test-only audit sees actual arguments/observations; only the production
  platform's values feed Core. WAV bytes come from the original input package.
- Six late failures, a publication-copy failure and preparation failure preserve
  the committed Host and preparation, then retry on that same owner without serving
  window replies again. Independent copies retain positions/output correctly.
  Resource owners survive coordinator/Host destruction through delivery context
  and release after the last owner in the separate release control disappears.
- All16 response families preserve declared results and independent positions;
  missing/duplicate entries,13 mismatched request bindings and unconsumed preparation
  reject under the declared controls. File absence, empty input and unsupported names
  remain distinct. Missing window cursor and ignored music notifications preserve
  prepared positions. These synthetic controls do not prove natural reachability.

All eight unchanged inspection/startup methods also passed, including32 whole key
transactions with concurrent readers, post-handoff copying, cancelled/indeterminate
private inspection, whole startup, late failures, permit/cursor checks and lifetimes.
The earlier28/82 comparisons remain separate evidence, not freshly rerun here.

Queue34416 completed with exit0 in59.612 seconds. Each of the11 saved child results
has its exact single passed method, three one-test/zero-failure summaries and
Selected-tests pass, with no guard, signal or residual process. Sum of child times
is26.128 seconds; sampled maximum455,294,976 bytes. Test03's recorded RSS0 means
no sample for that short process, not known zero memory use. No failed Native method,
automatic retry, changed comparator or changed expectation occurred.

## Preservation and archives

Finalizer36848 completed with exit0 in66.050 seconds at
2026-09-26T13:06:32.685912Z and is absent. Preparation/build/queue/observed children
are terminal and absent. It reread exact saved results and verified root1034,
prior2247, both current2249-file candidates,55 source pins, package membership and
fresh outputs. The monitor/reap body and exact saved-result predicate remain unchanged.

The full regular APFS artifact archive has4906 files,210 directories,no links and
11,392,003,407 logical bytes. All bodies/modes/nsmtimes/membership and distinct clone
inodes were checked; manifest SHA256 is
`ebd7cce3e5d3db3b6eabb884e0b4351fe4b9da7ff6dd942c4e0d881d8b41940f`.
The71-member PAX metadata archive has5,273,600 bytes, SHA256
`61c67f1524b94e6c4524bb7e043075f1c96f6f41b4ac0e5f50ed2dc7a40de7e0`;
all names, bodies, modes and nanosecond mtimes were verified. Task is frozen at
`build/research/application-prepared-startup-platform-validation-20260926/`.

At closure external free space was167,760,953,344 bytes, internal58,847,797,248;
observed decrease1,168,773,120 bytes. Original40/6GiB reserves plus17GiB source
commitment remain;28GiB physical/26GiB stop/150GiB logical bounds were respected.
T7 remains authorized separately; APFS-dependent work stayed on X5.

## Next actual observation boundary

Connect external observation acquisition to this platform and whole startup at
the actual request boundaries. A concrete timing dependency remains: WinMain reads
milliseconds before window initialization and FILETIME after it. Precomputing both
before a potentially delayed window exchange is not evidence of the latter sample.
The current preparation is immutable and missing nonwindow inputs throw; these
errors are not yet resumable typed requests serviced by a production backend.

The next implementation must preserve actual request payloads, retain fulfilled
nonwindow observations/resources across retries, and acquire later observations
only when the recovered caller requests them outside its Core transaction. Reuse
the existing exchange/Host architecture; acceptance must include the complete caller
and late rollback, plus real macOS observations where that backend is implemented.
An isolated queue, inventory or preinvented clock/device-success tape is insufficient.
No new capture is required merely to restate the already recovered call order.

Independent review/root promotion, physical window/resource mapping, synchronous
callbacks, actual input/audio, initialized loading messages/fills, Windows/clean-Mac,
full match and complete game remain open. Source59727 stays terminal at its34-Object
publication boundary; full137 source/transport/provenance and existing incidents
remain open. No original/emulator/capture/affected refused operation/Windows/device
executed here. EXE envelope was not recalculated. Full goal remains active.
