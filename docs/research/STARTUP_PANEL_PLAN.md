# Whole WinMain startup panel selection

Recover actual43cf94..43cfb4: whole information reader43c4a0, conditional content
43c780, conditional bitmap43cc60 with actual constructor/destructor, and default
writer43c690 selected after any ordinary failure. The pinned NTSD EXE/VC80 run
on one Unicorn2.1.4 CPU/stack. Previous standalone corpora stay immutable.

The goal is the actual ordering, retained globals, bitmap ownership, and shared
local provenance needed for native startup. Earlier window/CRT/WinMain state,
translated files, allocator/DIB/COM and user-buffered output FILE are declared
controlled inputs. No original resource/file is modified; no network/Windows
request or memory corruption stimulus is part of this study. All13 bundled DLL
patches must be checked for disjointness with complete studied bodies.

Finite acceptance:

1. Execute the pinned adinfo with the genuinely absent ad0 content, full controlled
   content with live/missing/null/key-failure bitmap, rejected info/date/end and
   ordinary file/read/write/close failures. Exercise each parent selection, both
   local backing controls and own repeated bitmap replacement/lifetime. Successful
   ad-path device responses bind explicitly to the original MENU_BACK1 DIB.
2. Keep every child on the same original CPU/stack through actual returns and
   actual43cfb4 stop. Track full globals/write masks, all live/dead bitmap bytes/
   masks, writer FILE/buffer, ordered child requests and actual instruction starts.
   Do not call this a full WinMain return, Windows/device or app execution.
3. Track shared locals: content's1104 bytes overlap the preceding reader's184
   bytes at offset398hex. Native carries its own produced reader bytes/masks into
   that overlap; other private CRT stack backing remains unknown. Add an explicit
   strict-provenance option to the shared content parser while retaining its old
   declared-backing contract by default. Unknown reads are native rejections with
   rollback, not false content returns or successful whole matches. Preserve any
   source stop before an unterminated scan separately; never force a memory fault.
4. Compare native composition with shared helpers and own state, never previous
   expected after-state. Verify late failures after bitmap replacement and default
   output roll back all staged globals/ownership/output/locals; buffer external
   effects until the whole caller commits.
5. Use an isolated committed native export excluding the six foreign transform
   files. Run raw comparisons plus retained info/content/panel/writer and previous
   panel-parent tests. Publish only after acceptance; independently verify every
   raw/packed byte/JSON/SHA/blob, all230 old fixtures and10 vendor pins, then run
   packaged tests and confirm all owned processes are terminal.

Ordinary partial files can expose private prior stack bytes. Preserve their
provenance boundary and source behavior instead of importing a source snapshot.
Full earlier startup/CRT/NLS, library transforms, date/time/music/input, WinMain/
dispatcher/application, Windows/device/latency/audio, complete matches/content and
clean-Mac acceptance remain open beyond this controlled composition.

Finite acceptance is now complete in [STARTUP_PANEL](STARTUP_PANEL.md):212 whole
callers and8 explicit unknown-local rejections across220 source executions.
All13 raw/packaged tests pass; the full WinMain/application/Windows goal is open.
