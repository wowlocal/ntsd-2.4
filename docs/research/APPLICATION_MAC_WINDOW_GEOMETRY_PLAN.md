# Native window geometry through whole WM_MOVE and first-menu presentation

2026-09-26, HEADa06324c. Previous turn was progress: observed bitmap bridge all69
methods/package/full archives passed. This removes a concrete remaining input
for front presentation on the path to the Naruto/Sasuke District match and full
native game. It does not equate a correct rectangle with rendered-frame acceptance.

Reference: original EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c;
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba.
Use saved WINDOW_LIFECYCLE(318 callbacks/2 initialization returns), WINDOW_INPUT,
WINDOW_INITIALIZATION, APPLICATION_MESSAGE_LOOP/MENU_SESSION/BOOTSTRAP,
APPLICATION_GRAPHICS_OWNERS and current observed-bitmap studies. Archive1203–1234,
1282–1322 binds original numeric/ownership/geometry rules. No original/emulator/
Windows execution or historical producer/auditor/refused operation is requested.
Independent reviewer unavailable; author analysis is not independent review.

## Contract and bounded implementation

Whole43b3d0 WM_MOVE ignores lParam coordinates. Windowed calls GetClientRect then
ClientToScreen(first corner), then ClientToScreen(second corner), observing each
prior output, then DefWindowProc. Numeric failures do not skip later calls; absent
output does not become zero. Native retains actual output bytes at0x453ccc.
The current MenuSession dispatches only WM_SIZE to OriginalWindowLifecycle;
WM_MOVE takes WindowInput's generic path. Add message3 to the existing lifecycle
route, leaving all old input/resize rules unchanged. The existing scalar store
observer accepts1/2/4 bytes; project lifecycle8/16-byte API outputs into ordered
4-byte observations while retaining each whole response in its lifecycle effect.
Check this projection independently from the supplied output bytes. The move branch needs no
new helper-entry backing. Other lifecycle modes/recreation remain separate.

Add a lifecycle callback seam to Bootstrap/Host, keeping the prepared-array path
and rejecting mixed arrays/callback. Reuse the existing RequestExchange, Host
expected-sequence lock and final-publication hook. A typed lifecycle delivery
cursor/history belongs to each staged PreparedStartupPlatform; a whole-iteration
coordinator services requests only after Core unwinds. Retain prior nonempty
histories and resource owners, including through later bitmap/empty iterations.
Empty attempts do not grow history. No expected after-state/private ABI is input.

Extend the existing Mac window backend for clientRect and screenPoint. Preparation
validates owned HWND/payload without AppKit observations; service begins before
actual view/window/screen reads. Client bounds produce0/0/width/height; point data
uses both signed32 fields and the current flipped view→window→screen transforms.
Return actual16/8 bytes only after a successful Native observation. Missing/dead
owners, malformed/unknown point fields and unsupported geometry remain explicit
Native boundaries, not fabricated Win32 errors. An operation failing after service
begins retains its indeterminate outcome/resources under the existing exchange.
A geometry service handles only those two requests; DefWindowProc and all other
API replies remain explicit controls in the new comparison, not actual Windows.

Inherit the established host policy: one game unit is one AppKit point; desktop
origin is top-left of the window's current screen, shared with the display owner.
Finite signed32 integral coordinates only, with no rounding of fractional or
out-of-range results. Existing raster already rejects fractional geometry or a
changed screen. Multi-screen migration, arbitrary transforms/DPI and those geometry
extensions stay open; these are Native mapping boundaries, not original game rules.
Original zero destination rectangles and NULL source rectangles remain distinct.

Platform API vocabulary (not another game reference):
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getclientrect
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-clienttoscreen
- https://developer.apple.com/documentation/appkit/nswindow/convertpoint(toscreen:)
Pin selected SDK NSWindow/NSView declarations. Apple web Markdown fetch returned
unsupported-content-type400; local SDK and official search description are usable.
This is a read failure, not a safety refusal or Windows observation.

## Finite cases and acceptance

Four new methods, plus all69 prior and3 unchanged WindowLifecycle methods:76.
1. Actual owned startup/resize→controlled WM_MOVE→physical425 bitmap replies→whole
   first menu. Compare full Core state against a separate prepared replay, exact
   geometry request/reply/batch order and every presentation rectangle. Independent
   geometry expectation uses actual window frame→contentRect, distinct from the
   provider's point conversions. Other API/queue values are explicit controls.
2. Late move-publication failure, physical window relocation before retry, saved
   replies without resampling; a later move gets fresh coordinates. Stale Host,
   reentry, context copies, nonempty/empty histories and retained marker lifetimes.
3. Physical point/client outputs and signed coordinates; strict argument/mask/
   closed/foreign/duplicate/cancelled guards. Pure numeric conversion boundaries
   cover finite integer limits, fractions, infinities and NaN without huge windows.
4. Controlled zero/negative geometry replies with/without writes preserve original
   ignored-result ordering, unknown/retained bytes, whole move state and late rollback.
All old source expected/masks/assertions/comparators/fixtures stay immutable.
No automatic AppKit-to-original event delivery, synchronous callbacks or general
front raster is claimed. Tests move only their own Native-created windows.

Baseline2268-file manifest SHA256
 a3cdcc93f350578cfdd744f9accb1eaac2f5e6f7a18c415b57fb26beb009dda2.
Clone actual candidate, not HEAD-only root. Modify4Core/1Mac; add2Core/1Mac/1test:
2272 files,203Core/61Reference/8MacPlatform/283tests,1686 unchanged resources.
Protect root1034/prior2257/baseline2268/source55, old failures, plans and archives.
Task application-mac-window-geometry-20260926 under X5 parent
01a0dc49-738f-7972-8fb0-e98fb2f34408. Verify writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,90GiB external/9GiB internal preflight;
40/6GiB reserves plus17GiB source commitment. Physical28GiB, stop26GiB, logical150GiB,
metadata16MiB/root2MiB. Prep1800s, build3600s/RSS12GiB, queue14400s, finalization3600s.
Inherit all69 method bounds unchanged; new and3 lifecycle methods600s/8GiB.
Stop at first nonpass; at most3 correction rounds before diagnosis. Reuse existing
clone/monitor/queue/package/archive tools and exact saved-result predicates.

Separate gates: syntax, fresh build, package bytes, all76 results, full protected
inputs/patch/regular/PAX archives, independent review. Commit checked increments.
Root promotion, general Blt/text/color-key/format, other IO/diagnostic UI/loading,
actual input/audio/Windows/clean-Mac/full match/full game remain open. Source59727
stays terminal34 Objects, not137. NTSDApp remains Practice; EXE envelope not recalculated.
