# Owned neutral gameplay scalar projection

Status: accepted finite scalar checkpoint; full gameplay comparison remains open. This commit-sized continuation finishes the
pending test arithmetic and saved output-store reader. It is a bounded subset of
[the full gameplay comparison plan](APPLICATION_LOADED_GAMEPLAY_COMPARISON_PLAN.md);
that plan, graphics/audio effects and whole gameplay acceptance remain open.

Reference EXE `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`
and DLL `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`:
read the 34 immutable first-body/continuous/initialized-bridge fixtures already
verified by [the reader](APPLICATION_GAMEPLAY_SOURCE_READER.md). These are saved
Unicorn observations with synthetic API responses, not Windows/device results.
No original execution, historical producer/auditor or refused operation is run.

## Declared comparison

Two variants, each 17 neutral calls and all 19 scalar stage endpoints. Compute
expected full normalized World/400 Actor records, globals and 101 BG records,
including unknown bytes and masks. First replay each formula on the saved source
before-state and require the complete saved after-state; then compute the own
result from actual Bootstrap/selection/Start/next-input state. Expected records
never enter Core. Carry state between stages and calls rather than resetting it
from observed after-state. Own X/Z, camera, RNG, resource IDs and backing remain own.

Typed arithmetic includes idempotent writes, intermediate binary64 stores,
contact copies, hit RNG, depth conversion twice, animation/MP timing, background
counters, command resets and queued sound weights. Unique observed PCs support
instruction identity, not a full ordered dynamic Actor write trace. Input/output
use the saved ordered global-store footprint and calculate values independently;
this is a trace-guided finite projection, not an independent control-flow oracle.
Full graphics command generation and audio effects are excluded, with no claim
that scalar equality proves them. Queue scalars alone do not establish playback.

Read-only Object/Frame semantics come from the actual loaded catalog whose
construction and bytes are accepted by the parent chain. Check full own Object/
Frame retention, bitmap records/owners, CRT and replay allocation at every stage,
plus actual World/Actor aliases. No changed game rule or expected byte is planned.
The enclosing Bootstrap return remains exercised by the accepted caller helper.
This subset adds no new rollback claim; earlier preflight/provider rollback
results are inherited only through unchanged implementation/tests.

## Execution and preservation

Root owns edits; `war_contract_review` reviews independently. Mutable paths are
the four gameplay reader/projection test files, this study/evidence and navigation.
Freeze the exact full Native tree with hashes/modes in
`build/research/application-owned-gameplay-comparison-native-20260913/candidateN`.
Task area is on X5 UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`.
Keep 40 GiB free there and 6 GiB internally; at most 40 GiB task data,
3600 seconds per Native run, three initial candidates before diagnosis.

Run release `OriginalApplicationGameplaySourceTests` (two changed dependency
methods) and `OriginalApplicationGameplayProjectionTests` (two new methods),
using an isolated scratch path and no NTSD raw overrides. Preserve each terminal
failure and exact candidate. Retain the preceding 141 methods through exact
unchanged dependency files; do not count them as newly run. Verify complete
package/fixture/resource bytes, candidate archive bodies/modes, staged Git and
review before committing. Actual commands and results belong in task job and
verification records. Full game, match, rendering, device/Windows/clean-Mac and
existing safety incidents remain open.

## Measured result

Four release methods passed in 61.951 seconds; build 343.98 seconds. Both variants completed 17 owned Bootstrap returns and 323 source-first scalar stages each. All available source Frame-heap endpoints retain their full records; absent middle snapshots remain unobserved. The own mutable Frame heap is checked at every stage. Eight negative comparator-domain trials cover inactive cooldown, hit count, unknown operand masks and aliases. These are comparator guard checks, not game rollback tests.

Candidate1 compiled but rejected both source sequences on an erroneous unrelated `44d02c==0` predicate. The saved value is1 and the recording branch reads `450bdc`, not that option; candidate2 removes that mistaken predicate without changing Core, expected bytes or the exact timer0 boundary. All prior candidate/logs and reviewer corrections remain preserved.

983 prior Native files are unchanged. All 383 fixtures, 102 resources and 485 package files match; all 987 final candidate archive bodies/modes are verified. The 141 inherited methods were not rerun. See [evidence](../evidence/application-gameplay-scalar-projection.json) for measured results, hashes and archive boundaries. Full graphics/audio/caller-journal acceptance remains the next part of the existing plan.
