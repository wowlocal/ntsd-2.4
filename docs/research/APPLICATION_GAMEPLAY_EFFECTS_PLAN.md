# Finish owned gameplay effects comparison

Parent `5fa4aed` verifies full scalar records at 19 stages × 17 calls × two
variants. The previous goal turn was progress: tests, review, package/archive/Git
checks and delivery completed. The full goal remains the standalone native game;
this continuation finishes the open effects/ownership/caller part of
[the existing plan](APPLICATION_LOADED_GAMEPLAY_COMPARISON_PLAN.md).

Read the same 34 pinned saved Unicorn observations, original NTSD EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c` and DLL
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d` contracts.
No original/emulator/historical producer/auditor or refused operation executes.
The new operations are saved-data reads and Native comparisons. Synthetic API
responses and source surfaces remain distinct from real Windows/device output.

Compute source-first complete front streams from independent input records:
background parallax/counters, own Actor Z ordering/blink/sheets/names, eight HUD
cells, bitmap reads/masks/clipping/direct rectangles, diagnostic text, four-pass
mode label, own recording filename, actual presentation mode and queued sound.
Expected events never enter Core. Preserve negative-frame double draws and
unknown allocator bytes; no translation of already clipped source events.

Then compare the complete own streams, chronological operation journal, current
graphics owners/DC generations/source-color associations and all non-mutated
owners, scalar state, full memory aliases and whole Bootstrap return. Keep the
scalar comparisons and add late failure trials after generated drawing/text/
sound effects and the enclosing commit. Validate no partial effects escape.
Missing source observations remain explicit. Device pixels, actual audio,
Windows and clean-Mac acceptance are not inferred from command comparison.

Root owns the two new test projection helpers and integration-test changes,
necessary joins if evidence requires them, this plan/study/evidence/navigation.
`war_contract_review` independently reviews output/graphics/audio/journal recipes
and final comparisons. Old expected/fixtures/resources/producers/plans stay
immutable. No game rule changes are planned merely to make a candidate pass.

New task area: `build/research/application-owned-gameplay-effects-native-20260913`
on X5 UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`; baseline pins all 987 Native
files from the accepted scalar candidate. Preserve 40 GiB X5/6 GiB internal
free, at most 40 GiB new task data, 3600 seconds per Native run, three initial
candidate rounds before diagnosis. Freeze exact full candidates/commands before
execution. Revalidate handles; no live/completed job restart for silence.

Run both extended `OriginalApplicationGameplayProjectionTests` methods, plus
appropriate changed-dependency/graphics/rollback checks. Retain earlier successful
methods only through exact unchanged code/test/fixture pins; do not rerun for
counts. Final publication requires full package bytes, candidate/evidence archives,
independent review and staged Git/delivery checks. Do not call the full comparison
accepted until every declared required effect, owner and caller gate passes.
