# Installed-library mode screen and retained text DC

Recover complete431d10 mode-screen behavior with the bundled library's actual
401290 replacement. This is the next output dependency after the continuous
menu music/resource prefix, needed before the native application can present
its real mode screen. Keep the existing pristine1264-case MODE_SCREEN corpus
unchanged. This study does not relabel those parents as installed-library runs
or replace the still-running full own catalog/loading continuation.

Reference: pinned NTSD EXE and bundled lib.dll; Unicorn2.1.4. Execute the actual
relocated DLL installer using the existing declared loader/VirtualAlloc/
VirtualProtect/RtlMoveMemory boundaries, then the actual431d10 prologue, body,
bitmap/clip/fill/input/sound/release/key-name/text children and normal ret16.
The full9-Actor controlled input domain is the retained MODE_SELECTION contract:
eight World references can address nine declared records, with no Object access.
Five declared bitmap records provide deterministic rectangles and opaque surface
tokens; they are controlled resource inputs, not original image pixels or an
own loaded catalog. An already present background is an explicit entry input.
No constructor result, expected after-state or private storage is injected.

Instruction, register, full stack/global/resource/library byte and read/write
mask observations establish ordering, string production and retained DC state.
Normal guards/cookie checks remain. COM/GDI, lstrlenA, critical-section,
Sleep/ShellExecute, free and PostQuit requests are recorded controlled adapters:
no host URL, process quit, device, external message or Windows operation occurs.
Initial stack backing and ABI arguments are declared separately; unknown bytes
remain unknown. Native creates its own literal/decoded/key-name strings. It
must never import source local backing to make a library text call agree.

Finite controls: eight modes with five signed network bytes; five GetDC results
with/without28 key labels; all256 key codes and four out-of-range values grouped
into28-label screens; three links with four held/previous combinations; four
selected modes with eight up/down/attack combinations; six consecutive retained
DC results on the same source/library state; explicit enabled-panel and worker
entry boundaries. These cover the changed caller/helper composition, not every
mode-screen branch or all application inputs. Preserve playback partial returns
before43249c separately. Enabled panel/worker dependencies stop at their actual
child boundary and remain explicit native rollback rejections in this study.
Do not skip them and report a convenient full return. Other already-studied
panel/file/background helpers retain their separate contracts.

First capture one immutable nominal probe. Preserve every completed case as an
atomic part before the finite capture continues. Unexpected errors retain the
exact error, time, source state, pending calls and raw bytes; stop that operation
without automatic retries or fault continuation. No intentional control-pointer
or protection-structure corruption, exploit, safeguard bypass or unrelated target.

Native shares one mode-screen implementation between pristine and library text
routes. State globals, bitmap ownership, local bytes and retained library DC
commit after the whole selected continuation; caller effects must be buffered.
Expose the screen's read-only World/Actor inputs without requiring a synthetic
loaded catalog. Check full controlled states, masks, liveness, requests and
normal return ABI, plus late failures after text, after sound/background release,
a late key label and final cursor draw. Retain both full pristine mode-screen,
mode-selection and installed text tests with no expected edits.

Build accepted47f31fd plus explicitly owned files in isolation. Preserve249
fixtures,14 pending catalog native files,3 source files,6 foreign files and all
frozen watcher inputs. Full own loading/menu return, app/window/audio/latency,
Windows, full match/all-content/network/clean-Mac goal remain open.

The first finite capture retained110 complete cases, then stopped at an actual
unmapped read423b11: the producer had incorrectly treated458420 as a boolean
instead of a header pointer. Preserve that exact error and original producer;
never call it a native match or continue from the fault. The remaining bounded
probe declares a valid4-byte panel header at26006200 with value1, binds458420
to that owned input and stops before423b1a. The worker probe remains separate.
No arbitrary damaged-pointer domain is added. All110 complete parts stay intact.
