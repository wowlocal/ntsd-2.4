# Own early screen body through installed library text

Forty-three fresh own application chains now execute actual427127, whole4236d0
and42712c..4275cb. All reach4275cb/SP1000ea74 through the bundled library's text
replacement and whole bitmap drawing/clipping. Native independently rebuilds the
startup/message-loop/dispatcher/resources/settings/background parents, retains
its own target and strings/DC, and compares the complete declared output order.
The encompassing World and application dispatcher have not returned.

The [finite plan](APPLICATION_SCREEN_BODY_PLAN.md) continues
[APPLICATION_FRONT_SCREEN](APPLICATION_FRONT_SCREEN.md). All39 critical parents
reproduce their entire accepted records exactly, including their18 unique settings
and seven bitmap parents. Four additional default-parent controls cover negative
and positive GetDC, failed later GDI/ReleaseDC, and failed Blt. The previous
setting-1 alternate and NULL-background stop remain separate immutable contracts.

## Reference and environment

Pinned NTSD EXE SHA256:
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
Bundled6144-byte lib.dll SHA256:
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
VC80 SHA256:
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Original control.txt and36 whole PE DIBs remain unchanged. Unicorn2.1.4 executes
original instructions; COM/GDI and critical-section responses are declared API
inputs. No Windows device, message delivery, URL or sound operation occurs.

Before the independently entered WinMain chain, the research loader relocates
the original DLL to36000000 and executes its actual process-attach installer.
All76 HIGHLOW relocations,104 original installer PCs and13 copies/62 patch bytes
reproduce the accepted LIB_LOADING installer exactly. The two declared allocated
buffers retain all4000/20000 zero bytes. Full20480-byte DLL backing reconstructs
from the immutable image, relocation/import inputs and installer effects.
This is a declared loader connection, not a completed PE/CRT/NLS startup.

No registers, game globals, caller stack, old resource allocations or CRT/PTD are
reset between the own parent and the new body. The complete library remains
unchanged through the parent; only the newly reached text helper changes its DC.
The research COM vtable acquires declared GetDC/ReleaseDC methods for the existing
own target. No target or surface object is replaced by an expected snapshot.
Normal original control flow runs; no control/protective storage corruption,
fault continuation or protection bypass is performed.

## Panel, text and resource ownership

The whole4236d0 updater requests EnterCriticalSection4554a4, reads own458424=0,
and requests Leave before returning to42712c. Worker bodies are not delivered
in these controlled chains, including parents that requested a thread. This does
not establish status2 composition or concurrent delivery. The earlier complete
[MENU_PANEL_UPDATE](MENU_PANEL_UPDATE.md) controlled corpus remains separately
verified and immutable; its enabled content/bitmap/writer branches are not
attributed to this own first-screen continuation.

The body copies three original literals, decodes them in place, and requests
three text passes at591/491,591/511,591/531. The installed401290 jump executes
whole36001298..36001309 and its four import thunks. Successful GetDC retains
12345678 in DLL+306e, requests SetBkMode(DC,1), SetTextColor, lstrlenA, TextOutA,
then ReleaseDC. No SetBkColor occurs. Negative GetDC skips these later operations
and preserves the prior zero DC, even though the API output still writes the
supplied handle into the caller argument slot. Positive HRESULT1 follows the
normal path. Later negative numeric results do not stop the screen.

After the text, the body requests Enter/Leave again around the live worker status,
then draws the settings button and panel through whole43f010/43ef70. Both drawings
use Native's independently retained GameEntry.target and original loaded bitmap
metadata. The settings1 parent selects frame8; other own critical parents select
frame6. No mouse/held/worker globals are injected to manufacture a branch.
The original controlled [FRONT_SCREEN_BODY](FRONT_SCREEN_BODY.md) matrices retain
their separate hover/click/button and invalid-target contracts.

Native's surface registry now retains each actual successful CreateSurface API
output during its own earlier constructor. A failed creation or color-key cleanup
produces zero according to the final native wrapper. Drawing receives this owned
binding, not a surface word copied out of the expected bitmap. All1074 retained
wrapper records,8609184 bytes plus masks, remain unchanged. All602 bitmap reads
are defined in this stage; the previous background's undefined reads keep their
own earlier evidence. Failed Blt still allows the subsequent operation.

## Caller-local and continuation provenance

Every own target audit has one producer:4246fd writes rootSP+20 before the
resource/settings/background chain. No intervening source instruction rewrites
that word. Two body reads at4274da and4275c1 use it. Native supplies its own
GameEntry target and initializes only that semantic field in the local record.

The original body writes92 of192 local bytes:the three strings and a vertical
coordinate. Across114982 CPU local reads,114896 reads/115025 bytes use writes
from the current body; the other86 reads are the retained target. The independent
verifier replays every prior store at each read and checks actual bytes/masks.
Native compares96 owned bytes per case, including target, and keeps the other96
bytes zero/false. Across43 cases,4128 private bytes stay unknown; none are
imported from original caller stack. Source retains its complete original stack
and historical masks without alteration.

The last three actual starts are4275bc `mov eax,[44d064]`,4275c1 restoring EDI
fromrootSP+20, and4275c5 reloading Sleep intoESI. Their original bytes and full
unchanged game globals derive selector0 for the next4275cb dispatch. This is an
instruction-derived EAX result:the raw snapshot stores the four nonvolatile
registers, not a separate measured EAX field. Native's new
`OriginalFrontScreenBody.advanceOwnStartup` retains its own selector and local
record. EDI/ESI continue as its own target/typed Sleep; no research executable
address becomes a native callback. An earlier stopped boundary produces no
retained selector. Next4275cb consumes this result rather than a later reread.

## Comparison and acceptance

6650 ordered events compare full game globals at each boundary.12757 CPU stores
and129 API stores reconstruct40235 written bytes, complete source stack/masks,
all global bytes and the DLL image. Only126 original DLL DC stores change library
state. The source reproduces full CRT/PTD/calendar state unchanged and CW037f;
this does not claim process-wide hardware-FPU status equivalence.

There are344 complete helper returns:43 updater,129 installed text,86 bitmap and
86 clip. Actual execution covers303 EXE and51 DLL starts; no CRT body executes in
this new stage. The caller reaches170/326 straight-line starts and the updater
7/67. Missing branches are listed byte-for-byte in the report. These are exact
observed coverage counts, not all branch outcomes or application completeness.

Each native continuation stages the full unfinished message-loop iteration and
then rolls it back at4275cb. Seven late observer failures cover initial Enter,
Leave, the50th local write, second background-mode request, third ReleaseDC,
second Blt and final body boundary. Committed startup/callback, globals, MSG,
counter/baseline, all graphics/resources, library DC and produced local result
remain unchanged. External effects must stay buffered until the whole iteration
can commit. These native observer failures are not original execution failures.

The first raw13 release tests passed38.704s/build205.91s. Review then recovered
the next selector consumer and added native retainedSelector; original raw bytes
and expected results were unchanged. Final raw13 tests passed39.290s/build195.20s. Packaged acceptance and exact
jobs are recorded in `build/research/application-screen-body-work.json`.
Final packaged13 tests passed38.813s/build0.28s without raw overrides.
NTSDNative linked; all source and native jobs are terminal. The616-file isolated
package retains614 committed base files plus owned additions and excludes the
six foreign worktree files.

No source or native test failure occurred in either acceptance. An ad-hoc
objdump inspection initially started inside an instruction; it was repeated at
observed start4275bc. The source PC inventory never used that incorrect start.

Raw64242750bytes SHA256:
`d9a56cf886174c1823985176be46387e42b203786d19f1f5c31ed6d6501d1279`.
Lossless fixture13339072bytes SHA256:
`edd6f2e5a96d8e16c97dc7477e47dd5c845536e830994fd47668bf1adddc2d8e`.
Full raw/packed JSON/bytes/SHA,1345 blobs,43 atomic parts,36 whole assets and
10 codec vendor hashes verify. All242 prior fixture files remain unchanged;
243 current. Never restart the completed candidate1 capture.

Tools: [source](../../tools/oracle_application_screen_body.py),
[independent verifier](../../tools/verify_application_screen_body.py),
[acceptance](../../tools/accept_application_screen_body.py),
[report](../evidence/application-screen-body.json).

## Next actual boundary

Continue4275cb with the own selector0, target, typed Sleep, produced local bytes,
DC and complete resource registry into427915/main menu. The separately accepted
setting-1 parent still requires its own alternate branch. Preserve library text
routing throughout the following output. Worker execution/delivery/reentrancy,
earlier CRT/NLS/private backing, whole World/dispatcher/WinMain returns and app
integration remain open.

The current Practice app is separate from this reference composer. CUA again
reports `Native apps: Error: Sky Computer Use native pipe startup failed`; no
window/input action occurred. Actual Windows files/devices, pixels, sound/input
latency, the complete Naruto/Sasuke District match, all original content/modes/
network/replays and clean-Mac acceptance remain part of the active full-game goal.
