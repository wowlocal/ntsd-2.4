# Independent active contacts and attachment comparison

Status: investigation; no new stage accepted. Parent e01d234 accepts the finite
active physics comparison. This extends the [whole body plan](APPLICATION_ACTIVE_BODY_PLAN.md)
through seven consecutive stages: depth-attachments, contacts, hits-items,
cpoint-actions, cpoint-placement, cpoint-cleanup and cpoint-attachments.
Consumer: the current own physics output and the following graphics/lifecycle
passes, ultimately the complete match and standalone native game.

## Evidence and question

Use both immutable original-active-gameplay fixtures, 48 calls each, with the
current acquired Object/Frame/heap/background inputs. Recover the seven passes
independently from WORLD_LINKS, WORLD_CONTACTS, WORLD_HITS, WORLD_CPOINTS and
installed LIB_WORLD_CONTACTS/LIB_WORLD_HITS evidence, including their archived
constraints. Reference EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
VC80 c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
lib.dll 28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba.
Saved Unicorn mappings, API replies and CW023f are controlled evidence, not
actual Windows/device behavior. No original code, emulator, completed capture
or historical auditor is executed by this card.

The saved inventory in the physics task area is unaccepted: unchanged output
does not establish a no-op. Eleven source pairs per variant pass broad contact
checks. Prove subsequent raw ITR filters using current fields and masks; do not
infer absent hooks from a catalog census. Compute own broad geometry and RNG
from own state, which differs from source positions, frames and RNG history.
Any newly required reachable path extends the equations before acceptance;
it cannot be excluded to turn a mismatch into a pass.

## Required comparison

- Reproduce all 672 source full pool/global endpoints and masks, saved Actor
  checkpoints, primitive effects and relevant helper/return boundaries.
- Retain current and collision/previous Frame identities separately, raw heap
  allocation provenance, both ordered signed vrest decrements, cached wrapped
  rectangle tests, installed filters and mandatory fusion cooldown tail.
- Preserve both depth passes, item counting and RNG, both hit passes, cpoint
  gates and held-owner cleanup. Unneeded retained scratch stays unknown.
- Compare the same seven stages sequentially from actual own physics output,
  including full match, memory/library/CRT/Frame/resource owners, caller local
  record and journal. No expected after-state is passed into Core.
- Keep both accepted physics methods and nested control/input/full body preflight
  with late rejection, rollback and retry checks. Add discriminating negative
  comparator trials for unknown causal inputs and unsupported finite paths.
- Full instruction stores/helper ABI are claimed only where actually compared.
  Complete active body, damaging/lifetime branches beyond this finite sequence,
  paused/round/match, device/Windows/clean-Mac and full game remain open.

## Ownership, execution and delivery

Root owns new OriginalApplicationActiveContacts*.swift comparison tests, an
optional observer in OriginalApplicationActivePhysicsTests.swift, this plan and
result/evidence, CURRENT_WORK and RESEARCH_MAP links. Core, old comparators,
expected fixtures/resources and archive remain unchanged. A demonstrated Core
discrepancy requires a plan amendment. The existing independent reviewer owns
only new numbered read-only review reports; author review is not independent.

Clean 997-file inputs are pinned in
build/research/application-owned-active-contacts-native-20260914/initial.json.
Use that task-owned X5 area, UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,
40GiB X5/6GiB internal reserves, task bound40GiB and3600seconds per Native run.
Initially three candidates before diagnosis and revised plan. Reuse existing
freeze/job/verification/archive tools with new paths, preserving failures.
Freeze actual files before Native execution; select new contacts and retained
physics methods after the observer change. Other regressions require exact pins.
Never restart a live or completed job for silence; verify identity before action.

Source comparison, Native regression, independent review, package byte/mode checks
and both archive readbacks are separate gates. Existing safety incidents stay
open without retry/rerouting. The user has authorized commits of completed work.
EXE envelope is not recalculated by this card.

## Candidate1 finite path

The separate stdlib source formula reproduced672 endpoints/576 Actor checkpoints
and1490 ordered helper returns. Each variant has11 broad pairs, all rejected
before team/narrow geometry, and48 nonzero item draws. Actual raw cached bounds
are preserved, including unusually large known heights. The equations keep
installed type/effect and invulnerability predicates explicit; cpoint/held-item
paths and creation require extension when reached. No expected/Core is changed.

Freeze1000 Native files: three new comparison files and one changed physics
observer,996 old files unchanged. Select two contacts and two retained physics
methods.149 unselected inherited methods retain their pins (153 unique methods
with the four selected). Eight new negative trials across both variants require
rejection for unknown vrest, live alias, nonempty hit count and item creation.
The source middle heap/Object snapshots are absent; acquired records and recovered
read-only paths do not become an observed interior-write trace.

## Candidate2 correction after independent review

Candidate1 and source formula1 used+1c instead of+2c for ITR.effect. Endpoint
equality did not expose this because later invulnerability rejects the pairs.
The exact old files/run and comparator-issue1.json are retained; they are not
acceptance evidence. Corrected source formula2 independently reproduces the same
672 endpoints. Expected fixtures and Core remain unchanged.

Candidate2 reads+2c and adds controls using current Naruto Object Frame63 kind0:
unknown effect with known+1c must fail before invulnerability; effect4/type0 with
invulnerability0 must reject the ITR; effect0/invulnerability0 must require an
extended path. These are explicitly comparison-only mutations, not natural
gameplay or changed source expected values. There are12 negative and2 positive
new controls across both variants. Helper comparisons now include exact return
PCs for both directional calls and both attachment call sites; full ABI remains
unclaimed. Same four selected methods and storage/time limits apply.
