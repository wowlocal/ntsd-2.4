# Host gameplay candidate — syntax-checked implementation

2026-09-22. Implements the [gameplay preflight](APPLICATION_HOST_GAMEPLAY_PREFLIGHT.md)
under the [frozen plan](APPLICATION_HOST_GAMEPLAY_CANDIDATE_PLAN.md) and explicit
[test-transport amendment](../evidence/application-host-gameplay-candidate-transport-amendment.json).
The [publication](../evidence/application-host-gameplay-candidate.json),
[closure](../evidence/application-host-gameplay-candidate-close.json),
[reviewable patch](../evidence/application-host-gameplay-candidate.patch) and
[documentation checks](../evidence/application-host-gameplay-candidate-docs.json)
identify this increment. **Syntax passed; compilation and all 69 selected methods
remain unexecuted.** The candidate is frozen and has not been promoted into root.

## Analysis and scope

Base HEAD is `3e4f0d9a485568f55307d4d129aa01b297db682f`. Actual inputs are all
1039 files of application-host-match-correction1-20260922 and its subsequent
[53-method completion](APPLICATION_HOST_MATCH_COMPLETION.md). Protected working
root remains1034. Prior compiler/RSS failures and the preflight's original1MiB
metadata overrun remain immutable; this increment does not change their outcomes.
Its own original4GiB storage allowance was met without an increase.

The missing consumer after the actual host's first gameplay input is now written:
the host retains that input, resumes its existing gameplay or paused body and
then completes the existing enclosing loop. This advances ownership toward the
first Naruto/Sasuke District match. A complete source/installed application tick,
playable match and full game are not established by the code or syntax result.
The original distribution's EXE/DLL hashes and declared synthetic source mappings,
API/input responses, FPU/ABI, unknown-storage and installed-library boundaries
remain those in the plan/preflight. No new original execution or envelope decoding
occurred. The previous goal turn was progress, not a verified wait or no-progress
restatement: it published the contract and evidence that selected this change.

Before editing, existing active/paused acquisition helpers were found to contain
their own full keyboard/text/recognizer/MSG/counter expectations around a direct
Bootstrap key call. To preserve those calculations against actual Host.step,
the separate amendment adds optional transport to those two test helpers. That
brings the authorized candidate paths from17 to19; it adds no case, reference,
Core rule, execution or storage/time allowance. The original plan stays unchanged.

## Implementation

Only Core OriginalApplicationHostSession changes in the runtime targets. Its
public LoadedOutcome has returned, matchPrelude and gameplayInput cases.
`prepareLoadedUntilBoundary` lets one acquisition select its next consumer;
existing return-only/menu APIs adapt to the same preparation implementation.
The private Prepared owner retains either a returned body, Start or actual
gameplay Ready with an independently staged platform. The new read-only accessor
is preparedGameplayInput; the old accessors remain mutually exclusive.

Preparation checks Ready.loading against the pending exact attempt, then invokes
the existing GameplaySession constructor's alias/branch/installed-library checks.
The historical Ready.entry pool owner is not substituted for the current loading
ticket. All current state, match, inputContext, replay, music, commands, round
result, resources, operation journal and graphics remain owned by the actual
Ready. The new resumeGameplay accepts only privately retained Ready, clones its
prepared platform, calls the provider and validates the returned child's exact
ticket, returned result and aliases before its final preparation observer.

Body success stores returned-child/platform without publishing a batch. Body
failure retains Ready/platform; tail failure retains returned-child/platform.
The existing corrected finishLoadedMenu remains the sole outer publication and
installs `self.platform` at commit. Existing stage/reentry guards, independent
class-copy requirement, publication ordinal and previously queued batches remain.
No gameplay rule, numeric literal/order, resource identity, child handler,
expected byte/mask or fixture changes.

The existing LoadedTestDriver now transports gameplay input/body/tail, exposes
explicit observation hooks for controls and counts successful stage transitions.
Actual host sequences use its existing loading/character/selection/launch parents;
no Bootstrap snapshot initializes a host. Neutral17, active48 after neutral17,
and pause/resume14 after neutral17 retain the same selected driver throughout.
Active wrappers propagate it from Output through Layout, Recording, Notices, HUD,
Commands, Lifecycle, Graphics, Contacts, Physics, ActiveBody.control and the base
sequence. The two acquisition helpers still calculate and compare complete state;
only their key-call transport may use Host.step. Legacy Bootstrap defaults remain.
The whole outer return comparisons retain actual current time/counter/MSG effects.

Six new HostGameplayTests are defined, not run:

| Method | Required comparison/control written |
| --- | --- |
| testHostNeutralGameplayRetainsSeventeenWholeReturns | Both backings; full neutral17 projection and one committed batch per return |
| testHostActiveGameplayRetainsBothFortyEightCallSchedules | Both backings; all active48 through the complete output projection chain, after actual neutral parents |
| testHostPauseStepResumeRetainsBothFourteenCallSchedules | Both backings; full14 including eight paused bodies, after neutral17 |
| testGameplayInputPreparationFailuresPreserveHostOwners | Eight provider/prologue/local/replay/round/commit/host-observer/control failures at actual first two inputs; prior launch batch retained, followed by full sequence retry |
| testGameplayBodyAndOuterFailuresRetainStagesAndRetry | Twelve normal/paused body failures including graphics, sound, dispatcher, checkpoint, commit and host observer; six time/Sleep/commit failures; retained-stage retry through existing comparisons |
| testGameplayTicketsAndReentryRejectForeignSiblingAndConsumedInputs | Actual foreign host and same-revision sibling, stale/historical ticket, missing library/wrong branch, nil returned result, wrong stage, reentry and consumed controls |

Controlled invalid Ready values derive from an actual acquired child, retain all
unmodified fields and are used only as negative controls. They do not seed natural
success chains. Same-revision siblings come from an actual Bootstrap copy; the
foreign child has its own actual host parents. Full Ready/state/match/resource,
platform, pending/prepared child and prior-batch preservation is checked before
retry. Comparison models and expected values remain unchanged. These written
assertions require execution before they can support a behavior claim.

The exact method inventory contains the previous53 plus ten existing neutral/
active-output/paused/source-reader/provider methods and these six new methods:
69 total. Result-file compatibility and existing safety-refused dependencies
remain required/open. No new original writer or result-file provider run is
selected, and ordinary unsupported errors are not relabeled as successful source
fault matches. The existing incident register is preserved; unavailable exact
triggers remain unknown. No new safety refusal occurred in this increment.

## Validation and preservation

Preparation PID79277 exited0 in2.637s, cloning1039 verified regular files onto X5
with distinct inodes. It copied no build/cache and preserved actual root dirty
inputs. The new candidate has1040 regular files, with19 changed paths including
one new test file. Its manifest SHA256 is
`4deadc3ce77d2151662b41df365e9ca2c9c4c6ab1da7f8eb0161ec11a0b282cc`.

The installed swift-frontend `-parse` process81822 exited0 in0.122s with an empty
log and sampled38,322,176-byte RSS. Exact command, input pins, minimal seven-key
environment, PID/start/cwd and terminal status are saved in parse1-config.json and
parse1.job.json. The300s/4GiB guards were not reached. Syntax is not typechecking,
linking, a package check, XCTest or a device observation. All old binary passes
retain their old candidate identity and do not validate this changed Core.

The first draft is preserved under author-draft1. Author inspection found446
existing comparison lines retained. Final verification reverses only the declared
transport signature/call edits and checks byte equality for all eleven active
wrappers and both acquisition helpers. Existing sequence test methods/following
helpers remain byte-identical; their comparison statements are preserved with
the declared caller transport substitutions. All69 method definitions exist
exactly once. These textual checks supplement, and never replace, future full
Native comparisons or independent review.

Finalizer82133 exited0 in9.623s and is absent. It verified root1034, prior1039,
current1040, source55, all protected context pins and file membership/modes/bodies.
All385 fixtures,102 runtime resource payloads and Package.swift are unchanged.
The50,151-byte patch SHA256 is
`9fbbadd521f0b7afb123e7f6b350cfeac14016ea11de1fb2d215c0890b867fef`.
It uses zero-context unified hunks applied with --unidiff-zero to the separately
pinned baseline; exact round-trip bytes were checked for all19 paths. No source
expected value or gameplay comparator is changed by this patch format.

The58-member candidate/draft/metadata archive is1,136,640 bytes, SHA256
`c707d3260005e243caa5c6f0622836377cbf80ac5e79b5bbb8acc5a547597377`.
Readback verified every member's bytes, mode, nanosecond mtime and exact membership.
The candidate and external task are frozen. Final observed free-space decrease,
including concurrent source growth, was276,967,424 bytes within4GiB; external
free157,993,431,040/internal66,449,293,312 bytes preserve original40GiB/6GiB reserves
and the source17GiB commitment. X5 UUID was verified before dependent IO.

Two read-only guessed filenames were absent: OriginalInputControlContext.swift
and historical parse1-runner.py. Their exact errors and tool chunks are retained
in inspection-diagnostics1/2.json; rg located the actual declaration, and the
existing saved parse command/job procedure supplied the syntax operation. No
original or Native game execution was triggered by either error.

Source59727 was revalidated against PID/start/full command/cwd/job at19:39:12 UTC:
3634 chunks,24 Objects,890,929,096 stores,32821.24s; still running, not complete.
It was not signaled, restarted or modified. Root Native remains untouched.

## Next gate

Freeze a new validation task using this exact1040-file candidate: fresh build,
all69 selected methods, package byte checks and artifact/metadata archive readback.
Retain previous53 method limits and declare bounds for the newly selected whole
parent chains before execution; stop the queue on the first nonpass and preserve
all terminal/partial results. This implementation result closes no Native gate.
Independent review is unavailable and remains open; author inspection is not
independent. Root promotion, source catalog completion, installed whole-application
trajectory, native app/window/input/audio, Windows/device comparisons, the first
complete match and full game remain open.
