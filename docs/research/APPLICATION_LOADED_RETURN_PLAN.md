# Own loaded application return

Status: implementation; the whole input/menu/application return is not accepted.
Parent: `caa953f`, [pool and interface](APPLICATION_POOL_INTERFACE.md), with a
clean tree when this plan was opened. The complete game in
[CONTINUE_GOAL](../CONTINUE_GOAL.md) remains the objective.

Continue the actual `PendingInput` at `41c581` through local/control/received
input, replay and round dispatch, fresh library menu, loading/early return and
the suspended message-loop tail. Preserve current catalog, 400 Actors, ten UI
owners, sound and music owners, RNG, complete records/masks, resource identities
and the ordered tentative operation journal. Publication requires the enclosing
iteration's final observer to succeed. Intermediate implementations can be
committed without claiming this whole contract has passed.

Work and immutable plan versions:
`build/research/application-loaded-return-native-20260913/plan1.json` and
`amendment1-loop-ownership.json`. The former pins all 934 existing Native files
and relevant instructions/evidence. Author: root; independent read-only reviewer:
`war_contract_review`. The immediate preceding status-only turn made no progress;
the last implementation turn committed the accepted pool/UI continuation.

The behavioral reference remains the original NTSD EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, with
the pinned original resources, library and CRT in the direct dependencies.
Only saved reference data are read. Native Swift compilation and new data-only
verification are permitted; original execution, emulation and historical
producers/auditors are not run. Existing source failures and safety incidents
remain unchanged and open.

## Required joins

| Join | Existing evidence and shared code | Required comparison / unresolved boundary |
| --- | --- | --- |
| Current memory to match state | `OriginalApplicationPoolSession`, `OriginalInitialMatchEntry`, [INITIAL_MATCH_ENTRY](INITIAL_MATCH_ENTRY.md), [MENU_STARTUP](MENU_STARTUP.md) | All six input checkpoints; current World/Actor tokens bind to non-null ordinals without replacing live records by constructor snapshots. |
| Saved playback and replay memory | Current `full[b588..<b8a8]`, `memory.replayPointers`, [INPUT_CONTROL](INPUT_CONTROL.md) | Preserve 800 bytes and masks plus the adjacent eight-byte pointer alias; write staged context back to the same full owner. |
| Arithmetic context | Explicit `OriginalArithmeticPrecision`; source setup before WinMain | Historical startup reference defaults to 64 bits; own WinMain does not establish the earlier 53-bit initializer. Carry an explicit context with provenance. |
| Music and fresh menu | `OriginalWinMainStartup.output.music`, `OriginalModeMenuContinuation`, [FRESH_LIBRARY_MENU](FRESH_LIBRARY_MENU.md) | Carry the retained WinMain music owner; `StartupSounds` alone contains only WAV owners. Compare music before resources, current surface owners, library DC and selected body. |
| Loading and early return | [MENU_RETURN](MENU_RETURN.md), [LIB_MENU_CONTINUATION](LIB_MENU_CONTINUATION.md), `finishWorldCall` | Actual continuation, presentation result, held clear at `424746`, then dispatcher return. Old separate controlled cases are not a new whole-original application capture. |
| Suspended outer iteration | [APPLICATION_TIMER](APPLICATION_TIMER.md), [APPLICATION_MESSAGE_LOOP](APPLICATION_MESSAGE_LOOP.md) | Retain MSG after PeekMessage, prepared baseline/interval/target and counter. Resume only the tail; do not repeat queue, clock prefix or dispatch. |

The first inspection established a missing owner in the last row:
`PendingLoading` discarded the tentative loop when returning the loading child.
Amendment 1 moves this prerequisite ahead of the input join. It does not reduce
the card to a timer task. A `PendingDispatch` must retain the exact value at the
game call, and later resume with the completed child's signed result. Recovery
occurs only for a negative result, followed by the fresh tail clock, signed
sleep calculation, wrapping counter stores and final observer. Effects remain
buffered throughout. A successful staged continuation is not backend delivery.

## Cases, gates and limits

The first candidate compares split dispatch/resume against all 2025 unchanged
timer cases (1215 dispatches, 810 not-due decisions, 8163 events). Retained
64 message-loop chains check 215 completed iterations and their original
required-dispatch boundary. Native-only controls add partial PeekMessage output,
an earlier committed MSG, three-read clamp, recovery/time/sleep/counter/final
failures, invalid tail writes, signed counter wrap and a fresh buffered retry.
They do not add original branch coverage or reproduce Windows failures.

Initial regression filter:

```text
(OriginalApplicationLoopContinuationTests|OriginalApplicationTimerTests|OriginalApplicationMessageLoopTests|OriginalApplicationMenuInputTests|OriginalApplicationLoadingSessionTests)/
```

Later input/menu candidates require the parent pool/catalog, both menu startup
and repeated menu corpora, fresh library menu and the current whole owner
comparison. Pin any additional paths and exact cases before extending execution.
Do not replay successful tests merely to restate their results.

Before each Native run, freeze the actual candidate, check unchanged protected
files, obtain independent contract review, and record command/PID/cwd/inputs and
terminal output. Before acceptance, inspect test assertions and results, compare
packages and archives bytewise, resolve material review findings, and update
the handoff. Exit zero alone does not establish fidelity. No expected bytes or
masks may be changed to accept the candidate.

Task storage is the verified X5 task directory with APFS UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. Initial limits: 40 GiB, 3600 seconds per
Native run, three candidate rounds before diagnosis, and the unchanged 6 GiB
internal reserve. The three preceding pool/UI Swift jobs are terminal and their
PIDs were absent when this plan opened. Revalidate actual process identity before
action; a quiet log is not a stopped process.

Windows/device raster inputs, macOS backend publication and full loaded-game
execution remain separate required evidence. Do not treat original DIB colors
as measured device pixels. This plan does not complete the first match, all
content/modes/networking, input/audio/latency or clean-Mac acceptance.

## Current input ownership stage

After the loop prerequisite was committed as `3292f1b`, the next stage continues
the same whole card with current World/Actor bindings, saved-playback/replay
aliases, retained actual WinMain music and explicit arithmetic context. Its
bounded plan and additional input pins are
`build/research/application-loaded-input-native-20260913/plan1.json` and
`implementation-plan2.json`; implementation/comparison limits are described in
[APPLICATION_LOADED_INPUT](APPLICATION_LOADED_INPUT.md). This separate Native
stage retains its own 40 GiB/3600-second/three-candidate budget and the unchanged
6 GiB internal reserve. Earlier failed loop candidates remain immutable.

The immediate previous turn made progress by committing and verifying delivery
of the loop prerequisite. It did not accept the whole card. New input acceptance
must preserve the menu and final enclosing-return requirements above.
