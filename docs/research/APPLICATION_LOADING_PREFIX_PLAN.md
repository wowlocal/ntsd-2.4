# Own loading prologue and common sounds

Continue actual41bc90 from the three accepted APPLICATION_MENU_INPUT activation
chains on the same original CPU/stack/World/resources/RNG/MSG. Recover the aligned
prologue, phase/pause and local command initialization, MENU_WAIT draw, two clear
regions,18 whole4014e0 WAV calls, count18 and presentation, ending at the actual
catalog allocation request4450ac (caller41bff5, size4d823a8). That allocator and
the catalog body remain the next dependency; no fabricated loading return.

Reference artifacts: pinned NTSD2.4 EXE, installed bundled lib.dll, VC80 and
original PE DIB/WAV bytes. Unicorn2.1.4 executes actual relocated DLL attach,
independently entered WinMain, delivered menu callbacks and loading instructions.
Win32/MMIO/COM/allocator/copy requests have declared research responses. Retain
the own DirectSound device, target, live/dead bitmap records and earlier sound
buffers; allocate disjoint research WAV storage. Trace bytes/masks, stack
producer reads, ordinary SEH/cookie code and helper returns to establish lifetime
and update order. Native must use its own prior output, never expected snapshots
or private stack words. No control/protective corruption, bypass, continuation
after faults, external messaging/network/URL operation or Windows/device claim.

Finite corpus: three unmodified activation parents; from default parent, at each
of WAV positions0,8,17 supply missing file, short data read, and failed sound
buffer creation (12 cases total). Missing/short reads retain actual ignored false
returns and cleanup; failed creation stops at the previously declared40187a
unsafe continuation and is an explicit native rejection, not a successful match.
Use only ordinary platform response failures; preserve all parent records.

Compare full globals/write masks/order, resource bytes/masks, WAV temporary/PCM/
format/descriptor ownership, output requests and source ABI, library/PTD/MSG,
20 zeroed local command bytes and opaque output fields under masks. Native composes
begin/common loading from the pending own menu iteration and preserves all prior
completed iterations on missing dependency or late observer failure. Include late
draw, middle WAV, final presentation and pre-commit failure checks.

Export native in isolation from70631c0, preserve245 old fixture hashes and all
foreign files, verify full raw/packed bytes/JSON/SHA, source atomic parts and
vendor/resource identities. Retain own menu-input and controlled initial-loading/
WAV contracts as appropriate. Record finite acceptance and terminal jobs before
the authorized checked milestone commit. Whole catalog/UI/input/loading return,
workers, full CRT/NLS, actual Windows/window/devices, full match/all content and
clean-Mac acceptance remain open under the active goal.
