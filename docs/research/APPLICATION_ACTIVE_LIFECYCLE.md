# Active post-draw lifecycle comparison

Status: accepted finite active lifecycle comparison; the whole active body remains open.
Implementation parent: `1bc856a3e370a509ce695121c35b77c066355ae0`.
Follow the [frozen scope](APPLICATION_ACTIVE_LIFECYCLE_PLAN.md),
[Actor50 provenance](APPLICATION_ACTIVE_LIFECYCLE_PROVENANCE.md) and
[WORKFLOW](WORKFLOW.md). The consumer is the remaining active body and a complete
Naruto/Sasuke match on District; the full standalone game remains the goal.

## Independent comparison

The comparison-only `OriginalApplicationActiveLifecycleProjection` reads the
current impulse output, current Object/Frame records and catalog order. It
calculates the complete live400-slot pass through `4214d5`, preserving the order
prefix → scheduler → opoint/lifetime for each slot. The saved pristine source
and the library-enabled own application are evaluated separately. Source event
numbers, coordinates and after-state never supply the owned expected state.
The pinned EXE, VC80, lib.dll, fixture hashes and synthetic reference environment
are recorded in the linked provenance study; no original code is executed here.

On this finite path the comparison covers healthy HP/MP recovery, animation
wait/next and jump transitions, sound queue accumulation, first matching Object
and free-slot selection, sparse construction, current parent placement and
same-pass scheduling/deactivation of the new later slot. Constructor writes
retain untouched backing and masks. Child depth uses current binary64Z + 1
before integer conversion; inherited facing retains horizontal negative zero.
Deactivation resets the frame and World activity, preserving the allocation,
Object binding and inactive storage through later calls.

The source reader first compares all records/masks, ordered lifecycle events,
helper order/arguments/receivers/results and return stack shape against the saved
stage. It verifies the declared constructor memset adapter and absence of access
to the six retained scratch words. Complete middle Object/Frame snapshots,
per-instruction stores and full helper ABI are not available or claimed.
Scheduler return comparison is limited to the observed ordinary/invalid-next
paths; held/skipped scheduler returns require a separate comparison.

The owned comparison obtains its predecessor from the real retained Bootstrap,
input, control, physics, contacts and graphics chain. It compares full match and
resource state, allocation identities, all owners, local record, chronological
journal and ordered primitive effects. A lifecycle event must occur inside its
checkpoint interval; another gameplay family inside that interval is rejected.
The graphics parent independently checks the actual returned command extent and
owners. That composition does not establish the semantics of later body stages.

## Controls and rollback

Current-input controls exercise Actor aliases, unknown current Frame, real team
commands, required Object header90, fractional depth, inherited facing, scheduled
Frame selection, retained opaque backing/masks and negative zero. The transient
must be visited and removed in the same pass. Source comparison retains exact
sound ordering and all helper returns.

Four late failures per schedule occur after construction, after the later sound,
at the lifecycle checkpoint after deactivation and before commit. Each restores
full caller state and buffered observations with no pending return. The same
session then retries successfully and equals the earlier successful complete
return, including commands, journal, resources and local record. Existing input,
body and outer rollback checks remain in the inherited chain.

## Delivery

Implementation changes only the three new ActiveLifecycle test helpers and
callback forwarding in ActiveGraphicsTests. Production Core, fixtures, resources,
reference expected values and masks are unchanged. Candidate1 is preserved and
was not run: review found missing scheduler/constructor result comparisons and a
gap in rejecting other gameplay event families inside the lifecycle interval.
Corrected candidates, exact reports and Native logs belong to
`build/research/application-owned-active-lifecycle-native-20260914/` on X5.

Candidate2 passed four selected release methods in220.779s; build356.75s:
Graphics control68.261s/primary48.023s, Lifecycle control52.416s/primary52.079s.
The new comparison covers96 source and96 owned lifecycle endpoints,216 source
helper returns, two declared constructor memset boundaries,14 sounds on each
side and two transient lifetimes on each side. Eight late rollbacks and two
same-session retries passed. The147 unaffected earlier methods are inherited
through exact file/result pins; they were not rerun. Together with the four
selected methods this retains151 unique methods. Changed graphics forwarding is
exercised through both new whole application chains.

All1006 Native files match frozen2;1002 older files,383 fixtures,102 resources
and485 packaged fixture/resource files are unchanged. NTSDNative linked.
`native-candidate2.tar` contains1006 fully read-back bodies/modes,4,442,327,040
bytes, SHA256`5ac5707f0a0a6cc91de61cb4a30f37dc185872e86b2beb78b0cbf792d6e42d23`.
The separate evidence archive retains rejected candidate code, manifests, exact
review reports, logs, verification and current study/handoff/map. Its membership,
body/mode verification and final publication review are linked from the
[delivery evidence](../evidence/application-active-lifecycle.json).

Prepublication review also found missing integrity joins in the first unused
archive/publication/Git scripts. Their exact bodies and issue record are retained;
corrected tools join tested inputs, archives, current documents and every tested
Native Git index blob. These tooling corrections did not change frozen2 or its
successful tests. No successful Native test was rerun for publication.

The only Native job is terminal2, PID56878, start2026-09-14T01:45:01.633380Z,
end2026-09-14T01:54:41.572295Z, exit0. Candidate1 was rejected before execution.
The raw stdout is immutable; a diagnostic view removes only identified XCTest
metadata interleaved with printed gameplay records, retaining exact byte spans.
No original, emulator, historical producer or auditor was executed.

## Remaining scope

Six remaining active stages stay open.
The next stage is commands/recovery, followed by HUD, notices,
recording, layout and output. The full active tick, damaging/held/cpoint and
alternate spawn paths outside these schedules, broader gameplay, match, game,
app integration, actual input/audio/raster, Windows and clean-Mac acceptance
remain open. The EXE envelope is not recalculated. Prior
[safety incidents](../evidence/codex-safety-incidents-2026-09-12.json) remain open;
the affected operations were not retried or rerouted.
