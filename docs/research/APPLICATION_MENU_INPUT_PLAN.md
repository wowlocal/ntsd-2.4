# Own queued input and repeated early-menu calls

Recover actual message delivery after the accepted APPLICATION_MENU_RETURN
iteration, then repeated43e9a0/4246b0 calls on the same CPU/stack/resources.
The first gameplay-selection chain should proceed through a delivered mouse
move/down, the main-menu World0->1 write and real3000-call CRT table rebuild,
whole World1 background release/presentation/World2, then the actual41bc90 loading
entry. That entry is the next separately composed loading dependency; do not
invent a loading return or claim a complete match. Next child work remains open.

Pinned NTSD EXE/lib.dll/VC80/control.txt/DIBs/WAVs, Unicorn2.1.4, actual relocated
DLL attach followed by independently entered WinMain. Declare Win32 Peek/Get/
Translate/Dispatch callback delivery, clock, COM/GDI/audio results. Execute real
WndProc instructions on the preserved stack, including normal SEH/cookie code.
Do not reset parent CPU/registers/globals/CRT, inject mouse/World/held/worker
values, supply expected snapshots, corrupt control/protective structures,
continue after faults, or perform actual network/URL/device operations. Unknown
private storage remains source evidence, never a native input. Revalidate jobs.

Finite source acceptance: fresh continuations of all47 completed menu-return
parents through one idle repeat iteration (the existing pre-NULL cursor parent
cannot continue). Then three independent activation sequences from default,
minimized-window parent and negative-presentation parent, using delivered
WM_MOUSEMOVE(350,230), WM_LBUTTONDOWN, due iteration, WM_LBUTTONUP, due World1
iteration and next due iteration to actual41bc90. No worker delivery is injected.
Real source branches or ordinary missing-resource boundaries choose the stop.
A newly reached missing dependency remains explicitly open until recovered;
record evidence before extending the plan and never replace it with success.

Compare full ordered platform/drawing/sound/CRT events, all source globals/
stack/known masks/MSG/library and resource live/dead bytes, actual own return ABI,
3000 per-call rand transitions, own target/local producer reads and complete
parent records. Source API callback frames are declared delivery, distinguished
from actual Windows ABI/queue behavior. Native composes its own loaded resources,
RNG, input, dispatcher, menu loop and tail, commits each completed message-loop
iteration and rolls back a failing pending one. Test late callback, drawing,
RNG-table, release, presentation and loop-commit observers where reached.

Isolate native export from046f95f, preserve244 fixture pins and10 vendor hashes,
full raw/packed bytes/JSON/SHA and atomic source checkpoints. Retain prior own
menu return and controlled loop/main/alternate/presentation contracts. No app
window, Windows, worker, full CRT/NLS, full loading/match or shipping-runtime
claim follows from a bounded research composition. All six foreign files stay
unchanged; checked milestone commits use the existing LFS/push hook.
