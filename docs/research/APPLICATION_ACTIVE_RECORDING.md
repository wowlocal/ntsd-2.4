# Active result recording from owned notices state

Accepted finite comparison of both48-call active schedules through the whole
result-recording gate421cdc..422218 and its actual continuation422944. Current
owned notices output supplies the inputs. Source and own expectations are
calculated separately from their current records, without importing an after-state.
[Plan](APPLICATION_ACTIVE_RECORDING_PLAN.md), [evidence](../evidence/application-active-recording.json).

## Analysis

Saved pristine EXE/VC80 and own library-enabled Native application remain distinct
environments. All96 source calls have timer450bdc0 and elapsed450bbc17..64 before,
18..65 after. Each has one four-byte store at421ce6. Full pool/masks, recording
memory/pointers/saved state and other globals remain unchanged. early.globals and
state.globals are two references to one changed global record. All other retained
components, early World and allocations are compared with bytes/masks/lifetimes.
No helper, event, primitive effect, global store beyond elapsed, or watched caller-
stack access occurs. Seven executed addresses plus stopped422944 form a set,
not an ordered dynamic trace. Source CW023f/FPSW0or4000/tagffff and recorded
scratch/spawn/stageDefeated survive; Native ABI/FPU equivalence is not added.

The independent finite model reads signed timer450bdc. Wrapped unsigned
(timer-101)<=248 is the explicitly unsupported result-table branch of this active
comparison. Other values choose422944. Signed timer<100 increments current
elapsed with32-bit wrap. Otherwise elapsed is not read or newly defined. No mode,
serialization flag, Actor/Object, replay byte or round word is causal on that
path. Enabled writer/result-table branches retain their separate controlled
contract and are not silently reduced to no output.

## Implementation and validation

Three new test-only Recording files verify saved source joins/stores and then
compare the full own match/state, globals, replay/graphics/memory owners, menu-local,
resources/music/backgrounds and chronological journal through notices→recording.
Unexpected recording annotations and primitive output fail. Parent whole-return
comparisons and complete graphics-command extent checks remain selected.
Production Core and all1015 prior Native files are unchanged.

Controls distinguish signed timer bounds99/100/350/Int32.min/max, counter overflow,
result-range endpoints101/349, causal unknown timer/elapsed, and unread elapsed at100.
A separate public call consumes the actual own notice snapshot, current native
saved-playback/memory owners, nil stageDefeated and unknown unused mode/result flags.
It verifies no provider/codec/file/event calls, exact expected globals and retained
context, plus the public continuation422944 and absent writer. Context comes from
the native application state layout, not source stack storage.

The recording checkpoint does not expose Result.continuation. The public control
checks it directly, and production Body passes the actual returned continuation
to ResultLayout. Equality of snapshots alone is not evidence for that value.
Whole result layout/output semantics remain the next comparison. Source SP64 is
unread here and is never imported. The separate gameplay formatter observability/
lifetime limit remains open; the menu-local record is not that formatter.

Four new late failures (recording checkpoint and beforeCommit per variant) restore
entry/environment and leave no pending return. Two same-session retries reproduce
full returned state, graphics, owners and journal. Parent late-failure checks remain.

Five release methods passed in283.095s after a361.77s build:
two Recording, two Notices and the full controlled ResultRecording test. Its95
callers retain82 writers,12 playback restores,13 no-writer gates,76 codec successes,
5 allocation(-4) and1 no-temporary(-2) results,18322 writes/33219692 requested bytes.
This is not95 codec successes. Existing missing-stage and late-destroy rollbacks
passed inside that test. The writer's separate21 matches/5 source-fault rejections
remain their prior contract; this active sequence has no natural replay file write.
161 unchanged previously accepted method identities are inherited;166 unique total.
Inherited methods are pinned, not claimed to have rerun.

All1018 actual/frozen Native files and485 packaged fixture/resource files were
verified.383 fixtures/102 resources are unchanged. Native tar members were read
back with exact bodies/modes; evidence archive and independent publication review
are separate gates referenced by the evidence report. The prior notices reader's
singular/plural XCTest metadata handling is reused without changing raw logs.
No source expected/mask, Core rule, historical failure or safety setting changed.
No original/emulator/historical producer/capture/auditor executed. Native job1 is
terminal in `build/research/application-owned-active-recording-native-20260914/`;
do not restart it. The map's obsolete static-only writer row is updated from the
already established95-case controlled evidence and current finite comparison;
the historical static plan and archived constraints are preserved.

## Next boundary

Compare result layout from the actual recording output and continuation, then
output. These two stages and full active tick remain open. Active writer/result
branches outside the finite path, full helper ABI/FPU/stores, formatter lifetime,
full match/game, native window/input/audio, actual device/Windows/clean-Mac and
existing safety incidents remain open. EXE envelope was not recalculated.
