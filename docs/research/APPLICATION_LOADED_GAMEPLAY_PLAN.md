# Owned application gameplay continuation

Status: implementation; not accepted. Parent: `ab5a2ea` and
[APPLICATION_LOADED_LAUNCH](APPLICATION_LOADED_LAUNCH.md). This work advances the
first complete Naruto/Sasuke District match; the full standalone native game,
all content/modes/networking and actual device/Windows/clean-Mac gates remain open.

## Contract and existing evidence

Continue the actual retained application input result at41e339 through the
19-stage shared gameplay body, dispatcher return, the same Bootstrap ticket and
the next input consumer. Preserve own installed-library command state, current
bitmap/surface identities, live replay/Actor aliases, mutable frame ownership,
CRT/game RNG and text DC. Library text must run at diagnostics, notices, result
layout and output; pristine helpers remain available for immutable regressions.
The application must buffer all external effects until the enclosing call commits.

Contract review also requires the recovered state85/86 control hook, installed
contact filters, whole library hit pass and interleaved transform prefix. The
second process-attach VirtualAlloc allocation has20000 declared known-zero bytes
in the accepted application loader input (LIB_RUNTIME); initialize its Native
owner once in application State, never once per tick. Prior menu/preparation
does not invoke the hit writer. Transform destination backing remains empty
until established, so the original extended write requires explicit provenance.

Use saved launch, gameplay control/physics/links/contacts/hits/cpoint/camera/
drawing/impulses/lifecycle/commands/HUD/notices/result/output and continuous
fixtures, plus accepted library helper evidence. The reference is the pinned
NTSD distribution EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c` and VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d` under the
documented Unicorn2.1.4 environment. This task reads those saved observations
and executes Native only. No original capture, historical producer/auditor or
refused operation is restarted. Existing safety incidents remain open.

Own Start differs from pristine source in positions, inactive selections, RNG,
resources, installed command3 and text. Those differences need explicit
independent projections or retained helper comparisons; an old expected delta
cannot overwrite different inputs. Source after-state never enters Core.
Unknown scratch remains unknown; source faults and Native rollback are distinct.

## Finite cases and gates

1. Both existing owned Start variants, all19 body stages, actual return and next
   input. Compare complete records/masks and owners, staged graphics/audio,
   replay bytes/aliases, RNG and installed text/command behavior.
2. Repeated own calls with neutral input, initially16 per variant, preserving
   prior returned state; compare against retained evidence where input contracts
   agree. Any new reachable unknown becomes a documented dependency.
3. Late failures at simulation, drawing, diagnostic text, command, output and
   outer commit; reject duplicate/stale tickets. Include changed replay input
   rollback only where actually exercised, never claim it from unchanged bytes.
4. Controlled successful/failed GetDC and late text observer cases check retained
   DC across diagnostics/notice/result/output. These are helper/ownership trials,
   not extra natural game paths.

Mandatory regressions: existing launch two methods, gameplay body two methods,
continuous gameplay, relevant notices/result/output and library text tests;
extend the exact list when the changed dependency set is known. Preserve the
previous93-method plan for final relevant application regression selection.
Freeze actual working Native files before builds and record commands, hashes,
PID/start/cwd/terminal outcomes. Source/package/archive verification and independent
review remain separate gates. Author root edits; reviewer war_contract_review
performs read-only contract review. No acceptance without review and comparisons.

First Native preflight runs exactly the two new OriginalApplicationGameplayTests
methods: own first body plus16 successive returns per variant. Explicit own
item-only draws are41,87,65,127,100,34,74,186,57,100,147,129,78,131,19,136,146
from the already pinned own RNG table/index39/counter0. These assertions are an
integration preflight, not the complete differential acceptance gate. The command
is swift test --package-path candidate1/native --scratch-path swift --jobs4
-c release --filter OriginalApplicationGameplayTests/ (arguments stored exactly
in preflight1.config.json before launch). Candidate1 and each terminal failure
remain immutable; subsequent candidates are separately identified.

Allowed changes: new application gameplay session/tests/comparison, shared
gameplay/text continuations and necessary application joins, this plan/new study,
new evidence, CURRENT_WORK and RESEARCH_MAP. Existing fixtures/resources, frozen
producers, prior reports and AGENTS_HISTORY are protected. Baseline is clean.

Storage: task-owned X5 area `application-loaded-gameplay-native-20260913` under
the existing `01a08766-6cdc-7b33-bb58-04076f1ec261` root; UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. Reserve40GiB on X5 and6GiB internally;
maximum40GiB new task storage,3600seconds per Native run, initially3candidate
rounds before contract diagnosis. Check actual mount/free space before large IO.
Shipping files remain regular/local. Stop at explicit dependency/mismatch/limit,
preserve the failed result and investigate before another identified candidate.

Next after acceptance: active owned match input and remaining return branches,
then actual native app/backend integration toward the complete match. This plan
does not declare a body comparison, playable match or full goal complete.

## Three-round diagnosis and candidate4

Candidate1 retained a compile error from forwarding the surface text renderer
to bitmap-font playback information. Candidate2 exposed an ownership adapter
error: catalog bitmap records live in the retained match catalog, whereas UI
and arena wrappers live in general allocation memory. Candidate3 fixes that
split with current wrapper/surface maps. Its two chains completed all34 calls,
but34 assertions incorrectly required backgrounds to remain unchanged.
All three terminal candidates, logs and failure records remain immutable.

Saved camera source records independently establish nine animated District
counter updates per draw. The recovered41a250 formula reproduces complete
bytes and masks of all101 background records in both saved cases. Candidate4
changes this expectation and keeps full-record comparisons; it also bounds
failure output for large arrays. Core and reference fixtures stay unchanged.
This evidence justifies a fourth identified round, using the same storage/time
limits and the two preflight methods, followed by the frozen132-method regression
selection. No original execution or blocked operation is involved.

The requested commit may preserve this tested integration as work in progress.
It must explicitly leave differential acceptance open: independent stage/graphics
projection, new surface-map/provider cases and remaining gameplay branches are
not established by successful neutral-call preflight or historical regressions.
Do not advance the accepted frontier on that basis.
