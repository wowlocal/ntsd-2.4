# Owned repeated loaded application cycle

Status: finite Native repeated-cycle/Bootstrap join verified; full card open. Continues the
[loaded-menu join](APPLICATION_LOADED_MENU.md) after `4f143d0` and the open
[loaded-return card](APPLICATION_LOADED_RETURN_PLAN.md).

The game behavior is the next ordinary World2 iteration after the first loaded
menu returns: preserve the current catalog, 400 Actors, interface, music and
bitmap owners; acquire input through the recovered phase cadence; use cached
menu resources; finish the new enclosing iteration. The reference is the pinned
original NTSD distribution (EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`).
Saved [MENU_CYCLE](MENU_CYCLE.md), [INPUT_CONTROL](INPUT_CONTROL.md),
[LIB_MENU_CONTINUATION](LIB_MENU_CONTINUATION.md) and their retained dependencies
supply behavioral evidence. This stage runs Native Swift against saved data;
no original execution, emulator, historical producer/auditor or refused-operation
retry is involved. Existing incidents and unsupported source faults stay open.

## Ownership

`OriginalApplicationMenuSession` now commits the loaded match, music, menu
resources and background records beside its current State and loop. The startup
record remains historical. On the next actual `step`, World2 reaches loading
before the early-screen prefix. `makeLoadedCycle` accepts only the new ticket
from the same Session revision. It refreshes current World, Actor and global
records while retaining mutable DAT frames, arenas, loaded bitmap history and
explicit arithmetic precision. The first pool parent supplies identity bindings;
its old state and operation journal are not republished.

`OriginalApplicationLoadedCycleSession` uses the existing repeated prologue and
six input checkpoints, producing a continuation tied to this fresh outer ticket.
The cached menu keeps current wrapper memory; it does not re-adopt historical
constructor records. Background constructor history may retain a dead wrapper;
current memory, surface release results and the current global pointer determine
its lifetime. `OriginalApplicationBootstrap` owns the final consumer: its session
and the caller's value-owned environment change together after the final hook.
All intermediate results remain tentative until the enclosing commit.

## Declared comparisons

The test parent starts the actual Bootstrap and follows the saved first-menu and
case47 input sequence, comparing full globals and owned allocation bytes/masks.
The accepted full Native catalog, common resources and pool comparators run on
this same parent. This does not fabricate a new Session from an expected snapshot.

Two chains use A5 and reversed/ramp menu bitmap allocations. Each chain starts
from its first loaded return, then runs neutral, key-down, held confirmation and
key-up cycles. Attack comes from the current controller configuration; the known
J mapping is a test precondition, not an engine rule. Native queue messages call
the recovered WndProc to write 100/117; this is controlled message delivery,
not physical keyboard/OS latency evidence.

The original cadence is phase0/1/0/1. Each phase0 performs two asyncSelect and two
ioctl requests even with networking disabled; their operands come from current
globals. Declared replies are -1/1/-1/0, with four output bytes for the second
ioctl. This does not open sockets. The phase1 press does not update Actor attack;
phase0 hold selects mode0/menu3, with background draw before Release/free/clear.
The subsequent phase1 release retains Actor attack and reaches the selected-body
boundary. That fourth input continuation is not a completed outer return.

All three returned cycles use zero new menu allocations, bitmap API requests or
music requests. The appended journal and all graphics commands, source colors,
resource identities and release-result history are checked by an independent
projection. The projection reads declared effects and pixel goldens, not Core
transition helpers. Final Bootstrap State is the returned menu State with only
the counter alias updated; timer arithmetic, current loaded owners, effects and
graphics are checked separately. Foreign/stale/reused tickets are rejected.

Injected failures cover prologue, second ioctl, menu screen/early return,
background free and outer time/sleep/final observer. Earlier committed input and
menu state survive, tentative environments roll back, and a valid retained ticket
can complete after a failed attempt. A separate Native ownership sentinel changes
arena perspective, frame order and release history before binding refresh. It
checks storage retention and adds no original game branch coverage.

## Gates and remaining work

Work: `build/research/application-loaded-cycle-native-20260913/`; immutable plan1,
amendments1–4, baseline pins, contract/candidate reviews, finite 51-method filter
(validation2: all prior 48 methods plus three existing WindowInput tests) and
run metadata. Limits: 40 GiB on the verified task X5 volume, 3600 seconds per Native
run, three candidates, unchanged 6 GiB internal reserve. All 383 old fixtures must
remain byte-identical; root, frozen candidate, SwiftPM packages and complete
archives must agree before acceptance.

This is a finite Native ownership composition plus retained original comparisons.
It does not establish a new whole-original application trace, backend publication,
real window/input/audio, played fight or clean-Mac acceptance. The next own join
is the already recovered character-menu handler consuming the fourth input child,
then character/arena/preparation and gameplay through actual retained outer calls.
Other modes, playback, panel IO, resource errors and real devices retain their
own required boundaries. The complete game goal remains open.

Frozen candidate3 passed all 51 bundled release methods in 310.184s, build
324.17s. The three new cycle methods executed successfully, including both
backings, mutable-owner retention and rollback. The retained WindowInput suite
also compares 4369 whole callbacks, three constructors and 385 retained calls.
All 958 current/frozen Native files, 383 unchanged fixtures and 471 packaged
files were verified. The complete Native archive was read back bytewise.
[Evidence](../evidence/application-loaded-cycle.json).

Candidate1 failed compilation before tests: the new phase assertion needed an
explicit Int32 conversion. Candidate2 compiled, but the new parent adapter
misclassified saved windowDefault replies as ordinary queue responses and omitted
the separate window replies. Its 48-method report contains 17 failures; two later
Cycle methods were reported passed after a cached static-parent error without
executing their scenario bodies. That run is not accepted. Candidate3 partitions
the saved request kinds, forwards cached pre-loading prefix observations to the
unchanged source adapter, and moves the child outside the older first-menu error
handler. A pre-freeze check also added the declared default reply for ordinary J
keydown/keyup and the three retained WindowInput regressions. Core and source
expected bytes did not change after candidate1. Both failed candidates, the static
review gaps and the separate documentation-authoring SyntaxError are preserved.
