# Active result layout from current recording output

Status: accepted in the finite scope; see [results](APPLICATION_ACTIVE_LAYOUT.md).
Frozen candidate1 plan remains immutable. Parent clean commit
`47bd85476ae598d46ca97054c215936521577835`, [recording](APPLICATION_ACTIVE_RECORDING.md).
Follow WORKFLOW, RESULT_LAYOUT, GAMEPLAY_RESULT_LAYOUT and immutable archive2375..2525.
Consumer: complete output and retained application return, then Naruto/Sasuke
District match and the full standalone native macOS game.

## Saved evidence and finite question

Read only both active48 fixtures, SHA256
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
Pinned EXE `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Saved pristine EXE/VC80 with synthetic API responses and own library-enabled
Native application are separate environments; no actual Windows/device claim.

Inventory1 checks all96 stage17 records: recording-selected422944 through
422994/SP1000e9bc, then output. Compare complete before/after references and
resolved records/masks, events/helpers/checkpoints/undefined reads, stores,
watched stack accesses and retained FPU/caller words. First observations show
indicator450b84=0 while playback44d030=1: playback is nested below the indicator
gate and must not run merely because its own flag is enabled. The inventory
must verify this across the complete corpus before Native execution.

The independent finite rule requires indicator continuation and known zero450b84;
it preserves every byte/mask and emits nothing. Nonzero indicators or result-table
entry are explicit unsupported boundaries of this finite model, not silently
accepted no-output cases. Keep599 controlled full layout matches and one separately
classified source-fault rejection, four existing rollback trials, missing formatter/
stage/target at consumption, mutable playback strings and exact event order.
No old producer/capture/auditor or original/emulator execution is authorized here.

## Own comparison and controls

Three new test-only ActiveLayout Projection/Source/Tests files. Chain the actual
owned recording snapshot into layout; calculate its expected identity only after
checking current timer's independent recording gate and current indicator flag.
Compare complete match/state/masks, replay/memory/graphics owners, resources,
music/backgrounds, menu-local and chronological journal. Reject misplaced layout
annotations and any primitive effect in its interval. Keep enclosing returned
state/graphics comparisons and add failures at layout and beforeCommit, followed
by same-session retry with exact rollback of entry and environment.

Application checkpoints do not expose Result.continuation or Body.Caller.formatter.
Static Body forwarding passes the actual recorder result to layout. Add a direct
public recorder→layout control using current owned notices input and native replay
context; compare its result with the actual recording snapshot, then pass that
actual continuation to public layout. This supplements the checkpoint gap; it
does not claim a newly exposed field or enabled formatter lifetime.

Controls reject table continuation, positive/negative indicator and unknown causal
indicator; an unknown playback/mode/elapsed/bitmap target stays unread on disabled
indicator. Public layout accepts nil stage/target/formatter on this unused path
and retains opaque372-byte formatter bytes/masks when supplied. No provider or
event may run. No source SP68/formatter backing is imported into Native.
372 is a known source extent ending before cookie5c0, not a recovered C-array size.

## Ownership, limits and acceptance

Root owns new3 Swift files, this plan/results/evidence and CURRENT_WORK/RESEARCH_MAP.
war_contract_review owns numbered independent read-only reports. Preserve all1018
old Native files,485 fixture/resource files, original expected/masks, archives,
historical failures and safety incidents. No Core change is initially needed.
Freeze actual current inputs and this plan; review the contract/candidate before
Native. Review inventory/comparator/publication independently of author's code.

Task `build/research/application-owned-active-layout-native-20260914/`, on verified
X5 UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. Initial1018 actual files pinned.
Keep40GiB external/6GiB internal reserve and40GiB task cap. Read96cases/300s;
Native3600s; three candidates before renewed contract diagnosis. Reuse parent
freeze/job/verifier/archive tools and exact XCTest log metadata separation.
Never rerun a terminal successful test for parsing or restart a quiet live job.

Select5 release methods:2 new Layout,2 retained Recording,1 controlled
OriginalResultLayoutTests. Derive inherited identities from the parent166 and
unchanged file pins; the selected2 Recording and1 Layout are already in that
set, so163 inherited+5 direct=168 unique. Expected1021 Native files,1018old exact,
383fixtures+102resources=485 packaged unchanged. Test, package, archive body/mode,
independent final review and Git delivery remain distinct gates.

After finite acceptance compare whole output and its actual return. Full active
tick, enabled table/indicator branches outside these48 schedules, helper ABI/FPU/
stores, formatter observability/lifetime, full match/game, native window/input/audio,
actual device/Windows/clean-Mac and existing safety incidents remain open.
EXE envelope is not recalculated. This methodology does not clear refusals.
