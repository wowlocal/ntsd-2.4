# Team Tournament setup

Mode3's separate menu120..125 now compares in **372 whole source/native returns
and one Start boundary before435a58**. The native dispatcher calls
OriginalTeamTournamentSetup. All373 finite calls compare retained World,
400 Actors, globals, catalog bindings, resources, library DC, ordered drawing,
text, fill, sound and RNG events. The primary sequence selects Naruto and Sasuke
as two humans in the first pair, with six Random CPUs; control uses eight CPUs.
This is the setup menu, without a played tournament or full application claim.

See [the finite plan](LIB_TEAM_TOURNAMENT_SETUP_PLAN.md),
[recorded evidence](../evidence/lib-team-tournament-setup.json),
[original producer](../../tools/oracle_lib_team_tournament_setup.py),
[independent auditor](../../tools/verify_lib_team_tournament_setup.py),
[native implementation](../../native/Sources/NTSDCore/OriginalTeamTournamentSetup.swift)
and [whole caller tests](../../native/Tests/NTSDCoreTests/OriginalLibTeamTournamentSetupTests.swift).

The reference uses pinned NTSD EXE/lib.dll/VC80 and original DAT/head/small BMP/
menu DIB resources in controlled Unicorn2.1.4 at CW023f. Actual41bc90 prologue
precedes the declared4229cc library-menu tail. Ordinary returns execute both
436fb5ret12 and422ab8ret4. Start clears the attack flag and plays its sound,
then stops before the first menu126 write. RootSP is entrySP minus a08;
originY20/originX28 and Random list40 account for pending pushes. Native retains
its own semantic locals. Original stack bytes are audited separately and never
used to supply unknown native storage. The following436fc0 Stage helper is
outside Team Tournament; the initial static listing includes it only to identify
the boundary and is not dynamic coverage.

Teams initialize to1/1/2/2/3/3/4/4. Each shuffle chooses two of four pairs using
shared RNG tags101/102, then swaps both fighter indices and both team words.
All22 shuffle calls perform50 swaps, totaling1100 pairs and2200 RNG calls.
Confirmation cancel restores pair order and team labels. Tags identify original
call sites, not separate RNG generators. All56 Random103 lists are rebuilt
from preceding state, excluding all eight live fighter choices. Random music
consumes another100 original draws. Original controller label44d320 retains its
file-backed NUL:1623 C,206 first-player and163 second-player outputs compare.
Small portraits come from all42 original resources through independent source
parsing and the already accepted native catalog loader. Team panels and pair
markers use declared atlas geometry; they compare requests and clipping, not
Windows/device pixels. Held keys, roster wrap, controller backtracking,
reselect/reroll, arena100/99/ordinary cycle, difficulties2/1/0, music and quit
are covered by the frozen input sequence. Other branch outcomes remain open.

The independent audit replays467524 stores/1853321 bytes,538300 instruction
reads/2050155 bytes and5910 API reads/32412 bytes. All6853 full source points
contain5825050 records/17521270690 bytes plus masks. It verifies21749 helper
returns, all373 atomic cases and3082 blobs/158411280 decoded bytes. Observed
original starts total2825:2439 EXE,51 lib and335 CRT. This is observed coverage,
not every branch outcome. False bitmap masks stay false; known file-backed
cookie values do not become evidence about a Windows host or process heap.

The source capture finished in83.69s without a source
fault or safety refusal. Native raw **8 tests passed in264.931s,
build275.55s**, including both Team tests and retained Tournament,
Stage and VS selection comparisons. Late third shuffle draw and controller-label
observer errors roll back the whole current call, preserving earlier calls.
Packaged **2 tests passed in71.243s, build0.36s**
without raw overrides. Existing unrelated trailing-closure compiler warnings
remain; no source result or game rule changed for this acceptance.

All281 earlier fixtures are unchanged;285 pins include the four new transport
files. Full raw1423543287-byte SHA38aaae56e4bf4c23a2bf34ea2ab3d7831cfd94b6caeaf9d83162419a1859534a,
packed bytes, decoded JSON, all699 package files and recursively archived inputs
verify. Transport deflation only packages research data. Native runtime does
not execute reference EXE/DLL code. All study jobs are terminal. Independent
catalog5 remains the same Python78863 process, last observed23
completed Objects; its prior20-Object byte reproduction remains a separate contract.
NTSDNative linked; no new app-window or device check was performed.

Next: continue before435a58 through the Team bracket, four-participant assignment,
preparation and result/winner paths, then War and the own initialized catalog/
library/outer-loop/app join. Full match/game/network, input/audio, Windows and
clean-Mac acceptance remain open.
