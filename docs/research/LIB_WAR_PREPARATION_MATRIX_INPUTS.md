# War preparation matrix: exact controlled input and lifetime boundaries

This implements the frozen [256-call success plan](LIB_WAR_PREPARATION_MATRIX_PLAN.md),
after the accepted22-call Native preflight at2469ed7. The matrix has not yet
been executed. Preserve that entire preflight and all old source/failure bytes.

The reference/environment remain pinned NTSD EXE/lib/VC80/original resources,
Unicorn2.1.4/CW023f/C locale and normal successful declared platform responses.
Memory/stack/allocation traces recover repeated arena/recording ownership and
participant order; source execution is development tooling, separate from the
Native shipping runtime. No unrelated system, control pointer, cookie or
protection structure is changed. A source fault or safeguard refusal stops its
operation and is retained; do not continue a fault or retry a blocked operation.

There are20 fresh401-constructor chains. Preserve each two-call resource/ready
prefix exactly, allowing only the parent metadata's firstCase relocation.
After these40 reproduced calls, the normal preflight key sequence performs live
Random before each initial Start. The before-third-call recording-input bridge
is exactly the accepted bound bridge, including alternating recording notice.

At first Start, `matrixInputBridge` declares only these fields:

* Fixed arenas:44d024=the declared arena;44d028=0;44d758/44d75c=the plan's legal
  multipliers. For each seat,451288+4*seat supplies status0/3/13 and the existing
  owned source Actor+364 supplies team1/2. Its Object+368 is never replaced;
  verify it still matches the actual live Random ordinal451248+4*seat.
* Random arenas:44d028=1,450bcc=the frozen index,450c34=0. Derive the index from
  the accepted seed17/controlffffffff VC80 game table. Actual range15/result0
  or14 must be observed at417170; result14 maps to99. No RNG return is hooked.
* Later generations:44d020=202,451b84=0,44d024=99/initial arena and44d028=0.
  Retain all previously produced World/Actor/recording/graphics state. These are
  controlled next-Start settings, not played-battle or ordinary UI reachability
  evidence. All Start input comes from seat0's actual Attack edge.

No call needs both the recording-input bridge and matrix-input bridge. Each
bridge records its exact bytes before normal key stimulus/call-frame setup.
The independent auditor must reconstruct the whole new before-state from the
previous full return, these declared changes and the existing ordinary inputs.
All fixed chains prepare six active Actors:three CPU and three human; skipped
seats retain their source Object selection. The two random chains retain the
preflight's eight mixed/eight human sets. Expected total:256 whole outer calls,
236 War returns,56 preparations,340 placements (168 CPU/172 human),8020 original
constructor-parent results and40 exact resource/ready parent reproductions.

Each preparation retains current device counters and all prior allocation
records. Arena wrappers start76004020+index*2000, after the two live War wrappers;
there are at most30 across load/99/reload. A released wrapper remains a dead
record with its original bytes/mask. Actual40c0e0 releases each surface before
its wrapper free; no resource is silently discarded or counter reset.

Recording generations use separate declared calloc results75000020,
77000020 and77640020, each630e18 bytes. The extra77000000..77c7ffff mapping is
research-only and disjoint from all existing regions. Original43d280 executes
its previous-recording free/clear before the new allocation. Preserve all old
bytes/masks and live flags; do not reuse a live allocation or replace an after-
state. Music reuses the preflight's exact-size allocation/normal API boundary;
the original decides whether its retained path needs a new graph.

Four additional source checkpoint sites retain overwritten numeric values:
43a4a8/43a5f2 follow the initial CPU/human floating coordinates;43a553/43a6a8
follow final position conversion. They record full owned source state/FPU and
actual seat before that instruction. These are additional observations in the
new matrix, not modifications to old expected checkpoints or a hardware FPU
claim. Their Native comparison must remain distinct from per-instruction
private-stack/ABI equivalence.

Freeze the256-entry manifest, producer, both plans, all original BG files and
other declared inputs before execution. Save every completed case atomically,
then assemble the full corpus without rewriting any prior part. Keep6GiB free.
Source-only success is not Native acceptance; Native full comparison, repeated
release/free rollback, the separate finite resource-error study, War gameplay,
own app and full game/Windows/device checks remain open.
