# Catalog5 trace storage on the mounted X5 volume

Status: the same candidate5 process continues with verified trace continuity.
This resolves growing trace allocation on the internal disk; the whole catalog
return and Native comparison remain open.

The [finite plan](APPLICATION_CATALOG_TRACE_STORAGE_PLAN.md) preserves the pinned
NTSD EXE/lib.dll/VC80 Unicorn2.1.4 experiment and its original20000-chunk,
20-Object checkpoint and6GiB internal-volume reserve. No source code, original
instruction, emulator register/memory, API response, virtual allocation, expected
byte or game rule changed. The worker was not restarted or replaced.

The mounted local APFS X5 volume had244800352256 bytes free. A new task-owned
research directory was created without inspecting or changing unrelated data:
`/Volumes/X5/ntsd-2.4-research/01a08766-6cdc-7b33-bb58-04076f1ec261/`.
The original repository pathname
`build/research/application-catalog-candidate5-resourced.traces`
now links to the same-named directory there. TraceLog stores that original
basename in its indexes, so canonical artifact paths remain valid. The volume
UUID is3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548. This external storage is a development
resource; the native app and its bundled fixtures do not acquire a drive dependency.

Python78863 was revalidated with its original start identity, parent78860 and
full command. SIGSTOP suspended that worker only; `ps` confirmed stateT and
`lsof` showed no trace file/directory descriptor, only stdin, the original log
and existing executable mappings. No unfinished temporary part was present.
All11313 closed files/1420742160 bytes were copied, compared byte-for-byte and
checked for identical SHA/mode/mtime. The existing directory was renamed to a
rollback backup and the canonical symlink published. A full read through that
path verified every file before the same worker resumed with SIGCONT.

The pause lasted7.046s, below the45s bound. Host elapsed-time logs include the
pause. The original harness uses its declared clock_next123457000, not that
host monotonic timer, for this catalog experiment. This is not a host/Windows
CPU measurement or a new source/native behavior comparison.

An independent read-only auditor checked all11304 immutable copied parts and
all nine complete saved index prefixes. It also verified264 newly appended
parts/31053103 packed bytes, including their full decoded bytes/SHA/JSON row
counts. The same PID/start identity remained active and post-resume chunks
increased767->768, stores193851829->194105552; six Objects were complete.
These observations prove continued source output, not the whole137-Object return.

Only after those checks, the redundant local backup was removed. Every old
immutable part remains accessible at its original canonical path; all old index
bytes are retained in the migration inventory in addition to their continued
prefixes. Free internal space measured8076685312->9534283776 bytes. Concurrent
filesystem activity means this is not an exact allocation accounting claim.
The original6GiB guard still measures the internal capture parent; it was not
lowered, redirected or bypassed. The final corpus/checkpoint JSON files and log
remain there. Future large independent raw captures/builds can use the X5 task
area to avoid consuming this reserve.

The [machine report](../evidence/application-catalog-trace-storage.json) pins
the plan, full inventory/index history, migration/continuity/cleanup receipts
and source inputs. The [migration tool](../../tools/migrate_catalog_trace_storage.py)
and [independent auditor](../../tools/verify_catalog_trace_storage.py) state
failure/rollback boundaries explicitly. The operation and audit completed0;
there was no refusal, source fault, input correction or process restart.

All890 candidate5 inputs, older886/887/887/890 watcher inputs,14 pending Native
files,3 pending source files,6 foreign files and all263 accepted fixtures remain
unchanged. Native code was not changed and no Native tests or app window were
run for this storage-only operation. The immutable663-file menu package and its
prior acceptance remain separate. Mode-branch work has only static findings
so far; no new mode capture or Native acceptance is claimed.

Next: whole initialized catalog completion and its full audits/Native join;
continue remaining mode consumers using isolated development storage meanwhile.
The full match/application, Windows, devices, all content/network and clean-Mac
requirements remain open.
