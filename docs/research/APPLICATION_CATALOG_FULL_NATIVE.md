# Complete catalog owned by the native application

Finite Native composition from the actual bootstrap/menu/common loading entry
through all 137 Objects, 17 ordinary backgrounds and Stage data to `PendingPool`.
This card uses an explicitly declared platform environment. It does not establish
an original whole-application return, a real device result or a complete game.

The preceding [owned session](APPLICATION_CATALOG_SESSION.md) retains its three
source comparisons through 20 child returns. The [full input preflight](APPLICATION_CATALOG_FULL_INPUTS.md)
established the original resources and the boundaries of the older controlled
full catalogs. No original executable, emulator or historical producer/auditor
was run for this card. Existing safety incidents remain open.

## Inputs and implementation

The pinned original NTSD EXE is SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
VC80 CRT is `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Existing bundled fixtures provide 156 DAT/registry files, 365 unique WAV files and
669 original images (665 BMP and four embedded DIB). The complete text-translation
catalog and 400 saved WAV calls provide comparison-only records and masks.
No expected after-state becomes a runtime input; all 379 old fixtures are unchanged.

`OriginalApplicationCatalogSession.load(resources:makeControls:)` separates
immutable resources from an attempt-owned provider of allocation, bitmap, file,
WAV, volume, clock and message replies. The existing finite-array interface adapts
to a fresh provider and retains its exhaustion/unused-control checks. Existing
shared game handlers, memory ownership and rollback remain in use. Captured bitmap
structure writes remain rejected. `finish` runs before publication; its throwing
boundary is supported by ordering, not claimed as an executed dedicated test.

The test starts with the existing own bootstrap/menu/common construction. Catalog
storage uses logical token `70000020`; Object and game child allocation tokens
retain the old nonoverlapping values, checked against all live owned ranges.
New images/surfaces/memory DCs/surface DCs use separate declared `7a/7b/7c/7d` ranges.
Original resources supply headers, dimensions, RGB and defined masks. File buffers
are declared separately from the old opaque stream handles.

The old sound adapter reused its PCM mappings on every call. Here each of the
666 returned PCM regions has a fresh aligned slot in `78000020..78df6360`, and
400 logical sound buffers use `79000000+16*i`. Only typed operands of each WAV
call are rebound: stream, actual owned device, sound buffer and PCM pointers.
Original PCM bytes/masks, scalar replies, destination slots and their low format
words are preserved. No Windows heap or device identity is inferred.

Clock replies are `123457000+20*n`; messages are empty `PeekMessageA` responses.
These are declared Native controls. The older full catalogs do not contain the
complete original application clock/message/animation sequence for this entry.

## Comparison and retained backing

The comparator checks all 155 child returns, decoded data, checksums relative to
the actual entry checksum, complete 137 Object/400-frame records, 101 background
records and 60 Stage records. It checks all 829 bitmap records, 14,586 Frame
allocations and 130 weapon strings, all four parent regions in both published
representations, 621 closed streams, temporary-file cleanup and 400 complete WAV
results. The independently fixed parent domain is
`0:7d0, 4d81060:990, 4d819f0:990, 4d82380:28`.

All 15,683 final allocation kind/token/count/order tuples, logical Object/bitmap/
surface bindings, WAV platform inputs and 400 published buffer globals compare.
A second journal is assembled from typed observation/control callbacks and
compared to the complete published operation sequence. The graphics projector
then independently checks all commands, bindings, rectangles, colors/masks,
resource generations and final owners. It uses saved original pixel goldens,
not Core graphics transitions or pixel-copy code to manufacture expected state.
This proves Native callback/operation/command composition under declared controls;
it cannot detect missing original behavior absent from both Native observations
and the unavailable full original application trace.

The first running comparison incorrectly equated the whole sound-cache span with
the old controlled zero-backed catalog. The actual entry retains other globals
inside that span. The original contract copies each full `path + NUL` at stride20;
it does not clear the remaining bytes. Independent data-only replay reproduces
all 400 original pre-call caches, 155 child caches and the final cache before the
same exact writes are composed with the own entry. There are 8,032 written bytes,
7,412 distinct offsets and 4,364 retained offsets, including 588 before offset8000.
The last write ends at7993. Splitting the cache at8000 would be incorrect.
Every composed byte remains compared; the old expectations and failed run remain.

## Validation and preservation

All **24 bundled release tests passed in 191.186s** (build 312.21s), without raw overrides. `NTSDNative` linked. The runner, frozen manifest and terminal log are preserved; all study job PIDs were checked absent.

Three new tests cover complete publication, cancellation after the final Stage
child, and cancellation immediately before publication followed by a successful
attempt using the same factory and a fresh provider. All four attempts reach the final child. Three reach
`beforeCommit`, and two publish `PendingPool` successfully; the Stage cancellation
stops before the complete catalog returns. All 21 prior selected catalog/files/sounds/precision/loading
methods remain required regressions. No played fight or backend publication occurs.

The first freezer's incorrect review-key lookup failed before candidate creation;
its producer/error remain. Native1 stopped at two new-test compile errors with
zero tests. Candidate2 compiled, then recorded cache mismatches. A one-second
read-only profile showed XCTest eagerly formatting the large nonnil PendingPool;
the failed run was explicitly interrupted and reaped, not counted as a completed
24-test run. The final candidate uses equivalent Boolean presence assertions.
Frozen candidate3 was never executed; candidate3b retains its comparator and only
changes these diagnostics. All four frozen copies and separate corrections remain.

All 916 current Native files (4,433,363,128 bytes), four frozen copies,
379 unchanged fixtures, 46 startup resources and 19 common-sound resources were
verified, including SwiftPM bundle bytes. All 24 plan/amendment inputs
remain exact or have their original Git-base bytes preserved. The final Native
archive has 916 members; the evidence archive has
85 members. Every archived name, byte, SHA and
mode was read back and compared. Earlier candidates reconstruct from the full
archive plus explicit overlays. Independent code/comparator review found no
material blockers. [Machine-readable evidence](../evidence/application-catalog-full-native.json).

Work records and commands are in
`build/research/application-catalog-full-native-20260913/` on the verified task-owned
X5 volume. The initial24GiB preservation budget was explicitly extended to32GiB
to retain the unexecuted freeze and final archives; the6GiB internal reserve and
three Native validation rounds were unchanged. Shipping resources stay local.

## Open continuation

Join the returned owned catalog to the existing pool/UI/loading continuation,
then the application outer loop and backend publication. Use saved accepted
inputs first; do not restart completed original captures. The complete original
application return, later drawing of newly loaded catalog surfaces, actual raster/
Windows/device behavior and full game remain open. Existing resource/message/
fill/mirror failure boundaries and safety incidents are not closed by this card.
