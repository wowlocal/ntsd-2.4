# War preparation: Native preflight, not the full preparation milestone

The separate [bound-input study](LIB_WAR_PREPARATION_INPUTS.md) completed22
original outer calls,20 War calls and two preparations. Its192093188-byte raw
corpus has SHA256
`e3abb5dec759c28103c5a746972d7fb1469e7fa7b8ca151a09fc952e6d5621f6`.
Full audit1 reconstructs444 points/475960 records/1359294944 bytes and masks,
423800 stores,266876 instruction reads,5389 API reads and6659 helper returns.
All4406 observed original instruction starts match the pinned EXE/lib/VC80
bytes. This is neither every branch outcome nor Windows/device execution.

The four accepted source2 resource/ready calls and both401-constructor parents
reproduce exactly. The two explicit recording-input bridges are new controlled
inputs, not recovered full application startup. All original recording operands
and both450be4 branch reads are defined. Control additionally formats the real
`Start recording` notice after preparing its recording buffer. The completed
unbound capture3 remains immutable with finite acceptance false; its six unknown
recording inputs and unknown450be4 are not Native input defaults.

## Declared comparison

Reference artifacts remain the original NTSD EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`, original
resources and controlled Unicorn2.1.4/CW023f/C-locale observations. All245
original BG files are included in the new capture's frozen inputs. Source3's
older pin set did not separately include them; its raw asset observations and
subsequent file comparison stay separate evidence.

OriginalWarPreparation composes43a21f..43a769 with retained owned World/Actors,
background layers, selected music, input reset and OriginalReplayRecording.
Actual memory/register/stack traces are needed to establish CPU seat+10 versus
human seat, Object/team preservation across4061d0, pending root2c/root38 and
intermediate integer/binary64 writes. Native uses its own semantic references;
private source stack/cookie backing is audited, never supplied as Native state.

The first Native acceptance set is these same22 calls, all444 source points
with Native-owned game fields compared where exposed, full before/after
World/400Actors/globals/101BG and owned bitmap/music/replay records and masks,
ordered events and complete source parents. Full original stores remain in
the source audit; Native comparison is at the declared public checkpoints and
helper/event boundaries, not a per-instruction Native write or x87-status trace.
Require16 participant placements
(6CPU/10human),20 arena bitmap allocations, two30-byte music allocations,
two full630e18 recordings and actual ret1c/ret4 composition. Preserve temporary
X350 versus400, human Y−50 then0, distinct RNG125/127, activation clear before
the team pass and44d034 before450bbc. Native constructors/resources are rebuilt
from the accepted137Object/101BG catalog, with original observations used only
for comparison and declared platform/input boundaries.

Require12 source-coupled late observer rejections: each preparation at its
first completed bitmap, after all participants, at music RenderFile, replay
allocation, completed recording and before the outer return. Compare complete
state/owners/environment against pre-call values after each rejection. These
are Native rollback trials, not additional successful original calls or source
allocation/COM failures. The first successful-call source corpus has normal
allocation/COM responses only. A missing live release owner must remain an
explicit error; first-load evidence does not verify release/reload composition.

The isolated package is the745-file accepted War setup package plus seven owned
new/changed files,749 files total. The23 unrelated pending files are excluded
and hash verified unchanged. Do not claim a passing comparison before the
terminal job/log establishes it. Record compilation/test errors and corrections
without changing source expected bytes or masks. Preserve every completed
capture, source/tool failure and frozen input set; never restart for silence.

## Remaining acceptance

These two first preparations are a preflight. The next finite source matrix
must cover all17 original arenas plus99, fixed/random selection including the
last-choice-to99 mapping, actual layer release/reload, sparse CPU/human seats,
both teams/multipliers, recording metadata/notice and ordinary relevant resource
errors. Declare its inputs and measurable end conditions before execution.
Mode-pointer1 and out-of-War team values need reachability evidence or an
explicit separate controlled-helper boundary; they are not proven War paths.

War43a860 gameplay, full own catalog/library initialization/outer loop/app,
full Naruto/Sasuke District match, all content/network/replays and window/input/
audio/Windows/clean-Mac verification remain open. The native-window availability
probe in this turn returned `Sky Computer Use native pipe startup failed`;
no app action or device test was performed. This does not block independent
source/Native work and is not a safety refusal.

## Preflight result

The22-call Native comparison passed, including all12 corrected late rollback
trials. Raw test3:17.241s/build117.53s. Final normal-resource release run passed
three tests in438.190s/build0.30s: new preparation17.199s, retained574-call War
setup363.318s and retained210-call Team Tournament preparation57.673s.
This retains the earlier default BEFORE43a21f contract when no preparation
continuation is connected. No source expected byte or Native game rule needed
correction to make the22 comparisons pass.

First compilation rejected an optional escaping adapter capturing non-escaping
callbacks. The synchronous War stage now receives staged presentation/music
owners directly; its failure log and original inputs remain saved. Test2 passed
22 calls and12 rollbacks but its two music predicates selected an earlier
method with offset34. Test3 requires actual RenderFile with the own converted
2c020020 buffer. The earlier successful result is retained with this limitation.
A one-second read-only profile confirmed that the live packaged process was
working in the retained War comparator/JSON processing; it was never restarted.

Two fixture transports total22891497 bytes and independently reconstruct every
192093188 original byte, complete JSON/audit and all1032 blobs.316 earlier
fixtures are unchanged;318 current fixtures and all751 Native package files
match their frozen hashes. All unchanged files match the745-file accepted Git
base, with seven owned code changes and two new fixtures. The23 protected
pending files are unchanged. The full package archive is2566938927 bytes,
SHA256 `f12996cbd259502a563901ac73bb5b6389ad9ebc5bf7c36cbe80c66d9e453d8d`,
and each of its751 members was independently read and checked.

Both current source input-pin sets remain unchanged:405 for unbound capture3,
653 for the separate bound capture. All source/SwiftPM/archive jobs are terminal.
NTSDNative linked; no app window, device or Windows execution was established.
The [machine-readable evidence](../evidence/lib-war-preparation-preflight.json)
records the distinct source/Native/artifact contracts and retained failures.
The [256-call success matrix](LIB_WAR_PREPARATION_MATRIX_PLAN.md) and ordinary
resource failures remain required before the whole preparation stage is complete.
