# Initial input/round entry with native loading ownership

Connect the actual OriginalInitialLoading result to41c581 local input,
control/received commands, replay bookkeeping and the round dispatcher. Retain
its UI, catalog, common/registered WAV owners, pause and both command buffers.
Keep the arithmetic context and saved-playback/early-resource ownership supplied
by their actual caller; do not infer them from source after-state. Stage caller
platform bookkeeping until the entire entry and final observer succeed.

Reference: unchanged MENU_STARTUP A5/ramp controlled EXE/VC80 chains through
4229cc and their following music/resources through429e5a; unchanged LOCAL_INPUT
two303-case corpora and parent loading resources. Pinned EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c
and VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
original DAT/BMP/WAV resources, controlled Unicorn environment. No new original
execution or expected data changes. These older captures retain their declared
CRT/device/clock boundaries and do not execute the current own installed-library
application catalog chain. Their pre-first-menu458588..4588a7 bytes are declared
initial input, not recovered private stack or a new application default.

Low-level evidence used: retained full World/400Actor/globals/masks, ordered six
input/round checkpoints, command buffers, helper calls and real ret12 witnesses.
They distinguish first-entry composition from rerunning the loading prologue
or choosing a round continuation in advance. The natural source path is phase1,
unpaused/menu10; other branches retain their separate input/round study scopes.

Finite acceptance: both complete MENU_STARTUP controls retain every existing
comparison through the new public entry, then existing music/resources. Both
LOCAL_INPUT corpora retain303cases and the previously dropped native UI owners
through every case. Native-only final/late-checkpoint failures must leave caller
context and input loading resources unchanged and publish no new entry result.
Observe full retained wave/bitmap bytes and masks, not just counts. No new source
branch coverage is claimed for these ownership/rollback checks.

Do not touch live source producers, frozen40 package/binary/resources or its886
pins, existing fixtures, pending14catalog files or six foreign files. Full own
catalog/pool/UI/41bc90, app/window/input latency/sound, Windows, full match,
all content and clean-Mac acceptance remain open.
