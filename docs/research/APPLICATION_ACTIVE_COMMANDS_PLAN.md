# Active commands/recovery comparison

Status: implemented and accepted for both finite48-call schedules; see
[results](APPLICATION_ACTIVE_COMMANDS.md). The frozen candidate plan remains
immutable in the evidence archive. Parent: accepted
[active lifecycle](APPLICATION_ACTIVE_LIFECYCLE.md).
Parent clean commit: `c821b9128f440d9e3e1c7fa80d995854e5ec9add`.
Read [WORKFLOW](WORKFLOW.md), [POSTDRAW_COMMANDS](POSTDRAW_COMMANDS.md),
[GAMEPLAY_COMMANDS](GAMEPLAY_COMMANDS.md), [LIB_STAGE_COMMANDS](LIB_STAGE_COMMANDS.md)
and the command constraints in the immutable AGENTS archive.
The consumer is the remaining HUD/notices/recording/layout/output sequence,
a complete Naruto/Sasuke District match and the full native game.

## Contract and evidence

Compare the whole400-slot command/recovery pass `4214d5..421a15`, source
SP`1000e9bc`, on both retained48-call schedules. Start each own comparison from
its actual lifecycle checkpoint in the accepted Bootstrap/input/control/physics/
contacts/graphics/lifecycle chain. The saved pristine source is evaluated first
from its own records. Source after-state and source event positions never supply
an owned expected value.

The reference remains the pinned original EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
VC80 SHA256`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Both saved active fixtures retain hashes
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
Their synthetic pristine EXE/VC80 environment is distinct from actual Windows
and the owned library-enabled application. The bundled library SHA remains
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`;
its command3/requested-ID behavior is a retained separate compatibility rule.

A new bounded saved-data reader reuses only Corpus/blob/known-byte primitives
from `tools/inventory_active_lifecycle_provenance.py`; its old inventory/main is
not called. New report `inventory1.json` in the task area covers96 stage13
endpoints. Both command flags and mode are0, E0/E4 timers are0, current Frame
state is never1700 and only slots0/1 are active. The whole stage has no recorded
events/helpers/boundaries/checkpoints/effects or retainedSP+34 access. Full
before/after records are equal. Its repeated cleanup writes are established by
the existing command studies; this active corpus has no per-instruction write
array. Do not relabel equal snapshots as new instruction-store evidence.

## Required comparison and controls

Independently derive these finite current-input results:

- Scan all400 current activity flags in order, preserving inactive allocations.
- Expose unsupported command/refill/mode, healing and state1700 branches before
  claiming a match; those limits do not restrict production game behavior.
- For each active slot write1000 toActor+2e8/+2ec/+2f0,0 to+2e4 and only byte+EB.
  Preserve adjacent bytes, all other backing/masks and command flags. Do not
  infer these writes solely from the unchanged after-state.
- Compare full World/400Actor/global records/masks, current Object/Frame inputs,
  resource owners, aliases, library/music state, local record and journal.
- Reject any primitive gameplay/front event in the scoped lifecycle→commands
  interval. Compare the saved absence of all event families/helpers/boundaries,
  all available source component joins and no retainedSP+34 access.
- Retain the complete returned command-extent/owner checks in the graphics parent
  and late lifecycle/outer rollback checks. Verify the command endpoint survives
  composition without erasing the inactive transient allocation.

Current-input comparator controls must distinguish an omitted cleanup, clearing
the entireE8 word, missing defined-mask writes, inactive storage erasure,
unknown current Frame and enabling an unsupported command/timer/state branch.
These controls change the comparison-only model, not frozen original cases or
live application input. They cannot manufacture new source branch evidence.

The accepted neutral projection and production `OriginalPostDrawCommands` may be
read as context but cannot supply expected after-state. Use the existing record
utilities and a small comparison-only current-input projection. If a production
mismatch is discovered, retain it and identify separate evidence before Core edits.
Middle source Object/Frame snapshots, full ABI and all instruction stores remain
outside this corpus's available observations and stay explicit limitations.

## Ownership, execution and delivery

Root owns new ActiveCommands projection/source/tests, optional callback forwarding
in ActiveLifecycleTests, this study/result/evidence and CURRENT_WORK/RESEARCH_MAP.
`war_contract_review` owns separate numbered read-only review reports. No fixture,
expected, mask, resource, archived failure or historical producer is changed.

Task storage: `build/research/application-owned-active-commands-native-20260914/`
on X5, APFS UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`.
`initial.json` pins all1006 actual Native files at the clean parent. Keep40GiB
external/6GiB internal reserves and40GiB task cap. Bound saved readers to96calls/
300s each, Native jobs to3600s, and three candidates before diagnosing/revising
an incomplete contract. Reuse freeze/job/verifier/archive tools from lifecycle.
Never restart a live or completed job for silence or publication formatting.

Initial release selection: both new ActiveCommands methods, both retained
ActiveLifecycle methods and four existing OriginalPostDrawCommands methods, with
no raw fixture overrides. The3898 saved controlled inputs and existing rollback
checks independently exercise non-idempotent production cleanup/timers, complementing
the idempotent active observations. Changed forwarding is
exercised transitively. Expected1009 frozen Native files,1005 old unchanged,
145 unaffected methods inherited by exact pins plus8 directly selected methods.
Native tests,485 package-file checks, full archive readback, independent review
and Git publication remain separate gates. Archive rejected candidates and exact
errors without adjusting source expected values or excluding mismatches.

No original/emulator/old producer/capture/auditor execution is needed or authorized
by this card. Existing safety incidents remain open; no retry or rerouting of an
affected operation. After acceptance continue HUD, then notices/recording/layout/
output. The full active tick, broader command/healing/gameplay branches, complete
match/game, app/window/input/audio integration, actual device/Windows/clean-Mac
acceptance remain open. The EXE envelope is not recalculated.
