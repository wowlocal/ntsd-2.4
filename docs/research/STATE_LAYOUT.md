# R02.1 — состояние и инициализация

Эталон: EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
**Частичный результат R02.1.** Конструкторы Actor/World проверены отдельно;
инициализация целого матча и полный словарь типов ещё не восстановлены.
Исходные байты неизвестных полей сохраняются, но это не раскрывает их назначение.
Следующий проверенный результат — [BOOTSTRAP.md](BOOTSTRAP.md): загрузочный пул
400 Actor с повторной инициализацией восьми слотов, нормализованные ссылки и
статические границы всей аллокации каталога. Это отдельно от появления игроков.
Затем проверен [родительский проход каталога](CATALOG_REGISTRY.md): разреженные
записи, порядок реестра и обращения к ещё не исполняемым дочерним загрузчикам.
Он также не является инициализацией выбранного матча.
Отдельный [Object loader](OBJECT_LOADER.md) теперь сохраняет настоящую загрузку
объявленных целых файлов и сравнивает её с native Core. Прежний сырой снимок
такта ниже по-прежнему использует синтетическую загрузку; его границы не изменены.
[BACKGROUND_LOADER.md](BACKGROUND_LOADER.md) добавляет точные записи/маски всех
17 BG и отдельный жизненный цикл слоёв. [STAGE_LOADER.md](STAGE_LOADER.md)
добавляет весь исходный Stage-файл и маски всех 60 записей таблицы. Эти отдельные
загрузчики описывают отдельные исторические corpora. Позднейший
[LOADED_CATALOG.md](LOADED_CATALOG.md) уже соединяет их в полный каталог;
[MATCH_PRELUDE.md](MATCH_PRELUDE.md), [MATCH_PREPARATION.md](MATCH_PREPARATION.md)
и [REPLAY_INITIALIZATION.md](REPLAY_INITIALIZATION.md) проверяют затем цепочку
World/Actor → пролог → подготовка → запись до `42d704`.
[MATCH_CONTINUATION.md](MATCH_CONTINUATION.md) добавляет дальнейшее меню до ret12.
[RANDOM_INITIALIZATION.md](RANDOM_INITIALIZATION.md) переносит CRT seed/state
и генерацию таблицы перед этой цепочкой. [MAIN_MENU.md](MAIN_MENU.md) добавляет
mouse WndProc, пять пунктов меню и сетевой init.
[MENU_PRESENTATION.md](MENU_PRESENTATION.md) добавляет общий tail/ret4,
следующий dispatcher World=1→2 и volume/overlay/present/shutdown до OS.
[INITIAL_LOADING.md](INITIAL_LOADING.md) соединяет первый41bc90..41c581,
а [LOCAL_INPUT.md](LOCAL_INPUT.md) продолжает его до41c5e5 и419a60/ret12:
полные globals/World/400 Actor, current+cd..d3/previous+c6..cc, два вида
упаковки команд и отдельный dispatch хвоста. Это ещё не OS polling, AI bodies
или полный игровой такт. Старые корпуса сохраняют исходные границы.
Остальные решения меню,
промежуточные потребители CRT и полный такт остаются открытыми.

## Конструкторы: установленные записи

S: Actor `0x4061d0..0x4064cc`, World `0x419e40..0x419e5f`.
D: [oracle_state.py](../../tools/oracle_state.py) исполняет обе функции через
возврат на четырёх шаблонах памяти (`00`, `a5`, `ff`, последовательность байтов)
по двум адресам. Единственная подмена — `memset` в `0x4450a0`; её записи включены
в маску с адресом возврата вызывающего кода. Проверяются защитные байты до/после
области и сохранение каждого нетронутого байта.

[Отчёт](../evidence/state-constructors.json) хранит адреса и байты всех записей,
включая повторные, и SHA-256 полного корпуса. Swift
[OriginalStateRecord](../../native/Sources/NTSDCore/OriginalStateRecord.swift)
совпал во всех **16 случаях: 24 512 байт и столько же флагов инициализации**.
Это проверка конструкторов, не запуска матча или появления объекта.

### Actor

Аллокация — **0x420 / 1056 байт**: `0x41c070` передаёт размер в `operator new`
по `0x41c075`; конструктор вызывается в `0x41c091`. Шаг `0x500` между Actor
в стенде не является размером структуры оригинала.

Конструктор записывает **913 байт**, оставляет **143 байта** нетронутыми.
Все диапазоны далее полуоткрытые: последний адрес не включён.

| Область | Конечное значение конструктора / представление |
| --- | --- |
| `+0x28,30,38,40,48,50` | По 8 байт: binary64 `0x3fb999999999999a` (0.1), из `0x447920` через x87 |
| `+0x58,60,68` | По 8 байт: положительный binary64-ноль |
| `+0x2e8,2ec,2f0` | По 4 байта: 1000; `2e8` сначала получает 0, затем 1000 |
| `+0x2f4,2f8,324,328,32c,33c,360,3f8` | По 4 байта: `ffffffff`; знаковое представление -1 |
| `+0x2fc,300,304,308` | По 4 байта: 500 |
| `+0x354` | 4 байта: 99 |
| `+0x3fc,400` | По 4 байта: `fffffc18`; знаковое представление -1000 |
| `+0x24..<28`, `81..<84`, `bd..<be`, `dd..<e0`, `31c..<320`, `368..<36c`, `370..<3e8`, `41c..<420` | **Нет записей**. Нельзя считать все эти области padding или нулевыми полями |
| Остальные байты `+0..<420` | Записывается ноль; ширина и адрес каждой операции — в отчёте |

Конструктор возвращается с EAX=-1000; это остаток инструкции, не указатель
`this`. Swift переносит состояние памяти, а не регистровую оболочку вызова.
После конструктора исходный цикл `0x41c096..0x41c0c1` отдельно задаёт координаты,
Object* в `+0x368`, указатель слота World и активность 0. Это ещё не все пути
появления. Нейтральные позиции/нулевые скорости старого стенда и `FighterState()`
— условия практики, **не** значения исходного конструктора или нового матча.

### World

Статический адрес обычного World — `0x458b00`. Инициализатор `0x446300`
передаёт его в ECX и прыгает в конструктор через `0x446305` (S; полный CRT-startup
здесь не исполнялся). D проверяет сам конструктор на тестовых адресах.

| Смещение | Размер / назначение | Инициализация |
| --- | --- | --- |
| `+0` | 4 байта, селектор диспетчера | Конструктор пишет 0 в `0x419e4e` |
| `+4` | 400 байт активности слотов | `memset(...,0,400)` в `0x419e54` |
| `+0x194` | 400 указателей Actor, **4 байта** каждый | Конструктор не пишет; цикл матча заполняет отдельно |
| `+0x7d4` | 4-байтный указатель каталога | Конструктор не пишет; назначается при загрузке |

Известный префикс заканчивается на `+0x7d8` (2008 байт). Его размер **не доказывает
sizeof(World)**. Конструктор записывает только 404 байта. PE-загрузчик отдельно
задаёт содержимое статической памяти/BSS; BSS-ноль не является доказательством
окончательного значения при начале боя.

## Поля практики и их исходное хранение

Таблица сопоставляет **все поля FighterState и его подмножества MovementState**.
Смысл и числовые операции проверены только в областях
[движения](../MOVEMENT.md), [боя](../COMBAT.md), [снарядов](../PROJECTILES.md).
`Int32` ниже обозначает используемый знаковый вид 4 байт; это не доказательство,
что все ещё неизвестные читатели поля всегда применяют ту же знаковость.
Swift `Int` в практике шире исходного dword; общая реализация должна сохранять
конкретные исходные операции переполнения и преобразования.

| Swift / смысл | Исходное место и вид | Читающие/меняющие стадии; дальнейшая работа |
| --- | --- | --- |
| `phase, tap` | Actor `+0,+4`, Int32 | `413080`, движение/ввод R04/R05 |
| `ix, iy, iz` | `+10,+14,+18`, Int32 | `40e490`, преобразования координат и геометрия R05/R07 |
| `frame, previousFrame, collisionFrame` | `+70,+74,+7c`, Int32 | Управление, физика, контакты, `40d960`; не сливать разные кадры |
| `wait` | `+88`, Int32 | Планировщик `40d960` R05 |
| `hitVX, hitVY, hitVZ` | `+28,+30,+38`, binary64 | Разрешение попаданий `42e100`, применение `4196f0` после рисунка R08 |
| `vx, vy, vz` | `+40,+48,+50`, binary64 | Управление/физика R05 |
| `x, y, z` | `+58,+60,+68`, binary64 | Физика, ограничения, связанные объекты R05/R09 |
| `facing` | `+80`, 1 байт | Управление и отражение спрайта; не читать как dword с соседними нетронутыми байтами |
| `attackBuffer, jumpBuffer, defendBuffer, defendCooldown` | `+be,+bf,+c0,+c1`, по 1 байту | `413080` и приоритеты R04; хранить исходные байты, включая знаковые операции |
| `rightBuffer, leftBuffer, upBuffer, downBuffer` | `+c2,+c3,+c4,+c5`, по 1 байту | `413080`, двойное нажатие/комбо R04 |
| Ветка удерживаемого оружия | `+98`, Int32 | Целый413080 выбирает обычные,2,4,6 и `kind%100==1` переходы; это не source ID. D [ACTOR_CONTROL](ACTOR_CONTROL.md) |
| `combos[0..<9]` | `+d4..<dd`, 9 байт | Распознавание/переходы R04; старый JSON снимок не доказывает семантику всех значений |
| История фронтов | `+408..418`, пять Int32 | `40e450` сдвигает клетки на каждом фронте; кодыR/L/U/D/defend/jump/attack=6/4/8/2/9/0/5. D [ACTOR_INPUT](ACTOR_INPUT.md), полные байты/маски |
| `fall, freeze, guardDamage` | `+b0,+b4,+b8`, Int32 | Бой, hitstop, восстановление R05/R08/R10 |
| `rest, vrest` | `+ec`, Int32; `+f0..<280`, 400 знаковых байтов | Контакты/сроки повторного попадания R07/R08. Два элемента старого melee — ограничение снимка |
| `superPunch` | `+ea`, 1 байт | Бой/приоритеты R04/R08 |
| `hp, redHP, mp` | `+2fc,+300,+308`, Int32 | Урон/стоимость/восстановление R08/R10; исходные стадии R01.1 шире практики |
| `hitCount, fallHurt, invulnerability` | `+20,+320,+8`, Int32 | Реакции/защита/время жизни R08/R10 |
| `damageDealt, damageReceived, kills` | `+348,+34c,+358`, Int32 | Статистика/урон/завершение R08/R10/R15 |
| Расход чакры | `+350`, Int32 | `40e2d0` прибавляет знаковый `Frame.mp%1000`, `+34c` получает HP-часть. D [ACTOR_INPUT](ACTOR_INPUT.md), включая32-битное переполнение |
| `cameraX, cameraVelocity` | Globals `450bc4,450bc8`, 4 байта | `41b5d0`; общие для мира, копии в FighterState — формат старого снимка |
| `renderFrame, renderFacing, renderX, renderY, renderZ` | Снятые перед планировщиком `frame,facing,ix,iy,iz` | Производная поза вывода R13; отдельных Actor-полей для этих копий не установлено |
| `sounds` | Список событий стенда/нативного аудиовыхода | Производный вывод вызовов `416fb0/417090`; не полный DirectSound state R14 |

Прочее состояние нативного ядра:

- `OriginalFighter.previousInput/currentInput`, `OriginalMovement.previousInput`
  и `OriginalMelee.currentInputs` — упакованные маски стенда. У Actor есть две
  последовательности входных байтов `+c6..<cd` и `+cd..<d4`; исходные фазы и
  источники ввода ещё требуют R04. Это не перенос полного глобального ввода.
- `OriginalFighter.mpSpent` и `MeleeState.mpSpent` соответствуют Actor `+350`;
  `ProjectileState.owner/team` — `+354/+364`. `slot` — индекс World, `id` —
  Object `+6f4` через Actor `+368`, `motion` — тот же набор полей Actor.
- `MeleeState.actors/projectiles` и `OriginalMelee.fighters/objects` — разделённые
  контейнеры практики. Оригинал использует общий пул 400 Actor. `draws`,
  `projectileDraws`, `ProjectilePose` — производные снимки поз и порядка рисунка.
- `OriginalRandom.table/index/counter` — `44ff90` (3000 байт), `450bcc`, `450c34`
  (по 4 байта), функция `417170`. `source/sourceSHA256` — происхождение фикстуры,
  не поля игры. Копии RNG в MeleeState — формат сравнения.
- `localPlayer` — выбранный игрок практики; не прямой перенос всех восьми
  входных слотов `450b4c`. `enablesChidoriNeedles` — ограничитель прототипа,
  не поле оригинала. Их нельзя переносить в общий движок как правила персонажа.
- `frames`, `parameters`, `voices`, `projectileCatalog`, `ProjectileDefinition`
  и `OriginalBackgroundLayer.fields` — загруженные определения/частичные
  каталоги. `OriginalFrameRecord` сопоставлен отдельно в
  [FRAME_LOADER.md](../FRAME_LOADER.md); целые Object/Catalog — R03.1.
- `OriginalMovement.width/zMin/zMax` — параметры текущей арены, взятые из DAT.
  `OriginalBackgroundLayer.counter` — dword каталога
  `+4d465d4 + arena*990 + layer*4`: чтение/знаковый остаток/запись
  `41a34c..41a35a` (S, D результата District в прежнем корпусе). Инициализация
  всего массива ещё требует внешнего загрузчика R03/R13.
- `OriginalClock.baseline` — UInt32 времени, исходно сохраняемое в ESI внешнего
  цикла (`43d157`), не поле Actor. Optional/reset — оболочка нативного запуска.
- `OriginalStateRecord.bytes/defined` — новая схема хранения/происхождения,
  не дополнительные игровые поля. При чтении используются явные UInt8/Int8,
  UInt16/Int16, UInt32/Int32 или raw binary64; нет преобразования указателей
  оригинала в адреса Swift. Эта схема пока не подключена к игровому циклу.

### Поля, которых ещё нет в общем нативном состоянии

| Область Actor | Что установлено / кто продолжает |
| --- | --- |
| `+c,+1c,+78,+84,+8c..<b0,+bc` | Записи конструктора известны; назначение всех полей/читателей не закрыто. R02/R05/R09 |
| `+e0,+e4,+e8,+e9,+eb` | Таймеры/флаги, часть условий в cleanup `4217b0..421a0f`. R02/R07/R10 |
| `+280..<2d0`, `+2d0..<2e4`, `+2e4` | 20 dword + 20 byte + dword; контактные массивы/счётчик. Ширина установлена, полная интерпретация — R07/R08 |
| `+2e8..<2fc` | Контактные значения/ссылки со значениями 1000/-1; полная семантика и срок жизни — R07/R09 |
| `+304` | Ресурсное поле, default 500; места использования в R10, не подменять постоянно текущим HP |
| `+30c..<348`, `+35c,+360,+36c` | Конструкторные записи/пропуски известны; `+31c` получает Object `+90` при загрузочной инициализации первых восьми слотов. Остальные связи/выбор цели/запросы требуют R02/R09/R10/R12 |
| `+3e8..<41c` | Defaults известны, назначение всех полей не установлено; R02/R06/R11/R12 |
| Нетронутые диапазоны, в том числе `+370..<3e8` | Сохраняются целиком; их отсутствие в наблюдавшихся чтениях не доказывает ненужность |

### Полный сбор контактов и слияние

D [WORLD_CONTACTS](WORLD_CONTACTS.md): весь419380/417200/417400/4064d0,
7925 управляемых случаев и оба собственных запуска матча до41eefb. Следующие
значения описывают использование в этих функциях, не всех читателей EXE.

| Actor field | Использование |
| --- | --- |
| `+70,+78,+7c`, Int32 | Три разные ссылки Frame: текущий, отдельный кадр для effect2/20/21, сохранённый префиксом collisionFrame. Не объединять с previousFrame74 практики |
| `+ec`, Int32 | arest: >0 блокирует417200; префикс419380 условно сбрасывает0 |
| `+f0..<280`,400 Int8 | Положительный vrest у обоих участников уменьшается перед обеими проверками активной пары; отрицательные байты сохраняются |
| `+280..<2d0`,20 dword | Номера слотов выбранных целей; nearest пишет первый, multiple — индексcount |
| `+2d0..<2e4`,20 byte | Соответствующие номера ITR; записывается младший byte исходного индекса |
| `+2e4`, Int32 | Счётчик контактов; multiple требует<20, nearest заменяет1; отрицательный индекс не исправляется |
| `+2e8,+2ec`, Int32 | Лучшее signed расстояние атакующего и отдельное расстояние цели для kind1; tie RNG133/134 |
| `+328,+32c,+330,+334,+338`, Int32 | Флаг слияния, слот другого участника, два сохранённых sourceID, cooldown.4064d0 обрабатывает20 слотов |

417400 сохраняет Frame130/134 как исходные32-битные ссылки на ITR/BDY heap.
Шаги80/40 байт и отдельные defined masks сохраняются в native storage.
Только known allocation extent доступен для чтения; соседняя неизвестная память
и реальные Windows allocator effects остаются отдельной областью исследования.
При собственном первом проходе меняется только7c0→219 у двух активных бойцов;
обновлённый full pool сравнен без подстановки ожидаемого снимка.


### Разрешение попаданий и изменяемый DAT heap

D [WORLD_HITS](WORLD_HITS.md): вся42e100 и caller41eefb..41f2ac,
7845 управляемых случаев и два собственных запуска. Назначения ниже относятся
к этим функциям; полный срок жизни полей в остальных стадиях ещё исследуется.

| Область | Использование |
| --- | --- |
| Actor `+280/+2d0/+2e4` | Живые target/ITR/count; номер ITR читается как signed Int8, count перечитывается после контакта |
| Actor `+2fc/+300/+34c` | HP, red HP, полученный урон; Int32 wrap, red HP теряет injury/3 со знаковым усечением |
| Actor `+340/+344` | Положительный делитель масштабирует injury×100; отдельный номер команды выбирает статистику globals |
| Actor `+348/+354/+358` | Нанесённый урон, слот владельца статистики, число убийств; credit slot не обязан быть активным |
| Actor `+31c` | Прочность предмета, расход исходного injury; bdefend100 задаёт−1 |
| Actor `+36c` | Счётчик искр; положительный предел10, отрицательное значение не исправляется |
| Actor `+370/+398/+3c0`, по10 Int32 | X, Y с глубиной, тип/счётчик искр; CRT rand для Y затем X |
| Global `45115c` | Число активных type1/2/4/6 в первом проходе; значение>=4 пропускает RNG появления предмета |
| Global `451b64/451b68/451b6c/451b70` | Две счётные и две damage-ячейки команд; адрес выбирается из Actor344 |

Ветка raw kind5 удерживаемого оружия может менять dvx/dvy ITR в исходном heap.
`OriginalMatchPreparation.frameAllocations` теперь владеет изменяемыми записями,
а каталог сохраняет загрузочное состояние. Контакты читают текущий heap;
подтверждённые внутренние адреса, порядок аллокаций и defined masks сохраняются.
Нативные hit-проходы публикуют World/Actor/globals/heap/CRT совместно при успехе.
При первом собственном проходе heap неизменен, RNG39/0→40/1; сравниваются все
14586 записей, не только активные боксы. Первоначальные таблицы выше описывают
более ранний исследовательский срез, а не отсутствие этих полей в новом Core.

### Cpoint actions и placement

D [WORLD_CPOINTS](WORLD_CPOINTS.md): целые418c30/4187b0, caller cleanup и
повтор417f80 до41f484.2681 управляемый случай и две собственные цепочки совпали.

| Поле Actor | Использование в этих проходах |
| --- | --- |
| `+8c/+90` | Слот захваченного партнёра / слот захватившего; точное reciprocal сравнение без activity/type gate партнёра |
| `+88` | Локальная стадия действия:0 допускает injury,1 фиксирует его применение,2 разрешает dircontrol |
| `+94` | Счётчик захвата; positive decrease вычитается, negative decrease прибавляется с отдельным exhaustion path |
| `+70/+7c` | Current/collision Frames:418c30 начинает с collision,4187b0 использует current после всех действий |
| `+324/+33c/+368` | Сохранённый sourceID, ID партнёра и новая Object binding при throwinjury−1; зависимые active Actor2f4 тоже получают новую binding |
| `+20/+28/+30` | Exhaustion ставит pending impulse flag1, партнёру X±4/Y−3; это не vx/vy40/48 |
| `+98/+9c/+a0` | Cleanup положительного held-type проверяет signed slot, target activity и backlink; invalid очищает только98 |

Raw cpoint vaction может адресовать предшествующий Frame массиву Object header
после отдельного abs текущего кадра. Объявленные -1..-5 проверены с сохранением
header bytes/masks; неизвестный backing не обнуляется. В загруженных исходных
137 Object все наблюдённые vaction находятся в0..<400. Throwvz у sourceID419,
frame49 не инициализирован загрузчиком и может читаться при directional throw;
естественная достижимость и Windows backing ещё не подтверждены.

## Object, каталог и globals

Object выделяется по `0x25360` байт в `0x412660..0x412695`, затем вызывается
`0x40ef70`. Известны ID/type `+6f4/+6f8`, параметры движения в начале записи,
400 кадров `+7a4 + n*178`, указатели/числа боксов и звук в кадрах. Снимок хранит
весь Object, включая заголовок, нетронутые поля и хвост после кадров. Полный
внешний загрузчик не исполняется стендом сырого такта. В отдельном
[oracle_objects.py](../../tools/oracle_objects.py) он теперь выполняется целиком
для Naruto/Sasuke/kunai и контроля; фактическое происхождение заголовков и bitmap
установлено для этих случаев. Состояние всего каталога/матча пока не получено.

Каталог выделяется по `0x4d823a8` (`41bfeb..41bff0`), конструктор/загрузка —
`4122f0`. Основные границы теперь восстановлены статически в
[BOOTSTRAP.md](BOOTSTRAP.md): таблица объектов, 60 областей стадий, 101 запись
фона и хвост. Это не раскрывает все поля/правила загрузки. В сыром снимке
сейчас сохранены таблица пяти загруженных объектов, известная
область арены и последующие отображённые поля; **огромный промежуток не отображён
в стенде**, явно перечислен в `catalogHoles`. Это основная открытая часть R02/R03.

В отдельном BG-стенде все 101 × 0x990 байт таблицы отображены с backing/масками.
Для всех 17 исходных арен и ordinal 98 контроля проверены чтение DAT `40c160`,
загрузка bitmap слоёв `40c030` и освобождение `40c0e0`. Поля и источники значений:
[BACKGROUND_LOADER.md](BACKGROUND_LOADER.md). Последний release оставляет старые
указатели во всех layer slots, кроме первого; это не null и не живые объекты.
Shadow bitmap не освобождается этими функциями. Нетронутые поля и их маски
сохраняются при повторном parse. Нормализация строго index+1 с 0=null.

В [Stage-стенде](STAGE_LOADER.md) отображён весь участок `7d0..<4d45db0`:
60 × `149b08` = 81 024 480 байт. Все 60 конечных записей сравниваются с Swift,
как и выбранная стадия после каждого initializer/phase. При первом stage-level
ID определены 288 904 байта записи: 100 фаз / 6000 наборов полей. В отсутствующей
стадии записывается только count=-1 в +0; +4 остаётся нетронутым. Music +c,
entry fields +ec+n*e0 и phase goto +34c0 сохраняются как байты, без выдуманного
sizeof(entry) или нормализации чисел в указатели. Повторная загрузка сохраняет
нетронутое прежнее состояние/его маски. Этот результат не заменяет синтетическую
загрузку старого tick-trace автоматически.

[state-global-accesses.json](../evidence/state-global-accesses.json) — инвентарь
прочитанных/изменённых globals с адресами инструкций и размерами обращений во
всех шести случаях R01.1, после синтетической инициализации. Не только конечные
изменения: запись прежнего значения также остаётся в инвентаре. Это перечень
наблюдавшихся обращений, а не все достижимые globals/типы.
Для массивов World одинаковые обращения сгруппированы по инструкции и диапазонам
индексов; обратное развёртывание проверяется на равенство полному списку.

| Globals | Установленная роль / источник и следующий блок |
| --- | --- |
| `450b90`, `450bfc`, `44fb60`, `44fcb0` | Фаза/пауза/очередь, 4-байтные операции R01.1; полный ввод R04/R15 |
| `450bd0`, `450bd4`, `450bd8` | Счётчики 12/3 и чередование; исходная инициализация и потребители R02/R10 |
| `450b4c` и следующие 7 dword; `450b8c` | Режимы входов, счётчик ввода; локальный ввод/повтор/сеть R04/R16 |
| `44ff90..450b48`, `450bcc`, `450c34` | Таблица3000+NUL и игровой index/counter. D: [RANDOM_INITIALIZATION.md](RANDOM_INITIALIZATION.md) создаёт таблицу CRT, сохраняя index/counter; Practice и старые трассы ещё используют replay input |
| CRT PTD+`14` | Отдельный UInt32 state, initial1 из _initptd; srand заменяет32bits, rand меняет с wrap. D pinned DLL/native; настоящий TLS/lifetime всех потребителей открыт |
| `458420` | UInt32 обнуляется seed-prefix43cf4b, D. Полное назначение не установлено |
| `450bc4`, `450bc8` | Камера, R13 |
| `44d02c`, `44d034`, `44d05c`, `451160`, `450bdc` | Селекторы времени/стоимости/проходов/режима; условия R01.1, полный жизненный цикл открыт |
| `450bb8`, `450bbc`, `450bc0`, `450bf8`, `450c00`, `45115c` | Запросы/счётчики/текущий проход; ссылки на реальные записи в инвентаре, семантика всех ветвей R06/R10/R15/R16 |
| `44d040..<44d055`, `450c38..<450c4c` | Рабочие области ввода/интерфейса; есть обращения разной ширины с перекрытием. Не превращать каждую запись в независимый Int32 |
| `450bec`, `457580`, `4593a4` | Записи диспетчеров; назначение и читатели требуют R02/R15 |
| `455638`, шаг 20; `458438`, `44eecc` | Кэш путей/состояние звука; пути могут перекрываться, устройство отключено в стенде. R03/R14 |

Дополнение [пролога запуска](MATCH_PRELUDE.md), отдельное от старого R01.1 corpus:

| Globals | Тип и подтверждённое поведение / граница |
| --- | --- |
| `451160`, `44d020` | Int32 режима и состояния меню; третий (mode) и второй (menu) аргументы `429730` по caller `4229d0/4229d5`. Пролог обнуляет `44d020`; `World+0` — отдельное поле |
| `450b94`, `44d024`, `44d028` | Int32 стадии, BG и признака случайного BG; mode=1 обнуляет признак и заменяет BG только для stage 0/10/20/30/40/50 |
| `44fd98` | NUL-terminated имя файла повтора; записывается настоящий результат `%s.lfr`, хвост старого содержимого сохраняется |
| `450bb0/450bb4/450b6c` | Int32, обнуляются прологом во всех режимах; полная семантика потребителей не установлена |
| `450be4/450b70` | Int32, ненулевой первый устанавливает второй в 1; при нуле второе поле сохраняется |
| `450b98` | Int32, ненулевое значение выбирает очистку поверхности и затем сбрасывается; ноль выбирает звук подтверждения |
| `44eecc` | Указатель COM-аудиоустройства: S `401970` передаёт его адрес импорту DSOUND ordinal 1 и далее разыменовывает vtable. Пролог проверяет только nullness; native не трактует его как host pointer |
| `455608/455610` | Указатель поверхности / поле указателя звукового буфера; в новом сравнении supplied opaque handles. Реальные helpers исполняются до device boundary, загрузка и вывод ещё не проверены |

Дополнение [продолжения меню](MATCH_CONTINUATION.md), D против native:

| Globals | Тип и действие |
| --- | --- |
| `44d06c` | Int32 кода команды; в продолжении обработаны 1–5 при ненулевом подтверждении из stack local |
| `451228`, восемь dword | Int32 флагов выбора: команда1 проверяет ==1, команда2 — !=0; начальные значения UI ещё supplied |
| `451248`, восемь dword | Int32 selected ordinal, включая -1 sentinel. Общий подбор исключает все восемь текущих значений, в том числе у неактивных seat |
| `451268/451288`, по восемь dword | Int32 latch/status; команда1 устанавливает 1/0, сама по себе не меняет activity World |
| `450c2c` | Int32, только точное 1 включает отдельную ветвь подбора BG/бойцов/команд; значение сохраняется равным1. Полная роль флага в UI ещё открыта |
| `450c30/458428` | Int32 параметра и дополнительного условия команды4; вычитание с wrap, граница -1 и возврат к2 по таблице продолжения |
| `44d078/4512c8` | Int32 timer/latch; команда1 задаёт 150/0; при timer<=0 и latch==0 в конце ставится latch=1 |

Дополнение [главного меню и мыши](MAIN_MENU.md), D против native:

| Globals / поле | Тип и действие |
| --- | --- |
| `4546f0/453cdc` | UInt32 координат, zero-extended low/high UInt16 lParam сообщения200; меню сравнивает их как Int32 |
| `457580/4527e4` | Int32 флагов левой/правой кнопки:201/202 и204/205 задают1/0; кнопочные сообщения координаты не меняют |
| `44d060` | Int32 предыдущего состояния/блокировки подтверждения; требуется ==0. Shared tail4287d2 копирует сюда457580; D следующего этапа MENU_PRESENTATION |
| `453da4` | Int32 смещения: base=значение+202 с wrap; происхождение настройки ещё открыто |
| `4511e0` | Int32, обнуляется при каждом входе в основной блок меню |
| `44d064`, World+0 | Разные selectors: пункты1/2/3/4 задают44d064=0/1/6/7; пункт1 отдельно пишетWorld+0=1 |
| `450b4c..450b58` | Четыре Int32, пункт1 задаёт1/2/3/4 после пересоздания RNG-таблицы |
| `45117c/4511a0` | Непрозрачные bitmap pointers для запросов основного меню/подсветки; loader и пиксели здесь не проверены |
| `44f2d4` | UInt32 host-entry pointer, результат сетевого lookup, сохраняется и при нуле; native opaque token |
| `44f1b4/44f1b0` | UInt32 socket token / соседний dword: оба обнуляются перед socket(), затем первый получает результат. После closesocket не очищается |
| `44f260/44f262/44f264` | UInt16 family2, UInt16 htons(5000), raw UInt32 выбранного адреса; padding сохраняется |
| `44f58c/44f58e/44f590` | UInt16 family2, UInt16 htons(12345), raw UInt32 выбранного адреса;16 bytes передаются bind, включая старый padding |
| `44f340`,200 bytes | После успешного init очищается целиком, затем получает NUL-terminated текст выбранного адреса |
| `44f1ae` | UInt8, обнуляется в конце успешного сетевого init |

Дополнение [завершения меню](MENU_PRESENTATION.md), D против native:

| Globals / поле | Тип и действие |
| --- | --- |
| World+0 | Int32 selector; полный вызов4246b0 при1 освобождает menu bitmap, рисует, пишет2 и вызывает overlay/present |
| `4511ac` | UInt32 menu bitmap pointer;423910 вызывает destructor/free и очищает pointer. S4238b9 подтверждает allocation1f50; loader423840 здесь не исполнен |
| `451170/45118c` | Непрозрачные bitmap pointers курсора и перехода World1; только draw requests, происхождение ресурсов открыто |
| `44d000` | Int32 общей громкости; изменение оборачивается, затем clamp0..100; разные формулы музыки и пяти sound buffers |
| `4553f2/4553f3` | UInt8 control bytes; только64 включает уменьшение/увеличение, второе побеждает при одновременном нажатии |
| `44f190` | Int32 таймера громкости:100 при изменении, уменьшается при >0 только без recording notice |
| `450b70/450b6c/450bfc` | Int32 notice/timer/block. Положительный notice при block0 увеличивает timer, >240 очищает оба; notice3 дополнительно увеличивает timer независимо от block |
| `44f040/44f044/44f048/44f04c` | UInt32 DirectShow interface tokens, shutdown освобождает в обратном адресном порядке и очищает каждый |
| `44eecc`, `458438/45843c`, `452948/451db0` | Audio-device token, Int32 counts и массивы COM tokens. Shutdown при ненулевом device освобождает оба списка и device, очищая только device pointer |
| `458348` | UInt32 present mode:1/2 Flip разных surfaces,3 Blt, остальные без device call |
| `455608/455634/453e0c`, `453ccc` | Surface tokens и16 bytes rect; overlay использует455608 независимо от переданного ему аргумента |
| `4588a8/4588ac` | Два внешних к ordinary globals UInt32 replay allocation pointers;43d2a0 освобождает и очищает каждый, не сбрасывая notice flags |

Дополнение [загрузки WAV и общих звуков](WAVE_LOADING.md), D против native:

| Globals / запись | Тип и действие |
| --- | --- |
| `44d05c` | Int32 first-load selector: exact1 выбирает41be98. В bounded sound caller сохраняется1, как и в прежнем конечном presentation/replay state. D следующего UI caller: обнуление41c577 после десяти stores, даже при отказах; полный loading path ещё не соединён |
| `451db0`,18 dword; `45843c` | UInt32 buffer tokens общих звуков в фиксированном EXE-порядке. После18 вызовов count всегда18, даже при отключённом устройстве или ошибке файла |
| `457588`,1600 bytes; `453e10`,320 bytes | Целиком обнуляются перед первой загрузкой WAV; другие globals сохраняются, кроме выходных sound slots/count |
| `44eecc` / destination slot | При device0 WAV helper возвращает1 и сохраняет прежний slot. Иначе сразу очищает slot; успех записывает предоставленный buffer pointer, обычная ошибка оставляет0 |
| WAVEFORMATEX,18 bytes | Первые16 берутся из format payload; последние2 равны low16(destination) из перекрытого сохранённого this. Все байты определены, cbSize не нормализуется |
| DSBUFFERDESC,36 bytes | size36, flagsE0, payload size, stack format pointer; остальные поля0. Только проверенный целый pointer+16 нормализуется; прочие байты сохраняются |
| Temporary PCM allocation | Размер data.cksize; при коротком/неуспешном чтении остаётся live с фактической маской записей. Успех освобождает после Unlock; create failure освобождает и достигает отдельной неподдержанной границы продолжения |

Дополнение [enabled catalog sounds](CATALOG_SOUNDS.md), D полного каталога:

| Globals / поле | Тип и действие |
| --- | --- |
| `458438`, `455638+20*i` | Count и raw path cache. Новая запись загружает WAV и выставляет громкость до копирования полного пути/NUL и увеличения count. Перекрытия дают400 записей для365 разных WAV |
| `452948+4*i` | UInt32 sound-buffer tokens. Оба caller передают адрес slot в4014e0, затем запрашивают SetVolume(-10000); игнорируют его HRESULT и не освобождают buffer |
| Frame+`174` | Int32 index; перед lookup=-1, затем match/new index назначается до device calls. Source pointer+170 записан раньше |
| Object+`a4/a8/ac` | Int32 weapon sound indices; cache hit заменяет index, no-hit при старом!=-1 сохраняет его и не создаёт запись; path pointer уже заменён caller |

Дополнение [первых UI bitmap](INITIAL_INTERFACE.md), D после реального пула:

| Globals / запись | Тип и действие |
| --- | --- |
| `44ff8c/44f8f8` | UInt32 wrapper pointers PAUSE/DEMO; каждый получает отдельную allocation1f50 либо0 при null malloc |
| `44fcb4/44fd8c/44f88c/44f87c` | Четыре UInt32 wrapper pointers SCORE_BOARD1..4, в указанном порядке |
| `44fd90/44fd94/44fb64/44fd7c` | UInt32 pointers WIN_ALIVE/WIN_DEAD/LOSE_DEAD/BARS; после последней записи сбрасывается44d05c |
| `457578` | UInt32 DirectDraw device token передаётся43ed10 с flags40; происхождение устройства здесь supplied |
| Bitmap+`0/4/8` | Surface pointer/Int32 width/height. Key failure освобождает surface и очищает только+0; отсутствующий ресурс вообще не записывает размеры. Остальные bytes/masks allocation сохраняются |

Дополнение [применения полученных команд](RECEIVED_INPUT.md), D двух
проходов после реальных первой загрузки и локального ввода:

| Поле / буфер | Тип и действие |
| --- | --- |
| `450b4c+seat*4` |4198f0 выбирает только signed status ровно−1.4197a0 игнорирует статусы |
| Actor+`c6..cc` / `cd..d3` | Previous/current: обе функции копируют семь сырых current bytes в previous в каждой фазе; только phase0 очищает и декодирует current |
| `44f198` | Remote packet: caller4198f0 использует первые восемь байтов; bit0 не кнопка, но сохраняется при копировании в запись |
| Caller stack+`434` / `440` | Два отдельных десятибайтных command buffers.4198f0 заменяет первые восемь output bytes приrecording!=0;4197a0 читает первые восемь playback bytes. Последние два сохраняются |
| Caller stack+`38` | Ненулевая локальная пауза пропускает оба received-input вызова; не глобальная очередь паузы |
| World+`194+seat*4` | Ссылка места на Actor: повторные ссылки/перестановки проверены. Позднее место видит записи раннего; ordinal0 не null |

Дополнение [горячих клавиш и обмена](INPUT_CONTROL.md): S и D против Swift,
5 986 случаев после двух воспроизведённых полных загрузок.

| Поле / буфер | Тип и действие |
| --- | --- |
| `44f1af` | UInt8: в phase0 ровно 0 выбирает local control, ровно 1/2 — send/receive и receive/send; остальные пропускают обе ветви |
| `44d040` / `44f198` | Outgoing/incoming по 22 байта. Входная клавиатурная команда OR в [10/12]; [9/11/13/14/15] — заголовок. Caller копирует только 21 байт шаблона |
| `450bf0` | Int32 последовательности: `(old &+ 1) % 50`, деление к нулю |
| `450bf4` | Int32: 32-битная сумма HP первых 20 мест с activity ровно 1, затем `% 100 + 1` |
| `44f620` | Int32 общей суммы каталога, для заголовка используются остатки/частное по 256; это не RNG |
| `455471/455470`, `4553e8..4553f0` | Command key bytes: только 100 считается нажатием, потребление пишет 117. Получение этих значений от ОС — отдельный участок |
| `450c18/1c/20/24` | Int32 счётчиков четырёх дополнительных commands; сложение с переполнением |
| `458588..4588a7` | 0x320 байт сохранённых настроек/строк.43df00 sign-extends byte45877c и копирует строки через NUL; происхождение при запуске повтора ещё открыто |
| Playback buffer+`630bb8/630bbc` | Сохранённые UInt32 значения458428/45842c, которые caller возвращает после43df00 |
| `4588a8/4588ac` | Два replay pointers: shared shutdown освобождает оба и очищает только указатели; буферные bytes/masks сохраняются в проверке ownership |

Дополнение [команд и записи повтора](REPLAY_TICK.md): S и D против Swift,
2 268 случаев после свежих loading/local/control родителей.

| Поле / буфер | Тип и действие |
| --- | --- |
| `450b8c` | Int32 tick: signed деление/остаток150, рост любого unpaused пути до647999. Запись пакета предшествует ограничению; достижение предела снизу ещё не выключает recording |
| `450b80` / `450b84` | Int32 flags, проверка !=0: запись / воспроизведение. Checksum error может выключить playback до выбора источника записи |
| Caller stack+`434` / `440` | Prefix41bdce очищает оба10-byte buffers даже при паузе; unpaused playback затем читает43dc50 в+440 |
| Caller stack+`44` | Playback checksum entry пишет signed `tick/150` при любом остатке; это тот же scratch word, который control мог передать ioctl |
| Buffer+`2b38 + 10*tick` | Все10 packet bytes; UInt32 адресная арифметика. Metadata overlap сохраняется, выход за owned allocation отклоняется |
| Buffer+`14b8 + 4*(tick/150)` | Int32 сумма HP первых20 World seats с activity ровно1, сложение с переполнением. При tick216000 перекрывает первые packet bytes |
| `450bdc` | Replay checksum error сохраняет этот timer, хотя hotkey416cd0 очищает его |
| `450b88`, `451160`, `44d020` | Checksum error при playback!=0/menu!=10: mode6, условный43df00/возврат flags, clear450b88/450b84, menu10. Буфер не освобождается |

Ссылки двух replay pointers на одну аллокацию проверены. Произвольное наложение
буферов с globals/стеком не входит в этот API; полное происхождение playback
storage и settings остаётся отдельной задачей startup/file IO.

Дополнение [исхода раунда](MATCH_ROUND.md), S и D против Swift:

| Поле / буфер | Тип и действие |
| --- | --- |
| `450c00` / `450bfc` | Round prefix пишет первый как `(450bfc==2)` даже при паузе. Только без паузы второй меняется2→1 |
| `450bd0/450bd4/450bd8` | Int32: `(old &+ 1)%12`, `(old &+ 1)%3`, `1 &- old`; signed remainder, не обязательные корректные начальные диапазоны |
| World activity, Actor+`2fc/364`, Object+`6f8` | Для живой команды нужны activity!=0, HP>0, type==0, team1…39 кроме5; все400 мест, повторные ссылки учитываются повторно |
| `450bf8` | Int32 последней непустой команды при обходе0…39; -1/сохранение зависят от mode/menu/числа команд |
| `451b7c` | Ровно1 разрешает рост round timer в mode4 при прочих условиях завершения |
| `450bdc` / `450c2c` | Round timer: события ровно80; возврат145→144 при450c2c==0; восстановление при>=350. Подтверждение ставит350 для следующего прохода |
| `450ba8` / `450b94` | Signed Stage-параметр влияет на рост таймера/подтверждение; индекс получает `(old/10)*10` к нулю. Полный Stage lifecycle отдельно |
| Caller local+`64` | Без паузы начинает с0; mode1 scan может записать1. Полный цикл восстановления использует это место как счётчик400 и оставляет0 |
| Actor+`324` | Int32 source ID для первой обратной подмены; >-1 запускает поиск первого совпадения, только успех очищает поле до-1 |
| Actor+`328/32c/330/334/338` | Неотрицательный328 включает восстановление двух участников;32c — World seat второго;330/334 — source IDs для обхода каталога;328 очищается,338 получает900 до возможного конструктора |
| Actor+`2fc/300` | При восстановлении сначала signed половины записываются второму, затем исходному; при alias деление повторяется |
| `44f04c/44f044` |402100 проверяет ненулевой первый, затем вызывает control+24 и position+20(+0 binary64); handles не очищаются, HRESULT не останавливает второй вызов |

ID50→6 при458428==0 — отдельная исходная ветвь перед328; это не общий default
Object или правило по имени персонажа. Constructor/Actor/World связи и
нетронутые bytes/masks сохраняются при восстановлении.

Дополнение [включённой музыки](MUSIC_PLAYBACK.md), S и D против Swift:

| Поле / буфер | Тип и действие |
| --- | --- |
| `44d010` | Любой ненулевой UInt32 разрешает402020; в настоящем первом menu prefix остаётся1 |
| `4512cc` / `44d020` | Previous/current menu; previous!=10 и current==10 запрашивают main.wma. До4297ae previous не обновляется |
| `44f040/44f044/44f048/44f04c` | Graph/control/event/position: исходные GUID query и раздельные HRESULT/output. Release очищает в обратном порядке; init error не обязательно оставляет graph0 |
| `44ef04` | Текущий путь до NUL. Init успех очищает только первый байт; при control!=0 конец402020 копирует весь новый путь даже после RenderFile error. Старые хвосты остаются |
| `44ef38` | Каталог для `%s\graph.log`; перекрывается current path длиной>=52. Не вводить искусственную границу предыдущей строки |
| `44d000` | Volume для401f30:0→-10000, иначе wrapping*34-3900; передача здесь без clamp |
| `4546f4` | Предоставленный window token для event+34(window,400,0); происхождение окна отдельно |
|401da0 wide allocation |2*(path length+1) bytes; backing/defined mask, выходные записи conversion и все выделения сохраняются. Освобождения в этом пути нет |

COM/file/codec/new и реальные Win32-строковые преобразования остаются границами.
Значения интерфейсов — явные непрозрачные tokens, не host pointers; маски не
превращают нетронутый allocator backing в определённые символы.

Дополнение [ресурсов меню](MENU_RESOURCES.md), S и D против Swift:

| Поле / буфер | Тип и действие |
| --- | --- |
| `4512cc` |4297ae всегда копирует current menu44d020, даже при пропуске загрузки |
| `4512c8` | UInt32 selection сохраняется в caller local+3c до загрузки; само поле здесь не меняется |
| `44d07c` | Любой ненулевой UInt32 включает11 allocations и сброс таблиц; обнуляется429c50 до записи SPARK height[13] |
| `4512c4/4512b0/4512b4/4512b8/4512bc/4512c0/4512ac/4512a8/44fd88/44fd84/44f8fc` | По порядку CHARMENU/CM1..5/CMA/CMA2/CMC/RFACE/SPARK; явные allocator tokens, null0. Старые wrappers не освобождаются |
| `451288/451268/451248` | Три массива по8 UInt32; для каждого индекса записываются0/0/FFFFFFFF в этом порядке |
| SPARK+`0c` | Число кадров20 при существующем wrapper, даже без surface; null wrapper останавливает перенос перед исходным429b21 |
| SPARK+`10/7e0/fb0/1780` | Массивы x/y/width/height по500 Int32. Записываются только16 индексов:1..4/6..9/11..14/16..19; остальные bytes/masks сохраняются |

Первые10 null allocations допускают дальнейшую загрузку; последний null
оставляет load flag и частично изменённые globals, возвращая явную границу.
Не подменять её безопасным продолжением. Surface+0 нормализуется1/0; остальные
слова wrapper, включая неинициализированные координаты, не нормализуются.

Дополнение [общей отрисовки bitmap](BITMAP_DRAWING.md), S и D против Swift:

| Поле / представление | Чтение в43f010/43ef70 |
| --- | --- |
| `44d78c/44d790` | Signed Int32 viewportWidth/Height; strict сравнения, без предварительного clamp |
| Bitmap+`0c` | Signed count:0 включает целое изображение; после него поле читается вновь для signed frame<count |
| Bitmap+`4/8` | Width/height целой картинки; суммы с x/y имеют32-bit wrap |
| Bitmap+`10/fb0/7e0/1780` | Кадровые x/width/y/height читаются в этом порядке по смещению4*frame с32-bit wrap |
| Bitmap+`0` | Известный surface token хранится в loaders как1/0, но draw восстанавливает raw token перед чтением/арифметикой: frame=-4 может использовать его как x |
| DDBLTFX100 bytes | При любом mirror!=0 size100,flags2, остальные92 байта0; при mirror0 null effects pointer |

Сохраняются отрицательные и wrapped aliases **внутри** предоставленного wrapper.
Выход за wrapper — явная остановка перед исходной инструкцией, не clamp индекса.
Нетронутый backing читается с сохранением defined=false и отмечается observer;
общий строгий typed read остаётся прежним. Это не доказательство происхождения
реальных Windows heap bytes или подключения draw к нативному оконному выводу.

## Снимок: что сохраняется и что пока неизвестно

[oracle_state_trace.py](../../tools/oracle_state_trace.py) повторяет шесть случаев
R01.1, снимает начальное состояние и состояние после каждого наблюдения:
**38 снимков**, по 409 областей / 1 538 892 байта. Сохраняются:

- все 400 выделенных Actor по 0x420, независимо от активности;
- пять загруженных Object по 0x25360, используемый префикс heap, доступные
  области каталога;
- вся PE `.data` по `0x44d000`, размер `0xc724`, включая World и неизвестные globals.

Байты хранятся hex-блоками по SHA-256, со ссылками из снимков; это устраняет
дублирование, **не** выбрасывает неизвестные поля. После записи корпуса проверяются
хеши и размеры всех блоков. Float не проходит через десятичный JSON: сохраняются
биты, включая signed zero, subnormal и payload NaN.

У подтверждённых pointer-полей отдельно указаны исходные биты и ссылка
`region + offset`: World → Actor/Catalog, Actor → Object, каталог → Object,
живые itr/bdy и звуковые пути кадров. Идентичность объекта — индекс записи
стенда, не уникальность ID. При нулевом count указатель боксов не интерпретируется.
Остальные байты остаются непрозрачными: произвольный dword не объявляется
указателем лишь из-за похожего адреса. **Это ещё не полная нормализация состояния
между разными размещениями памяти.**

У снимка после инициализации пока **нет полной маски происхождения**: исходные
конструкторы, ручные настройки стенда и BSS различаются в описании, но ещё не
прослежены побайтно на всём жизненном цикле. Отдельный корпус конструкторов такую
маску имеет. В наблюдавшихся Actor-чтениях единственная область, не записанная
конструктором, — Object* `+368`, назначенная стендом. Это не разрешение игнорировать
прочие нетронутые области в новых сценариях.

Снимок не включает stack/регистры, неизменяемые секции EXE, состояния устройств
и внешние структуры Python. Инвентарь обращений не видит host-записи CRT/платформенных
подмен. Новая аллокация за пределом объявленного heap останавливает стенд, чтобы
он не выдавал усечённый снимок за полный. Границы ОС, частичная загрузка DAT и
отключённый звук унаследованы из [R01.1](TICK_PIPELINE.md).

Конечные выбранные поля совпали с сохранённым отчётом R01.1. Побайтовый снимок
также сохраняет известное расхождение с прежним конвейером: через 12 тактов
RNG counter=12, HP=401, MP=107; старый конвейер даёт 0/400/100. Полный такт
с нативным движком пока не сравнивается, Windows W остаётся открытой.

## Воспроизведение и ближайшая работа

```bash
uv run tools/oracle_state.py
uv run tools/oracle_state_trace.py
swift test --package-path native
```

Для state-trace нужен прежний импорт `build/imported/game.json`; его создаёт
`python3 tools/import_ntsd.py`. Полные корпуса остаются в `build/original/`,
краткие доказательства — в `docs/evidence/`; конструкторный корпус также сохранён
для автономных Swift-тестов.

Загрузочный пул уже проверен отдельно в [BOOTSTRAP.md](BOOTSTRAP.md).
Следующее: R02.1 — исходная инициализация каталога, происхождение всех
прочитанных полей и оставшиеся связи/типы; R03.1 — загрузка целых объектов;
после них R01.2 — снимки по проходам и общий нативный такт. Новые конструкторы
не меняют доступный объём игровой практики и не закрывают R02 целиком.

Дополнение [ранних ресурсов меню](FRONT_MENU_RESOURCES.md), S и D против Swift:

| Поле | Правило раннего startup |
| --- | --- |
| World+0 | Constructor задаёт0; ветвь ресурсов выполняется при selector !=1 и !=2 |
| `4511f8` | На каждом входе UInt32 wrap `1 - old`, включая skip |
| `44d068` | Любое ненулевое значение запускает загрузку; очищается лишь427092 после423480, вне этого D-корпуса |
| `44fcc0 + 11*i`, i0…7 | UInt16 имени `1\0`…`8\0`; остальные байты поля не очищаются |
| 24 bitmap slots | Порядок/адреса в study; null allocation записывает0, новые wrappers заменяют старые без release |
| Bitmap+`0c/10/7e0/fb0/1780` | Девять UI sheets получают151 literal rectangle и count в конце каждого листа; шесть шрифтов сначала получают count256, затем interleaved glyph rectangles |
| Остальные bitmap bytes | Сохраняют backing/mask; count cursor/MENU_WAIT/CS/FRAME здесь не инициализируется |

Прежние menu fixtures сохраняют свою область доказательств. Новые wrappers ещё
не переданы в ту цепочку: требуется423480 и настоящий ранний экран.

Дополнение [настроек](SETTINGS_LOADING.md), D после собственного раннего parent:

| Поле | Правило423480/caller |
| --- | --- |
| `44fb70 + 50*player + 4*field` | Четыре группы11 Int32,44 отдельных %d; неудачная конверсия сохраняет прежние bytes |
| `44fcc0 + 11*player`, player0…3 | Четыре %s без ширины, затем backtick→space до NUL; возможны перекрытия |
| `450be8/450be4` | Два %d с завершающим whitespace, в указанном порядке |
| `44fd18/44f890` | Fgets100, затем удаление только trailing space/CR/LF; TAB/NUL tail сохраняются буквально |
| `44f900` | Очищается только первый байт; затем append старого/нового scratch до NUL. Нет заданного ограничения длины поля |
| `44d068` | После helper427092 записывает caller EBX, естественный0. Null FILE не достигает этой записи |

Scratch сохраняется при failed fgets и между supplied повторными caller inputs.
Сведения о границах500-byte scratch/глобальной области и CRT находятся в study.

Дополнение [первого рисунка раннего экрана](FRONT_SCREEN_PRELUDE.md), D после
собственного World/resources/settings parent:

| Поле | Правило42709b/ранних helpers |
| --- | --- |
| `44d064/450be8` | Selector-2: setting-1→selector-3, иначе selector0; только setting1 вызывает4237e0 |
| `44d788`, строки`4527b0/451d48` | Gate sentinel-99, fixed prefix now\0, затем unsigned-byte lexical comparison |
| `458424` | Читается между Enter/LeaveCriticalSection4554a4; любое ненулевое значение подавляет CreateThread |
| `455608` | Непрозрачный target415160; отдельная привязка от внешнего bitmap targetEDI |
| `4511ac` | Null вызывает423840, ненулевой сохраняется; новый wrapper заменяет global без release прежнего |
| Background+`0c/rectangles` | Остаются с исходным backing/mask после43ee50; caller передаётframe-1 и может прочитать их через fallthrough |
| `453da4` | Int32 y первого фона только при selector0, иначе y0 |
| `44d78c/44d790` | Signed viewport общей bitmap обрезки, не размер fixed fill794×550 |

TimeGetTime UInt32%13+1 выбирает MENU_BACK1…13 без игрового RNG. В100-byte fill FX
записаны только size+0 и color+50; остальные92 байта сохраняют явно заданный
stack backing. Модель не делает эти bytes initialized и не обнуляет их.

Дополнение [информационной панели](MENU_CONTENT.md), S/D отдельного43c780:

| Поле | Формат и записи |
| --- | --- |
| `44d784` | Signed индекс в обоих путях ad%d; helper его не меняет |
| `453c68/453d40` | Текстовый/bitmap путь, sprintf пишет только результат и NUL |
| `44d778` | Header `%d `; инициализируется-99 после первого fgets |
| `4583b8/453c08/453f70/453ce0 +4i` | Четыре scanf-поля24 ba rows; при ошибке только первое снова-99 |
| `454a18+100i` | Строка ba, начальные UInt16 `-\0`, допустим первый byte?/h/H; %s не ограничен stride |
| `452928/4546d0/453f50 +4i` | Три scanf-поля8 ta rows; при ошибке все снова-99 |
| `4546f8+100i` | Строка ta, те же prefix/UInt16/%s правила |
| `4554bc/453da8` | Число/строка un; сброс числа-99, строка сохраняет частичную запись |
| `453da4/45757c` | Два числа y; ошибка сбрасывает оба в0, в обратном scanf порядке |
| EntryESP−`454`..EntryESP−`5` | 1104-byte scratch: markers0/52, fgets buffers104/604; общая память и маска |

Все числовые значения должны отличаться от-99. Старые globals продолжаются
между вызовами; поздние строки не очищаются заранее. Неудачный fgets оставляет
старый буфер, который исходник всё равно сканирует. Исходные ad0/ad1 отсутствуют;
проверены missing paths и явные present-file controls, не полный parent/startup.

Дополнение [bitmap информационной панели](MENU_PANEL_BITMAP.md), S/D всего43cc60:

| Поле | Правило |
| --- | --- |
| `458420` | Old wrapper проходит destructor/free, затем global0; новый malloc/constructor снова записывается сюда, включая null |
| `453d40/457578` | Путь и opaque graphics device общего43ee50; helper путь не меняет |
| Bitmap+`0` | Destructor Release для ненулевой surface, затем0; null surface не освобождается повторно |
| Bitmap+`10/7e0/fb0/1780 +4i`, i=0..10 | 11 фиксированных x/y/width/height, исходный порядок полей; не зависят от размеров DIB |
| Bitmap+`0c` | Count11 только после всех44 metadata writes, только при ненулевой surface |

Missing/key-failure wrappers сохраняются до следующего вызова, с исходными
нетронутыми count/rectangles. Malloc может повторно выдать освобождённый адрес;
каждое поколение имеет свои bytes/masks и live flag. Freed snapshots не становятся
новым backing. Provenance Windows allocator и родитель4236d0 пока открыты.

Дополнение [записи настроек панели](MENU_INFO_WRITING.md), S/D43c690/43c710:

| Поле | Правило |
| --- | --- |
| `4527b0/44d784/44d788` | Defaults после file IO: только UInt32 now\0, затем0, затем4, даже при failed open; cache не меняет |
| `453c68/453d40` | Два sprintf ad%d; defaults после file IO/reset, cache перед fopen |
| Declared FILE+`0/4/8/0c/10/14/18/1c` | ptr/count/base/flags/fd/char buffer/size/temp filename; все32 байта сравниваются |
| Output buffer | Маска записи и старые bytes сохраняются; failed full-block flush всё равно пишет следующий byte в base[0] |

FILE начинается с flags102 и пользовательского буфера; это явный fopen response.
Fclose после flush сбрасывает ptr=base/count0, затем вызывает descriptor close
и очищает flags, оставляя base/size/bytes. Cache возвращает fclose result либо0
при failed fopen; defaults оставляет EAX последнего sprintf. Логические LF bytes
проверены до Windows text translation. Общий new byte-output helper не моделирует
другие stream modes, EILSEQ42 или Windows fopen/allocator provenance.

Дополнение [общего обновления панели](MENU_PANEL_UPDATE.md), S/D всего4236d0
через caller427127 после свежего early-screen parent:

| Поле | Правило |
| --- | --- |
| `4554a4` | Enter до чтения статуса, Leave после полного пути; при явной остановке до unsupported scanf Leave ещё не исполнен |
| `458424` | Только значение2 запускает обновление; очистка в0 после сравнения версий и до children |
| `44d77c/44d778` | Signed32 incoming/current version; результат сравнения сохраняется до последующих записей content loader |
| `44d784` | Каждая попытка начинает с `(Int32(1) &- oldIndex) % 2`, signed remainder; произвольный старый индекс не обязан восстановиться после двух переключений |
| `458350 → 4527b0` | Byte-copy через NUL при incoming<=current или первом успешном content+bitmap; второй успех не копирует дату |
| `458420` | Поколения принадлежат shared bitmap loader; defaults/cache не возвращают уничтоженный bitmap и не освобождают новую пустую оболочку |

Двойная ошибка выполняет defaults и затем cache, включая повторную файловую
запись.1104-byte scratch берётся из настоящего child entryESP−454 как явный
before-input. Все25 ранних bitmap и World сохраняются после каждого случая;
статус и частичные записи сохраняются при двух контрольных остановках до43c817.
Это не установление worker/Windows startup provenance или полный ранний экран.

Дополнение [тела раннего экрана](FRONT_SCREEN_BODY.md), S/D42712c..4275cb:

| Поле | Правило |
| --- | --- |
| Caller local+`48/84/64` |28/31/29-byte исходные константы; затем byte subtract index&3 с повторным strlen |
| Caller local+`14/20` | Wrapped ybase45757c+491; target surface из прежнего caller frame, не обязательно global455608 |
| Caller local+`a4/ae/b0` | Общая копия suffix авторской строки через NUL; отдельные truncation bytes сохраняют прежний хвост |
| `4546f0/453cdc/45757c` | Signed mouse x/y и vertical shift; website upper-y guard отключён при shift0 |
| `44d060/457580` | Previous/held: клик только0/1, held очищается перед звуком; previous здесь не обновляется |
| `458424/450be8` | Status1/2 → frame11; иначе setting0 → frames6/7, любой другой →8/9 |
| `44d064` | Только принятый клик кнопки записывает−3, перед sound helper |

Все25 ранних bitmap сохраняются без изменения. Текст берёт global455608,
bitmap — caller local+20. GetDC HRESULT возвращается неизменным; его ошибка
не отменяет родительскую обработку клика. Прежний notice overlay использует
тот же общий native401290. Actual GDI raster/Windows открыты; альтернативы4275cb
продолжены отдельным исследованием ниже.

Дополнение [сохранения настроек](SETTINGS_WRITING.md), S/D423230:

| Поле | Правило |
| --- | --- |
| `44fb70+50*g+4*i`, g0..3/i0..10 |44 signed32 значения, отдельный `%d ` для каждого, затем newline для каждой группы; остальные байты не меняются |
| `44fcc0+11*i`, i0..3 | Временные byte replacements: backtick→apostrophe, иначе space→backtick; длина пересчитывается, строки могут пересекаться |
| Пустые четыре имени | Только UInt16 digit/NUL; сохранившийся хвост не очищается |
| `44fd18/44f900/44f890` | Только при первом byte0 — исходные name/info/email defaults, после fprintf четырёх имён; записи4/4/2 и дополнительный byte email |
| `450be8/450be4` | Последовательные signed `%d\n` после default metadata |
| Имена после fclose | Только backtick→space; предыдущие апострофы остаются, включая результат перекрывающихся проходов |
| EAX helper | Последняя вычисленная длина четвёртого имени, независимо от fclose result |

Shared printf output count — отдельная величина от FILE count. Ошибка byte
ставит output count−1, следующий segment всё равно начинает запись и может
увеличить count до0. Prefix и value разделены. FILE flags/error и пользовательский
buffer сохраняют прежние правила `_flsbuf`; их нельзя обнулить при восстановлении
printf count. Полный FILE/CRT lifetime остаётся открытым. Настоящие menu callers
продолжены ниже.

Дополнение [альтернатив раннего экрана](FRONT_SCREEN_ALTERNATE.md), S/D4275cb..42790f:

| Поле | Правило |
| --- | --- |
| Caller EAX / `44d064` | Входной EAX выбирает−3/−1/0/other; глобальный selector может отличаться. После выбора enable/disable/cancel глобальный selector0, но текущая ветвь продолжается до42873e |
| `4511f4` | Int32 анимация панели.0 оставляет global0 и local y96; иначе wrapped y+high32(signed−715827883*(y+90))+signCorrection. Раскрытие−17; выбор enable/disable обнуляет global, сохраняя прежний local y |
| `450be8`, `457580` | Enable/disable пишут1/0 и очищают held **до**423230; null FILE оставляет эти изменения, selector/y ещё не сброшены |
| `4511f0` | Test low byte bit0; при0 DWORD OR1 и первый вызов таймера |
| `4511ec` | UInt32 отметка timeGetTime. Разность unsigned с переполнением; шаг только при>150. После шага новый отдельный вызов таймера |
| `4511e8` | Int32 число fill элементов; при шаге wrapped(count+1) signed remainder14. Положительный count без clamp определяет число415160 |
| `4511e4` | DWORD счётчик проходов waiting screen, add1 с переполнением даже при отмене |
| `44d060/457580` | Click только0/1. В waiting cancel sound455614 предшествует обеим global writes; previous здесь не обновляется |

Общий worker gate4237e0/43c450 теперь используется prefix и enable dialog;
реальный worker остаётся границей. Fill DDBLTFX сохраняет92 untouched bytes/masks,
переданные до каждого child call. Эти правила пока не подключены к app UI.

Дополнение [возврата раннего меню](FRONT_MENU_COMPLETION.md), S/D:

| Поле | Проверенная композиция |
| --- | --- |
| `451170/45117c/4511a0/45118c` | Главное меню, подсветка, cursor и MENU_WAIT теперь вызывают43f010 на настоящих ранних wrappers |
| Bitmap+`0c` у cursor/wait | Untouched count. Frame−1 рисует whole image, затем отдельное frame<count может дать второй проход; A5/ramp различаются |
| `44d060` | Tail записывает текущий457580 после shutdown guard; network error direct epilogue не делает эту запись |
| PTD+`14` | От actual initptd=1 к39 последовательным table rebuilds на корпус; каждый actual rand result/before/after проверен, между menu cases seed не сбрасывается |
| World+`0`, `4511ac` | Выбор первого пункта→1, следующий полный4246b0 освобождает живой фон и пишет World2. Global4511ac=0; dead bitmap bytes/masks сохраняются |
| `4588a8/4588ac` | В этой свежей цепочке подтверждены нулевые BSS pointers; выделение/free replay этим корпусом не расширено |
| Caller stack | Настоящий ret4, SP1000f42c, четыре исходных nonvolatile registers и SEH12345678. Полный scratch main-menu/overlay здесь не объявляется native storage |

Ранний main-menu runner теперь требует только World/globals/CRT. Подготовка
матча использует тот же код через свой прежний API. Ожидаемые after records
не подставляются в native; raw surface tokens берутся из declared parent inputs.

Дополнение [повторных ранних вызовов](FRONT_MENU_LOOP.md), S/D:

| Поле | Проверенная последовательность |
| --- | --- |
| World+`0` | Каждый4246b0 заново выбирает World1/World2/other из сохранённого собственного состояния. World2 останавливается на настоящем вызове41bc90 |
| `4511f8` | DWORD `1−old` в42476b для other World; сохраняется между вызовами. Контроли MIN/MAX/−1/0/1/2 подтверждают wrap, не Bool |
| `44d068` | После первого settings load равен0; повторные4246b0 пропускают ранние24 constructor, сохраняя wrappers |
| `44d064` | Prefix выбирает body только при0. После body его фактический EAX соответствует обновлённому global; клик настроек может выбрать−3 в том же внешнем вызове |
| `4511f4/4511ec/4511e8` | Анимация и waiting timer/count сохраняются между полными ret4; вычисления выполняются общими обработчиками |
| `44d060/457580` | Tail обновляет previous; body/settings могут очистить held раньше tail. После входа41bc90 held ещё не сброшен: запись424746 ждёт возврата loading |
| `4511ac` | Объявленное обнуление вызывает новую конструкцию MENU_BACK13, не освобождая старую MENU_BACK1. World1 освобождает только текущий wrapper |
| PTD+`14` | Пять последовательных RNG rebuilds/15000 actual rand на корпус из собственного первого состояния CRT; никакого нового seed между вызовами |

Нативный dispatcher вызывает фазы по собственному состоянию; reference только
проверяет их порядок и подаёт объявленные ответы платформы. Scratch192bytes
задаётся перед body как raw input и проверяется после его записей. Проходы без
body не объявляют полный caller scratch восстановленным. Partial state после
null FILE сохранено; следующий probe — новый внешний caller, не Windows SEH recovery.

Дополнение [меню→первая загрузка](MENU_LOADING.md), S/D:

| Состояние | Проверенный переход |
| --- | --- |
| World22000020 | Собственный constructor/menu World2 продолжается41bc90; указатели catalog/400 Actor записаны исходными загрузчиками, без нового World |
| ESP1000eff8 | Реальный return424746 и argument28002020; пролог41bc90 выравнивает стек к body1000e9bc |
| `44eecc/453e0c/455634/458348` | До первого меню объявлены audio/surfaces28002020 и mode1/3. Они сохраняются до common loading и каталога |
| `45118c` | Реальный ранний MENU_WAIT в41bea9; сохранены count+0c, raw reads, clip, ret24/EAX. A5:1 Blt; ramp:2 Blt |
| `452948+4*index` | Каждый из400 новых registry outputs получает собственное before-значение исходного меню; общий звук не очищает эти слова. Native связывает также прежний44eecc с каждым WAV input |
| Ранние bitmap | Все26 records/liveness неизменны после loading; текущий фон уже dead, старый фон live. Новые10 UI wrappers принадлежат загрузчику отдельно |
| World mask | Один общий tracker конструктора/меню/loading. Присоединение наблюдателей не обнуляет bytes или provenance |

Загрузчик Native получает World/globals от своего проверенного раннего
dispatcher. Raw after snapshots только проверяются. Frozen timer/file/scanner/
COM boundaries сохранены; полный41bc90 ret и Windows lifecycle остаются открытыми.

Дополнение [собственное startup продолжение](MENU_STARTUP.md), S/D:

- World22000020 и400 Actor после меню/loading продолжаются через input и round
  без новых игровых записей стенда. Оба natural варианта имеют phase1/pause0;
  control означает ramp/reverse addresses, а не паузу старого standalone caller.
- Command buffer лежит по bodySP+434; соседний playback buffer+440 сохранён.
 419a60 возвращается в41c5e5,4198f0 вызван41d490 и возвращается41d495.
- Настоящий429730 создаёт собственный menu frame1000df08 из body1000e9bc.
  Старый1000df48 был связан с другим внешним loading caller и не подставляется.
- Replay pointers всё ещё0; новых replay allocations нет. Saved-playback
  bytes458588..4588a7 — отдельный PE input до первого меню, сохранённый в context.
- Music allocations31000000 и menu bitmaps32000000 не пересекают живые ранние
  wrappers/панель/PCM. Старые26 records/liveness и CRT сравниваются до/после.
- Native теперь соединяет local/control/received/replay/round в
  OriginalLoadedMatchEntry. Music и character-menu resources используют его
  собственный menu результат; selection dispatch431d10 пока открыт.

Дополнение [общий ввод и режимы](MODE_SELECTION.md), S/D самостоятельного
corpus; происхождение собственного caller431d10 ещё не соединено:

| Поле | Восстановленное назначение |
| --- | --- |
| `World+194`,8 pointers |431b70 читает все восемь независимо от activity/status/Object; alias допустим |
| `Actor+cd/ce/d0/cf/d1/d2/d3` | Приоритет up/down/right/left/attack/jump/defend; любой ненулевой byte |
| `451320+4*seat` | DWORD-защёлка места; любой ненулевой запрещает новый edge, полностью отпущенный проход сбрасывает в0 |
| `4513a4/a8/b0/ac/b4/b8/bc` | Общие DWORD edges, обнуляются431b70 каждый раз; разные места могут выставить разные флаги |
| `451160` | В431d10 — выбранный режим0..7; signed wrapping navigation и network skip6 |
| `4512c8` | Четвёртый аргумент431d10, selection/seat; не указатель на mode. Обнуляется при подтверждении0/1/4 |
| `4513c0` | Таймер подсказки; navigation/attack сбрасывают0. Полный help/tail ещё открыт |
| `44d020` | При confirmation0/1/4 становится3;2→20;3→120;5→2 |
| `44d024/44d028` |0/1/4 задают100/1;2/3 задают0/0;5 сохраняет прежние значения |
| `44d780` |0..5 confirmation задаёт-1 до освобождения фонового wrapper |
| `44eecc` | Quit вызывает только4019b0. Music/replay здесь не освобождаются; это не весь ранний shutdown |

Дополнение [собственный экран режимов](MODE_SCREEN.md), S/D1264 случаев:

| Поле / область | Уточнение |
| --- | --- |
| `451b7c` |431d68 очищает перед первым fill |
| `4511ac` | Общий фон423840: unsigned timeGetTime%13+1; никакого расхода CRT/game RNG |
| `4513c0` | Положительный таймер рисует28 подписей, затем уменьшается; mouse tail ставит450 после отрисовки |
| `4513c4` | Previous mouse held экрана режимов; ссылки требуют0/held1, последний tail копирует уже изменённый held |
| `44fb74+80*seat+4*button` |28 кодов клавиш для исходного422b00, четыре колонки по7 |
| `453da4` | Сдвиг фона/логотипа/списка и mouse hint hitbox; исходное Int32 overflow |
| `45757c` | Сдвиг трёх подписей и URL, отдельный от453da4 |
| `body+10..body+714` |704 local bytes/masks; mode/menu/target/World/selection pointers, строки и scratch |
| `body+14` | Сначала указатель menu; key-name заменяет его горизонтальной поправкой текста |

Собственный mode body ESP=1000d7dc, local base=1000d7ec. После ret16 ESP
возвращается к1000df08/PC429eb7. Cookie и сохранённые регистры лежат за пределами
этих704 байт. Поздние caller probes задают EDX1/ECX2 как настоящий dispatch
prefix. Финальный VS-confirm даёт menu3; выбор персонажей ещё не исполнялся.

Дополнение [собственный возврат из меню](MENU_RETURN.md), S/D190 output cases;
только первые два проходят все вложенные эпилоги:

| Поле / область | Уточнение |
| --- | --- |
| `44d058`, `44f1af` | Network notice требует signed DWORD>0 и signed byte>0; count уменьшается после text, включая failed GetDC |
| `451158`, `451154` | Только menu44d020==0: первый DWORD=0, второй=сырой UInt32 timeGetTime |
| `457580` | Безусловный0 только после настоящего ret4 из41bc90 в424746 |
| `1000efb4`, `1000f418` | Собственные SEH records match и outer frame; исходные эпилоги восстанавливают FS:0 до12345678 |
| `45560c..45561c` | Initial WinMain assignment открыт; volume probes явно используют первые пять уже загруженных common WAV buffers451db0 |

После429730/ret12 ESP=1000e9bc; после41bc90/ret4 ESP=1000f000;
после4246b0/ret4 ESP=1000f42c и восстановлены четыре nonvolatile registers.
Поздние188 probes начинают4229e2 и заканчиваются до422a95, не реконструируют
следующий целый caller/SEH. Native сохраняет функциональное состояние и порядок
событий; исполнение x86 SEH относится только к исследовательскому стенду.

Дополнение [повторный внешний цикл](MENU_CYCLE.md), D8 вызовов от4246b0:

| Поле / область | Подтверждено на собственном состоянии |
| --- | --- |
| `450b90` | Повторные фазы0/1/0/1 получены самим41bc90; никакой подстановки фазы |
| `450bfc`, `44fb60`, `44fcb0` | Phase0 копирует очередь паузы по прежнему правилу; все эти значения в новой естественной цепочке0 |
| `4553c2` | Acquired J из собственного control.txt: только два OS inputs100 и117 |
| `Actor0+d1` | На фазе1 после нажатия остаётся0; на следующей phase0 становится1; после отпускания в phase1 остаётся1 |
| `44d020`, `4512cc` | Третий вызов подтверждает VS/menu3; четвёртый сохраняет menu3 и обновляет4512cc=3 перед429e5a |
| `4511ac` | Настоящий mode confirmation освобождает текущий фон; остальные live/dead wrappers сохраняются |

Шесть полных ret4 восстановили прежние nonvolatile/FS; ещё два вызова
заканчиваются перед selection dispatch429e5a. Menu3→1 и последующие seat latches
этим corpus ещё не исполнены. Не очищать current attack вручную из-за того, что acquired keyboard
уже117: исходный local input применит отпускание в следующую phase0.

Дополнение [выбор персонажей](CHARACTER_SCREEN.md), D68 собственных экранов:

| Поле / область | Подтверждено в VS последовательности |
| --- | --- |
| `451288/451248/451268`, по8 dword | Menu3 задаёт status0/selected−1/latch1; удержанное подтверждение VS не присоединяет место |
| `Actor+364`, все400 | Общая проверка signed team0..4, выходящие значения становятся0; все маски сохранены |
| `451224` | Wrapped(old+1) signed%30; собственная последовательность завершается4 |
| `Actor+368`, `451248` | Generic right по type0 и source ID gate выбирает ordinal17/21; имена персонажей не управляют Core |
| `World+4..+b` | Каждый экран явно обновляет8 activity bytes по итоговому status ровно3, включая не участвующие места |
| `44d078/44d074/4512c8` | После ready обоих: countdown147, delayed gate0, selection0; переход к следующему интерфейсу ещё не исполнялся |
| `Actor+d1`, `451268/45126c` | После последнего настоящего phase0 attack отпущен, оба latch0; status3 может очистить latch в том же кадре после подтверждения status2 |

Полученное состояние сохраняется через собственные ret12/ret4/ret4 до STOP;
следующий caller должен продолжить его. Team0 остаётся independent, ещё не
переведён в игровые slot+10 команды позднего MATCH_PREPARATION. Конструкторные
незаполненные bitmap поля остаются с исходными байтами/масками и входят во все
read/clip/Blt проверки; catalog tracker metadata отделены от следующего input.

Дополнение [собственный Start](MATCH_SELECTION.md), D100 новых внешних входов:

| Поле / область | Подтверждено в собственном VS/count0 пути |
| --- | --- |
| `44d078`, `4512c8` | Jump ускоряет на30 поверх−1/кадр;147→−21 и selection1, финальный countdown−53/selection3 |
| `44d070`, `451220` | Sentinel−100 становится0 из исходных2 independent teams/6 inactive; popup обнуляет451220 |
| `451228..451244` | Флаги signed selected<0; шесть неактивных мест также получают ordinal через общий RNG tagd7 |
| `451248..451264`, `Actor+368` | Перед Start17,21,24,38,39,32,22,36, с немедленным обновлением binding каждого места; исходные ID и ordinals различаются |
| `4511fc`, `451200` | Даже при CPU0 выполняется чтение текущего computer index/seat; собственные значения0/0 |
| `44d06c`, `44d024`, `44d028` | Down2→3; arena100→99→0 и random flag0; up3→2→1→0, затем Start |
| BG `+3cc` | Имя из собственного загруженного BG, включая специальные99/100; вывод до обработки ввода |
| `44f18c`, `44f128`, `44f060`, `44eed0` | Музыкальный choice0, label Random/title Music: Random; каждый кадр настроек выбирает путь, последний bgm\boss2.wma |
| `450bcc`, `450c34` | Общие index35/counter35:6 roster +29 music RNG. До будущего43d2c0 counter ещё не сброшен |
| `44d020`, `World+0`, `451224` | Перед42cf8a menu1/World2/pulse24; menu0 и игровая подготовка ещё впереди |

Последний press Start применён собственным phase0. Пять caller counters
сравниваются как signed значения исходных UInt32; в частности+28 получает−1
при selection!=0. Полные игровые записи, bitmap backing/masks и порядок событий
сохраняются; подробные границы проверки находятся в MATCH_SELECTION.

Позднейший [MATCH_LAUNCH](MATCH_LAUNCH.md) продолжает это же состояние до
первого41e339. Подготовка даёт team10/11, x442/289, y0/0, z504/519,
HP500/500 и RNG39/39. Все15 слоёв District добавлены к собственному каталогу;
десять initial UI ресурсов теперь сохраняются в `OriginalMatchPreparation.interface`.
`43d2c0` копирует собственные настройки и все18 Actor, включая шесть случайных
неактивных привязок, в новый calloc`0x630e18`/`72000020`. `4588ac` остаётся0,
`4588a8` связывается с общей input/replay memory; RNG counter39→0/index39.
`450b8c` сбрасывается88→0, затем следующий внешний phase1 записывает packet0
и checksum1000 в`buffer+14b8`, увеличивая tick до1. Весь буфер/маски проверяются
после дальнейших стадий; результаты не копируются обратно как новые входы.

[WORLD_CONTROL](WORLD_CONTROL.md) теперь продолжает это состояние до41e634.
Actor+33c — искомый sourceID для501; +324 получает прежний sourceID после
успешного поиска; +368 хранит ordinal выбранной первой записи каталога.
Actor+2f4 сравнивается с номером текущего слота при обновлении живых связанных
объектов. World+194 допускает alias одной Actor-аллокации; ordinal0 не null.
Два собственных прохода управления сохраняют весь pool/globals/resources/replay,
а1491 synthetic случай проверяет изменения и порядок.

[ACTOR_PHYSICS](ACTOR_PHYSICS.md) и [WORLD_PHYSICS](WORLD_PHYSICS.md) продолжают
обе собственные цепочки до41eed1. Во всех15363 исходных кадрах проверены три
объявленных входа движения;515 synthetic World cases проверяют весь pool.

| Поле / область | Подтверждено новым физическим проходом |
| --- | --- |
| `Actor+3e8/3ec/3f0/3f4`, Int32 | Блокировки z−/z+/x−/x+ требуют ровно1 и очищаются после перемещения |
| `Actor+31c`, Int32; `Object+90/+94/+a8` | Durability, его начальное значение/потеря и signed sound index; конструктор Actor сам31c не инициализирует |
| `Actor+304/+30c/+310/+314`, Int32 | MaxHP/lives и сохранённые значения для возрождения; порядок копирования в300/2fc сохраняется |
| `Actor+318`, Int32 | Возрождение sourceID30..36 ставит140; общий смысл прочих читателей ещё требует исследования |
| `Actor+324`, Int32; `+dc`, byte | Обратный sourceID и combo progress3; DC очищается до поиска,324 только после найденного Object |
| `Actor+8`, Int32 | Death gate строго1..4, respawn устанавливает20; не заменять проверку на общий Boolean |
| `World+4..<404` | Physics допускает любой ненулевой activity; среднее для respawn требует activity ровно1, без HP-фильтра |
| `457588`, `457bc8`, `452170` | Catalog sound flag/left/right; queue416fb0 предшествует реальному устройству |
| `45971c` | Флаг CPU для4450d0, вне снимка globals44d000..458440; явный sse2Conversion в Actor API, собственный corpus использует legacy0 |

После первого собственного физического прохода оба frame219, binary x/z
442.1/504.1 и289.1/519.1, integer x/z прежние, y/скорости0. HP500/MP200,
RNG39/0, phase1/tick1, полный replay/ресурсы сохранены. Integer и binary
координаты не сливать: spawn998 копирует integer z+1, binary z без прибавки;
respawn записывает только binary x/z. Созданный998 получает frame6, текущий
восстановленный Actor сохраняет219, если последующие исходные ветви не меняют его.

[WORLD_LINKS](WORLD_LINKS.md) продолжает собственное состояние до41eed8.
Поля второго417f80-прохода теперь имеют общую нативную реализацию:

| Поле / область | Подтверждённое использование417f80 |
| --- | --- |
| `Actor+98`, signed Int32 | Отрицательное значение включает обработку удерживаемого объекта; ошибочная связь очищает только98 |
| `Actor+a0`, UInt32 gate; `Actor+9c`, Int32 | A0 — слот владельца<=399, его9c обязан равняться текущему номеру слота; ссылки относятся к слотам, не аллокациям |
| `Frame+d8..f8`, девять Int32 | wpoint kind/x/y/weaponact/attacking/cover/dvx/dvy/dvz;417f80 не использует attacking |
| `Frame+50/+54`, Int32 | Центры владельца/предмета после назначения weaponact; сохранённый старый owner wpoint остаётся отдельной ссылкой |
| `Actor+2f8`, Int32 | Явный бросок type1/4/6 сохраняет номер владельца; это не2f4/354 |
| `Actor+28/+30/+20` | HitCount==1 выбирает hit velocity для выпадения state12/10; иначе обычные40/48 |
| `Actor+cd/+ce`, bytes | Любой ненулевой current up/down управляет signed wpoint.dvz; оба/ни одного сохраняют vz |
| `BG+4/+8`, signed Int32 | Два последовательных ограничения binary z для active type0, затем4450d0→integer z |

Два собственных первых вызова417f80 не меняют послефизическое состояние,
но исполняют все400 слотов каждого прохода. В synthetic случаях бросок может
ограничить binary y до−2 после конверсии, оставив прежний integer y. ID123
проверяет MP предмета, затем присваивает MP владельца150; не менять адресата
проверки. При исчерпании предмета9c/A0 получают0, обычные броски оставляют их.

[WORLD_CAMERA](WORLD_CAMERA.md) продолжает собственный вызов до41f496.
Границы и фон используют живые101 BG-записи `OriginalMatchPreparation`,
сохраняют исходный каталог загрузки отдельно и обновляют400-slot World/pool:

| Поле / область | Использование в полном41b5d0/41a250 |
| --- | --- |
| `BG+0/+4/+8`, signed Int32 | Ширина, min/max z; камера последовательно ограничивается0, затемwidth−794, который может быть отрицательным |
| `450bc4/450bc8`, signed Int32 | Позиция/скорость камеры, обе сохраняются между тактами; в первом own вызове0→1 |
| `450bb0/450bb4`, signed Int32 | Разные верхние ограничения цели камеры и x бойца, с разными нулевыми/положительными gates |
| `450b74/450b84/450b7c` | Совместный replay gate переопределяет позицию и сохраняет результат clamp, не отменяя обновлённую скорость |
| `BG+824+4*layer`, signed Int32 | Изменяемый счётчик: wrapped increment и signed remainder при period>0; сохраняется при пропущенном рисунке |
| `BG+914..988`, index+1/zero-null | Живые bitmap bindings слоёв; BG99 использует914/918/91c |
| `455608` и retained caller target | Fill использует глобальную поверхность; bitmap получает отдельный аргумент. В own caller target28002020 из исходногоesp+68 |

Полный pool после собственной камеры не меняется; девять counters District
увеличиваются0→1. Bitmap+0c остаётся undefined после конструктора, и renderer
сохраняет маску при чтении. Новая функция атомарно публикует состояние, но
реальное выполнение device requests должен откладывать внешний игровой такт.

[WORLD_DRAWING](WORLD_DRAWING.md) восстанавливает весь41a5a0 с Actor/Object
children. Рисование предшествует планировщику и само изменяет spark storage:

| Поле / область | Использование в41a5a0/40de30/40be70/40bf30 |
| --- | --- |
| `Actor+18`, signed Int32 | Стабильная глубина сортировки всех активных слотов; ties сохраняют slot order и aliases |
| `Actor+8`, signed Int32 | Wrapped abs/remainder4; разные gates −70 для тени и−25 для спрайта/имени |
| `Actor+b4`, `450bd8` | Отрицательное b4 добавляет phase*6−3 к x спрайта/point |
| `Actor+318`, signed Int32 | Смещение Frame.pic при выборе рисунка; width lookup его не использует |
| `Object+498/62c/6a4/6cc` | Signed count/first/columns/rows; first matching wrapped picture range |
| `Object+754/+77c`, index+1 | Обычный/зеркальный bitmap; в обоих случаях helper получает mirrored0 |
| `Bitmap+fb0+4*(pic−first)` | Ширина обычного листа для facing1; backing и маска не исправляются |
| `Actor+30c`, signed Int32 | Lives label при>1, только две последние десятичные цифры при>9 |
| `44fcc0+11*slot` | Сырые NUL-строки, не ограниченные stride; signed Int8 glyphs |
| `Actor+36c/370/398/3c0` | Live count и x/y/frame sparks; frame++ после draw, удаляется только последний invalid элемент |
| `Frame+80/+84`, signed Int32 | При lowHP/positive80 задают anchor для1×3 source rectangle0,20; state9997 пропускает эту ветвь |
| `BG+14/+18/+98c` | Размер и bitmap тени; деление signed размеров пополам к нулю |

Глобальные bitmap-указатели остаются opaque tokens и могут ссылаться на ранние
меню-ресурсы, initial interface или каталог. Они не превращаются автоматически
в ordinal. Публичный World draw сохраняет всю подготовку матча при ошибке;
вывод на device должен публиковаться после успеха внешнего такта.


[WORLD_IMPULSES](WORLD_IMPULSES.md) продолжает собственный вызов до41f550:

| Поле / область | Использование4196f0 и диагностического caller |
| --- | --- |
| `Actor+b4`, signed Int32 | Любое ненулевое значение откладывает весь impulse pass этого Actor |
| `Actor+20`, signed Int32 | Счётчик; ненулевой включает деление на wrapped signed(count+1), после трёх velocity stores становится0 |
| `Actor+28/+30/+38`, binary64 | Pending векторы; при count0 всё равно очищаются в+0; в собственном первом такте ещё содержали конструкторные0.1 |
| `Actor+40/+48/+50`, binary64 | Новые скорости при count!=0, отдельные stores после extended multiply/divide; count0 не меняет их |
| `World+1bc` | Указатель слота10, источник диагностических bytes независимо от активности |
| `Actor+c4/c5/c3/c2/be/c0`, signed Int8 | Аргументы исходного диагностического sprintf; это не held-input cd..d3 |
| FPU control word | Состояние CPU/thread, не поле World/globals. Own harness наследует0; controlled corpus задаёт037f. EXE startup выбирает53 бита: см. [обязательную коррекцию](FPU_PRECISION.md) |

Все48 изменившихся pool bytes собственного прохода — шесть pending binary64
значений двух бойцов. Другие записи/маски/ресурсы сохранены. Native53-bit
арифметика и новые цепочки с восстановленным FPU context пока открыты.


## Оконный производитель ввода

[WINDOW_INPUT](WINDOW_INPUT.md) сравнивает целые выбранные callbacks с полными
байтами и масками. Это объявленные контрольные входы;385 связанных вызовов
сохраняют собственный нативный выход. Полное происхождение CRT/WinMain и
перевод событий macOS пока не восстановлены.

| Адрес / offset | Значение и порядок |
| --- | --- |
| `455378+VK` | Keydown100 пишет byte100 после text helper; keyup101 пишет117. VK0...255; остальные значения вне текущего контракта |
| `458440+0` | Текст активен только при точном1; Enter13 очищает. Constructor4031b0 пишет0 |
| `458440+4..12f` | 300 сохраняемых text bytes; B..Y, digits, space, dot. Constructor не очищает |
| `458570` (`+130`) | Живой index. После character at299 значение300, затем NUL пишет в младший byte самого index→256; это наблюдённый alias внутри record |
| `458574` (`+134`) | Total увеличивается перед index при принятом символе; backspace не уменьшает |
| `45857c`, `458578` | Состояния LF2.NET и HEROFIGHTER.COM. Первая обрабатывается раньше; constructor этих слов не пишет. Начальные нули явно заданы |
| `455471`, `455470` | Sequence completion пишет100; это sentinel indices249/248 в keyboard storage |
| `4546f0`, `453cdc` | X/Y mouse200: unsigned16 halves, запись X перед Y; button messages их сохраняют |
| `457580`, `4527e4` | Left/right mouse flags,201/202 и204/205 |
| `453fd8`, `454008` | Joystick1/2: bounds+0/4/8/c; X/Y+10/14; byte up/down/right/left+18/19/1a/1b; buttons+1c..1f. Wrapped signed quarter thresholds |
| `4588a8`, `4588ac` | В ESC answer6 каждый live buffer сначала free, затем pointer clear; первая пара до второй. Явное владение отдельно от глобальной записи |

Local458440/0x140 включает312-byte editor и два sequence words. Это не
разрешение читать произвольный индекс или импортировать исходный after-state.
Счётчики и backing остаются известными только в объявленном домене; поздняя
ошибка observer/неизвестное index/отсутствующий replay owner откатывает весь
callback. Внешние эффекты нужно буферизовать до commit.


## Жизненный цикл окна и режима вывода

[WINDOW_LIFECYCLE](WINDOW_LIFECYCLE.md) сравнивает318 callbacks и две
инициализации. Windows API outputs и доставка сообщений остаются объявленными
контрольными входами; фактическая reentrancy/WinMain provenance не установлена.

| Адрес | Подтверждённое поведение |
| --- | --- |
| `458434` | SYSKEYUP Enter пишет1 перед сменой режима/уничтожением, затем0 после ShowWindow. Destroy2 читает после sound/music/replay cleanup; только0 вызывает PostQuitMessage |
| `458430` | При включённом44d794 заменяется на(old==0 ? 1 : 0). Move и cursor проверяют nonzero, не только1 |
| `44d794` | Gate смены режима; debug Alt enter precedes read. Caller не проверяет Alt-bit в lParam |
| `455608`, `455634`, `457578` |401a80 при draw!=0 освобождает/очищает back, primary, draw по порядку. При draw0 оба surface words остаются прежними |
| `4546f4` |401ae0 вызывает DestroyWindow(nonzero), но не очищает HWND. Последующий43bdd0 записывает новый результат, в том числе0 |
| `4554c4`, `457584` | Палитра и retained clipper не освобождаются/очищаются401a80. Source external311 не проверяет primary-null; достижимость такой доставки остаётся открытой |
| `453ccc..453cdb` | Client rectangle16bytes: GetClientRect, затем два live ClientToScreen at+0/+8; fullscreen SetRect(0,0,metric0,metric1), с metric1 раньше0 |
| `451dac` | Size5 exactwParam1: InvalidateRect→store0; остальные значения store1; оба return0 |
| `4554c0` | Instance для внутреннего43bdd0; native configure использует retained input, wrapper43bec0 отдельно пишет его |

Сохранённые18 callbacks используют выход собственных двух инициализаций.
479helper-entry backings сохраняют неизвестные поля567 оконных/display-структур;
маски описывают только поля, записанные helper. Порядок API/CPU stores проверен
целиком, но реальные Windows stack/device bytes ими не устанавливаются.

## Локальные параметры музыкальных событий

[GRAPH_EVENTS](GRAPH_EVENTS.md) сравнивает371 полный callback400 и4 отдельных
создания графа. У401e90 после сохранения EBP и выравнивания ESP вниз64 выделяется
64-byte frame; в этих whole callbacks его адрес2000ef80.

| Адрес / offset | Подтверждённое поведение |
| --- | --- |
| Frame+34 | Event code. API пишет первым, затем caller читает перед seek и повторно перед FreeEventParams |
| Frame+3c | Parameter1. API пишет вторым, но CPU читает после parameter2 |
| Frame+38 | Parameter2. API пишет третьим; CPU читает перед parameter1 |
| `44f040`, `44f044`, `44f048`, `44f04c` | Graph/control/event/position.401c90 пишет независимые API outputs; отрицательные query HRESULTs не отменяют их |
| `4546f4` | Notify event+34 получает этот HWND и message400. В own graph chain HWND объявлен, не получен из полного WinMain |
| `44ef04` |401c90 очищает только первый byte после notify+34 и flags+38; create failure не очищает |

Отсутствующий GetEvent output сохраняет прежнее слово. Даже E_ABORT может
сначала записать outputs; только точный0x80004004 прекращает обработку.
Остальные статусы читают local code, при1 вызывают seek с binary64+0, затем
FreeEventParams(code,param1,param2). Globals во всех callbacks неизменны.
Пять последовательных notifications используют собственный native graph,
полученный от initializer с исходно нулевыми четырьмя слотами.

Начальные64 bytes объявлены через stack pattern; это не восстановление полного
стека приложения. Empty E_ABORT не требует ни одного из неизвестных слов.
Нужное неизвестное чтение, отсутствующий interface, exhaustion и поздняя ошибка
дают полный native rollback. Реальная COM reentrancy/доставка и device lifetime
остаются открытыми; внешние эффекты буферизуются до общего commit.
