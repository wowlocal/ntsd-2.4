# Host gameplay validation — frozen plan, 2026-09-22

Follow [WORKFLOW](WORKFLOW.md), [TASK_TEMPLATE](TASK_TEMPLATE.md),
[CURRENT_WORK](CURRENT_WORK.md) and the
[implemented gameplay candidate](APPLICATION_HOST_GAMEPLAY_CANDIDATE.md).
Base HEAD `066f8c2fe917c0eeb8ad04dbb176488e11f44aa4`. Previous turn was progress:
it implemented and published a frozen1040-file candidate, syntax-checked19 changed
paths, retained69 method definitions and verified its patch/archive. Validate
that actual candidate and the retained gameplay/paused body through the whole
host return, toward the first Naruto/Sasuke District match and complete game.

## Inputs, environment and scope

APFS-clone all1040 files from application-host-gameplay-candidate-20260922 using
distinct regular inodes. Verify bodies/modes/membership; copy no build/cache.
Candidate manifest SHA256:
`4deadc3ce77d2151662b41df365e9ca2c9c4c6ab1da7f8eb0161ec11a0b282cc`.
No candidate/Core/test/reference/fixture/resource/expected/mask edits in this task.
Protected root1034 and all prior candidates, failures, archives, plans and source55
remain unchanged. Include actual dirty inputs; HEAD alone is not the working tree.

Only the original distribution is the behavioral reference: EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Reuse the unchanged saved source readers and independently calculated Native
projections. Their synthetic mappings/API/keyboard responses, FPU/ABI,
installed-library, bitmap-mask and unknown-storage limits remain explicit.
Native tests are not actual Windows/device or installed whole-game evidence.
No new original execution, historical capture/auditor rerun, device IO, dependency
installation, sampling, result-file provider execution or safety-refused operation.
Existing round-result/replay-writer, Actor+7b4, War resource-fault and Windows ISO
incidents stay open; unavailable exact triggers remain unknown.

Fresh installed Swift6.4, Swift5/macOS14 release/WMO, native SwiftPM jobs2,
`-Xswiftc -enable-testing`, new scratch/cache/config/security, minimal
HOME/USER/LOGNAME/LANG/LC_ALL/PATH/TMPDIR environment. Pin toolchain version/SDK,
actual command and candidate inputs. Require fresh exact Core/ReferenceChecks/
Tests source membership and products (expected191/61/270 original sources),
385 fixture files and102 runtime resources as regular packaged files. No raw
resource override or reuse of prior binary passes for changed Core.

## Exact comparisons and limits

Run all69 frozen names from selected-methods1.json, one named XCTest process each.
Preserve the previous53 parent/host controls plus ten existing neutral/source/
active-output/paused/provider methods and six new host gameplay methods. Both
backings retain all17 neutral,48 active and14 pause/resume calls with actual host
parents, full state/masks/effects/journals/RNG/resources/outer-loop comparisons,
late failures, exact tickets, reentry, stage-specific retries and prior batches.
No expected/mask weakening, split case, skipped mismatch or smaller substitute.

Build3600s/12GiB aggregate RSS. Preserve previous53 limits from the completed
host plan: methods1–50 at600s,51–53 at900s/16GiB;26/28/35–37/43–50 at12GiB and
the other1–50 methods at8GiB. Newly selected54–63 use900s/16GiB. New host64–69
use1800s/16GiB because they traverse whole parents, both backings or additional
independent foreign/sibling and retry chains. These are declared bounds for new
work, not measured peaks or weakened comparison scope. Host physical RAM24GiB;
the source is a separate live process. Full queue14400s; poll1s/RSS2s/size15s.
RSS0 for a short process means no sample, not known zero memory.

Require exact named passed result, all three complete one-test/zero-failure suite
summaries, Selected-tests pass, actual exit0, no guard and verified process absence.
Stop on first build/test failure, guard or monitor error. Keep complete/partial
logs and distinguish compile error, test mismatch, resource limit, monitoring
failure, missing evidence and safety refusal. No correction or limit increase in
this task; a diagnosed correction needs a separate candidate/plan and preserves
this failure. Up to three separately identified rounds before contract diagnosis.

## Storage, processes and ownership

Task `/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-gameplay-validation-20260922`,
alias `build/research/application-host-gameplay-validation-20260922`. Verify X5
APFS UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548` and81GiB external/7GiB internal
at start. Allow24GiB observed decrease including concurrent source growth; stop
at22GiB and retain2GiB for closure/archive. Logical150GiB. Original40GiB/6GiB
reserves and source17GiB gross commitment remain. No deletion or reserve cuts.

Reuse existing clone/pin/guarded-runner/queue/package/archive procedures. Preserve
the established process ownership/transition/guard/reap body; verify its unchanged
code and finite positive/negative cwd/phase controls before execution. Exact
commands/config hashes go in existing job records. Revalidate PID/start/full
command/cwd/job before actions; only this task's identified Native descendants
may receive declared guard signals. Monitor errors do not prove a child stopped.
Source59727 is never restarted or signaled for silence.

Commands, in dependency order: prepare_application_host_gameplay_validation.py;
run_application_host_gameplay_validation.py with build1-config path/SHA;
verify_application_host_gameplay_package.py with task/build1; then
run_application_host_gameplay_tests.py with task. Python3 invokes these existing-
pattern helpers. Finalize with finalize_application_host_gameplay_validation.py
only after owned processes are terminal and the outcome/diagnosis is preserved.

Root author owns this plan, APPLICATION_HOST_GAMEPLAY_VALIDATION.md, matching
docs/evidence/application-host-gameplay-validation*, those five helper files and
own CURRENT_WORK/RESEARCH_MAP insertions. No root Native edit or promotion.
Independent review is unavailable and remains open; author checks are not review.

## Publication gates

Verify protected inputs, terminal processes, package bytes and complete artifact/
metadata archive membership/body/mode/nanosecond mtime separately. Publish the
actual outcome, failures and exact unstarted methods. If build fails, copied
resource bytes/partial products do not establish a complete package or Native
comparison. Commit each coherent checked increment with hooks intact.
Full catalog source/comparison, independent review, root promotion, installed
whole-application trajectory, native app/window/input/audio, Windows/devices,
first complete match and full game remain open until their actual criteria pass.
