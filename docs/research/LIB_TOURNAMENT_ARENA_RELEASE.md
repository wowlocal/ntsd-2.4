# Retained Tournament arena release

The original now executes51 complete preparations in17 retained three-call
sequences. Each ordinary arena is loaded, released while selecting special99,
then loaded again. The original432ab0ret12 and outer422ab8ret4 execute on every
call. This extends [Tournament preparation](LIB_TOURNAMENT_PREPARATION.md) at its
previously open nonempty-arena boundary.

The [finite plan](LIB_TOURNAMENT_ARENA_RELEASE_PLAN.md) identifies the pinned NTSD
EXE/lib.dll/VC80 and original DAT/BMP/DIB inputs. Unicorn2.1.4/CW023f executes the
original instructions; allocation, local time and COM/GDI are controlled research
responses. The three confirmation fields are explicit menu-boundary stimuli,
not outputs of an intervening played fight. Whole library/application startup,
Windows/device behavior and the native shipping runtime remain distinct.

## Produced ownership and order

Every independent parent executes the original World and400 Actor constructors.
Its first confirmation constructs actual arena layers and a complete recording.
Subsequent calls retain that entire World/Actor/catalog/resource/replay state.
Only44d020 (menu27),44d024 (arena) and44d028 (random selection0), plus ordinary
call-frame arguments, change at the declared bridge. No expected after-state or
unknown source stack is supplied as a native producer.

40c0e0 checks the first layer pointer. For a loaded arena,40c114 reads the live
wrapper,40c116 its surface and40c11e invokes surface Release.40c120 re-reads the
wrapper pointer, then40c123 invokes free. After all layers,40c13d clears only
the first pointer. The remaining pointers retain their freed wrapper addresses.
The shadow and builtin special-arena resources are retained.

All302 ordinary layers, including CastleRoof's30, execute this order. The
declared Release results0,1,17 and0xffffffff do not change subsequent free or
clear behavior. The source harness's `released` flag records the supplied
platform response/Release request; it does not prove destruction of a real COM
object, especially when the returned reference count is nonzero.

The third confirmation encounters zero first pointers and therefore does not
free the stale tails again. It creates fresh wrappers in original layer order,
including repeated file paths. Across the corpus there are604 actual bitmap
constructors and302 Release/free pairs. Declared allocator tokens never reuse
an address within a sequence. Dead bytes remain readable in the research
snapshot solely to verify preservation; this is not a Windows heap assertion.

Every confirmation also executes whole43d2c0. All51 recording allocations total
331,075,272 bytes. The prior recording is freed before the next calloc, while
playback ownership survives. Distinct source tokens identify each generation;
Native builds each buffer from its own retained state.

`OriginalBitmapRelease` implements the inline Release-then-free requests through
the existing native platform-request boundary, ignoring the numeric return.
The enclosing `OriginalTournamentPreparation` and value environment retain
transactional ownership and buffer external effects until the whole call
commits. Missing surface/wrapper bindings remain explicit errors.

## Evidence and current acceptance

The independent complete trace audit verifies6817 constructor results,
5,731,334 stores,818,706 instruction reads,3982 API reads and45,507 helper
returns. Its1649 complete points compare1,787,569 records and16,726,960,638 bytes
with masks. All1208 tracked game-owned release operand reads and370,821 recording
reads have established provenance. The controlled COM/vtable scaffolding remains
an explicitly supplied platform dependency.357 preparation FPU points retain
CW023f; this is not a host CPU/FPU measurement.

All2909 observed instruction starts match the original pinned bytes or the
already accepted library patches. Remaining unknown observations are the
retained original cookie storage and small-portrait frame-count backing; their
masks remain unchanged. They are not newly accepted native release operands.

All17 first confirmations reproduce every prior accepted preparation's actual
bytes, masks, events, instructions, reads, writes and helper returns. Only study
labels and additional declared research metadata differ. All51 atomic source
parts match the complete corpus, including1,898,572 before/after/point records
and31,012 part-level blob comparisons. No prior fixture or expected byte changed.

The first native build rejected a test's use of internal `setGlobal`; the test
now writes its three declared fields through public record storage. The second
input also makes constructor/loader history lookup respect chronological return
order because event indices restart at each call. Native game logic and source
results were unchanged. The first parent-preservation audit also caught the new
nested study label; its exact error and verifier are retained, and the corrected
audit excludes only that label while comparing all behavior fields.

Native2 passed six release tests in166.406s after a104.36s build: the new
three tests took93.712s and the unchanged98-case preparation regression took
72.694s. The packaged three tests passed80.220s/build0.32s without a raw override.
Failures after surface Release, wrapper free, replay allocation, completed replay,
outer return and a subsequent bitmap allocation verify full rollback; an omitted
release continuation is still explicitly rejected. Native game code did not
change after its initial implementation.

All693 package files and their archive composition verify.278 prior fixtures
remain unchanged;281 files include the three new transport files.88,720,875
packaged bytes restore the entire1,086,612,920 raw bytes with identical JSON and
SHA. Native game compression remains unchanged; this deflation only transports
research fixtures. All study source, Native and audit jobs are terminal.
[Evidence and pins](../evidence/lib-tournament-arena-release.json) and
`build/research/lib-tournament-arena-release/lib-tournament-arena-release-work.json`
retain the complete attempts and verification history.

Separately, the same live catalog5 process reached20 Objects. Its new atomic
checkpoint reproduces the accepted catalog4 checkpoint except nine trace-directory
names. All32,258 referenced packed trace files/4,079,557,743 bytes match both each
other and their hashes. This reproduces prior source evidence; no new Native
catalog execution or complete catalog return is claimed.

Team Tournament/War, the own initialized catalog/library/application join, full
played match, window/input/audio timing, actual Windows and clean-Mac checks
remain required for the full native-game goal.
