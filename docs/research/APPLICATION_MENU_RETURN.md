# Own menus, dispatcher return and first due iteration

The new finite source corpus continues all43 accepted
[APPLICATION_SCREEN_BODY](APPLICATION_SCREEN_BODY.md) parents and the separate
own settings-1 alternative, plus four negative/positive menu Blt/presentation
HRESULT controls. Forty-seven chains execute the whole early-menu tail, return
from World and the application dispatcher, then finish the first due no-message
iteration at43d110/SP1000effc. One earlier ordinary allocation-failure parent
stops before the NULL-cursor read; it is not a successful complete iteration.

The [plan](APPLICATION_MENU_RETURN_PLAN.md) describes the finite comparison.
Original NTSD EXE/lib.dll/VC80/control.txt and36 full PE DIBs are pinned as in the
parent. Unicorn2.1.4 executes the original code on the existing own CPU/stack,
with declared COM/GDI/time/thread boundaries. The actual relocated DLL installer
precedes the independently entered WinMain:104 installer PCs,76 relocations,
13 copies/62 bytes and both zeroed allocations reproduce LIB_LOADING. Earlier
PE/CRT/NLS startup and actual Windows loader/device behavior remain open.
No registers, stack, game globals, resource backing or CRT state are reset between
parent and child. Ordinary source SEH/cookie instructions execute without
protective/control corruption, bypass or continuation after a memory fault.

## Own path and return-value producer

Actual4275cb consumes the parent's produced EAX selector. Selector0 reaches
427915/main menu, draws MENU at263/202, clears4511e0, and executes whole423b00
with own458420=0. The settings-1 parent reaches selector-3 and draws its three
option-panel images before joining42873e. No mouse/held state or worker result
is injected. Existing controlled menu/network/alternate matrices keep their
separate contracts; no network response is newly executed in these own cases.

The tail draws the cursor at0/2 with signed frame-1 and calls whole4028a0 and
43e940. Own volume/notice flags are inactive, so the overlay makes no API request.
The mode3 presenter requests Blt(primary, rectangle453ccc, back, flags1000000).
Native uses independently retained CreateSurface outputs and bitmap metadata;
source surface words are not imported from expected wrappers. The cursor's
undefined+0c count remains a declared A5 backing read, twice per completed draw.
Positive and negative drawing/presentation results do not abort this own caller.

World executes428805/ret4 to43ecbf/SP1000eea0. Its EAX equals the declared
presentation HRESULT:0 ordinarily,ffffffff/1 in the two presentation controls.
The enclosing dispatcher uses its own retained EBP1, established at43ea6b or
43ea8d before World, and EDI2. After World it rereads4593a0; live mode2 would
require414b70. Own mode0 skips that child,43ecd8 moves EBP into EAX, and43ed01/ret
returns1 to43d187/SP1000eff8. Thus even a negative World presentation result
produces dispatcher1; the timer does not request surface recovery.

The final declared timeGetTime response is123456901. The native timer keeps its
own updated baseline123456822, requests no Sleep, and advances counter1 to2.
The next43d110 is an unexecuted stop. The source field called `baseline` is raw
ESI: only after the dispatcher restores the loop registers does it again mean
the timer baseline. Earlier World/internal ESI values are not timer state.

## State, ABI and ownership

All43 full body records,40 front records,19 settings records and seven bitmap
parents reproduce the immutable accepted corpus. The independent auditor checks
48 actual own dispatcher-producer paths,94 actual World/dispatcher ABI returns,
285 full state checkpoints,5358 original stores/21432 bytes,94 normal SEH-chain
stores and all1199 retained bitmap records/9611184 bytes plus masks. Library
and CRT/PTD/calendar bytes remain unchanged throughout this new stage.

Each complete World epilogue reads five retained root-local words:cookie at+0,
saved EDI/ESI/EBP/EBX at+4/+8/+c/+10. The cookie CALL then writes its own return
marker4287ff at+10, which4450ba reads. Across47 returns these are235 retained
private reads and47 produced call-marker reads. Their source bytes/masks are
preserved and reconstructed, while native uses its semantic state/result and
never imports cookie, SEH or saved-register backing. The prior body-local record
becomes dead after World; native does not rewrite it to imitate that private ABI.

The NULL-cursor case retains its completed MENU draw/panel and stops at43f04b
before reading the cursor count. That instruction already executed for MENU in
the same case, so it is correctly present in the case's PC inventory. The final
NULL invocation is unexecuted at that read. Native explicitly rejects the missing
resource and rolls back the entire pending iteration; no complete return is
attributed to this source stop.

The new stage records1159 ordered events:98 draw entries (one NULL),585 reads
(96 undefined),97 clips/97 Blts,141 scalar stores,47 optional-panel calls,
47 presentation methods and47 final clock requests. There are476 helper returns
and399 actual EXE PCs; no DLL/CRT body executes in this new stage. Parent DLL
text execution remains part of each independently rebuilt chain. Coverage is
listed in the evidence report; it is not all branch outcomes or repeated input.

Native composes existing alternate/main/bitmap/presentation routines with
`OriginalApplicationDispatchEntry.finishWorldCall` and the existing message-loop
transaction. Its own outer/World records are reconciled before committing the
iteration; no expected after-state supplies overlapping storage. Optional scalar
store observers expose main/tail order for this join; they are not advertised as
complete observers of all nested network/RNG/release writes. Late failures at
first/second Blt, panel, presentation, final mouse-word store, dispatcher return,
final time, counter store and pre-commit preserve committed startup/callback,
MSG/timer/counter, own graphics/resources, RNG, library DC and produced local state.
External effects must stay buffered until the entire iteration commits.

## Verification and remaining scope

Acceptance results, exact jobs, isolated export and review are retained in
`build/research/application-menu-return-work.json`; the published evidence below
records final immutable raw/packed identities. The initial source probe reached
the timer tail but its instruction allowlist ended before43d1f4; its failure
snapshot/log remain preserved. The range was corrected after the process stopped,
without source state/expected changes. The initial native compile needed escaping
observer declarations. The first executable17-test trial reported two World-EAX
assertion failures (60.352s/build206.21s); all other comparisons passed. Final
raw17 tests passed59.937s/build61.34s, including all nine late rollback cases. Review corrected a default-only World-EAX assumption for
the two HRESULT controls and an overly strict PC-inventory assertion for the
NULL cursor. These corrections did not alter original expected data or game rules.


Raw67627503bytes SHA256:
`7723fdb6706e94a647e489e57e3729d1c81529ad474a02d936cb5038846e677d`.
Lossless fixture14123660bytes SHA256:
`0fa998dbc893663030a57d146d9ac3959c7c02e283340efe30c6fd34d57becc7`.
Full raw/packed bytes/JSON/SHA,1455 blobs,48 atomic parts,36 assets and10 vendor
hashes verify. All243 prior fixtures remain unchanged;244 current. The618-file
isolated package exports616 committed base files plus the new test and fixture,
with only owned overlays and none of the six foreign changes. Never restart the
completed candidate1 source capture. NTSDNative linked; this is no window proof.

Final packaged17 release tests passed59.457s/build65.75s without raw
overrides, including the own body/resources and retained main/alternate/tail/loop
contracts. All source/SwiftPM jobs are terminal.

Tools: [source](../../tools/oracle_application_menu_return.py),
[verifier](../../tools/verify_application_menu_return.py),
[acceptance](../../tools/accept_application_menu_return.py),
[report](../evidence/application-menu-return.json).

This is the first due iteration after a separately compared queued resize callback,
not WinMain exit, full match or app integration. Next retain the committed own
state through subsequent message/input delivery and menu-to-loading/selection
transitions, while preserving installed-library routing and recovering actually
reached missing children. Worker execution/delivery/reentrancy, other dispatcher
modes and live mode2 post-World service remain open. Earlier full CRT/NLS, real
Windows execution/files/devices, window/raster/audio/input latency, the Naruto/
Sasuke District match, all content/modes/network/replays and clean-Mac acceptance
remain part of the full active goal. CUA inventory returned no apps/browsers and
`Native apps: Error: Sky Computer Use native pipe startup failed`; no window or
device was exercised. Do not retry the separately recorded blocked Microsoft URL.

## Subsequent own input and loading entry

[APPLICATION_MENU_INPUT](APPLICATION_MENU_INPUT.md) preserves all47 completed
own parents, delivers actual mouse callbacks and continues repeated menu calls.
Fifty chains compare62 completed iterations plus three actual41bc90 loading
entries after single-player selection/3000rand/confirmation sound and background
release/World2. Source CPU/stack/resources and native own state are retained;
no mouse/World/expected-state injection. Whole loading remains the next dependency.
