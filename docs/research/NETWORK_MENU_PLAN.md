# Complete network menu composition

Status: controlled UI acceptance is documented in [NETWORK_MENU](NETWORK_MENU.md).
The application joins remain open. This study follows [NETWORK_EXIT](NETWORK_EXIT.md) and
[NETWORK_CLIENT](NETWORK_CLIENT.md). Previous goal turn made progress in2875131.
The full native-game goal is unchanged. This plan covers remaining menu1/2/3
behavior and its enclosing continuation, not just an already passing handshake.

Recover427ca7..42873e/4287de: choice/cancel, background load/release, original
bitmap/text/fill/sound output, address formatting, link request, server waiting
timer/dots, World transition, hostname input and deferred connect. Use the pinned
EXE/lib.dll/VC80 and declared controlled platform responses. No external server
or actual OS network operation is authorized by these source fixtures.

Character helper422f60 is accepted in [MENU_CHARACTER](MENU_CHARACTER.md):10492
whole calls, exact Shift/API order,217old→218current fixture pins. Its output
and GetKeyState request order
feed the300-key hostname scan. Compare all0..299 virtual-key inputs, signed/
unsigned range controls, Shift exact-byte checks and Caps Lock low16 outcomes,
including different responses to successive requests. Native must preserve
actual AL behavior and not substitute host text input or modern key mapping.
Document unused high EAX separately from the caller's signed AL consumer.

Then compare complete menu bodies and their actual helpers, retaining mutable
World/globals/local masks and resources. Null background must execute its real
loader; cancel must perform real sound/background release/network exit order.
Do not suppress output, sound or an unknown helper to obtain a return. Cover
mouse boundaries and click history, unsigned elapsed-time/signed phase remainder,
all key/Enter/Backspace interactions, hostname0..50, partial/error receives and
the existing whole client/exit contracts. Bound unbounded strings before source
control storage; ordinary reachable inputs and unknown provenance stay explicit.

An own continuation must start from the recorder's actual previous PC and retain
its registers/stack/resources, not a fabricated EBP19 or an imported after-state.
The original lib.dll is installed before any claimed library-enabled execution;
any earlier pristine-resource parent is a declared controlled boundary and must
not be relabeled a fully initialized library application. Compare presentation
and epilogue using their existing helpers before claiming whole4246b0 return.

Native provider/storage/late-output errors must roll back the whole composed
operation. Preserve raw source bytes, masks, actual fault/error boundaries and
all217 prior fixtures; source atomic checkpoints, independent provenance/order/
PC/bytes checks and raw native acceptance precede fixture packing. Verify full
transport JSON/SHA/blobs, prior/vendor pins and isolated owned native exports;
run packaged retained tests afterward. Real Windows/TCP/reentrancy, CRT/NLS,
lib transforms/routing, native app/device/input latency, full match/content and
clean-Mac acceptance remain required independently of these research controls.

The retained217 baseline above preceded the character publication. Subsequent
menu work must preserve all218 current fixtures. Existing World prefix0x7d8
does not contain hostname: recover the actual menu1→3 NUL/index producer and
own subsequent bytes; do not import allocator/page zeros as initialized text.
The legitimate append limit50 requires51 storage bytes including NUL, not an
inferred complete World allocation size. Unwritten bytes remain unknown.

Pristine FrontMenuLoop currently stops at427ca7; its resource/parent evidence
must stay pristine. A library-enabled chain must install the DLL before its
claimed execution and route actual401290 to accepted OriginalLibSurfaceText.
Installing after a pristine parent is a declared join, not whole app startup.
The Enter branch sets pending, renders Connecting and jumps42873e; actual
428420 connection occurs on a later tick. Preserve this deferred timing.

## Probe history, superseded by final acceptance

The new source installs the actual relocated DLL before the World constructor,
then executes the complete fresh resource/settings/body/main-menu first return.
Its complete parent equals the pinned pristine first-return parent apart from
the declared SetBkColor→SetBkMode1 events and added DC observations. No source
bytes are rewritten. Native library body/presentation routes carry their own DC;
the first-return probe and four retained body/completion tests pass.

The next probe executes eight own caller starts: an idle tick, original network
initialization, choice/empty-hostname producer, client idle, typed A, Enter and
the deferred connection. Six UI bodies/nine network requests and their real
tails compare natively; both retained early-menu-loop tests also pass. This is
preliminary evidence, not a published corpus or completion of this plan.
Source then extends through host waiting/dots/back/cancel and the following
background reload:15 caller starts/12 UI bodies/11 network requests pass, with
four native tests2.251s/build178.42s. This remains a probe. Jobs and immutable
probe paths/hashes are recorded in `build/research/network-menu-work.json`.

The outer World/globals/hostname/resource records and ordered events must match.
Caller text/client writes are compared to newly defined native bytes; preserve
the source's full before/after stack bytes and its actual write mask separately.
Native retains its own prior body bytes where known and does not import the
remaining private caller stack. Any read requiring that unknown backing must
reject rather than count as a successful own match. Thus this composition does
not claim full private stack/ABI equivalence. Standalone controlled client and
exit corpora retain their broader, explicitly supplied local-storage contracts.

The first keyboard matrix retained392 atomic parts through key121, then key122
entered the volume tail4028a0/402810 and reached PC0. A separate key122 control
reproduces that boundary: this early-menu parent has no initialized five sound
slots. PC0 was the emulation end address, not a Windows crash or an executed
null instruction. The next control explicitly binds45560c..45561c to five
already mapped COM test objects before whole-caller inputs; the volume return
then succeeds. These declared platform bindings do not close own audio startup.
Preserve both failed observer logs and their checkpoints. No native/source game
rule or previous expected byte was changed to conceal the missing dependency.

## Finite acceptance criteria

- Both allocator/backing controls must independently reconstruct their whole
  parents and the menu1→3 hostname producer, preserving all218 fixture pins.
- Each of the forum, host, client, cancel, server-back, client-connect and
  client-back rectangles: each edge and its immediate outside neighbor, plus
  interior, with held/previous pairs(0,0),(1,0),(2,0),(1,1).
- Server timer: first-use bit2 with other bits retained; elapsed0/149/150/151,
  UInt32 wrap; phases-2147483648/-1/0/1/3/13/14/2147483647. Positive dots after
  Back must still draw. Connected byte0/1/2 tests the exact World transition.
- Each key0..299 in the complete scan, with both Shift paths, plus simultaneous
  keys in ascending order, Backspace/Enter interactions, hostname lengths0/1/
  49/50, consumed117 versus retained100 flags and pending0/1/2. The accepted
  character corpus remains the wider Caps Lock/high-word dependency check.
- Original background load/release and exit must execute. Exercise declared
  resource/device failure inputs only within recovered ownership, without
  corrupting control fields or manufacturing an exploit/crash domain.
- Deferred client socket/lookup/connect/greeting/receive boundaries must retain
  UI timing and complete output/epilogue. An unknown partial-buffer read stays
  an explicit unsupported own boundary; retained controlled client fixtures
  remain unchanged and are tested separately.
- Late native failures after text, hostname mutation, resource release and
  network observers must roll back all enclosing records, DC and ownership.
  Compare source PCs/helpers/ordered requests and independently verify every
  raw/packed byte sequence, JSON, SHA, blob and original/vendor/export pin.

No final fixture, branch-coverage claim or accepted milestone follows from a
probe. Full startup/CRT/NLS, listener/peer composition, native runtime wiring,
window input/animation/audio/latency, Windows and clean-Mac checks remain open.

## Accepted boundary

[NETWORK_MENU](NETWORK_MENU.md) completes this finite controlled matrix: two
944-start/938-UI corpora, three separate own partial-read rejections and two
20-return/16-UI enabled-sound corpora. Actual output/epilogues and both World
transitions compare. Seven immutable fixtures preserve218 prior pins and the
intermediate223 pins, giving225 current. The early VM retains CW0; it has not
executed CRT FPU startup. Own sound/listener initialization, partial private
stack provenance and production runtime routing remain explicit dependencies.
Further work must preserve all225 fixtures and must not relabel API bindings
as an initialized Windows/device/application execution.
