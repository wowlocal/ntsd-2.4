# Complete network menu composition

Status: active research following [NETWORK_EXIT](NETWORK_EXIT.md) and
[NETWORK_CLIENT](NETWORK_CLIENT.md). Previous goal turn made progress in8cb4275.
The full native-game goal is unchanged. This plan covers remaining menu1/2/3
behavior and its enclosing continuation, not just an already passing handshake.

Recover427ca7..42873e/4287de: choice/cancel, background load/release, original
bitmap/text/fill/sound output, address formatting, link request, server waiting
timer/dots, World transition, hostname input and deferred connect. Use the pinned
EXE/lib.dll/VC80 and declared controlled platform responses. No external server
or actual OS network operation is authorized by these source fixtures.

Character helper422f60 is accepted in [MENU_CHARACTER](MENU_CHARACTER.md):10492
whole calls, exact Shift/API order,217old→218current fixture pins. Its output
and GetKeyState request order
feed the300-key hostname scan. Compare all0..299 virtual-key inputs, signed/
unsigned range controls, Shift exact-byte checks and Caps Lock low16 outcomes,
including different responses to successive requests. Native must preserve
actual AL behavior and not substitute host text input or modern key mapping.
Document unused high EAX separately from the caller's signed AL consumer.

Then compare complete menu bodies and their actual helpers, retaining mutable
World/globals/local masks and resources. Null background must execute its real
loader; cancel must perform real sound/background release/network exit order.
Do not suppress output, sound or an unknown helper to obtain a return. Cover
mouse boundaries and click history, unsigned elapsed-time/signed phase remainder,
all key/Enter/Backspace interactions, hostname0..50, partial/error receives and
the existing whole client/exit contracts. Bound unbounded strings before source
control storage; ordinary reachable inputs and unknown provenance stay explicit.

An own continuation must start from the recorder's actual previous PC and retain
its registers/stack/resources, not a fabricated EBP19 or an imported after-state.
The original lib.dll is installed before any claimed library-enabled execution;
any earlier pristine-resource parent is a declared controlled boundary and must
not be relabeled a fully initialized library application. Compare presentation
and epilogue using their existing helpers before claiming whole4246b0 return.

Native provider/storage/late-output errors must roll back the whole composed
operation. Preserve raw source bytes, masks, actual fault/error boundaries and
all217 prior fixtures; source atomic checkpoints, independent provenance/order/
PC/bytes checks and raw native acceptance precede fixture packing. Verify full
transport JSON/SHA/blobs, prior/vendor pins and isolated owned native exports;
run packaged retained tests afterward. Real Windows/TCP/reentrancy, CRT/NLS,
lib transforms/routing, native app/device/input latency, full match/content and
clean-Mac acceptance remain required independently of these research controls.

The retained217 baseline above preceded the character publication. Subsequent
menu work must preserve all218 current fixtures. Existing World prefix0x7d8
does not contain hostname: recover the actual menu1→3 NUL/index producer and
own subsequent bytes; do not import allocator/page zeros as initialized text.
The legitimate append limit50 requires51 storage bytes including NUL, not an
inferred complete World allocation size. Unwritten bytes remain unknown.

Pristine FrontMenuLoop currently stops at427ca7; its resource/parent evidence
must stay pristine. A library-enabled chain must install the DLL before its
claimed execution and route actual401290 to accepted OriginalLibSurfaceText.
Installing after a pristine parent is a declared join, not whole app startup.
The Enter branch sets pending, renders Connecting and jumps42873e; actual
428420 connection occurs on a later tick. Preserve this deferred timing.
