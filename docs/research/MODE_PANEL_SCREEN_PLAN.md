# Whole installed-library mode screen with enabled panel

The preceding goal turn made progress: accepteda2a66cc implements and compares
the complete panel body, preserving251 fixtures. This study joins that actual
423b00 child to whole431d10 with installed library text, input, background
release, help and cursor tail. It must use live resource ownership after earlier
selection releases, preserve panel/outer mouse ordering and commit one native
transaction. The own guarded catalog11636 and watcher15664 remain live; no
source restart or change to their frozen inputs is authorized by this study.

Reference is the pinned NTSD EXE and bundled lib.dll under Unicorn2.1.4, using
the accepted controlled loader/installer and whole mode-screen/panel observers.
Instruction/store/read/stack/register traces establish caller/helper order,
local string provenance, ownership changes and complete ret16 cleanup. This
is compatibility research only; EXE/DLL/emulation are absent from native runtime.
COM/GDI/timer/Sleep/Shell/free/quit are declared adapters, not host operations or
Windows/device observations. Preserve ordinary cookie checks. Do not introduce
arbitrary damaged control pointers, protection corruption or fault continuation.

Keep five declared500-frame mode-screen bitmap inputs, nine controlled Actors,
World and two replay allocations from LIB_MODE_SCREEN. Add a separate owned
794x550/count11 panel atlas with13 defined rectangle slots and A5/ramp unknown
unused bytes, at two distinct addresses for the two controls. Auxiliary guide
and badge records retain their declared mode-screen rectangles. This establishes
controlled caller composition, not original raster pixels or own loaded resources.
OriginalMenuPanelDrawing receives the enclosing screen's current staged bitmap
ownership. Neither private source stack nor expected after-state is native input.

Declare16 cases per backing control before capture: nominal,28 help labels,
main link click, banner18 below its bottom edge, notice hover, notice blink,
left/right navigation, timer reset, mode confirmation/background release,
that release followed by notice click, an earlier outer link consuming held
before a panel banner, negative GetDC followed by panel notice, playback before
the panel, zero timer range and no eligible navigation row. The last two stop
before zero DIV/repeated ninth scan as in MENU_PANEL_DRAW; they are explicit
native rollback rejections, not successful returns. Playback is a partial
continuation before43249c. Preserve every completed case atomically.

First capture one nominal probe, then the finite32-case suite. Verify every
read/store/final mask, actual instruction bytes and helper return, input atlas
construction, source parts and raw/packed bytes. Compare complete native globals,
allocation storage/liveness, local defined bytes, semantic retained DC and all
ordered effects. Test enclosing rollback after background release at the panel
timer and Shell, at a help label after the panel, and at the final cursor Blt.
The caller buffers external effects until the enclosing screen succeeds.

Use accepteda2a66cc plus only owned mode-screen source/new tests in isolation.
Retain the pristine mode screen/selection, installed-library screen/text and
whole panel tests. Keep251 fixtures,14 pending native catalog files,3 source
files,6 foreign files and all frozen watcher bundles unchanged. Source errors
retain exact logs/time/state; no automatic fault retry or expected-byte edits.
Whole own initialization/catalog/menu, the outer timer loop, app input/animation/
audio, Windows/full-match/all-content/network/clean-Mac goal remains open.
