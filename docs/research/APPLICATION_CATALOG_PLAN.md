# Current guarded-capture status

Both old additional captures are now terminal1: parent1 at2026-09-11
04:11:26.405842UTC (24244.519s), parent2 at04:11:01.192218UTC (24199.795s).
[Preserved failures](../evidence/application-catalog-control-wave-guard-errors.json)
retain180253388/180320549bytes and exact source errors. Both reached410a4d
with24completed Objects/154registered WAVs and the same pending155th
SNDDATA_2144.wav allocation/API inputs as primary1. Its first-region guard
assertion fails in the research return observer. The old allocation-layout
explanation remains distinct from terminal guard bytes, which were not separately
captured. No source/Windows game crash or successful native24/full-catalog match
is claimed. Sessions45694/5385 are terminal1 and all four source PIDs absent.
No automatic retry or replacement control capture was launched. Corrected
primary4 and its890-pin guarded20 watcher remain live unchanged.

Both additional twenty-Object audit sets are now fully terminal0:
[final/intermediate/read/native completion](../evidence/application-catalog-control20-completion.json).
Each read audit checks556244408 stack reads/1920124614 bytes and73 catalog reads/
187 bytes against all541554289 writes, with zero undefined observed bytes.
Unobserved/private storage remains unknown. Their old full source captures are
terminal as above; corrected primary remains live. No full catalog return or publication is accepted.

The bounded [WAV guard study](CATALOG_WAVE_GUARDS.md) now passes155 paired
original helper calls and native comparisons, preserving all154 prior own PCM
records and all actual guards. The fresh guarded firstObject has complete source
equality and a passing native test. Its full capture remains live; the guarded20
watcher15664/session49998 will run all three source audits and the frozen native
test with890 immutable inputs. Both additional20 intermediate, full final
trace/blob and read audits pass and are terminal. Full own catalog
is still incomplete. Subsequent paragraphs retain chronological status notes.

Primarycandidate1 is terminal at a research allocation-guard assertion after24
Objects/154WAVs. The complete failure and prior20 checkpoint are preserved.
SNDDATA_2144.wav's20440-byte temporary exposes a missing32 bytes in the declared
mapping size: its trailing guard overlaps24bytes of the next guard. This follows
from recorded allocation inputs and pinned host initialization code; terminal
guard contents were not separately captured. It is not a source/Windows game
crash or a successful native catalog match. A separate guarded primary4 capture
is live, retaining every guard assertion and all old source/expected files.
See build/research/application-catalog-guarded-plan.md and work JSON.

Both additional own parents now have20 checkpoints and pass the frozen native
child-state comparison (13.369/13.519s). Their final/intermediate/read audits
remain running. The old primary40 watcher is terminal143 with no children;
its immutable inputs remain. The original two captures are still live unchanged.
Full own catalog return and publication remain open. The accepted independent
INITIAL_MATCH_ENTRY milestone984995b does not close this dependency.

# Own catalog loading on the application CPU

**Work in progress.** The primary own capture now retains an atomic20-Object
checkpoint. A separate native observer compares actual completed child state at
every one of those returns:53,566 full Object/Frame/bitmap record comparisons,
59,581,783 bytes plus masks. The last snapshot contains20 Objects,4995 Frame
allocations and194 wrappers (5,643,803 bytes plus masks), with94 whole registered
WAVs and the complete observed semantic request/global prefix checked separately.
All three retained first-Object parents also match through the same observer.
Source expected memory is never supplied to native loaders. The parent registry,
full own catalog return, later weapon paths and postcatalog continuation remain open.

The13-test separate build completed237.65s;12 tests passed, including the new
four-input storage test29.823s and retained full controlled catalogs. One old
missing-file test failed because the launcher omitted its source-path environment;
the unchanged binary passed that test separately4.804s after supplying the path.
The failed invocation and exact error remain retained. No source/game rule was
changed for this correction. All633 archived native input files are hash verified.

Independent source replay matches all2142 retained state checkpoints,5,323,542
record comparisons/13,016,786,426 bytes plus masks and541,554,289 writes. Full
trace/blob verification also passed17406blobs/32258parts and5318final regions.
Read provenance passed556244408 stack reads/1920124614bytes and73catalog reads/
187bytes, all with zero undefined read bytes. This does not make unread/private
storage known or establish Windows execution. All20-Object audit jobs are terminal.
After the one-shot watcher and its children were terminal0, the exact tested
native observer/probe were copied into the worktree; old23pins stay archived.
All three full original captures continue;
no full own catalog ret8 is accepted. Current PIDs, sessions, hashes and results
are in `build/research/application-catalog-work.json`; the child-state comparison
scope is in `build/research/application-catalog-child-storage-plan.md`.
Chronological notes below preserve earlier states and corrections. The pending
catalog study changes no accepted fixture; independent UI commit72cd808 added
its own accepted controlled fixture while preserving the246 earlier pins.

Recover the actual4450ac allocation response and whole4122f0 catalog caller
after APPLICATION_LOADING_PREFIX, retaining the own World, stack, library,
CRT/PTD/RNG, graphics/audio devices, input and all live/dead resources. The
desired result is the original full catalog built on this application chain,
then its transfer to native loading. A missing child remains an explicit open
dependency at its actual entry, never a fake successful return.

Pinned original NTSD EXE/lib.dll/VC80, data/data.txt, DAT/BMP/WAV resources;
Unicorn2.1.4 is development tooling. Real normal SEH/cookie, allocation caller,
catalog, bitmap loader/copy and CRT reader instructions establish the producer
and lifetime of catalog fields and child requests. Declared allocator/MMIO/COM/
GDI/file translation/thread/time/message responses remain controlled boundaries.
No private stack or expected state imports, parent CPU resets, control/protective
corruption, bypass, fault continuation or unrelated network/URL/device actions.
Distinguish source emulation, native comparison, actual Windows and window checks.

Initial finite source set: the three unchanged successful own prefix parents;
then ordinary catalog-file absence and negative file-close responses from the
default parent once those actual requests are reached. Preserve all accepted
parents/bytes/masks. First probe records every newly reached missing child and
its own arguments/state before extending the observer; do not restart a live
capture or retry a safety-refused operation. Keep intermediate artifacts atomic
and explicitly incomplete until the full declared comparison is accepted.

Catalog backing is a new81273768-byte allocation at70000020, outside the retained
common-WAV regions60000000/64000000/68000000. New bitmap wrappers use50000020
and subsequent disjoint allocator records. Never attach the historical fixed
60000020 catalog or its separate/reset CRT scanner over the own resources.
The existing whole bitmap loader and C-locale CRT file reader provide the first
dependencies. New FILE/read-buffer records are declared fopen outputs, with
actual CRT buffer/count/EOF updates and supplied translated _read bytes.

The DAT child4148a0 executes actual same-CPU fscanf/feof/fprintf and output
fclose. Writable fopen supplies a disjoint user-buffered FILE (flags102,
65536-byte buffer), as in the accepted menu-info output boundary. _write captures
every requested logical byte; declared text translation expands LF to CRLF.
The next read of that temporary path consumes those produced bytes, with CTRL-Z
termination and CRLF contraction. Read-file close remains a declared boundary.
No separate scanner, decrypted fixture, host temporary file, or reset PTD/errno
supplies a decoder result. A finite scan-limit probe checks adapter composition
and trace volume; its stop after a real CRT return is explicitly incomplete.
Ordered low-level arrays are stored without sampling in immutable zlib-framed
JSON-array parts. Each part records its first ordinal, count, full raw/packed
length and SHA256. Atomic indexes retain completed parts while the same source
process continues in one-million-instruction chunks; neither chunking nor
transport compression changes CPU state. Probe3/probe4 independently reproduce
all previous fields; exact array counts and verification are in the work artifacts.

Advancing loading reuses the accepted LIB_LOADING bodies, including the actual
installed label jump, bitmap/clip/library text, overlay and presentation. Its
nominal message boundary reports an empty queue after the prior delivered menu
input. No host message or URL is issued. Source fill inputs retain the actual
own helper-entry backing/masks; native provenance must be checked before use.
Frame malloc requests from the actual Object parser use a disjoint42000020
bump arena with declared unknown A5 backing. Registered4014e0 calls reuse the
accepted whole WAV adapter on the same stack/device; each new temporary/lock
buffer is separately mapped from80000000. The first SetVolume interface slot
is an explicit added COM response binding, preserving all earlier slots. No
host sound plays. An Object-limit probe may stop only after its actual ret16,
without claiming the remaining catalog. Writable FILE snapshots are retained
inline in the ordered trace to avoid duplicating a separate blob per character;
all32 bytes remain present, and earlier probe formats remain immutable.

Probe1..6 exposed an adapter mistake: the new bitmap adapter compared requested
`shadow1/back99_1/back99_2/back99_3` case-sensitively with uppercase PE names.
All four original constructors therefore returned missing resources and issued
their actual message/debug requests. Keep those bytes as missing-resource
controls; they are not nominal whole-catalog evidence. Probe6 was already live
when identified and remains a separately named incomplete missing-resource
control, not a nominal replacement or retry for silence. Its full first DAT
child in probe5 remains a valid direct decoder comparison in that declared
parent state. No accepted fixture was altered.

The corrected catalog adapter folds only ASCII a-z in the lookup name and
retains the verbatim original argument plus exact uppercase PE resource name.
This agrees with the older accepted loaded-catalog adapter and Microsoft's
[resource implementation notes](https://source.dot.net/Microsoft.NET.HostModel/src/runtime/src/coreclr/tools/Common/Compiler/Win32Resources/ResourceData.cs.html)
about Win32 uppercase conversion. The four pinned keys need no locale-dependent
non-ASCII mapping; actual Windows resource/NLS execution remains open. A short
independent corrected-resource probe stops after the real third registry scan,
before the Object/decoder, while the existing missing-resource control finishes.
Newly created surface responses also expose their Blt slot, needed by the
accepted missing-mirror fallback. The source executes that caller and records
both live surfaces, rectangles and all100 DDBLTFX bytes with own write provenance.
This declared COM call returns0 without drawing on a host device; native ownership
and the meaningful/retained fields still require their own comparison.

The nominal clock begins123457000 and advances20ms at each timer request, so
later loading updates must execute animation instead of selecting a permanently
frozen no-draw path. Objects use48000020+ordinal40000, disjoint from50000020 bitmap
wrappers and54000000 FILE/buffer storage. These are allocator/API response
identities, not imported game pointers or actual Windows allocation observations.

Compare the full catalog/child bytes and masks, all allocated records and
ownership, globals, source stack producer reads, ABI returns, CRT/library state,
loading-progress clocks/events, original assets and exact parent dictionaries.
Native must build from its own loading prefix and original files, with a late
failure rolling back the complete pending iteration. Preserve the old controlled
catalog/initial-loading contracts and246 fixture pins. Isolated native export
starts at4545435; six foreign files are excluded. Full application, catalog return
until proved, workers, CRT/NLS startup, Windows/devices, complete match/all content/
network/replays and clean-Mac acceptance remain open under the original goal.

## Incomplete evidence retained so far

### Independent postcatalog UI preparation

While the three full catalog CPUs continue, a separate native change prepares
the later41c2f5..41c581 interface caller. Its optional constructor callback
receives the actual caller index, allocation/backing, device and resource name;
the default image-result provider route is retained. The callback result must
bind the requested required resource and canonical wrapper storage. Null malloc
still skips construction, all ten global stores precede44d05c=0, and previous
global pointers are not released here. Caller graphics/IO effects must be staged
outside this loader with the whole pending iteration.

Finite acceptance uses the unchanged13-case [initial-interface corpus](INITIAL_INTERFACE.md):
both routes must retain every global checkpoint, wrapper byte/mask, pool record
and ordered request. That corpus's43ed10 remains a declared device-result
boundary. Native-only controls additionally exercise whole surface construction
with failed CreateSurface after the original DIB dimensions were produced,
a late tenth-constructor error after nine stores, and wrong resource/optional
bindings. These controls are separate from successful original comparisons.
Retain the existing whole image/copy and bootstrap tests. No new EXE process or
own postcatalog continuation is run by this preparation.

The isolated export and jobs are recorded in
`build/research/application-postcatalog-work.json`; input archives are frozen
before compilation. This export is separate from the live catalog watcher and
its23 pinned files. The separate `ui-constructor1` release job passed10 tests in
3.547s/build222.90s (PID86901/session27453 terminal0). Both interface routes
compare the same13 original cases,114 constructors,5470 records/13029720bytes
plus masks and519 events; they are not26 independent source cases. Retained
bitmap surface and bootstrap tests also passed. Four native-only scenarios
cover CreateSurface failure, the late constructor error and two binding errors.
All624 frozen export files/1234963865bytes were independently hash-verified,
along with246 unchanged fixtures, six foreign files and23 watcher pins.
NTSDNative linked; no app window was exercised by this job.
The actual own catalog return, postcatalog allocation/mapping provenance,
pool/UI join and full41bc90 remain open;41c581 is still an internal boundary.

Probe5 completes the first4148a0 from the own application CPU, with the four
missing embedded resources described above. `chars\\pein.dat` has72226 raw bytes,
72225 input text bytes; actual CRT writes72102 logical bytes in requests65536
and6566, producing74271 temporary text bytes. Its actual ret reaches40f111;
Object/catalog have not returned. All20963672 writes,21581337 stack reads,
144333 scan/EOF returns and72103 fprintf/fclose returns survive in1257 immutable
parts (4936515235 raw /153112518 packed bytes). Independent reconstruction
matches81926848 bytes and masks across36 declared regions; all69322 blobs verify.
Raw46417916bytes SHA256`a8d7444bd83e782852ca11dff695757faf15f18bdde7afa4987735d9314b9b47`.
RNG, library bytes/DC, message and counter remain unchanged; the catalog SEH
frame and timer are live, so neither is falsely described as restored.

The existing public Swift DAT decoder directly matches those72102 logical
bytes. This diagnostic calls that child directly, not a native own catalog.
Five isolated release tests pass12.553s/build208.87s, including retained318-call
LIB_LOADING and three loaded-catalog controls. An initial `swift build
--build-tests` lacked testable release modules; normal `swift test` rebuilt and
passed without changing game rules or expected bytes. NTSDNative linked; no
window, device or Windows run. This diagnostic test remains in the research
export and does not add an accepted fixture or production rule.

Corrected-resource probe7 preserves every parent/prefix dictionary and completes
four each constructor/loader/copy/memset, with no missing-resource message.
It stops at41264e after the real third registry scan. Independent reconstruction
matches81635168 bytes/masks across33 regions; all3781 blobs and7 trace parts
verify. Raw32272347bytes SHA256`74483cd464b3cdba37659ec50e7067c7cd5c0685b2ea8f59660f6f1b6fb416db`.
This corrects the research API response, not original executable/asset bytes.

Jobs, producer versions, terminal logs and exact diagnostic scope are retained
in `build/research/application-catalog-work.json`. All246 accepted fixtures and
six foreign files remain unchanged. The nominal complete catalog, native own
composition and full application goal remain in progress.

The four corrected-resource constructor children also match the public native
surface-loading API directly:68 ordered API requests and all wrapper bytes/masks,
with non-null response identities canonicalized only at the established surface
word. Unknown private request fields stay unknown. Six diagnostic/retained release
tests pass12.367s; this is still not a native own catalog join. Probe6 terminated
with an actual unmapped fetch at0004c072 from411b85 (malloc200): the research IAT
still held its unresolved import RVA. Its terminal failure and streamed evidence
remain; no fault continuation or source-game crash claim. The new nominal probe
binds that declared allocator before entry and records the binding explicitly.

Nominal probe8 now completes the first Object through actual41269a/ret16,
with400 Frames,222 Frame allocations,10 bitmaps,39 progress calls and9 WAVs.
Its raw37673163 bytes have SHA256
`5e62f4828793afcf4c8a3de60de67392965d9d9894279349a873cee35b4a9692`.
Independent reconstruction matches82158754 bytes and masks across266 regions;
all4478 blobs and1407 trace parts verify. The23535451 writes,24182215 stack
reads,157293 scan/EOF returns and72105 output returns remain lossless
(5534175855 raw /175786132 packed trace bytes). The catalog is still live.

The existing native parser/storage directly matches that first Object at its
declared bitmap/progress/WAV callbacks. A newly recovered callback is required:
40f129 initializes a countdown to10;40f1ac decrements after each outer-token
checksum and40f1bb calls43d230 before interpreting every tenth token. Inner
field scans do not advance it. Source records22 such full calls returning to
40f1c0, in addition to38 progress-tail message calls. Native OriginalObjectLoader
and OriginalLoadedCatalog now expose that message boundary in the same order.
All22 checksums and positions among allocation/WAV calls match; a late22nd
observer failure rolls back parser resources after earlier allocations/sounds.
Seven diagnostic/retained tests pass12.954s/build201.75s. This callback does not
yet join native application message delivery or prove reentrant loading.

Two further direct controls compare the39 whole progress helpers against native
OriginalLibLoadingProgress (1406 ordered observations/full globals), and all9
registered WAV helpers against OriginalRegisteredSoundLoading (136 WAV requests,
PCM/masks/ownership and subsequent SetVolume). They pass0.143s/build67.30s.
The progress adapter distinguishes source callee-entry markers from native
stage markers while comparing actual requests; raw markers remain unchanged.
Those controls use explicitly declared source helper inputs. They do not import
those inputs into a native own startup or claim full native catalog composition.

The next transport probe preserves every record snapshot through an ordered
addressed-record-table delta chain. Each snapshot references a complete prior
table plus exact replacement/appended records; bytes/masks still use their full
content hashes. No sampling or discarded state is permitted. An indexed lookup
updates masks for disjoint allocated regions instead of scanning every prior
allocation on every store. Probe9 independently repeats the first Object and
must reproduce every old field, expanded ordered snapshot and trace-part byte
of immutable probe8 before the format is used for the long catalog capture.
The already running probe9 uses its frozen producer; later tool extensions do
not alter that process.

The prepared full-catalog extension permits the previously studied whole
40c160..40c903 BG and40c910..40d09d Stage children, including explicit-output
DAT414a30..414b6f. Actual child returns retain saved registers, SP, results and
all catalog bytes/masks. BG free44717c is an explicit research response only
after a known newly allocated wrapper's actual surface Release, with dead bytes
retained. Any other request remains an open boundary. A finite20000-million-
instruction ceiling and6GiB recording reserve stop the research capture at its
current PC without claiming a source game failure or continuing a fault.
Neither this prepared extension nor probe9 has completed the full catalog.

Probe9 is terminal and reproduces every old probe8 field after expanding its39
record-table snapshots, including all4478 blobs and all5534175855 raw trace
bytes. The separate reconstruction again matches82158754 bytes/masks across266
regions; the additional snapshot transport is63919 raw /13151 packed bytes.
Raw37043451bytes SHA256
`b2cdf3e9b3f1136723bae7779a33ded8fffd998dad348daddbf6731453b4893b`.
Equivalence6.943s and reconstruction67.180s both passed. This storage change
does not change source execution, native behavior or the accepted old fixtures.

The first nominal whole-catalog candidate now targets the actual137 Object,
17 BG and Stage continuation on parent0. It retains atomic immutable checkpoints
after the first Object and every20 completed Objects, then continues on the same
CPU without resetting or importing captured state. Checkpoints reference the
same lossless trace parts and retain all preceding helper/state/asset bytes.
They are explicitly continuing captures, not complete catalog returns. The
remaining two nominal parents and ordinary failure cases follow this first
candidate's completed dependency audit. Exact active jobs and terminal results
belong to the work JSON; never infer completion from a quiet log or restart a
live job for silence.

The first whole-candidate Object checkpoint is retained at37043556 bytes,
SHA256`918507a0f8a7544a6c949a8b361f7899a8e41f8a8c7dfa56a38e68a9eff806a3`.
Every previous game state/record/trace byte reproduces probe8. Its only declared
metadata differences are the continuing checkpoint end, the unexecuted BG free
binding and empty new BG/Stage return lists. The source process continues into
the next DAT; this checkpoint is not a completed catalog.

The first Object parser/storage also passes when its initial globals/cache come
from independently built native startup/menu/input/common loading. The accepted
prefix checker now returns its already compared result for that continuation.
Eight diagnostic/retained tests pass17.486s/build68.11s; the own case takes4.904s.
No source entry snapshot supplies this own input. This still retains the old
bitmap/progress/WAV callback boundaries.

Pending constructor integration adds an optional complete bitmap callback to
Object/BG/catalog loading, preserving the old metadata path for retained controls.
The source tests the actual mirror surface word, so the joined parser uses that
word after possible constructor cleanup. A first executable diagnostic exposed
a test-adapter error: it supplied own45841c=0 as graphics device; actual43ee9c
reads own457578=31001000, also used by the accepted front-resource loader.
The failed eight-test log and exact test version are retained (ten request
mismatches, not a successful match). The corrected diagnostic reads457578 and passes all8 tests17.677s/build66.20s,
including10 full constructors/158 ordered API requests. Original expected bytes and game rules were not
changed to repair that adapter binding. Complete native output/global ownership,
missing-mirror Blt provenance and application message delivery remain open.


The next finite native diagnostic composes the first Object with its own prefix,
all39 live loading updates,22 independent43d230 pumps,10 complete bitmap helpers
and9 full registered WAV calls. It must use own wrappers/libraryDC/device/globals,
original asset bytes and only declared platform responses. Compare ordered
observable requests and full final game globals/storage; intermediate checksum/
cache synchronization and private CRT/stack equivalence remain separate until
explicitly recovered. The shared message helper is extracted unchanged from
OriginalLibLoadingProgress. RegisteredSoundLoading forwards the already verified
WaveLoader output-store callback to preserve clear/create/SetVolume ordering.
No source job is restarted and no accepted expected bytes are edited.


A second diagnostic additionally observes the native outer-token checksum and
sound-cache commit after the actual WAV/SetVolume return. The shared Frame and
Object loaders forward these existing mutations without importing expected
state. A loading-progress checkpoint exposes its candidate globals immediately
before each observed event; it does not commit external effects. This permits
full event-global comparisons as well as final globals. These additions are
pending executable verification and are not a new accepted fixture/milestone.


The second join comparison passed the first own Object with all1963 full event
globals, then the retained LIB_LOADING test terminated with Swift exclusivity
signal6: the new observer read the same state borrowed by overlayWithLibrary.
The exact terminal log and frozen inputs remain in the work artifacts. This
failed suite is not acceptance. The repair keeps a separate overlay candidate
and forwards its existing native write callback into the observer view; no
expected byte or game rule changes. A fresh retained+own comparison is required.


The first own output join passes8 release tests17.654s/build204.68s:1963 ordered
semantic observations,39 progress updates,60 message checks,158 bitmap API
requests,9 complete registered WAVs and full final globals. Native uses retained
front wrappers and surface identities, library text/DC, device and common18
WAV results from its own prefix. It does not import source entry state. The
second join also matches all1963 event globals before the separate retained
overlay exclusivity failure described above. The repaired full suite remains
pending; this is not accepted whole-catalog/application evidence.


The repaired overlay observer passes all8 release tests17.611s/build207.72s,
including the own1963 full event/final globals, retained318 LIB_LOADING calls/
20727 events/two rollback trials and three full loaded-catalog controls. It
forwards native writes into a separate observer view; no original bytes changed.
The earlier compile attempt required the existing callback's @escaping annotation
and is retained as terminal failure, separately from this successful run. The
new ninth-cache-commit rollback trial remains a finite follow-up.


The ninth-cache-commit exception follow-up passes both declared and own-prefix
Object trials (2tests5.360s). Candidate checksum/cache/count/bitmaps/Frameheap
return to their pre-call values after earlier sound/allocation callbacks. This
is component rollback; external graphics/events still require staging for the
whole application operation. All native jobs are terminal; the original full
catalog source continues. No accepted fixture or completed catalog claim yet.


A bounded missing-mirror dependency reuses immutable original-objects.json:
its forced missing-mirror weapon4 case retains one actual caller Blt with both
rectangles0,0,490,198 and100 effect bytes. oracle_objects.py explicitly supplies
memset's zero writes and the bitmap/device boundaries; the EXE caller itself
computes the request. This is not execution of the CRT memset or the own loading
CPU. The static two-branch audit independently identifies memset100, size100,
DDFX2, flags01000800 and dimensions from the normal wrapper. Extend the native
Object callback and existing reference comparison to all retained fields, while
preserving failed-wrapper ownership, constructor/progress order and ignored
HRESULT behavior. Compare all three unchanged Object suites; a callback exception
must roll back candidate Object resources. Actual own mirror/device join remains
open until the live catalog reaches it; do not recapture/rewrite old fixtures.


The retained mirror extension passes9 release tests27.423s/build208.76s:
all three Object source suites plus the existing fourth Object test, three full
controlled loaded catalogs and two first-Object diagnostics. Native now compares
the existing Blt rectangles/flags/100 effect bytes and rolls back Object resources
when its mirror observer throws. Original fixtures are unchanged; this does not
turn the supplied memset/device boundaries into actual CRT/Windows/own evidence.
The live own capture and native full catalog/file operation join remain open.

The retained400-call CATALOG_SOUNDS corpus contains310 paths longer than one
20-byte entry. The furthest write ends exclusively at457571 (last data\093.wav),
seven bytes before graphics global457578. The complete immutable-call extent
report is build/research/application-catalog-retained-cache-extents.json. This
is an audit of that controlled corpus; dynamic own/reentrant writes still require
comparison and existing live neighboring globals must remain preserved.


The ordinary file-open dependency can now be probed independently while the
full nominal capture remains live: its actual412555 request has been reached
in four complete Objects' parent. A preliminary controlled fopenNULL response
for data\data.txt preserves original assets and all existing globals/errno,
then stops at the actual unconditional feof(NULL) entry78179e0d. This first probe
must not execute/count the stopped feof instruction or claim a CRT fault/return.
The static original path sets errno22 and calls_invalid_parameter, but actual
handler selection/termination remains the next bounded dependency. No handler,
encoded pointer or protective state is changed. Preserve raw source input/results,
normal resources before the failed open and all common WAVs. This preliminary
probe does not replace the three nominal whole-catalog acceptance parents.


The fopenNULL entry probe is terminal and retains the complete parent/prefix/
before dictionaries unchanged. Four bitmap helpers/68 APIs precede the failed
open; no FILE/Object exists. It stops at unexecuted78179e0d with NULL and real
return412564. Full globals/RNG/library are unchanged. Independent reconstruction
matches81565536bytes/masks across32regions;3774blobs/4traceparts/700writes verify.
Raw32233382bytes SHA256df7f54b07343ec1572f98060dcd99186db19cf8e2305c0ee88f43f6bf6b858db.
This is an open CRT dependency, not an observed crash or successful native match.


After that terminal entry probe, the next fresh own chain keeps the same NULL
open response and executes actual feof/errno/invalid-parameter handler selection.
It stops before Watson78138945 platform diagnostics/termination, or at an unknown
inherited boundary/actual failure, retaining the complete trace. The encoded
handler, decoder/TLS state, normal protective state and all earlier bindings are
left unchanged; no handler is installed/disabled and no fault is continued. Any
unexpected CRT return is recorded separately. This bounded ordinary IO study
is independent of the still-running nominal catalog and does not restart it.


The fresh CRT continuation terminates at an unsupported research boundary:
ValueError(('TlsGetValue','0x78132be8')). Actual feof78179e22 writes errno22 at
PTD20000008; invalid-parameter decoding then requests TLS indexffffffff. No TLS
response was supplied, no handler/encoded state changed and Watson was not
reached. This is an adapter dependency, not a game memory fault. The failure's
raw32234680bytes SHA2562ca6cb4c932fcdc61d95131cf636b246d25902dbf652943715f454240bfa7dec
and full traceback remain. Independent716-write reconstruction matches the same
81565536bytes/masks,3775blobs and5traceparts; the sole PTD write is independently
verified. Game globals/RNG/library/CW/DC and full parents remain unchanged. Keep
TLS/CRT-startup provenance open; do not turn this into a successful native return.


Native OriginalCatalogRegistry and OriginalLoadedCatalog now accept a deferred
source-provider overload; the byte-source overload remains as a convenience.
The provider runs only after all four built-in constructors and the parent time
sample. A fresh own-prefix rejection test compares69 prior API/time observations
and all four bitmap records before the provider throws; it must preserve the
existing own state and publish no registry. This is a rejection at the open CRT
dependency, not a modeled successful feof/call/Watson outcome. Retained registry,
full-catalog and first-Object comparisons are running; no acceptance claim yet.


The deferred-file verification is terminal:8 release tests pass22.109s/build205.57s, including the own rejection4.775s, retained registry controls, three complete controlled catalogs and the first own Object. All69 API/time observations and four staged bitmap records match before rejection; no registry is published and prior own resources survive. This verifies the native refusal boundary and preceding order, not feof/TLS/Watson or a successful catalog return. All246 fixtures and six foreign files remain unchanged. The full nominal source process is still live.


Next finite native file dependency: whole first Pein DAT decoding and Object temporary-file cleanup, against immutable probe8 on the same pinned EXE/lib/VC80/Unicorn application CPU. The source uses declared 65536-byte user buffers and at-most4096-byte translated read responses; these are harness inputs, not recovered Windows buffer defaults. Native must produce the two decoder writes65536/6566, the actual input-close before final output flush, all raw/logical temporary bytes and final23-byte cleanup text without importing source after-state. Compare ordered file requests and expose unsupported numeric IO/failure boundaries explicitly. The full scanner/file join, other own parents and actual Windows files remain open until separately compared. No new source capture or live restart is needed for this existing complete child.

Static audit application-catalog-dat-file-static.txt uses actual4148a0..414b6f,40f0c2..40f125 and412277..4122e7 instruction starts. Initial audit1 incorrectly began the tail at4121d0 inside an instruction; those misdecoded prefix lines are retained only in application-catalog-dat-file-static1.txt and excluded from the corrected audit. The verified tail always closes the parser input, opens data\temporary.txt in w, writes the literal Do not erase this file. and closes it, including the plaintext path. This static finding alone is not a new dynamic/native match.

The pending first-Object native file join will observe each native scanner's
consumed position plus its delimiter/EOF lookahead. A native stream refills only
when that access reaches the declared translated buffer end. The immutable
probe8's39 read requests and all file events must then occupy their actual
positions among the1963 existing graphics/progress/WAV/allocation observations,
with full event globals. Native-produced temporary bytes supply the parser;
expected temporary bytes/cursors never drive it. This is a finite check of the
existing scanner subset and declared buffering, not all CRT lexical/read-error
branches, private FILE ABI, host files or Windows. Late file/scanner observers
must roll back their enclosing owned candidate.


The first native file comparison passes10 release tests22.434s/build206.73s. Whole DAT:25 file requests,19 reads including EOF,65536+6566 logical writes,74271 raw temporary bytes and input-close-before-final-flush all match. A separate49-event Object file-only lifecycle retains the23-byte final cleanup write and all raw/logical bytes. Two late failures preserve prior owned files/streams. Existing Object/loaded-catalog/decoder tests remain successful. No private FILE ABI, numeric IO failure, parser-event interleaving or Windows claim follows from these controls; own interleaving is prepared next.


The two remaining nominal parents from the initial finite set are ready for independent captures with the unchanged frozen producer: prefix-spec indexes1/2 (own application parents48/49). Parent0 continues in its existing process. Each new run retains the same20000M instruction ceiling,6GiB storage reserve and immutable first/every20-Object checkpoints. These are distinct required parents, not replacements/restarts of parent0. All use original resources and their own full preceding CPU/resource chain; no captured catalog/stack state is imported.


Next bounded ordinary close-response control: after the nominal first-Object requests are known, a fresh parent0 chain will supply -1 at both read-file fclose boundaries and both output-descriptor _close boundaries, while all opens/reads/writes and source assets remain nominal. Read fclose is the existing declared boundary; output fclose continues executing actual VC80 around the supplied descriptor result. The responses themselves change no errno/global/protective state; any actual CRT writes/return values remain observed. Stop after the first real Object ret16 or the first unsupported boundary/failure. Compare all game/file bytes, ignored numeric returns and cleanup order separately from actual Windows file errors. This independent control does not restart any of the three live nominal sources and does not continue the unsupported TLS path.

The first own native file interleaving now matches:11 release tests23.167s/
build209.41s, own case5.249s. All51 file requests are in their source positions
among1963 preceding semantic observations, for2014 events with full globals.
Native scanner positions cause39 translated refills, and final logical cursors,
EOF/closed flags, all produced temporary bytes and the23-byte cleanup agree.
Source FILE fields are read only to compare those logical positions; their
private storage is not imported or claimed as native ABI. Whole catalog and
other parents remain pending. All previously retained controls remain unchanged.

A subsequent whole-file transaction wrapper stages decode, parser input and
cleanup together. Its first verification attempt failed to compile only because
a new diagnostic used aggregate XCTest equality on non-Equatable
OriginalLoaderResources. The assertion now compares its existing fields; no
game rule or expected value changed. That job also retained the prior two-test
file diagnostic because the runner copied it only for a narrower job prefix;
the exact used file is saved. The next runner copies the new third late-cleanup
test, and the active package was not modified during its build.

Static BG/Stage/registry file audit is retained in
build/research/application-catalog-bg-stage-file-static.txt. BG uses the same
suffix-controlled decode/open and final temporary rewrite as Object. Stage
unconditionally calls414a30 with data/stage.dat and data/temporary.txt, then
uses the same close/rewrite/close tail. Registry fclose4127c2 precedes Stage
call4127cd. These are static caller findings, not dynamic own BG/Stage coverage.


Pending native composition hooks now preserve each parent/BG outer-token checksum at its source point, before following file/bitmap callbacks, and expose registry close before Stage. BG/Stage parsers use the same observed native scanner positions as Object. These hooks do not themselves join whole own BG/Stage file lifetimes; retained controls and a file-aware catalog factory remain required. Stage still does not contribute a checksum. This implementation was prepared after the all-three-parent first-Object job was frozen.


All three own-prefix first-Object comparisons pass:13 release tests33.937s/build68.89s, including the two additional parents10.662s. Each compares2014 events/full globals and51 file events, complete Object/Frame/bitmap/sound storage, native temporary output and logical file cursors. The new checkpoints independently reconstruct266 regions/82158754bytes+masks each,4477/4478blobs and1408 immutable parts. Their entire prefix and all six ancestry dictionaries reproduce the accepted prefix/menu-input raw corpora. Lossless record-table expansion is a separate transport artifact; no original expected bytes were replaced. These are first-Object checkpoints, not returned catalogs; all three source CPUs continue.


The negative-close source control returned the first Object normally after840.02s. Four declared-1 close responses are retained; output fclose executes actual VC80. Complete after globals/resource records and CRT snapshot equal nominal, while private stack bytes differ; the DAT decoder returnsffffffff, ignored by its Object caller. Raw37044483bytes SHA2567d6ef2ce704e6716a7ffc6b873e21894bd8d951932bf797660dba52c7ae9f632. Full source reconstruction and native comparison are pending. Native stream allocations now carry the declared0/-1 close result; file events retain it, decoder return values are recorded, and parent cleanup still proceeds. Other numeric IO errors/privateCRT/Windows remain open.


The first-Object negative-close native comparison is terminal and successful:
18 release tests42.062s/build211.96s, own negative-close5.182s. Four numeric
close errors and decoder -1 survive while Object returns and its game/file
state matches nominal. Full source reconstruction preserves all4 differing
private stack bytes; no expected bytes changed. BG/Stage/registry scanner and
checksum hooks pass retained controls, but their own full application join
remains pending. All three nominal source CPUs remain live.

Next finite native composition check: loadWithFiles composes the entire
registry/Object/BG/Stage lifetime, closes the registry before Stage and supplies
each parser from its own decoded temporary bytes. Compare against the three
immutable older loaded-catalog raw corpora (text, raw/zero, interleaved): all
existing game bytes/masks/checksums plus621/621/25 declared fopen/fclose pairs
interleaved with829/829/26 original image metadata requests. Those old controls
run a separate VC80 scanner adapter; their file events do not prove the new
same-CPU refill boundary, own initialized catalog or Windows/private FILE ABI.
Native65536/4096 buffering is an explicit test input here, not inferred from
those old file events. A final Stage cleanup observer failure must publish
neither candidate catalog nor files; surrounding graphics/audio/global output
still needs its own whole-iteration transaction. No new source execution or
changes to old raw/packed fixtures are needed for this bounded comparison.


Prepared after the first factory job was frozen: the same public factory will
be driven from each of the three rebuilt own prefixes and the negative-close
control to the existing first-Object checkpoint. Compare all2014 event globals,
51 file requests and child checksum/cache metadata through cleanup, then throw
an explicit native checkpoint marker from onChild. It must not publish a whole
catalog or files. The existing separate Object comparison remains the complete
Object/Frame/bitmap storage proof; this new prefix check validates shared factory
wiring and does not claim an actual full source/native catalog return. No new
source capture, invented BG/Stage state or expected temporary input is used.


The retained full raw factory comparison passed:21 release tests58.292s/
build215.34s. New factory cases: whole text7.447s, whole raw/zero7.356s,
interleaved0.710s and final Stage cleanup rollback0.504s. All original
Object/BG/Stage/bitmap/Frame bytes+masks, decoded children and shared checksums
match, plus621/621/25 open-close pairs interleaved with829/829/26 image metadata
requests. The source/fixture inputs remain unchanged. This accepts that bounded
older controlled catalog contract through the native file factory; whole own
application/CRT/Windows/device coverage remains open. A separate new test now
checks the shared factory against existing own first-Object checkpoints.


The shared factory first-Object prefix test passed four own trials in20.402s/
build70.14s: three nominal parents plus the negative-close control, each2014
event globals/51file requests through the real child-return boundary. Its native
onChild checkpoint deliberately throws and publishes no full catalog/files.
The unchanged direct child tests retain the complete Object/Frame storage proof.

While the original CPUs continue, trace-audit1 snapshots each primary atomic
trace index once and independently verifies every listed part's packed/raw SHA,
length, complete JSON array and ordinal continuity. Indexes are independent
transport prefixes, not one simultaneous game-state checkpoint. The audit reads
only completed immutable parts and writes separate input/status reports; it
does not change source code/process state or supply native game inputs. Final
full source reconstruction, whole own catalog comparison and acceptance remain
required. This avoids leaving all transport verification until capture completion.


Current Practice app baseline was exercised separately using the isolated
NTSDNative binary and existing AppKit/SpriteKit --screenshot route. PID73591
returned0;1588x1200 capture visually shows District, Naruto/Sasuke, Practice HUD
and AppKit controls. All1391 bundled BMP/WAV files (802261230bytes) equal the
pinned original resource files; game.json equals the existing imported bundle
input. This does not exercise the new own catalog/UI route or establish input
latency, animation timing, sound output or Windows fidelity. CUA inventory again
failed native-pipe startup and supplied no interactive app surface; the exact
technical error, process/asset/render hashes and separate scope are retained in
application-catalog-app-render1-process.json. No source capture was stopped.


Static preparation for the following own boundary is in
build/research/application-postcatalog-static.json/.txt:1 jump instruction at
41c018,156 pool/staging starts41c052..41c2f5,162 interface starts41c2f5..41c581,
all decoded bytes checked against the pinned EXE. This is not new dynamic or
native own coverage.41c018 jumps directly41c052; the playback-only intervening
code is not the selected continuation. Retain actual EBX458b00 World and the
returned EAX catalog; current entrySP1000e430 plus ret8/return address reaches
bodySP1000e43c. No source private stack words become native inputs.

The older controlled InitialLoading adapter's Actor pool70000020 collides with
this own catalog70000020..74d823c8, and its synthetic World68000020 conflicts
with retained common-WAV storage. A planned Actor arena75000020 (400 requests
of420bytes at500spacing) has no overlap in the retained first-Object mapping
list, but every final live mapping must be revalidated before a future source
map. No mapping or allocation response was supplied by this static audit.

The following finite own stage, after whole catalog acceptance, should retain
all three actual application parents through400 allocations/constructors, eight
reconstructions/staging actors and all10 UI constructors/stores to41c581. Native
object0/+90 comes from its own newly loaded catalog; preserve all common and
registered WAVs, files, frame/bitmap allocations, World, library/DC/CRT/input.
OriginalWorldBootstrap and OriginalInitialInterfaceLoading are earlier controlled
implementations, not current own comparisons. Live allocation/constructor/global
order and full new/prior records/masks require comparison; one late UI observer
failure must roll back the entire pending own iteration.41c581 still is not the
full41bc90 return. Do not start this source stage by replacing or restarting any
current live catalog CPU, or by treating an earlier expected catalog as a native
producer. The current whole-catalog comparison remains the immediate priority.


New bounded dependency found while preparing the full own composition: weapon
hit/drop/broken path fields allocate strlen+1 bytes at callers40fbe6/40fc65/
40fce8, store the pointer, copy through NUL and only then enter40bd90 sound
registration. Existing native values preserve the final path but previously
exposed no allocation request/owned history, so later own allocateFrame events
would be unmatched. The unchanged two full old catalogs each retain130 such
mallocs/1700bytes (46hit/43drop/41broken); all full bytes/masks are NUL-complete.
The interleaved corpus has none. Existing objects-probe.json supplies two
repeated hit fields with two separate retained allocations, to check replacement
without inventing a new source stimulus. These are old controlled source inputs,
not current own dynamic coverage or private host/Windows heap equivalence.

Native preparation adds an optional allocator-token callback and owned complete
path records to each loaded Object, retaining previous records on replacement.
Default tokens remain nil rather than inventing reference addresses. Tokens0 or
repeated within an Object are explicit unsupported rejections, not newly matched
source fault cases. The shared factory forwards requests. Extended old fullraw
comparisons will check their interleaving with all Frame/bitmap allocations,
full130 path records and masks, while previous game-byte metrics remain separate.
A late second-allocation observer must roll back prior loader resources.
Implementation/tests are prepared but have not run yet.

The initial static weapon listing began40fb94 inside a preceding instruction;
its misdecoded prefix is retained only in application-catalog-weapon-path-static1.txt
and excluded. Corrected119 starts40fb9c..40fd28 are decoded from the complete
40ef70 function and byte-verified in application-catalog-weapon-path-static.json/.txt.
No source byte, expected result or running process changed.


Weapon-path integration passed27 release tests88.340s/build215.96s. Each old
whole catalog compares all130 allocations/1700bytes+masks; the factory checks
the full15545/15545/770 bitmap/Frame/path allocation sequence across the three
raw controls. Two repeated hit-path allocations retain their distinct full
source bytes and the final header path selects the second; the late second
allocation observer rolls back loader resources. All previous game bytes/masks,
file order and four own first-Object factory prefixes still match. Tokens are
opaque allocator bindings, not host pointer/private heap equivalence. Later
source destruction and superseded-path leak behavior are not claimed here.
All246 fixtures, six foreign files and frozen source producers remain unchanged;
all SwiftPM jobs are terminal. The three full own source CPUs remain live, so
no own later-weapon/full-catalog return or milestone acceptance is claimed.

## Additional source state and operand provenance

Independent immutable-write replay now checks every retained intermediate state
for all three first-Object parents and the negative-close control:81states and
3461allocated-record comparisons per case, including full bytes/masks/liveness,
50088global/9216stack/20480library bytes, retained192local/28MSG aliases and
saved-register/return metadata. Local/MSG have no writes on this observed path;
all81 control words are037f source observations. This does not recover unread
storage or establish host/hardware FPU or private C++ ABI equivalence.

A second verifier merges all recorded operand reads with preceding writes. Each
of the same four sources matches24182215stack reads/83440145bytes at1175PCs and
35catalog byte reads at1PC, with exact known masks and no unknown observed byte.
It replays23535451writes and verifies every956packed/raw part hash and length.
These are existing Unicorn observations, checked without executing EXE/DLL,
controlling a live process, changing expected bytes or importing memory into
native state. The complete scripts/reports are retained underbuild/research as
verify-application-catalog-snapshots.py and verify-application-catalog-reads.py;
source hashes, jobs and exact times are in application-catalog-work.json.

The whole catalog return remains open. These stronger checks must also cover
later checkpoints/full output; the existing20Object watcher only starts the
older final reconstruction and native semantic comparison. No accepted fixture
or runtime game rule changed during this source-evidence verification.
