# Application-owned pause, single-step and resume

Accepted finite pause/step/resume composition for two retained14-call schedules.
Base `06569929b92deeff35234bbf098136845f0438b3`.
[Plan](APPLICATION_PAUSED_GAMEPLAY_PLAN.md),
[evidence](../evidence/application-paused-gameplay.json).
The full standalone game goal remains open. See the [plan](APPLICATION_PAUSED_GAMEPLAY_PLAN.md)
and [prior frontier](APPLICATION_EQUIVALENCE_FRONTIER.md) for the declared scope and missing complete
installed source application trajectory.

The application adapter now continues its actual paused PendingContinuation
without repeating the completed input/round work. The shared paused body owns
installed text/DC across indicators and ordinary output, retaining hit/transform,
match, replay and resource state until its enclosing transaction commits. The
same Bootstrap ticket remains pending through the normal outer tail.

The finite comparison begins after the own first+16 neutral calls, matching the
saved paused fixtures' declared parent. It sends six explicit WndProc messages:
F1 down/up on1/2, F2 down/up on5/6, F1 down/up on11/12. Those message iterations
have their own MSG/counter/recognizer effects; the saved source directly writes
its keyboard table and changes only three bytes. Source acquisition is not
claimed as recorded Windows message delivery.

Current phase and pause latches choose unpaused1,2,7,8,13,14 and paused3–6,9–12.
Replay/elapsed reach18,19,hold,20,21,hold,22,23. The paused caller still initializes
its local-input sentinels and round flag before skipping the respective helper
or simulation path. Independent expectations retain those idempotent writes.

Paused rendering advances District background animation directly, preserves the
camera and current Actor simulation state, draws the World and preserving HUD,
then PAUSE at360/288 and common output. Notice1 text still draws while its timer
holds. Other notice types retain their own contracts, including notice3's separate
increment; enabled playback and its information/indicator remain out of scope.

Both saved14-call schedules and both own continuations are compared at162 stage
endpoints each. Source checks cover every available pool/global/background record,
resource component, early alias, saved/replay backing and observed Frame endpoint.
No middle Frame or whole Object snapshot is invented. Paused global-store order
is checked from41d742 through complete output; background store PCs and endpoint
bytes/masks do not establish unobserved intermediate store order or full Native ABI.

Candidate1 was never executed. Independent review found the wrong paused-store
PC, absent intermediate source owner checks and a newly created input session in
retry tests. Author self-review found omitted idempotent paused caller writes.
Candidate2 fixes all four and preserves candidate1 and its reports. A separate
saved-data reader confirms329 available snapshots per variant with original raw
hashes; it executes no original code and does not count as Native acceptance.

The Native gate selects9 exact methods: two new application pause schedules, two
active-output19-stage regressions, two historical whole paused calls, installed
surface-text/DC two methods and preserving HUD. It includes the complete own
neutral parents. Each new variant also requires18 injected failures and4 retries
(two input sessions and two body sessions), with outer-tail failures retaining
the same ticket and already committed acquisition. All9 release methods passed in 299.767s, with a 382.75s build.
Each own schedule compared14288 exact events; combined324 source and324 own
endpoints,28576 own events,28 Bootstrap returns,16 paused calls,12 explicit
WndProc messages,36 rollback checks and8 same-session retries. These counts
cover the new pause schedules; retained parent/regression counters are not added.
167 other method identities are inherited by pins, not rerun;176 unique in the
retained union. Two of the9 selected methods are newly authored application tests;
the two historical paused methods join the previous172-method union separately.

Shipping fixtures/resources and source expected values remain immutable. X5 work
retains the40GiB external/6GiB internal reserve; release package byte verification,
Native archive verification and independent final publication review are separate
gates. All1028 current/frozen Native files and485 packaged bodies/modes passed;
1020 old Native files are unchanged, four are changed and four added. All383
fixtures and102 resources remain identical. The1028-member Native archive has
SHA256 `0cf445f0c600e4fa36b1d41c7628b17c1460abe9d5bdbab74089dc6516fd0387`;
its full bodies/modes were read back. Evidence links the separately verified
publication archive and independent reviews. Native job2 is terminal in
`build/research/application-owned-paused-gameplay-native-20260914/`; do not restart.
The verifier passed without a reader correction or a second Native run. EXE envelope is not recalculated. The native AppKit/SpriteKit practice,
full damaging fight/match/content/modes/AI/network/playback, actual devices/Windows
and clean-Mac checks are not newly accepted here. Existing safety incidents remain
open; no original, emulator, historical producer or auditor is restarted.


The historical frontier's first ready pause consumer is now implemented in this
finite scope; its missing complete installed source trajectory and storage/ABI
boundaries remain unchanged. NEXT is a read-only readiness card for an actual
command/damage continuation toward the first Naruto/Sasuke District match: map
existing actor-input/combat/active evidence to the current application owners,
identify an already evidenced whole caller and its missing dependencies, and
freeze a bounded plan before any new execution. Do not infer a full fight from
isolated helpers or restart terminal/refused operations. This card does not
itself authorize an unplanned source experiment.
