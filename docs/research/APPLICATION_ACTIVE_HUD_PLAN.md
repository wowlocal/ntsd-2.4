# Active HUD from current commands output

Status: accepted in the finite scope; see [results](APPLICATION_ACTIVE_HUD.md).
The candidate3 frozen plan and failed prior candidates remain immutable. Parent clean commit
`0c1e6d4344e9c13a0de58108119281d0b1f67b5f`, accepted
[commands](APPLICATION_ACTIVE_COMMANDS.md). Read [WORKFLOW](WORKFLOW.md),
[WORLD_HUD](WORLD_HUD.md), [GAMEPLAY_HUD](GAMEPLAY_HUD.md) and archive2710..2780.
Consumer: complete HUD → notices/recording/layout/output, full active tick,
Naruto/Sasuke District match and complete standalone native game.

## Evidence and finite contract

Original baseline EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Saved active48 fixtures SHA256
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
Pristine EXE/VC80 synthetic execution and owned library-enabled application are
separate environments. Actual Windows/device/pixels are not established.

Compare both48 stage14 caller421a15..421a2d and HUD41ae60..41b12d/ret4.
Source-first current-input projection must match complete saved pool/masks/globals,
ordered drawing/read/clip/rectangle/Blt events and all available retained components.
Then independently evaluate current own commands output through the same equations.
No source after-state, event position, unknown backing or private ABI becomes own input.

Caller clears450bc0 at421a1c and450bb8 at421a22 before HUD drawing.
Eight cells draw interface, prefer nonzero primarycell then fallbackcell+10,
resolve current World Actor mapping and Object+728 portrait, preserve aliases.
HP>0 produces wrapped signed31*recoverableHP/125 and31*HP/125 bars,
conditional healing overlay, MP backdrop124 and wrapped31*MP/125.
HP<=0 skips all bars; teams1..4 and default use original globals. Target is
currentglobal455608. Viewport794x550 bounds the reused bitmap expansion, with
original negative-picture fallthrough and unknown metadata values/masks retained.
No Frame, grounded/neutral state, Object ID or type is a causal HUD prerequisite.

Saved inventory2: both48 have selected0/1, HP/recoverable500, MP206..221,
E0/E4zero, teams10/11. All source snapshot refs unchanged, yet two idempotent
command stores are explicitly recorded. Primary5952events/960unknown reads,
control8640/2496. Current own values are computed, not asserted to equal source.
Saved stage effects/boundaries/checkpoints are empty. Full source helper order,
arguments, draw/rectangle/HUD receivers, declared alternating Blt results and observed stack shapes
are compared. Clip receiver/saved-register values are not compared; saved arrays
are shape checked. These are not a Native private ABI implementation. Source FPU
CW023f/FPSW0or4000/tagffff is preserved, no new Native FPU equivalence claimed.
Active stack trace includes only retainedSP68 read421a15, not the push421a19;
that separate store belongs to earlier controlled evidence. Argument unused
by HUD; no own target is imported from it.

## Implementation and discrimination

New comparison-only ActiveHUDProjection reuses accepted current record and bitmap
expansion primitives, never Core HUD/drawing/clipping. New source reader verifies
saved commands/HUD/notices joins and hashes before own comparison. New own tests
compose the complete prior input/control/physics/contacts/graphics/lifecycle/commands
sequence, compare full match/owned state, journal, resources and caller locals.
Graphics owner is checked against the independently derived HUD command prefix;
parent whole-return comparison retains complete command-extent validation.

Controls distinguish command resets/defined masks, current signed MP widths,
maxHP irrelevance, dead HP suppressing otherwise unknown MP, nonzero fallback
activity and aliases without deduplication, unknown causal fields and bitmap masks.
They validate the comparator, not new natural source reachability. Broader HUD
controls remain independent retained WorldHUD/paused fixtures.
At first current own call inject seventh HUD Blt, HUD checkpoint and precommit
failures; compare entry/environment rollback and one successful same-session retry.
Retain parent lifecycle late failures and whole-caller application rollback.

## Ownership, limits and gates

Root owns three new ActiveHUD Swift files, optional CommandsTests forwarding,
this plan/result/evidence and CURRENT_WORK/RESEARCH_MAP. Reviewer war_contract_review
owns independent numbered reports. Core, resources, fixtures, expected/masks,
historical failures/plans/producers and immutable AGENTS archive stay pinned.

Task area `build/research/application-owned-active-hud-native-20260914/` on X5,
APFS UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`; initial.json pins1009 actual
Native files. Keep external40GiB/internal6GiB reserves and40GiB task cap.
Readers96calls/300s; Native3600s; three candidates before contract diagnosis.
Reuse existing frozen-copy, job, verification, package and archive tools.

Eight release methods: two new HUD, two retained Commands, four WorldHUD
(including1789 paused/1753 active controls and two late failures). This corrects
scope1's preliminary three-WorldHUD count. Expect1012 Native files,1008 old exact.
151 unaffected methods inherited by exact accepted pins plus8 directly selected
methods =159 unique. Counts are method identities, not summed overlapping cases.
Candidate1 was rejected before execution for a missing HUD-event scope guard.
Candidate2 completed all endpoint comparisons but its two negative Object-token
controls caused XCTUnwrap to record an expected thrown read as a test failure.
The exact failed job/logs remain immutable. Candidate3 separates that throwing
read from the nonthrowing unwrap; source/game rules and comparison domain stay
unchanged. This is the third candidate, with an explicit diagnosed test-API cause;
any further failure requires renewed diagnosis before extending the plan.
Freeze actual inputs and plan before build; independently review current candidate.
Native comparison,485 package files, archive body/mode verification, independent
publication review and Git publication are separate gates. Preserve reader1's
raw-address/normalized-event error and diagnostic; inventory2 corrects only the
reader's mapping from current bitmap owner order, never expected data.

No original/emulator/historical producer/capture/auditor executes. No affected
safety operation is retried/rerouted. Old incidents remain open. After acceptance
continue notices, recording, layout and output; complete tick/match/game,
app/window/input/audio/device/Windows/clean-Mac acceptance remain open.
EXE envelope is not recalculated.
