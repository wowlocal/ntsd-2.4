# Native bitmap acquisition and owned surface copy

2026-09-26, HEAD a75f3a2. Status: implementation/validation, not accepted.
Previous goal increment made progress: display correction2 passed all35 methods;
the intervening T7 request only confirmed its already committed storage permission.

## Consumer and finite contract

Connect the recovered whole43ee50 constructor,43ed10 loader and4013d0 copy to
the same physical display owner used by whole WinMain. This supplies original
front/menu artwork to the subsequent whole front-screen platform adapter and
therefore the startup/loading path to the first Naruto/Sasuke District match.
Do not build another execution framework. Use OriginalRequestExchange for actual
host work outside Core attempts; its immutable receipts survive retries and late
caller rollback. Physical work cannot be claimed undone by a Core value rollback.

Reference: original distribution EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c and original
packaged DIB/BMP bytes. Read existing BITMAP_SURFACE_LOADING,
APPLICATION_SURFACE_COLORS and APPLICATION_GRAPHICS_OWNERS evidence, plus archive
1968–2025. No original execution, historical producer restart or Windows call.
The existing decoder, source colors and whole-caller comparators remain unchanged.

Native policy: immutable declared file/resource inputs, monotonic identities from
the window's pool, owned bitmap/memory-DC/surface-DC resources, offscreen surfaces
in the existing XRGB8888/sRGB display storage. Missing declared names return0;
undeclared input, scaling, unsupported format/caps and foreign/stale owners reject
explicitly before service. A bitmap can be selected into one memory DC; an active
surface DC prevents destruction. Successful cleanup retires that lease once.
Color-key metadata is retained for the later Blt consumer; this card does not
claim its raster application. No private source pointers or known-zero holes.
GetObject only defines native-produced size/stride/planes/depth fields; ignored
private pointer/type fields remain unknown. Source DIB holes propagate to surface
masks. Copy is checked one-to-one SRCCOPY, not inferred stretching or quantization.

Prepared files/resources are explicit namespaces including declared absences;
no filesystem lookup occurs during a Core retry. Resource IDs/BOOL/HRESULT replies
describe this host implementation, not observations of a Windows driver. Required
resource diagnostics remain separate external requests; the bitmap service must
not claim a message box or debug sink ran. Ordinary source numeric-error behavior
is retained by unchanged Core and its original whole-loader/late-failure tests.

## Checks, ownership and publication

Four new methods: all bundled DIBs through whole constructor/full pixel and mask
comparison on the actual startup display; late constructor failure/retry and
declared missing inputs; DC selection/acquisition/cleanup, unknown RLE and bounded
rectangles; protocol/foreign/duplicate/cancelled permits and allocation exhaustion.
Include a prepared BMP-file path and explicit XRGB optional-format load. Expected
XRGB words are independently assembled from accepted source RGB bytes, with full
mask comparison; existing original decoder fixtures provide independent pixel
evidence. Keep all35 prior methods, all3 whole BitmapSurfaceLoading methods, all3
DIBPixels methods and all3 SurfaceSourceColors methods:48 methods total. Freeze
the exact names/limits before execution; stop at the first nonpass. At most3
candidate correction rounds before contract diagnosis; preserve every failure.

Baseline: actual2263-file correction2 candidate, manifest SHA256
89f795574ca6ef954a2f44c0a3c1078a8c259c562f51863092bbfa9ea5131417.
Modify its display backend, add bitmap service and new test file. Preserve root
1034 actual Native files, all2263 baseline inputs, source55 pins, fixtures, earlier
candidates, reference masks and archive AGENTS. Only task-owned candidate/alias,
preparation adapter/templates, plan/study/evidence and own navigation insertions
may change. Independent reviewer unavailable; author inspection is not review.
The safety register remains open; no affected operation is requested here.

Task application-mac-bitmap-20260926 under existing X5 parent
01a0dc49-738f-7972-8fb0-e98fb2f34408. Recheck writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,90GiB external/9GiB internal before prep.
Retain40/6GiB reserves and17GiB existing source commitment. Physical28GiB, stop
observed decrease26GiB, logical150GiB, metadata16MiB/root2MiB. Prep1800s;
build3600s/RSS12GiB; queue14400s, added methods600s/RSS8GiB, inherited limits
unchanged; finalization3600s. Reuse guarded runner/queue/package/archive tools and
exact result predicates. Check PID/start/command/cwd/job before any process action.
Source59727 remains terminal34 Objects, not137. No source restart.

Acceptance separates fresh build/package, all48 methods, unchanged protected
inputs, regular artifact/PAX metadata verification and independent review. Commit
each coherent checked increment with open gates stated. Whole front-screen host
adapter, general Blt/presentation/callback/rectangle provenance, palette/text,
Windows/input/audio/clean-Mac/full match/full game remain open. NTSDApp remains
Practice. EXE envelope not recalculated; a bitmap provider is not a playable port.
