# Application transaction driver candidate — frozen plan, 2026-09-22

Scope follows [host preflight](APPLICATION_HOST_TRANSACTION_PREFLIGHT.md),
[WORKFLOW](WORKFLOW.md), [CURRENT_WORK](CURRENT_WORK.md) and the application
Bootstrap/MenuSession/startup-input/graphics/loaded-cycle dependencies. The next
product result is a persistent recovered application owner that can replace the
practice driver's transaction handling once real host providers exist.

This phase implements a candidate and its comparison wiring; it does not claim
compiled Native acceptance. No original execution, source retry or device IO.
The original EXE/lib/CRT identities and controlled D evidence are unchanged from
the preflight. Saved menu-return case0 and the existing 48 primary/50 input-chain
comparators are the behavioral basis; synthetic replies remain test inputs.

Actual base HEAD: e898b97117a73f2aeebc7b4cbc75579e6ac42ca3. Actual Native input is
the 1034-file `first-damage-loading53-native-20260922/candidate1-inputs.json`
manifest; validate every body/mode/membership against root and the existing X5
candidate before cloning. Preserve root Native, 55 live source code pins, all
old fixtures, baseline game resources, prior candidates/plans/expected/masks and
the immutable instruction archive. The live source continues unchanged.

Owner: root author. No independent reviewer available; leave that gate open.
Existing safety incidents remain open and their operations are outside this work.

## Finite implementation

Add `OriginalApplicationHostSession<Platform>` in candidate Core. It owns one
Bootstrap and an independent staged-copy platform, prepares immutable iteration
inputs on another independent platform copy, commits both together, retains an
exact pending loading attempt without delivery, and provides one destructive
handoff of each committed batch. A recursive lock serializes calls; an in-flight
guard rejects reentry. Graphics remain metadata alongside the original ordered
effects, not a second delivery list. No physical IO/replay guarantee is inferred.

Bootstrap may accept nil first-menu initialization for already initialized
iterations, forwarding the existing Session nil-initialization contract, and
forward the existing `bodyProduced` diagnostic hook. This must preserve all old
call behavior. No Core gameplay, timer, input, allocation or device rule changes.

Adapt the saved test composers through an optional host path, keeping their old
default path and all expected comparisons. Host-only sentinels in a staged test
platform measure mutable queue/reservation independence; they are not game state.
Wire all 48 primary parents, six startup/17 first-menu late controls, same-attempt
retry, all 50 input chains, pending-ticket retention, no duplicate handoff and
reentry rejection. Retain the preflight's six acceptance groups. Exact method
inventory is frozen in this task before a later compile/test phase.

Allowed candidate changes: new Core host file; Bootstrap forwarding; new host
tests and optional wiring in BootstrapTests, MenuReturnTests, MenuInputTests and
WinMainStartupTests. Package.swift is unchanged. Other changes require an explicit
new explanation; never weaken old comparators or alter fixtures to pass.

## Storage, processes and outputs

Task-owned X5 directory:
`/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-transaction-candidate-20260922`.
Repository alias: `build/research/application-host-transaction-candidate-20260922`.
Verify mount UUID 3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548 and original 40GiB external /
6GiB internal reserves before writes. APFS-clone the existing regular X5 Native
candidate; compare body/mode/membership and require distinct regular inodes.
Do not create a HEAD-only checkout or import old .build/.swiftpm caches.

Candidate phase allowance 256MiB observed free-space decrease, including concurrent
source growth, with a 224MiB stop threshold before final metadata/archive. No
reserve reduction or deletion of evidence. At most three candidate corrections,
120 minutes for this phase. Syntax parsing uses the installed Swift toolchain,
minimal environment and task TMPDIR; retain command/PID/start/cwd/job/log/exit.
No build, link or XCTest run in this phase. A later validation task must obtain
a fresh bounded allowance and compile the changed Core plus dependent targets;
old linked binaries cannot validate this change. Current space supports a
prospective 12GiB validation allowance after the declared source budget and
40GiB reserve, but this is not a build authorization or a measured build size.

Root mutable paths: this frozen plan, APPLICATION_HOST_TRANSACTION_CANDIDATE.md,
docs/evidence/application-host-transaction-candidate* (including a reviewable
text patch), one small task preparation/finalization helper if needed, and own
CURRENT_WORK/RESEARCH_MAP navigation hunks. Candidate Swift edits occur only in
the new external tree. Do not modify closed task directories. Preserve original
producer outputs and failed attempts. New errors get distinct records.

## Candidate gate and delivery

Hash actual inputs and plans; inspect prior archive constraints for edited
filenames/dependencies. Verify the full clone, frozen candidate composition,
unchanged old fixtures/resources/root Native/live code pins, Swift syntax parse,
exact changed-file patch and archive bodies/modes/membership. Record author
inspection separately from independent review. Publish implementation and remaining
compile/test/device gates honestly. Stage only this increment's files/navigation
hunks and make an iterative commit under the current WORKFLOW rule; leave prior
unrelated pending work intact. Do not promote candidate files to root while its
1034-file input contract remains protected.
