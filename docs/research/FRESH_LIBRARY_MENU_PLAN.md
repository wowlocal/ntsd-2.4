# Fresh music/resources through the library menu return

Previous goal turn made progress:694b5e1 accepts the controlled cached menu
through actual422ab8/ret4. Current source/native inspection finds fresh music
and eleven bitmap constructors accepted only through429e5a; their full enclosing
library menu/output transaction needs a direct comparison on the same CPU.
This study joins those existing mechanisms without waiting for or substituting
the still-running full own catalog.

Reference: pinned original NTSD EXE/lib.dll, embedded11 DIBs and VC80 under
Unicorn2.1.4. Reuse the actual41bc90 prologue and declared4229cc context from
LIB_MENU_CONTINUATION. The earlier tick body and outer424746 caller remain
unexecuted. Actual429730 music, sprintf, bitmap constructors/loaders/copies,
installed text, screen/panel, output and ret instructions must run continuously.
Trace all reads/stores/regions, helper returns, ownership and stage snapshots
because the graph-log and bitmap dimension storage share a stack lifetime.
Native must consume its own produced bytes and resource records, never private
source stack or expected after-state.

Keep the controlled common screen resources/World/Actors of the previous study;
new eleven menu wrappers use separately declared guarded A5/ramp allocator
backing. COM/GDI/image/timer/PTD/Sleep/Shell/allocator responses are explicit
research boundaries. Image dimensions come from the pinned original embedded
DIBs. No Windows pixels, host device/URL operation, full CRT startup, control
pointer damage, protection corruption, fault continuation or bypass is claimed.
Actual normal cookies/SEH/guards remain enabled. Every unexpected error retains
its exact log/state/time and stops that operation; no automatic fault retries.

Declare26 finite cases before capture:13 per backing control, all fresh
loadFlag1/previousMenu0/currentMenu10. Nominal, confirmation/background release,
network+overlay, volume-up, help, panel click, playback, positive last surface
result, missing RFACE plus failed following description, failed graph creation,
failed render, NULL wide allocation, and NULL SPARK allocation. The last stops
before the known429b21 NULL write and must roll back natively, not be counted
as a complete match. Preserve the prior wider helper error/unknown-input corpora.

Acceptance: one nominal probe then all26 atomic cases; independently verify
original bytes, reference assets, complete writes/reads/masks/checkpoints and
actual return cleanup. Compare the native composition's globals/World/common
and new bitmap/music owners/local strings/library DC and complete ordered
platform requests. Add late rollback checks after fresh music/resources and
output; the caller event buffer must remain uncommitted. Preserve253fixtures,
all14 pending native/3source/6foreign hashes and frozen watcher bundles.

Do not mutate old producer files, captures or accepted native packages. Use
an isolated accepted694b5e1 package with only owned new changes and an archive
composition retaining every input. Maintain the6GiB source reserve; revalidate
catalog11636/watcher15664 and never restart them for silence. Own catalog/menu,
other selected bodies, app/window/Windows/device/full-match/all-content/network/
clean-Mac acceptance remain open. This is a controlled fresh caller composition,
not proof of the skipped own initialization or the whole game.
