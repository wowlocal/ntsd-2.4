# Owned Start through match preparation and application return

Status: finite Native continuation verified by the declared machine checks.
Final independent publication status is recorded in the linked evidence.
Base: `7d9fd92da35a6d5f1abf6d049e98be0e28a3aafd`.

The game behavior is the actual retained Start child after the own Bootstrap,
human selection and District selection chains. Continue that child through
prelude, installed-library preparation, enabled music, replay initialization,
remaining menu, enclosing Bootstrap return and the next gameplay input boundary.
The first full Naruto/Sasuke match and the complete native game remain open.

Reference: pinned original NTSD EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
the library/CRT contracts in [MATCH_LAUNCH](MATCH_LAUNCH.md),
[MATCH_PREPARATION](MATCH_PREPARATION.md), [LIB_STAGE_COMMANDS](LIB_STAGE_COMMANDS.md),
[MUSIC_PLAYBACK](MUSIC_PLAYBACK.md) and [REPLAY_INITIALIZATION](REPLAY_INITIALIZATION.md).
Two saved eight-section launch corpora remain immutable. This study reads their
records and raw original District BMP files; only Native Swift executes.
No original execution, emulation, historical producer/auditor or refused operation
is retried. The existing safety incidents and source-fault dependencies stay open.

## Contract and ownership

`OriginalApplicationMatchLaunchSession` resumes the exact pending Start ticket.
It preserves current World/400 Actors, catalog, library state, RNG, menu and
sound owners, music heap, memory aliases and buffered graphics journal. It uses
the shared handlers and commits the child only after its final observer succeeds.
The enclosing Bootstrap retains responsibility for publishing the iteration.
The completed selection body is not replayed to resume the child.

The installed library requested Object ID is initially unknown. Its address is
PE section-alignment padding, not established BSS. The first actual perspective
setter establishes the value. Known-input initializer calls remain supported;
the public property is now optional, an explicit provenance API change. Command3
reads the first Object header before requiring the ID; an empty catalog need not
read an unknown ID. This differs from the pristine executable's unpatched rule.

Two ordinary retained chains use own stage5 music and own RNG table. Independent
comparison projects the saved write footprints and formula onto those declared
inputs: four draws177/53/189/18, installed human X240/240 and Z503/468. It checks
complete records/masks rather than injecting expected state into the engine.
The 18 replay participants include inactive slots: all234 array words, names,
settings,3001 RNG bytes and the whole6491672-byte buffer must compare.

District needs15 bitmap constructions from13 original files. The new ordinary
package retains every original byte and manifest hash; direct manual BI_RGB
projection checks662035 known pixels,1986105 RGB bytes. These are source colors,
not measured Windows raster output. The test checks all225 bitmap requests and
responses, current graphics identities and lifetimes, music operations, recording
allocation, the complete recording-notice overlay and presentation/timer order.
Seventeen initial layer-release helper calls free no old surface. A separate
controlled repetition starts from the owned returned state to exercise15 actual
Release/free pairs and replacement of the old recording, plus changed text DC.
This control is not a second naturally played match or a new source capture.

## Finite validation and preservation

Work: `build/research/application-loaded-launch-native-20260913/` on the declared
X5 task area. Immutable plan1 and amendments1..3 pin input provenance, allowed
paths and the addition of raw District files/current checkpoint ownership.
Initial limits:40GiB task storage,6GiB internal reserve,3600 seconds per Native
run and three candidate rounds. Author: root; read-only reviewer: war_contract_review.

`validation-plan1.json` enumerates36 suites and93 methods, retaining all65
preceding methods plus direct shared dependencies. New controls stop late in
time/sound/bitmap/music/input-reset/replay/menu-return/commit and next input;
both session and caller-owned buffered environment must roll back. They inject
Native observer errors and do not claim original API-failure equivalence.

Before validation, review found and corrected stale replay-pointer aliases,
intermediate text DC snapshots, an incomplete return overlay journal and gaps
in whole-record/event comparison. The initial saved-data reader's wrong zlib
wrapper mode is retained as a diagnostic; raw deflate decoding fixes the reader.
Old expected fixtures and original bytes remain unchanged. Build/test outcomes,
review findings, package/archive checks and final delivery will be recorded in
[application-loaded-launch evidence](../evidence/application-loaded-launch.json).

The next dependency is the own gameplay body after41e339, using the retained
installed-library command state, current resources and full rollback. Other
modes, positive CPU chains, playback, full match/application correspondence,
backend delivery, real window/input/audio, Windows and clean-Mac remain open.

## Machine result

Candidate2 passes all93 release methods in732.971s, build0.37s.
All980 Native files,383 unchanged fixtures and485 package files
match the frozen candidate bytewise. The complete Native archive is verified.
The saved diagnostics, every failed candidate and terminal job remain retained;
no completed original source was restarted. No full-game or device claim follows.

Native1 completed91 passing methods and two failed new methods: the declared
test wrapper73010020 and future replay72000020 overlapped the own catalog
70000020..74d823c8. Native correctly rejected the first allocation. Candidate2
changes only the two new test files, moving own logical allocations while
retaining the saved source replay72000020. Its two-method preflight passes in
46.980s/build339.61s. The final93-method run above uses exactly that candidate;
these are95 successful executions of93 unique methods, not95 unique tests.
