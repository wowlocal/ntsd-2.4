# Active camera, drawing and post-draw impulses

Status: three finite stages and their current owners verified.
The [plan](APPLICATION_ACTIVE_GRAPHICS_PLAN.md) continues the accepted
[contact stages](APPLICATION_ACTIVE_CONTACTS.md) on both retained48-call inputs.
The consumer is the remaining lifecycle/commands/HUD/output path toward a full
match and a standalone native game.

## Independent calculation

The new comparison reads current pool/global/Object/Frame/BG/bitmap records and
their masks. It calculates camera bounds, facing look-ahead, smoothing, District
parallax and animation counters, then depth-sorted shadows, sprites and names.
Mirrored sprite width comes from the normal sheet and unoffset Frame.pic; bitmap
selection uses Frame.pic+Actor318. Sprite destination y includes integerY.
Frame number remains distinct from its picture in the source helper arguments.

The declared active path has two distinct living fighters, District bitmap
layers without fill or looping, no extra lives/sparks and zero horizontal
Actor1c. Unsupported branches stay explicit; these guards are comparison limits,
not newly invented game rules. Current owned positions and animation can differ
from the saved source, so source coordinates/order are never reused as own
expectations. Immutable neutral drawing helpers retain their previous guards.

Post-draw text reads six signed bytes from inactive World slot10. Pristine
SetBkColor differs explicitly from the installed SetBkMode1/retained DC contract.
The finite count0 impulse path clears pending values to positive zero, while
nonzero hitstop preserves them. Accumulated nonzero impulses remain outside
this active comparison; the earlier general impulse contracts are not removed.

Source checks cover full pool/global/BG endpoints, bitmap reads and masks,
ordered grouped effects, relevant helper arguments/results/return PCs, camera
global stores and available owners. Middle Object/Frame heap snapshots are
absent; no snapshots are invented. Full helper ABI and instruction stores are
not claimed. Synthetic addresses, backing and API replies are not observations
of a Windows heap, device or final pixels.

## Owned application and validation gates

Own checks continue the real input/control/physics/contact test chain and keep
all existing same-ticket retries and late rollback checks. An optional return
observer forwards the actual PendingReturn unchanged. Each selected checkpoint
compares complete match/storage, caller local record and chronological journal;
the returned command prefix independently checks graphics owners and colors.

The full return also checks command extent and binding relations, so extra
commands cannot hide beyond a selected prefix. For the later seven stages only
their observed effects supply that relation check: their positions, animation
and game semantics are still unaccepted. Core code and source expected records
are unchanged.

Four release methods passed in198.941s/build359.54s. They compare288 source and
288 own endpoints,10560 events on each side and1728 undefined bitmap reads on
each side, with220 source camera global stores and3600 source helper returns.
Twenty negative and four positive comparator trials pass.145 other methods
retain exact pins (149 unique retained methods); input/control/physics paths
in changed helper files are exercised transitively, not counted as extra tests.

All1003 Native files and485 packaged files were verified;996 previous Native
files,383 fixtures and102 resources remain unchanged. Both Native and evidence
archives were read back by bytes and modes; the evidence archive has46 members.
The separate publication verifier initially used the wrong retained contact-log
label. That failure and its corrected verifier are preserved; Native was not
rerun for it. [Machine evidence](../evidence/application-active-graphics.json)
records exact jobs, hashes, archive restoration, reviews and limitations.

The first frozen
candidate was rejected before any Native run: review found Frame-vs-picture
helper arguments and a nondiscriminating camera trial at the common clamp0.
Its files and both issue records are retained. The revised comparison-only
trial sets both x500, camera130/velocity0, giving opposite movement after the
two facing targets; it is never supplied to the application or source.

Candidate2 built successfully and passed both contact regressions, but the two
new tests stopped before graphics comparison: the new trace reader declared
boundary entries as strings, while a later lifecycle stage contains a structured
memset record. Candidate3 decodes that record's kind and still requires no
boundary entries in the three selected stages. Both immutable fixtures retain
their lifecycle record; the reader error and terminal logs are preserved.

Both Native jobs are terminal; candidate1 was never run. The next saved-data
inventory identifies a call17 Actor50 reconstruction alongside the two fighters,
97 returns from40d960 and7 catalogSound events per variant. Establish its
input provenance and complete lifetime before accepting the next lifecycle stage.
The final publication review remains the commit gate.

Full active tick, later transient lifetime, full match/game, runtime integration,
actual window/input/audio, device/Windows/clean-Mac and existing safety incidents
remain open. No original game/emulator or historical capture/auditor is run.
The documented EXE envelope has not been recalculated.
