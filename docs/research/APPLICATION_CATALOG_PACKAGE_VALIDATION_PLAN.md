# Catalog package — fixed Native build, comparison and standalone read

2026-09-26; base HEAD547ab9c. Previous goal turn was progress: the original-data
package/reader candidate was implemented and fully archived. Continue its first
open gate, not a new candidate or original capture. Read WORKFLOW, CURRENT_WORK,
the catalog package plan/study and their direct startup/catalog/image dependencies.
This validates the production input producer required by the standalone host on
the path to the complete Naruto/Sasuke District match and full game.

## Inputs and evidence boundary

The frozen2241-file Native candidate is application-catalog-package-20260926/
candidate1; manifest41725682877ec5430eca64c165ac46f857e25af8ea534c7ed388208293cfde54.
Its1199 regular package files carry1198 payloads/661,635,220 original bytes.
Package manifestfd42d041549f00f5ba715063d7e560a9e2b01ab720017d1da5e032c34ca9451d
binds521 file,669 image and8 music inputs. Preserve root1034, previous1040,
candidate2241, source55, all fixtures/expected/masks, source jobs and archives.
Actual dirty inputs are pinned; a HEAD-only checkout is not equivalent.

Reference remains original EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
This stage compiles/executes Native and reads saved comparison records as data.
No original EXE/DLL, emulator, historical capture/auditor, live game/device or
affected refused operation executes. Logical responses and backings remain the
previous declared controls, not measured Windows behavior. Incidents stay open.
Independent review is unavailable/open; author verification cannot close it.

## Finite execution and acceptance

1. Revalidate terminal prior jobs and frozen manifests. APFS-clone all2241 Native
   files into a fresh task, retaining contents/modes/ns mtimes and distinct inodes.
2. One fresh release native-SwiftPM build with --build-tests, two jobs,
   --disable-index-store and -enable-testing; task-owned scratch/cache/config/tmp.
   Reuse the checked host-gameplay monitor without changing its process identity,
   descendant/guard/reap body. Adapt only task name and exact76-phase domain.
3. Verify fresh192 Core/61 Reference/271 test source inventories and outputs;
  385 test fixtures plus1301 ordinary Core resource files must match2241 inputs
   by membership, length and SHA. Exit0/linkage alone does not pass this gate.
4. Run exactly selected-methods2 from the frozen candidate:4 new reader methods,
  3 direct full-catalog/late rollback methods and all69 prior host/caller methods.
   No grouping/splitting or schedule truncation. Retain both backings, complete
  17/48/14 schedules, masks/owners and late failure/retry. Input-map comparison
   precedes the direct catalog/host consumers. Stop queue at first nonpass.
5. Use the separately verified one-anchor XCTest result-reader correction:
   exact method, one result, line-end anchor, three one-test/zero-failure summaries,
   Selected-tests pass, exit0, no guard/signal/residual process. Preserve the old
   reader nonpass and its controls; do not edit an expected result or waive failure.
6. After successful compilation/package comparison, link a small Core-only client
   against the exact fresh Core/codec object files. It calls the public bundled
   reader and reports complete file/image/music/known-pixel counts. No test or
   reference module/object/fixture may be linked or installed. Pin source, command,
   objects, binary, linked libraries and all ordinary app files before execution.
   Install in a task-owned directory under /private/tmp, outside checkout and
   research volumes; verify all1199 installed package bytes independently.
   Execute against its app bundle with filesystem reads of repository and
   /Volumes/X5,/Volumes/T7 denied by a declared sandbox-exec profile. Probe the
   restriction explicitly; a sandbox/tool error is not a reader pass. One complete
   read plus one missing-app-package rejection are required, preserving both logs.
   This is an input-delivery check, not a window or clean-Mac game acceptance.
7. Check protected inputs and every archive member's name/body/mode/ns mtime.
   Publish separate build/comparison/package/standalone/archive gates and the
   remaining review/device/full-game limits; commit each coherent checked increment.

## Fixed limits and ownership

Task: /Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/application-catalog-package-validation-20260926,
with build/research alias. Verify writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548. Require90GiB external/9GiB internal before
preparation. Keep40GiB external/6GiB internal plus17GiB source commitment;
external physical allowance28GiB, stop at26GiB decrease, logical150GiB (APFS clone
and compiler-product accounting). New internal standalone artifacts<=2GiB;
internal reserve stays6GiB, no evidence/cache deletion or reserve reduction.
Metadata<=16MiB per archive, root additions<=4MiB. Use X5; T7 stays available for
future separately bounded archives without altering this task's pinned paths.

Build timeout3600s/RSS12GiB. Whole76 queue deadline14400s. New reader methods
1–4:600s/RSS12GiB each; direct catalog methods5–7:900s/RSS16GiB each. The remaining
69 retain their original ordinal-based limits: old1–50 timeout600s,51–63 timeout
900s,64–69 timeout1800s; old>=51 RSS16GiB, old26/28/35–37/43–50 RSS12GiB, other
old methods RSS8GiB. Full method RSS is not reset at an internal subcase. Reuse
poll1s/RSS2s/logical15s. Client compile<=600s/RSS12GiB and each client execution
<=300s/RSS8GiB; standalone stages sequential with XCTest to avoid overlap.
Finalization<=3600s. No automatic rerun on failure or monitor observation timeout.

Mutable: new task/alias and generated tool copies; preparation adapter under
tools/prepare_application_catalog_package_validation.py; task-owned standalone
client/app/profile/logs; this study/evidence and own navigation additions.
Candidate source files and every old tool/plan/result remain unchanged. A compile
or comparison defect gets diagnosis and a separately identified correction.
Do not promote root Native or run the shipping build script as a side effect.

If a monitor fails, verify its exact child PID/start/command/cwd before handling
it; do not presume stopped. Preserve partial/terminal logs. Source59727 remains
terminal at its recorded34-Object publication boundary; never restart it. Full137
source return/transport, host delivery owners/prepared backend, actual AppKit
input/render/audio, Windows, clean-Mac, first full match and full game stay open.
EXE envelope will not be recalculated by this producer validation.
