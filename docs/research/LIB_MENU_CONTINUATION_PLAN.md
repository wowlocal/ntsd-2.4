# Installed-library menu continuation through the actual match return

Previous goal turn made progress: accepted4147a33 compares the whole enabled
panel inside431d10. Current inspection identifies the remaining library-aware
output caller: OriginalMenuReturn still uses pristine401290 text after menu
return.4242e0 is the already recovered loading-progress function, not a new
application timer dependency; APPLICATION_TIMER/MESSAGE_LOOP also remain accepted.

Recover the declared4229cc menu continuation, actual429730 prologue/cached
resource prefix, whole mode screen/panel, real42e0f9 ret12,4229e2 network notice,
4028a0 overlay,43e940 presentation and actual422ab8 ret4. Reference: pinned NTSD
EXE/lib.dll/VC80 under Unicorn2.1.4. Use the established actual41bc90 prologue
to create its ordinary stack/cookie/SEH frame, then enter the declared menu-tail
context. The intervening phase/input/gameplay body is not executed or claimed
as an own full tick. No private frame bytes are synthesized to force an epilogue.
Preserve original cookie checks, full writes/reads/register/SEH observations and
the exact point of each controlled entry. Native compares semantic game state;
it never imports private source stack or expected after-state.

Use the accepted mode-screen controlled World/nine Actors, six owned bitmap
records and two replay allocations. Current/previous menu10 and loaded resource
flag0 represent the ordinary retained-resource gate; no needed helper is disabled
to fabricate a return. New first-time resource/music loading and other character
screens keep their existing studies and own initialized joins. The original
selected caller must choose its actual continuation. Stop before playback or
an unimplemented menu screen; never jump over it as a successful return.

Install the library through the accepted research loader. Attach the pinned
CRT image and its actual initialized C-locale/PTD using the established research
attachment contract. Real formatting, library text, bitmap/clip/fill, sound,
volume and presentation helpers run. COM/GDI/timer/Sleep/Shell/free/quit and CRT
thread-state responses are declared research adapters, not host/device/Windows
operations. Instruction/read/store observations recover output ordering, string
provenance and all mutable state. No unrelated systems, damaged control pointers,
protection corruption, fault continuation or safeguard bypass are in scope.

After static inventory, publish a finite manifest before capture. Include both
backing controls, ordinary/network-notice gates and GetDC errors, all overlay
notice types and expiry, volume input/COM responses, presentation modes, mode
confirmation/resource release, panel click/help ordering and playback/panel
boundaries. Start with one immutable nominal probe; retain every completed case
atomically. All errors retain exact time/log/state and stop the affected operation.

Native must share pristine/library output rules and keep the whole menu-tail
composition transactional: globals, World, bitmap/replay ownership, local strings
and retained text DC commit together. Late failures after screen/panel, network
text, volume, presentation and before return must roll back all owners and leave
the caller event buffer uncommitted. Keep unknown fill padding separate. Verify
actual return cleanup separately from native semantic results.

Preserve252 fixtures and all14 pending native/3 source/6 foreign hashes, plus
the four frozen watcher bundles. Catalog11636 and watcher15664 are live and
must never be restarted for silence. Use isolated accepted inputs and save all
owned revisions; clone unchanged files and reference their already verified
base archive when needed to retain the6GiB source disk reserve. The full own
catalog/menu/app/Windows/device/full-match/all-content/network/clean-Mac goal
remains open; this controlled continuation is not proof of the skipped tick body.
