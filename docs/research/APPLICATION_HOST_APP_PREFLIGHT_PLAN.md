# Host-to-app input and delivery — bounded read-only preflight

2026-09-26, base HEAD95be05f. The preceding goal turn was progress: the unchanged
candidate passed all69 selected methods and package/archive closure, and T7 was
authorized separately. Continue WORKFLOW/CURRENT_WORK and TASK_TEMPLATE. This
preflight identifies the next shipping consumer of the existing persistent host
on the path to a complete Naruto/Sasuke District match and then the full game.
It does not substitute a demonstration window or fixture player for that goal.

## Question, inputs and boundaries

Inspect all three Host.Batch contents (startup, iteration, loaded), their actual
ordered operation/effect/graphics producers and the shipping NTSDApp entry.
Determine where device replies are required before commit, which bytes/identities
already have native owners, and which host preparation/delivery contracts remain
missing. Distinguish immutable package inputs, owned computation, host observation,
resource reservation and terminal side effect; record exact failure/retry limits.
Select a concrete next implementation only from the resulting dependencies.

Actual input: frozen1040-file application-host-gameplay-correction1-20260926
candidate, manifest5505b78d77d5c92eb6932508deeab30184ee406452d22ce2522e151df70490b5,
and its all69 completion publication. Protected root has1034 Native files and
unrelated dirty tests; a HEAD-only checkout is not the input. Read the existing
transaction preflight, gameplay completion and direct startup/graphics/loop/menu/
loaded/gameplay provider studies. Search the immutable instruction archive by
source/dependency; preserve its unknown-backing, ordered output and device limits.
Pin up to80 consulted Native Swift files and up to45 study/policy/metadata files.
Pin actual bytes, not just HEAD; preserve source55 and both Native memberships.

Reference remains original Windows NTSD EXE
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
Existing evidence has declared synthetic mappings/API replies/ABI/FPU and
unknown-storage masks; no new Windows/device equivalence follows from inspection.
This task reads Native source and saved metadata only: no EXE/DLL execution,
fixture decoding, trace/blob replay, Native build/test, app launch, OS device
experiment or installation. It neither retries nor resolves safety incidents.

## Finite deliverables and acceptance

1. Revalidate terminal queue90854/finalizer26386 and source59727; preserve all
   existing results, previous reader nonpass and full-game/open-review limits.
2. Map startup/iteration/loaded effect order, overlapping graphics views, resource
   generations and consumption lifetime from their current producers to Batch.
   An absent association or payload remains a specific gap, not a guessed zip.
3. Inventory every public startup provider method and each host/loaded provider
   input group with provenance, current native owner, needed host observation,
   side effect and retry boundary. No test constants become production defaults.
4. Assess actual NTSDApp input/render/audio/time routes and reusable package
   resources. Practice rules are not the target implementation. State the missing
   integration edge and any device/Windows/unsupported contract explicitly.
5. Deliver one finite next candidate/validation proposal, including whole-parent
   comparisons, retained owners, late failures/retries and an app/device gate
   when its dependencies permit. If prerequisite evidence is missing, identify
   the exact next question rather than inventing a successful adapter response.
6. Verify protected bytes/membership, input pins and publication/archive separately;
   document evidence versus inference. Independent review is unavailable/open;
   author analysis and syntax checks cannot close it. EXE envelope not recalculated.

## Storage, commands and process ownership

New metadata task: /Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/application-host-app-preflight-20260926,
with build/research alias. Verify X5 APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548
and free space using filesystem statistics (diskutil's APFS FreeSpace field may
be0 despite available container space). Retain40GiB external/6GiB internal and
the17GiB source commitment. Require60GiB external/7GiB internal before IO. New
physical allowance64MiB; task logical16MiB, archive<=8MiB, root metadata<=3MiB;
elapsed3600s. No copying builds, fixtures or traces. T7 is allowed for later
artifacts under the new policy but this metadata task uses existing POSIX helpers.

Use rg/read-only shell/Python, existing archive_catalog53_storage.pin/checked,
existing manifest/context/job layout and PAX metadata archive/readback procedure.
No new execution framework. Before inspection create context1.json/plan1.md and
prepare1.job.json; append consulted pins in a separate finite inventory as needed.
Final outputs: observations1.json, verification1.json, metadata1.tar,
publication1.json, external-close1.json and study/docs receipts. Record command,
PID/start/cwd and terminal outcomes. Preserve preliminary read errors: missing
guessed tool filename (chunkbad757) and absent guessed preflight directory
(chunkd3a28a); both are read-only inspection errors, not source/Native failures.

Mutable paths: this new task/alias; docs/research/APPLICATION_HOST_APP_PREFLIGHT*.md;
docs/evidence/application-host-app-preflight*.json; own navigation additions only.
Do not alter Native, tools, frozen candidates/plans/results, old WORKFLOW snapshot,
incident register, immutable AGENTS archive or unrelated pending files. Current
WORKFLOW now differs intentionally after T7 authorization; pin the current policy
and its explicit amendment, not an old mutable-path hash as a new current input.
Commit this coherent checked result before starting another independent task.
