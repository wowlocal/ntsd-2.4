# Observed whole startup — fresh25-method validation

2026-09-26, HEAD1580322. Continue frozen2254-file implementation candidate,
manifest c47909457f499bd3253f9b74d1806269e8b3226e791da2ed21b18fdba78124f5.
Preparation64933/parser74740/finalizer77836 are terminal0/absent, full archive and
patch roundtrip verified. Actual accumulated input provenance, root1034 and all
unrelated pending work remain protected. Use WORKFLOW and the implementation
[plan](APPLICATION_OBSERVED_STARTUP_PLAN.md)/[study](APPLICATION_OBSERVED_STARTUP.md).

## Required result

Validate resumable external observation acquisition in the complete startup caller,
not an isolated response queue. The same Host must keep actual fulfilled values
and resources through late rollback, with FILETIME after window responses. This is
necessary app startup work toward the full Naruto/Sasuke District match; full game
remains the goal. Reuse existing original-source comparisons without execution of
original code. EXE/DLL hashes, provenance and original stopped/unknown outcomes
remain exactly those pinned in the implementation plan.

Run the exact selected four new and21 retained complete methods in order. New whole
comparison retains23 complete/5 source stops/7 provenance outcomes,6325events,
119WAV650window replies, full globals/store order/masks/owned results/operations.
New failure and lifetime controls cover six late phases plus publication copy and
preparation rollback; typed controls cover17 families and cross-family rejection.
The clock method exercises integer boundaries and actual Darwin readings serviced
at whole-caller permits, retained after late failure. These are actual macOS clocks
with other APIs supplied as declared controls, not Windows/device equivalence.
All21 retained methods (prior11 plus window10) remain exact, including280 whole
window cases4574responses and prior whole WinMain/Host checks.

1. Revalidate writable X5 APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548, terminal
   jobs and root1034/prior2249/source55. Clone2254 files by body/mode/nsmtime and
   distinct inodes; preserve membership and all original candidate bytes.
2. Adapt the existing last validation's monitor/queue/package checker only for
   task identity,25-method phase/count domain and199Core/277test source counts.
   Preserve process identity/reap body and exact saved-result predicate verbatim.
   Use len(methods) for summary count; do not copy prior preparedMethods:8 error.
3. Fresh native-SwiftPM release build-tests/jobs2/enable-testing/disable-index-store
   in task-owned scratch/cache/config/tmp. Verify199Core/61Reference/277tests plus
   generated accessors/fresh outputs;1686 resource files385fixtures1301Core exact.
4. Each named test must exit0 with its one exact passed-method record, three
   one-test/zero-failure summaries, Selected-tests pass, no guard/signal/residual
   process. Stop at first nonpass; preserve it and diagnose before any correction.
5. Validate all saved terminal results and protected inputs, regular APFS full
   artifact archive and PAX metadata by names/bodies/modes/nsmtimes. Publish separate
   build/package/comparison/archive/independent-review gates, update handoff, commit.

## Bounds and ownership

Task application-observed-startup-validation-20260926 under existing X5 parent
01a0dc49-738f-7972-8fb0-e98fb2f34408. Require90GiB external/9GiB internal before
preparation. Retain40/6GiB reserves plus17GiB source commitment. Allow28GiB physical,
stop26GiB observed decrease,150GiB logical,metadata16MiB/root2MiB. APFS-dependent
work stays on X5; T7 remains separately authorized. Build<=3600s/RSS12GiB, four new
methods<=600s/RSS8GiB; retain each of21 old method limits exactly, including the
four old300s/4GiB controls. Queue<=14400s, preparation<=1800s, finalization<=3600s.
Poll1s/RSS2s/logical15s, bounds do not reset per subcase. Three-round correction
limit from implementation plan remains. No automatic rerun; keep every failure.
Revalidate PID/start/fullcommand/cwd/job before action; timeout/silence is not exit.

Only this new task/alias, adapted tools, plan/study/evidence and own navigation
insertions change. Root Native, exact candidate, old producers/plans/archives,
expected/masks and safety register remain immutable. No new original/emulator/
capture/auditor/affected refused operation, actual window/audio/input or Windows
execution. Source59727 stays terminal at34 Objects; full137 remains open. Actual
Mac clock IO is explicitly permitted by this plan outside Core transactions.

Independent review unavailable and open; author review is not independent. Root
promotion, physical backend/resource/window/callback/input/audio, nonthrowing
prepared inputs/aggregate WAV, initialized loading/Windows/clean-Mac/full match/game
remain open. EXE envelope not recalculated. After passing, continue physical
resource/window acquisition with concrete device boundaries; no inventory-only loop.
