# Bitmap surface loading and own front-resource continuation

Recover whole43ed10 and its actual4013d0 child from the pinned NTSD2.4 EXE,
VC80 and original PE/file bitmaps on Unicorn2.1.4. These create the surfaces
used by the first front menu. Previous constructor/resource corpora supplied
43ed10 results; retain them unchanged as those earlier boundary contracts.

Finite acceptance: execute resource and file/fallback image paths, ordinary
image/GetObject/CreateSurface/GetDC/GDI/COM failures and signed results, optional
pixel-format words and copier dimensions at declared valid pointer/structure
boundaries. All original paths/PE resource bytes remain pinned. Execute actual
CRT memset for the108-byte descriptor; preserve its instructions and stores.
Compare full globals, output dimensions, structures/masks, request/cleanup order,
returned bits, own surface/bitmap lifetimes and late native rollback. Unknown
output/local storage is not filled from expected snapshots; reject a required
unknown read explicitly and distinguish it from a successful source comparison.

Then continue fresh accepted own WinMain/loop/dispatcher chains from actual
4450ac through front resources and whole43ee50/43ed10/4013d0 to the existing
427089 settings-call boundary. Use original23 distinct embedded DIBs, own startup
device and independently issued wrapper/surface allocations. Include restore
and minimize, plus ordinary resource/create/color-key/allocation failures.
Preserve all parent bytes/events, same CPU/stack/resources, first allocations,
all wrapper bytes/masks, original metadata writes and pending-loop rollback.
No loader, constructor or copy helper is replaced with a successful result.
The4450ac allocation request remains a declared boundary with issued backing
or NULL; its CRT malloc body is not executed. Win32/COM APIs also remain
explicitly declared platform inputs.

Low-level tracing establishes stack/descriptor ownership, live output pointers,
allocation lifetimes and original ignored-error behavior. Normal SEH/cookie
instructions may execute; no control/protective storage corruption, bypass,
arbitrary fault continuation, original file mutation or external target occurs.
The graphics API observations do not claim actual Windows raster/device output;
CRT loader/NLS and other installed-library routes remain open. Preserve239 prior
fixture pins, raw then packaged isolated native acceptance, full byte/JSON/SHA/
blob/vendor/parent verification and six foreign worktree files. Use committed
5a98b5f native export plus owned overlays. No whole dispatcher/World/menu frame,
app window, match/all-content or clean-Mac completion follows from this stage.
