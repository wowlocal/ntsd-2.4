# Host loading regression completion

**2026-09-22: all42 selected methods are verified on the unchanged round3
binary:34 retained passes plus eight newly completed methods.**
Independent comparator/contract review, root promotion and actual app/device
acceptance remain open. Native comparison, package preservation and archive gates pass.

The [frozen revised plan](APPLICATION_HOST_LOADING_REGRESSIONS_PLAN.md) follows
the [round3 result and resource diagnosis](APPLICATION_HOST_LOADING_CORRECTION3.md).
Three correction rounds ended before this separate unchanged-binary continuation.
No new candidate/test change, compiler/linker, original execution or device
observation occurs here. Root author owns verification; review is not independent.
[Publication](../evidence/application-host-loading-regressions.json),
[closure](../evidence/application-host-loading-regressions-close.json) and
[documentation receipt](../evidence/application-host-loading-regressions-docs.json)
record separate gates. The historical partial results remain unchanged and are
superseded only for completion of this exact42-method selection.

## Contract and preservation

Base HEAD is `93df1ddce3ec9bdde4eb99c6a08221b216ad0e14`. The exact1037-file candidate,
62,536,136-byte binary and487 package files are read in place from the frozen
round3 task, with its original candidate cwd. All new commands/jobs/logs/TMPDIR
belong to `build/research/application-host-loading-regressions-20260922`.
Preparation PID47869 completed exit0 in7.328s, rechecking full candidate/package
bytes, root1034/source55 pins and all34 exact named passes and their actual exits.
Six cwd and13 phase-classification controls passed before Native execution; the
three predicates and monitor loop remain unchanged apart from the declared
artifact argument. These controls are synthetic classification, not new original
execution or a reproduction of a Darwin process transition.

Only whole methods35–42 run. The three LoadedCycle methods35–37 have12GiB sampled
RSS allowance; five initialized methods38–42 retain8GiB. Every method still has
600s, the queue3600s, and stops on first nonpass. This capacity choice follows
observed RSS guards and verified24GiB host capacity; it does not identify the
prior peak's cause or promise future availability. Neither the earlier8GiB guard
nor the600s timeout is relabeled. Code, comparisons, selectors, original expected
records/masks and all method bodies are unchanged, with no splitting or skipping.

## Results

All eight methods completed with the exact requested named result, three complete
zero-failure suite summaries and actual exit0. Queue PID47948 completed exit0 in
131.274s; test-process times total94.517s. The sampled peak was8,716,664,832 bytes.
The previously guarded whole cycle method passed in21.515s; its old guard remains
an incomplete historical outcome. No guard/signal or monitor error occurred in
this new queue. All processes were checked absent before closure.

| Required methods | Retained | New | Verified |
| --- | ---: | ---: | ---: |
| Earlier host/startup/menu/input/graphics/loop | 25 | 0 | 25 |
| Host loading/ticket/retry/prefix controls | 4 | 0 | 4 |
| Loaded menu | 5 | 0 | 5 |
| Loaded cycle | 0 | 3 | 3 |
| Initialized menu | 0 | 2 | 2 |
| Initialized loading | 0 | 3 | 3 |
| Total | 34 | 8 | 42 |

These are unique selected methods on one binary, not additional original cases,
branch coverage or whole-source acceptance. The original first-menu/input parents,
full state/masks, resources/aliases, ordered operations, graphics, numeric values
and rollback checks remain in the selected tests. The only test corrections are
the separately preserved fresh/cached wrapper assertion and Bool presence check;
no original expected value, mask, Core or fixture changed during these rounds.

## Remaining scope

All earlier assertion failures, process guards and the sampled formatting diagnosis
remain immutable. The final preservation check covers all2482 prior artifact files
and156 directories, with exact file bytes/modes/mtimes and directory membership.
The51-member metadata archive is409,600 bytes, SHA256
`dd72df4047308cff2faf078fec15644275bf25615d5baba3c4612d1ac77b5696`, with exact
bytes/modes/nanosecond PAX timestamps/membership verified. Finalizer PID48699
completed exit0 in20.104s and was subsequently confirmed absent. The external
task is frozen; observed decrease62,922,752 bytes remains within1GiB, including
concurrent source growth. Original40/6GiB reserves
and source commitments remain; the1GiB allowance includes concurrent source growth.
Short-process RSS0 means no sample. The source capture is independently revalidated
and never restarted by this task. At17:07:07 UTC it remained the same live
PID59727:2670 chunks,18 completed Objects,19 decoders,654717456 stores. This is
an in-progress observation with no whole catalog return, not a comparison result.

Controlled loading/menu composition does not establish actual window/input/audio,
whole Catalog53 source, another returned fourth selection iteration or a game.
The next permissible task is bounded read-only preflight of the
host's real loading ticket/platform through existing `LoadedMenu.Outcome.matchPrelude`
and `PendingMatchPrelude` consumers, with saved whole-caller and rollback evidence
selected before extending the host API. Independent review, root promotion, real
providers/timing/devices, later typed child retention, existing safety dependencies
and full-game/clean-Mac acceptance remain open.
