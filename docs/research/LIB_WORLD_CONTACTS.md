# Bundled library contact collection

`OriginalLibWorldContacts` implements both installed contact filters inside
the existing whole native collection pass. All10661 controlled original calls
match complete400-Actor/World bytes and masks, globals and3917 ordered events.
The7925 immutable pristine controls also reproduce unchanged through the
installed library. This stage collects candidate contacts; damage, full-match
behavior and the initialized library-enabled application join remain open.

## Reference and boundary

The [finite plan](LIB_WORLD_CONTACTS_PLAN.md) was recorded before capture.
References are the original NTSD2.4 EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c` and bundled
lib.dll SHA256`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
[Source tool](../../tools/oracle_lib_world_contacts.py) uses Unicorn2.1.4 and
the committed [research loader](../../tools/lib_runtime_loader.py). It applies
76HIGHLOW relocations to36000000 and executes all104 installer instruction
starts. All13 actual copies/62bytes reproduce the accepted preferred-base
installation after accounting for relocation. Two allocations,26 protection
requests and13 copies use declared successful API responses. This is not a
Windows loader/protection test or native runtime DLL loading.

Calls execute whole417400/ret12,417200/ret8,419380/ret4,4064d0 and the
41eed8..41eefb caller according to each declared input. All real rectangle,
RNG, abs and fusion/Actor-construction helpers retain their bodies and ABIs;
the existing constructor memset adapter remains declared. Four Object records,
bounded ITR/BDY allocations and400 Actors derive from explicit inputs and
fresh original a5/ramp constructors. Unwritten bytes retain their masks.
Native reconstructs its own corresponding records, never source after-state.

## Installed rules

4176ac→10001807 filters the defender's Object+6f8 category before the unchanged
invulnerability and team gates. Rejection advances to the next ITR, including
when more BDY records remain. Acceptance resumes4176cb.

| ITR kind | Category accepted by this hook |
| --- | --- |
|8,36,80–85,824|0|
|86–89,800,801,808,825|3|
|802–807,809|All; the category is still read and compared with0|
|810–816|1,2,4,6|
|817–823|1|
|Other values|No category read/filter in this hook|

The802–807/809 comparison has no conditional jump. Native retains its required
data read and does not invent a filter. The old invulnerability exception
continues to accept only kind8/14; new accepted categories do not create new
invulnerability exceptions. Later geometry, mode, nearest/multiple buffers,
RNG and fusion remain the common original rules.

4177b9→100011b9 reads the **attacker's current frame70 state**, independently
of the two entry collisionFrame7c references used for ITR/BDY. The original
pair establishes attacker slot at entrySP+4, defender at entrySP+8. After0x30
local bytes and four register pushes these are currentSP+0x44/+0x48. ESI is World,
EDI is defender slot; at the team hook EBX is the defender Actor. DLL's extra
EDX push makes its `[esp+48]` refer to the same attacker argument. Every hook
entry verifies these relationships from the actual enclosing417400 call;
native does not import stack words or register snapshots.

For a state other than20 the hook resumes4177ca and preserves the old team
check. State20 allows equal teams, an attacker with team0, or kind8 by jumping
417866. Otherwise it jumps41780b, directly into the old effect21/22 test,
bypassing the old requirement that attacker state equal18. Other effects pass
that section;21/22 still encounter the existing projectile/category exceptions.
The earlier defender state10/13 and sourceID212 gates remain earlier. Kind8's
test inside the hook is retained in source, although the ordinary earlier kind
dispatch bypasses this hook for kind8; instruction coverage is not branch-outcome
coverage. Both hooks preserve live EDX and the FPU environment at continuation.

## Finite evidence

Besides all7925 previous inputs, the source adds1872 kind/category/invulnerability/
rest cases,594 state20 cases,162 state3/18/21 controls,60 defender-state bypass
cases and48 whole collection/caller cases. Whole cases include both directions,
multiple ITR/BDY, aliases, later slots and real RNG. All10661 return normally;
there are no captured source memory faults or safety refusals in this corpus.

| Observation | Count |
| --- | ---: |
|Full pool bytes compared|4524613688|
|Full pool mask bytes compared|4524613688|
|Full globals bytes compared|491941184|
|Real helper returns|193615|
|Ordered RNG/reconstruction events|3917|
|Actual EXE instruction starts|1571|
|Actual DLL hook instruction starts|142|
|Type-hook entries|19216|
|Team-hook entries|13063|

All142 statically decoded starts in these two hooks execute. This does not
cover all branch outcomes or the remaining DLL functions. Type-hook
continuations:18047 to4176cb,1169 to417f59. Team-hook continuations:12541 to4177ca,
261 to417866,261 to41780b. All32279 checkpoints retain CW037f/FPSW/tag across
the hook, and every whole return has SP1000e000 and empty FPU stack. Saved
register/stack cleanup checks remain in the real helper observer. Hook metadata
also includes explicitly sampled Actor fields; those samples are not themselves
original memory-access instructions. Source FPU history is not native process
FPSW/tag or hardware equivalence.

Every expected pool/mask/global byte is retained in1924 content-addressed,
lossless blobs. Full Object and box backing remains unchanged on each source
call. The [independent verifier](../../tools/verify_lib_world_contacts.py)
checks all blobs, complete prior-case identity, installer relocation/copies,
stack/slot/Actor provenance and actual instruction inventories.

The [read-only catalog census](../../tools/inspect_lib_contacts_catalog.py)
verifies all137 original DAT hashes and accepted loaded Object/allocation
bytes/masks. Across54800 defined state words,15363 present frames and4384 live
ITR records, there is **no state20 and no newly added kind**. Kind8 occurs402
times. The new-rule inputs therefore remain synthetic. Later runtime mutation
is not disproven; do not infer that installed hooks or data provenance can be
skipped in the initialized application. Full results are in the
[catalog report](../evidence/lib-contacts-catalog-inventory.json).

## Native acceptance and publication

[OriginalLibWorldContacts](../../native/Sources/NTSDCore/OriginalLibWorldContacts.swift)
shares [OriginalWorldContacts](../../native/Sources/NTSDCore/OriginalWorldContacts.swift)
with an explicit library selection, defaultfalse for existing callers. The
public native caller commits World, Actors and globals together. Native-only
tests reject an undefined category even for otherwise unfiltered kind802 and
roll back after the kind80 path reaches real tie RNG. External effects must
remain buffered until the encompassing tick commits.

Raw acceptance:6 release tests passed14.567s/build175.45s; the10661-case library
test took8.426s and the7925-case pristine test6.137s. Both rollback trials and
retained pristine raw-memory/rollback tests pass. The525-file isolated native
export is0a7c7a6 plus exactly three owned files, excluding unfinished concurrent
transform work. NTSDNative linked; no application window was exercised.

Source raw48983626bytes SHA256
`b9ac56c5b7bfd33ae0ec73cbce521c6616065fb7ee7f326d75789eabaaa4a620`;
packed3074744bytes SHA256
`eca68d80be987ed91f3fbea635fa9c685aa463d26ecbf1c3b7e833c2186e3f16`.
Publication preserves204 old fixture hashes and adds one fixture,205 at
publication. [Acceptance](../../tools/accept_lib_world_contacts.py) reuses only
the verified terminal raw run with its log SHA, complete525-file manifest,
exact three current native file hashes and all executed source-module hashes.
[Artifact verification](../../tools/verify_lib_world_contacts_artifacts.py)
checks full restored raw bytes including newline, complete JSON, every blob,
all fixture pins and10 vendored codec files. Compression here is fixture
transport, not the game's native replay codec.

The first source attempt returned its first call but failed a new harness
FPU-tag assertion: inherited constructor/installer initialization left tagd555,
CW0 before the parent set CW037f. The new harness had not declared initial
tags. Its terminal failure/source/log hashes are preserved; the fresh complete
capture explicitly sets FPSW0/tagffff before each controlled whole call.
No original expected output or game instruction changed. The first independent
verifier treated the initialization fixture's instruction objects as integers;
correcting its field access left every source and expected byte unchanged.
There was no safety refusal, memory-fault capture or live-process restart.

Final packaged acceptance:8 release tests passed51.661s/build175.11s without
raw overrides. Both retained initialized pristine chains pass37.172s; library
controls pass8.375s and pristine controls6.114s. The526-file final export uses
the same0a7c7a6 base and exact three tested native files plus the new fixture,
excluding unfinished transforms. All owned source and SwiftPM jobs are terminal.
NTSDNative linked; there is no new app/window/device or Windows evidence.
Exact jobs, manifests, failures and log hashes are retained in
`build/research/lib-world-contacts-work.json`. The source partial checkpoint is
explicitly incomplete and is not a published acceptance fixture.

```sh
uv run tools/oracle_lib_world_contacts.py
python3 tools/verify_lib_world_contacts.py
swift test --package-path native -c release --filter 'Original(LibWorldContacts|WorldContacts|GameplayContacts)Tests'
python3 tools/inspect_lib_contacts_catalog.py
python3 tools/verify_lib_world_contacts_artifacts.py
```

The source producer refuses to overwrite its completed raw corpus. Reproduction
requires a fresh output location/workspace. Initialized library routing, other
installed movement/damage/transform/loading hooks, full-game content and match,
application output/input/audio, actual Windows and clean-macOS checks stay open.
