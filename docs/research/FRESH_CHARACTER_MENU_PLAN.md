# Fresh resources into the human character screen

Recover the declared menu tail4229cc through fresh music/eleven embedded bitmap
loads, the human character body429e5a..42b295/42e0b6, output and actual422ab8ret4.
This extends [FRESH_LIBRARY_MENU](FRESH_LIBRARY_MENU.md) to the actual consumer
of its newly owned CHARMENU, CMA/CM1..5 and RFACE records. Existing pristine
[CHARACTER_SCREEN](CHARACTER_SCREEN.md) remains a separate accepted contract.

Reference: pinned NTSD EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
bundled lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
VC80 CRT `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
and the eleven original embedded DIBs. Unicorn2.1.4 executes the installed
instructions at CW023f. Actual41bc90 prologue establishes ordinary cookie/SEH;
declared tail entry skips the earlier tick, as in its accepted parent. COM,
allocator, GDI, filesystem and timer results remain controlled research inputs.
No DLL/EXE/emulator enters native shipping runtime; this is not Windows/device,
an own initialized tick, the outer424746 loop or a full match.

The finite manifest covers both allocation patterns/address orders, menu3
initialization in modes0/1/2/4, menu1 first join/cancel/all-eight join, attack
priority, countdown drawing, signed counter wrap, negative GetDC, ordinary music
allocation failure, and the prior NULL-SPARK stop before its write. Every fresh
load resets selected entries to Random; no catalog navigation or selected
Object bytes are supplied. All400 declared Actor records and World bindings
are tracked. No arbitrary control-pointer/protection corruption or fault
continuation is part of this study.

Acceptance is finite:

1. Save every original store/read, helper return, event, full final record and
   intermediate checkpoint with immutable bytes/masks and source instruction
   bytes. Independently replay them, verify ownership and read provenance,
   installed-library patches and actual return ABI. Keep undefined bitmap
   fields distinguishable from defined bytes; recover any newly read storage
   before accepting it natively.
2. Share the pristine character rules with a library-text variant. Stage the
   match state, music, bitmap ownership, retained DC and external event buffer
   through the whole caller. Compare all declared World/Actor/global bytes and
   masks, each fresh bitmap/music record and exact event order.
3. Compare caller's five semantic counters at their actual checkpoints without
   importing private x86 scratch. NULL-SPARK is an explicit rollback rejection,
   never a successful source return. Native late observer failures must roll
   back body mutations as well as earlier fresh resources and output.
4. Retain both34-frame own pristine character corpora and accepted fresh-mode
   composition. Build from immutable accepted package plus owned edits only;
   preserve all254 old fixture pins and pending/foreign/live producer inputs.
   Publish only after raw and packaged acceptance and complete artifact checks.

The long guarded catalog and its one-shot audits continue independently. Do
not restart completed source captures or audits, or modify their frozen inputs.
Specific character selection, subsequent selection stages, own initialized
library chain, app input/audio/window, Windows and clean-Mac checks remain open.

## Findings that refine the planned labels

The captured menu1/menu3 entries execute the music gate and skip its child:
only current menu10 with previous menu!=10 requests main.wma. Both `null-wide`
cases leave that supplied allocator response unread. They are gate controls,
not allocation-failure coverage. No graph.log sprintf or music-wide allocation
occurs. The initial producer/runner scope text is retained verbatim in raw
artifacts; this finding governs every acceptance claim.

The cancel branch executes431c70 through431d0d and its actual CRT memset.
Two early captures stopped on missing observer allow-list entries, with five
atomic cases each preserved. Completed capture3 retained every state/event but
missed the reset-memset and three later ordinary cookie-helper return records.
Capture4 adds these eight observations total; independent comparison verifies
all prior case states, reads, writes, events and instruction bytes unchanged.
No rejected game fault or safety refusal occurred in these producer corrections.

Both bitmap backing patterns remain declared inputs. Ramp backing makes the
untouched count positive, so repeated frame−1 drawing also reads untouched
fields+fac/+7dc/+177c. These retain false masks and prior allocation bytes, as
in the accepted bitmap helper; no private heap provenance is inferred.
