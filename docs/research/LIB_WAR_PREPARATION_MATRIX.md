# War preparation: arena, participant and retained-resource success matrix

The finite source matrix and raw Native comparison pass256 whole outer calls,
236 War returns and56 preparations. It covers17 original arenas,99, both planned
Random outcomes,340 placements (168 CPU/172 human),619 arena bitmap constructors,
302 surface Release/free pairs and56 recordings/363533632 bytes. Final bundled
verification also passes. This accepts the finite success matrix; resource-error
contracts, full preparation and the full-game goal remain open.

## Behavior and evidence boundary

The pinned original NTSD EXE/lib.dll/VC80 and original DAT/BMP resources run in
Unicorn2.1.4/CW023f/C locale with declared normal platform APIs. The EXE SHA is
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c;
lib.dll is28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba;
VC80 is c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
Instruction, stack, memory and allocation traces recover whole43a21f..43a769,
the439e76 continuation and actual ret1c/ret4. They distinguish live selected
Objects from reconstructed participant slots and new resources from dead owners.
Synthetic allocation/COM boundaries are not Windows, host heap or device facts.
No EXE/DLL/emulator becomes part of the Native shipping runtime.

The [finite plan](LIB_WAR_PREPARATION_MATRIX_PLAN.md),
[initial inputs](LIB_WAR_PREPARATION_MATRIX_INPUTS.md),
[allocation correction](LIB_WAR_PREPARATION_MATRIX_LIFETIMES.md),
[Random correction](LIB_WAR_PREPARATION_MATRIX_RANDOM_INPUTS.md) and
[Native comparison plan](LIB_WAR_PREPARATION_MATRIX_NATIVE_PLAN.md) retain the
pre-execution contracts. All cases use ordinary declared configuration and
successful APIs. The controlled next-Start bridges do not represent played
battles or establish own full-startup/UI reachability.

## Original order and retained ownership

Fixed chains use actual live Random Object selection, then declare sparse
status0/3/13, teams1/2 and legal100..300 multipliers. The source and Native retain
those Object owners while constructing human slot=seat or CPU slot=seat+10.
Initial floating coordinates are CPU350/0/300 and human400/-50/300; final X is
100 or arenaWidth-100, Y0 and Z the actual RNG result plus the arena lower bound.
All680 additional numeric checkpoints compare full owned Native state. Regular
checkpoints retain their previous contract; no private stack/CPU ABI equivalence
is claimed. World activation clears before the dead team-assignment pass.

Each fixed chain loads its arena, selects99 to release it, then loads it again.
Dead wrappers, bytes and masks remain recorded. Only the first layer pointer is
cleared by release; stale tail pointers do not cause a second free. Sixty arena
wrappers are sufficient for two loads of the largest30-layer arena, following
the two live War resources. Native retains the same released-owner set and
surface-before-wrapper order, with external effects staged through outer return.

Twenty first preparations free the declared parent recording at26003000;
36 later preparations free a generated recording. All56 free-before-calloc
sequences and complete new buffers compare. Music produces56 exact-size wide
buffers/1604 bytes, including transitions to the main track; allocation patterns
and owned pointer generations are declared separately from real Windows heaps.
Input reset, naming,28 recording notices and44d034-before450bbc ordering survive.

## Preserved failures and corpus composition

Capture1 completed25 calls and stopped at the synthetic allocator's mistaken
30-wrapper cumulative limit. Its complete failure trace,25 parts and662 pins
remain unchanged. This was not an original memory fault or allocation failure.
Capture2 corrected the reserve, reproduced all25 parts and completed245 normal
returns. Its post-return assertion then rejected the first Random case: music
stream1 had already advanced the shared RNG, producing arena9 instead of the
declared0. The full case is retained as a failed expectation, not a successful
match to that expectation. All693 input pins stayed unchanged.

Capture3 retained234 completed fixed calls bytewise and executed only two fresh
11-call Random chains. Their ordinary-chain prefix reproduces ten old calls
exactly. Whole Start observes music stream1 before arena stream123; corrected
before-call index1/counter0 produces actual arena0/99. No RNG return or after-
state is inserted. The corpus contains20 original401-constructor parents and40
old resource/ready parent reproductions; those counts overlap other reproduction
counts and are not unique-startup counts. All943 capture3 pins stayed unchanged.
All original source jobs are terminal and must not be restarted for this matrix.
The retained oracle_lib_war_preparation_matrix.py main program belongs to the
historical capture2 plan. The accepted final assembly uses
oracle_lib_war_preparation_matrix_random.py and its corrected manifest2; it does
not reinterpret the historical failed arena expectation as a match.

The first full auditor stopped on an absent optional graphics field in a resource
parent. The second omitted the pre-existing parent recording from its free
filter. Frozen auditors, journals and corrections remain. Audit3 verifies all
6596 points/7125784 records/40878800788 bytes and masks,8625139 stores,
3608524 instruction reads,65425 API reads and105415 helper returns. It verifies
all6140 blobs, all256 atomic cases and4456 original instruction starts.
Of330 declared preparation starts,295 execute;28 dead team writes,three alignment
starts and four mode1 starts remain absent. This is not all branch outcomes.

## Native result and remaining work

Native uses the accepted catalog/War preparation implementation, adding separate
synchronous observations of the four numeric sites. The comparison adapter keeps
resource history and generation ownership, and applies only the declared inputs.
No game rule or original expected byte was changed to obtain the raw match.

The first test build used internal convenience methods from another module;
direct public record access corrected that test, without widening the core API.
Retained22 calls and12 prior rollback trials pass17.200s/build204.18s. The full
256-call raw comparison and six new source-coupled release/free/recording-free
rollback trials pass148.773s/build0.34s. Both jobs are terminal. The rollback
checks cover the whole coupled state, ownership and buffered effects.
They inject Native observer errors at events from successful source calls;
they do not claim equivalence to an original resource/API failure.

Raw2866445440 bytes have SHA256
79b14f3aaa971e5ef1d60eac6d72955c1f9c924fcc2c2948caa74de67d24495a.
Seven lossless transports total328654511 bytes. Independent transport verification
reconstructs every original raw byte, JSON value and all6140 blobs/615145016
decoded bytes. Final bundled Native execution passes two tests in206.977s after
a197.80s build, without raw overrides. The largest
case is26481646 bytes, within the existing32MB limit; metadata is184493027 bytes,
within its existing256MB limit. Packing only stores research data; it does not
replace game replay compression.

All318 old fixture hashes stay unchanged;325 current fixtures and759 Native
package files match the declared Git base plus11 owned code/fixture files.
All759 archive members were independently reread and checked. The archive is
2794788229 bytes, SHA256
b66bf18dea2d656581cf396d385dca676be7098da3d0bf04bf62548ad06cbfa7.
Twenty-three unrelated workspace files remain unchanged. All source, audit,
Native, packing and archive jobs are terminal. Evidence is retained in
[lib-war-preparation-matrix.json](../evidence/lib-war-preparation-matrix.json).

Ordinary resource/API failures and replay allocation failure require their own
finite source/Native contracts before full preparation acceptance. War gameplay,
own catalog/startup/outer-loop/app, played matches, all content/network and
Windows/window/input/audio/clean-Mac checks remain open. NTSDNative linked in
these builds; no app window or device was exercised. Current jobs and immutable
reviews are under build/research/lib-war-preparation and
build/research/application-catalog-work.json/libWarPreparation/matrix.
