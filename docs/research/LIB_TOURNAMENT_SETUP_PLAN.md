# Retained Tournament roster, draw and settings

Recover mode2's actual432ab0 dispatcher, menu20 initialization, eight fighter
and Human/Computer choices, renumbering, confirmation, eleven shuffle passes,
live Random choices and menu25 settings. The normal mode screen selects menu20;
it does not enter the shared menu3 character selector. Team Tournament434ab0
and War remain separate dependencies.

Use pinned NTSD EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
original DAT/head/small BMP and menu DIBs in controlled Unicorn2.1.4/CW023f.
Each call executes the actual41bc90 prologue then declared4229cc menu tail,
fresh/cached resources, real installed library text and actual422ab8ret4.
Start stops BEFORE4338c3, the first write entering tournament bracket menu26.
The preceding attack flag clear and sound execute. No bracket, match prelude,
whole initialized library startup, Windows or device equivalence is claimed.

Read-only disassembly and complete reads/stores/stack/register/helper traces
recover input priority, same-call phase changes, UI ordering and RNG ownership.
Two finite retained input sequences are written before execution. Initial
menu20, mode2, blink phase0/29, unlock0/1, and CRT seeds17/ffffffff are declared
controlled inputs. Reuse the five declared synthetic atlas operand records;
45116c aliases the existing atlas451178. This tests original drawing requests
and clipping against declared geometry, not original atlas pixels. Eleven
new menu resources still execute their actual constructors. Add Object+728 and
42 small portrait operands from raw DAT/BMP files; native operands must come
from its independently loaded catalog. The mutable one-character controller
label at44d31c starts with the exact two file-backed EXE bytes7800, including
its actual NUL terminator. Unknown stack bytes remain unknown.

After each real return only ordinary button bytes and three ABI words change;
all previous World/400Actor/catalog/globals/resource/DC/stack bytes and masks
remain live. Primary selects Naruto and Sasuke as humans with six Random CPUs;
control uses eight Random CPUs. Exercise held input, Random/first/last roster,
controller toggles and backtracking, confirmation cancel, all eleven shuffle
passes, randomization after excluding all eight live choices, reroll, reselect,
arena100/99/District, three difficulty values, Music OFF/Random/Main, settings
back and quit. Finish primary at Start and control at an actual quit return.
The three selectable difficulties are a cycle2/1/0; existing-1 display is not
an ordinarily selectable tournament difficulty and remains a separate branch.

Acceptance: every finite result and complete record/mask/event comparison;
independent replay of all stores, reads, checkpoints and instruction bytes;
ordinary helper return ABI; all f7/f8 shuffle draws and f9 candidate lists
reconstructed from preceding state; raw small-portrait inputs compared with
the unchanged accepted catalog. Whole-current-call rollback after a late
shuffle draw and a late controller-label output must preserve earlier calls.
Any shared Native dispatcher edits require retained Stage and VS regression.
Use a new isolated copy of accepted668-file Stage package; preserve267 fixtures,
all frozen manifests,14 pending Native/3 pending source/6 foreign files.

Large research data and isolated builds use the task-owned X5 directory via
build/research/lib-tournament-setup; shipping fixtures stay regular local files.
Catalog5 Python78863 remains independent; revalidate it and never restart for
silence or change its6GiB internal-parent reserve. Preserve actual errors and
refusals with operation/time/session. No deliberate pointer/protection damage,
arbitrary post-fault exploration or blocked-operation retry. Full native game,
all content/network, Windows/window/device/clean-Mac acceptance remains open.
