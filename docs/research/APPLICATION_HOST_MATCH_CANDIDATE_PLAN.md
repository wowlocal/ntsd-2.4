# Host Start/launch candidate — frozen implementation plan, 2026-09-22

Follow [WORKFLOW](WORKFLOW.md), [TASK_TEMPLATE](TASK_TEMPLATE.md) and the accepted
[read-only preflight](APPLICATION_HOST_MATCH_PREFLIGHT.md). Base HEAD:
`a8725aa28491256eaa9efc4307a22c1a8bd68b64`. Implement typed host retention from
actual Start through the existing launch child and final outer publication.
This advances native startup-to-match ownership; the complete game remains open.

Actual baseline is the frozen1037-file round3 host candidate, with its42-method
validation. Root1034 remains live-source pinned. Reference EXE SHA-256 is
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
Use the preflight's pinned original corpora/DLL/resources and current comparators.
No original execution, historical producer/auditor rerun, device IO, dependency
install, root promotion or refused operation is authorized by this phase.

## Finite implementation and controls

Implement the preflight's four API/storage steps in OriginalApplicationHostSession:
mutually exclusive prepared returned/Start states, typed preparation, retained
Start resume, and unchanged atomic outer completion. Keep exact loading-attempt
checks, all old API behavior, independent platform copies, stage/reentry guards,
rollback and prior batch ownership. No gameplay/numeric/resource rule changes.

Test-only factoring may add OriginalApplicationLoadedTestDriver and change the
two character/selection chain runners to transport their existing complete
comparisons through either Bootstrap or the actual host. Preserve the original
default route and all source comparisons/late-failure methods. No manufactured
Start or snapshot seeds a natural host chain. Keep launch comparison unchanged.
Add OriginalApplicationHostMatchLaunchTests with the three names and complete
success/late-failure/ticket/reentry/copy controls selected in the preflight.

Freeze53 exact methods from its observation receipt:50 current methods plus3 new.
Both bitmap backings reach real Start, then15 District surfaces, enabled music,
full replay initialization, returned child, enclosing tail and next input boundary.
Do not claim the next input as a returned gameplay tick. Preserve own RNG/table,
library state, resource generations, full records/masks and operation/graphics
order; old source values are expected comparisons only.

## Storage and phase boundary

Task `/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-match-candidate-20260922`,
alias `build/research/application-host-match-candidate-20260922`. Verify APFS UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`,58GiB external/7GiB internal at start.
Retain original40GiB/6GiB reserves and source17GiB gross budget. Candidate-only
allowance1GiB observed decrease including concurrent source; stop at896MiB and
retain128MiB for closure. Use verified APFS clones with distinct regular inodes,
no build/cache copy. Preserve all old tasks and inputs.

Reuse existing preparation/pin/job/patch/archive procedures. Record PID/start/
command/cwd and terminal status for preparation and the changed-file Swift parse.
Parse only using the installed toolchain and minimal environment,300s limit;
this phase does not typecheck, link or run XCTest. Preserve any failed draft and
diagnostic before correcting it; three rounds before contract diagnosis. A later
fresh compilation/53-method/package/archive task gets its own limits, informed
by the prior whole-parent RSS/timeout observations. Old binary passes cannot
validate this changed Core.

Mutable root paths: this plan, APPLICATION_HOST_MATCH_CANDIDATE.md,
docs/evidence/application-host-match-candidate*, two preparation/finalization
helpers and own CURRENT_WORK/RESEARCH_MAP additions. Five candidate Swift paths
are authorized: HostSession; LoadedCharacterTests; LoadedSelectionTests; new
LoadedTestDriver and HostMatchLaunchTests. Keep all other candidate files,
root Native, fixtures/resources/expected/masks, source55 pins, archive and
unrelated dirty files unchanged. Revalidate source identity/job; never restart
or signal it for silence. Existing safety incidents remain open.

Root is author; independent review is unavailable and stays open. Acceptance for
this implementation increment: syntax result, complete input/body/mode/membership
checks, exact53-method inventory, preserved comparison statements, reviewable
patch round-trip and changed-file/metadata archive readback. Freeze external
candidate and commit the coherent result with explicit uncompiled/untested status.
Native comparison, independent review, root promotion, devices and full game are
separate unfinished gates. Existing Git hooks remain enabled.
