# War setup — unfinished work, 2026-09-11

The [finite plan](LIB_WAR_SETUP_PLAN.md) and [input correction](LIB_WAR_SETUP_INPUTS.md)
are retained. Source capture2 completed all574 declared calls and exited0:
573 returns, including two resource-parent returns, and one BEFORE43a21f.
The final raw document has3456631996 bytes and SHA256
`9b7e923c7f43ef328c4e7760ae5633589d83ce2bcbef430258f8d8f53a40b1aa`.
The source process is terminal; do not repeat it.

Probe3 independently verified four calls/83 points/88813 records/
233445114 bytes,780 helper returns and eight ready participant bindings at
438bbb. Its raw SHA256 is
`42a78cea14df494bde5c8120c3687a5179b051161ab2eb9e4e4dd540781ed44a`.
The whole574-case audit has not run. No whole Native War menu match is claimed.

X5 disappeared after source completion and during the pending dependency build.
`diskutil list external physical` returned no disk, and the exact UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548 could not be found. Source and Swift
processes were revalidated as terminal; no process was restarted. The build
wrapper exited1 while hashing an unavailable input file; its actual Swift
result is unavailable locally. This was a storage interruption, not a refusal.

`build/research/lib-war-storage-interruption/` retains the interruption, exact
source completion/hash, pending-file archives and work records. Its small
`core-compile` package uses accepted Git cd80865 Core/codec plus two pending
Core files and their comparator, with a minimal Swift5/macOS14 manifest.
Core and tests compile. Removing an unnecessary `@testable` import corrected
the first compile-only test error without changing game algorithms.
One Native-only late frame-observer rollback test passed0.001s/build77.65s.
This is no original numeric/frame comparison, whole menu or application check.
All266 literal words match the pinned EXE's1064 bytes. All295 accepted
fixtures match Git bytes;6 foreign/14 pending Native/3 pending source files
remain unchanged. Existing upstream compiler warnings were not modified.

The [pending Native implementation](LIB_WAR_SETUP_NATIVE_PENDING.md) now adds
the whole War menu, bitmap ownership, surface-loading composition and three
shared dispatcher hooks. A Native-only24-Object test completed an ordinary
menu return and six late rollback trials in0.059s/build75.04s. It checks staged
World/Actors/globals/resources/library text/events, without source/device output.
The first whole compile needed three `try` annotations; an earlier rollback
trial rejected zero in its synthetic RNG table. Errors remain in separate logs.
The original producer, frozen plan/input correction and expected bytes are unchanged.
All current jobs are terminal. Full source store/preset/rectangle/menu/output
comparisons and the outer output rollback case still await the preserved corpus.
The whole casewise comparator and War surface adapter now compile, including
eight declared source-coupled late rollback trials. They remain unexecuted until
the preserved574 cases and full audit are available. The index tool requires
the exact raw hash and completed full audit before accepting atomic parts.
A9,437,389-byte synthetic stream-reader probe passes cross-chunk decoding and
truncated-input rejection; it is not a source audit. The Native-only six-trial
test passed again0.061s/build152.06s with the new before-resource observer.
Accepted reference checks were copied from Git, excluding pending catalog edits.
Six retained packaged menu/rollback tests pass185.895s/build15.44s:373 Team
Tournament setup,373 Tournament setup and252 selection calls remain consistent
through the shared dispatcher. Session37523/Swift39833 is terminal0. All263
small-package inputs are pinned; the20 owned pending files form an explicitly
unfinished snapshot. No new War source comparison or application-window claim
follows from those retained checks.
The accepted milestone remains cd80865; this work is unfinished.

After X5 returns, verify its exact UUID, source2 raw size/SHA and574 atomic
parts before using them. Reconcile existing job records with actual processes.
Do not replace canonical symlinks, lower storage reserves, restart source2 or
the completed catalog5, or treat incomplete observations as acceptance.
Run the full read/store/mask/point/REP/Random audit, derive explicit dependency
inputs, run the whole Native comparator and verify the menu/shared
dispatcher with retained tests and packaging. Start preparation43a21f,
War gameplay43a860, own catalog/library/outer loop/app, full match/content/
network and Windows/window/input/audio/clean-Mac stay open.
