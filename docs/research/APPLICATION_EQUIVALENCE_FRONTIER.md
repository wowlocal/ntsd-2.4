# Application correspondence frontier after active output

Status: saved-evidence dependency analysis; no new Native or whole-application
acceptance. Parent `28a89d596f893db83f77d8a3b8a3bc68fbba9ae9`,
[accepted active output](APPLICATION_ACTIVE_OUTPUT.md).
[Evidence](../evidence/application-equivalence-frontier.json) pins the inventory,
inputs and independent review. The complete [game goal](../CONTINUE_GOAL.md)
remains unchanged. The next implementation is the application pause/step/resume
continuation, not another active48 recapture or relabelled full-game result.

## What the current comparisons establish

The own Bootstrap starts with its declared PE-backed bytes and platform inputs,
then retains catalog, selection, match preparation, input,19 gameplay stages
and actual Native outer returns. Independent calculations compare current
inputs, complete available records/masks, resource owners and event/graphics
journals. This is meaningful finite Native composition. It is not a single
original application execution with identical initialization and installed DLL.

| Dependency | Established evidence | Remaining requirement |
| --- | --- | --- |
| Initialization and RNG | [Bootstrap](APPLICATION_BOOTSTRAP.md) owns its startup, clocks and RNG; [selection](APPLICATION_LOADED_SELECTION.md) projects the actual table and draw order | A same-input source/application trajectory must reproduce its own initialization, rather than substitute a pristine table into Native |
| Selection and preparation | Own inactive selections36/37/32/41/18/31 and stage5 music differ from pristine24/38/39/32/22/36 and boss2; [launch](APPLICATION_LOADED_LAUNCH.md) preserves own draws and installed human positions | These are different declared inputs/branches, not interchangeable expected after-states; current checks do not prove cross-environment final equality |
| Installed library | Shared library contracts and current finite19 consumers are compared; [patch overlap](ACTIVE_LIB_IMPACT.md) identifies five affected sites in pristine active inventories | A full source startup-to-match run with actual installation and its consumers is absent; inventory intersections do not establish DLL branch outcomes |
| Available storage | Full declared active endpoints, masks, owners and journal compare; Frame storage survives Native calls | Source middle Object/Frame snapshots remain missing. Enclosing snapshots cannot prove absence of interior writes or seed an earlier Native state |
| Caller details | Actual saved source returns and selected helpers/FPU observations are checked | Full Native ABI/FPU/instruction-store correspondence and private formatter/continuation observability are not established |
| Paused consumer | [Paused gameplay](PAUSED_GAMEPLAY.md) has two complete saved14-call schedules and shared Native pause rules | The current application session accepts only gameplay; its pause body lacks installed text-state plumbing |
| Product integration | Recovered Core owns typed requests/transactions; [MeleeScene](../../native/Sources/NTSDApp/MeleeScene.swift) still uses OriginalMelee | Connect the recovered engine to the actual app; preserve original pixel/input/audio behavior using established device contracts |
| Full game and environments | Current finite neutral/active schedules and controlled dependencies remain accepted | Damaging fight/round/full match, remaining content/modes/AI/network/playback and actual Windows/device/clean-Mac criteria remain open |

The historical ACTIVE_LIB_IMPACT NEXT predates accepted shared handlers and
finite19 comparisons. Those later results close their named Native joins; they
do not create the absent full installed source trajectory. Existing safety
incidents retain their separate unresolved dependencies. This analysis does not
retry or authorize a refused operation. EXE envelope was not recalculated.

## First ready consumer: application pause, step and resume

The retained paused fixtures are `original-paused-gameplay.json` and its control,
SHA256 `0c2e53bbe82ab643161f0f336142920d9eac3f56f0852ec7c63bd97c0ef51889` and
`be991d1fe0001ffbe5098da526782034d66791e47a225470a497c8e560b7094c`.
They continue their complete sixteen-call neutral parents. The own equivalent
entry is the actual `OriginalApplicationGameplayProjectionTests.sequence`
`onComplete` application, after its separately compared first+16 neutral calls.
Do not label this a direct source continuation of active48.

The new inventory reads all28 saved calls, resolves2861/2862 blobs and107
components per fixture, verifies each original JSON against its packed payload
plus the documented final newline, and preserves separate raw/transport hashes.
The source is the pinned EXE/VC80 in the original declared Unicorn environment;
no original, Native, historical producer or auditor is executed by this inventory.

Each schedule acquires F1 at call1, F2 at5 and F1 at11. Other calls request no
held pause keys. These are direct source keyboard-table writes, not captured
WndProc/OS events. There are only three changed source key bytes per schedule:
the input handler already consumes each pressed key. Native message delivery
must be independently declared and compared through WndProc, including its MSG,
loop counter and recognizers; source writes do not choose Native after-state.

The first two calls run unpaused; calls3–6 pause;7–8 single-step;9–12 pause;
13–14 resume. Cached pause, phase and both latches are reconstructed through the
prologue from each retained input. Replay and elapsed counters are
18,19,19,19,19,19,20,21,21,21,21,21,22,23. Paused local input skips its helper
and pre-dispatch checkpoint, yielding five input checkpoints versus six unpaused.

Each paused body has background41d74d, drawing41d762, HUD41d76a,
pauseBitmap41d78b and indicators422994 checkpoints before whole output. All28
calls return to30000000/SP1000f42c. The output reference set has seven components;
its enclosing return additionally has Frame heap. Unlike active48, this pause
corpus has no whole Object/objectStrings snapshots at these returns. Keep the
accepted catalog provenance and the missing evidence explicit.

## Implementation contract for the next card

1. Resume the existing `PendingContinuation` with `.pausedRendering` directly.
   Calling `OriginalLoadedGameplayCall.run` here would repeat already completed
   input/round work. Keep the same Bootstrap ticket until its outer tail commits.
2. Extend the shared paused body and application adapter to retain installed
   text/DC state through layout/output and expose its actual checkpoint state.
   Pristine calls must preserve their existing behavior. Retained hit/transform,
   catalog, replay, sound and graphics owners must survive the paused path.
3. Draw the background directly without the active camera pass. Preserve
   `450bb8/450bc0` through `drawPreservingCommands`, draw PAUSE at360/288, then
   indicators and complete output. No simulation stages run while cached paused.
4. Independently derive expectations from current input. The active-only
   projection cannot be broadened by removing a guard: notice1 still draws in
   pause, but its timer does not increase. The inventory checks that difference.
5. Declare F1/F2 message delivery and input phase/latch behavior before execution.
   Check the outer KeyScan/diagnostics domain, full input state and replay
   counters; never inject pause flags, RNG, Actor state or expected backing.
6. Compare both14-call schedules, all paused endpoints/events/masks, installed
   text, complete returned owners and graphics/audio journal. Retain six
   unpaused calls, the accepted active/neutral regressions and old pause controls.
   Add late output/inner/outer commit failures and same-ticket retry preserving
   earlier acquired input. External device effects remain buffered until commit.

The implementation card must freeze actual files, exact selected methods,
platform replies, storage limits and independent candidate review before Native.
Its acceptance will be finite application composition under declared inputs;
it must not claim a new installed original trace, real devices or a complete match.
No new capture is required to begin this bounded integration.

## Analysis provenance and preservation

Work: `build/research/application-equivalence-frontier-20260914/`, verified X5
UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. Plan1 limits the reader to28calls,
300seconds and1MiB new evidence, retaining40GiB external/6GiB internal reserves.
Root owns this analysis and navigation; war_contract_review independently checks
its contract and claims. All1024 Native inputs remain byte/mode-identical to the
accepted output manifest, including383 fixtures and102 resources. No code or
reference mask changed and no prior terminal capture/test was restarted.

Reader1 stopped on an incorrect raw/transport hash equality before examining
call behavior. Reader2 applies the already documented exact newline relation,
compares the complete saved original bytes and JSON, and passes. Both scripts,
the exact failure and correction are preserved. This reader result proves the
listed saved-data facts and guides implementation; it is not a new Native test.
