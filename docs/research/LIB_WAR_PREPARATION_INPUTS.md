# War preparation: declared recording inputs after the ready parent

The frozen [22-call preflight](LIB_WAR_PREPARATION_PARENT_PLAN.md) completed in
capture3:22 outer returns,20 War returns and two whole preparations. Its raw
191867336 bytes have SHA256
`1c47170deeeb395042fe02042cb016d99f6e6bb378d1f998eae96b627cb079e2`.
The complete source audit reconstructs444 checkpoints/475960 records/
1359294944 bytes and masks. This does **not** establish Native preparation.
Capture3 remains immutable and must not be restarted or replaced.

Audit1 stopped at43d313 because45842c had no declared input provenance. Audit2
finishes every byte/mask/read/return while retaining finite acceptance=false.
It identifies six unbound recording inputs in both preparations:45842c,
450b94,450b90 and the first NUL bytes at44fd18/44f900/44f890. The recording
notice branch also reads unbound450be4. Actual zero bytes with unknown masks
are not known Native inputs. Source execution returned normally; these are
provenance boundaries, not source memory faults or successful Native matches.

Use a separate22-call bound-input corpus. Preserve the four exact resource/ready
parent calls from source2, then explicitly supply the following ordinary caller
configuration before the third call of each chain, alongside its settings key
edge. These are independent test inputs, not values imported from capture3.

| Input | Primary | Control | Purpose |
| --- | --- | --- | --- |
|450b90|1|1|Declared phase copied to replay+14b4|
|450b94|0|0|Declared global parameter copied to replay+4|
|45842c|0|0|Declared input-control flag copied to replay+0c|
|450be4|0|1|Recording-notice input, both ordinary branches|
|44fd18|`War preservation`|`War preservation`|Replay information string|
|44f900|`Controlled reference`|`Controlled reference`|Replay author string|
|44f890|`NTSD 2.4`|`NTSD 2.4`|Declared version text in replay metadata|

These addresses are ordinary game state and strings, not control pointers,
cookies or protection structures. Keep them as explicitly controlled inputs;
this correction does not recover their full application startup/setting-load
lifetime. Existing Team/Tournament preparation already declares corresponding
recording inputs; no naming or default is inferred from their raw zero values.

The first two calls of each chain and original401-constructor parents must
still reproduce source2 exactly. Later calls retain every produced state except
this one documented input bridge. They cannot be claimed byte-identical to the
ten earlier unbound-prefix calls. Keep those ten successful reproductions and
both complete unbound preparations as separate evidence.

The input bridge records its exact writes before the third call. The auditor
must reconstruct the new before-state from the previous return plus these
specified writes, normal key stimulus and call-frame words. Require all original
recording operands and the450be4 branch input to be defined, complete22 calls,
whole records/masks, resource/music/replay ownership and actual ret1c/ret4.
Preserve the source3 unknown masks and its failed/complete audit records.

References, controlled Unicorn2.1.4/CW023f environment, original resources,
normal declared APIs, no-fault-continuation boundary and6GiB reserve remain as
in the parent plan. Capture1's nested `graph.log` observer error and capture2's
26-byte music allocation assumption remain preserved. Separate exact-size
music storage supplies the actual30-byte stage-track request without changing
the earlier26-byte reservation or original allocation instructions.

Freeze a new manifest/producer/input-pin set before this new capture. Native
preparation, the full finite arena/participant/recording matrix, late rollback,
War gameplay, own initialization/app and Windows/device/full-game checks stay open.
