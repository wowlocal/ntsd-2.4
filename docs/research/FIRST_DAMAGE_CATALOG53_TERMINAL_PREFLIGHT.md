# Catalog53 terminal publication — metadata verified, whole return missing

2026-09-26. The [bounded preflight plan](FIRST_DAMAGE_CATALOG53_TERMINAL_PREFLIGHT_PLAN.md)
has been executed against the saved terminal publication. Its249,780,969 bytes
match SHA256 `010c6cfec0280e501954cf2c678a08de620022d9eceedf73a6b94b90071c6232`.
All nine trace manifests have internally consistent metadata, and every referenced
regular file is present with its declared size. **Trace/blob bodies, state/read
provenance, complete input consumption and Native equivalence are not verified
by this metadata result.** [Evidence](../evidence/first-damage-catalog53-terminal-preflight.json).

## Analysis and observed boundary

The existing source59727 is terminal0/absent, ended2026-09-23T00:10:17.364994Z.
Its outcome is researchBoundary/end dependency, caused by researchPublicationLimit
with reason logicalCap during streaming. The source was not restarted or resumed.
No original instructions, emulator, Native test or device operation executed in
this preflight. The running host comparison is a separate retained process.

The catalog entered at PC4450ac/SP1000e430/CW023f and stopped at
PC414993/SP1000db14/CW023f. The terminal state records1,305,182,059 writes and
record-table snapshot1623. It is not the required whole-caller return41c018.
Declared used15,024,171,029 plus prospective8,417,857 bytes would exceed the
14GiB stream cap15,032,385,536; the terminal publication allowance preserved the
boundary and final metadata. This is a research resource boundary, not a game
memory fault, Native rejection or successful catalog load.

The recorded scope is34 Object returns and34 decoder returns,35 allocated Objects,
9,075 Frame allocations,302 bitmaps,139 FILE records and267 registered WAV records.
The next Object and `chars\\kisame.dat` decoder remain pending. There are no
catalog child returns, and no pending catalog child. These are exact metadata
counts, not a new semantic audit of every return, allocation or WAV contract.
The complete source plan requires137 Objects and input exhaustion; the terminal
catalog53 metadata has no consumedInputs field. Remaining input journals are not
guessed from record counts. The actual partial requests/replies need the separate
provenance audit before any input-prefix comparison claim.

The original module hashes agree with the declared EXE/lib/VC80 reference. The
capture retains its initialized parent, synthetic mappings/API/FILE/PCM/surface
identities, ABI/FPU and unknown-mask boundaries. This does not prove Windows
behavior or permit importing source after-state into Native. Existing incidents
and the earlier catalog5/first-checkpoint results retain their original scope.

## Metadata and storage inventory

Nine complete-encoding manifests declare80,296 unique part references with no
duplicates, gaps or inconsistent counts under the existing TraceReader constructor.
No iterator was run. They declare4,013,950,953 rows,326,092,501,942 raw bytes and
14,966,193,823 packed bytes. Per-part metadata meets the unchanged200,000,000-byte/
50,000-row limits. Body hash, compression EOF, decoded JSON and row contents
remain unchecked.

The trace directory contains80,305 regular files totaling14,988,031,020 bytes.
Every referenced part exists with its declared packed size; there are no missing
or wrongly sized/nonregular parts. The nine remaining files are group index JSONs,
21,837,197 bytes together. Their bodies have not yet been compared with the
terminal publication's authoritative manifests. File-inventory digest:
`5677274aaf6b1827429b55601345582ca126ad719ec7fd13dc1263ae09c3cb6a`.
No packed part body was hashed in this pass.

The27,852 inner blobs declare1,891,246,796 raw bytes and contain164,815,528 base64
characters. Their schema fields are count/deflate/sha256. No base64 or DEFLATE
payload was decoded, and their content-address hashes remain unverified.
The evidence preserves all nine per-group totals and the complete terminal shape;
list endpoint schemas are navigation aids, not sampled evidence of whole lists.

## Execution, preservation and publication

Reader38330 exited0 in13.735s, with observed peak RSS1,047,379,968 bytes, below
600s/4GiB. Exactly two reads of the outer source JSON were used: streaming pin,
then strict duplicate-key/nonfinite-rejecting JSON parse with a repeated byte hash.
Its file identity/size/mtime remained stable. No large copies or decoded files
were created. The265,448-byte inventory is separate from the original publication.

All55 source code pins, protected plans/tools/incident register/immutable AGENTS
archive, root1034 and live host candidate1040 files were checked unchanged before
and after. X5 APFS UUID and original40GiB/6GiB reserves were verified, retaining
the source17GiB and host24GiB commitments. The metadata archive gate and exact
owned output/space observations are recorded in the publication receipt. No
source artifacts were deleted, changed, thinned or excluded.

The first console inspection printed too much of the metadata outline and was
truncated by the tool output limit; the complete inventory file was unaffected.
A subsequent bounded view selected terminal fields and all nine group totals.
This was an output-display limitation, not missing source evidence or a failed
audit. Independent review remains unavailable; this is an author preflight.

## Next required gate

First finish or diagnose the same host69-method queue and its archive gates.
The next independent source task is a separately frozen **complete saved-data
transport audit** of all80,296 parts,27,852 blobs and nine group indexes using the
existing strict reader/decoder. It must verify packed/raw lengths and hashes,
compression EOF, exact row counts/order and index-to-terminal-manifest equality,
without decoded output files. Minimum declared decoded work is327,983,748,738
bytes (trace and blob totals); the first checkpoint's7.58GB budget cannot cover it.
Use a finite proposed340GiB decoded/100,000-part/4GiB RSS/7,200s budget in its new
plan, with <=64MiB metadata and retained reserves. These are proposed limits, not
a launched job or a measured runtime. A timeout must preserve partial audit
coverage; it does not authorize a source restart or acceptance of a subset.

Then adapt the existing provenance, snapshot and read auditors for this exact
34-return/pending35 boundary, preserving all earlier predicates and expected
bytes. Full parent/input/instruction provenance, every state/mask and all ordered
reads/writes require those separate checks; transport alone is insufficient.
Even successful audit/archive of this partial source cannot close the missing
137-Object whole return. The source-completion dependency, installed trajectory,
independent review, app/Windows/device/clean-Mac checks, first complete match and
full game remain open. No replacement capture is authorized by this preflight.
