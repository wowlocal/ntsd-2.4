# War success matrix: Native comparison plan

Compare the frozen256-call/56-preparation source matrix after its independent
full source audit. Its [Random correction](LIB_WAR_PREPARATION_MATRIX_RANDOM_INPUTS.md)
retains234 complete fixed calls and executes only22 fresh Random calls, including
ten exact prior prefix reproductions. The [input plan](LIB_WAR_PREPARATION_MATRIX_INPUTS.md) and
[reserve correction](LIB_WAR_PREPARATION_MATRIX_LIFETIMES.md) retain the exact
NTSD EXE/lib/VC80/resources, Unicorn2.1.4/CW023f/C locale and synthetic API
boundary. Instruction/memory/lifetime traces establish participant order and
arena/replay ownership; Native compares its independently produced owned state,
not private stack bytes or per-instruction hardware FPU status.

Reuse the accepted bound preflight comparison and independent137-Object/101-BG
catalog reconstruction. Apply only declared before-call settings, participant,
metadata and RNG inputs. Preserve each actual Native Random Object choice.
Add separate synchronous numeric observations immediately after the initial and
final floating coordinate writes; compare all680 full owned-state checkpoints.
The existing regular checkpoint contract remains unchanged for the22-call
preflight. No additional observation changes game arithmetic or source results.

Keep device counters, prior arena wrappers and freed records through all three
preparations of each fixed chain. Check whole bitmap bytes/masks, constructor
history, Release then free, and Native released-set ownership. Derive new wrapper
and replay addresses from declared generation/allocation rules. Native retains
old replay buffers as dead allocations for full byte/mask comparison; the game
replay pointer must select the new live recording. Platform buffers use declared
allocation patterns and sizes; raw BMP/DIB parsing generates returned metadata.

Run all256 outer calls/236 War returns/56 preparations and20 original constructor
parents,340 placements and56 complete recordings. Exercise six new staged
rollback trials at source-coupled release, wrapper free and old recording free
in the first two load/99 chains. Each must reach its named event and leave the
whole Native state, resource ownership and buffered external effects unchanged.
Retain the earlier12 first-preparation rollback trials and bound22 comparison.

Freeze raw source/audit/index and isolated Native package pins before testing.
All prior318 fixture bytes remain unchanged. Package the accepted source only
losslessly, verify complete reconstructed raw bytes/JSON/SHA and every blob, then
run the same Native matrix and preflight from bundled fixtures without raw
overrides. Preserve compile/comparison errors and all source expectations.
Ordinary resource-error contracts, War gameplay, own startup/outer loop/app,
played matches, full content/network and Windows/window/input/audio/clean-Mac
checks remain open. The matrix alone does not accept the full preparation goal.
