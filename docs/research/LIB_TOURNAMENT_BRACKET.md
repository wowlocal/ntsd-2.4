# Tournament bracket, seat assignment and winner output

The finite study continues [setup](LIB_TOURNAMENT_SETUP.md) past4338c3 through
menu26..29 of432ab0. Its [pinned plan](LIB_TOURNAMENT_BRACKET_PLAN.md) separates
actual returned menus from the next match-preparation boundary434349.
All250 original calls and their independent audit match Native. The packaged
comparison and both transactional rollback trials pass without a raw override.
The accepted680-file package remains isolated from pending catalog/transform work.

## Reference and scope

The pinned NTSD EXE, installed bundled lib.dll and actual VC80 CRT execute in
controlled Unicorn2.1.4 at CW023f. Fresh menu resources come from eleven original
embedded DIBs. Catalog operands derive from137 raw DAT files and42 head/small
BMP pairs; Native rebuilds its own accepted ordinary catalog. The bracket's
504 geometry bytes44d120..44d317 are unchanged file-backed EXE data with known
provenance. The controlled global input owns these bytes; source after-state
and unknown caller storage are not Native inputs.

The250 finite calls comprise78 human-chain calls,165 all-computer-chain calls,
and7 independent controlled bracket/result/winner/held-input entries.249 execute the actual
422ab8 ret4 after432ab0 ret12; one stops BEFORE434349, with both caller frames
still pending. Earlier tick/input acquisition and full own library-catalog
initialization are not executed by this menu harness. The all-computer chain
reaches its winner by original menu simulation, not by playing seven fights.
The menu28 cases declare ordinary positive post-fight HP/state and a result;
they do not claim a recovered own match-result producer or a played match.

## Recovered behavior

- Start sets eight HP values to500, retained states to−1 and scores to0,
  round level0 and countdown25, then executes enabled4025b0 music. Both chains
  replace the previous track through the real graph/play/CRT helpers. Two
  owned26-byte wide-path buffers remain live; returned API failures are ignored
  in the original order. Winner count21 stops music before the winner sound.
- Score rectangles use seven width/height pairs and eight sets of positions.
  Scores clamp at7 for rendering/winner detection; odd losing scores use102860.
  Menu26 scans the first two scores equal to the round level. An empty scan
  advances the level by2 without rescanning that call. Waiting reaches exactly80;
  Jump forces80. Blinking chooses between the two candidates.
- Two computers resolve immediately in the menu. A sole participant whose
  signed ID/10 is5 wins; otherwise streamFA resolves ordinary opponents and FB
  resolves two group5 opponents. The winner gains2 and loser1; winning6 becomes7.
  StreamFC scales retained HP by the original signed wrapped arithmetic and
  truncating division. The finite corpus executes7 FA,1 FB and10 FC calls;
  all35 RNG invocations share the retained table/index/counter, including16 F9
  roster choices and one100 draw-result choice.
- A human fight clears all400 activity flags and twenty seat mappings, marks
  both selected bracket scores, and enters menu27 in the same call. Any available
  controller seat can claim the displayed participant. Previous Attack atCA
  suppresses repeats; an occupied seat plays the refusal sound. Cancellation
  clears assignments and allows reassignment. The corpus covers an occupied-seat attempt, cancellation and new assignments;
  the independent held-input case executes the previous-Attack==1 branch. CPU assignment
  and the actual actor/arena/replay construction beginning434349 remain next.
- Menu28 copies Actor+300 HP and+33c state through live seat mappings, awards
  the result (or uses stream100), clears mappings/activity, and resumes menu26.
  Menu29 waits, draws the controller/portrait/winner panel and accepts Attack
  only when its countdown reaches50, then resets input and returns menu10.

Previous Attack is an explicit input boundary here. The accepted419a60 local
input producer copies CD..D3 to C6..CC before acquiring new buttons;431c70 reset
only clears current buttons. The finite manifest supplies those previous bytes
explicitly on each call, then current button changes. It does not claim the
outer input-acquisition loop was executed by this capture.

## Evidence and limitations

Source3 completed in55.493s:1,026,658,386 raw bytes,
SHA256 `8acfd2ce4a35ebd5ef38f330582202707b91ba5d19988a2e019b22ed13065ad3`.
All250 completed cases were retained atomically and reproduced in the whole
corpus. The independent audit checks5,127 points/4,357,950 records/
13,108,354,710 bytes,212,500 final records/639,182,500 bytes,250,314 stores/
1,006,622 bytes,254,696 instruction reads and4,212 API reads,11,201 helper returns,
5,773 semantic caller words and1,901 decoded blobs/84,531,676 bytes.
It verifies3,204 actual instruction starts:2,816 EXE,51 lib and337 CRT.
This is observed instruction coverage, not every branch outcome.

The report's inherited `musicAllocations:143` counter counts repeated retained
music records at startup checkpoints; there are exactly two allocation requests.
All raw expected bytes and masks remain intact, including undefined bitmap reads.
The native game codec remains private C1.1.4; fixture transport compression is
separate from game output.

Source1 stopped at401dd4 because the adapter routed the return from the actual
VC80 graph-log formatter through a menu-only guard. The exact exception, source
version, timestamps, terminal process evidence and completed cases are retained.
Source2 routes this helper return correctly; no original fault or game rule was
changed. Native1 needed five missing `try` annotations. Native2 exposed an
observer-contract omission for four unchanged guards on menu20..25, and an
ambiguous duplicate `claim-first-release-human` label in the immutable manifest.
Native3 checks every extra guard's full Native state and proves no
original store/event intervened. Native4 identifies startup records by label plus
complete declared entry globals, retaining both distinct original calls.

Native4 passed the249-case corpus and both rollback scenarios in58.103s after a
91.14s build. A subsequent read-coverage check found five previous-Attack==0
reads but none==1: the misleading `held` label actually followed a release and
exercised an occupied-seat new press. Before further execution, the finite
manifest was extended by one independent menu27 case with CA==D1==1. Source3
and the final audit reproduce **all249 source2 cases and installations unchanged**,
plus all their blobs/assets/catalog/arena operands, then verify the new held
read and unchanged assignment count. Source2 remains immutable and separately
pinned; no expected call was removed or relabelled. Native5 passes the expanded
250-case comparison and both rollback scenarios in62.762s/build94.23s. Native
Core game logic is unchanged since the five syntax annotations after Native1.
The two retained setup tests pass on the same Core (66.043s in Native3); that
job's three duplicate-label comparator failures are retained separately.

The capture3 wrapper inventories every Python tool, including the independent
verifier which the source producer does not import. Its capture-time version is
retained as `source-audit1-published.py`; only the later expanded audit changes
that tool. All inventoried files stayed unchanged during source execution.
The original Native1 runner's prose still named the668-file Stage base; its
actual manifest/assertions/copy used the674-file accepted Tournament setup base.
The executed runner is archived and the current description is corrected.

Packaged acceptance passes2 tests in57.694s/build0.32s (rollback25.635s,
whole32.059s), without any raw-fixture override. Four new transport files total
135,255,139 bytes and restore the complete raw bytes/JSON/SHA; all271 older
fixtures remain unchanged,275 current. The independent artifact verification
reconstructs every archived Native input layer, checks all676 pre-fixture files,
and adds only these four fixtures for the680-file accepted package. Final input
checks retain the frozen/pending/foreign files. NTSDNative links; no window or
device was exercised. Machine-readable pins and terminal evidence are in
[lib-tournament-bracket.json](../evidence/lib-tournament-bracket.json).

The next required consumer434349 assigns CPU slots, constructs active actors
with Tournament HP/team/state, chooses/loads the arena, resets inactive actors
and initializes the replay. The earlier apparent `[esp+1c]` menu-pointer issue
at4347ba was a static ESP accounting error: three pending arguments mean this
reads rootSP+10. No pointer fault was observed or probed. Preparation must use
recovered Frame/background/replay operands, not source stack or post-state.
Team Tournament/War, own initialized library composition, full tick/match,
window/input/audio/device/Windows/clean-Mac and the full goal remain open.
