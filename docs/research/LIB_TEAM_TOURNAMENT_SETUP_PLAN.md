# Retained Team Tournament setup

Recover mode3's separate434ab0 dispatcher: menu120 initialization, eight fighter
and Human/Computer choices, controller renumbering, four teams of two, confirmation,
eleven shuffle passes, live Random choices and menu125 settings. Original Team
routine ends436fb5ret12; the following436fc0 Stage helper is separate. Initial
static disassembly includes that following helper for boundary identification;
its instruction count is not Team dynamic coverage.

Use pinned NTSD EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
original DAT/head/small BMP and menu DIBs in controlled Unicorn2.1.4/CW023f.
Each call executes actual41bc90 prologue then declared4229cc menu tail,
fresh/cached resources, real installed lib text and actual422ab8ret4.
Start stops BEFORE435a58, the first bracket menu126 write; the input clear
and sound already execute. Team function's rootSP is entrySP minus a08,
with originY at20/originX at28 and Random list at40. Pending pushes are counted
at each operand; native retains semantic locals, never expected stack bytes.

Read-only disassembly and complete reads/stores/stack/register/helper traces
are needed to recover same-call phase changes, team pair aliases, helper order
and shared RNG ownership. Two finite373-call retained input sequences are
frozen before execution. They reuse the accepted Tournament button programs
with declared initial menu120/mode3/pulse0 or29/unlock0 or1/CRT seeds17 orffffffff.
Primary selects Naruto/Sasuke humans and six Random CPUs; control all eight
Random CPUs. After each real return only button bytes and three ordinary ABI
words change. Whole World/400Actor/catalog/globals/resource/DC/stack backing and
masks remain live. This is a controlled library-menu chain, not a complete
initialized application or played tournament.

The five declared atlas operand records retain accepted geometry;45116c aliases
451178. Eleven actual resource constructors still execute. All42 small portraits
come from original DAT/BMP through the source parser and independently loaded
native catalog. Short controller label44d320 uses exact file-backed EXE7800;
unknown small-bitmap counts and original cookie storage remain unknown. Compare
drawing/API requests and clipping; no Windows pixels or device claims follow.

Exercise held inputs, Random/first/last roster choices, controller toggles and
backtracking, confirmation cancel, eleven50-swap frames and both pair members/
team words. Tags101/102 use range4 and share the original RNG state. Random103
excludes all eight live choices; recover each list from actual preceding stores.
Exercise reroll/reselect, arena100/99/ordinary arena cycle, difficulty2/1/0,
Music OFF/Random/Main, settings back/quit. End primary before435a58 and control
at an actual quit return. Existing difficulty-1 display is a separate branch.

Acceptance: all finite cases/full records and masks/ordered events/checkpoints;
independent reads/stores/checkpoints/instruction-byte/helper-ABI audit; all101/
102 pair swaps and103 candidate lists reconstructed from prior state; original
small resources compared to unchanged accepted native catalog. Whole-current-call
rollback after late shuffle RNG and late controller label must preserve prior
calls. Shared dispatcher requires retained Tournament, Stage and VS tests.
Create a new isolated package from accepted693-file arena-release package,
preserve281 fixtures and all frozen manifests/14 pending Native/3 pending source/
6 foreign files. Never build pending workspace implementations as accepted Core.

Large corpora/builds use task-owned X5 through build/research/lib-team-tournament;
shipping fixtures stay regular local files. Independent catalog5 Python78863
remains live; revalidate it and do not restart for silence or change its reserve.
Preserve actual failures/refusals with time, operation and session. No deliberate
control-pointer/protection damage, arbitrary fault continuation or blocked-action
retry. Team bracket/preparation/results, War, full catalog/library/outer loop/app
join, full match/game/network, Windows/window/device/clean-Mac remain open.
