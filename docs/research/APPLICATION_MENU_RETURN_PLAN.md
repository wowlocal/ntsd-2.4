# Own early menus, World/dispatcher return and first due iteration

Continue actual4275cb/SP1000ea74 using the own produced selector/target/Sleep,
local strings, library DC and full resource registry. Execute main427915 or the
own settings-1 alternative, whole optional-panel/overlay/present helpers, the
actual World428805/ret4, then the enclosing43e9a0 epilogue and timer/counter tail
to the next43d110. Do not supply a convenient dispatcher success response.

Pinned NTSD EXE/lib.dll/VC80, original control.txt/DIBs on Unicorn2.1.4 remain
the reference. The actual relocated DLL attach precedes the independently entered
WinMain chain; earlier full CRT/NLS/Windows loader stays open. All new child code
runs on the parent's existing CPU/stack/resources without expected-state imports,
register/scratch resets or worker delivery. Normal SEH/cookie instructions execute;
no control/protective corruption, bypass or arbitrary fault continuation.

Finite acceptance: all43 accepted APPLICATION_SCREEN_BODY parents; the separately
accepted settings-1 APPLICATION_FRONT_SCREEN parent; four default-parent controls
for negative/positive menu Blt and presentation HRESULT. Follow each actual path
through its return or first missing/NULL resource read. Preserve that source stop
separately from successful native iterations. Other mouse/worker/selector branches
retain the earlier controlled contracts; do not inject them into this own chain.

Recover complete original globals/stack/library/bitmap bytes and masks, ordered
helper/API events, actual World/dispatcher return registers, SEH/cookie lifetimes,
caller-local reads and the timer/counter consumer. Fresh final timeGetTime is a
declared response123456901 after the default background's123456900. Image/device,
GDI/COM/thread behavior remains at declared boundaries; no real window, raster,
network/URL or audio operation occurs. Newly reached unavailable children must
be recovered or reported at their real boundary before claiming a return.

Native composes its own alternate/main/presentation helpers and full bitmap
resolver, using own loaded surfaces and library DC. Preserve produced local
bytes; unknown caller storage stays unknown. Recover the dispatcher return-value
producer and live post-World mode test; do not import saved register values.
Commit the full due message-loop iteration only after its dispatcher, final
clock/Sleep and counter finish. Late native observer errors must preserve all
committed startup/callback/timer/MSG/resource/library state.

Raw and packaged isolated acceptance must preserve243 old fixtures, all reference
assets and10 codec vendor hashes. Verify complete raw/packed bytes/JSON/SHA,
atomic cases/blobs, exact retained parent records, source code identities and
owned-file review. Export committed159d969 with owned overlays only; six foreign
files remain unchanged. A checked first iteration does not close worker delivery,
all dispatcher modes/repeated input, actual Windows/devices/app integration,
full Naruto/Sasuke match/all content/network/replays or clean-Mac acceptance.
