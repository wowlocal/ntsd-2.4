# Bundled library hit-effect continuation — finite plan

Recover installed42fcb1→10001322 inside whole42e100/ret4 and its two-pass
41eefb..41f2ac caller. EAX is the live ITR.effect loaded by42fcaa/42fcae,
not an Actor state. Reference NTSD2.4 EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, bundled
lib.dll SHA256`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
and pinned VC80 SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.

Unicorn2.1.4 and the existing controlled World/Actor/ITR/CRT harness execute
the actual installed library with declared relocation and Windows API responses.
Low-level execution is necessary to establish live effect/defender provenance,
the unexpected0xb2 read stride, unchanged live FPU stack and continuation after
prior damage/reflection mutations. Actual Windows, initialized library-enabled
matches and device behavior remain separate; no DLL or emulator runs in Native.

Static findings to verify before native acceptance:

- Effects3/30 retain the original42fcbb freeze path; other values below6000
  resume42fd1d. The apparent MP branch1000138d has contradictory preceding
  comparisons; do not implement imagined5000-range MP behavior.
- Signed effects>=6000 check the current defender Object category. Type0 reads
  four bytes at Object+7ac+previousFrame78*0xb2. This is not stride0x178. If the
  raw word differs from effect, source stores effect-6000 as currentFrame70.
  It retains other reaction fields and resumes42fd1d.
- Read addresses must resolve against recovered header/inline-Frame storage,
  preserving masks and cross-frame reads. Unknown bytes are not filled from
  expected source snapshots. Ordinary invalid Frame/storage reads are explicit
  native rejection/rollback boundaries, not successful original matches.

Finite acceptance:

1. Read-only census of all137 original DAT hashes and full loaded Object/ITR
   bytes/masks: enumerate actual high effects and the0xb2 words for all400
   previous-frame indices in each type0 Object. Distinguish defined data from
   uninitialized bytes and static availability from runtime reachability.
2. Execute whole hit callers for retained original hit inputs plus a finite
   high-effect matrix: all original6000+ effect values, all400 previous-frame
   offsets, category bypasses, safe equality cases, effect5000-range controls,
   signed boundaries that do not create unrecovered Frame references, and
   alias/multiple-contact ordering. Preserve immutable old outcomes separately.
   Do not deliberately corrupt control pointers or protective structures.
3. Retain complete pool/masks/globals/mutable ITR heap/CRT results and ordered
   real helper events, actual instruction and hook/stride-read/store evidence.
   Execute all reachable hook starts; document the statically unreachable MP
   branch without injecting EIP or patching its gates to force coverage.
4. Implement native library-enabled whole hit APIs with a shared pristine pass
   and mask-checked original Object byte resolution. Verify rollback after
   late observer errors and unknown stride backing. Existing pristine fixtures
   and their declared unloaded-library scope remain unchanged.
5. Raw and packaged release comparisons, relevant retained whole/initialized
   controls, full raw/packed/JSON/blob/SHA and prior-fixture/vendor checks.
   Publish a bounded milestone only after owned source/build jobs are terminal.

The static dependency audit requires composing430c8c→100013d0 in this same
whole-hit study. Its kind8 path adds the literal eight DLL bytes at+3014
(`087a4400cb764100`, binary64 bits004176cb00447a08), not the original EXE's1.
Do not interpret that datum as a pointer dereference or replace the tiny value.
Additional kinds select timer/frame assignment and binary X/Y/Z copies in
their original order. Kinds824/825 use a separate20000-byte allocation, with
the first word of each attacker-slot8-byte row retaining the selected defender;
sentinel777 binds that word. Preserve untouched bytes and retained-call order.

Before capture, extend the finite matrix with every installed movement kind,
unhandled neighbors, ordinary finite/tiny/signed-zero coordinates, aliases,
timer wrap, valid requested frames and target-buffer equal/miss/sentinel cases.
Start the target buffer from the actual installer's declared zero allocation;
nonzero controls are explicit gameplay-state inputs, not forged source output.
Retained buffer chains independently carry source and native results. Compare
all20000 bytes as well as whole game storage. The unused4000-byte allocation
and complete DLL image must remain unchanged in source.

Both7845-case pristine precision corpora are reexecuted with both actual hooks.
Changed kind8 outcomes must be recorded as changes, not forced to the pristine
expected values. Other installed hooks are not disabled; unexpected entry is
an open dependency. All control storage addresses remain within recovered game
records. The final application/full-match goal remains unchanged.

The completed18137-call source run identifies164 changed pristine outcomes.
A bounded fresh pristine-EXE control will reexecute exactly those164 whole
calls, recover their full old bytes and compare exact deltas with the completed
library corpus. All old hashes/events must reproduce; this is an explicit
separate unloaded-library VM, not removal of a hook to accept library behavior.
