# Host gameplay correction1 — frozen plan, 2026-09-26

Continue the [failed validation](APPLICATION_HOST_GAMEPLAY_VALIDATION.md) under
[WORKFLOW](WORKFLOW.md). Base HEAD `47ef76e`; previous goal turn was progress:
it preserved the failed fresh compilation and diagnosed one missing declaration.
The consumer is the existing host gameplay/paused continuation through the whole
outer return, required before the first Naruto/Sasuke District match. Full game
scope remains that of CONTINUE_GOAL; this is not a substitute playable milestone.

## Exact correction and protected inputs

Clone all1040 regular files from the frozen gameplay candidate, manifest
`4deadc3ce77d2151662b41df365e9ca2c9c4c6ab1da7f8eb0161ec11a0b282cc`;
also verify the failed validation candidate against that same manifest.
Change only native/Tests/NTSDCoreTests/OriginalApplicationActiveOutputTests.swift:
`func sequence(_ reverse: Bool) throws {` becomes
`func sequence(_ reverse: Bool, driver: OriginalApplicationLoadedTestDriver? = nil) throws {`.
This spelling was inspected in the frozen candidate; preserve before/after bytes
in the patch.
Do not change the body, caller, expected, masks, methods, Core or resources.
Default nil retains the old Bootstrap transport. Pin the actual dirty root1034,
both prior1040 candidates, source55, previous failure and tool inputs.

Only the pinned original distribution is the reference: EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Use saved envelopes and existing independent Native projections with their
synthetic mappings/API/keyboard, ABI/FPU, unknown-storage and installed-library
boundaries. No new original execution, capture replay, device IO or refused
operation. Existing safety incidents remain open; exact unknown triggers stay
unknown. Independent review is unavailable; author verification is not review.

## Native gates and limits

Retain all69 names and all limits from the frozen validation plan unchanged:
fresh SwiftPM release/WMO/jobs2/enable-testing, new scratch/cache/config/security,
minimal environment. Build3600s/12GiB aggregate RSS. Tests1–50:600s,8GiB except
26/28/35–37/43–50:12GiB;51–63:900s/16GiB;64–69:1800s/16GiB. Queue14400s.
One exact named XCTest per process. Require the named pass, three complete
one-test/zero-failure summaries, Selected-tests pass, exit0, no guard, absence.
Stop on first nonpass; no automatic code/limit change in this candidate.
Retain all whole17/48/14 schedules, both backings, all owners/events/masks,
late failures and same-session retries. No splitting or skipped comparisons.

Require fresh191/61/270 original source inventories, binary, all385 fixtures and
102 runtime resource payloads as regular files. Package bytes, Native results,
full artifact archive and metadata readback are separate gates. Prior binaries
cannot validate this candidate. Preserve every failure, partial log and unstarted
method; diagnose new failures separately before another correction round.

## Storage, tools and process ownership

Task: `/Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/application-host-gameplay-correction1-20260926`,
with a build/research alias. Check APFS UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`,81GiB external/7GiB internal at start.
Keep original40GiB/6GiB reserves and source17GiB commitment; allowance24GiB,
stop22GiB plus2GiB closure, logical150GiB. No deletion or reserve reduction.

Reuse the existing preparation, guarded runner,69-method queue, package verifier
and finalizer. A small task adapter generates pinned copies with exact changes
only for task/tool paths, one candidate declaration/manifest, prior-candidate
verification, and terminal-source observation. Retain the runner's ownership,
transition, guard, signal/reap and exact result-parser bodies unchanged. Verify
the transformation and existing positive/negative controls before build.
Generated metadata/tools use at most2MiB internal space; large IO stays on X5.
Commands: python3 tools/prepare_application_host_gameplay_correction1.py;
then generated runner with build1-config path/SHA, generated package verifier
with task/build1, generated test queue with task, and generated finalizer.

Revalidate PID/start/command/cwd/job before process actions. Source59727 is now
terminal0/absent (2026-09-23T00:10:17.364994Z), with outcome researchBoundary,
end dependency, publication logical-cap boundary. Exit0 is not full catalog
acceptance. Preserve that job/publication; do not restart or signal it. Source
terminal verification is read-only and separate from catalog publication audit.

Root author owns this plan/result, matching evidence, the small adapter, and own
CURRENT_WORK/RESEARCH_MAP insertions; no root Native promotion. Final publication
must verify prior candidates/root/source pins, exact one-file delta, artifact and
metadata bodies/modes/mtimes/membership. Commit the checked increment. Independent
review, root promotion, complete source catalog/comparison, installed trajectory,
app/window/input/audio, Windows/clean-Mac, first match and full game remain open.
