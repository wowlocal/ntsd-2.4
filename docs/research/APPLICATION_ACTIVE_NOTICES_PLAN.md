# Active notices from current HUD output

Status: accepted in the finite scope; see [results](APPLICATION_ACTIVE_NOTICES.md).
The frozen candidate1 plan remains immutable. Parent clean commit
`e638817aa23837b6f83f07212cf3887b188c4417`, accepted
[HUD](APPLICATION_ACTIVE_HUD.md). Follow [WORKFLOW](WORKFLOW.md),
[POSTHUD_NOTICES](POSTHUD_NOTICES.md), [GAMEPLAY_NOTICES](GAMEPLAY_NOTICES.md)
and immutable archive2637..2710. Consumer: recording/layout/output and complete
active tick, Naruto/Sasuke District match, full standalone native game.

## Contract and evidence

Read saved primary/control active48 fixtures, hashes
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
Original EXE hash `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Saved pristine EXE/VC80 with synthetic APIs and own library-enabled Native
application are distinct environments; no Windows/device observation is added.

Both48 stage15 `421a2d..421cdc`, SP1000e9bc, join preceding HUD and following
recording. Inventory1 verifies actual current globals450bec/450c2c/450c28/451160
all0, no events/helpers/effects/boundaries/checkpoints, no global writes or
watched caller-stack accesses. Complete before/after component refs are equal.
Saved instruction set contains ten executed caller PCs plus stopped421cdc,
not an ordered dynamic trace. HUD helper returns EDI0 (saved[3]), required by
the first comparisons. Caller421a33 reads IAT447174 into ESI; this is not a
string access or an API invocation. No Native IAT/register model is claimed; saved CW023f/FPSW0or4000/tagffff
and scratch/spawn/stageDefeated retained. This is source evidence only, not a
new Native register/FPU/store comparison.

Derive finite no-output behavior independently: diagnostic450bec must be0;
exit450c2c must differ from1; selector450c28 must differ from1 and2. Read only
causal words with their masks; mode451160, Actor/frame/resource data and local
string contents are not causal on this path. A selected branch is explicitly
outside this finite active comparison, never silently omitted. Existing819
direct/8 separate sNaN discrepancies with QNaN companions/4 cookie-overwrite
Native rejections and74424 numeric controls remain unchanged.

Saved continuous stack hooks watch rootSP+34..<74 and+44c..<5c4. Empty
phase-labelled accesses establish no notice string access in these96 calls;
they do not establish initialized zero backing. Raw active local bytes are not
captured. Own application gameplay caller defaults to unresolved formatter;
Menu.Snapshot.local is a different menu-local record, NOT that formatter.
Current API does not expose body formatter at checkpoints. Retain this explicit
observability limit. Direct public notice calls on current own HUD match with
nil and independently owned opaque340-byte storage verify unused backing
retention and no provider/event request. These controls supplement the full
application checkpoint comparison, not substitute for enabled caller lifetimes.

## Implementation and checks

Three new test-only ActiveNotices Projection/Source/Tests files. Source reader
validates fixture hashes, all component joins, globals/pool/masks, retained
component identities, empty event families and source trace. Own tests compose
the complete preceding HUD chain and independently check current branch gates,
full match/state including graphics, resource/music/background owners, menu-local
record and chronological journal at notices. Reject all primitive observations
inside HUD/notices interval and notice annotations outside it. Later recording,
layout/output semantics remain open; parent whole-return/command checks retained.

Discriminating controls: reject each selected branch and unknown causal word;
accept nonzero nonselecting exit/selector values and unknown irrelevant mode;
retain nil and opaque local bytes/masks with no API effects. Controls are Native
or comparator probes, not new source reachability claims. At first own call
inject notices checkpoint and beforeCommit failures, verify entry/environment
rollback, then full same-session retry. Retain all parent late-failure checks.

Root owns those3 files, this plan/result/evidence, CURRENT_WORK/RESEARCH_MAP;
war_contract_review owns numbered read-only independent reports. Core, old1012
Native files, fixtures/resources/expected/masks, old producers/failures and archive
stay unchanged. Freeze actual files/plan, review before Native execution.
Selected7 release methods:2 new Notices,2 retained HUD,2 controlled PostHUDNotices,
1 DiagnosticNumber. Inherited method identities must be derived from accepted
HUD verification and unchanged file pins, without double counting. Actual parent
contains159 unique methods; removing the two reselected HUD methods leaves157
inherited methods, plus7 direct methods =164 unique. Controlled notices/numeric
methods are not present in that inherited list and are added by this run.

## Operations, limits and gates

Task area `build/research/application-owned-active-notices-native-20260914/`,
X5 APFS UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. initial.json pins1012 actual
Native files. Keep external40GiB/internal6GiB reserves; task40GiB cap. Readers
96calls/300s; Native3600s; three candidates before renewed contract diagnosis.
Reuse parent freeze/job/package/archive tools. Expected1015 Native files, all1012
old exact;485 package files separately verified. Native tests, package bytes,
archive bodies/modes, independent publication review and Git are separate gates.

No original/emulator/historical producer/capture/auditor execution or retry.
Open safety incidents remain open, no model/settings change or refusal rerouting.
After acceptance continue recording, layout/output. Full tick/match/game,
app/window/input/audio/device/Windows/clean-Mac and enabled caller-string lifetimes
remain open. EXE envelope is not recalculated. No automatic goal completion.
