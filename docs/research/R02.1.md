# R02.1 — состояние полного такта

**Статус:** в работе; конструкторы, загрузочный пул и родительский проход каталога
перенесены в Swift; весь исходный каталог с Object/BG/Stage и общими ресурсами
теперь также загружен совместно и сравнён; общая подготовка матча с этим каталогом
совпала в 50 случаях; создание буфера повтора через настоящий вызов меню
также перенесено полностью. Сырой снимок проверен исполнением оригинала.
Полная инициализация и словарь типов не завершены.
**Родитель:** R02 в [карте](../RESEARCH_MAP.md).
**Зависимость:** результаты [R01.1](R01.1.md).
**Эталон:** SHA-256 `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
**После неё:** R03.1 — внешний загрузчик; затем R01.2 и общие игровые механизмы.

## Вопрос и объём

Какое состояние читает и меняет обычный путь `43e9a0 → 4246b0 → 41bc90`,
какие у полей размеры/знаковость, кто задаёт начальные значения и владеет памятью?
Начать с двух исходных бойцов, их текущих зависимостей и найденных в R01.1
пропусков. Остальные типы/режимы явно учитывать как расширения словаря.

## Опоры

- Actor выделяется по `0x420` байт: `0x41c070..0x41c091`. Конструктор —
  `0x4061d0`. Шаг `0x500` в Python остаётся размещением стенда.
- Вызов матча использует World по исходному статическому адресу `0x458b00`;
  `+0` — селектор диспетчера, `+4` — 400 байт активности, `+0x194` — 400 указателей,
  `+0x7d4` — каталог. Полный жизненный цикл и все поля World ещё не закрыты.
- Каталог выделяется размером `0x4d823a8` в `0x41bff0` и заполняется через
  `0x4122f0`. Не выводить layout каталога из одной аллокации.
- Frame stride `0x178`, 400 записей, Object `+0x7a4`; смещения проверенного
  загрузчика — [FRAME_LOADER.md](../FRAME_LOADER.md).
- Фазы, пауза, периодические счётчики и ресурсные поля —
  [TICK_PIPELINE.md](TICK_PIPELINE.md). `globalWrites` в полном `tick-trace.json`
  даёт адрес инструкции, адрес записи и размер для шести исследованных случаев.
  Это не полный список достижимых записей.

## Работа

- [x] Получить воспроизводимый дамп и маску записей конструктора Actor на
  непустом шаблоне памяти; отличить записанные defaults от нетронутых байтов.
- [ ] Восстановить инициализацию World и каталога, указатели, размеры аллокаций,
  счётчики, связи owner/team/clone/grab/weapon; разделить исходные и тестовые адреса.
- [x] Выполнить весь загрузочный проход пула 400 Actor и повторный конструктор
  первых восьми; сравнить полные записи/маски после обоих проходов с Swift.
- [x] Исполнить родительский проход каталога `4122f0`, перенести порядок реестра,
  вызовы дочерних загрузчиков и собственные записи; явно отделить непрозрачные
  дочерние функции от загруженного содержимого.
- [x] Выполнить родителя с настоящими Object/BG/Stage/bitmap/sound children и
  сравнить нативный целый каталог с общей суммой, звуками и порядком ресурсов.
- [x] Соединить полный каталог с World/400 Actor и общей подготовкой
  `42d1ff..42d6ed`; сравнить полные записи до/после, RNG, BG lifecycle и input reset.
- [x] Выполнить пролог `42cf8a..42d1ff` с реальным VC80 sprintf и sound/fill
  helpers; сравнить режим/Stage/filename/globals в цепочке подготовки и записи.
- [x] Перенести продолжение `42d704..42e0f9`, включая все схемы `450c2c==1`,
  общий подбор персонажей, команды меню и настоящий возврат; сравнить полные записи.
- [ ] Восстановить происхождение ранних menu/RNG-входов;
  проверить целый путь, не только отдельные участки запуска.
- [x] Исполнить CRT seed/table initialization через исходные вызовы меню,
  перенести отдельное CRT state и сравнить сгенерированные таблицы в цепочке
  полной подготовки/записи; промежуточные потребители CRT остаются явными входами.
- [x] Перенести все пять пунктов главного меню, mouse200..205 через реальный
  WndProc ret16, сетевой init с исходным выбором адреса и ошибками; сравнить
  полученное состояние в цепочке полного каталога/подготовки/записи.
- [x] Перенести общий tail меню через ret4 и целый dispatcher branch World=1→2,
  с реальными volume/overlay/present/shutdown helpers до платформенных границ;
  сравнить полученный World=2 в цепочке подготовки/записи.
- [x] Перенести общий WAV loader4014e0 для всех409 исходных файлов и первый
  загрузочный caller41be98..41bfeb с18 общими звуками; сравнить полные PCM,
  format/descriptor, globals и порядок вызовов при включённом/отключённом звуке.
- [x] Соединить первый проход41bc90: общие звуки, каталог с включённым звуком,
  пул400 Actor и10 UI bitmap; получить исходный сброс44d05c в41c577.
- [x] Продолжить реальную загрузку через local input419a60 и caller41c5e5,
  сравнить remote/playback4198f0/4197a0 и их настоящий порядок вызовов.
- [x] Перенести phase0 control41c5e5..41d46f: общие hotkeys, сетевые пакеты,
  checksum/error shutdown, reset и возврат сохранённых настроек из повтора;
  сравнить оба непрерывных первых caller и предоставленные контрольные входы.
- [ ] Продолжить playback source43dc50, checksum/recording41d4b7..41d714
  и pause/menu→429730, сохраняя происхождение World/Actor/буферов.
- [x] Продолжить реальный пул через10 UI bitmap до41c581, включая malloc/device
  failures, constructor lifecycle/ret12 и исходный сброс44d05c; сравнить весь
  неизменный пул, bitmap storage и globals после каждой записи.
- [x] Соединить enabled Object/Frame/weapon sound registry с настоящим WAV
  helper и SetVolume в одном полном catalog run; сверить PCM/cache/buffers,
  порядок/ABI и все загруженные записи, включая повторный Object.
- [x] Продолжить подготовку через настоящий вызов `42d6fc` и весь `43d2c0`:
  буфер начального состояния повтора, освобождение старого, сброс RNG counter.
- [ ] Составить `STATE_LAYOUT.md`: поле, размер, числовой тип, источник значения,
  места чтения/записи, стадия такта, доказательство, неизвестное.
- [ ] Составить инвентарь globals пути R01.1, включая ввод/паузу, RNG, ресурсы,
  матч, повторы, фон и звук; не считать BSS-ноль окончательным default матча.
- [ ] Спроектировать снимок с сохранением битовых значений float/double,
  знаковых byte/word/dword и нормализацией указателей по объектам/слотам.
- [x] Сопоставить каждое поле текущего Swift-состояния с исходным либо отметить
  как производное поле вывода. Перечислить отсутствующие поля и владельцев задач.
- [ ] Проверить дампы/снимки исполнением конструктора и существующими широкими
  трассами; убедиться, что RNG и HP/MP-различия из R01.1 остаются видимыми.
- [x] Обновить карту, справочник и границы R01.2/R03.1 для текущего результата.

## Выполненный этап

- [STATE_LAYOUT.md](STATE_LAYOUT.md) содержит точные конечные записи и нетронутые
  области конструкторов, сопоставление состояния практики и список неизвестного.
  Actor: 913/1056 записанных байт; World: 404/2008 байт исследованного префикса.
- [oracle_state.py](../../tools/oracle_state.py) исполнил оба конструктора на
  четырёх шаблонах и двух адресах. Нативные конструкторы совпали в 16 случаях
  по всем 24 512 байтам и маскам. Они пока не подключены к игровому циклу.
- [oracle_state_trace.py](../../tools/oracle_state_trace.py) сохраняет 38 снимков
  шести случаев R01.1: целые Actor/Object, PE .data и доступные части каталога/heap.
  Есть частичные ссылки по region/offset и инвентарь обращений к globals.
  Это ещё не полный переносимый снимок или маска инициализации матча.
- RNG/HP/MP из R01.1 сохранены, синтетическая инициализация и исходные расхождения
  с практикой не скрыты. На этапе конструкторов было 14 Swift-тестов без ошибок,
  включая прежние сохранённые игровые сценарии и новые проверки состояния.
- [BOOTSTRAP.md](BOOTSTRAP.md): ещё 8 случаев / 16 контрольных точек загрузочного
  пула; 6416 записей полностью совпали с Swift с нормализацией только известных
  указателей. Текущее `swift test --package-path native`: 16 тестов без ошибок.
  Повторный конструктор сохраняет ранее известное происхождение нетронутых полей.
- Каталог разложен по основным областям S: 500 мест для Object* (ёмкость выведена
  из границы), 60 областей стадий, 101 запись фона, два счётчика и хвост имени
  файла. Полное происхождение загруженных данных ещё не проверено D.
- [CATALOG_REGISTRY.md](CATALOG_REGISTRY.md): D против Swift родителя `4122f0`,
  12 случаев / 48 записей / 83 232 байта и маски, 3446 упорядоченных запросов.
  Исходный реестр содержит 137 объектов и 17 фонов; индексы и ID различаются.
  Дочерние загрузчики и их побочные эффекты — явные границы стенда.
  `swift test --package-path native`: 18 тестов без ошибок.
- [OBJECT_LOADER.md](OBJECT_LOADER.md), [R03.1](R03.1.md): целый путь Object для
  Naruto/Sasuke/kunai и контрольного потока. Native decoder/заголовки/кадры/таблицы
  bitmap, общий sound cache и checksum совпали в объявленных CRT/device-границах.
  Сохранены raw Object/heap и маски; padding/указатели Frame ещё не сравниваются
  с native raw storage. Файловый режим старого декодера оказался отдельной границей.
  После этого этапа: 20 Swift-тестов без ошибок; весь прежний изолированный
  корпус из 15 056 определений кадров повторно совпал с неизменным хешем.

- [BACKGROUND_LOADER.md](BACKGROUND_LOADER.md): все 17 исходных арен,
  отдельные загрузка/выгрузка слоёв и повторная District; 149 вызовов / 39 разборов
  с контрольными потоками. Все 5 759 520 байт/масок BG/bitmap, checksum и порядок
  ресурсов совпали с Swift в объявленных границах. Это не отрисовка и не путь меню.
  После этого этапа: 22 Swift-теста без ошибок; весь прежний Object corpus
  повторно совпал с неизменными сохранёнными файлами/хешами. Повторно прошли
  и все 15 056 определений изолированного Frame corpus.

- [STAGE_LOADER.md](STAGE_LOADER.md): весь исходный stage.dat — 25 стадий,
  138 фаз, 818 записей; отдельные контроли порядка/повторного разбора/границ.
  В 6 загрузках сравниваются 436 промежуточных точек и все 60 полных Stage при
  каждом EOF — всего 796 записей по 1 350 408 байт и маски. Неизвестные байты
  и ранее полученное происхождение сохранены. Stage не добавляет checksum.
  24 Swift-теста прошли, включая прежние сохранённые сценарии. Это не Stage gameplay.

## Следующее действие

[RAW_FRAME_STORAGE.md](RAW_FRAME_STORAGE.md) добавляет нативное сравнение всех
137 Object с MSVCR80 .6195, полными Frame/malloc, перекрытиями имени/указателя/
индекса и десятым листом. 38 непрозрачных указателей сохранены побайтно, включая
их частичные изменения. С двумя контролями совпали 71006 Frame-наблюдений,
14598 аллокаций, 37036545 байт и масок; прошли 27 Swift-тестов.

[LOADED_CATALOG.md](LOADED_CATALOG.md) соединяет настоящие дочерние загрузчики
внутри `4122f0`. Весь исходный реестр совпал с Native Core при text/a5 и raw/00,
также проверено чередование Object/BG с повторными ID. Сравниваются все области
Catalog, полные Object/Frame, bitmap, Frame heap, общий checksum и звук при
каждом дочернем возврате. Итоговые суммы 31475378 / 31461560, звуков 400.

[MATCH_PREPARATION.md](MATCH_PREPARATION.md) теперь соединяет этот каталог с
World/400 Actor и общей подготовкой `42d1ff..42d6ed`. Сравниваются 50 случаев,
52102 записи / 79596336 байт и масок, 720 RNG-вызовов, 796 новых bitmap и
766 освобождений. Два шаблона памяти, все исходные Object и арены, перезапуски,
Stage и контрольные коды проверяют общий механизм; часть входов синтетическая.

[REPLAY_INITIALIZATION.md](REPLAY_INITIALIZATION.md) добавляет настоящий вызов
`42d6fc`, весь `43d2c0`, caller cleanup до `42d704` и `43d280` при замене/очистке.
50 целых буферов / 324583600 байт и масок совпали с Swift. Индекс RNG сохраняется,
его счётчик сбрасывается. Входы metadata/control по-прежнему заданы явно.

[MATCH_PRELUDE.md](MATCH_PRELUDE.md) добавляет предшествующий `42cf8a..42d1ff`,
настоящий sprintf MSVCR80 и целые sound/fill helpers до COM-границы. В 50 цепочках
совпали globals/маски, имя повтора и порядок событий; затем 77300 записей
подготовки / 115871104 байт/масок и 50 полных буферов. Выбор меню и время заданы явно.

[MATCH_CONTINUATION.md](MATCH_CONTINUATION.md) добавляет оставшиеся действия
до `42e0f9` и настоящий `ret 12`. 222 возврата, 225298 записей и 327834160
байт/масок совпали с native, включая все 30 схем команд и списки кандидатов.

[RANDOM_INITIALIZATION.md](RANDOM_INITIALIZATION.md) добавляет настоящий
VC80 srand/rand, seed-prefix и оба вызова генератора таблицы. В 50 цепочках
совпали 176178 результатов/состояний CRT, таблицы, полная подготовка и буферы
записи. Таблицы больше не берутся из повтора: исходный вход — PE/BSS;
таймер, промежуточное число CRT draws и меню заданы явно.

[MAIN_MENU.md](MAIN_MENU.md) добавляет1020 menu probes,450 сообщений мыши,
139759680 байт/масок,4150 событий и250 генераций таблицы через реальное меню.
Сетевой init и выбор адреса исполнены целиком с заданными OS responses;
затем совпали50 полных цепочек подготовки/записи.

[MENU_PRESENTATION.md](MENU_PRESENTATION.md) добавляет1482 реальных возврата,
66 переходов World=1→2,177955008 байт/масок,5382 события и184 actual CRT formats.
Проверены громкость, overlay, вывод до COM/GDI, shutdown/free и latch;
новый World=2 далее участвует в50 полных цепочках подготовки/записи.
Это ещё не непрерывный путь экранов: entry tail и контекст429730 заданы явно.

[WAVE_LOADING.md](WAVE_LOADING.md) добавляет431 изолированный вызов для409
исходных WAV и22 контролей, затем3 прохода первого sound caller с54 настоящими
дочерними вызовами. Совпали75829038 байт/масок и6982 события. MMIO и COM —
явные границы; вывода звука здесь нет. В прежнем снимке после записи44d05c
остаётся1, поэтому его нельзя использовать как окончательное состояние
перед тактом, просто пропустив загрузку.

[INITIAL_INTERFACE.md](INITIAL_INTERFACE.md) теперь закрывает10 UI bitmap
после настоящего пула:13 проходов,114 bitmap/5304 Actor constructors,
13029720 байт/масок и519 событий. В41c577 флаг сбрасывается даже при отказах;
этот сброс получен из кода, но начало всего прохода по-прежнему не соединено.

[CATALOG_SOUNDS.md](CATALOG_SOUNDS.md) теперь соединяет enabled registry
с настоящим4014e0: весь каталог даёт400 вызовов для365 файлов; совпали
66210142 audio bytes/masks и112063739 bytes/masks каталога. Контроль с
повторным Object также прошёл; lookup/индексы сохраняют перекрытия path cache.

[INITIAL_LOADING.md](INITIAL_LOADING.md) теперь соединяет настоящий первый
41bc90..41c581 без замены стека/загруженного состояния: два полных прохода,
362760002 bytes/masks,36 общих/800 registry WAVs,816 Actor/20 UI constructors.
Фаза/пауза, исходный cache с ненулевыми device/mode globals, callbacks каждого
BMP-поля и настоящий44d05c=0 совпали. Animated loading при elapsed>33,
playback prefix и полный ret4 остаются открытыми.
[LOCAL_INPUT.md](LOCAL_INPUT.md) продолжает оба исходных загрузочных прохода
до41c5e5 и проверяет606 случаев419a60:601 ret12,485214 records/567956264
bytes/masks. Keyboard/joystick и packing перенесены;1512 AI/618 object calls
проверены только как явные границы, без тел этих обработчиков. Старые полные
loading fixtures остаются закреплены; native передаёт собственный результат.
[RECEIVED_INPUT.md](RECEIVED_INPUT.md) теперь проверяет1452 случая4198f0/4197a0
и caller41d469:757 remote/710 playback returns,1373541288 bytes/masks. Все256
значений командного байта, status−1, обе фазы/граничные значения, полный байт
записи, игнорирование activity и повторные Actor-ссылки совпали. Основной первый
phase1 caller продолжает загрузку/локальный ввод на том же CPU/стеке; первый
phase0 control начинает явно с41d469, не притворяется исполнением сети/hotkeys.
Оба полных исходных loading transports и natural local input воспроизведены;
native пересобирает и проверяет весь прежний corpus. Старые fixtures не изменены.
[INPUT_CONTROL.md](INPUT_CONTROL.md) теперь закрывает41c5e5..41d46f в объявленном
объёме: 5 986 случаев совпали с Swift после двух свежих полных загрузок,
включая 25 984 hotkey calls, 996 send/1 096 receive boundaries, 152 сообщения,
4 восстановления настроек и 30 сбросов ввода. Тела общих helpers исполняются,
platform responses остаются явными входами. Первый phase0 caller теперь проходит
прежний разрыв непрерывно. Shared sound/shutdown/reset переиспользуются;
старые fixtures не изменены. Это ещё не socket transport или playback startup.
Следующий этап — источник playback packet43dc50, checksum/recording41d4b7..41d714
и pause/menu→429730 на этом загруженном состоянии, а также animated4242e0.
Затем происхождение остальных
входов/меню-ресурсов, ветвь World=2/41bc90 к429730, остальные экраны и весь
жизненный цикл CRT state.
Их происхождение нельзя заменить общим
подготовленным состоянием. Завершить словарь остальных полей/связей и provenance, расширить
нормализацию указателей. Таблица globals уже имеет воспроизводимый инвентарь,
но семантика и инициализация всех записей ещё открыты. R03.1 начата на готовой
части оснований R02.1; обе карточки остаются открытыми;
сырой снимок сам по себе не снимает эту зависимость.

## Готовность

Для объявленного объёма существует словарь с исходными адресами и воспроизводимый
снимок. Нет скрытого обнуления неинициализированных полей и потери состояния при
сравнении. Неперенесённые механизмы имеют поля и явные дальнейшие задачи.
Эта карточка не закрывает все игровые правила R05–R16 и не заменяет Windows W.
