# Host loading validation completion — round2, 2026-09-22

Follow [WORKFLOW](WORKFLOW.md) and the [round1 result](APPLICATION_HOST_LOADING_CORRECTION1.md).
The game question remains actual loading owners, host commit, retry and ticket
identity. This continues the same finite42-method contract toward interactive
application loading, with no new source rule or original/device execution.
Base HEAD `90fd80c1f3e592a06b860988cd0ebe999c3f2d19`; actual input is the frozen
corrected1037-file tree, compiled binary and487 resource files in
`application-host-loading-correction1-20260922`. Keep root1034, source55 pins,
original expected/masks, all failures, source producers and prior archives immutable.
Reference EXE SHA256 `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
DLL/CRT and source input provenance remain those pinned by the preceding plans.

## Cases and evidence

Retain the27 exact named passes with reaped exit0 from round1. Run whole method28
`OriginalApplicationHostLoadingTests/testDifferentSameRevisionTicketsAndRepeatedCompletionAreRejected`
and the fourteen unstarted methods29–42, in order, one XCTest process per method.
Use the unchanged selection, binary, original candidate cwd and sibling resource
bundles, without raw overrides, compiler/linker, relocation or method splitting.
New job/log/config/TMPDIR and metadata belong only to this task. Reverify full
candidate/package bytes and prior successful jobs/logs before execution and closure.

Prior method28 is an incomplete resident-guard result: sampled8,786,558,976 bytes
exceeded8GiB; revalidated SIGTERM and reaped-15, no assertion or XCTest completion.
The preserved capacity observation reports24GiB physical RAM and source451472KiB
RSS. For this one whole-method rerun declare12GiB sampled tree RSS. This leaves
half the physical RAM outside that declared test allowance, not a guarantee of
availability. Every other method retains8GiB. Original guard/overshoot is retained;
never relabel it a pass or change the old limit. This is round2 of at most3.

Each method600s, total15-method queue3600s, poll1s/RSS2s/logical15s. Stop on the
first failed/incomplete method, guard or monitor error; preserve partial results
and do not automatically retry. Full gate is42 unique methods on the same binary:
27 old plus15 new. Exact named result and three complete zero-failure summaries
are required with actual exit0; process exit alone is insufficient.

Reuse the existing host runner/queue and Catalog53 completion/archive procedures.
Adapt only task/input paths and the declared external artifact root; keep process
ownership/transition predicates and monitoring loop, passing the verified artifact
root for cwd containment. Only fixed test28–42 xctest commands are allowed.
Before any guard signal verify PID/start/full command/cwd/job; never signal source59727.
Preserve terminal observation errors and sample gaps. Short-method RSS0 means no
sample. No original game or actual Darwin-transition experiment is needed here.

## Storage and closure

New task `/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-loading-completion-20260922`,
alias `build/research/application-host-loading-completion-20260922`. Verify X5 APFS
UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. No large artifact copy/build. Allow1GiB
observed decrease including concurrent source growth; runtime stop768MiB, closure
stop896MiB, leaving128MiB margin. New logical data64MiB, metadata archive32MiB.
Keep original40GiB external/6GiB internal reserves; runtime floors44GiB/7GiB.
Project source remaining14GiB stream/2GiB terminal/1GiB metadata commitments before
running. No deletion, source restart or reserve reduction.

Preparation/verification are read-only on closed tasks. Pin actual working inputs,
current tool versions/OS capacity, minimal environment and exact commands. Compare
runner predicates and monitor loop to the previous version; saved controls remain
valid only for unchanged bodies, and new artifact-path gates get finite positive/
negative controls before execution. No new process-signal probe is needed.

Root mutable paths: this plan, APPLICATION_HOST_LOADING_COMPLETION.md,
docs/evidence/application-host-loading-completion*, tools/prepare_application_host_loading_completion.py,
tools/run_application_host_loading_validation3.py, tools/run_application_host_loading_tests3.py,
tools/finalize_application_host_loading_completion.py, own CURRENT_WORK/RESEARCH_MAP
paragraphs. Root author owns edits; no independent reviewer is available.

After execution reverify protected/root/candidate/package/prior artifact bodies,
modes, mtimes and membership as applicable; preserve all old outcomes. Archive
only new small evidence with full bytes/mode/nanosecond-mtime/membership checks,
referencing the verified2482-file regular artifact archive. Separate Native,
package, archive, independent comparator/contract review and app/device gates.
Publish partial failure if needed. Confirm all task processes terminal/absent,
update handoff and commit the coherent checked increment with existing hooks.
No changes to safety/model settings or existing incidents. Root promotion, real
providers/timing/window/input/audio, later typed child retention, whole Catalog53,
full game and clean-Mac acceptance remain open.
