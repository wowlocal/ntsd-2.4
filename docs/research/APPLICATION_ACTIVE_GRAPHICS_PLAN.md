# Independent active camera, drawing and impulse comparison

Status: investigation. Parent ddf339f accepts seven contact stages after control
and physics. This extends the [whole active body plan](APPLICATION_ACTIVE_BODY_PLAN.md)
through camera-background, world-drawing and post-draw-impulses. Consumer: current
own attachments output and the subsequent lifecycle/commands/HUD/output, on the
path to a complete match and standalone native game.

Planning checkpoint, 2026-09-14: only this plan and navigation are being committed.
No active graphics comparison implementation or Native candidate has been run
for this card. All ten remaining body stages are still unaccepted. Counts below
describe saved evidence and required future checks, not new Native acceptance.

## Question and evidence

Use both immutable original-active-gameplay fixtures,48 calls each. Compute
camera bounds/target/smoothing and BG animation from current fields, then all
ordered graphics requests/read masks/clipping and diagnostic/impulse effects.
Facing1 requires width from the normal unoffset Frame.pic sheet, independently
of the mirrored sheet chosen by Frame.pic+Actor318. Sprite destination y includes
integerY; depth ordering, blink and frame/sheet selection come from own state.
Do not reuse source event positions/order for a different own trajectory.

Reference EXE SHA2563f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c;
VC80 c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d;
lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba.
Read [WORLD_CAMERA](WORLD_CAMERA.md), [WORLD_DRAWING](WORLD_DRAWING.md),
[WORLD_IMPULSES](WORLD_IMPULSES.md), arithmetic/coordinate precision,
[LIB_RUNTIME](LIB_RUNTIME.md) text replacement, accepted neutral effects and
archived constraints.
Saved Unicorn CW023f, synthetic addresses/API/DC/HRESULT responses are controlled
evidence, not observed Windows/device behavior. No original game/emulator or
historical capture/auditor is executed by this card.

Reuse the accepted low-level independent bitmap/read/clip expansion, resource
resolution and graphics/journal comparison. Keep neutral rules strict and
unchanged; new active equations belong in new comparison files. Raw source
Object/BG bitmap references must join the existing catalog token mapping before
comparison; current own opaque resource identities retain their owners.

## Required comparison and boundaries

- Reproduce all288 source full pool/global/BG endpoints and masks, available
  checkpoints and relevant helper arguments/results/return PCs, and complete
  grouped/primitive effects. The prior inventory reports3072 camera,1872 drawing
  and336 impulse grouped events per variant; these are finite observations,
  not rules or own expected values.
- Preserve all864 source bitmap+0c reads marked undefined per variant. Compute
  with the actual declared backing and retain masks; unknown does not become0.
  Blt commands/source-color ownership do not establish final device pixels.
- Compare all288 own endpoints consecutively from current own attachments,
  whole match/storage, BG/Frame/bitmap/image/library/CRT/music owners, caller
  local record, chronological journal and graphics resource/DC generations.
- Diagnostic text reads World slot10 even inactive, six signed bytes in original
  order. Pristine SetBkColor and installed SetBkMode1/retainedDC remain distinct.
  Impulse count/hitstop gates precede arithmetic; unknown scratch stays unknown.
- Retain both contact methods and nested physics/control/input/body preflight,
  late failure rollback and same-ticket retries. Add discriminating controls for
  actual new camera/drawing/ownership branches and causal unknowns.
- Any newly required reachable path extends the equations before acceptance.
  No mismatch is excluded, no expected after-state enters Core, and no old mask
  or fixture is modified. Full ABI/instruction stores are not inferred from
  endpoints or PC counts. Missing middle heap snapshots remain explicit.

## Ownership and execution

Root owns new OriginalApplicationActiveGraphics*.swift files, an optional
observer in OriginalApplicationActiveContactsTests.swift, this plan/result/
evidence and CURRENT_WORK/RESEARCH_MAP links. Core, existing neutral utilities,
previous fixtures/resources and archive stay unchanged. A Core discrepancy
requires explicit evidence and a plan amendment. Existing independent reviewer
owns new numbered read-only reports; author checks are not independent review.

Clean1000 Native inputs are pinned in
build/research/application-owned-active-graphics-native-20260914/initial.json.
Its SHA256 is333e581d19e9daf6116261558010e0e1f75c227e775fd8a9a496ad4d4330f636;
base isddf339f898d848e0cef2084bc92cc746f9dd66ca and recorded dirty state is empty.
The original plan1.md and initial.json remain in task-owned external evidence
storage; they are not shipping resources or newly published acceptance evidence.
Freeze actual modified files before each Native run; select the two new graphics
methods and two contact regressions when the observer changes. Other methods
are inherited only by exact unchanged pins. Reuse existing freeze/job/verification/
archive tools; retain failed candidates and an exact restoration path.

Use task-owned X5 storage, UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,
40GiB X5/6GiB internal reserves, task bound40GiB,3600seconds per run, initially
three candidates before diagnosis/revised plan. Verify live process identity
before action; never restart for silence. Source-first comparison, Native,
independent review, package bytes/modes, archive readback and publication joins
are separate gates. User authorization for completed commits remains in effect.

Before implementation, resolve access to actual chronological graphics commands:
middle snapshots expose graphics owners but the command list is on PendingReturn.
If observing it requires changes beyond the optional contact observer, amend the
owned paths and regression selection first. Never compare predicted commands as
if they were actual output. Exact candidate commands and output/job paths must
be frozen before execution; this planning checkpoint is not an execution record.

Existing safety incidents remain open without retries or rerouting. After future
acceptance of these three stages, seven further body stages would remain open.
Currently all ten, later transient lifetime, full tick/match/game and actual app/
device/Windows/clean-Mac acceptance remain open. EXE envelope is not recalculated.
