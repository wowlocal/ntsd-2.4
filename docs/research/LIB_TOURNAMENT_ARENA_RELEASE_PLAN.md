# Retained Tournament arena release — finite acceptance

Recover the nonempty40c0e0 release path inside whole434349 Tournament preparation,
including replacement of the retained replay allocation and actual432ab0ret12 /
422ab8ret4. Continue the constructor-backed controlled parent established in
[LIB_TOURNAMENT_PREPARATION](LIB_TOURNAMENT_PREPARATION.md). This closes a real
resource-lifetime dependency of subsequent Tournament preparations; it does not
claim a played intervening fight or the complete initialized application.

The reference is the pinned original NTSD EXE, bundled lib.dll, VC80 CRT and
original DAT/BMP/DIB inputs from that milestone. Original instructions execute
under Unicorn2.1.4/CW023f. Allocation, time and COM/GDI are declared research
responses. Instruction, memory, stack, mask and helper observations establish
which live layer surfaces and wrappers are consumed, their release/free order,
the first-pointer clear and retained stale pointer bytes. These observations are
not Windows heap/device behavior. No damaged control/protection structures,
fault continuation, unrelated targets or shipping EXE/DLL execution are involved.

Freeze17 independent three-call sequences before capture. Each first call is the
accepted controlled confirmation for one ordinary arena, and actually constructs
all its layers. The second retains that World/Actor/catalog/bitmap/replay state
and supplies an explicitly declared menu27 confirmation selecting special99.
The third retains the resulting state and selects the original ordinary arena.
Only the declared menu/arena/random-selection fields and ordinary button/call
arguments change between calls; no source result state is supplied to Native.
These are controlled post-match menu boundaries, not simulated match outcomes.
The same VM retains all source allocations and masks throughout each sequence.
New wrapper/replay allocations use distinct declared tokens; dead allocation
bytes remain available solely for research observation.

Vary the declared surface Release return among0,1,17 and0xffffffff across
sequences. Recover whether that result affects subsequent free/clear/load.
Cover all302 ordinary layers, the30-layer maximum, repeated paths, and the
zero-first-pointer path after release with retained stale tail pointers. The
third call must allocate fresh wrappers rather than free those stale tails again.
Keep builtin special arena resources and shadow ownership unchanged.

Acceptance requires all51 complete calls, complete before/after/point bytes and
masks, all400 Actors,101 backgrounds, globals, bitmap/replay ownership and ordered
API/free requests. Independently rebuild native state from declared inputs and
the accepted resource loaders. Compare whole replay allocations, free-before-
calloc order, helper ABI returns and real ret12/ret4. Verify each bridge changes
only its declared fields. Audit actual release reads and writes against preceding
constructor outputs; do not infer native inputs from unknown bytes.

Test native rollback after a surface release, wrapper free, later allocation,
completed replay and outer return. Buffer platform effects until the enclosing
call commits. Retain the existing98-call preparation regression and all accepted
fixtures unchanged. Require lossless transport, full raw/packed bytes/JSON/SHA,
isolated package and protected pending/foreign input checks before publication.
Preserve any failed attempt and exact error; stop retries of any safety refusal.
The existing long catalog source process must not be restarted for silence.

Team Tournament/War, the own initialized catalog/library/application join, full
played match, window/input/audio timing, Windows and clean-Mac remain open.
