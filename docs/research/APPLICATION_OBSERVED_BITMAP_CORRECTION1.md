# Observed bitmap Host — correction1 validation

2026-09-26. Correction1 explicitly supplies `late:false` at the two test helper
calls that collided with XCTestCase.run(). It preserves the original failed
candidate, build diagnostics and verified archives. [Plan](APPLICATION_OBSERVED_BITMAP_CORRECTION1_PLAN.md),
[original implementation and failure](APPLICATION_OBSERVED_BITMAP.md).

Preparation34184 finished with exit0 in12.042s. All2268 baseline files were cloned
with body/mode/nsmtime verification and distinct regular inodes. Only
OriginalApplicationObservedBitmapTests.swift differs: two invocation expressions,
no assertions. Every production byte, older test and fixture is unchanged. The
corrected Swift file parses. Selected methods and per-method limits are bytewise
identical to the original69-method selection. Candidate manifest SHA256:
`a3cdcc93f350578cfdd744f9accb1eaac2f5e6f7a18c415b57fb26beb009dda2`.

This remains the whole first-menu physical bitmap integration requirement. A
separate prepared replay of actual API values checks Core state; physical pixels
and masks compare with the accepted source-color owner. Other API replies remain
explicit source controls. Native resources and test-owned wrapper addresses do
not establish Windows device or heap behavior. No original/emulator execution,
historical producer restart or expected-value change occurred.

Fresh build/package, all69 exact results, protected inputs, patch and full archives
are separate gates. Independent review, root promotion, actual front rendering,
general Blt/callback/rectangle provenance, exact zero presentation rectangles,
diagnostic UI/other providers/loading/Windows/input/audio/clean-Mac/full match/game
remain open. NTSDApp remains Practice; EXE envelope is not recalculated.

## Verified build and live comparison snapshot

Build35275 completed with exit0 in299.584s; sampled
process-tree peak6673022976 bytes. No guard, signal or remaining process.
The fresh package verifies201 Core/61 Reference/7 MacPlatform/282 test sources,
385 fixtures and1301 runtime files. XCTest binary66,213,704 bytes, SHA256
`6b8174851ffbc1737a115a8438dd4462ccf126ac5ad4cddb05ec5c121ec651c0`.

All five new methods pass. The whole first-menu path serves425 actual Native
bitmap requests and retains25 surfaces. Core state agrees with the separate
prepared replay; every physical pixel/mask agrees with the source-color owner.
Late-publication failure leaves the Host unchanged, and retry repeats no physical
bitmap operation. Missing MENU_CLIP produces the required message/debug order
once across repeated injected rollback. Output extents/conflicting bytes reject
atomically; stale Host/foreign/cancelled/reentrant controls and retained context
lifetimes pass. These remain the declared finite Native controls, not Windows
or whole-front raster acceptance.

[Progress publication](../evidence/application-observed-bitmap-correction1-start.json)
records17/69 passing methods at2026-09-26T16:56:39.972493+00:00. Queue46279
was verified live by PID/start/command/cwd ontest-18; it was not
restarted. Publisher48350 finished with exit0 in15.723s,
rechecking root1034, prior2257, failed baseline2268, corrected2268 and source55.
The150-member PAX progress archive verifies every body/mode/nsmtime/name.
The original failed build and all its archived evidence are unchanged.

NEXT: observe this same queue and current child. Preserve and diagnose any
nonpass; otherwise finalize all69/package/one-file patch/full artifacts using
the already prepared finalizer. The live snapshot does not close full comparison,
complete artifact archival or independent review. No new correction or build
is justified while this queue is live. The full game goal stays active.
