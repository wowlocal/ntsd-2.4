# Owned selection through the application to Start

Status: finite Native selection/Start composition verified; the full application
card remains open. Continues the accepted
[human character/application join](APPLICATION_LOADED_CHARACTER.md) after
`1747cdd`, within the still-open [loaded-return card](APPLICATION_LOADED_RETURN_PLAN.md)
and complete [native game goal](../CONTINUE_GOAL.md).

The game behavior is the next current input/menu chain from human-ready
countdown147/selection0: jump accelerates the countdown, the count popup accepts
zero computers, six inactive seats still consume roster RNG, settings select
District, and Start retains the pending match-prelude child. This is a required
route toward the first Naruto/Sasuke match. Positive computer counts, reroll and
other modes keep their broader accepted shared regressions and application
dependencies; this route does not replace that scope.

The reference is the pinned original NTSD EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, original
DLL/resources and the saved [MATCH_SELECTION](MATCH_SELECTION.md) corpora.
Only saved data and Native Swift are used; the original, emulation and historical
producers/auditors are not run. No refused operation is retried.

The immutable bounded plan is
`build/research/application-loaded-selection-native-20260913/plan1.json`.
Two saved50-frame schedules follow the existing own34-character chains. They
contain98 new returned menu/Bootstrap children and two unreturned Start entries.
Current controller configuration drives WndProc messages; no Actor, countdown,
selection, phase or RNG field is filled from expected snapshots.

The shared `OriginalMatchSelection` supplies the recovered handler. A typed
pending match-prelude retains current full state, resources, music, semantic
locals, graphics/operation journals and the same suspended Bootstrap ticket.
The pending Start is not a completed outer return: preparation, held clear,
presentation and the enclosing tail remain its next consumer's work.

Independent input review found different RNG tables in the actual Bootstrap
parent and the pristine source. The current application's table must survive.
Comparison must independently verify the original RNG formula and event order,
then project the own table's dependent inactive-seat selections and music path,
including preserved string tails. This is an explicit Native composition with
different declared initialization, not a claim that its random results equal
the pristine corpus. The discrepancy and review remain preserved.

Independent contract/comparator review has completed for full
write footprints, source masks, state/checkpoint transitions, current catalog/
atlas/arena inputs, RNG/candidate/music event order and library graphics/sound
projections. The65-method filter retains all57 previous methods and six relevant
MatchSelection, LibSelectionStage and LibSelectionCommands regressions, plus
two new application methods. Candidate1 freezes962 actual files; Native1 is
terminal:63 methods passed and both new methods failed at primary frame21.
The sound comparator incorrectly reused human45560c for confirmation455610;
Core already uses the correct resource. Saved helper return PCs and original
call-site bytes prove the distinction. Review4 preserves the earlier review gap;
the full failed log/candidate remain immutable. A separately reviewed test-only
correction keeps human45560c, binds selection455610 and checks distinct live
startup owners. Review5 independently accepted the correction for validation.
Candidate3 freezes962 files; Native3 is terminal with all65 methods passing.
Native2 was stopped during expensive XCTest reflection at pending Start;
its final log also exposes a shared-test counter61 expectation against actual0.
Boolean presence predicates and the wrapped-counter expectation fix only tests.
Review7 independently verified both corrections; all fullstate checks remain.
Native1 build325.52s/test546.788s and its exact two failures remain preserved.
Review found a duplicate local test name
before compilation; its original draft and mechanical rename are preserved.

The independently computed own inactive selections are36,37,32,41,18,31 and
the last selected music path is `bgm\stage5.wma`. The pristine source retains
24,38,39,32,22,36 and `bgm\boss2.wma`. Native3 verifies these projected expectations in both complete own chains.
Both use six tagd7 draws followed by29tag1 draws; selected
music does not start playback in this stage. The existing music owner survives.
Source comparison covers768 body checkpoints per chain, not1513: the larger
historical count also includes other parts of the enclosing reference call.

X5 UUID/reserve and both prior terminal jobs were checked before creating the
task area. Limits are40GiB,3600seconds per Native run, three candidates and the
unchanged6GiB internal reserve. Preserve all960 baseline files,383 fixtures,
471 package files, failed candidates, expected bytes and masks. Tests, package/
archive integrity, independent final review and exact Git delivery are separate
gates. Full application correspondence, match preparation/gameplay, backend
publication, real window/input/audio, Windows and clean-Mac acceptance remain open.

Native3 passed65 bundled release methods in564.928s/build325.72s. This
includes every57 previous method, six shared selection/reselect regressions and
the two new methods. Both50-screen chains reach Start:98 returned outer children,
two pending children and1536 body checkpoints. The rollback method repeats the
primary50-screen chain as a parent; it adds no unique original source coverage.
Latefinalrosterdraw/music/arena text/checkpoint/publication failures and the
return-only API rejection retain prior commits; fresh retry retains Start once.

All962 current/frozen Native files,383 unchanged fixtures and471 package files
were verified. The full successful candidate archive and the four differing files
of failed candidate1 also supply the two differences of interrupted candidate2;
restoration maps verify all962 paths for both. Full frozen candidates1/2
also remain in place. Separate evidence/archive/final review and exact Git gates
are recorded in [application-loaded-selection.json](../evidence/application-loaded-selection.json).
All three Native jobs are terminal; do not rerun them for new report formatting.

Next, continue this pending child through actual prelude, enabled music, District
preparation/replay and the true enclosing return, then its next gameplay input.
Do not replay the prior menu body or replace its own RNG, choices or path. This
finite stage provides no new original execution, host backend, window/device,
Windows, clean-Mac or full-match acceptance.
