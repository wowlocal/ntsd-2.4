# Installed library control hook: finite comparison plan

The finite comparison is now complete in [LIB_ACTOR_CONTROL](LIB_ACTOR_CONTROL.md).
The plan and declared input domain below are retained. A subsequent read-only
catalog census finds no85/86 among the original loaded frame states; the new
branch inputs here remain synthetic and do not establish natural reachability.

Recover the complete413080..4143cb/ret8 control call with the bundled
41408b->10001125 hook installed by its actual DLL entry. The reference files
are the pinned NTSD2.4 EXE and bundled lib.dll from the original distribution,
with SHA256s listed in [LIB_RUNTIME](LIB_RUNTIME.md). The earlier pristine
control fixtures remain unchanged. This study does not establish full startup,
natural reachability of every synthetic input, a match or Windows/device output.

Use the committed whole-entry installer on Unicorn2.1.4, retaining its CPU and
actual installed code. After its pre-CRT stop, create controlled Actor/Object/
Frame/global inputs in separately declared research storage. The existing
Actor constructor runs with its declared memset adapter. Whole control,
input/combo, RNG and sound accumulator instructions then execute without game
helper stubs. Library installation and the controlled Actor setup are distinct;
no old initialized gameplay snapshot is supplied as a library-enabled parent.

Low-level observations are required because the hook consumes live EDI and
x87 stack values produced by earlier original instructions. Keep actual entry/
continuation registers and FPU contents, complete Actor bytes/masks, Object
immutability, full-global hashes plus source stores, ordered effects, helper
returns and normal stack/register restoration. Mark native arithmetic/storage
comparison separately from source process-wide FPU status observations.

Static findings to verify dynamically:413b3b/413d72 paths clear EDI before
the hook;413f90/413f97 load zero and one. The library routes state5 to the old
block, other ordinary states to414243, and states85/86 through two ordered
10001178 calls before414243. Its facing checks use byte inequalities against
each direction value, not a blanket normalization to boolean. Its comparison
against EDI must retain the zero-frame gate. State85 adds one to the current
frame;86 still executes an add of zero. Later dvx/dvy/dvz must use the resulting
frame and original ECX Object backing. These are hypotheses until source
execution and native comparisons verify them.

The initial finite corpus has7168 whole controlled calls:

-4096 state85/86 cases: four frame indices, four facing bytes, all sixteen
 left/right combinations from0/1/2/255, eight finite vx values including both
 signed zeros, signed minimum subnormals and signed largest finite values.
-384 routing controls: states3/5/84/87, four frame indices, two facing values,
 four ordinary left/right combinations and three vx signs.
-2304 same-call transitions: nine earlier frame paths, all128 held-key masks
 and each new state for later selected frames, retaining the original initial
 frame state. These test composition before reaching the hook.
-384 later-frame velocity controls at explicit CW037f, varying both new states,
 vx boundaries, facing and dvx/dvy; the main corpus uses initialized CW023f.

Native must use its own Actor/Object/globals and the recovered branch rules.
Keep a separate public library-enabled control operation sharing the existing
whole control implementation; the pristine entry remains a retained control.
Do not route a whole initialized match through this one hook before the other
library dependencies and their ownership are joined. A missing newly selected
frame must reject after the staged facing/frame updates and roll back the whole
native call. External callbacks remain buffered by the enclosing transaction.

Acceptance requires every complete source case and normal return to match
Native, source reconstruction from recorded stores, exact artifact packing and
preservation of every prior fixture pin. Check the retained pristine whole
control corpus and late rollback after the implementation change. Run a final
packaged comparison without raw overrides in an isolated native-only package;
exclude unrelated unfinished stage-command work. Broaden this finite set only
when actual observations expose missing behavior, a discrepancy or an evidence
gap. Preserve any source fault/refusal and its exact context; a rejected fault
is never a successful native match. No control-pointer corruption or protection
bypass is part of this study.

Previous goal turn made progress in5feeb80 and5a28b0a. Current jobs, immutable
source pins, failures and acceptance are recorded in
`build/research/lib-actor-control-work.json`.
