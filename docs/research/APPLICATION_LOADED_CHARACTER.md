# Owned character selection through the application loop

Status: finite Native human-menu/application composition verified.
Continues [the repeated loaded cycle](APPLICATION_LOADED_CYCLE.md) after
`11446d8`. The complete [loaded-return card](APPLICATION_LOADED_RETURN_PLAN.md)
and standalone native game goal remain open.

The behavior is human character selection from the actual Bootstrap's fourth
loaded input child: preserve the retained attack, initialize menu3, join two
players, choose Naruto and Sasuke by generic catalog traversal, confirm teams,
and finish each new enclosing application iteration. The pinned original NTSD
EXE SHA-256 is `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
The accepted [CHARACTER_SCREEN](CHARACTER_SCREEN.md),
[FRESH_CHARACTER_MENU](FRESH_CHARACTER_MENU.md),
[LIB_CHARACTER_ROSTER](LIB_CHARACTER_ROSTER.md), input and menu-return studies
supply the recovered behavior. This task executes only Native Swift and reads
saved reference data. It does not execute the original, emulate instructions,
restart historical producers or retry a refused operation.

Work and immutable plan: `build/research/application-loaded-character-native-20260913/plan1.json`.
The plan and comparison amendment define 68 human screens/outer returns across
two allocation backings; the first screen in each completes the already-created
fourth loading ticket. Thus only 66 further loading entries are new. The input
schedule comes from the original 34-case corpora, but current controller files
resolve each seat/button to WndProc keyboard messages. Expected snapshots never
seed the application, its Actors, selection, latch, phase or catalog.

## Ownership and comparison

The existing loaded-menu session branches to the shared library character
handler after the common music/resource gate. Cached startup still writes the
current menu into the previous-menu word. The comparator verifies that write
separately before comparing the human body. Current match bitmap records supply
portrait bytes; the retained catalog provides stable ordinal/token/surface
bindings. Constructor history is not copied over a live bitmap. Mode screens
and the return/outer transaction keep their shared paths.

The old pristine character environment differs from the actual application's
library text and loaded sound owners. The comparison therefore has explicit
parts rather than a claim of a new whole-original application trace:

- Full World/400 Actor/global transitions and five semantic locals at all 12
  body checkpoints. Entry equality covers the complete possible human write
  footprint, including stores that wrote their old value. Other owned bytes and
  masks must survive unchanged.
- Once-per-chain input joins for all 137 Object eligibility/name/portrait
  bindings and 829 bitmap ordinal/path/record/mask entries. These are comparisons
  against original saved catalog bytes, never Native inputs.
- Saved draw ordering/coordinates, clip and Blt results, with current bitmap
  raw read values and independent defined masks. Unknown words stay unknown.
  Declared Native fill backing is distinct from original private stack backing.
- The installed library's transparent SetBkMode1 replaces pristine SetBkColor;
  remaining GDI text/coordinates/colors and order compare. Six original
  confirmation sound requests acquire 18 methods from the actual current
  WinMain sound owner per chain.
- The whole returned model/state preserves the checked body and performs the
  held clear. The five return checkpoints and present request use the actual
  declared display mode. Independent graphics projection verifies all appended
  commands, resource generations and retained source colors. This is not a
  measured Windows raster or real audio output.

Late failures cover body checkpoints, portrait Blt, text output, present, return
and outer time/sleep/final commit. A separately declared Native sentinel changes
one current portrait's unknown count field to a known -7 and checks that drawing
reads that current record. A controlled menu1/selection1 input must stop at the
explicit selection-stage dependency and roll back. These controls add no new
original game-reachability observations.

## Validation and boundaries

The frozen validation plan selects 57 release methods: every previous 51 method
name, two retained pristine CharacterScreen methods, two LibCharacterRoster
methods and two new application methods. The prior cycle test retains its
original fourth-input frontier using an explicit observer stop at the first
character checkpoint. Its previous accepted source/candidate is immutable.

X5 UUID and the unchanged 6 GiB internal reserve were checked before creating
the task area. Limits remain 40 GiB, 3600 seconds per Native run and three
candidate rounds before contract diagnosis. Preserve all 383 old fixtures, every
failed candidate, source masks and prior accepted studies. The independent
contract review identified and corrected the cached-prefix comparison,
idempotent-write footprint, undefined-word reader and full-return checks before
a Native run. A raw-deflate saved-data reader error is also retained separately.

Candidate1 compiled in326.85s and ran57 release methods in365.316s.56 methods
passed, including the new rollback test. The two human chains both executed,
but their method failed34 assertions: its comparator incorrectly required Flip
for every saved return. The A5 source uses displayMode1/Flip; the control source
uses displayMode3/Blt with rectangle bytes. The corrected test independently
projects the full present event from each source's input globals and separately
from the current own globals. Core, source expected and the other four Native
paths remain unchanged. Candidate1, its terminal log, the incorrect assertion
and the missed pre-run review case are preserved.

Candidate2 passes all 57 release methods in365.385s, build319.31s. The two new
methods pass in16.787s (rollback) and5.572s (both human chains). Both chains reach
selected17/21, status3/3, team0/0, selection0 and countdown147 through current
input. Together they compare68 Bootstrap returns and816 body checkpoints.
Each chain has312 draws and18 sound-owner methods; A5 has1560 bitmap reads/
312 Blt, while the control backing has2840 reads/568 Blt. Raw values and masks
are compared separately, so different backing does not become a common known
value. All68 saved return events also match the independent source-input
projection:34 displayMode1/Flip and34 displayMode3/Blt with complete rectangles.

All960 current/frozen Native files,383 unchanged fixtures and471 packaged files
were verified bytewise. The complete Native archive was read back by member
name/type/size/mode/body/hash. The separate evidence archive retains the plans,
reviews, saved input documents, both terminal runs and the incorrect candidate1
test; its other959 files are byte-identical to candidate2. No old expected or
Core byte changed for the second candidate. Evidence and final review are in
[application-loaded-character.json](../evidence/application-loaded-character.json).
Both study SwiftPM/XCTest jobs are terminal; completed jobs must not be restarted.

This verifies the finite Native composition. Computer/arena
selection, preparation/gameplay, complete original application correspondence,
backend publication, real window/input/audio, Windows and clean-Mac acceptance
remain required. The next consumer after the human-ready state is the existing
selection/countdown handler through the same retained application owner.
