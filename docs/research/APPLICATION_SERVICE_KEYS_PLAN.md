# Application dispatcher service-key prefix

The finite prefix comparison is now recorded in
[APPLICATION_SERVICE_KEYS](APPLICATION_SERVICE_KEYS.md). The whole initialized
dispatcher and device join below remain open.

Finite independent dependency while the48-call active captures run: compare
the original43e9db..43ea95 prefix before any clear, loading or mode callback.
The reference is the pinned NTSD2.4 EXE, SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
under Unicorn2.1.4. Acquired300-byte keyboard state and existing sequence0..3,
diagnostic0/1 and dispatcher mode0..2 are declared controlled inputs. No Actor,
pointer, security structure or fault stimulus is changed. There are no imports
or helper calls in this prefix. Low-level observations recover key-scan order,
sequence retention/reset, global store order and function-key precedence.

Acceptance requires full prefix exits, all ordered stores and unchanged
remaining global/keyboard bytes. Cover every single pressed key0..299 from
all four sequence states and both diagnostic states; cross all combinations
of A/B/C and F1/F2/F3 with those states and each dispatcher mode. Add retained
multi-call sequences to distinguish held keys from press-edge processing.
Source instruction inventory must separate alignment and the unexecuted stop.
Native must use declared inputs, not expected outputs, and preserve its state
on a late observer failure. Such an observer is a native transactional boundary,
not a source exception. Preserve all existing fixtures and verify lossless
raw/packed JSON, lengths, hashes and vendor pins before publication.

This is not the whole43e9a0 dispatcher, initialized own input acquisition,
Windows/device behavior or a new active-gameplay match. The actual next clear,
loading callbacks and fixed World458b00 still require composition. In
particular, the current own harness constructor uses a separately mapped World;
the literal458b00 in43ecb5 must gain real constructor/ownership provenance before
an initialized outer-dispatcher claim. Do not copy an expected World snapshot
or replace that instruction to conceal the distinction.

The adjacent236-instruction43e9a0..43ed01 static audit also shows live455608
surface reads and separate mode checks after callbacks. Its caller argument
is not read by the decoded body. Those static findings do not prove dynamic
whole-dispatcher coverage or make its loading routes complete.
