# Active result layout from owned recording state

Accepted finite comparison of both48-call schedules through the recorder-selected
indicator entry422944 and result-layout endpoint422994/SP1000e9bc. This connects
the current owned recording result to the final output stage. [Plan](APPLICATION_ACTIVE_LAYOUT_PLAN.md),
[evidence](../evidence/application-active-layout.json).

## Analysis

All96 saved source calls have indicator450b84=0 but playback44d030=1. The source
skips playback information because it is nested under the indicator gate. Full
before/after component references and resolved global/pool/memory/saved/early
bytes, masks and owners are unchanged. Recording and output joins are checked.
There is no event, helper, primitive effect, global store or watched caller-stack
access. Two executed addresses422944/42294b precede the unexecuted422994 stop;
this is an address set, not an ordered trace. Source CW023f/FPSW0or4000/tagffff
and scratch/spawn/stage words survive. No new Native ABI/FPU equivalence is claimed.

The independent finite rule requires indicator continuation and known zero450b84,
then preserves every byte and mask without reading playback, mode, elapsed,
bitmap or target. Nonzero flags and table continuation remain explicit boundaries
of this finite comparison. Existing599 controlled table/indicator matches and one
source-fault rejection retain their distinct outcomes and wider contracts.

## Implementation and validation

Three new test-only ActiveLayout files compare96 source and96 own endpoints from
current recording snapshots, complete state/masks, replay/memory/graphics owners,
resources/music/backgrounds, menu-local and chronological journal. Misplaced
layout annotations and any primitive effect within the interval fail. Parent
return and complete graphics-command comparisons remain included.

A separate public recorder call starts from current own notices state and native
saved-playback/context ownership. Its output matches the actual recording snapshot;
its actual Result.continuation is then passed to public layout. Body forwards
that result directly too. Application checkpoints do not expose continuation or
Body.Caller.formatter: direct controls/static forwarding supplement this gap and
do not claim a newly observable field or recovered enabled formatter lifetime.

Controls distinguish table continuation, positive/negative nonzero indicators,
unknown causal indicator, and unknown unread playback/mode/elapsed/bitmap/target.
Public layout produces no calls or events with nil stage/target/formatter and
retains supplied opaque372-byte formatter bytes/masks. No source SP68 or private
caller backing becomes an own input.372 is a known extent, not a C-array capacity.
The menu-local snapshot remains separate from gameplay formatter storage.

Four new late failures at layout or beforeCommit roll back entry/environment and
pending return. Two retries in the same session reproduce complete returned state,
owners, journal and graphics. Parent rollback checks remain selected.

Five release methods passed in280.916s/build362.24s:
two new Layout, two retained Recording and the full controlled ResultLayout test.
The latter retains599 whole matches, one separate source-fault rejection,
386781 events,25661 Blts and four rollback trials, including the late third
playback-overlay font pass and missing formatter/stage/indicator-target controls.
163 unchanged method identities are inherited, not rerun;168 unique total.
All1021 actual/frozen Native files and485 packaged files were verified;1018 old
Native files,383fixtures and102resources are unchanged. Both archives have exact
body/mode readback; final publication review is linked separately in evidence.

Production Core, expected/masks and original assets were not changed. No original,
emulator, historical producer/capture/auditor or prior terminal test was executed.
Current Native job1 is terminal in `build/research/application-owned-active-layout-native-20260914/`;
do not restart it. Saved source and own library-enabled Native remain different
environments; there is no actual Windows/device measurement here.

## Next boundary

Saved [output planning findings](../../build/research/application-owned-active-layout-native-20260914/next-output-findings1.md)
identify multiple active sound slots and missing middle Object/Frame snapshots.
An initial planning reader wrongly equated the output component set with the
enclosing return set; its error/script remain preserved beside reader2. This
planning inventory is not output acceptance and did not rerun Native or original.

Compare whole output from current layout state: mode label, notice/volume,
presentation, queued sound, then actual dispatcher/application return. Output
and full active tick remain open. Enabled table/indicator branches outside these
schedules, full helper ABI/FPU/stores, formatter observability/lifetime, full match/
game, native window/input/audio, actual device/Windows/clean-Mac and prior safety
incidents remain open. EXE envelope was not recalculated.
