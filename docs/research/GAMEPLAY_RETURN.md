# First initialized gameplay output and both returns

Both fresh original chains now reproduce their entire accepted
[result-layout parents](GAMEPLAY_RESULT_LAYOUT.md), execute the remaining
output with sound enabled, and return from41bc90 and4246b0 on their retained
caller frames. Both native comparisons pass: each577173 complete records,
912855320bytes and masks,67state checkpoints and1614FPU checkpoints. Primary
checks703 helper returns across the joined chain; control checks711. Two new
immutable fixtures are accepted, preserving all186 previous pins.

## Reference and observable boundary

[oracle_gameplay_return.py](../../tools/oracle_gameplay_return.py) runs the
pinned NTSD2.4 EXE, SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
and actual VC80 CRT, SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
on the same initialized Unicorn2.1.4 CPU as the complete startup/menu/catalog/
selection/launch/first-gameplay chain. Instruction, memory, register and FPU
observations recover output ordering, queue ownership, mutations and return
restoration. No gameplay global, result word or saved frame is supplied to
select this continuation.

The418 distinct sound buffers come from the already loaded catalog and built-in
resource globals. Research COM method adapters are bound to their existing
object vtables at offsets40,3c,48,34,30; their numeric results are0. The binding
inventory is recorded separately. Full game state before output must still
equal the complete parent after-state. GDI/DC and presentation responses are
also declared boundaries. Neither these adapters nor source tokens establish
Windows/device behavior or host allocation provenance.

## Actual own output

Each continuation records794 ordered events,167 helper returns and no undefined
bitmap read. The source renders `VS mode (Difficult)` through four mutable font
passes:76draws,532reads,76clips and76Blts. All read bytes and defined masks are
checked against the owned bitmap records. The original recording notice uses
one actual VC80 format and the full GetDC/GDI/ReleaseDC sequence. Its timer
450b6c changes1→2. No volume key is enabled on this own path.

Presentation follows the notice. Primary mode1 requests Flip; control mode3
requests Blt with the original destination rectangle bytes. Enabled queued
sound then clears catalog flag4575a0 before the sound calls, sets pan−525 and
volume0 on the actual loaded buffer24001060, and executes Stop, SetCurrentPosition0
and Play0. Recording allocation and resource ownership remain live.

Thirteen original global stores touch32 distinct bytes;21 bytes change. The
complete global result is independently reconstructed from those stores.
Every other state byte, mask and ownership record remains identical. Source
store ordering is retained separately; native comparison uses complete globals
and defined masks plus ordered output events, not private x86 stack writes.

## Normal return and native transaction

| Boundary | ESP | Restored SEH link |
| --- | --- | --- |
|422a95 match epilogue entry|1000e9bc|1000efb4, still the match frame|
|422ab8/ret4 →424746|1000f000|1000f418|
|424746 held457580=0 →4287de|1000f000|1000f418|
|428805/ret4 →declared outer STOP|1000f42c|12345678|

Read-only entry observations at4246b0 and41bc90 establish their actual caller
frames. Both returns restore the corresponding four saved registers and SEH
links. The original executes all15 output-caller instructions, all12 match
epilogue instructions and all12 dispatcher epilogue instructions, including
three normal cookie checks across the output helper and both returns.
Primary observes568EXE+303CRT PCs; control576EXE+303CRT. Platform adapters and
the unexecuted terminal STOP are excluded. These counts are for this new
continuation, not all branches of the enclosing functions.

All1606 complete parent FPU checkpoints reproduce. Eight new output/return
checkpoints retain CW023f/FPSW4000; both entry and exit tags areffff. The resulting
audit has1614 checkpoints. This is source-emulator evidence, not a Windows or
host hardware FPU measurement.

`OriginalGameplayOutput.returnFromDispatcher` composes the accepted mode label,
overlay, present and queued-sound helpers, then performs the dispatcher clear.
Its native World/globals/memory commit follows the final observer. The reference
join uses independently rebuilt native state and native resource ownership;
source machine frames are assertions only. A trial throws at the final
dispatcher observer after all preceding output requests and must restore the
complete native output state. Callers must buffer external effects until their
whole tick commits.

## Verification and remaining work

[accept_gameplay_return.py](../../tools/accept_gameplay_return.py) checks both
complete parents, blobs, global reconstruction, unchanged storage, bitmap
reads, loaded sound inventory, event order, helper ABI, actual returns and FPU.
[verify_gameplay_return_artifacts.py](../../tools/verify_gameplay_return_artifacts.py)
independently verifies complete raw/packed bytes, JSON, hashes, every inner blob,
prior fixture pins and the ten pinned replay-codec vendor files.

Raw acceptance passes all3 release tests in44.480s/build177.49s: both own
returns44.059s and retained170-case controlled output0.422s. No source or game
correction was needed for those comparisons. The independent artifact check
verifies both full raw/packed byte sequences, complete JSON/SHA/lengths, all5515
inner blobs,188 current pins and10 unchanged vendor files. Reports are
[primary](../evidence/gameplay-return.json) and
[control](../evidence/gameplay-return-control.json).

Final packaged verification passes all3 release tests in43.474s/build167.94s,
without raw-data overrides: both own returns43.050s and the retained controlled
output0.424s. NTSDNative links. All source and SwiftPM jobs for this milestone
are terminal; neither source process was restarted.

Both source sessions11350/20649 finished with exit0. Neither was restarted.
The first native build found a comparison-variable name collision with an
earlier menu-return value; renaming the decoded gameplay corpus fixed it.
The corrected test-support build passes in110.84s. No game rule or expected
source bytes changed for that build correction.

A separate smoke check launched the newly linked NTSDNative executable and
visually inspected its Naruto/Sasuke/District window image. It ran the existing
OriginalMelee prototype, which does not use this new composition. It establishes
window launch/rendering only, not new-engine integration, input latency or audio
quality. Its image/log and all job evidence are in
`build/research/gameplay-return-work.json`.

The next bounded comparison is in
[CONTINUOUS_GAMEPLAY_PLAN](CONTINUOUS_GAMEPLAY_PLAN.md). A returned first match
and mode-dispatcher call is not a continuous match, the real43e9a0/outer app
loop, a Windows run, device/pixel/latency equivalence or clean-Mac delivery.
The full-game goal remains open.
