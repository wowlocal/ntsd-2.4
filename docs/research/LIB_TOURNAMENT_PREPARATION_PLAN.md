# Tournament preparation — finite acceptance

Continue the accepted Tournament bracket at434349 through CPU-seat assignment,
date/round replay name, Actor reconstruction and HP/state/team placement, arena
release/load, input reset, whole43d2c0 recording initialization,432ab0ret12 and
outer422ab8ret4. This is the next dependency of
[LIB_TOURNAMENT_BRACKET](LIB_TOURNAMENT_BRACKET.md), not a played match.

Reference artifacts are the pinned original NTSD EXE, installed bundled lib.dll,
VC80 CRT, original DAT/BMP/DIB files and the immutable accepted loaded-catalog
dependency. Unicorn2.1.4 executes original instructions at CW023f with declared
allocator, GetLocalTime, COM/GDI and file-image responses. Instruction, memory,
mask, stack, FPU and helper-return observations recover initialization provenance,
the exact operation order, pointer ownership and real caller returns. Research
execution is separate from the native shipping runtime and from Windows/device
evidence. No damaged control pointers, protection structures or fault continuation
are introduced. Preserve any source/adapter failure and stop retries of a safety
refusal. Revalidate live jobs; never restart the catalog capture for silence.

The old bracket fixture deliberately leaves most Actor bytes unknown. Whole
recording reads eighteen Actors, including inactive ones. A new controlled parent
must therefore execute4061d0 on all400 declared allocations before its first menu
call. Native must independently run its constructor over the same declared raw
allocation patterns. Retain Object bindings and untouched bytes/masks, compare
all constructor results, and then retain that state through the fresh menu chain.
This is a constructor-backed controlled parent, not an own full startup claim.
Catalog operand views may use already accepted source producer records only when
their immutable fixture/hash and pointer translations are explicit; Native must
rebuild them with its normal raw-resource loader. Replay configuration/strings
must have file-backed or explicit controlled-input provenance before execution.
Never promote arbitrary source bytes to known native inputs.

Freeze a finite manifest before capture: the existing human setup/assignment
sequence with its last call extended to a return; controlled confirmation entries
cover every17 ordinary arenas and special99, random-arena ordinary/special choices,
human/human and human/computer placement, round0/2/4 and the default suffix path,
retained HP/state values, enabled sound and network flag alternatives. Controlled
entries are declared menu boundaries, not claims of preceding played fights.
Only declared button/history stimuli and ordinary call-frame arguments change
between retained calls. Keep the exact accepted bracket corpus separately.

Acceptance compares complete World/400Actors/globals/catalog/resource/replay
bytes and masks, ordered events, semantic locals, original helper ABI returns
and each actual end. Check constructor and recording read provenance, all eighteen
recorded slots, full0x630e18 allocation and unchanged playback ownership. Verify
4347ba with its twelve pending argument bytes: [esp+1c] is rootSP10, the menu
pointer. Check actual original date formatting and retained local string writes;
do not import caller stack backing into Native. Preserve original RNG streams
FD/FE/FF, signed arithmetic and operation order. Native must reject unavailable
ownership and roll back after allocation, bitmap, replay or late-output observer
failure; buffer external effects until the enclosing call commits.

Require relevant retained menu and replay regressions, lossless fixture transport,
independent full raw/packed JSON/bytes/SHA checks and unchanged earlier fixture,
pending and foreign pins before publishing a verified milestone. Freeze each
accepted isolated package. Full match, Team Tournament/War, initialized library
catalog composition, window/input/audio timing, Windows and clean-Mac checks remain
open. Merely implementing or capturing preparation does not satisfy this plan.
