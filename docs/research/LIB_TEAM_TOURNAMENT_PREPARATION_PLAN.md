# Team Tournament preparation and retained arenas

Continue the verified [Team bracket](LIB_TEAM_TOURNAMENT_BRACKET.md) from436747
through menu0, date/round replay filename, four participant Actor initialization,
arena release/load, input reset, whole43d2c0 recording initialization and actual
436fb5ret12/422ab8ret4. The preceding bracket already executes CPU placement.
This study must complete preparation, including retained nonempty arena release;
it does not claim a played battle or initialized whole application.

References are the pinned original NTSD EXE, installed bundled lib.dll, VC80 CRT,
original DAT/head/small BMP/menu DIB and the immutable accepted loaded-catalog
dependency. Controlled Unicorn2.1.4/CW023f executes original instructions with
declared time/allocator/COM/GDI responses. Disassembly, memory/stack/register,
mask, FPU and helper-return traces recover participant and resource ownership,
initialization provenance and exact output/cleanup order. Reference execution is
development tooling, separate from Native runtime and Windows/device evidence.
No damaged control/protection structures, unknown after-state imports or fault
continuation are introduced. Preserve failures; stop retries of safety refusals.

Before any menu call, execute419e40 and all400 original4061d0 constructors over
declared allocations. Native independently reconstructs the same owned records.
Retain untouched bytes/masks and actual Object bindings. Whole recording reads
eighteen slots, including inactive ones. Source catalog operand views have the
accepted fixture/hash and explicit pointer translations; Native rebuilds137
Objects/101 backgrounds from raw resources. Required PE-initialized spans,
including44eed0's52 bytes, must have provenance before their consumer executes.
Keep the prior377 bracket source/Native contract unchanged and separately tested.

Freeze210 calls before capture: both fresh bracket chains (87 eight-human and69
mixed calls) with their last calls extended through preparation, plus54 controlled
confirmations. For each17 ordinary arenas, load it, retain complete produced
state while selecting99 to release it, then reload the same arena (51 calls).
Also cover direct99 and both ordinary/special random-arena outcomes (3 calls).
The resulting56 preparations exercise all302 layer releases and604 ordinary
bitmap constructors across the controlled three-call sequences. Fresh chains add
their actual arena loads. Counts are planned stimuli, not accepted observations.

Controlled confirmation inputs vary all-human/mixed/all-CPU participants,
nonidentity pair order, retained health/state/team fields, phase2/4/default replay
suffixes, enabled sound and network flags, and two declared dates. The four
participants remain valid original catalog entries with pair-preserving order.
The repeat calls change only declared menu/arena/random-selection globals and
ordinary input/call-frame words. No intervening fight is invented. COM Release
responses0/1/17/ffffffff are declared, with original handling observed unchanged.
Distinct wrapper/replay tokens retain live/dead storage and stale tail pointers.

Recover109 arena choice and10a/10b placement RNG, signed arithmetic, original
team lookup44d100, live HP/state values and all constructor results. Verify the
actual date suffix bytes from EXE, global44d034-before450bbc order, all arena
release/free/first-pointer-clear operations and full0x630e18 replay bytes/masks.
Old recording must free before new allocation; playback ownership survives.
Audit436afa while twelve caller argument bytes remain; distinguish frame-relative
stack words from their apparent ESP operands. Native string storage is produced
by native formatting, never imported from source caller bytes.

Acceptance requires all210 complete source/native returns, full World/400Actors/
globals/catalog/resources/library/replay bytes and masks, ordered events, complete
source store/read/checkpoint/instruction/ABI/FPU audit, all atomic source cases and
whole input/fixture/archive SHA verification. Test current-call rollback at late
release/free, bitmap allocation, completed recording and enclosing return, while
preserving earlier calls. Exercise unavailable ownership explicitly. Keep devices
and allocator effects staged until the enclosing call commits. Run retained377
Team bracket and51 Tournament arena-release comparisons on the same isolated Core.

Use a new copy of the accepted707-file package; preserve290 fixture pins,14
pending Native files,3 pending source files and6 foreign files. New large data,
builds and comparator temporary files belong to the task-owned X5 directory.
Revalidate independent catalog5 Python78863; never restart for silence or reduce
its6GiB reserve. Full played match/game/network, War, initialized catalog/library/
outer loop/app, window/input/audio, Windows and clean-Mac remain open.
