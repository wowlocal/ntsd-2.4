# Team Tournament bracket

Team Tournament now matches **377 finite source/native calls:375 whole returns
and two boundaries before436747**. OriginalTeamTournamentBracket implements
menu126 pair progression, menu127 four participant assignment, menu128 result
consumption and menu129 paired winner output. New chains contain87 eight-human,
69 mixed and166 all-CPU calls;55 additional entries cover48 CPU configurations,
three result fields, three winner timers and held Attack. These are controlled
menu chains and supplied post-fight results, without a played match claim.

See [the finite plan](LIB_TEAM_TOURNAMENT_BRACKET_PLAN.md),
[evidence](../evidence/lib-team-tournament-bracket.json),
[source producer](../../tools/oracle_lib_team_tournament_bracket.py),
[independent auditor](../../tools/verify_lib_team_tournament_bracket.py),
[native rules](../../native/Sources/NTSDCore/OriginalTeamTournamentBracket.swift),
[layout constants](../../native/Sources/NTSDCore/OriginalTournamentLayout.swift)
and [whole caller tests](../../native/Tests/NTSDCoreTests/OriginalLibTeamTournamentBracketTests.swift).

The behavioral reference is the pinned NTSD EXE, bundled lib.dll, VC80 CRT and
original DAT/head/small BMP/menu DIB resources. Controlled Unicorn2.1.4/CW023f
executes original instructions with declared API/COM/GDI responses and atlas
geometry. Actual41bc90 prologue precedes the declared4229cc menu tail; this is
not an own full tick. Disassembly and full memory/stack/helper traces distinguish
pair positions from catalog ordinals and preserve live ownership and output order.
Both436fb5ret12 and422ab8ret4 execute on375 calls. Human/mixed chains retain
all four assignments through the actual CPU placement, then stop BEFORE436747,
which has not executed. The Team frame SP is its own routine entry SP minus0xa08
(characterSP minus0xa18);
semantic originY20/originX28 stay separate from full source stack bytes.

Start initializes every bracket level to2, health500 and state-1, starts music,
and scans even pair positions. Empty rounds advance by2. A natural timer path
runs25/26 through80; Jump advances it. The all-CPU sequence computes three pair
contests to a winner. All16 placements of original ID/10==5 among four fighters
are tested at difficulties0/1/2. One special team wins directly; neither special
uses RNG105, both special uses106. Winning fighters each gain2 levels, losers1;
6 promotes to7. Tags107 then108 each use range15+18*difficulty and signed,
wrapped `(random+85-18*difficulty)*HP/100`. The first HP store precedes the
second RNG call. Independent reconstruction verifies51 CPU decisions and102
ordered HP stores, including all48 controlled configurations.

Any human opens four-seat assignment. Actual released/held CA history is supplied
at the419a60 input-producer boundary;431c70 does not initialize that history.
Occupied seats reject a fresh press, while held Attack readsCA1 and skips it.
Cancel clears all400 activity flags and20 assignments before reassignment.
CPU placement searches a free paired human/CPU position and retains sounds,
World flags, assignment words and Actor+368 bindings. Human/mixed calls stop
with counter5 after this placement. Match preparation, time, battle Actor/arena
construction and replay allocation are the next dependency.

Three controlled result entries1/2/-1 retain four supplied currentHP/maxHP/state/
team values. Defeated actors halve their own maximum HP before copying it to the
tournament globals:321 becomes160 and239 becomes119; living280/198 stay intact.
All12 participant result stores and six Actor halving operations independently
verify. Team matches promote their assigned participants; a draw uses10c to
choose a pair. Winner output retains two portraits, nonidentity pair order,
team label, timer thresholds20/21/50 and stop-music-before-sound ordering.
The second portrait uses selected[first mapped fighter+1], as actual436f38 does.
Three Start music graphs each retain a26-byte wide allocation.

Original layout44d120..44d317 is504 immutable EXE bytes/126 words with SHA
`ae205dff1239899386e0f76ff80dc3c8f22472b2b19ebb11ae02b1b3bbcc87c8`.
Native initializes these recovered constants and its RNG before comparing entry
state. It does not import expected after-state or unknown stack storage. All
original small/head resources are independently rebuilt; undefined bitmap count
masks remain undefined. Geometry compares drawing requests, not device pixels.
The three new setup sequences are declared inputs, distinct from the earlier373
setup source cases; those fixtures remain unchanged and pass Native regression.

The full audit verifies9062 points/7702700 records/
23169087260 bytes plus masks, 565555 stores/2333622
bytes, 509765 instruction reads/1960067 bytes
and8296 API reads/29448 bytes. It checks all377 atomic
cases, 23922 helper returns and3354 blobs/156775396
decoded bytes. Observed starts total3548: 3160 EXE,
51 lib and337 CRT. This is observed coverage,
not all branch outcomes. All2574 short controller labels and24 live
Random lists verify. Pinned cookie backing remains file provenance, without a
Windows heap/host claim.

Source capture completed in112.819s. Native raw **6 tests passed
in223.791s, build267.34s**: new377 plus retained373 Team setup and250
Tournament bracket cases, including their rollback tests. The new two tests took
96.690s. Late Start music, human assignment, second HP RNG and final mixed CPU
placement each roll back the whole current call and preserve prior calls.
Packaged **2 tests passed in94.072s, build0.38s** without raw
overrides. NTSDNative linked; no window or device was exercised.

The initial finite-manifest Python producer raised KeyError for absent actorWords
before any original execution; its error and producer are retained. Source and
Native comparison then passed without game/expected changes. Audit1 passed full
read/store/point checks but retained the ordinary Tournament label address44d31c,
so its separate label counter was0. Code review recovered Team's44d320; audit2
repeats the full audit and requires nonzero C and all1..8 labels. Both audit
versions/reports survive. Original expected bytes and Native rules were unchanged.

All285 prior fixtures remain immutable;290 current pins include five new transport
files. Full raw1821539102 bytes/SHA`0704dab739e8c61c73cf8eb70480e0e200c9b7d20d692386c52b174b9146b849`,
239677253 packed bytes, complete JSON and all707 package files/recursive
archive compositions verify. Transport deflation only packs research fixtures.
Two previously accepted terminal raw corpora and two completed Native input
archives moved byte-for-byte to task-owned
X5 with hash checks and canonical symlinks, preserving internal free space and
the running catalog's6GiB reserve. All current study jobs are terminal.
Catalog5 remains the same Python78863 process, last observed25 completed
Objects; the earlier20-Object byte reproduction remains a separate contract.

Next: whole Team preparation from436747, then War and own initialized catalog/
library/outer-loop/app integration. Full match/game/network, window/input/audio,
Windows and clean-Mac verification remain open. Reference execution is research
tooling and is absent from the native shipping runtime.
