# Own first43e9a0 and embedded World entry

Continue the actual own startup/message-loop required43e9a0 entry from
APPLICATION_MESSAGE_LOOP, without changing SP, registers, globals, resources or
stack backing between parent and continuation. Pinned NTSD2.4 EXE/VC80/resources
run on one Unicorn2.1.4 CPU. Normal SEH/cookie prologue instructions execute;
no control/protective storage is corrupted and no protection check is bypassed.
Observe the original art/query/clear helpers and staticWorld458b00 routing until
the first required front-resource operation. Win32/COM outputs remain declared,
not actual Windows/device delivery; earlier CRT/NLS remains the parent boundary.

Finite acceptance: eight fresh own chains include normal restore/minimize
callbacks and six ordinary query/clear HRESULT controls. Reproduce the complete
startup parent and preceding loop bytes/events. Compare ordered global stores,
owned query/clear fields and masks, actual globals/target/World, CW and actual
instruction starts. Preserve ALL source opaque local bytes and their earlier
store provenance; Native must leave private retained fields unknown rather than
copy them from the expected record. This is the accepted WinMain platform-record
contract, not full native equality for opaque private-stack bytes.

Run the original4246b0 prologue and selected early branch using actual static
World458b00, not the earlier controlled World22000020. Stop at the first required
resource allocation before supplying output. Native must enter its existing
front-resource consumer on independently PE-backed World and own globals, match
the prefix, then reject that required operation with rollback of the entire
pending message-loop iteration. A reached boundary is not a completed dispatcher,
resource allocation, World call, frame or game match. Other modes/loader routes
must remain explicit and must not be disabled to obtain a convenient return.

Preserve238 prior pins; atomic source cases; raw then packaged native comparison;
full raw/packed bytes, JSON, SHA, blobs, parent and vendor checks. Use isolated
committed6fb8ce4 native files plus owned overlays, excluding six foreign files.
Late surface/consumer errors must preserve own startup, prior callback, globals,
MSG/counter/baseline and buffered effects. Full dispatcher/editor/worker/menu,
CRT/NLS/library transforms, real window/devices/Windows, matches/all content and
clean-Mac acceptance remain open after this entry milestone.
