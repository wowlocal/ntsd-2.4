# Production catalog continuation from PendingCatalog

Status: the owned continuation and three finite twenty-Object comparisons pass;
independent final graphics-comparator review passes. The full application catalog
return remains open. [Implementation and validation](APPLICATION_CATALOG_SESSION.md)
record the precise boundary; this is progress toward the full native game.

The image prerequisite was completed in commit `0d0c419`. The owner task packet is
`build/research/application-catalog-owner-native-20260913/`, initially pinning 88
dependencies and all 905 Native files. Six explicit amendments define transport,
observed stores and test helpers. Its final union contains 425 dependency pins.
New work uses the verified X5 task directory, a 24 GiB task budget and the existing
6 GiB internal reserve. Reference/Native limits remain 900/2400 seconds and three
candidate rounds. Only Native and read-only saved-data checks were executed.
The predecessor image packet remains immutable in
`build/research/application-catalog-session-native-20260913/`.

## Existing evidence and precise continuation boundary

The behavioral reference remains the original NTSD EXE with SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, bundled
lib.dll and VC80 in the declared controlled Unicorn environment. This task reads
saved captures and original resource files as data. It does not execute original
code, an emulator, or historical producers/auditors.

Three audited nominal parents have contiguous twenty-Object checkpoints:
guarded parent 0 and nominal parents 1/2. The older complete catalogs remain
separate regressions with their declared scanner and device boundaries. They do
not prove a full catalog return from the current application chain. Candidate5
ended at the storage limit after 28 completed Objects with the next Object
pending; its terminal status does not establish a successful catalog return.

Independent preflight checked an important caller boundary in all three saved
twenty-Object captures. At PC 41269a the twentieth child has returned, but the
parent has not stored its slot or incremented the count. The catalog has 188
known bytes, nineteen initialized Object slots and count 19; slot 19 remains
unknown A5 backing. A future Native checkpoint must preserve that phase.
The five stores between the catalog allocation and constructor entry affect
private stack state only; they do not establish a new global catalog pointer.

The declared allocation is 81,273,768 bytes at logical token 70000020. The older
60000020 layout must not overlap live common-WAV storage. Allocation identities,
all child results and parent stores need explicit comparison; the old filtered
semantic event list alone does not cover those markers. Six other successful
common-loading variants have no corresponding nominal postcatalog capture here.

## Required image-input prerequisite

The completed data-only inventory found 669 distinct catalog image inputs:
665 original BMP files and four embedded DIBs, totaling 628,035,022 bytes.
Every input matches its retained source identity. Preflight image decoding admitted
584 inputs and lacked support for 85: forty uncompressed 8-bit indexed images,
three uncompressed 4-bit indexed images, and forty-two RGB24 images with a
declared optimization color table before their pixel data. Seven unsupported
inputs occur within the guarded twenty-Object prefix itself.

This is a dependency of the catalog continuation, not a replacement objective.
[The image-input study](APPLICATION_CATALOG_DIB_INPUTS.md) tracks its implementation
and separate acceptance. Amendment1 defines the format/ownership extension;
amendment2 pins all original inputs, four exact PE extracts and 22 new controls.
All eighteen old startup decoder controls and the 374 existing fixtures remain
unchanged. Independent review accepted the new reference before Core edits;
Native implementation and validation are recorded in the linked image study.

File-image requests also differ from embedded-image requests. The saved prefix
has 194 requests with flags 2010, including four NULL responses, and four
successful embedded fallbacks with flags 2000. A resource's explicit origin must
govern that binding. Missing-file responses must retain the fallback behavior.
Adding resource inputs must preserve existing image/surface/DC owners and reject
conflicting bytes or origins atomically.

## Current finite result and remaining acceptance

The production owner now invokes the actual shared loaders from owned application
state. Candidate2 passes all 21 release methods, including complete child/file/
WAV/global/graphics comparisons at sixty retained caller boundaries and whole
rollback. The twentieth child still precedes its parent slot/count store.
Independent plan/reference/draft review is preserved;
[review4](../evidence/application-catalog-session-final-review.json) closes the
subsequent final graphics-comparator gap for exact f601614.
See the [study](APPLICATION_CATALOG_SESSION.md) and
[evidence](../evidence/application-catalog-session.json) for acceptance limits.

[Full input preflight](APPLICATION_CATALOG_FULL_INPUTS.md) now verifies the original
156 DAT/registry files, 365 WAV and 669 images. The old full audio adapter reuses
PCM mappings across calls; a future owned environment needs declared new bindings.
Full caller clock/message/graphics replies remain missing from the older catalogs.

Full catalog/pool/UI/loading and outer-loop returns, actual raster/device output,
AppKit input/clock/audio, War gameplay, complete matches/content/network, Windows
and clean-Mac verification remain open. All three safety incidents and the
separate Windows download refusal remain open; no affected operation is retried.
