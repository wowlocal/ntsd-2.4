# Input initialization and its own window-message consumers

Recover whole43bf10..43c0ba and the complete43d078..43d100 caller: initial key
bytes, four retained joystick records, WinMM discovery/calibration/capture,
then the five original menu WAV loads. Continue selected actual joystick
WndProc messages using the producer's own globals. The pinned original EXE,
five WAVs and Unicorn2.1.4 are the reference environment; APIs and memory-copy
boundaries are controlled development adapters, not Windows/device evidence.

This directly extends [MENU_SOUND_STARTUP](MENU_SOUND_STARTUP.md) upward and
connects calibration to [WINDOW_INPUT](WINDOW_INPUT.md). Earlier WinMain,
CRT/NLS/window/music/worker state remains an explicit entry boundary. Verify
the accepted DLL patches are disjoint from all executed/studied bodies; no
installed startup chain is claimed. Do not change the226 immutable fixtures.

Low-level execution is needed because initialization retains some prior bytes,
probes only two of four records, ignores several API failures and reuses stack
descriptors. Trace exact field writes, API outputs, read provenance, signed
wrapped midpoint arithmetic and actual caller/child returns. No control pointer,
cookie or exception mutation, memory corruption stimulus or external network
request is authorized by this experiment.

Finite acceptance criteria:

1. Execute absent devices, nonzero device counts, both available/unplugged
   devices and ordinary position/capture/threshold/capabilities error responses.
   Test both initial backing patterns and retained direction bytes across all
   four records. Preserve exactly256 key writes, not a fabricated300-byte fill.
2. Compare complete52-byte JOYINFOEX requests and404-byte JOYCAPSA output
   backing/masks. Preserve all source bytes; native compares the full mask and
   every defined byte while leaving unwritten backing unknown. Each successful
   API output has explicit provenance. Reuse own
   first-device outputs for a later failed query, rather than resetting storage.
   If an ordinary error leaves a consumed field unknown, retain source evidence
   and explicitly reject/roll back natively; do not count it as a match.
3. Compare actual caps-to-global order and signed32-bit wrapped averages using
   ordinary ranges, inverted ranges and declared signed-boundary controls.
   Continue whole3a0/3a1 position and3b5..3b8 button callbacks with these own
   globals, including threshold neighbors. Do not inject callback bounds.
4. Execute the full new caller through43d100, including shared menu-sound
   loading and ordinary audio-disabled/file-error controls. Retain all PCM,
   allocations and ordered sound effects from the shared loader; compare full
   globals/write masks/live event state and actual stack cleanup. Keep source
   unsupported-read cases separate from complete native matches.
5. Native late failure at capabilities, fifth WAV return and a later callback
   must roll back the appropriate whole operation. Run new raw comparisons plus
   retained menu-sound, WAV and window-input tests from an isolated committed
   native export. Publish only after raw tests pass; independently verify full
   raw/packed bytes/JSON/SHA, every blob and old fixture/vendor/foreign hashes,
   then run packaged tests and preserve terminal process evidence.

This is a dependency for connecting original input to the application. Actual
device calibration, input latency, full WinMain/menus/match/Windows and clean-Mac
acceptance remain required and open beyond this controlled study.

Finite controlled acceptance is now complete in [INPUT_STARTUP](INPUT_STARTUP.md):
56 whole input/sound segments,1900 own callbacks and four separate native
unknown-read rejections. Two successful API controls supplement the unchanged
58-case main corpus. App/device/Windows and the full goal remain open.
