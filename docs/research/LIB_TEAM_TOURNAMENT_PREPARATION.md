# Team Tournament preparation

OriginalTeamTournamentPreparation matches **210 whole original caller returns**:
154 menu-only calls and56 preparations from436747. Each executes actual
436fb5ret12 and422ab8ret4. Two fresh chains contain87 eight-human and69 mixed
calls;54 controlled confirmations cover17 ordinary-arena load/release/reload
sequences, direct99 and both ordinary/special random choices. This completes the
controlled preparation continuation of [Team bracket](LIB_TEAM_TOURNAMENT_BRACKET.md).
It does not establish a played battle or own initialized application.

See the [frozen finite plan](LIB_TEAM_TOURNAMENT_PREPARATION_PLAN.md),
[evidence](../evidence/lib-team-tournament-preparation.json),
[source producer](../../tools/oracle_lib_team_tournament_preparation.py),
[independent auditor](../../tools/verify_lib_team_tournament_preparation.py),
[native implementation](../../native/Sources/NTSDCore/OriginalTeamTournamentPreparation.swift)
and [comparison](../../native/Tests/NTSDCoreTests/OriginalLibTeamTournamentPreparationTests.swift).

Pinned NTSD EXE/lib.dll/VC80 instructions execute under controlled Unicorn2.1.4,
CW023f, original DAT/BMP/DIB and declared time/allocator/COM/GDI responses. Memory,
known-byte masks, FPU checkpoints, helper returns and stack traces recover Actor,
bitmap, caller-string and recording ownership. Emulation is development tooling;
its synthetic addresses and API responses are not Windows/device observations.
Each of22 fresh parents executes419e40 and4004061d0 constructors. All8822 parent
returns compare with independently constructed Native records. Native rebuilds
137 Objects and101 backgrounds from raw resources and initializes geometry/RNG
from recovered constants; it imports no expected after-state or unknown stack.

The original formats `%4d%02d%02d_%02d%02d%02d`, appends `_2on2_`, then
`SemiFinal` at phase2 or `Final` at phase4. Other phases append no suffix; phase6
is a declared controlled scalar case with no natural reachability claim.
Year7 retains leading spaces. `%s.lfr` writes the filename. In26 calls the
whole enclosing caller also formats `Start recording '%s'...` at4029ae.
Actual shared output and its order remain part of the full comparison.

Arena RNG109 uses count-2 and maps count-3 to99. Every active Actor among the
first20 slots is reconstructed, retaining its owned Object binding. The original
copies participant HP to2fc/300/304, state to33c and team from44d100 to364,
sets MP500/frame75, copies Object+90 to Actor+31c, draws X then Z through10a/10b,
and writes integer
and binary64 coordinates plus the slot number. The original writes44d034 before
450bbc, reconstructs inactive20..<400, resets input and begins whole mode3 replay.
At436afa twelve argument bytes remain: `[esp+1c]` is frame rootSP10, containing
the actual44d020 pointer. Native strings come from its own formatting.

The frozen plan's four-participant language means four participant identities.
Its assumption that each retained confirmation would prepare exactly four Actors
was too narrow. The34 bridge calls deliberately retain prior assignments while
setting menu127/counter5 again; they do not execute a fight/result/assignment
clear. Actual4366c5 repeats CPU placement in the first free human/CPU seat pair.
Stores43670a/436711/436736 prove assignment, active flag and Object binding.
There are34 preparations with4 active Actors,6 with6 and16 with8: **300 Actor
preparations**. Repeated CPU identities occupy additional slots until the eight
paired seats fill. The auditor independently derives these placements from the
preceding source boundary. Normal result handling clears assignments; these
controlled resource-lifetime bridges do not prove ordinary post-fight duplication.
The whole Native comparator already matched all210 cases before this audit fix.

The17 ordinary-arena sequences select99 and release their previously loaded
layers, then reload the original arena. Dead wrapper bytes and stale tail
pointers survive without double free.
**302 surface Release/free pairs** and649 bitmap constructors/
10339 API requests compare. Release responses0/1/17/ffffffff
are ignored before wrapper free; only the first layer pointer clears. Shadows,
builtin99 and playback survive. Old recording frees before new allocation;
**56 whole0x630e18 recordings/363533632 bytes and complete masks**
compare, including retained dead generations. COM/device destruction and host
heap behavior are separate from these declared response/ownership contracts.

The independent audit reconstructs all5426 checkpoints/
5827637 records/28589773748 bytes plus masks,
6551690 stores/22748385 written bytes,
1123172 instruction reads/3665214 bytes
and6719 API reads/83281 bytes. It verifies
1208 known release reads and407480 recording
reads, all210 atomically retained cases, 59951 helper returns,
392 preparation FPU points, 5240 blobs/
506767972 decoded bytes and4208 instruction starts
(3680 EXE/51 lib/477 CRT).
RNG streams are {'0x103': 16, '0x10a': 300, '0x10b': 300, '0x109': 2}. Unknown masks remain unknown.
This is observed instruction coverage, not all branch outcomes.

Source capture completed in266.752s. Native raw **8 tests passed
in339.02s, build276.18s**, including new3 tests/165.460s,
retained377 Team bracket and51 Tournament arena-release cases on the same Core.
Six late errors at release/free/replay allocation/completed recording/enclosing
return/bitmap reload roll back the whole current call and preserve prior calls.
An unavailable live-arena release continuation is explicitly rejected with
rollback, separately from the210 source matches. Packaged **3 tests passed
in132.8s, build0.38s**, without raw overrides.
NTSDNative linked; no app window or device was exercised.

Review history is retained. A draft-wide mask replacement was caught and restored
before any source execution. A waiting wrapper queried a source child after it
had exited while the parent was publishing hashes; that wrapper failed before
Swift started. Source was never restarted, and Native1 started only after terminal
source verification. Audit1 incorrectly counted only the two name formats;
actual Start recording output required a third format in enabled cases. Audit2
incorrectly required four active Actors in the retained CPU bridges. Both failed
versions/logs remain; audit3 repeats the full audit with independently derived
placement and output. A supplementary event counter also initially used a missing
optional `kind` key; its error remains and the corrected counter uses the recorded
request/allocation schema. No source expected byte or Native game rule changed
for any of these corrections. The source producer and finite plan stay pinned.

All290 prior fixtures remain unchanged;295 current pins include five new transport
files. Full raw2116304179 bytes/SHA`b9d2cbb7489d12ed4fbc05beb42d3a9d5be97099b110a6a57910f0202f1133d1`,
200312305 packed bytes, complete JSON and all715 package/archive
files verify. Transport deflation only stores research fixtures. All study source,
Native and audit jobs are terminal. Catalog5 also terminated at the unchanged
6GiB research-storage guard after28 complete Objects, with the29th pending. All
48855 packed trace parts/6183435332 bytes and23754 embedded blobs verify, as do
its890 input pins; this is integrity evidence, without a new28-Object Native
comparison or complete decoded-trace audit. Its205769031-byte terminal snapshot
SHA is `c8ba9595aedf9bf4fbba98ddff9783dd24710fe90c955a28e655f54420fadc61`.
No source process was restarted or reserve lowered. Three earlier completed
Native catalog input archives (3708467200 bytes) were preserved byte-for-byte on
X5 with canonical symlinks, restoring internal free space; their members are
unchanged. The full catalog dependency remains open.

Next are War and own initialized catalog/library/outer-loop/app integration.
Full played match/game/network, window/input/audio, Windows and clean-Mac checks
remain open. The complete user goal is not achieved by this controlled milestone.
