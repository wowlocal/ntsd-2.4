# Active contact passes on current owned physics output

Status: seven finite stages and current owners verified. The [plan](APPLICATION_ACTIVE_CONTACTS_PLAN.md)
extends accepted [physics](APPLICATION_ACTIVE_PHYSICS.md) through seven consecutive
stages of the active body. Full body and game acceptance remain open.

## Calculation and composition

The comparison observes actual physics output from the same Bootstrap call,
then carries each current own snapshot to the next of seven stages. Independent
test storage calculates both depth passes, contact prefix and signed vrest,
cached wrapped rectangle tests, installed ITR filters, fusion cooldowns, the
two hit passes with item RNG, cpoint predicates and held-owner cleanup.
Expected records never become Core input. Own positions, Frame and heap identities,
RNG and allocation lifetimes remain current across all48 calls per variant.

The saved reference has11 broad pairs per variant. Every actual ITR in those
pairs is rejected before team/narrow geometry; checking invulnerability alone
would miss the earlier effect/type reads. Cached bounds remain their actual
known values, including large heights. Current70, previous78 and collision7c
are distinct. Both signed vrest decrements precede the two directed tests.
The mandatory fusion tail examines20 cooldowns, including inactive slots.

Both live fighters have no collected hit and no held/cpoint action on this finite
path. Item count is explicitly reset; RNG uses the current own table/index/counter
with counter then index stores. A zero item draw or actual hit/cpoint/held branch
requires further independent comparison. The tests cannot silently skip it.
The second depth pass executes again, including integer-Z writes when idempotent.

Every own endpoint compares complete match records/masks, World/400 Actors,
globals, mutable Frame allocations, Object/background/library/CRT ownership,
current memory/graphics/music owners, caller local record and full journal.
The accepted physics/control/input helpers and whole-body rejection/rollback/
same-ticket retry checks remain nested prerequisites.

## Evidence and corrections

Corrected independent stdlib equations reproduce672 source full pool/global
endpoints,576 Actor checkpoints and1490 ordered helper returns. Swift checks
the same source before its own comparison, including helper arguments, selected
results, exact return PCs and stack shape. This is not full helper ABI or an
observed instruction-store trace. Formula writes include idempotent assignments.

Independent review found that candidate1 and formula1 read ITR.effect from+1c
instead of+2c. Later invulnerability hid the mistake in endpoint equality.
The old files, report and run are preserved as rejected comparator evidence.
Candidate2 corrects the offset and tests unknown effect before invulnerability,
effect4 rejection and the required later path without either rejection gate.
These controlled changes affect comparison copies only; no source expected,
game data or Core behavior changes. The first candidate is not acceptance proof.

## Boundaries and validation

Four release methods passed in171.106s/build352.34s. The two new
methods compare672 source and672 own endpoints, with96 own item RNG events.
Twelve new negative and two positive comparator controls pass. The same schedules
repeat in the retained physics methods; repeated preflight rows add no distinct
cases.149 unselected methods retain exact pins (153 unique retained methods).

All1000 Native files,383 fixtures/102 resources and485 package files were checked
against frozen inputs;996 prior Native files are unchanged. Both Native and
evidence archives were read back, separately from publication documents. Independent
contract and corrected candidate reviews are linked from the evidence; final
publication review remains the commit gate. Both Native jobs are terminal; the
rejected first candidate is preserved and restorable from the archives. The task area is
`build/research/application-owned-active-contacts-native-20260914/` on verified X5.

Middle source Object/heap snapshots were not captured. Acquired records supply
the inputs to recovered read-only paths; they do not become an observed interior
snapshot or a proof about unknown backing. Own mutable storage is compared in full.
Actual damaging/held/cpoint/spawn branches beyond the finite path, the following
ten body stages, full helper ABI/stores, paused/round/match/game, application
integration and device/Windows/clean-Mac acceptance remain open. Existing safety
incidents are unchanged. No original code, emulator, historical capture or
auditor is executed by this card. EXE envelope is not recalculated.

A separate unaccepted next-step inventory distinguishes grouped graphics events
from primitive effects. Per source variant, the next camera/drawing/impulse stages
contain3072/1872/336 grouped events, including864 reads marked undefined at
bitmap offset12. Those masks remain explicit; they are not known zero or measured
device pixels. The inventory prepares the next comparison and does not accept it.

[Machine evidence](../evidence/application-active-contacts.json) records the exact
commands, outcomes, pins, archives, independent reviews and open gates.
