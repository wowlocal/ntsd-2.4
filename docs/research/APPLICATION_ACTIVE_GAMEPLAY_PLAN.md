# Owned active gameplay after the accepted neutral sequence

Status: finite input comparison and own48 body preflight verified; full active
body comparison pending. See [result](APPLICATION_ACTIVE_GAMEPLAY_INPUT.md). Base `40bf4ca`; clean working
tree and all989 Native files pinned in
`build/research/application-owned-active-gameplay-native-20260913/initial.json`.
This card continues [the accepted neutral comparison](APPLICATION_GAMEPLAY_EFFECTS.md),
not a restart of historical captures. Its product purpose is retained player
movement/actions through the actual application on the way to a full match.

## Contract and evidence

Use both immutable48-call [active controls](ACTIVE_GAMEPLAY_CAPTURE.md), their
[plan](ACTIVE_GAMEPLAY_PLAN.md), [library overlap](ACTIVE_LIB_IMPACT.md), and
the accepted installed-library control/contact/hit/transform/command/text
contracts. The controls execute pristine EXE/VC80; they are not an observation
of the bundled-library application. Source-first projections must verify saved
observations before deriving an own result. Library replacements need their
separate recovered evidence. A Native-only trajectory is a preflight, never
a differential match or acceptance of this card.

Reference distribution: `downloads/NTSD_2.4_2.0a_clean/NTSD 2.4_2.0a`.
EXE SHA256 `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
VC80 SHA256 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`;
lib.dll SHA256 `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
No original execution or historical producer/auditor rerun is planned. Read saved
JSON, source bytes, existing disassembly and Native code only. Source environment
is the declared Unicorn2.1.4/CW023f with modeled platform responses; own execution
is Swift release on macOS with explicit53-bit arithmetic and platform replies.
Neither environment establishes Windows/device observations.

Each own chain retains full startup/catalog/selection/Start and17 accepted
neutral Bootstrap returns before48 active calls. Per-call buttons are:
1..16 Naruto left/Sasuke right;17..24 add attack;25..28 release;
29..36 Naruto defend/Sasuke attack;37..40 both jump;41..44 Naruto up/Sasuke down;
45..48 release. Read the current own seven key bindings at every acquisition;
overlapping bindings share a held key. Feed declared WM_KEYDOWN/WM_KEYUP messages
through Bootstrap/WndProc. No source Actor command, location, phase, RNG, stack,
recording after-state or unknown memory drives the application. The reference
acquisition uses semantic keyboard responses; its resulting bytes are comparison
outputs, not a claim of actual OS message timing.

| Question | Required observations and acceptance |
| --- | --- |
| Acquisition/input | Both48 schedules, full300 keyboard bytes, live key bindings, phase/control requests, command/playback buffers, ordered stores and replay mutations; acquired keys survive a later rejected game transaction |
| Retained body | All19 stages per call with full normalized World/400 Actors/globals/BG records and masks; complete available Object/Frame/weapon-string endpoints, exact RNG/store/effect order and installed hook contracts |
| Temporary objects | Observe the saved call17 constructor/lifetime and independently derive own activity and allocation ownership; slot50 is not assumed to have the same lifetime if own interactions differ |
| Output | Recompute drawing order, positions/clips/bitmap reads/masks, HUD/text and sound queue from own live state; compare chronological journal, graphics owners/colors, exact aliases and current resources |
| Enclosing caller | All48 own Bootstrap returns per variant, no imported after-state; retained loop/replay/resource owners and late body/input/outer-tail rollback |

Own positions/RNG differ from the pristine controls. In particular, contacts may
take different branches: never translate old clipped drawing events or import
source contact outcomes. An unsupported independent projection remains a named
comparison gap. The neutral comparator's guards stay strict; active behavior
must not broaden acceptance of an already frozen neutral contract.

## Implementation and validation

Root owns new `OriginalApplicationActiveGameplay*.swift` test/comparison files,
the optional continuation hook in `OriginalApplicationGameplayProjectionTests.swift`,
this plan, its later result/evidence and CURRENT_WORK/RESEARCH_MAP entries. Core
changes require a demonstrated application contract gap and an explicit plan
addition before editing. Fixtures, source bytes, historical plans/results and
AGENTS_HISTORY stay immutable. The latter must retain SHA256
`1f124556e757a1f1efe7dabbc19df76bc482ca9a316df2295eb86beeae1f5dff`.

The read-only `war_contract_review` reviewer owns only new numbered review files
in this task area. Contract review, deterministic comparison, Native execution,
package verification and final publication remain separate gates. Review gaps
stay explicit. Reuse existing fixture readers and per-task freeze/run/verify
patterns; do not create another capture framework.

First establish the retained own48 trajectory and acquisition/input comparisons,
then finish independent state/effect projections across the entire trajectory.
Select the new two full-chain tests plus the two existing neutral projection
methods when their shared helper changes; retain old reader and all unchanged
regressions through exact source/package pins. A preflight passing while body
comparison is open is reported as incomplete. Add targeted old input/replay/lib
regressions if their implementation changes. Run no old suite merely for counts.
Full snapshot comparisons use bounded diagnostics, not multi-megabyte XCTest
expected/actual failure dumps.

New large IO belongs to the task X5 area, verified APFS UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. Reserves:40GiB X5 and6GiB internal;
task maximum40GiB,3600seconds per Native run, three initial candidates followed
by diagnosis/revised bounds if necessary. Pin actual working inputs before every
run and retain logs, failures, source/package manifests and terminal job status.
Before acting on a process revalidate PID/start/command/cwd/job; silence or
an observation timeout is not termination. New long builds must detach their
thin owner process so a goal continuation cannot silently terminate the job.

## Open dependencies and stopping rules

The [safety incidents](../evidence/codex-safety-incidents-2026-09-12.json) remain
open. This card does not resume an affected operation. Preserve any new refusal
exactly and stop retries/rerouting of that operation under WORKFLOW. Source fault,
missing evidence, comparator mismatch, Native rejection and safety refusal remain
distinct. Do not alter expected values/masks to accept a candidate.

Paused gameplay is the next separate retained branch. This48-call trajectory
does not establish a damaging hit, round result or full match; original controls
retain both fighters at HP500. Actual raster/audio, OS input latency, Windows,
clean-Mac packaging, other modes/content/AI/replays/network and the full game
remain open until their own criteria pass.

## Candidate history and additional input evidence

Candidate1 ended1 during compilation: the new body observer omitted other
`LoadedMenu.Observation` cases. Its frozen source and log remain unchanged;
the correction rejects non-gameplay observations explicitly.

Candidate2 ended1 before own active execution. Both new source-first checks
rejected an incorrect comparator assumption that all seats after the first two
had status0. Both saved48-call controls instead retain statuses
`[1,2,3,4,0,0,0,0]`; LOCAL_INPUT deliberately ignores activity for these seats.
Its two existing neutral regressions passed59.068seconds/build380.04seconds.
Neither failure changes Core or the source expected values.

The independent review additionally identified configured inactive seats2/3 as
joystick profiles1/2. The corrected comparator reads their live state and actual
button mappings; the semantic48 schedule still acquires only the first two local
keyboards. Joystick state remains declared retained startup data, not new host
device observation. The reviewer checked the new formula against all saved input
endpoints before another Native run.

The next candidate also adds source-first complete prologue and six input
pool/globals endpoints, whole recording-allocation comparison, ordered control/
writePacket/team observations and input journal. These endpoint formulas do not
claim ordered per-instruction stores. WndProc comparison includes both sequence
recognizers and guards the actual inactive editor; the source acquisition alone
does not observe those effects. Late rejection checks compare the actual app to
its committed acquired-input state, including public loaded resource owners,
and repeat input with the same pending ticket without another WndProc event.
Full active body projections remain pending under the original acceptance scope.


## Candidate3 diagnosis and bounded continuation

Candidate3 ended1 after378.05seconds build and109.118seconds tests. Both new
methods passed their saved-data checks and accepted neutral parent, then rejected
the first own acquisition on the comparator's incorrect inactive-text assumption.
The two separate neutral methods passed38.617seconds. No own active return was
observed. Exact frozen candidate3 and terminal log remain immutable. Independent
review2 also missed that this guard contradicted the actual retained parent;
that review gap is preserved alongside the author error.

WINDOW_INPUT specifies an editor branch when458440 is exactly1; dispatcher
startup sets that word to1 after art setup. Native therefore may legitimately
retain active text entry while gameplay keys are acquired. The source controls'
semantic acquisition did not execute this WndProc branch. Candidate4 adds an
independent full-record prediction for ordinary text writes, retained counters,
backspace/Enter and the existing recognizers. It explicitly guards this finite
sequence below the text/index alias boundary; no editor flag is forced or reset,
and no Core/source expected value changes. The full actual State comparison is
retained. This diagnoses the missing caller contract after the initial three
rounds and permits one corrected candidate4 with the same four methods,3600second
run limit and unchanged storage reserves. A further failure requires its own
diagnosis before another candidate, not an automatic repeat.


## Candidate4 diagnosis and message-loop boundary

Candidate4 ended1 after396.13seconds build and108.245seconds tests. Both new
methods reached first own acquisition and found complete-state byteb58042 versus
expected41; no active body return occurred. The two separate neutral methods
passed38.349seconds. The missing comparison effect is the enclosing message
iteration's458580 update, not an original/Native game mismatch: every non-quit
message loop iteration wraps its counter increment and resets on signed >60.

Candidate5 preserves the complete WndProc state calculation and independently
adds that loop tail, exact retained28-byte MSG from declared retrieval, unchanged
timer baseline and coherence of own loop/global counters. The accepted
APPLICATION_MESSAGE_LOOP contract supplies this formula; no expected after-state
is imported. Author and review3 omitted this caller effect despite the complete
State assertion exposing it. Exact candidate4, log and review remain preserved.
One corrected candidate5 is permitted after this diagnosis, with the same four
methods and3600second/40GiB limits. Further failures require new diagnosis.
