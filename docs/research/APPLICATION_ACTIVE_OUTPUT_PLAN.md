# Active output and retained return from current layout

Status: accepted in the finite scope; see [results](APPLICATION_ACTIVE_OUTPUT.md).
Frozen candidate1 plan remains immutable. Clean parent
`edf936f5da87e06db4bb83c59ae5ae80f811efb6`, [layout](APPLICATION_ACTIVE_LAYOUT.md).
Follow WORKFLOW and TASK_TEMPLATE, GAMEPLAY_OUTPUT, GAMEPLAY_RETURN, MODE_LABEL,
QUEUED_SOUND and immutable archive2310..2525/2785..2786. This completes the last
finite active body stage and its consumer return on the way to the Naruto/Sasuke
District match and full standalone native macOS game.

## Evidence and question

Read existing active48 fixtures only, SHA256
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
Pinned EXE `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Saved pristine EXE/VC80 with synthetic API responses and current own library-enabled
Native are separate environments. Source machine frames are evidence, not Native
inputs. No new original/emulator/historical producer/capture/auditor execution.

The output at422994 calls mode label, notice/volume, presentation, queued sound,
then returns through422ab8 and428805/ret4 to declared STOP30000000/SP1000f42c.
Preserve that order, full globals/pool/masks, memory/replay/bitmap/audio owners,
text bytes, bitmap read provenance and graphics/audio journal through PendingReturn.
Source output has7 component references; enclosing return adds objects,
objectStrings and frameHeap. Preserve the missing middle snapshot boundary.
Do not infer equality of absent data or insert it into Native from a later snapshot.

The saved planning inventory in the parent task found indicator0, VS/Difficult,
recording notice18..65 and no volume hotkeys. Each schedule has3648draw/clip/Blt,
25536bitmap reads,48formats,83methods,7queue writes/plays. Its initial overly broad
reference-set assertion and corrected reader remain immutable. New acceptance
must explicitly gate full inventory claims and reconstruct globals from writes.

## Independent specification and own comparison

Three new test-only ActiveOutput Projection/Source/Tests files. Reuse the accepted
independent bitmap/text expansion and resource models, not Core output handlers.
Derive literal/suffix stores, four font passes/NUL writes, notice timer and text,
presentation, sound and dispatcher clear from each actual current input.
Source writes/effect lists are comparisons; they may not choose own mutations.
Retain exact source store PC/address/width/value order and complete after bytes.

The old neutral model and journal are slot6-only and remain unchanged. New sound
expectations inspect fixed400catalog then80builtin slots. Device guard comes first;
signed nonpositive flags stay untouched. Read right before left, wrap sum, clear
positive flag before testing clamped sum. Preserve wrapped pan/volume arithmetic
and truncating division, fresh master-volume playback gate, buffer aliases and
Stop/SetPosition/Play order. Numeric device failures do not become exceptions.
Current input flags select active slots4/5/10/109/366 in the saved schedules;
do not hardcode those as the only permitted or loaded slots.

Resolve own requested sound buffers through actual catalog/common/startup owners
and their current game-global word. Source buffers use their declared loaded
inventory; no source address becomes a Native owner. Expand each sound method
exactly once in the journal, preserving its separate front observation route.
Compare all output graphics and earlier accepted command prefix through return,
all non-graphics owners and final snapshot/dispatcher result before app commit.

Controls distinguish event order/value/defined masks; source-only expected stores
cannot drive own state. Exercise generic queue edges, disabled device with unknown
queue backing, inactive unknown channel words, positive clear with nonpositive
sum, wrapped values, nonpositive master, alias buffers and catalog/builtin order.
Retain controlled170 output calls,968 queue/playback and148 mode-label cases.
Add late failures during output drawing, after final dispatcher observation,
after sound methods on a call with two queued slots, and beforeCommit. Preserve
entry/environment/return rollback and same-session retry. External effects remain
buffered; storage rollback does not undo submitted device IO.

## Ownership, limits and gates

Root owns the new3 Swift files, this plan/results/evidence and CURRENT_WORK/MAP.
war_contract_review owns independent numbered read-only reports. All1021 old Native
files,383fixtures/102resources, source masks/expected, archives/failures and safety
incidents remain immutable. No production Core change is initially needed.
Freeze actual inputs and plan; independent contract/candidate review before Native.

Task `build/research/application-owned-active-output-native-20260914/`, verified
X5 UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`. Pin1021 actual starting files.
Keep40GiB external/6GiB internal reserve and40GiB task cap. Readers96calls/300s;
Native3600s; three candidates before contract diagnosis. Reuse parent snapshot,
job, comparator, archive and delivery tools. No automatic retries after refusals;
never restart a quiet live or completed producer/test for formatting.

Select7 release methods:2 new Output,2 retained Layout,1 GameplayOutput,
1 QueuedSound,1 ModeLabel. Only2 Layout and1 GameplayOutput selected identities occur in parent168;
QueuedSound and ModeLabel are not inherited there. Thus165 inherited+7 direct=172
unique. The earlier count draft is preserved; independent identity review corrected it.
Expected1024 Native files,1021old exact and485 packaged bytes/modes unchanged.
Native, package verification, archive bodies/modes, independent publication review
and Git delivery are separate gates. Preserve failed candidates/readers unchanged.

## Completion boundary

Accept complete finite output/return only after state, events, journal, resource
owners and rollbacks pass. Independently audit composition of all19 stages and
retained application input/return; a stage count alone does not establish a tick.
Do not claim full original application equivalence from pristine-source versus
library-enabled model comparisons or fill missing middle Object/Frame evidence.
If full active composition has a remaining dependency, name it and continue it
before choosing app integration or a broader gameplay branch. Full game, actual
match, native window/input/audio, Windows/device/clean-Mac, full helper ABI/FPU and
instruction stores, formatter observability and prior safety incidents remain
open until their actual evidence exists. EXE envelope is not recalculated.
