# Active notices from owned HUD state

Accepted finite comparison of both48-call active schedules through the whole
post-HUD notices stage421a2d..421cdc. The current owned HUD output supplies the
inputs; no expected source after-state or unknown caller backing is imported.
[Plan](APPLICATION_ACTIVE_NOTICES_PLAN.md), [evidence](../evidence/application-active-notices.json).

## Analysis

Saved pristine EXE/VC80 and the owned library-enabled application remain distinct
environments. All96 source endpoints have current diagnostic/exit/selector/mode
flags0, no helper/event/primitive effect, no global write or watched caller-stack
access. Both complete state component sets are retained. The exact saved set is
ten executed caller addresses plus unexecuted421cdc, not an ordered instruction
trace. Preceding HUD retains EDI0;421a33 loads the sprintf IAT address into ESI
without calling it. CW023f/FPSW0or4000/tagffff and observed scratch/spawn/round words
survive; no new Native register/FPU equivalence is claimed.

Current-input comparison requires diagnostic450bec==0, exit450c2c!=1 and selector
450c28 outside1/2. Nonzero nonselecting values are accepted. Mode451160, Actor
state, resource metadata and strings are not causal inputs on this path. Any
selected branch remains a distinct extension, never silently treated as empty.

The saved producer watches rootSP+34..<74 and+44c..<5c4. It establishes no string
access during these calls, not zero or initialized string contents. Active raw
local backing is not captured. Body.Caller.formatter covers372 bytes at44c;
notices receives its340-byte suffix at46c. Menu.Snapshot.local is a separate
menu-local record. It cannot prove formatter retention, and the body API does
not expose formatter checkpoints. This observability/lifetime limit remains
explicit. Direct public notice controls preserve nil and separately owned opaque
340-byte storage/masks, with no event/provider calls, on current and nonselecting
flags. They do not establish enabled own formatter lifetime.

## Implementation and validation

Three new comparison-only Swift files calculate current branch selection and
check source boundaries, then compare all owned match/state bytes and masks,
graphics/memory owners, music/resources/backgrounds, menu locals and journal
through the real HUD-to-notices interval. Unexpected primitive output or misplaced
notice annotations fail. The complete parent pipeline and whole-return graphics
checks remain in use. Production Core and all1012 prior Native files are unchanged.

Negative controls reject selected diagnostic/exit/key branches and unknown causal
words; positive controls admit nonselecting exit/selector and unknown unused mode.
Four new late failures (notices checkpoint and beforeCommit in each variant)
restore entry and buffered environment. Two same-session retries reproduce the
full returned owners/journal/graphics. All parent rollback checks remain selected.

Seven release methods passed in269.509s after a363.95s build:
two Notices, two HUD, two controlled PostHUDNotices and one DiagnosticNumber.
Retained controls preserve819 direct source comparisons,8 explicit signaling-NaN
source discrepancies with separate quiet-NaN companions,4 cookie-overwrite
rejections and74424 numeric comparisons. This is not827 direct notice matches.
157 other previously accepted method identities are inherited by exact unchanged
pins;164 unique methods total. Inherited tests were not rerun.

All1015 actual/frozen Native files and485 packaged fixture/resource files were
verified;383 fixtures and102 resources remain unchanged. Native candidate tar
members were read back with exact bytes/modes. Evidence archive and independent
publication review are separate gates referenced in the evidence report.
The reviewer caught a draft verifier's incorrect HUD log prefix before execution;
the original draft and diagnosis are preserved. Publication draft review also
strengthened exact archive/job/review joins and pre-write checks; all three old
scripts and findings remain preserved. After the successful test run, reader1
failed because embedded XCTest metadata used singular `1 test`; reader2 adds
that exact alternative while preserving the raw log, error and old diagnostic.
No Native rerun or expected change was needed. No source expected/mask, old
failure, Core rule, model or safety setting was changed. No original/emulator or
historical producer/capture/auditor executed. The Native job is terminal in
`build/research/application-owned-active-notices-native-20260914/`; do not rerun it.

## Next boundary

Recording must consume the actual own notice output and retained round/replay
owners, then layout/output. These three remaining stages and full active tick
are open. Enabled diagnostic/exit/key-notice caller lifetimes, broader gameplay
branches, full helper ABI/FPU/stores, full match/game, native app window/input/audio,
actual device/Windows/clean-Mac and existing safety incidents remain open.
EXE envelope was not recalculated. This finite acceptance does not complete the goal.
