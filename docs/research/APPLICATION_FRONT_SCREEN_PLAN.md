# Own early screen after settings

Continue actual42709b/SP1000ea74 from fresh APPLICATION_SETTINGS parents through
selector/gate, whole415160 fill,423840 background loading and first43f010/43ef70
draw to427127 or4275cb. Execute whole43ee50/43ed10/4013d0 and actual VC80 sprintf
for the new background, retaining the existing CPU/stack/PTD/device/resources.
Pinned NTSD EXE, VC80, original control.txt and original PE DIBs on Unicorn2.1.4
remain the behavioral reference; reference execution is research tooling only.

Finite acceptance: all17 completed own settings parents; all13 background timer
residues; missing background, negative/positive CreateSurface, failed GetDC,
negative color key, NULL background allocation, failed fill and failed draw.
Also reach the normal setting1 worker request and setting-1 alternate path by
declared control-file inputs through the real settings helper. Include failed
CreateThread with GetLastError. Original files remain immutable. No direct
selector/target/stack/register injection, prior allocation rewrite or CRT reset.

Trace full parent continuity, original instruction bytes and helper returns,
all globals/stack/wrapper bytes and masks, ordered requests, own surface binding,
all bitmap reads/clips/Blt and source fill-frame provenance. Native composes its
own settings result and bitmap loader, compares only owned fill fields, and
leaves92 private fill bytes unknown. Untouched background allocator words remain
the explicitly declared A5 backing, with false masks and documented reads; this
does not recover Windows heap contents. Preserve all earlier fixture bytes.

Allocator, image/GDI/COM, timer and thread API responses are declared boundaries.
Worker delivery/body and reentrant platform callbacks are separate dependencies;
CreateThread alone does not prove a worker ran. Stop before any NULL dereference
and compare it separately from successful screen prefixes. Normal SEH/cookies
may execute; no control/protective storage corruption, bypass, arbitrary fault
continuation or unrelated target. Native late failures must roll back the whole
pending message-loop iteration, retaining committed startup/callback/ownership.

Raw and packaged isolated native acceptance must preserve241 old fixture pins,
all original assets and10 codec vendor hashes, and exclude six foreign files.
Verify full raw/packed bytes/JSON/SHA/blobs, fresh parents and all declared
instruction/stack boundaries before accepting. Use committedbf22d19 plus owned
overlays. No full World/dispatcher/WinMain return, app window/device/Windows,
full-match/all-content/network or clean-Mac completion follows from this prefix.
Next is the actual screen body4236d0/427127 or alternate4275cb.
