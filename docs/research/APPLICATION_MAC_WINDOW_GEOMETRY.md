# Native window geometry through whole Host WM_MOVE

All76 selected methods, fresh build/package and full archives verified, 2026-09-26. The task follows the
[finite plan](APPLICATION_MAC_WINDOW_GEOMETRY_PLAN.md) and the accepted
[observed bitmap integration](APPLICATION_OBSERVED_BITMAP_CORRECTION1.md).
Its immediate consumer is the first menu's destination rectangle. This removes
one presentation dependency on the path to a complete Naruto/Sasuke District
match; general raster and the complete native game remain open.

The authoritative game rule comes from [WINDOW_LIFECYCLE](WINDOW_LIFECYCLE.md):
whole43b3d0 message3 ignores packed lParam coordinates, queries the client rectangle,
converts first and second corners in order, and returns DefWindowProc's result.
API numeric failures do not suppress subsequent requests; absent writes retain
previous bytes/masks. Original EXE SHA256 remains
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c.
This task reads accepted evidence and runs Native Swift/AppKit; it executes no
original EXE/DLL, emulator or historical capture/auditor.

## Implementation

Task-owned candidate is `build/research/application-mac-window-geometry-20260926/`.
It clones all2268 files of the verified bitmap correction1, including the actual
uncommitted inputs represented there. Root1034, prior2257, source55, baseline and
old failures remain protected. Four new files and five modified source files give
2272 files:203Core/61Reference/8MacPlatform/283tests, plus unchanged resources.

MenuSession routes message3 through the existing lifecycle handler, as it already
did for message5. Its scalar store observations now project each8/16-byte geometry
API output into consecutive4-byte words. Whole response bytes remain in lifecycle
effects. Existing1/2/4-byte observations are unchanged; observers perform no IO.
Bootstrap and Host accept a pure lifecycle-response provider while retaining the
prepared-array path. Mixing a callback and nonempty prepared replies is rejected.

The lifecycle exchange uses the shared request/receipt protocol, Host sequence and
final-publication hook. Each staged platform retains its cursor and earlier
nonempty iterations by value. Actual physical queries occur after the Core attempt
has unwound and service has begun. Recalculation after a late failure consumes the
same saved replies and owners. A future move samples new geometry. Other providers
and DefWindowProc remain explicitly controlled in these tests.

MacWindowBackend validates payload and ownership before service. Client bounds and
flipped-view/window/screen point conversion produce actual16/8-byte replies. This
inherits the existing Native policy: integral signed32 logical AppKit points and
a top-left origin on the window's current screen. Missing/dead owners, unreadable
point data, fractions, nonfinite values and out-of-range coordinates are Native
boundaries. They are not new original-game restrictions or fabricated API failures.

## Comparison and review

Four new methods exercise whole startup/resize→WM_MOVE→first menu with physical
bitmap replies; late-publication rollback and retained histories; actual client/
point outputs and service/owner boundaries; and controlled ignored numeric failures
with and without writes. Geometry expectations use window frame→contentRect,
separate from the provider's view point conversions. A prepared replay compares
whole Core state using the observed API values; it supplies no expected after-state
or private source ABI to Native. Store projections are independently reconstructed
from complete reply bytes. Tests move only windows created and owned by this task.

All69 prior methods and all3 WindowLifecycle methods remain unchanged, yielding76
selected methods. The inherited expensive context tests retain their1800s/16GiB
limits; other methods retain600s/8GiB. Build uses two jobs and a12GiB sampled tree
RSS bound. Original40/6GiB reserves plus17GiB source commitment remain unchanged.
The task uses verified APFS X5; T7 is authorized for artifacts but not required here.

Author contract inspection found and addressed the missing lifecycle route and
scalar-store extent. A separate reviewer is unavailable in this session; author
inspection is not independent review. Preparation3016 passed in11.767s. Fresh
build3843 passed in301.070s, sampled tree RSS6,689,062,912 bytes. Package verification
confirmed all2272 candidate files and1686 resource files (385fixtures/1301runtime).
The66,509,960-byte test binary SHA256 is
6f1a1f8a8cb8bfc76db58ac7843b5b394e339b06aa2b15aa6b5db9c2df983808.

All four new methods passed. The observed client rectangle was
[1107,317,1901,867]; the following first menu retained425 physical bitmap replies,
25 surfaces and one presentation with that destination and a NULL source rectangle.
Full Core replay, independently projected geometry stores, late retry without
resampling, fresh subsequent moves, marker-owner lifetimes, service failures and
ignored zero/negative replies passed. All76 methods passed on this exact binary, including all69 prior and three
unchanged original lifecycle methods. Queue14614 terminal0/absent in695.243s;
summed test-process time488.558s, sampled peak tree RSS10,296,344,576 bytes.
The three large context methods kept their original1800s/16GiB limits.

One read-only status observation encountered a missing next-child job file after
the queue had announced that phase but before the runner wrote it. The admission
gap is recorded in observation-gap1.json. Queue identity/cwd were revalidated live;
no child, queue or source was restarted. This was an observer error, not a Native
mismatch or producer failure.

## Remaining application boundary

Automatic AppKit event delivery, synchronous window callbacks, general Blt/text/
color-key/format behavior and multi-screen/DPI/fractional geometry remain open.
The test's real macOS window does not establish actual Windows behavior. Other
providers, loading, input/audio, independent review, root promotion and clean-Mac/
full match/full game acceptance remain open. NTSDApp still uses Practice; EXE
envelope was not recalculated. Source59727 remains terminal at34 Objects, not137.

## Verified closure and next consumer

Finalizer36409 terminal0/absent in90.229s. All2272 candidate files and the fresh
package, root1034/prior2257/baseline2268/source55, nine-file patch roundtrip,
4962 regular artifact files/213 directories and338 PAX metadata members verify.
Bodies, modes, nanosecond mtimes, membership and distinct clone inodes are checked;
no links occur in the artifact archive. The task is frozen. All owned preparation,
build, test and finalization processes were rechecked absent. Old candidates,
failures, source publications and expected/masks remain unchanged.

Candidate manifest SHA256:
880a5a79a117d9ffc0f773056ff58da829d434f081a90f703d2abe4cbb4a88e1.
[Publication](../evidence/application-mac-window-geometry.json),
[closure](../evidence/application-mac-window-geometry-close.json),
[patch](../evidence/application-mac-window-geometry.patch).

NEXT: bound the whole first-menu raster/presentation contract using the exact
committed graphics commands and these actual window/display/bitmap owners. The
current physical backend handles allocation, clear and bitmap copies, while
front Blt/text/presentation remain projected requests. Resolve their required
color-key/clip/format/text semantics from saved original evidence before building
the physical consumer, and retain full-color/mask and late-failure checks. This
is the next direct obstruction to seeing the recovered menu in the native app;
a rectangle or resource inspector alone does not close that result. Independent
review and the application/Windows/full-game gates above remain open.
