# Active commands/recovery comparison

Status: accepted finite active commands/recovery comparison; whole active body remains open.
Parent clean commit: `c821b9128f440d9e3e1c7fa80d995854e5ec9add`.
See the [plan](APPLICATION_ACTIVE_COMMANDS_PLAN.md), accepted
[lifecycle](APPLICATION_ACTIVE_LIFECYCLE.md) and [WORKFLOW](WORKFLOW.md).
This closes the next required whole body stage toward a complete Naruto/Sasuke
District match and the full standalone native game.

## Source evidence and independent expectation

The finite comparison covers `4214d5..421a15`, source SP`1000e9bc`, on both
retained48-call active schedules. It reads current lifecycle output on each
side independently. The pristine source's coordinates, frame numbers, after-state
and arbitrary stack backing never supply the owned expected state.

The new saved-data inventory and independent contract review confirm that both
command words450bb8/450bc0 and mode451160 are0 in all96 calls. Only slots0/1
are active. TimersE0/E4 are0 and current Frame states0/1/3/4/7 never equal1700.
The command stage has no recorded primitive effects, events, helpers, boundaries
or checkpoints. All available before/after snapshot component references agree.
This does not make the stage a no-op: static original code and the accepted
[controlled command study](POSTDRAW_COMMANDS.md) establish these stores per
active Actor:

| Original PC | Output |
| --- | --- |
|4219dd|Actor+2e8 =1000|
|4219e3|Actor+2ec =1000|
|4219e9|Actor+2f0 =1000|
|4219ef|Actor+2e4 =0|
|4219f5|Only byteActor+EB =0|

The active corpus has no per-instruction write array. Its values are already
present, making those writes idempotent. The50 distinct observed PCs include
the unexecuted stop421a15. Neither unchanged endpoints nor instruction counts
prove all branches or an entire tick.

The commands projection derives cleanup from current activity and Frame data,
with explicit unsupported guards for nonzero command/refill/mode, healing
timers and state1700. It does not read HP/type/team or old write-only output
bytes on this finite path. These are comparator limits; production retains the
broader requested-item, library command3, resource, healing and cleanup rules.

## Masks, ownership and composition

Source-first comparison checks full World/400Actor/global records and masks,
available retained components, recording owners and absence of every recorded
primitive family/helper. There is no source access to retainedSP+34 in this
stage; its arbitrary before values3724541916 and2 remain unread. The owned
body initializes its own optional spawn slot to nil. No source word is imported.
The independent reader observes unchanged source CW023f/tagffff and FPSW0 or4000;
this is saved metadata, not a new Swift ABI/FPU comparison. Full middle
Object/Frame snapshots and all instruction stores remain unavailable here.

Owned checkpoints come from the real retained Bootstrap, input, control,
physics, contacts, graphics and lifecycle chain. The comparison checks full
match state, every allocation/Object binding, resource and library owners,
local record and chronological journal. Any gameplay/front event inside the
lifecycle→commands interval is rejected. The inherited graphics comparison
checks the entire returned command extent and its owner relations. Every inactive
Actor record and allocation, including the transient slot50, must survive from
the commands checkpoint to the actual complete return.

Comparison-only controls make cleanup outputs nonzero and undefined, preserving
sentinels in the other threeE8 bytes and neighboringEC. Cleanup must write known
words/byteEB without reading the poisoned outputs. Further controls reject
unknown activity/timers/Frame number/Frame state, aliasing and unsupported
command/healing/state1700 branches, and retain an inactive opaque byte/mask.
Frozen source inputs and actual game inputs are not modified by these controls.

The two new methods inherit lifecycle late-failure/same-session retry and outer
rollback checks. Those remain parent checks, not newly invented command-specific
rollback cases. Four unchanged `OriginalPostDrawCommandsTests` additionally
revalidate all3898 saved controlled source cases and the existing construction,
retained-slot and music-recovery rollback boundaries. They cover meaningful
non-idempotent production behavior without new original execution.

## Validation and delivery

Candidate1 passed all eight selected release methods in240.707s; build361.23s.
Commands control77.395s/primary56.320s and Lifecycle control52.138s/primary51.887s
passed through the full retained application chain. The3898-case controlled
command regression passed in2.963s; its three existing rollback methods also
passed. There are96 source and96 owned commands endpoints, with no primitive
effects in either finite sequence. Source snapshot invariance is separately
established; owned checks compare current-input cleanup without assuming invariance.

All1009 Native files match frozen1;1005 old files,383 fixtures,102 resources and
485 packaged files are unchanged. NTSDNative linked. The145 unaffected methods
are inherited by exact file/result pins and were not rerun; together with8 direct
methods this retains153 unique methods. No Core, fixture, resource or expected
mask changed. Changed code is only the three new ActiveCommands test helpers and
optional callback forwarding in ActiveLifecycleTests.

`native-candidate1.tar` contains1009 fully read-back bodies/modes,
4,442,347,520bytes, SHA256`769ef1df7f5ee85d2841be9cc8edc3623c674974fd6d0ef0503d030ba3c4351a`.
The separate evidence archive retains the saved-input inventory, exact independent
reviews and their preserved reader errors, frozen plan/manifest, Native logs,
verification and current study/handoff/map. Archive membership, verification and
final publication review are linked from the
[delivery evidence](../evidence/application-active-commands.json).

Prepublication review corrected an ambiguous draft report field: only saved-source
snapshot invariance is claimed, not unmeasured owned invariance. The unused draft
publisher and issue record are retained. This editorial correction did not change
the tested Native candidate. The exact raw stdout is preserved; the diagnostic
view removes only31 identified XCTest metadata spans interleaved with printed
records, without inventing gameplay bytes.

Native job1 is terminal: PID63914, start2026-09-14T02:21:36.491064Z,
end2026-09-14T02:31:41.299588Z, exit0. No successful test was repeated for
publication. The EXE and historical captures were not executed.

Task artifacts are in
`build/research/application-owned-active-commands-native-20260914/` on verified X5.
The independent contract review retains its initial incorrect fixedFPSW4000
assumption and an overlarge diagnostic export, followed by the corrected bounded
reader output. No original/emulator/capture/historical auditor was executed.
The current candidate and logs are preserved separately from the accepted parent.

## Remaining work

Five remaining active stages stay open.
Next is the complete HUD stage, followed by notices, recording,
layout and output. Later-stage semantics, full active tick, broader active
command/refill/healing/state1700/gameplay paths, the complete match/game,
app/window/input/audio integration, actual device/Windows and clean-Mac
acceptance remain open. The EXE envelope is not recalculated. Existing
[safety incidents](../evidence/codex-safety-incidents-2026-09-12.json) stay open;
this work does not retry or reroute their affected operations.
