# Own early screen body with the bundled library

Continue each completed critical APPLICATION_FRONT_SCREEN parent from actual
427127/SP1000ea74 through whole4236d0 and42712c..4275cb, including the installed
401290 jump, whole DLL text body and43f010/43ef70 children. The game behavior
is early-screen text/link/button drawing and panel-update order with its own
startup resources, target, cached Sleep and caller-local string producers.

Reference: pinned original NTSD EXE/lib.dll/VC80, control.txt and PE DIBs on
Unicorn2.1.4. Before the independently entered WinMain chain, execute the actual
relocated DLL process-attach installer with the retained research loader. This
declared loader connection is not a completed PE/CRT/NLS startup. Preserve every
original parent record exactly; installation must not change any parent outcome.
No source register, caller stack, selector, mouse or worker state injection
between the accepted own parent and this body. No worker execution is implied.

Finite acceptance: all39 completed critical parents and four default-parent
controls for negative/positive GetDC, failed later GDI/ReleaseDC and failed Blt.
Keep the alternate-setting parent and NULL-background stop as their previously
accepted boundaries. Do not rerun the completed parent capture process: each new
study case executes a fresh parent for continuation and verifies its immutable
record. Existing controlled panel/body/library-text corpora retain their scope.

Trace full original globals/stack/masks, instruction bytes, ordered API requests,
helper returns, caller-local reads/writes, all old resource bytes/masks and full
library state. Recover the actual target producer and prove local text reads use
current-call writes. Native initializes only its own semantic target, leaves
unproduced private bytes zero/unknown, invokes the existing library text path,
and retains independently produced strings/DC for subsequent screen consumers.
Do not import source stack or library executable addresses as native game input.

GDI/COM/critical-section responses are declared; no pixels, external URLs,
messages, audio or Windows calls occur. Recover any newly reached missing child
before proceeding. Stop before a newly observed invalid read, keep any source
failure separate from native success, and preserve the original evidence. No
control/protective storage corruption, bypass or arbitrary fault continuation.
Native observer failures must roll back the entire pending message-loop iteration.

Compare raw and packaged isolated native tests, all242 old fixture pins, original
assets,10 codec vendor hashes, full raw/packed JSON/bytes/SHA/blobs/atomic parts.
Export committed e9dc846 with owned overlays; exclude the six foreign files.
This is progress toward the full application, not World/dispatcher/WinMain return,
worker delivery, Windows/app/device, full match/all content or clean-Mac acceptance.
