# Retained library VS selection through Start

Status:252 source/native calls match, including250 actual returns and two
match-prelude boundaries. Raw and packaged acceptance both pass.

The [finite plan](LIB_SELECTION_STAGE_PLAN.md) extends
[LIB_CHARACTER_ROSTER](LIB_CHARACTER_ROSTER.md) through computer count,
computer character/team selection, arena/difficulty/music settings and the
actual42cf8a match-prelude boundary. The pinned NTSD EXE, bundled lib.dll,
VC80 and original DAT/BMP/DIB resources run only in controlled Unicorn2.1.4
research tooling at CW023f. Native code uses ordinary Swift state and its own
normally loaded catalog; no EXE/DLL/emulator ships in this path.

## Original execution

`oracle_lib_selection_stage.py` executes252 calls:127 primary and125 control.
Each has the actual41bc90 prologue, declared4229cc continuation and fresh/cached
menu resources.250 calls reach the actual422ab8ret4; two stop BEFORE42cf8a with
the429730 helper still pending. These two are successful declared boundaries,
not whole returns. The earlier tick body, acquisition and outer loop do not run.

Both sequences retain their complete previous World,400Actor, globals, resources,
library DC and stack between calls. Only the declared next button bytes and
three caller ABI words are supplied. The initial RNG table/configuration and
catalog operand views are controlled inputs documented in the plan; this is
not an initialized library application chain. The control changes the human
team button, so the prior70-case whole state is not claimed as an identical
parent. Its immutable corpus remains a separate native regression.

Both select Naruto17/Sasuke21 and two computers. Count navigation wraps through
the available seats. Confirmation assigns status11 to the first inactive seats
and records their order in451200; it does not mark World activity yet. The
current computer index is4511fc. Input comes from active human Actors in seat
order, sharing451268 latches with the human prefix. Up/down/right/left/attack/
jump priority and selection-at-entry2 determine the aggregate flags. Down only
sets the latch in this stage.

Settled computer portraits/names/teams render before the current selection.
Current portrait/name renders before right/left/Random changes. Roster navigation
preserves Actor+368 when selecting Random and skips noncharacters and locked
ID/10 groups3/5. Character confirmation plays45560c, changes11->12 and clears
the attack flag before the same-call team body. Jump from the first character
returns to count and clears only status11 seats; from later characters it
returns to the preceding computer's team. Team cancellation plays455614 and
returns to character selection.

The last computer excludes a common nonzero team of all other ready seats
(status%10==3). Any Independent seat or differing team removes that exclusion.
The control's humans and first computer all have Team1; the last computer's
left input therefore selects Independent instead of Team1. Team confirmation
changes12->13 and advances the current index. The final confirmation records
all eight Random flags and selects unused type0/ID<30 catalog ordinals in slot
order using actual417170 tagd9. Inactive slots participate; each earlier result
immediately changes later candidates. This is the shared RNG, not a separate
stream. A retained World Actor-table cursor at SP38 is a semantic native offset,
never imported source stack backing.

Both routes select Random->Lee On Road99->District0, change difficulty, exercise
Music OFF/Random/Main Theme and reach Start. Whole402130 consumes RNG for every
Random music call even without a navigation edge. Whole402100 requests control
+24 then position+20 with positive-zero seek, ignoring both supplied failure
HRESULTs. There are no music allocations or track-loading calls in this study.
Both fresh sequences create11 menu bitmaps and then retain them.

## Source evidence

The [machine report](../evidence/lib-selection-stage.json) records the exact
jobs, errors, immutable inputs and acceptance results. The
[source producer](../../tools/oracle_lib_selection_stage.py) and
[independent auditor](../../tools/verify_lib_selection_stage.py) retain their
separate execution/provenance contracts.

Raw252 capture3 corpus:1044042967bytes,
SHA256 `d4c6da12ed8bffb4534890ae701dce40825cb1dbc4d5c27b1d39b99bab9ffd3e`.
Capture3 completed0 in58.888s; all252 atomic cases and pinned inputs survive.
The earlier capture1 RNG input and setup-only capture2 error are preserved
separately below. No source crash or safety refusal occurred.

The independent read-only auditor replays277968 stores/1097530bytes,
496589 instruction reads/1945078bytes and8132 API reads/65268bytes.
All5666 checkpoints/4340156 records/12577896740bytes and193032 final records/
559412280bytes reproduce with complete masks and resource liveness. It also
verifies18150 caller counter words,9967 helper returns, every atomic case,
2628 blobs/134249912 decoded bytes and actual instruction bytes at3981 starts
(3490EXE+51lib+440CRT). This is observed start coverage, not every branch outcome.

4784 undefined bitmap reads retain their exact allocator bytes and false masks;
the remaining false-mask reads are the pinned EXE cookie, not imported unknown
stack values. All declared catalog/arena operand views remain unchanged. Arena
names derive independently from the registered raw DAT names and original
constructor literals; normal native background records must supply the same
strings. No unknown complete background allocation is used as native input.

Observed events include340 fills,1720 draws,11117 bitmap reads,2149 clips/Blts,
3784 text helper sequences,40 sound requests/120 sound methods,238 formats,
10 candidate lists,42 RNG calls,62 music configurations and4 stops/8 music
methods. There are250 presentation methods. Event order is part of acceptance.

## Open boundaries

Raw native acceptance passed2 tests in62.271s (whole252 comparison33.357s,
late rollback28.915s), build81.60s. Six retained tests passed with the same Core
in native2: fresh character menu8.715s, retained roster23.206s, both pristine
own selection chains35.918s. No Core change followed those six successes.
The isolated655-file tree and all recursive input archives are verified.
All256 previous fixtures are unchanged;259 files now include the new transport
index and two byte-exact parts,133974409bytes total. The reconstructed source
bytes, SHA and complete JSON equal raw capture3. Packaging does not change
the game codec or run reference code. Final packaged acceptance passed2 tests in60.022s (whole32.196s, rollback
27.826s), build0.29s, without raw environment overrides. NTSDNative linked.
No app window or device was exercised. All study source/SwiftPM jobs are terminal;
full application catalog5 remains a separate live process.
The first Swift build stopped on a missing outer `try` for a short-circuit
condition; its input archive/log are retained. Only that syntax was corrected
before the second build. That build passed all six retained tests, while the
two new tests correctly rejected the declared RNG table containing12 zero bytes.
Capture1 and its successful source-only audit remain immutable; they are not
native matches. New tables derive from the verified CRT recurrence at seeds17/
ffffffff with3000 `rand()%255+1` bytes and the original zero terminator.
Capture2 stopped before any menu call because Unicorn requires Python `bytes`;
its bytearray type error is preserved. Capture3 uses the same generated values
with the corrected Python type. Native3 then exposed the new test adapter
passing an original atlas surface token as a semantic binding. Only the local
atlas draw view was normalized to1; exact owner/source/expected bytes and all
Native Core rules remain unchanged. Native4 passes the complete corrected input sequence and both late rollbacks.
The transport reader only joins and verifies the lossless research fixture;
it is test code, separate from the shipping game runtime.

Reselection/reroll options1/2, other game modes, count extremes outside this
finite ordinary sequence and the match-prelude continuation need their own
comparisons. The whole initialized library catalog is still running separately.
No full tick/match, Windows execution, app-window/device output, input latency,
audio fidelity, all-content/network or clean-Mac completion follows from this
controlled study. The full native-game goal remains open.

The subsequent [LIB_SELECTION_COMMANDS](LIB_SELECTION_COMMANDS.md) study now
matches338 retained calls through options1/2, including194 unchanged whole
parent calls, repeated Random, reselect and a fresh ready route. Its raw and
packaged tests and full provenance audits pass; this parent corpus is unchanged.
Other modes and the own initialized library/prelude/application join remain open.
