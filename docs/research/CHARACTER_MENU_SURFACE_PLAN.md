# Character-menu resources with whole bitmap helpers

Recover the whole4297ae..429e5a resource caller with all enabled43ee50
constructors,43ed10 image/surface loading,4013d0 copying and actual VC80 memset.
The older MENU_RESOURCES studies supply43ed10 results. This independent
controlled dependency replaces that boundary with executed original helpers;
it does not replace the ongoing own application catalog capture or prove an
installed-lib menu continuation, full menu return, app window or Windows device.

Reference: pinned original NTSD EXE, VC80 and all eleven embedded menu DIBs;
Unicorn2.1.4 with the existing BitmapSurface MMIO-free image/GDI/COM/allocator
adapters. Ordinary malloc, image and numeric API responses are declared inputs.
Normal constructor cookie checks execute unchanged. Instruction/stack/register
and allocation traces recover request order, dimensions, masks, SPARK writes,
resource lifetimes and the producer of any consumed retained storage. Native
must produce its own bitmap records and platform structures; private expected
stack bytes are never supplied as successful native state.

Entry is a declared controlled menu frame at1000f000 with EBP44d020 and an
opaque World token that this caller must not dereference. Whole frame backing,
menu/previous-menu/selection/load flags, device and initial wrapper globals are
explicit inputs. Caller locals and all stores/reads retain full bytes/masks.
Separate disjoint bitmap records have16-byte guards on both sides and0x2000
spacing. The experiment never edits live producers or source resources.

Finite initial set: A5 and ramp/reversed nominal; two disabled-load menu/selection
extremes; negative nonzero load flag; first/last/all/alternating NULL allocations;
first/last/all missing images; first negative and last positive CreateSurface;
first/last negative color key; last GetDC failure; last StretchBlt zero; last
GetSurfaceDesc failure; first GetObject failure. These last two may expose
unavailable private operands. Recover their actual prior writes/read masks before
deciding the native contract; do not import expected values or call rejection a
match. Keep any unresolved dependency open.

At NULL SPARK stop before actual429b21, because page zero is mapped for the
controlled SEH environment. Do not execute a fabricated successful null write.
Other unexpected faults/dependencies stop with their original error and full
available observations. No deliberate control/protection corruption, bypass,
fault continuation, exploit, external system or network operation is requested.

Acceptance requires full native records/masks/globals at every retained caller
checkpoint, actual ordered requests and native-produced outputs, helper return
and lifetime checks, and independent replay of all writes/reads. Ready returns,
NULL-SPARK boundaries and any unavailable-operand native rejection are separate
contracts. Only actually matched returns count as matches. A nominal producer
probe checks the observer before the finite capture, with distinct immutable
output names and no automatic retries. Add late native rollback checks to the
full resource operation and retain the existing menu/music/UI/bitmap corpora.

Keep all247 fixture pins, both live old captures, corrected primary4 and its
890 frozen watcher inputs unchanged. Package only newly accepted immutable
evidence after source/native verification. No unproven whole game, app, Windows,
audio/device or clean-Mac claim follows from this controlled dependency.
