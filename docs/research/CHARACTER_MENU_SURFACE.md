# Whole character-menu bitmap resources

The controlled4297ae..429e5a resource caller now uses the recovered43ee50,
43ed10,4013d0 and actual VC80 memset dependencies. Native
`OriginalMenuResourceLoading.loadWithSurfaceLoading` stages globals, all bitmap
owners and its value-semantic platform context. Its default historical resource
route remains available to the unchanged own startup/menu corpora. This study
does not turn those historical43ed10 result boundaries into an own full-helper
continuation, complete a menu return, or connect the application renderer.

Reference artifacts are the pinned original NTSD EXE, VC80 and eleven embedded
DIB resources. Unicorn2.1.4 executes their original code with the retained
image/GDI/COM and allocator adapters. The controlled frame is at1000f000;
EBP44d020, the opaque World token, menu flags, previous-menu/selection words,
frame inputs and allocation patterns are declared. World is never dereferenced.
No device, Windows heap, Windows GDI pixels or private native C++ ABI is measured.
The captured default CW0 is retained unchanged; there is no FPU behavior claim.

The [initial plan](CHARACTER_MENU_SURFACE_PLAN.md) specifies20 controls. The
[retained-dimension extension](CHARACTER_MENU_SURFACE_RETAINED_PLAN.md) adds7
controls after an observed composition dependency. All27 source calls reached
their declared boundary:25ready and2NULL-SPARK. Native compares23ready returns
and the two partial NULL-SPARK states separately. Two further source returns
consume unavailable operands and are explicitly rejected with complete native
rollback; they are not successful native matches.

Across the25 fully compared controlled states,345 checkpoints plus final states
compare2319 bitmap records/18589104bytes and their masks,4289 ordered events
(3802 platform requests), and905 helper returns:234constructor/234loader/
217copy/220memset. The23ready subset has2234 record comparisons/17907744bytes,
4087events/3632platform requests and224constructor/224loader/207copy/210memset.
Bitmap+0 uses the established opaque-surface-token to canonical1/0 binding;
every other bitmap byte and initialization mask compares exactly. Global wrapper
pointers retain the declared allocation tokens. These are controlled cases and observed instruction starts, not every branch
outcome or ordinary-game reachability of all injected API failures.

The resource prefix captures selection and replaces4512cc before allocations.
NULL allocations skip constructors. Missing resources retain wrappers; a failed
CreateSurface retains dimensions already written and does not DeleteObject.
A positive nonzero CreateSurface return can produce an unused platform surface
while the loader returns zero. Negative color-key results emit the original
diagnostics, release the surface and clear its wrapper pointer. SPARK still
receives its rectangle writes when its image/surface is unavailable, provided
the wrapper allocation exists. Seat arrays precede SPARK writes;44d07c clears
between width[13] and height[13]. The two NULL-SPARK captures stop before429b21's
write because the controlled SEH environment maps page zero. Native's standalone
API retains the partial state and cannot enter menu dispatch from that result.

Copy descriptor dimensions have a recovered lifetime within this contiguous
resource pass. With SPARK's description request failing,40147e/40148a read the
last native-produced descriptor height/width at the same helper frame depth.
The original SPARK image is612x256; successful RFACE description leaves120x120.
If RFACE was skipped by NULL allocation, missing image or failed CreateSurface,
the surviving CMC dimensions are365x103. A color-key failure happens after copy
and retains120x120; a failed RFACE description retains365x103. Both failed first
description and GetDC can leave unknown dimensions unconsumed, after which later
successful descriptions establish their own values.

Native `CopyScratch` retains only these eight bytes, produced from its own
platform responses, during one menu resource pass. It starts unknown and does
not import captured stack bytes, assume a prior caller lifetime, or expose a
general private-stack ABI. Current-call API write masks remain separate from
historical ownership: unknown outgoing private bytes are canonical zero in
Native, even when the separate lifetime record owns earlier dimensions. All
declared output fields are derived from the original DIBs and the native-created
surface descriptors; expected response structures are comparison-only.

The first failed GetObject reads unknown initial bytes at43ed79/43ed94 and is
rejected as unavailable bitmap width. A failed first description with successful
GetDC reads unknown initial bytes at40147e/40148a and is rejected as unavailable
surface width. The original controlled adapters allow those numerical calls to
return; this is not evidence that Windows would accept their dimensions. Keep
those exact source bytes and masks, and leave original prior-frame provenance
open. Native exceptions retain globals, bitmap ownership and the event context.
Six additional native-only failures after bitmap5, CreateSurface/DeleteObject/
colorKey11, SPARK's flag checkpoint and the final observer verify late rollback.

Independent replay verifies all40733 writes/192598bytes,10796 reads/42458bytes,
402 complete stack/global snapshots,4176 API global snapshots,962 API structures/
63492bytes and all993 source helper returns. All504 blobs/17195210decoded bytes
pass length/SHA verification. Source coverage contains613EXE and33CRT actual
instruction starts; stop boundaries and synthetic API entries are not executed
original instructions. The four unknown reads consume16 initial unknown bytes;
later stores do not retroactively recover those inputs' provenance.

The first observer inherited a d000..f400 stack snapshot. The independent replay
found already-traced constructor-unwind state writes at rootSP+aa0 outside it.
A distinct27-case observer adds a read-only full mapped-stack snapshot without
changing inputs, instructions or protection checks. Every previous case field,
373old blobs and11assets remains exactly equal after removing only the added
full-stack fields. The nominal probe also equals the first finite case. The
original captures remain immutable. The verifier initially used zlib framing
instead of raw deflate, then exposed this snapshot extent gap; both diagnostics
and verifier versions are preserved. A separate transport-packing attempt used
Python3.11's compress argument on system3.9 and wrote no artifact; compressobj
fixed that tooling mismatch. No expected bytes or native rules changed for these
tooling corrections, and no source fault or safety refusal occurred.

Raw acceptance passed13 release tests in88.119s/build223.87s, including both
historical menu startup/resource/repeated-call corpora, the prior bitmap helpers
and the10-case whole initial UI corpus. The4 owned native files were built over
f92ae48 in an isolated633-file archive;629base files and247fixtures are unchanged.
The final transport fixture contains the entire9382573-byte expanded source
capture, losslessly packed to1994747bytes. Final packaged2 release tests passed
2.806s/build222.35s through the default Bundle.module lookup, without raw override.
The634-file final input archive retains every633 previously tested file and adds
only the fixture. All248pins and the unrelated working/frozen inputs are checked.
NTSDNative linked in both builds; no application window or device was exercised.
See [evidence](../evidence/character-menu-surface.json) and
`build/research/character-menu-surface-work.json` for jobs, pins and verification.

All new source and native jobs are terminal0. The original own catalog2/3 later
ended at the preserved WAV research guard assertion; their audited20 snapshots
remain unchanged. Corrected primary4 continues with its frozen watcher untouched.
The full own catalog/loading, menu entry with installed library and complete
bitmap/music composition, app window, Windows/device, full match/all-content
and clean-Mac checks remain open. EXE/DLL execution and transport deflation are
research tooling, absent from the native shipping runtime.
