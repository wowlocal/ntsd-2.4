# Production catalog input package and reader

2026-09-26; base HEAD1d1ebed. Continue the completed
[host/app preflight](APPLICATION_HOST_APP_PREFLIGHT.md), using WORKFLOW and
TASK_TEMPLATE. The missing result is a self-contained original-data provider
for Catalog.Resources and the persistent host, toward the complete Naruto/Sasuke
District match and full game. Current production startup/common/interface/menu/
arena packages remain intact. Independent contract review is unavailable/open.

## Contract and fixed evidence

Reference: original NTSD EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
This task reads original resource bytes and saved metadata/input blobs as data;
no original game, emulator, historical capture/auditor or device executes.
Safety incidents remain open; this does not retry their affected operations.

Freeze the 521 file roles (156 catalog/DAT/Stage and365 WAV),669 image roles
(665 BMP/four embedded DIB) and8 baseline WMA roles before copying. Derive file
names/input SHA from the two immutable comparison envelopes used by current
CatalogFullReference; only input blobs are decoded. Maximum envelope raw sizes
are95,289,959 and47,296,738; maximum individual input blob100,000,000 bytes.
Do not inspect/decode expected state to construct runtime inputs. Join all669
images to the accepted input-inventory1/pixel-inputs1 manifests and exact baseline
bytes or pinned PE data ranges. Preserve original names/case, raw headers,
resource origins, duplicate content under distinct names and absent-versus-empty.
Count unique payload paths separately. No DAT precomputation, expected records,
device replies, executable/DLL code or temporary output goes into the package.

Use a versioned manifest with pinned digest and exact member/size/SHA contract.
Load regular files before a Core attempt into value-owned file/image/music maps.
Unknown names throw; file overlays distinguish absence from empty. Bitmap origin
uses the existing BMP/DIB initializers, retaining source bytes, metadata, colors
and masks. Loading a corrupt/partial/conflicting/nonregular package publishes no
value. Preserve all existing Core allocation, CRT, device and time controls.

## Candidate and validation stages

Start from the actual frozen1040-file gameplay-correction1 candidate, manifest
5505b78d77d5c92eb6932508deeab30184ee406452d22ce2522e151df70490b5.
Pin root1034, source55, prior candidate/closed task and current policy. Revalidate
terminal jobs; never restart prior source59727 or completed test queues.
Create a separate APFS-cloned candidate; root Native and old candidates stay
unchanged. Reuse regular-package tools/reader patterns and existing pin/manifest/
PAX verification, not a new execution framework.

Candidate stage implements the package builder, Native reader, package placement
and test input routing. Keep the old fixture input producer available for exact
comparison, with its expected state and masks unchanged. Four new check groups:

1. Complete original file/image/music bytes, origins and metadata against fixed
   independent input inventories, including every file header and DIB origin.
2. Owned snapshot and overlays after source files are removed or changed; unknown
   name and absent/empty distinctions; all old resource packages preserved.
3. Missing/corrupt/duplicate/conflicting/extra/nonregular package rejection and
   no partial publication; strict app lookup without module fallback.
4. Actual Catalog.Resources and whole host consumption of production inputs on
   both prior backings, retaining all69 methods,17/48/14 schedules, ownership,
   masks, late failure and retry. The old independent input maps compare before
   acceptance; don't derive expected values from this reader.

Candidate acceptance: finite input manifest joined bytewise to originals;
all regular package bytes/membership verified; implementation and syntax checked;
old expected/reference/resource pins unchanged; archived candidate/patch/metadata
verified. This is implementation only, not Native comparison acceptance. Commit
the coherent candidate before the separate build/comparison increment.

Validation then needs a fresh isolated release build, four new groups and all69
retained methods. Freeze exact selectors, child/batch time limits, memory and
storage configuration before launch using the existing guarded queue. No Native
compilation or XCTest is authorized by the candidate-stage bounds below; parse
checks are allowed. Separately verify all packaged bytes and an ordinary app
layout, and execute a Core-only reader client outside checkout/fixture/research
paths against that layout. This tests standalone input delivery, not a window,
WMA decoding/playback, Windows filesystem semantics or the full137 source return.

## Ownership and resource bounds

Task: /Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/application-catalog-package-20260926,
with build/research alias. Verify writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548; require70GiB external/7GiB internal.
Keep40GiB external/6GiB internal reserves and17GiB source commitment. Candidate
physical allowance12GiB; stop at11GiB observed decrease; task logical16GiB,
candidate/source package each below6GiB, archives<=8GiB, metadata<=16MiB,
root-owned metadata/tool additions<=4MiB. Candidate elapsed<=7200s. New package
payload<=700MiB, manifest<=2MiB; at most1300 unique payloads. Preserve any failed
attempt and diagnose before a separate correction; no automatic source retries.
T7 remains authorized for archives, but this candidate uses APFS clones on X5.

Mutable: this new task/alias; tools/package_catalog_inputs.py plus its pinned
manifest; isolated candidate Core reader, Package.swift, package resource files,
CatalogFullReference input preparation and new package tests; task-owned copy of
package_assets.py; this study/evidence and own navigation additions. No root
Native/tools/package_assets.py promotion yet. Existing source producers, policies,
archives, expected/masks and unrelated working bytes stay unchanged.
Record preparation/packaging/finalization commands, PID/start/cwd, input manifests,
terminal outcome and failure logs. Use existing helpers and archive procedure.

After candidate publication continue its bounded validation, then retained host
delivery owners/prepared backend contracts and actual AppKit input/render/audio.
Independent review, full catalog source/transport, device/Windows/clean-Mac/full
match/game remain open. EXE envelope will not be recalculated by this data step.
