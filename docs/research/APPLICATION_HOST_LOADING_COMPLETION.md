# Host loading validation completion

**2026-09-22: method28 timed out;27 prior methods remain verified and15
methods still need complete passing results.**
Native comparison remains incomplete. Independent comparator/contract review, root promotion and actual application
acceptance remain open.

The [frozen plan](APPLICATION_HOST_LOADING_COMPLETION_PLAN.md) continues the
[round1 candidate](APPLICATION_HOST_LOADING_CORRECTION1.md). It retains the
[original assertion failure](APPLICATION_HOST_LOADING_VALIDATION.md) and the
separate round1 resident guard. Root author performs verification; author review
is not independent review. See the [publication](../evidence/application-host-loading-completion.json),
[closure](../evidence/application-host-loading-completion-close.json) and
[documentation receipt](../evidence/application-host-loading-completion-docs.json).
No original source code, compiler/linker, actual
Windows/device behavior or refused operation is executed for this continuation.

## Inputs and scope

Base HEAD is `90fd80c1f3e592a06b860988cd0ebe999c3f2d19`. The exact corrected1037-file
candidate,62,536,360-byte binary and487 packaged resource files are read in place
from the frozen round1 task. The original candidate cwd and sibling resource
bundles are retained. New commands/jobs/logs/TMPDIR belong to
`build/research/application-host-loading-completion-20260922`; closed artifacts
are unchanged. This is the same42-method contract, with27 prior exact passes
and15 methods to complete, not additional source coverage.

Preparation PID35262 completed exit0 in8.000s. It rechecked all candidate and
package bytes, root1034 files,55 source code pins and every prior named pass/log/
actual exit. The monitor's three classification predicates are unchanged; its
execution/guard/reap loop differs only by receiving the verified artifact root
for cwd containment. Six synthetic cwd cases and20 phase-acceptance controls
passed. They are classification checks, not another original or Darwin experiment.

Whole method28 is rerun because its previous process was stopped before a named
result by the8GiB RSS guard. Only that method gets a declared12GiB ceiling, based
on the recorded24GiB host capacity; the other14 retain8GiB. No method body,
selector, candidate, expected byte, mask or comparison is changed. Per-method600s
and total3600s limits remain. The earlier guard/overshoot and reaped-15 remain
immutable; absence of an assertion in its partial log was never a pass.

## Validation

Queue PID35317 ran only method28, PID35339. The method reached its unchanged
600s limit and was reaped-15 after600.748s; the queue ended after606.508s.
The monitor retained the exact identity/cwd before SIGTERM and verified no
remaining process. Peak sampled RSS7,676,477,440 bytes stayed below12GiB. No
assertion error or complete named result appeared in the367-byte log. The fourteen
later methods were never started. The guard outcome is incomplete, not a pass;
all15 remain required. No automatic repeat occurred.

A separately declared one-second stack sample at5ms intervals was taken after
the method remained CPU-active for over400s. Native PID/start/command/cwd/job
were verified before this observation. Sampler PID37864 completed exit0 in2.011s;
its278,579-byte report and exact command/environment are retained. The report's
144 main-thread samples place the ticket-identity method at
`OriginalApplicationHostLoadingTests.swift:275`, inside XCTest's `XCTAssertNotNil`
and recursive Swift value debug printing. This is a sampled observation, not a
measurement of where the whole ten minutes went, nor a nil/non-nil or pass result.
The reported physical footprint9.9G/peak13.2G is kept verbatim and is distinct
from the monitor's `ps` RSS metric. Sampling may perturb timing; the600s limit
was not extended. An earlier `sample -h` usage query returned255 and did no sampling.

At that line, the test checks whether the large retained `PendingReturn` exists
using `XCTAssertNotNil(alternate)`. The supported next correction is a separate
test version with `XCTAssertTrue(alternate != nil)`: the same presence predicate,
with only Bool passed for XCTest formatting. All actual throw, ticket, state,
platform and full-payload comparisons must remain. This author diagnosis does
not retrospectively accept either guarded run and still needs independent review.

The finalizer verifies the27 prior named passes and current timeout separately,
rechecks the unchanged candidate/package and prior artifact archive, and archives
new metadata. Native comparison stays false. The original assertion failure and
both process guards remain linked immutable outcomes.

## Archive and limits

The prior regular artifact archive contains2482 files. Its recorded154 directories
are the two source traversal roots and their subdirectories; two destination
ancestors (`swift` and `swift/arm64-apple-macosx`) bring actual membership to156.
The new verifier checks that complete membership, original traversal directory
modes/mtimes and all file bodies/modes/mtimes. The old manifest is unchanged.
New small metadata is archived separately with exact nanosecond PAX mtimes.

The new allowance is1GiB observed free-space decrease including concurrent source
growth, runtime stop768MiB/closure896MiB, and64MiB new logical data. Original40GiB
external/6GiB internal reserves remain, with44/7GiB runtime floors. Source59727
is revalidated and preserved. Short-method RSS0 means no sample, not known zero.

Controlled Native loading/menu composition does not establish actual devices,
the running whole Catalog53 source, another returned fourth iteration or a game.
Later typed child retention, live providers/timing/window/input/audio, independent
review, root promotion, existing safety dependencies and clean-Mac/full-game
acceptance remain open.

Next: round3, a new isolated test-only correction of this presence assertion,
fresh compiled test candidate and the full42-method selection. No extra source
execution or broader time/memory limit is justified by this sample alone.
