# Retained copy dimensions within the character menu

The immutable20-case capture in CHARACTER_MENU_SURFACE_PLAN recovered a new
composition rule. In description-last-negative,40147e/40148a read height/width
at1000ede8/1000edec from the preceding RFACE description response: write1531,
event184,120x120. No intervening store touches these eight bytes. SPARK's native
descriptor remains612x256; its failed description request must use the retained
copy destination120x120. The caller and helper frame depths coincide.

The first GetObject failure instead reads eight initial unknown bytes at43ed79/
43ed94. Its controlled returned state is preserved as source-only evidence;
Native must reject unavailable bitmap width and roll back, never import A5.

Extend the same pinned NTSD/VC80/DIB/Unicorn2.1.4 study by exactly seven cases:
SPARK description failure after RFACE NULL allocation, missing image, negative
CreateSurface, negative color key, and failed RFACE description; first image
description failure; first and last description failures with first GetDC failure.
The final control distinguishes an unconsumed unavailable dimension from later
native-produced dimensions. Allocation/API inputs are ordinary declared results.
No live source input, old corpus, expected byte, guard, cookie or control pointer
is changed. The original20 capture and probe remain immutable and terminal.

Trace all writes and reads across the reused frame, including any intervening
CRT/helper/platform writes. Independently establish the last producer of each
consumed dimension byte. Native retains only its own platform-produced height/
width during this one contiguous resource pass; it does not import arbitrary
caller backing or claim other frame depths/lifetimes. Keep per-helper request
write masks distinct from historical provenance. Unknown first-description
operands remain explicit rejection boundaries if actually consumed.

Acceptance includes all prior20 outcomes under their separate contracts and the
seven new cases, full records/masks/checkpoints/globals/events/lifetimes, previous
bitmap/UI/menu corpora and late native rollback. No whole own menu/Windows/device
or full-game claim. Unexpected faults retain exact error/observations and stop
without automatic retry. This extends a game-resource compatibility study only.
