# Continuous character-menu music and bitmap initialization

The subsequent [fresh library menu](FRESH_LIBRARY_MENU.md) joins music and all
11 bitmap constructors to the controlled screen/output/actual ret4:22 returns
and two playback continuations match, with two explicit NULL-SPARK rollbacks.
Full historical stack masks and current screen-local write lifetimes stay separate.
Own initialized catalog/menu and the earlier tick body remain open.

This study recovers the original menu entry from4229cc through the429730
prologue, music and every resource helper to429e5a. It supports the native game
menu. It is a controlled prefix, not an own installed-lib catalog continuation,
a returned character menu, an app window or a Windows/device measurement.
The finite criteria are in [the initial plan](CHARACTER_MENU_MUSIC_SURFACE_PLAN.md)
and [the seven-case storage extension](CHARACTER_MENU_MUSIC_LOG_PLAN.md).

The pinned NTSD EXE and VC80 DLL execute on one Unicorn2.1.4 CPU. Eleven original
embedded DIBs supply the image dimensions. The declared COM, image/GDI, allocator,
file and C-locale/PTD adapters retain numeric errors and output-write rules.
Actual CRT sprintf and memset execute. No EXE, DLL, emulation or Windows API
implementation enters the shipping native runtime.

Actual caller pushes and the prologue take rootSP1000eab4 to bodySP1000e000.
The target is declared at root+68; EBX is an opaque World token, never read as a
World on this prefix. The original writes its normal SEH/cookie state; all guards
and applicable helper cookie checks remain enabled. The menu's enclosing
protection/SEH cleanup has not returned. CW037f is a declared research input and
is unchanged, not a host FPU claim. The stopped429e5a is excluded from coverage.

The14 initial controls cover ordinary menu gates, cached/disabled music,
negative graph/render results, NULL wide-path allocation, ramp/reverse backing,
bitmap-load disable, retained copy dimensions and the first unavailable image
operands. Seven additional controls recover the first GetObject failure after
music gates/errors and empty/40-byte directories. All21 source prefixes reach
429e5a; that does not make their unknown-operand cases native matches.

## Recovered graph-log producer

The actual sprintf output starts at1000dee4. The subsequent loader43ed10 reuses
that address for its24-byte BITMAP record. On a failed first GetObject,43ed79
reads width at+4 and43ed94 reads height at+8. For `C:\NTSD\graph.log`, these are
5453445c and67726170: the bytes of `TSD\` and `grap`. Each byte's last producer
is actual CRT78141684, with no intervening replacement. Negative RenderFile and
NULL wide allocation retain this same producer. A40-byte directory also owns
both complete fields.

Failed graph creation, cached music and disabled music create no graph-log
string; their first dimensions remain unknown. With an empty directory, the
output is `\graph.log\0`: width is fully known, while height contains `6f6700`
followed by one unknown byte. The first failed copy description consumes a
separate eight-byte area at1000dde8/ec, still unknown despite preceding music.
These five cases are explicit native rollback rejections, not matched source
returns. The source platform adapter accepts supplied surface descriptors;
this is no evidence that Windows accepts their large dimensions.

The independent verifier replays every write/read against full stack, SEH,
globals, CRT data/PTD, bitmap and music allocations. It verifies all219 loader
entry dimension lifetimes, including each partial mask. Actual graph-log bytes
or preceding GetObject responses explain the retained loader fields; the
previous surface description explains retained copy fields. Current-helper API
request masks remain distinct from historical storage ownership.

## Native composition

`OriginalCharacterMenuStartup.runWithSurfaceLoading` shares the original
music-before-previous-menu-write transaction with the existing resource route.
The native music routine creates the graph-log string. Its format event seeds
only the recovered eight loader dimension bytes, including a NUL when present.
Subsequent native GetObject responses replace their own bytes. The standalone
loader and constructor start with unknown scratch; the menu retains it only
for this contiguous call depth. No source private backing or expected result
is supplied to the core.

Globals, music allocations, bitmap owners and the value-semantic platform
environment commit together after the final observer. Thrown dependencies or
observers retain all four caller values. Numeric API errors preserve original
cleanup/continuation rules. The standalone NULL-SPARK boundary remains separate;
the enclosing startup transaction retains its explicit rejection contract.

Sixteen complete controlled prefixes are compared through227 resource
checkpoints and final state:1650 bitmap records/13226400 bytes plus masks,
18 music allocation snapshots/468 bytes plus masks,3427 ordered events
(2785 image/GDI/COM requests and312 music events). The compared helper returns
are165 constructors,165 loaders,163 copies,164 memsets,11 sprintfs and61 music
helpers. Five unknown-operand prefixes are rejected and rolled back separately.
Eight native-only late failures cover afterMusic, bitmap5, CreateSurface,
DeleteObject, colorKey, SPARKflag, final resource checkpoint and beforeCommit.
They add rollback evidence, not source instruction coverage.

The original21-source corpus has42410 writes/195385 bytes,15770 reads/60213
bytes,344 full snapshots,4099 event-global snapshots,874 API structures/57684
bytes and529 independently decoded blobs/18965218 bytes. There are890 bitmap/
CRT helper returns and77 music helper returns.922 EXE and335 CRT instruction
starts execute;13 controlled `_getptd` boundaries are excluded. Nine reads
contain33 unknown bytes in the five rejected cases. No every-branch, hardware,
full-game, application or actual device coverage is claimed.

## Verification and remaining work

Source jobs completed normally: nominal probe1.237s,14-case capture3.412s and
seven-case extension2.403s. The first producer is preserved verbatim before its
single directory-input generalization; both old captures remain immutable.
The combined corpus retains every parent case, blob and embedded resource.
The isolated raw acceptance passed15 release tests in89.487s after a218.86s
build. The new two tests passed2.362s; retained bitmap/UI/startup/repeated-menu
checks retain their original assertions. All248 old fixtures remain unchanged.
The combined raw corpus is10208849 bytes, SHA256
`6c06a17885eaae0eb8308387b67ca0a96c414966fab674878f2d7f34fc1d9553`.
Its249th fixture is2276096 bytes, SHA256
`4ec002db9419a58bbb58deb785ba6d861571cd8b0a451118acace2fbce92672c`.
Full raw/packed bytes, JSON, all529 blobs and both immutable parent corpora match.
The635-file raw archive has630 unchanged accepted base files. A read-only
atomic-part verifier initially required the merged11-image asset set in every
case, including skipped loading. Its assertion is preserved; exact per-entry
subset checks now verify all21 parts without changing a source/expected byte.
Native implementation and tests passed on their first build; existing unrelated
inferred-Void/trailing-closure warnings remain.
Final default-package acceptance passed2 release tests in2.389s after a217.81s
build, without a raw override. All636 archived files were verified, including
all635 unchanged raw-test inputs. NTSDNative linked; no window/device was run.
All study source/SwiftPM jobs are terminal0. Evidence is in
[the published record](../evidence/character-menu-music-surface.json) and
[the work ledger](../../build/research/character-menu-music-surface-work.json).

Own installed-lib catalog/loading composition is still running independently.
Its full return, use of the complete helpers in that own menu chain, later menu
dispatch/return, app rendering/audio/input, Windows comparisons and the full
match/all-content/network/clean-Mac goal remain open. Earlier unknown backing
outside these recovered log/GetObject/description lifetimes stays unknown.
