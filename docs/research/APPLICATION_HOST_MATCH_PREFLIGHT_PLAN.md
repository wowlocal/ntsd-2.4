# Host retained Start continuation — frozen preflight plan, 2026-09-22

Follow [WORKFLOW](WORKFLOW.md) and [TASK_TEMPLATE](TASK_TEMPLATE.md). This is
the next bounded task after [42 host loading regressions](APPLICATION_HOST_LOADING_REGRESSIONS.md).
The game question is how the host can keep its actual pending Start and platform,
continue match preparation, and publish the enclosing application iteration once.
It advances the route to the first Naruto/Sasuke match; the complete game remains
the goal. Base HEAD: `c65662191bf7a587e059b88f619057d7ab561c40`.

The actual input is the frozen1037-file
`build/research/application-host-loading-correction3-20260922/candidate1/native`
and its retained round3 binary/regression publication, beside the live-pinned
root1034 files. HEAD alone is not the input. Pin consulted code, tests, studies,
saved envelopes and existing manifests before recording final conclusions.
Reference EXE SHA-256:
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
Original resources, DLL/CRT and controlled-source boundaries remain those in
the loaded-selection/launch studies and direct dependencies.

This task reads code and saved evidence. It performs no original execution,
Native build/test, historical producer/auditor rerun, device IO, install or
refused operation. Read the immutable instruction archive's prelude/selection/
launch/ownership constraints. Existing safety incidents remain open. In
particular, reading saved replay-initialization evidence does not authorize the
blocked round-result/replay-writer path.

## Finite questions and outputs

1. Trace the current host ticket, startup/loaded owners and staged platform into
   `LoadedMenu.Outcome.matchPrelude`/`PendingMatchPrelude`, its launch consumer,
   returned child and Bootstrap tail. Distinguish the unreturned Start from a
   published iteration and identify all retained state/journal/graphics owners.
2. Inspect exact-attempt validation, sibling/foreign/stale rejection, failure and
   retry boundaries, deep-copy requirements, callback/reentry and publication.
   Identify static integration gaps without calling them reproduced failures.
3. Select existing whole-caller and late-rollback comparisons for a finite host
   extension. Inspect at most four relevant saved compressed envelopes, as data,
   verifying declared length/SHA before using observations. Each decoded envelope
   is bounded to256MiB; cumulative decoded bytes512MiB. Decode sequentially and
   retain only scalar inventories. No trace replay or source execution.
4. Write an actionable API/storage contract and bounded next candidate scope,
   including actual retained-parent construction, exact method selection and
   failures to preserve. Explain which guarantees are historical comparison,
   new static analysis, declared Native controls or still unknown.

## Limits, ownership and verification

Root author only; an independent reviewer is unavailable, and that gap remains
open. Read-only preflight is bounded to1800s and1MiB new local metadata. No new
external task/build is needed. Preserve the40GiB external/6GiB internal reserve;
the live source retains its separate17GiB gross budget. If X5 becomes unavailable,
pause dependent reads and continue only independent work.

Mutable paths: this plan, `APPLICATION_HOST_MATCH_PREFLIGHT.md`, matching
`docs/evidence/application-host-match-preflight*.json`, and only the new
CURRENT_WORK/RESEARCH_MAP navigation paragraphs. Root Native, original/expected
bytes and masks, old plans/candidates/results/archives, source55 pins and unrelated
dirty files are protected. Revalidate source PID/start/command/cwd/job; do not
restart or signal it for silence. No automatic experiment retries.

Acceptance: all four questions answered; consulted inputs and root1034/candidate1037/
source55 preserved; valid JSON/local links and bounded receipt size; exact Git
scope checked and committed with existing hooks. No new Native comparison,
root promotion, EXE-envelope recalculation, device or full-game acceptance follows.
The next implementation requires its own frozen candidate/build/validation plan.
