# Active result recording from current notices output

Status: accepted in the finite scope; see [results](APPLICATION_ACTIVE_RECORDING.md).
The frozen candidate1 plan remains immutable. Parent clean commit
`b93c09524c2dcc2e129daf1f3bc78f71ca0d635a`, accepted
[notices](APPLICATION_ACTIVE_NOTICES.md). Follow [WORKFLOW](WORKFLOW.md),
[RESULT_RECORDING](RESULT_RECORDING.md), [GAMEPLAY_RESULT_RECORDING](GAMEPLAY_RESULT_RECORDING.md),
[RESULT_RECORDING_PLAN](RESULT_RECORDING_PLAN.md) and immutable archive2459..2525.
Consumer: actual result continuation → layout/output → complete active tick,
Naruto/Sasuke District match and full standalone native game.

## Evidence and finite contract

Read existing active48 fixtures only: SHA256
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
Original EXE `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Saved pristine EXE/VC80 synthetic API execution and own library-enabled Native
application are separate environments. No Windows/device/host IO observation.

Both48 stage16 cover421cdc..422218 with actual skipped-table endpoint422944,
SP1000e9bc, joining notices and layout. Inventory1 finds timer450bdc0 on every
source call, elapsed450bbc17..64 before the call and18..65 after, exactly one
four-byte store at421ce6 per call. Full pool/masks, recording/saved/pointers,
all other globals and retained components remain identical. early.globals and
state.globals alias the same record: two component hashes are not two mutations.
Source events/helpers/effects/boundaries/checkpoints/undefined reads and watched
stack accesses are empty. Seven executed PC addresses plus unexecuted stop form
a set, not a dynamic ordered trace. Source FPU CW023f/FPSW0or4000/tagffff and
scratch/spawn/stageDefeated are preserved; no new Native ABI/FPU claim.

Independent finite specification reads signed timer450bdc. Unsigned wrapped
(timer-101)<=248 selects the result-table continuation and is explicitly outside
this active no-writer comparison; it must not be silently skipped. Otherwise
continuation is422944. Signed timer<100 increments current450bbc with32-bit wrap;
other timers leave it unread. There are no effects or replay mutations on this
path. Mode, serialization flags, Actors/Object IDs and stageDefeated are not
causal on this branch. Never import source SP64 or require a known round word
when unread. Actual own round result still passes through the enclosing public
body into the recorder; the subsequent layout consumes its actual continuation.
The recording checkpoint does not expose Result.continuation. The public-handler
control checks that value directly; the existing Body passes result.continuation
straight to layout. Snapshot equality alone is not proof of that value.

## Implementation and discrimination

Three new test-only ActiveRecording Projection/Source/Tests files. Derive source
expected globals from its actual before-state, compare all global bytes and full
pool/masks plus other retained state/component identities, current stores, joins
and zero effect extent. Then derive own expected globals from actual own notices
snapshot. Compare full match/state, memory/replay/graphics owners, resources,
music/backgrounds, menu-local and chronological journal at recording. Reject
any output inside the interval and misplaced recording annotations. Parent whole
return/graphics checks and late rollbacks remain included. Neither source after-
state nor private source ABI nor unknown string backing becomes an own input.

Controls: timer99/100/350, signed Int32.min/max, elapsed Int32.max wraps; reject
result-range endpoints101/349; unknown timer or incremented elapsed is rejected,
but unread elapsed at100 stays unknown and unchanged. Poison unused mode/result
flags and pass nil stageDefeated to a separate public handler control on current
own state: no codec/file/provider calls, exact own state/context retention except
elapsed. These are controlled Native/comparator trials, not natural source claims.
New failures at recording checkpoint and beforeCommit restore entry/environment;
retry in the same session must reproduce full returned state/journal/graphics.

Retain controlled95 result calls:82writers/12playback restores,13no-writer gates,
76codec successes,5allocation(-4),1no-temporary(-2),18322writes/33219692bytes.
Numeric IO/codec failures remain ignored where original ignores them. The writer's
separate21matches/5source-fault rejections, full aliases/cleanup/restore order,
missing-stage and late-destruction rollback remain their prior contracts. This
new active branch does not establish a naturally completed recorded replay.

## Ownership, limits and gates

Root owns new3 Swift files, this plan/result/evidence and CURRENT_WORK/RESEARCH_MAP.
Update the map's stale static-only result-writer row using already verified95
controlled results and current finite evidence, preserving historical plans.
war_contract_review owns numbered independent read-only reports. No edits to Core,
old1015 Native files, fixture/resource bytes/masks, failures, frozen producers or
immutable archive. Freeze actual inputs/plan; independent review before Native.

Task `build/research/application-owned-active-recording-native-20260914/`, X5
APFS UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`; initial.json pins1015 actual
files. Keep40GiB external/6GiB internal reserve and40GiB task cap. Read96calls/300s;
Native3600s; three candidates before renewed contract diagnosis. Reuse parent
freeze/job/readback/archive tools, including exact singular/plural XCTest metadata
handling. Preserve all old logs and never rerun a successful Native job for parsing.

Select five release methods:2 new Recording,2 retained Notices and1 controlled
OriginalResultRecordingTests (including its missing-stage/late-destroy controls).
Derive inherited methods by exact parent verification identities and unchanged
file pins. Actual parent164 includes the two Notices and one ResultRecording
methods reselected here;161 inherited +5 direct =166 unique. Expect1018 Native files, all1015 old exact,485 package files unchanged.
Native comparison, package bytes, full archive bodies/modes, independent publication
review and Git delivery are separate gates. No original/emulator/historical
producer/capture/auditor executes; safety incidents remain open without rerouting.
After acceptance continue layout/output. Full active tick, wider gameplay branches,
formatter provenance, full match/game, app/window/input/audio and actual device/
Windows/clean-Mac remain open. EXE envelope is not recalculated.
