# Native character-menu startup transaction

Recover the ordered menu continuation4229cc->429730..429e5a: conditional
main-menu music precedes all eleven bitmap resources, and the music condition
reads the previous-menu word before the resource prefix replaces it. Expose
that existing native sequence as one operation preserving globals, music
allocations, resource ownership and buffered platform effects on failure.

Reference: unchanged pinned NTSD EXE/VC80/original resources in the historical
MENU_STARTUP and MENU_CYCLE controlled Unicorn chains. Their real helper calls,
full states, masks and events already recover this ordering. This composition
does not run new original instructions, import after-state/private storage,
change game rules, or close the separate installed-lib own catalog dependency.
The existing MUSIC_PLAYBACK and MENU_RESOURCES corpora retain their distinct
controlled branch and explicit fault-boundary coverage.

Finite acceptance: both full MENU_STARTUP parents through music and all eleven
resources; both repeated MENU_CYCLE chains with retained music/bitmap owners;
both187-case MENU_RESOURCES corpora including their music/round/replay parents.
Keep exact old comparisons and expected artifacts. Add native-only failures
after completed music, a later bitmap, the SPARK flag checkpoint and final
observer, plus the explicit NULL-SPARK rejection. Use actual native entry
globals and only declared allocation/platform inputs from the retained corpus.
Verify no candidate globals/music/bitmaps/environment publish on any throw.
An explicit rejection is not a successful original fault match.

Build an isolated export of accepted ebe460a plus only the owned core,
reference and test changes. Keep the pending catalog14 files, six foreign
files, live original producers and all frozen watcher inputs untouched.
Verify all247 accepted fixture hashes and exact tested archive contents.
Update evidence and save the checked composition separately. Full own loading,
menu dispatch/render/return, app/Windows/devices/fullmatch/all-content/cleanMac
remain open and must not be replaced by this bounded acceptance.
