# Owned neutral gameplay state and effects

Accepted finite comparison: two own17-call sequences,19 stages per call,
34 complete Bootstrap returns. Parent scalar checkpoint: `5fa4aed`.
[Plan](APPLICATION_GAMEPLAY_EFFECTS_PLAN.md),
[evidence](../evidence/application-gameplay-effects.json).

Independent input calculations first reproduce all retained source streams,
then calculate Native expectations from own coordinates, RNG, loaded objects,
bitmap/graphics owners and WAV buffers. Source output never enters Core.
Scalar input/output retains the accepted trace-guided store footprint.

The comparison covers camera parallax and animation, signed Z ordering,
blink/shadow/sheet/name drawing, all eight HUD cells and bar rectangles,
every bitmap read and mask, clipping, diagnostic text and installed-library
SetBkMode/DC retention, four font passes, own recording filename, presentation
and queued sound. The source pristine text contract uses SetBkColor; this is
kept distinct from the installed library. Synthetic HRESULT/DC/device responses
are declared inputs, not actual Windows or device observations.

Complete chronological journals and independently expanded graphics commands
compare current resource identities, DC generations and source-color bindings.
All scalar records/masks, own Frame storage, Actor/World aliases, retained
allocations, image backing, replay/music/interface/library owners and local
caller unknown bytes remain checked. Source lifecycle events and RNG helper
returns are checked separately from front events, including empty branches. The optional active-only `effects` field is absent
in the neutral corpus; its absence is not an extra captured event stream. The
finite non-front checks use the available helper and typed lifecycle evidence.
The diagnostic reads World+1bc (Actor10 in the saved case); healthbar uses the
current owner, not source bitmap ordinal839 as a Native identity.

Fourteen body rejection trials and twelve outer-tail rejection trials retain
the first and seventeenth inputs and earlier committed state. Stops occur after
generated drawing, text and queued sound, and at the inner/outer commits.
These are Native observer/IO failures, not additional source fault matches.

Release4 methods passed in 71.192s; build
381.09s.141 prior methods are inherited through unchanged
code/test/fixture pins, not rerun for counts. All989 Native files,383 fixtures,
102 resources and485 package bodies/modes verify;985 old Native files unchanged.
Full Native and evidence archives were read back and verified separately.

Candidate1 lost its process and tool handle during compilation without a
recorded exit code; its running job/log and interruption record are retained.
Candidate2 compiled but both tests rejected a legitimate shared bitmap wrapper
because the new uniqueness guard was too strict. Candidate3 verifies both alias
records and their surface before using current memory. Review also corrected
the unused declared queriedAudio value to its saved0x31002400. All failed files
and logs remain separate from the accepted candidate.
No original execution, emulator, historical producer/auditor, or blocked operation
was rerun. No Core rule, original expected byte or mask changed for acceptance.

This accepts the finite neutral comparison. It does not establish active combat,
a full match, other modes, networking, a rendered device image or actual audio.
Next: carry own input through the already saved active/paused gameplay branches,
with independent state/effect calculations and rollback, then continue toward the
full native application. Existing raster/Windows/clean-Mac and safety boundaries
remain open. The full goal is unchanged.
