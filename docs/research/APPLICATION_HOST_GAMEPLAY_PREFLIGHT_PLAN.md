# Host gameplay input and body retention — read-only preflight

2026-09-22. Follow [WORKFLOW](WORKFLOW.md), [TASK_TEMPLATE](TASK_TEMPLATE.md) and
the [53-method host result](APPLICATION_HOST_MATCH_COMPLETION.md). Base HEAD
`d1e37997e42d854581e2ee2a2bae086d078886da`. Trace the actual host's next input
through the existing gameplay/paused body and whole outer return, toward the
first Naruto/Sasuke District match and the complete native game.

Actual input is the frozen1039-file correction1 candidate, its unchanged binary
and53-method completion publication, alongside protected root1034. Common files
read from root must match the actual candidate before their contents support
conclusions. Pin consulted code, tests, studies, manifests and saved fixtures;
HEAD alone is not the working input. Instruction archive and source55 remain
immutable. Relevant historical loaded/continuous/paused contracts and newer
application neutral/active-output/paused evidence resolve old NEXT prose.

Reference EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Existing accepted source/Native comparisons retain their declared pristine vs
installed-library, CW, resource/API, unknown-storage and reachability boundaries.

## Finite questions and outputs

1. Trace LoadingContext/actual cycle into Input.PendingContinuation, including
   current loading ticket versus historical pool entry, input context, state,
   match/library/music/replay/resources, commands, round result, journals and
   graphics. Trace GameplaySession's normal/paused branches and PendingReturn
   through Bootstrap and the host's final platform/batch installation.
2. Define host retention, exact-attempt validation, deep-copy/publication, retry
   and reentry requirements. A dispatcher must decide from one actual input
   outcome; it must not guess a branch before acquisition, reset state or repeat
   completed input to choose a different public API. Preserve current menu/Start
   APIs and53 regressions while describing the new common outcome boundary.
3. Inspect six saved outer envelopes: continuous-gameplay, active-gameplay and
   paused-gameplay, each primary/control. Verify candidate fixture SHA and
   declared outer decoded length/SHA before reading scalar inventories, case
   order, stage/return boundaries, acquired inputs and paused flags. Sequential
   raw-DEFLATE decoding only,128MiB per envelope/384MiB cumulative. Do not
   reinflate inner blobs or claim a new full record/store audit. Existing source
   readers/comparators and their historical validation supply that separate gate.
4. Specify a bounded candidate/test scope that preserves full neutral17,
   active48 and paused14 schedules and their actual host parents, whole
   state/effect/journal comparisons and late failures. Select exact existing and
   proposed methods, distinguish static inference from historical observation,
   and identify open dependencies before implementation.

## Operations, limits and ownership

Read-only analysis plus at most1MiB new local plan/study/JSON metadata;1800s
preflight. No new large external task is needed. Verify X5 APFS UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548` before dependent reads; preserve original
40GiB external/6GiB internal reserves and source17GiB gross commitment.
Revalidate source PID/start/full command/cwd/job; do not restart or signal it.

No original execution, Native build/test, producer/auditor replay, device IO,
sampling, install or safety-refused operation. The round-result/replay-writer,
Actor+7b4, War allocation/resource-fault and Windows ISO incidents remain open;
reading their saved evidence does not authorize retries. Exact unavailable
triggers remain unknown. Native body/provider interfaces retain their existing
ordinary unsupported/error boundaries; this preflight does not execute them.

Root author owns this plan, APPLICATION_HOST_GAMEPLAY_PREFLIGHT.md,
docs/evidence/application-host-gameplay-preflight*.json and only new CURRENT_WORK/
RESEARCH_MAP paragraphs. No Core/test/expected/mask/fixture/resource edits,
promotion or changes to frozen plans/candidates/results. An independent reviewer
is unavailable; author inspection cannot close that gate. Reuse existing
preflight pin/fixture-inventory/JSON/link/commit procedures, not a new framework.

Acceptance: answer all four questions, preserve root1034/candidate1039/source55
and consulted inputs, verify JSON/local links/metadata size and exact Git scope,
then commit the coherent preflight with hooks intact. This adds no Native
comparison, EXE-envelope update, device or full-game acceptance. Implementation
and its complete regression/build/package/archive checks require a separate
bounded plan. Missing dependencies remain explicit rather than becoming silent
exclusions or fabricated gameplay returns.
