# Computer selection and arena continuation with installed-library text

Continue [LIB_CHARACTER_ROSTER](LIB_CHARACTER_ROSTER.md) into the remaining
VS selection state machine. Existing [MATCH_SELECTION](MATCH_SELECTION.md)
accepts the pristine own zero-computer path, but positive computer character/
team selection is explicitly unsupported in Native. Do not replace it with0.

Recover full computer-count42b296..42b964, computer character/team42b964..42cb86,
then VS settings/arena/music configuration through the actual42cf8a match-prelude
boundary or ordinary menu return. Preserve original human-seat prefix, shared
input latch, current computer-seat binding, roster eligibility/Random, team
exclusion, selection-at-entry gating, RNG consumption and output order.
Other mode dispatch and match preparation remain distinct dependencies, not
successful full returns. Keep the original full-game objective open.

Reference files: pinned NTSD EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
bundled lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
original DAT/BMP/DIB assets and accepted immutable catalog/menu corpora.
Read-only disassembly first identifies real instruction starts, register/stack
aliases and exact helper continuations. It does not establish dynamic coverage.
Subsequent controlled Unicorn2.1.4/CW023f calls must execute the original
instructions with declared API/COM/GDI/allocator/file/caller boundaries. This
is research tooling, never an EXE/DLL/emulator in the shipping runtime.

Before a new capture, finalize a finite manifest of ordinary selection inputs:
count navigation including positive values, press/hold/release, computer
character/Random and team confirmation, and entry to arena/Start. Input backing
needed beyond the existing roster must first be derived from original producers
or explicitly declared as controlled entry state. Keep these views distinct
from the unfinished own library catalog. Preserve prior menu owners and values
between calls; never import expected after-state/private stack bytes into Native.

Acceptance: whole declared original calls/continuation boundaries; exact full
World/Actor/globals/owned resource bytes/masks, ordered bitmap/text/sound/RNG/
configuration events, source helper returns and caller counters; independent
read/write/checkpoint/provenance audit; native whole-call rollback after a
selected-computer output and late arena output. Preserve old256 fixtures and
frozen/pending/foreign files, and use isolated accepted native inputs. New source
faults or unrecovered dependencies must stay explicit, not counted as matches.

No deliberate pointer/protection corruption, arbitrary post-fault execution or
platform-safeguard retry is part of this study. Preserve any exact refusal and
continue independent permitted work. Revalidate the ongoing full catalog5 CPU
by its actual handles and never restart it for quiet output. Source/Native
comparisons alone do not prove own full initialization, Windows/device pixels,
input latency/audio, a full match or clean-Mac/all-content/network completion.

## Finite controlled study

The manifest in `build/research/lib-selection-stage-finite-inputs.json` contains
252 calls in two retained sequences. Both begin with the original fresh bitmap
startup and ordinary Naruto/Sasuke selection. The control puts both humans on
team1; this changes a declared button from the earlier roster sequence. Each
then accelerates the countdown, wraps the computer count, confirms two CPUs,
returns to count, traverses first/last/Random characters, cancels character/team
selection and returns to the preceding CPU. Both complete the two CPU teams,
enter settings, select Random/Lee On Road/District, change difficulty/music
including OFF with the actual402100 child, then stop before42cf8a. Ret4 remains
required on every prior call. Reselection/reroll options1/2 and other modes are
explicitly separate from this finite capture; no full state-space claim.

New controlled initial globals declare count-100/currentCPU0/list0, arena100/
random1, musicRandom0, difficulty0, no Start shortcut and RNG index/counter0.
The3000 RNG bytes are derived from the verified VC80 recurrence at seeds17 and
ffffffff, each `rand()%255+1`, followed by the original zero terminator. These
are declared entry values, not a recovered initialized application table. Preserve their source and native
consumption exactly. Consequently the new prefix is not claimed to reproduce
the prior70-case complete state unchanged. The prior fixture is immutable and
will be checked as a separate regression after shared Native changes.

Arena operand views contain only the actual registered DAT names (original
29-byte truncation/underscore conversion) and constructor names at99/100.
The catalog background count comes from original `data/data.txt`. Source metadata
must pin every raw file; Native must use its normally loaded catalog backgrounds.
No complete synthetic background record or unknown source backing becomes a
Native input. COM control+24 and position+20 return the declared failure HRESULT;
the latter receives actual positive-zero binary64 bytes. Neither numeric error
stops the original menu. These controlled responses are not device evidence.

Accept full state/event/counter comparisons at each body boundary, plus late
computer-portrait and arena-text rollback with every earlier call retained.
The source audit must also replay all recorded reads/stores through these
boundaries and the unreturned42cf8a caller, preserving its pending helper.

Capture1 used an incorrectly declared `(17*i+3)%256` table containing12 zero
bytes. Its252 source calls and independent full audit are retained unchanged,
but Native correctly rejected that table; neither new test passed. The six
existing regression tests did pass. Source1/plan1/verifier1 snapshots, exact
raw SHA, native inputs/logs and `lib-selection-stage-rng-input-error.json`
preserve that boundary. It is not a source crash or successful native match.
Capture2 must freshly execute the same finite button manifest with the table
derived from [RANDOM_INITIALIZATION](RANDOM_INITIALIZATION.md). No Native RNG
rules, source1 expected bytes or old fixtures are changed to obtain acceptance.

Capture2 stopped during the initial table write because Unicorn's Python
binding requires `bytes`, not `bytearray`. Its exact ctypes error, job/log,
producer/plan snapshots and pins are preserved; no menu call ran. Capture3
converts the same generated bytes to the required Python type. This is a host
harness setup error, not a game fault or safety refusal. The finite buttons and
declared generator inputs are unchanged from capture2.
