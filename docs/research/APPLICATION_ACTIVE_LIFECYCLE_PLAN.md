# Active post-draw lifecycle comparison

Status: saved-input/transient-lifetime prerequisite independently verified;
new Native lifecycle comparison is not yet implemented or accepted.
Parent: `6c4af220209e1744b4a072b11a6e20ce5fac2167`, clean working tree before
this prerequisite. Follow [WORKFLOW](WORKFLOW.md) and [CURRENT_WORK](CURRENT_WORK.md).
The consumer is the remaining active body, then a complete Naruto/Sasuke District
match and the full standalone native game in [CONTINUE_GOAL](../CONTINUE_GOAL.md).

## Evidence and scope

Continue from the accepted [camera/drawing/impulses](APPLICATION_ACTIVE_GRAPHICS.md).
The required stage is the complete live400-slot pass `41f550..4214cf`, ending
before `4214d5`, source SP`1000e9bc`, for both retained48-call schedules.
Use current states independently on the pristine source and library-enabled
owned application sides; their coordinates, RNG and animation are not identical.
No source case number, position or event time supplies a Native expectation.

Pinned source EXE SHA256:
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
VC80 SHA256:
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
The pristine captures do not load bundled lib.dll, whose identity remains
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Both active fixture SHA256 values, parent joins and the completed producer are
pinned in the [provenance evidence](../evidence/application-active-lifecycle-provenance.json).
Read [ACTIVE_GAMEPLAY_CAPTURE](ACTIVE_GAMEPLAY_CAPTURE.md),
[POSTDRAW_SLOT_PREFIX](POSTDRAW_SLOT_PREFIX.md), [ACTOR_SCHEDULER](ACTOR_SCHEDULER.md),
[POSTDRAW_OPOINT](POSTDRAW_OPOINT.md), [POSTDRAW_LIFECYCLE](POSTDRAW_LIFECYCLE.md),
[GAMEPLAY_LIFECYCLE](GAMEPLAY_LIFECYCLE.md) and the matching archive constraints.

The new [provenance inventory](APPLICATION_ACTIVE_LIFECYCLE_PROVENANCE.md) resolves
the first dependency: why slot50 appears and disappears inside source call17.
It does not itself compare the complete Native pass. Existing controlled
5432-case lifecycle and6084-case scheduler evidence remains separate and immutable.

## Required comparison

| Question | Required check |
| --- | --- |
| Current input | Obtain the own impulse checkpoint from the real retained Bootstrap/input/control/physics/contact/graphics chain; source starts from its own saved impulse output. |
| Complete order | Iterate live slots in original order; finish each slot's recovery, scheduler, creation and lifetime before advancing. Visit newly activated later slots in this same pass. |
| Current records | Independently calculate complete World/400Actor/global records and masks. Read current Object/header/Frame values; do not use source after-state, unknown backing or private stack as input. |
| Transient creation | Derive opoint gates, count, first free slot, first matching catalog ID, constructor writes, Object identity, position/velocity and ownership from current inputs. Preserve untouched allocation bytes and masks. |
| Transient deletion | Verify catalog sound before invalid-next return; preserve the scheduler's early-exit previousFrame behavior, then reset frame and deactivate. Keep the inactive allocation and its Object binding through later stages/calls. |
| Observable effects | Derive the ordered reconstruction/sound events and sound accumulator writes; compare relevant helper calls/arguments/returns against saved source. No new lifecycle RNG may be silently discarded. |
| Whole caller | Compare match, live/dead resource owners, allocation aliases, local record, retained text/DC, chronological journal and actual returned command extent. Later stage semantics remain separate until independently checked. |
| Failure and rollback | Retain existing parent rollback checks and add failures after transient construction and after its later sound/lifecycle checkpoint, before commit. Verify complete caller rollback and successful retry from the same owned ticket. |

Full source middle Object/Frame-heap snapshots are absent. Check the available
stage components plus full acquired/returned records and report that boundary.
The saved helper list is in return order; nested sound returns precede their
enclosing scheduler return. It is not a complete instruction trace.
The constructor's one declared memset remains a recorded host adapter, not an
executed CRT body. Its400 bytes at Actor+f0 are already known zero on this input;
that does not make other retained bytes, especially Actor+31c, known zero.

Use a comparison-only projection validated on saved source before applying it
to own checkpoints. The accepted neutral-only lifecycle projection cannot just
be relabelled active: it excludes opoints and most active sounds/transitions.
Do not call the production lifecycle implementation as its own expected model.
Finite guards must expose actual unsupported branches, including transformations,
damage/HP recovery, alternate opoints, weapon/command/death/fire effects, retained
scratch dereferences and aliases outside the declared current path.
Those guards are comparison limits, not original game rules or dropped scope.

Comparator controls must distinguish omitted same-pass scheduling/deletion,
wrong Object or action selection, inherited coordinate substitution, integerZ
used instead of binary64Z, reset inactive backing, lost masks, reordered sounds
and retained owner/journal changes. Use meaningful current-input variants;
never alter the immutable source cases or feed control variants into the game.

## Delivery and limits

Root owns implementation and publication; `war_contract_review` owns independent
read-only review reports. Candidate paths: new ActiveLifecycle projection/source/
tests, optional callback forwarding in the existing ActiveGraphics test chain,
this study/evidence and the current handoff. Core changes require separately
identified evidence of a production mismatch, not adjustment to match a test.
Fixtures, resources, expected values, masks, historical failures and frozen
producers remain immutable. Preserve unrelated pending work.

Reuse existing frozen-candidate manifests, Native runner, comparators and archive
verification from the graphics parent. Freeze actual working inputs, not HEAD
alone. Initial selected release methods: two new ActiveLifecycle tests and the
two ActiveGraphics regressions; preserve exact pins of unaffected earlier
methods. Changed helper paths are exercised transitively and must not be counted
as additional directly executed tests. Add constructor/rollback regressions if
the implementation changes those components; publish the exact final selection
before running it.

Storage: task-owned
`build/research/application-owned-active-lifecycle-native-20260914/` on mounted
X5, APFS UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`.
Keep40GiB X5 and6GiB internal reserves, task cap40GiB, at most3600s per Native
job and three candidate rounds before diagnosing the contract. Prerequisite
readers are limited to the saved96calls and300s each, with no Native build.
Revalidate storage and process identities before dependent actions; never restart
a terminal or quiet live job. Native tests, package byte verification, archives,
independent review and Git publication remain distinct acceptance gates.

No original, emulator, old capture or historical auditor is executed in this
continuation. Existing [safety incidents](../evidence/codex-safety-incidents-2026-09-12.json)
remain open. Stop affected operations on refusal; do not retry or reroute them.
Neither this plan nor a new goal clears those incidents.

After acceptance, continue with commands/HUD/notices/recording/layout/output.
All seven remaining active stages, the complete tick/match/game, application
integration, actual window/input/audio, Windows/device/clean-Mac and broader
uncovered gameplay remain open until their own criteria pass.
The EXE envelope is not recalculated by this prerequisite.
