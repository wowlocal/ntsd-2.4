# Owned gameplay comparison and provider contracts

Status: implementation and comparison; full acceptance open. Parent `b3949f9`
preserves the [integration checkpoint](APPLICATION_LOADED_GAMEPLAY.md), including
134 passing methods. The complete objective remains the standalone native NTSD
game; the first Naruto/Sasuke District match is an intermediate milestone.

The current commit checkpoint covers provider/owner tests and the independently
reviewed saved-data projection contract. The full comparator in steps1–2 below
remains subsequent work; completing steps3–4 alone does not accept gameplay.

## Scope and evidence

Finish the open acceptance work for the actual owned gameplay input after41e339,
all19 body stages, the enclosing Bootstrap return and16 following neutral calls
in both retained backing variants. Compare full records/masks, owners, RNG,
graphics/audio and rollback. Source before/after values are assertions, never
Core inputs or a replacement for own startup/selection/preparation.

Reference: pinned original NTSD distribution, EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Use the saved first-body and continuous Unicorn2.1.4 observations and accepted
library/control/physics/contacts/hits/drawing/lifecycle/recording contracts.
Read saved instructions, stores, snapshots and hashes only. No original code,
emulator, completed producer/auditor or refused operation is executed.
Existing safety incidents and source-fault boundaries remain open.

Own X/Z, inactive selections, game/CRT RNG, installed hooks, resource identities
and Actor draw order differ from the pristine source. Each projection must first
reproduce the saved source result, then compute the own result from own input
using the recovered rule. Enumerate read/branch requirements and idempotent
stores; a before/after delta alone cannot establish a complete write footprint.
If saved coverage cannot establish a required value, keep the dependency open.
Do not use Native output to calculate expected state or silently omit a field.

## Finite work and checks

1. Read/normalize the saved19-stage and16-call records with verified hashes and
   identities. Independently review control/physics/hits/lifecycle store/read
   footprints, including writes whose old and new bytes are equal. Compare the
   complete own state at the declared stages and returns.
2. Project camera and ordered draw blocks from own positions and source rules.
   Own Z order must be derived before drawing. Recover clipping/bitmap reads
   and text/terminal effects; translating already clipped old events is invalid.
3. Directly compare all829 catalog and15 newly created arena ordinal/wrapper/
   surface bindings against retained construction inputs/replies. Check selected
   District/count boundaries and missing/released/stale owners with full rollback.
4. Controlled command recovery enables the music-resume provider. Controlled
   timer101 enables result recording using the own replay allocation and declared
   call-local formatter backing. These are provider/ownership tests, not natural
   played outcomes or new source API-failure matches. Check missing providers,
   live-allocation overlap, success/IO responses, chronological journal and late
   rollback after changed replay/library state. Unknown backing remains unknown.

Initial provider validation: three bounded tests (maps/owner rejection, music
recovery, result-file provider chain). Keep the existing two17-call preflights.
Freeze exact candidate files before execution and record the precise filter/job.
Final regression selection retains the previous134 methods through exact source,
test and fixture equality with their terminal passing candidate. No Core or old
test change is planned for this checkpoint, so those successes are not rerun.
Run the four writer/recording methods for the newly enabled provider paths:
`OriginalReplayWriterTests`, `OriginalResultRecordingTests` and both
`OriginalGameplayResultRecordingTests` variants. Expand only for changed
dependencies or a diagnosed mismatch; successful tests are not rerun for counts.

Root owns code/tests/doc edits. `war_contract_review` independently reviews
source footprints and later comparator/provider changes; its opinion does not
replace a deterministic comparison. All previous candidates, plans, logs,
failures, fixtures/resources and AGENTS_HISTORY remain immutable.

Allowed paths: application gameplay session/necessary joins, gameplay tests and
new comparison helpers, this plan/study/evidence, CURRENT_WORK and RESEARCH_MAP.
No change to a game rule or expected byte solely to obtain acceptance.

## Limits and delivery

Use `build/research/application-loaded-gameplay-comparison-native-20260913`, a
new task-owned directory on X5 UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`.
Keep40GiB free there and6GiB internally; maximum40GiB new task storage,
3600seconds per Native run, initially three candidate rounds before diagnosis.
This separate work area preserves the preceding34.65GiB checkpoint unchanged.
Shipping fixtures stay regular and self-contained locally.

Verify live handles before acting; silence/timeouts are not terminal. Save each
terminal failure and diagnose it before any separately identified correction.
Check source/package/archive/Git bytes and publish measured results separately
from semantic acceptance. Do not declare the card accepted until all required
comparisons and reviewer findings close. Full game/match/backend/Windows/device/
clean-Mac and unresolved source/safety dependencies remain open.
