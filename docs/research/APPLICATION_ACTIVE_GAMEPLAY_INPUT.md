# Owned active input and gameplay preflight

Finite input comparison and retained application preflight, under
[the active gameplay plan](APPLICATION_ACTIVE_GAMEPLAY_PLAN.md).
The full active body differential remains open. The accepted neutral comparison
is preserved as the parent; this result does not accept a complete active match.

Two saved48-call controls are checked before Native execution. Independent
formulas compare acquired keyboard bytes, complete prologue and six input
World/400 Actor/global endpoints with defined masks, replay allocation updates,
and control/writePacket/team event order. Inactive configured seats2/3 retain
their actual joystick profiles. The input endpoint comparison does not claim
ordered per-instruction stores or actual Windows/device input.

Each own application runs startup/catalog/selection/Start and17 fully compared
neutral returns, then obtains the48-call movement/action schedule through
Bootstrap/WndProc using live bindings. The keyboard comparison includes the two
WndProc sequence recognizers. The own input comparator uses current state and
resource identities; source after-state never drives Core.

The body preflight visits all19 stages and publishes through the same Bootstrap
caller, recording own positions, frames, activity, RNG and observations. These
recordings describe Native behavior; they are not expected output or proof of
original equivalence. Own geometry and RNG differ from the pristine source
controls, so contact outcomes, attack frames, temporary Actor lifetimes and
rendering must be independently derived before body acceptance.

At calls1/17/48 of each variant, tests reject input at local/replay/commit,
reject the late body commit, retry the same pending input without reacquisition,
and reject each outer time/sleep/commit boundary. Full committed acquired-input
state and current public resource owners are compared, including replay,
graphics, library, catalog, music and caller loop state. Immutable historical
owners and private allocation cursors retain the review's explicit static
no-mutation limitation.

The four failed candidates and their exact frozen sources/logs remain preserved:
first a nonexhaustive observer switch, then an incorrect comparator assumption
about inactive seat status, then the incorrect inactive-text-editor guard, and
finally the omitted enclosing message-loop counter update. The corrected full
acquisition comparison includes the wrapped counter and signed reset, exact MSG
record and unchanged timer baseline.
Dispatcher startup activates the retained editor, so the corrected independent
formula includes ordinary text bytes and counters under an explicit index<299
guard; the historical index299 alias contract is not broadened. Independent
review additionally caught joystick mapping and strengthened prologue, owner
rollback and input journal checks. Author and reviewer misses remain recorded.
Core, original fixtures, expected bytes and masks were unchanged.

Next: implement source-first projections for the full retained active body,
including installed-library paths, contacts, RNG, transient objects, numeric
stores, front/nonfront events, audio/graphics and chronological caller journals.
The instruction inventory alone does not establish branch outcomes. Paused
branches, a damaging hit/round result, full match/game, device raster/audio,
Windows/clean-Mac and existing safety incidents remain open. No original code,
emulator or historical capture/auditor was executed for this preflight.

The fifth runner's description contains an archived active58 typo introduced by
a filename substitution. Its command, frozen plan and tests still require48;
the running producer was preserved. scope-metadata-correction5.json records the
distinction, and publication requires the actual two48-call/96-row summaries.

The first log reader stopped before publication because merged XCTest output
interrupted two buffered JSON records. The corrected reader removes only two
exact complete metadata spans from a diagnostic view; log-separation5.json
retains their raw byte offsets/text. The original log, initial reader and its
failure remain unchanged. All96 rows are required; none is omitted or invented.

Four selected release methods passed in114.261seconds, build380.85seconds:
two new full-chain input/body preflights and two unchanged neutral regressions.
Both own schedules returned48 times with20 WndProc transitions and912 body stages
per variant. Eighteen input rejections, six late body rejections, six same-ticket
input retries and eighteen outer-tail rejections passed. The143 prior methods
are inherited through unchanged source pins, not rerun here.

All991 Native files were verified against the frozen candidate;988 prior files
are unchanged. All383 fixtures/102 resources and485 packaged files retain exact
bytes and modes. Native archive and evidence archive bodies/modes were read back;
all four failed candidates can be reconstructed from pinned final files and
archived deltas. NTSDNative linked; no window or device test was performed.
[Evidence](../evidence/application-active-gameplay-input.json) pins reports,
logs, review and archives. All five Native jobs are terminal. Do not restart them.
