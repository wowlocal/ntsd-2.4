# First damage from current application input: Native-only diagnostic

Status: implementation; no diagnostic execution or new gameplay acceptance.
Base `3de9cc9e007e5d16d63d9038584c35f4e19fc897`; clean actual Native1028 inputs.
[Readiness](FIRST_MATCH_READINESS.md) establishes why active48 cannot show an
ordinary hit: its whole source trajectory retains positive startup protection.
Task → reach the first actual unprotected combat dependency → compare a damaging
whole call, then play a complete Naruto/Sasuke District match in the native app.
The full [goal](../CONTINUE_GOAL.md) remains open.

## Reference, ownership and boundary

Only the pinned NTSD EXE/DLL/resources are behavioral references: EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
This card executes Native Swift only. The existing own first+16 neutral producer
and its retained original comparison build the parent; new calls use no source
case, expected after-state, original execution or historical producer/auditor.
All source/fixture/mask/asset bytes and old finite comparators stay unchanged.
[Safety incidents](../evidence/codex-safety-incidents-2026-09-12.json) remain open.

Use actual Bootstrap→makeLoadedCycle→LoadedCycle.advance→GameplaySession.advance
→finishLoadedMenu. Do not lengthen the old48-case source loop or relax its
rejected-contact/hitcount0 gates. The diagnostic owns current loaded catalog,
World/Actor/Frame heap, installed text/hit/transform state, replay and resource
identities throughout. Reading current state to choose keys is a test controller,
not a new game AI or a rule about original controls.

Root owns new `OriginalApplicationFirstDamage*` XCTest/support files, this plan,
results/evidence and navigation. No Core or existing test edit is planned.
war_contract_review independently reviews the contract, diagnostic and publication;
only the reviewer writes numbered review reports. Native-only diagnostic artifacts
are not accepted original fixtures and are never shipped in the runtime.

## Exactly two bounded input scenarios

Primary: own neutral parent(false), only slot0 requests an ordinary attack.
Control: own neutral parent(true), only slot1 requests an ordinary attack.
Both slots are human-controlled; the other requests no held buttons.
Maximum160 additional complete calls per scenario, plus declared failed attempts.
Each scenario gets its own independently produced parent; no reset during it.

1. Wait with neutral input until both current Actor+8 words are exactly0.
   Expiry in lifecycle is intermediate; the next contacts pass reads that zero.
2. Move only the selected attacker toward the other fighter: align integer Z to
   within2, then close absolute integer X distance to28. If closer than20, move
   away until20; face the opponent via a directional input if necessary.
3. Release for two gameplay calls, hold ordinary attack for two, then release for
   twelve. Repeat alignment/release/attack only until the first stopping condition.
   These thresholds and pulse lengths are declared scenario choices, not recovered
   physics constants. Frames, wait, HP/MP and RNG are never assigned by the test.
4. Resolve up/down/left/right/attack from actual current loaded key bindings.
   Reuse the existing independent WndProc acquisition checks for their declared
   ordinary-key domain, including complete keyboard/MSG/editor/recognizer state.
   Do not claim OS message timing or live window behavior.

## Declared providers and observations

Retain the parent's modeled local/offline platform profile. Control requests must
match the current two asyncSelect and two ioctl requests in order; numeric replies
are−1,1,−1,0 with last bytes78/56/34/12. These are synthetic API inputs, not actual
network operations or Windows observations. No AI/object child is supplied; a
reached child is a provider dependency. Menu/epilogue continuation is a routing
boundary, not a successful gameplay return. Ordinary presentation keeps declared
zero results and retained DC12345678. Clock uses existing baseline+1000 controlled
responses; no actual host timing is measured. Other allocation/file/music/fill
requests reject explicitly instead of inventing outcomes. Stop on that dependency.

Record declared buttons, current bindings, acquisition before/after complete
full-state bytes/masks, keyboard, last MSG, counter and baseline (also partial after
on error), and declared ordered key transitions. This is not a new observation of
each intermediate MSG; the reused acquisition comparator checks each actual key
iteration. Require prologue and all six input checkpoints in exact order. For each actual body checkpoint record complete World/400Actor/global
bytes+mask digests, current two fighter fields, input/round phases, RNG and stage
order. At links (before contacts), contacts and hits retain both complete Actor records/masks,
current70/previous74/contact-filter78/collision7c Frame records, Object headers, full referenced ITR/BDY allocation bytes
and masks, team/category/geometry/protection fields, and primitive effects. Raw
observations describe reached Native storage; no external expected bytes are
created. Unknown diagnostic-only fields are recorded as unavailable, not filled
or read as known zero. A diagnostic reader failure is distinct from Core failure.

Keep the complete ordered body event journal and terminal operations/graphics
counts/digests. Coherence checks use current bindings/owners; they are not an
independent behavioral prediction. A returned outer call must retain all current
state, graphics and resources under existing finish checks. Never deliver IO.

On the first new call, inject one body beforeCommit failure, check the full prior
application and diagnostic environment, retry the same body session, then inject
outer beforeCommit failure and retry the same Bootstrap ticket. On first observed
hit-stage HP decrease, repeat these late checks before publishing the actual
whole return. Existing acquired key state survives every body/tail failure.
Any unplanned failure ends that scenario; do not automatically retry it.

## Outcomes and acceptance

Stop separately for: first committed hit-stage HP decrease; Core unsupported
storage/rule; missing declared provider; unsupported menu/epilogue route;
old-comparator domain rejection; diagnostic/harness error; or160-call limit.
The first five are observations of a diagnostic boundary, not comparison PASS.
XCTest passing means the diagnostic and its declared invariants completed; only
its explicit outcome establishes whether damage was observed. A partial hit before
later failure is not a committed hit. A non-hit HP cost/regeneration change does
not satisfy the damage condition. Require a selected-attacker kind0 contact targeting
the actual defender, defender HP decrease and matching damage-taken +34c delta.
Record contact target, signed ITR index, collision Frame and kind; an unconfirmed
HP decrease is a diagnostic boundary, not damage acceptance. Retain actual error type/message/stage/input.

Ready diagnostic requires exact frozen files/methods, independent pre-run review,
terminal logs with both distinct outcomes, rollback assertions, byte-identical
383 fixtures/102 resources,485 packaged files and separately read-back archives.
No new original/game/Windows equivalence is accepted. Use the reached branches to
choose the next independent comparison and identify missing same-input source
observations; do not hide them behind the existing controlled library corpus.

Work: `build/research/first-damage-native-20260914/` on verified X5 UUID
`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`;40GiB external/6GiB internal reserve,
40GiB task limit,3600seconds per Native job, initial3candidate rounds. Retain all
failed candidates/jobs and never restart a live or terminal producer for silence.
Select exactly the two new diagnostic methods; their existing own neutral parents
supply required regression checks. No Core changes require a wider regression run.
Full match, native app/input/audio/raster, Windows/device/clean-Mac and remaining
content/modes/AI/network/replays remain open. EXE envelope is not recalculated.
