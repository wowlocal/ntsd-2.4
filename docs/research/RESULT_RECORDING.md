# Whole result-recording caller — comparison in progress

`OriginalResultRecording` implements421cdc..422218 with the original alternate
continuation422944. This work is **not yet accepted**. The95-call controlled
source suite is running; both fresh initialized chains have completed and match
the native raw comparison. Existing175 fixtures remain the accepted corpus;
no result-recording fixture has been published.

The first controlled original call and native caller agree on
complete globals and serialized recording, compressed/adjusted output, allocation
liveness and replay pointers, stream states and three descriptor requests totaling
10157 bytes. It executes177 caller instruction starts and the actual43dd60 writer.
Its raw native comparison passed0.157s after a33.95s test build. Initial test
compilation required only an explicit array type/spacing in its expected codec
write-mask expression. The core release build passed113.77s; NTSDNative linked.
These are initial checks, not acceptance of the full caller domain or app evidence.

A subsequent immutable snapshot of18 completed source calls passes the native
comparison: five whole writers,15 descriptor requests/50495 bytes. Both retained
own GAMEPLAY_NOTICES comparisons also pass unchanged (481245 records and
806849858 bytes/masks each;536/544 helpers;61 state/1604 FPU checkpoints).
The three release tests passed41.967s after a95.45s build. New own result tests
compile, but are not run until their fresh source captures finish.

The next22-case completed snapshot also passes natively in1.282s without a
rebuild: nine whole writers,27 descriptor requests/91297 bytes. The full95-case
producer and both own source chains remain live; handle/log details are saved
in build/research/result-recording-work.json and must be revalidated before
continuing. No fixture or acceptance report has been published for this work.

A33-case raw snapshot passes in2.964s:20 whole writers,60 descriptor
requests/203556 bytes. This includes primary/fallback/both seat choices in
modes0/1/4, retained stage values0/1/2/ffffffff and the native unknown-stage
rollback trial. The controlled suite continues with winner, enemy-scan, playback,
packing and failure cases; its full acceptance remains pending.

The latest53 completed cases pass in5.606s without a rebuild:40 whole writers,
two actual playback restores and120 descriptor requests/406441 bytes. Full
playback/recording aliasing and native rollback at the late writer destruction
after playback restoration now pass. Source95-call completion is still pending.

Both fresh initialized source processes are now terminal. Each reproduces the
entire pinned GAMEPLAY_NOTICES parent before executing the seven actual result
caller instructions and stopping before422944. The complete state changes only
450bbc from0 to1. Each rootSP64 audit has one access since the last round
initialization: its real41d7d7 write0. The native join consumes the retained
`OriginalMatchRoundResult`, with no expected stack word imported.

Both raw native own comparisons pass in40.632s without a rebuild. Each compares
513221 records/842185012 bytes and masks,536/544 helpers,63 state checkpoints
and1605 FPU checkpoints. CW023f/FPSW4000/tagffff remain unchanged; all parent
FPU checkpoints survive. The recording is still live, and neither own first
tick calls the writer. This is not a returned tick or Windows/app execution.
Full raw JSON, SHA, parent identity, exact state differences, all transport blobs
and stack/FPU audits are checked by `accept_gameplay_result_recording.py --verify-only`;
details are in build/research/gameplay-result-recording-raw-verification.json.
The own corpora remain unpublished until the complete controlled caller passes.

The new producer is [oracle_result_recording.py](../../tools/oracle_result_recording.py).
Each controlled case executes the original result caller,43df00 when selected,
and the complete43dd60/codec/C++/CRT chain. It inherits the declared allocation,
thread and file descriptor boundaries from [REPLAY_WRITER](REPLAY_WRITER.md).
Every completed case is atomically checkpointed with SHA-addressed, zlib-framed
transport blobs. The initial one-case source process was confirmed terminal
before continuing fresh cases from that verified checkpoint. No source process
is restarted because its log is quiet.

Inputs declare full World/400Actor/fourObject headers, complete recording and
playback allocations and saved playback settings. Only known pointers are
normalized for native records. Cases include timer and gate boundaries,
primary/fallback preference, source IDs and statistics, signed winner/HP and
retained stage results, all five positions in the unrolled400-slot enemy scan,
mode4 signed decimal packing with32-bit wrap, playback/recording allocation
aliasing, stream errors and codec/temporary allocation failures. No numeric
compression or IO failure becomes an invented early return.

`OriginalResultRecording` stages globals and ownership together, including
playback restoration before recording function-key counters. Its writer is the
existing actual native `OriginalReplayWriter`; no injected compressed buffer
or transport codec replaces game output. Native tests include unknown retained
stage-result rejection and rollback at destruction after recording serialization,
playback restoration and writer cleanup. Both the unknown-stage and late destruction trials now pass.

`MenuCycleReference.Portion.roundResults` now preserves the outputs of its own
`OriginalLoadedMatchEntry.run`. The new own comparison passes that retained result
to the public caller. It never initializes it from expected source stack bytes.
[oracle_gameplay_result_recording.py](../../tools/oracle_gameplay_result_recording.py)
reproduces each entire pinned GAMEPLAY_NOTICES parent and audits rootSP64 from
the last actual41d7d7 round initialization before continuing the caller. Both
own captures and their native raw comparisons have now completed above;
joint fixture publication still awaits the95-case controlled caller.

A further static stack correction:4222ce's `[esp+64]` store follows six pushes,
so it writes rootSP+4c, not rootSP+64. The result layout at422673 still reads the
retained round word. The old plan's claim of a new rootSP64 lifetime at4222ce was
incorrect. This is a static correction, not dynamic result-layout evidence.

Next verification: finish and compare all controlled cases; retain both verified
fresh own parents, full states/masks/FPU and stack lifetime; only then publish immutable
fixtures with [accept_result_recording.py](../../tools/accept_result_recording.py).
Result layout422218..422944, indicators, labels, presentation, queued sound and
the real422ab8/ret4 remain subsequent work. Full-game/app/Windows/device evidence
remains open.

[RESULT_LAYOUT_PLAN](RESULT_LAYOUT_PLAN.md) now records a reproducible static
ESP audit and the following table/indicator/bitmap-font dependencies. It is
planning evidence, not acceptance or execution coverage for those consumers.
