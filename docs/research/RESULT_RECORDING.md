# Whole result-recording caller

`OriginalResultRecording` implements whole421cdc..422218 with the original
alternate continuation422944. All95 controlled original returns match the native
comparison, including82 actual writer calls and12 playback restorations. Both
fresh initialized own chains also match through422944/SP1000e9bc, retaining their
own round result without importing source stack bytes. The first own tick still
has not returned. See [GAMEPLAY_RESULT_RECORDING](GAMEPLAY_RESULT_RECORDING.md)
for that distinct initialized-chain evidence.

## Recovered behavior

Signed450bdc<100 increments450bbc with32-bit wrap. Only unsigned(timer−101)<=248
enters the result-table continuation. Exactly101 additionally permits serialization
when450be4/450b80 are nonzero and450b84 is zero. The other continuation is422944;
a native caller must not draw a result table unconditionally after this routine.

Eight seats prefer primary slots0..<8 over fallback10..<18. An absent participant
writes only−1 to its presence field, retaining the other old recording bytes.
Selected seats record Object ID, primary/fallback kind, team and five statistics.
A signed nonnegative winner selects outcome by team and HP. Mode1 instead reads
rootSP64 and distinguishes exactly1 from other values before checking HP. The
native API requires this retained word only when the source actually reads it.

The caller stores participant count, mode4 summary counters and elapsed ticks,
then sets450b6c=0/450b70=2. Nonzero4588ac and450b88 invoke whole43df00 playback
restoration, including recorded sound flags, **before** saving the function-key
counts. Recording and playback may share the same allocation. The400-slot scan
then counts active living type0/team5 Actors. Mode1 writes the resulting completion
bit; mode4 uses signed decimal quotients/remainders and wrapping accumulation with
the original1adbb constant. The native implementation retains this exact order.

Finally the existing [OriginalReplayWriter](REPLAY_WRITER.md) consumes the entire
recording through the real native1.1.4 compressor and stream implementation.
Numeric compression/IO errors do not introduce an early return. The source still
closes the stream, frees temporary and recording buffers, clears4588a8, destroys
the stream, then clears450b80. A final live timer==101 test clears450b84. Shared
playback pointers remain unchanged even when they now identify released recording
backing. Native ownership does not invent a still-live duplicate allocation.

## Source environment and comparison

[oracle_result_recording.py](../../tools/oracle_result_recording.py) verifies the
pinned EXE/VC80 DLLs and executes the complete caller,43df00 and actual writer,
codec, C++ and CRT children. Memory and register tracing are needed to recover
record serialization, retained stack lifetime, aliasing and cleanup order for
the native macOS port. Full controlled World/400Actor/fourObject records, saved
settings and recording/playback buffers are declared inputs. Allocation, thread,
open and descriptor responses inherit the boundaries in
[REPLAY_WRITER](REPLAY_WRITER.md), [REPLAY_COMPRESSION](REPLAY_COMPRESSION.md) and
[REPLAY_STREAM](REPLAY_STREAM.md). No source instruction or codec output is stubbed.

All95 returns compare complete globals, records/masks, playback/saved backing,
replay pointers and allocation liveness; actual compressed/adjusted buffers and
write masks; every descriptor request, stream state, codec allocation lifecycle,
lazy processor selection and caller continuation. Known pointers alone are
normalized. A thrown native observer at destruction after playback restoration,
serialization and staged cleanup verifies rollback of globals and ownership.
A separate missing-stage-result trial fails only at its actual required read.
External effects must remain buffered until the enclosing tick commits.

The95 calls include13 gates without a writer,76 writer compression successes,
five codec allocation-error returns(−4), and one no-temporary-buffer return(−2).
IO failures include failed open, short write, failed close and unbuffered fallback.
These are normal whole-caller returns, not95 compression successes. The writer's
separate21 matches plus five explicit source-fault rejections retain their own
contract; this caller corpus does not turn those faults into successful matches.
Full-random capacity failure remains covered by the retained whole-writer study;
this caller uses the accepted initialized recording plus its own serialized data.

Source coverage is296/296 caller starts,116 writer starts,40 restoration starts
and3212 actual DLL PCs. There are1277 codec helper returns,410 stream returns,
608 completed REP copies and1936406 actual longest-match invocations. The82
writer calls issue18322 descriptor writes/33219692 requested bytes. The controlled
CPU signature remains306c4; this is not a host or Windows CPU measurement.
Instruction-start coverage does not establish every branch outcome. Private C++
ABI/host heap pressure, arbitrary corrupted backing, Windows files/exceptions,
actual macOS IO and a full returned tick remain outside this comparison.

## Acceptance and retained artifacts

Each source case was atomically checkpointed with hash-verified transport blobs.
The initial source process was terminal before continuation from its checkpoint;
no live process was restarted for silence. All95 completed before acceptance.
Earlier completed snapshots of1/18/22/25/33/53/71/86 calls also matched; the53-case
snapshot first included late destruction rollback after playback restoration.
Initial test compilation needed only an explicit array type/spacing in its mask
expectation. No accepted source result or game rule was changed to make it pass.

[accept_result_recording.py](../../tools/accept_result_recording.py) independently
checks all source writes, full buffers, ABI returns, stack reads and296 caller
starts. [accept_result_tail_dependencies.py](../../tools/accept_result_tail_dependencies.py)
uses this same verifier and all other completed-stage verifiers, runs the complete
native acceptance set together, then publishes in dependency order. This avoids
repeated builds without omitting tests. Ten release tests passed58.577s after a
149.00s build: this caller10.977s, both own continuations40.441s, bitmap font,
mode label, playback information, queued sound and retained writer/815-codec/
66-stream comparisons. NTSDNative linked; no app window or device was exercised.

The controlled raw file has13337524 bytes, SHA256
`277965e7f0c6c2b33da420fed9478acb7b40cbc83a1a241e0ce7e0059dfe5bac`;
its immutable fixture has3209836 bytes and745 inner blobs. The initial175 accepted
fixture hashes remain unchanged. Seven new fixtures, including the own chains
and completed output helpers, bring the current pin count to182.
[verify_result_tail_artifacts.py](../../tools/verify_result_tail_artifacts.py)
checks every full raw/packed byte sequence, complete JSON, SHA, length, inner blob,
intermediate pin set and ten vendor hashes. Its first final vendor check used the
wrong subdirectory; the corrected path verifies unchanged files. Transport
compression packages research data and never substitutes for game replay output.
Evidence is [result-recording.json](../evidence/result-recording.json).

Whole result layout422218..422944 and its indicator caller remain next in
[RESULT_LAYOUT_PLAN](RESULT_LAYOUT_PLAN.md). That static audit corrects4222ce to
rootSP4c;422673 still consumes the retained round result. The bitmap-font,
mode-label, playback-info and queued-sound helpers now have their own comparisons,
but their whole initialized composition and actual422ab8/ret4 are still open.

Final packaged verification: the ten-test release set passes58.142s after a0.23s
resource-copy build, without raw-corpus overrides. This study takes11.120s. All
source and SwiftPM jobs are terminal; full artifact/pin/vendor checks pass. This
does not establish an app-window, Windows or actual device result.
