# Retained Host delivery context — bounded candidate

2026-09-26; base HEADd23848c. Previous goal turn was progress: all76 catalog
package comparisons and complete artifact gates closed. Continue WORKFLOW,
CURRENT_WORK, TASK_TEMPLATE and the existing Host/app and transaction preflights.
This implements the next concrete app dependency: a delayed consumer must retain
the actual resources of its own committed batch after the Host advances. It is
required before connecting ordered output to a prepared backend and ultimately
the complete Naruto/Sasuke District match; the full game remains the goal.

## Inputs, provenance and boundary

Use the frozen2241-file candidate from application-catalog-package-validation-20260926,
manifest41725682877ec5430eca64c165ac46f857e25af8ea534c7ed388208293cfde54.
Root1034 and its dirty test changes are protected, not the implementation input.
Preserve the preceding1040/2241 candidates, all76 old method bodies, all fixtures,
package resources/manifest, expected bytes/masks, source55 and all closed jobs.
The immutable instruction archive and safety register stay unchanged.

Original Windows EXE SHA2563f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d remain
the only behavioral reference. Existing source observations and whole-caller
comparators remain authoritative. New code implements Native publication ownership,
not another engine rule or original allocation ABI. Existing controlled API/reply/
backing inputs are not actual Windows/device observations. No original, emulator,
source capture/auditor, affected refused operation, AppKit or live device executes.
Independent review is unavailable/open; author checks do not replace it.

Relevant preserved archive constraints:1942–1948 (PE-derived owned state, unknown
private bytes, whole rollback),3337–3349 (presentation/resource order and actual
shared callers) and WAV_LOADING's owned regions/masks/failure lifetimes. Read the
Host transaction/preflight/validation, Host/app preflight, Bootstrap, graphics
owners and WAV loading studies. Movement/combat/projectile historical practice
limits remain historical; this task changes no gameplay mathematics.

## Contract and immediate consumer

All three Host.Batch variants gain one retained DeliveryContext with the actual
post-commit Application value and a private independent Platform copy. A public
inspection method returns another independent platform copy, never the retained
mutable object. Existing stagedCopy value-state/identity contract applies; a new
object sharing mutable queues/allocators is still invalid. Shared immutable input
bytes may remain shared. No expected state or latest-host lookup constructs it.

Prepare the context before publishing sequence/batch/application/platform. A
throwing or same-object copy publishes nothing. Startup's failedAttempt callback
must see a publication-preparation failure once; ordinary Bootstrap failures keep
their existing callback path. On loaded-tail failure retain the exact pending and
prepared child/platform for the existing tail retry. Sequence and earlier queued
batches do not change. A successful final tail clears its pending/prepared owners
only as part of the same locked, nonthrowing publication.

Application value ownership retains startup.input.sounds (all five actual WAV
results), startup output/calendar/music/panel, current Session state/masks/graphics,
and loadedOwners' current match/music/resources plus the retained catalog/common/
startup child chain. Commands already own their generation-qualified/color data;
MusicMemory retains its wide allocations. Keep those existing lifetimes and raw
values. New retention is not proof of a real device lease or successful release.

Batch.contents remains the authoritative original ordered stream, with graphics
as its overlapping metadata. Do not flatten/reorder/duplicate effects, change
payloads, infer rectangles or skip failed requests. This task adds an owner
context, not an IO dispatcher or a synthetic success backend. Its immediate
consumer is the future prepared-backend delivery adapter for these same batches.
Holding or draining a batch is not physical exactly-once delivery or device ack.

## Finite cases and independent expectations

Add six methods, using current whole-parent helpers and source comparators:

1. Delayed startup and two key-message iterations: leave all three queued, then
   drain FIFO. Startup and each iteration retain their own full state, ordered
   work, five PCM results/masks/music and independent platform values. Compare
   startup to the existing direct Bootstrap/source adapter; key bytes117/100/117,
   message order and host queue increments are declared input expectations.
2. Startup context preparation: throwing and same-object copy after the real
   startup computation. Exactly one failure callback, no published owners/batch,
   unchanged caller/host platform; retry from the same committed owner succeeds.
3. Iteration context preparation: both copy failures after a real key iteration;
   earlier startup/key batches, state, queue/counter and sequence remain. Retry
   the same immutable packet, without consuming an expected-result cursor.
4. Delayed loaded batches on both existing backings: actual fresh catalog/pool/menu
   and cached cycles through the known bitmap release. Retain the older batches
   while current state advances. Compare them to the existing independently
   calculated checkFinished contract and source-backed child results, including
   full ordered child operations plus explicit outer time tail. Old resources
   remain available with their old live flags/generations after the later release.
5. Loaded publication preparation: both copy failures at the final tail, on both
   backings. Preserve actual pending/prepared tickets, owners, earlier queued
   batches and platform, then complete that same prepared child once. Existing
   reentry and whole-call checks remain required.
6. Lifetime and isolation: mutate returned Application/platform inspection copies,
   advance/drop the Host and retain a handed-off context. Actual retained bytes,
   masks/PCM and ordered contents survive unchanged; no Host reference is retained
   merely to make the value usable. Include startup and a real loaded boundary.

Do not compare a new context with itself or generate an expected operation list
by invoking a new delivery producer. Existing direct Core/source-backed helpers
and explicit temporal inputs provide the independent expectations. Keep every
old76 method body unchanged and select all82 (six new plus the existing76) for
subsequent fresh compilation/comparison, preserving both backings and complete
17/48/14 schedules. Parsing alone is not compilation or test acceptance.

## Operations, limits and ownership

New task/alias: application-host-delivery-context-20260926 under
/Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/.
Verify writable APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548, require65GiB free
external and9GiB internal before preparation, retain40/6GiB plus17GiB source
commitment. Candidate-only physical allowance4GiB, stop at3.5GiB decrease; logical
bound16GiB, metadata16MiB/archive, root additions<=1MiB, elapsed<=7200s. APFS clone
all2241 files with bytes/modes/nsmtime/membership and distinct-inode verification.
No copying a HEAD-only tree, deletion, reserve reduction or movement of old inputs.
T7 stays authorized for separate future bounded archives; this task uses X5.

Reuse existing preparation/pin/clone/archive patterns. Mutable Native scope is
OriginalApplicationHostSession.swift, the existing WinMainStartup test Adapter's
copy-failure controls, and new OriginalApplicationHostDeliveryContextTests.swift
(three files; expected2242 Native members). A necessary scope expansion gets an
explicit pre-edit amendment. Existing public startup/iteration/loaded operation
producers and all game rules stay unchanged. Root Native/package scripts remain
unmodified. Root tools/prepare_application_host_delivery_context.py, this study,
evidence and own navigation additions may change; freeze all actual inputs.

At most three coherent implementation revisions before a contract diagnosis.
Parse the three changed/new Swift sources, <=120s each, retaining exact command,
output, status and input hashes. This candidate plan does not authorize builds or
XCTest execution: freeze the exact82-method build/test/RSS/time/space configuration
in a separate validation increment after the candidate is reviewable. Prepare a
complete round-tripping text patch and regular candidate/metadata archives, verify
every member's bytes/mode/nsmtime/composition, publish separate implementation,
syntax, comparison and review gates, and commit the coherent checked increment.

Source59727 stays terminal at its prior34-Object publication bound; do not restart.
Full137/transport, independent review/root promotion, prepared observations and
reservations/cancellation/post-commit failures, AppKit input/render/audio, Windows,
clean-Mac, complete match and full game stay open. EXE envelope is not recalculated.
