# Retained Host delivery context — fresh Native validation

2026-09-26; base HEAD3716487. The preceding increment implemented and archived
the candidate, parsed all three changed Swift files and preserved the old76
methods. Continue its first open gate: compile and compare that exact candidate.
This tests resource retention across delayed startup/iteration/loaded delivery
before a prepared backend connects the recovered engine to the native app and
the complete Naruto/Sasuke District match. Full game remains the objective.

Read WORKFLOW/CURRENT_WORK, the retained-context plan/study, Host/app preflight,
Host transaction/loading and completed catalog-package validation. The existing
source evidence, owners, numeric semantics and whole-caller comparators remain
authoritative. Author implementation/checking is not independent review.

## Inputs and permitted operations

Base: application-host-delivery-context-20260926/candidate1,2242 Native files,
manifest SHA256ab4c0eb67b1c6b25abd4690124e116465dad709db807acd1bd2636c41350f430.
Selection is its exact82 methods: six new context methods then all76 prior
catalog/host/caller methods in their existing order. Root1034, prior2241, all
fixtures/resources/expected/masks, source55, original assets, closed jobs, archives,
immutable instructions and safety incidents stay unchanged. Actual candidate
inputs include prior uncommitted-baseline changes, not a HEAD-only checkout.

Only behavioral reference: original Windows EXE
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
Compile/run Native and read saved comparison records as data. No original,
emulator, capture/auditor, affected refused operation, live device or Windows
execution. Existing supplied API replies/backings do not establish actual host
behavior. No new expected producer or source capture is needed for this contract.

## Fixed gates

1. Verify mounts/reserves, terminal prior jobs and actual input manifests. Clone
   all2242 files into a fresh X5 task, checking bytes/modes/nsmtimes, complete
   membership and distinct regular inodes. Preserve the closed base task.
2. Reuse the prior catalog validation monitor, queue and package checker. Adapt
   only task identity,82-phase/method count,272 test source count and method-limit
   mapping. Keep the full identity/cwd/descendant/guard/reap loop and saved-result
   predicates unchanged. Preserve exact adaptation diffs and synthetic controls.
3. One fresh native-SwiftPM release build, --build-tests, --jobs2,
   --disable-index-store and -enable-testing; fresh task scratch/cache/config/tmp.
   Verify192 Core,61 Reference and272 test source inventories plus generated
   accessors and fresh compiled outputs. Verify all385 fixtures+1301 Core files
   by complete membership and content. Linking/exit0 alone does not pass.
4. Run all82 methods as82 sequential named XCTest processes on that same binary.
   Six context methods retain the entire cases declared in the candidate plan;
   all76 old bodies/limits and both backings/17/48/14 schedules remain intact.
   Stop the queue on its first nonpass; do not split a method or skip a mismatch.
5. Result acceptance retains the verified one-anchor exact-method reader,
   exactly one passed result, three one-test/zero-failure summaries, Selected-tests
   pass, exit0, no guard/signal and no residual processes. Preserve full logs and
   distinguish compiler/harness/guard/Native failures and missing evidence.
6. Verify old/candidate/root/source pins. Preserve full candidate and release
   products in a regular APFS clone archive and metadata in PAX, checking every
   name/body/mode/nsmtime and directory membership. Publish independent build,
   package, comparison, archive and review gates; commit coherent checked progress.

The prior relocated Core-only catalog reader check remains a separate preserved
result. This Host ownership change does not alter its reader or resource package;
do not repeat that completed standalone client merely for a new pass count.
Current82 includes the unchanged packaged reader regressions. There is no new
window, real input/audio, clean-Mac or original-application acceptance claim.

## Bounds and process ownership

Task/alias application-host-delivery-context-validation-20260926 under
/Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/.
Verify writable APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548; require90GiB
external and9GiB internal before preparation. Keep40GiB external/6GiB internal
plus17GiB source commitment. Physical allowance28GiB, stop at26GiB observed
decrease; logical150GiB; metadata<=16MiB/archive; root additions<=4MiB. No evidence
deletion/reserve reduction. T7 stays authorized separately; pinned paths unchanged.

Build<=3600s/RSS12GiB; total queue<=14400s. New methods1–3<=600s/RSS8GiB;
new4–6<=1800s/RSS16GiB. Each of the retained76 receives exactly its saved
method-limits1 values from the prior catalog-package validation, keyed by method,
including original ordinal provenance. Never apply the shifted82 ordinal to an
old limit. Full-method RSS does not reset at a subcase. Poll1s/RSS2s/logical15s.
Preparation<=1800s; finalization<=3600s. No automatic rebuild/rerun on failure or
observation timeout; diagnose from the same authoritative process/job/results.

Mutable: new task/alias, task-owned generated tool copies, root preparation
adapter tools/prepare_application_host_delivery_context_validation.py, this study,
evidence and own navigation insertions. Candidate/source/expected and prior tools
are immutable. A necessary code correction gets a separately pinned candidate
and pre-edit plan, preserving this outcome; up to three rounds before contract
diagnosis. Do not promote root Native or invoke shipping packaging as a side effect.

Before process actions verify PID/start/full command/cwd/job; silence is not a
stop. Source59727 remains terminal at its recorded34-Object publication boundary,
not a full137 return; never restart. No new original experiment is declared.
Independent review/root promotion/prepared observations and backend delivery,
catalog transport/full137 source, AppKit input/render/audio, Windows/clean-Mac,
complete match and full game stay open. EXE envelope is not recalculated.
