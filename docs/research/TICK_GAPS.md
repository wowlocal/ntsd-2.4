# R01.1 — что пропускает текущая сборка такта

Основание: [TICK_PIPELINE.md](TICK_PIPELINE.md), EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`.
Нативный игровой код — срез `a9f2cfe`; старт этого исследования — `c9a5263`.

Существующий [Projectiles.tick2](../../tools/oracle_projectiles.py) — ручная
композиция исходных функций. [OriginalMelee.advance](../../native/Sources/NTSDCore/OriginalMelee.swift)
воспроизводит её объявленный объём. Успех их сравнения остаётся полезным,
но не распространяется на окружающий исходный цикл.

Новая общая основа теперь отдельно проверяет [первую загрузку](INITIAL_LOADING.md)
и [локальный ввод419a60](LOCAL_INPUT.md) на полном каталоге/пуле, включая фазы,
keyboard/joystick bytes и упаковку. Она ещё не подключена к оконному Practice.
Новый [received-input участок](RECEIVED_INPUT.md) также сравнивает4198f0/4197a0
и их caller: status−1 remote, все восемь playback, сырые previous/phase0 current,
полный байт записи и порядок повторных Actor-ссылок.
[Control/network участок](INPUT_CONTROL.md) теперь закрывает41c5e5..41d46f
в обоих первых caller после загрузки: hotkeys, packets/checksums, shutdown,
input reset и возврат сохранённых настроек совпали с native в5 986 случаях.
[Replay tick](REPLAY_TICK.md) теперь закрывает источник43dc50, запись43db40,
checksum/counter до41d714 в2 268 случаях:614 связанных цепочек продолжают
prefix/local/control/received/tail, включая24 последовательных шага на проход.
[Round control](MATCH_ROUND.md) теперь также сравнивает счётчики, живые команды,
подтверждение и восстановление участников до реальных выходов перед gameplay/
menu/эпилогом:4 148 случаев,90 конструкторов,275 последовательных участков
на проход. Паузная ветвь заканчивается перед первым draw helper в41d73b.
[Включённая музыка](MUSIC_PLAYBACK.md) теперь продолжает основной4229cc→429730
через настоящий402020 до4297ae:748 случаев совпали с native, включая общие
release/volume и сохранение выделенных wide-path buffers. Флаг music остаётся1;
контрольный menu entry предоставлен после паузной остановки. Menu bitmap/body,
реальный codec/file и музыкальный вывод ещё не выполнены этим корпусом.
[Ресурсы меню](MENU_RESOURCES.md) продолжают4297ae..429e5a:374 случая,
2098 настоящих43ee50, все11 global stores, три массива участников и частичная
SPARK-разметка совпали с native после свежих родителей. Сохранены предыдущие
wrappers и нетронутые bytes/masks;12 null-SPARK проб заканчиваются перед
разыменованием. Сам dispatch429e5a/431d10 и пиксели ещё не перенесены.
[Общий bitmap draw](BITMAP_DRAWING.md) теперь исполняет всё тело43f010/43ef70
до COM Blt:3971 изолированный случай совпал с native, включая clipping,
mirror,198 двойных рисований и20 invalid-access boundaries. Это заменяет
границу целого draw helper для нового корпуса; старые caller fixtures не
переписаны. Native ещё не соединён с устройством/Practice или полным menu loop.
AI bodies, OS acquisition, реальный сетевой транспорт, ранний playback prefix,
startup/file IO, паузный вывод и дальнейшие тела стадий остаются открытыми; прежняя таблица ниже
описывает ограничения существующего оконного прототипа.

| Стадия оригинала | Python / Swift сейчас | Следствие и очередь |
| --- | --- | --- |
| Верхние диспетчеры, вход/выход `41bc90` | Не выполнялись старым корпусом. Новый `oracle_tick_trace.py` выполняет исходный путь с подготовленными данными; нативного общего соответствия нет | R01.2 после R02/R03; текущая оконная практика не равна матчу |
| Фаза `450b90`, очередь паузы, внешние команды | `4198f0` вызывается с фазой 0; control — с `(0,0)`. Native получает уже нормализованный FighterInput и собственную паузу | R02/R04/R15/R17. Трасса подтверждает чередование 1/0 и пару обновлений по команде `416df0`; нельзя считать все буферы независимыми от фазы |
| Входы/повторы/сеть | Синтетические маски двух игроков; нет полных буферов обмена, checksum и записи | R04/R16. Локальная ветка тоже вызывает socket thunks, однако сетевые сообщения в новой трассе отключены |
| Счётчики и состояние матча до контроля | Нет счётчиков modulo 12/3 и полного исхода раунда | R02/R10/R15; без них восстановление ресурсов и некоторые ветви состояния не имеют правильной фазы |
| Окружающий `413080` проход | Функция выполняется отдельно; специальные state 400/401/500/501 вокруг неё не выполняются | R05/R11/R12. Адрес `403270` — установленный вход выбора действий, но полная семантика ещё не закрыта |
| Окружающий `40e490` проход | Физика callee проверена, встроенные специальные состояния/создания после неё отсутствуют | R05/R06/R11. Исполнить callee и заменить всё окружение пустым нельзя |
| Сбор контактов | Вызывается без исходного переключателя `44d05c=2` | R07; отдельно восстановить обновление collisionFrame и одноразовые подавления |
| Два прохода разрешения | Старый Python вызывает `42e100` для всех активных слотов сразу. Swift также обрабатывает объявленный каталог в собственном проходе | R07/R08: оригинал сначала обходит type 0, затем, после попытки создания предмета, signed type > 0. Отрицательный type не входит ни в один проход. Текущая расстановка 0/1/50 может давать тот же порядок пар, но не доказывает общее правило |
| Случайные предметы между проходами | Полностью отсутствуют | **R06/R02:** вызов RNG `(146,200)` нужен даже без появившегося предмета. Это установленный пропуск RNG, не гипотеза о втором ударе по персонажу |
| `418c30`, `4187b0`, проверка взаимных ссылок | Нет общих cpoint/связанных объектов | R09; неизвестные поля и условия занести в R02, затем восстановить весь порядок |
| Второе ограничение глубины | Старый корпус вызывает только первое ограничение и дальнейшую камеру; второй вызов `417f80` пропущен | R05/R07/R09; при отсутствии изменений связанных объектов его результат может совпадать, что не делает вызов лишним |
| Камера и рисунок объектов | Python исполняет `41b5d0` до входа в фон и сам снимает позы; Swift вручную вычисляет камеру и draw list | R13/R17. Новый trace исполняет функции рисунка, но с частичными заголовками bitmap и внешним выводом без устройства; изображения этим не проверены |
| Mode 1/4 после рисунка | `437860`, `43a860` отсутствуют | R15/R10/R12; нельзя объявить их чисто визуальными по месту вызова |
| Импульсы после рисунка | `4196f0` присутствует; Swift применяет соответствующие накопленные скорости | Сохранить порядок в общем цикле и прежнюю регрессию R08 |
| Жизненный цикл каждого слота до планировщика | Специальные состояния, трансформации/создания, HP/MP-восстановление до `41fb06` пропущены | R05/R06/R10/R11; новая трасса даёт HP 401 / MP 107 вместо исходных 400 / 100 через 12 вызовов, без изменения DAT |
| Планировщик и postSchedule | `40d960` + фрагмент `41fb0b..41fc61` присутствуют | Это часть одного обхода, не основание вынести планирование всех объектов в отдельный проход |
| opoint и удаление | Присутствуют выбранные фрагменты и каталог 203/224/440 | R03/R06; сохранение динамического обхода слотов обязательно. Встроенные альтернативные создания `420e93..4213a9` не закрыты |
| Запрошенные предметы после обхода | Ветка `450bb8=1` отсутствует | R06/R10/R15; новый сценарий её не возбуждает |
| Ресурсы и cleanup в конце | Python вручную сбрасывает четыре контактных поля и byte `eb`; остальная стадия пропущена | R10/R07: таймеры `e0/e4`, лечение и запросы ресурсов должны выполняться до следующего такта |
| HUD, результаты, счётчик боя, вывод/звук | Оригинального полного HUD/результатов нет; свои сцены/аудиовыход. События выбранных звуков сравнивались отдельно | R10/R13/R14/R15/R16/R17. Широкая трасса не включает устройство и не доказывает исходное сведение или задержку |

[Ранний startup ресурсов](FRONT_MENU_RESOURCES.md) теперь отдельно выполняет
World constructor/4246b0 до423480 и совпадает с Swift в290 случаях. Он создаёт
настоящие4511a0/451178 и другие wrappers с исходными прямоугольниками/шрифтами.
Соединение с ранним экраном и прежним loading/menu требует чтения настроек
423480 и последующего427092/42709b. Старые fixtures не получают подставленных
ресурсов; новый участок не доказывает full tick, пиксели или Windows.

[Чтение настроек](SETTINGS_LOADING.md) уже продолжает оба свежих ранних
родителя через настоящий423480/VC80 до42709b и совпадает с Swift в188 случаях.
Сброс427092 выполнен из caller EBX; EOF, неудачные чтения и строковые перекрытия
не исправляются. Настоящий ранний экран и его связь с поздним loading/menu ещё
открыты; fopen/fclose/text translation и Windows остаются границами.

[Первый рисунок раннего экрана](FRONT_SCREEN_PRELUDE.md) уже продолжает свежие
World/resources/settings через настоящие gate/fill/background/draw helpers
до427127/4275cb.356 случаев и полные retained records совпали с native.
Далее4236d0 и тело экрана; старые tick/menu fixtures не получают подставленных
ранних ресурсов. Пиксели, worker execution, full tick и Windows остаются открытыми.

[Данные информационной панели](MENU_CONTENT.md) теперь имеют общий native loader
43c780 и704 сравненных случая с actual VC80 readers. Отсутствие исходных ad0/ad1
сохранено; успешный формат исследован на явных контрольных файлах. Следом нужны
43cc60/43c690/43c710 и соединение всего4236d0 с настоящим427127 caller.
Отдельный helper не продолжает tick/menu fixtures и не доказывает весь экран.

[Bitmap панели](MENU_PANEL_BITMAP.md) теперь сравнивает весь43cc60 и настоящие
constructor/destructor в118 случаях: fixed atlas, free-before-malloc, null/error
branches и все поколения повторно выданных адресов. Следом43c690/43c710 и весь
4236d0; путь/DIB/allocator/COM этого отдельного корпуса ещё являются входами.

[Запись настроек панели](MENU_INFO_WRITING.md) теперь сравнивает43c690/43c710
с actual VC80 output в534 случаях. Сохранены defaults после failed open и
partial-write buffer tails; FILE flags102/descriptor IO/text translation имеют
явные границы.

[Обновление панели](MENU_PANEL_UPDATE.md) теперь соединяет весь4236d0 с четырьмя
реальными children через настоящий427127 после свежего early-screen parent.
266 случаев совпали с native, включая fallback/date asymmetry, двойную запись
defaults/cache, все поколения bitmap и две остановки до scanf без Leave.
Первый status0 получен из собственного parent; последующие caller/worker/file
inputs предоставлены явно. Далее тело42712c/альтернативы4275cb и соединение
с прежним menu/loading. Границы CRT/device/worker и проверка Windows остаются.

[Тело раннего экрана](FRONT_SCREEN_BODY.md) продолжает эту цепочку до4275cb
в1142 совпавших с native случаях. Исходные text/sound/bitmap children исполняются
полностью до объявленных OS/COM границ; сохранены все local bytes/masks и ранние
ресурсы. Это ещё не связь раннего экрана с предыдущим полным main-menu/tail:
следом selector−3/−1, settings writer423230, затем соединение обеих цепочек.
Две null-text остановки до401295 не считаются безопасным успешным рисунком.

[Settings writer](SETTINGS_WRITING.md) теперь отдельно совпадает с native
в452 случаях, с actual fprintf/fclose и всеми FILE/buffer/globals snapshots.
Это готовая зависимость selector−3; сам427688/427704 caller ещё не исполняется
этим корпусом. Source сохраняет временные изменения общих имён и partial IO.
Shared printf исправлен по обнаруженному failed-sign/continued-digits пути;
прежние writer/panel corpora остаются проверками своего исходного объёма.

[Альтернативы раннего экрана](FRONT_SCREEN_ALTERNATE.md) теперь продолжают
4275cb..42790f в1066 совпавших случаях с6842 actual helper returns. Writer
исполняется из обоих427688/427704 callers; bitmap/clip/sound/fill/gate тоже
исполняются на общем CPU/стеке. Первый selector0 получен от свежего body,
последующие caller/OS/FILE inputs заданы явно. Signed animation/remainder,
unsigned timer delta и порядок действий сохранены. До42873e/427915/427ca7
дошли1042 случая; ещё24 остановились до null FILE/bitmap/fill разыменований. Полная связь с прежним
main-menu/tail ещё не выполнена; app UI/pixels/audio/worker/Windows открыты.

[Возврат раннего меню](FRONT_MENU_COMPLETION.md) теперь соединяет первый
startup/body/dispatch с главными пунктами и tail через настоящий ret4.806 возвратов,
6996 helpers и234000 actual rand совпали; графические children используют ранние
загруженные wrappers, а не условные addresses прежних probes. Последний выбор
матча продолжается полным World1→2 с destructor/free настоящего фона. Это не
исполнение World2→41bc90 и не все повторные ранние экраны: поздние427915/42873e
entries предоставлены явно после реального пролога. Эти границы и app/W открыты.

[Повторный ранний dispatcher](FRONT_MENU_LOOP.md) теперь исполняет каждый
4246b0 от входа с сохранёнными World/globals/bitmap/CRT после первого ret4.
Совпали238 calls/1094 phases,228 ret4,3945 helpers,30000 actual rand и
376899720 bytes/masks. Настройки, waiting и возвращение к main выбираются
своим состоянием, без подачи внутренних EAX/PC/register controls. Последний
World2 доходит до настоящего41bc90, но его тело ещё не соединено с этим
ранним состоянием. Далее эта композиция loading/catalog/input; worker/status2
children в повторном потоке, остальные selectors, приложение и W открыты.

[Меню→первая загрузка](MENU_LOADING.md) теперь соединяет тот же ранний World2
через424741/41bc90 до41c581. Два полных прохода совпали с native без замены
World/globals/CPU/stack:36+800 WAVs,274 Objects,816 Actor/20 UI constructors,
363365426 bytes/masks после меню. MENU_WAIT рисуется через реальный ранний
wrapper, прежние26 bitmap и CRT сохраняются. Теперь продолжить объединённое
состояние после41c581 через прежние input/round/menu механизмы. Animated4242e0,
полный41bc90 return, app integration и Windows по-прежнему открыты.

## Как устранять пропуски

1. **R02.1:** словарь состояния полного пути и всех пропущенных полей, точная
   инициализация, исходные размеры, перечень изменяемых globals. Новый trace
   сохраняет места записей; это начальный материал, не полный список полей.
2. **R03.1:** загрузка объектов целиком и каталога зависимостей. Сейчас и широкий
   стенд, и прежний используют частичное синтетическое состояние заголовков.
3. **R01.2:** полноценный снимок исходного такта с этими данными и воспроизводимыми
   входами; сохранять первое расхождение с нативной реализацией. Совпадения старых
   корпусов сохраняются отдельными проверками ограниченных механизмов.
4. **R04/R06/R05/R07/R08/R09/R10:** общий перенос по правилам EXE. Пропущенный RNG
   добавляется вместе с настоящим механизмом и его состоянием, а не как лишний
   произвольный вызов ради совпадения одной трассы. HP/MP также переносятся по
   исходным условиям и порядку, а не по замеченной периодичности одного случая.

Не удалять старые эталоны и не обновлять их ожидаемые значения для маскировки
расхождений. Новые сквозные случаи должны сообщать свои отличающиеся границы.
В этом исследовании Swift и исходные игровые ресурсы не менялись.
