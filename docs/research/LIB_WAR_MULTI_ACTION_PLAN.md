# War actions from multiple human seats

The immutable574-case setup corpus shows that one seat's simultaneous keys
are reduced to its first action before438bbb. The accepted431b70 menu input
helper scans eight seats independently, so different human seats can still set
Attack, Jump and Defense together. The original plan's combined-action coverage
therefore remains open after the [44-cell supplement](LIB_WAR_CELL_COVERAGE_PLAN.md).

Freeze40 calls using the original control parent with eight ready Random human
seats. Reference EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba and
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d remain
pinned. Run controlled Unicorn2.1.4/CW023f and the same normal declared resource,
allocator/COM/GDI/music responses through41bc90/4229cc/438b40/ret1c/ret4.
No Windows/device, prior selection reachability or played battle is claimed.

The first two fresh calls must reproduce source2 cases412/413 exactly. The
constructor-parent `firstCase` annotation is relocated from412 to0 because this
is a separate corpus; constructor bytes and complete call observations are not
changed. Two Down and two Left press/release edges reach row1/cursor0/side0.
For each nonempty mask1..7, seat0 supplies Attack, seat1 Jump and seat2 Defense
as selected by that mask. Release each selected key in the following call.
Repeat the seven masks on row2 after a separate Down edge. All three seats are
declared human; no CPU key backing or input-global flag is forced.

Full reads/stores/stack/helper/checkpoint/ownership observations are needed to
verify the flags actually delivered at438bbb and the original arithmetic order:
Attack increment, Defense addition/clamp, Jump decrement, final wrap. Require
all14 row/mask combinations, actual stores only to the selected active/reserve
cell, all40 outer/39 War returns,401 original constructors and two War bitmaps.
Audit all records/masks/events and compare the retained chain natively. Preserve
the574- and192-case corpora and their separate comparisons and rollback trials.

Use new manifest, producer/input pins, atomic parts and raw output on X5 with
the6GiB reserve. Do not restart either completed capture or continue an original
fault. This supplement closes a concrete input-composition gap; full setup
acceptance still requires combined source/Native/transport checks. War battle
preparation/gameplay, own catalog/library/outer loop/app, full game and
window/input/audio/Windows/clean-Mac verification remain open.
