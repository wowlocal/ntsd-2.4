# Owned gameplay provider and owner checks

Status: Native provider/owner checks pass; full gameplay comparison remains open.
This is a bounded addition to the
[gameplay integration checkpoint](APPLICATION_LOADED_GAMEPLAY.md), under the
[comparison plan](APPLICATION_LOADED_GAMEPLAY_COMPARISON_PLAN.md). The full
source-first gameplay comparison and the complete game remain open.

## Scope

The new tests start through actual owned selection, installed-library launch,
Bootstrap return and the next gameplay input. They then exercise resource
providers inside the existing19-stage body and return through the same ticket.
Controlled field changes enable recovery commands and result recording; they
are not naturally played outcomes or new original API-failure observations.

Reference contracts remain the pinned NTSD EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, saved
bitmap/command/writer/recording observations and existing Native comparisons.
Only native macOS release code and saved fixtures are executed/read. No original
code, emulator, historical producer/auditor or refused operation is executed.
No host file is opened by gameplay callbacks: the environment buffers requests,
bytes, results and observations until the enclosing commit.

## Checks

- All829 catalog and15 new arena ordinal/wrapper/surface mappings are checked.
  Catalog bindings come from the retained construction snapshot; new bindings
  are checked against the launch allocation/CreateSurface request/reply journal.
  The selected District and101-background boundaries remain explicit.
- Seven class-wide owner trials remove catalog wrappers/surfaces, substitute
  stale surfaces, release catalog records, or remove/deactivate/stale arena
  owners. They establish rejection of those classes, not one negative trial
  for every ordinal. Controlled one-layer release descriptors set release state
  through the owner API; they never replace actual game backgrounds.
- Recovery sets450bc0=1 and two active HP values to100/200. The actual missing
  music provider rejects; supplied callbacks receive the current control twice.
  Their−17 responses are journaled, and HP recovers through the existing rule.
- Result recording sets450bdc=101 and processor selector44dd50=2. The current
  replay allocation remains the input. Missing allocation/processor/open/write/
  close providers reject at their actual calls; an overlapping allocation rejects.
  Call-local formatter bytes start undefined and must be produced before reading.
- Six returned-IO trials cover open success/failure, close−1 and write0/−1/1.
  They check binary path/mode/share, stream states, ordered cleanup, cleared replay
  pointer, Bootstrap commit and its next input. A failed4096-byte flush retains
  its triggering byte for close under the saved CRT contract. Payload/framing
  comparisons here are ownership/transport invariants; independent codec-byte
  comparison remains in the retained writer tests.
- Provider callbacks and recording observations independently collect a complete
  ordered journal, compared with the session's staged operations. Injected music,
  write/close, stage and final-commit errors discard tentative state/effects.
  After final-commit failures, the same gameplay session successfully retries
  and its enclosing Bootstrap accepts the original ticket.

## Validation and preservation

Candidate1 did not compile: the new test omitted `try` on presentation input
and attempted to mutate the read-only released-bitmap view. Candidate2 fixes
those test errors and adds journal, same-session retry and returned-IO checks.
The first reviewer missed both authoring errors; its correction is preserved
separately. Candidate2 passes music and recording, but its owner test expects
the wrong error for released catalog bitmaps. The existing HUD rejects them
before the application's surface callback. Candidate3 requires that exact HUD
error for this trial and preserves the other six expectations. This second
review omission is retained too. No Core code, old test, fixture or expected
byte changed.

Artifacts are in
`build/research/application-loaded-gameplay-comparison-native-20260913/` on the
declared X5 volume. Previous gameplay candidates/jobs/archives remain immutable.
The old134 passing methods are retained by complete file equality, rather than
counted as new executions. Three provider methods and four writer/recording
methods are the new execution gate; package/archive/Git verification is separate.

Candidate3 passes all three provider methods in46.702s/build337.52s. Its four
writer/recording regressions pass in53.406s/build0.36s, including21 whole writer
returns and five explicit source-fault rejections, plus95 recording callers.
These overlapping helper observations are not additional whole application
matches. All four Native jobs in this work area are terminal. The two failures
and each frozen candidate remain preserved; no successful capture is restarted.
Measured outcomes and integrity gates are recorded in the
[evidence manifest](../evidence/application-loaded-gameplay-providers.json).

## Remaining comparison

The separate read-only projection review inventories source control/physics/
hits/lifecycle writes, including idempotent stores. It requires computing own
coordinates with the exact binary64 intermediate stores: translating a rounded
source after-value can differ by one ULP. Continuous intermediate snapshots lack
the complete Frame heap, so they cannot establish unobserved per-stage records.

This contract review is an input to the future comparator, not its acceptance.
Full19-stage records/masks/stores, own-position/RNG projection, ordered graphics
and audio remain open, as do active combat, other modes, playback, backend,
Windows/device/clean-Mac, the full match/game and previous safety dependencies.
