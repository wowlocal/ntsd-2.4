# Installed-library transformation and extended write

Recover the complete41f550..41fb0b slot prefix with actual41f5fc->1000109d
installation, retaining inactive4214c6 and all original scheduling/creation
children. Pinned artifacts and the development-only Unicorn boundary are in
[LIB_RUNTIME](LIB_RUNTIME.md). Earlier pristine fixtures remain unchanged.
The adjacent actor-control study owns41408b and separate files; do not alter it.

The DLL routes8000..<9000 through the original transform code. Its additional
4000..<4999 branch searches for the first ID=state-4000. A successful lookup
changes Actor+368. It does not reset frame0 or write318=140 as the8000 path does.
It then writes the live EAX value at Actor+7b4: EAX is the Actor pointer on a
match, the scanned catalog count on a positive-count miss, or0 for count<=0.
These are static findings to verify, not source execution or native claims.

The recovered Actor allocation is420hex bytes. A7b4 write is outside it.
Do not enlarge the Actor, silently redirect it to a familiar field or assume
Windows heap adjacency. In the existing controlled500hex-stride source pool,
it lies in the next record at+2b4, or beyond the400-record pool for its last
allocation. Observe that actual write and full subsequent prefix. Declare any
extra mapped backing; preserve guards, all storage masks and unknown bytes.
Inspect the real loaded catalog for4000-range frame states separately from
claims of natural gameplay reachability.

Native needs explicit storage ownership and an address-token boundary for this
write. Unknown target backing must reject with rollback. A supplied binding may
point into another independently owned Actor or external record; do not derive
that binding from an expected after-state or make500hex a native heap rule.
A pointer-valued write must derive from a declared Actor identity/address token,
not an expected destination word. Later consumers see the actual own write.
Test lookup success/miss/count0, duplicates, type/activity gates, boundary states,
retained frames, same-call9995 and9996/scheduler composition, aliases and late
observer rollback. Retain whole897-case pristine prefix results where unaffected.

The full initialized library-enabled loop/application, actual Windows heap
behavior and the complete native-game goal stay open. Source faults/rejections
remain separate from successful complete native comparisons. Keep terminal
failures and immutable completed captures; never restart a live job for silence.
