# War setup: finite compatibility study

Recover the original mode4/menu200..202/210 troop setup, both team multipliers,
44 editable troop cells, five presets at three strengths, apply/cancel/Jump,
live Random fighters, arena/difficulty/music settings and output/sound order.
Execute the actual438b40 caller,4389a0 pulse and438ad0 thin frames through
439ecd ret1c and outer422ab8 ret4. Branches after439ecd belong to this function;
its final branch is43a84c, before the separate43a860 War gameplay controller.
Start stops BEFORE43a21f, after actual troop finalization and confirmation sound.
The entire Actor/arena/replay preparation beginning there remains a dependency.

Reference: NTSD EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
original DAT/head/small BMP and embedded DIB resources. Controlled Unicorn2.1.4,
CW023f, C locale, declared allocator/COM/GDI/music responses. Actual41bc90
prologue then declared4229cc menu continuation runs the installed original lib.
No Windows/device/pixel, played battle or complete initialized app claim.

Disassembly plus full reads/stores/masks, stack/register/helper/FPU and resource
ownership traces are needed to distinguish same-call transitions, pending
arguments, live aliases, text and preset copy order. Preserve actual unknown
bitmap counts and uninitialized backing; never import source after-state into
Native. Root War SP is entrySP minus a50. Its seven ordinary arguments come
from actual42a0eb..42a10a. The standard cookie and SEH sequence executes normally.

Two fresh actual World/400Actor constructor parents precede declared ready
mode4/menu200 entries. Primary has Naruto/Sasuke plus six Random CPUs; control
has eight ready Random humans and reversed allocation/pattern/seed inputs.
Team1 occupies seats0..3, team2 seats4..7; actual startup owns11 menu bitmaps,
and War itself constructs BATTLEMODE/BATTLETROOPS. PE44d350..44d777 supplies
the immutable unit IDs, count, presets and initial scalars. Catalog operand
views retain the accepted independently rebuilt137 Object/101 Background domain.
Earlier mode4 character/CPU selection reachability is separate and still open.

The frozen manifest2 contains572 calls:411 primary and161 control, with571
expected whole returns and one declared Start boundary. Manifest1 was a
544-call planning draft, never executed; fourteen extra reserve +5 actions
and their releases cover the30-unit wrap. Freeze before source execution. The primary
retained button program visits all44 editable cells, count increment/+5/clamp/
wrap/decrement and simultaneous Attack/Jump/Defense, both multiplier wraps,
all four frame phases, horizontal/vertical wraps, popup apply/cancel/Jump,
reroll, every arena selector state, difficulty/music settings and Start boundary.
The control varies ready Human/CPU labels, all eight live Random choices and
ends at a real quit return. Thirty declared popup confirmations cover every
preset/strength/side combination with real resource/array/backup history.
Those bridges set only menu selection/strength scalars and button bytes, retain
all produced storage, and are explicitly distinguished from continuous input
reachability. No intervening battle is claimed.

Acceptance requires every frozen call, whole owned records/masks, ordered
events, all helper ABIs, read/store reconstruction and source instruction-byte
audit. Reconstruct each Random list from prior live choices; verify full REP
copy/zero spans and preserve source masks separately if hook coverage differs.
Native must independently initialize literal PE constants and own both new
resources. Stage the whole call, including resource, RNG and output changes;
late second-resource/frame/text/preset/reroll failures must roll back. Re-run
retained Team/Tournament/VS dispatcher checks in an isolated accepted package.

All295 accepted fixtures,715 package inputs,14 pending Native,3 pending source
and6 foreign files stay unchanged. Large files and TMPDIR use task-owned X5.
Write each completed source case atomically before proceeding and stream final
assembly to avoid multiple complete raw JSON trees. Source5 is already terminal
at its unchanged6GiB storage boundary: preserve it and never restart it.
Preserve actual failures/refusals with operation/time/session; never retry a
safety refusal, modify control/protection structures or continue a source fault.
Full War preparation/gameplay, catalog/library/outer loop/app join, full match,
all content/network, window/input/audio/Windows/clean-Mac verification stay open.
