# Active lifecycle transient provenance

This saved-data prerequisite connects the source input of call17 to the transient
in slot50 and its retained allocation after deletion. It prepares the
[complete active lifecycle comparison](APPLICATION_ACTIVE_LIFECYCLE_PLAN.md);
it does not accept another Native stage or a whole tick.

The [earlier active capture study](ACTIVE_GAMEPLAY_CAPTURE.md) already reported
this transient. The new reader checks the concrete inputs, masks, parent and
stage joins needed to build an independent comparator on current own state.
Both immutable pristine EXE/VC80 fixtures are used. Original instructions, the
emulator and historical producers/auditors are not executed.

## Input and observed lifetime

All eight components at the complete neutral parent's last return match the
active initial state. The active capture additionally observes Object records
and weapon strings. For all96calls, acquisition preserves the entire pool,
masks, recording allocations and state metadata; its globals changes are exactly
the recorded keyboard-byte changes. Object/Frame-heap/string records retain
their acquired-to-return identities. Acquisition plans match the saved schedule.
These joins support the declared controlled input provenance, not real input
device or DLL-enabled Windows execution.

In each source call17, the current Naruto Actor selects Object17/ID2, Frame60,
wait0 and a different previous frame. Frame60 has wait0/next61. Its scheduler
resets and increments wait, then selects Frame61 with wait0. The current
Frame61 supplies this opoint:

| Field | Value |
| --- | ---: |
| kind / requested Object ID | 1 /203 |
| x / y | 43 /81 |
| action / dvx / dvy | 200 /0 /0 |
| packed facing | 0 (one creation, inherited facing) |
| parent centerX / centerY | 39 /79 |

The first matching ID203 is Object64; the first free slot in50..<400 is50.
The source parent has X374, Y0, binary64Z504.1 and facing1. The opoint therefore
gives X`374+39−43=370`, Y`0−79+81=2`, binary64Z`504.1+1=505.1` and integerZ505.
The three recorded conversion returns independently agree with370/2/505.
Using integer parentZ would lose the retained binary64 fraction.

The child is type3, Frame200/state3005, wait0/next1000, sound366. Recorded helper
return order is scheduler0, constructor, three conversions, sound109/scheduler1,
then sound366/scheduler50. The latter scheduler returns1000. The established
invalid-next branch exits before copying previousFrame; the surrounding lifetime
branch resets frame0 and deactivates the slot before advancing the loop.
This explains why all lifecycle endpoints and full returns still have only
slots0/1 active. Intermediate child states are inferred from loaded inputs,
helper observations and the pinned instruction contract; there is no invented
full snapshot between creation and removal.

The allocation survives. The saved-data readers check that Actor50's bytes/masks remain at
their inherited values through the preceding stage endpoints, change in call17
lifecycle, then stay unchanged through every later stage and complete return
to call48. The reconstruction uses existing backing. Its known-zero400-byte
range at+f0 is preserved by the recorded memset adapter; this is not proof of
an executed CRT memset implementation. Actor+31c initially contains undefined
backing, and its later known0 comes from selected header+90, not the constructor.
Object binding and inactive storage must not be cleared on removal.
The saved stack observations contain no lifecycle access overlapping the six
retained words at caller SP+44/+50/+5c/+60/+6c/+70 across all96calls. Their
values need not be invented for this finite path; a future dereference still
requires its own provenance.

Across the two variants, the reader verifies194 scheduler returns, two
constructors, six conversions and14 catalog-sound helpers/events. Each variant
has six calls with lifecycle events:17,18,21,29,33,39. The call17 queued output
contains sound entries109 then366. These are declared playback requests, not
measured audio. All available non-state/non-early lifecycle components are
retained. Full middle Object/Frame-heap snapshots and complete instruction-level
stores remain outside this evidence.

The independent reviewer checked all1872 Actor50 snapshot positions per variant
(whole before/acquired/after and both endpoints of all18 stages), retaining
651 inherited and1221 post-deletion records/masks. A separate input-derived
sound calculation reproduces the complete globals record at all96 lifecycle
endpoints and their early mirrors. The call17 sound queue survives the later
commands and clears at output, while the resource buffers remain owned.
These are saved-source checks, not a newly accepted Native comparison.

## Consequence for the Native comparison

The accepted graphics parent's saved own preflight also observes creation in
call17, but reports childX168 and the other fighter's sound atX308. Its sound11
appears in call32, while the pristine source observes sound10 in call33.
These are diagnostic observations with `bodyDifferentialAccepted=false`.
The next projection must derive both sides from their own current Frames,
positions and sound accumulator, preserving time and resource ownership across
calls. Copying source coordinates or assigning sound events by call number
would validate the wrong behavior.

The [reader](../../tools/inventory_active_lifecycle_provenance.py) checks its
compressed inputs, raw-envelope lengths/hashes, all component manifests and
each consumed blob. It keeps exact masks and labels undefined displayed fields.
Its first run failed because it assumed every eventless section contained a
`lifecycle` key; the capture represents those sections as an empty dictionary.
The reader, traceback and observed shapes are preserved. The corrected reader
accepts that declared empty representation and rejects other event groups.
Further successful revisions check all earlier Actor50 stage endpoints,
exclude other recorded constructor helpers and verify the six unread retained
scratch words. Earlier successful reports remain preserved. No fixture changed.

[Machine evidence](../evidence/application-active-lifecycle-provenance.json)
records exact reader/report/review pins and the next comparison's remaining gates.
No new Native run or application-package acceptance is inherited from this inventory.
The seven remaining active stages and full game are open. The EXE envelope was
not recalculated; previous safety incidents remain open.
