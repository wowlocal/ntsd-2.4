# Retained VS reselection and random commands

Status: all338 source/native calls and the independent source audit match.
Raw and packaged acceptance passed.

The [bounded plan](LIB_SELECTION_COMMANDS_PLAN.md) extends
[LIB_SELECTION_STAGE](LIB_SELECTION_STAGE.md) with the remaining ordinary VS
settings commands1/2. The original NTSD EXE, bundled lib.dll and VC80 execute
only in controlled Unicorn2.1.4 research tooling at CW023f. Native reuses
[OriginalMatchContinuation](../../native/Sources/NTSDCore/OriginalMatchContinuation.swift)
from the full library character caller; no EXE/DLL/emulator enters its runtime.

Both retained sequences first reproduce the accepted human/computer/team prefix.
They reroll twice, hold/release the button, reselect, rejoin and reconfirm both
humans, reconfirm two computers, reroll again, select District/music and reach
Start. Each fresh sequence owns11 menu bitmaps. Source state, resources, library
DC and stack survive every previous call; only declared buttons and three caller
ABI words are supplied for the next call. Initial catalog operand views and
CRT-derived RNG tables remain declared inputs, not an initialized application.

Option2 sounds once and visits every seat in order, selecting only seats whose
451228 flag is nonzero. Each list excludes all eight currently selected ordinals,
uses type0/signed ID<30 and starts at catalog ordinal1. Actual417170 tagEA
consumes the shared RNG and immediately changes selected ordinal/Actor Object;
the next seat observes that change. In these sequences only seats3..7 are random,
so Naruto17/Sasuke21 and the first CPU remain selected. Held input consumes no
additional tagEA draw. Inactive seats still participate when their flag is set.

Option1 sounds once, writes all eight status words0 and latches1, and changes a
selected ordinal to-1 only when its451228 flag equals1. It then writes countdown150
and selection stage0. Actor Object references and the remaining selected
ordinals survive. The next human-screen pass determines activity from status;
this ordering is compared through the following join/character/team/countdown
and computer-selection calls, not reconstructed from expected snapshots.

The reroll caller writes SP20 as its World Actor-table cursor and SP34 as its
seat byte offset. All eight iterations leave World+1b4 and32. Native derives the
World-relative offset1b4 from its own loop contract; source addresses and unknown
stack bytes are never Native inputs. Reselection does not change these counters.
Other-mode dispatch and empty-candidate scratch remain explicit dependencies.

The original capture has336 actual422ab8ret4 returns and two stops before42cf8a,
with429730 still pending. Its actual41bc90 prologue and declared4229cc entry do
not execute the earlier tick body, whole startup, outer loop or match preparation.
A full own library catalog, full match, app window, audio/input latency, Windows,
all content/network and clean-Mac validation remain open.

The independent source auditor verifies375375 stores/1481526bytes,
714856 instruction reads/2804192bytes and10840 API reads/87818bytes. All7598
checkpoints/5820068 records/16866724220bytes and258908 final records/750322820bytes
reproduce with full masks and liveness.194 whole preceding calls equal the
unchanged atomic parent, including every stored point/event/read/helper.
24370 caller counter words and13572 helper returns are checked.4102 actual
instruction starts comprise3611 EXE,51 library and440 CRT starts; these are
observed starts, not all branch outcomes.3694 blobs/197460712 decoded bytes
and every atomic completed case verify.6664 undefined bitmap reads preserve
the source bytes and masks; other false-mask reads have pinned EXE-cookie
provenance. No unknown observed caller-stack byte is imported into Native.

Source capture1 completed0 in82.918s with all inputs unchanged. Its exact
1406196370 bytes have SHA256
`6fbc8465786f92034727115206aaed31144d981d790612ba04b3b75473e28c8e`.
There were no capture errors or source faults in this study. The earlier
selection-stage input/adapter errors remain preserved in their own report.

The raw release run passed four tests in139.662s after a249.54s build:
new whole commands43.078s, two late rollback trials36.272s, and the unchanged
prior selection corpus/rollback tests60.313s. All declared ordinary inputs matched. Core and comparator required no
correction after capture. An existing unrelated trailing-closure warning remains unchanged.

The first failure observer throws on the third tagEA return, after two earlier
seat assignments. The second throws at42e0d2 after reselection writes. Both
verify full current-call World/400Actor/globals, retained DC, bitmap/music owners
and buffered environment rollback; all prior successful calls and supplied
button inputs remain intact. These injected observer failures are Native
transaction tests, not original game faults.

Independent artifact verification reconstructs all659 Native input files from
recursive immutable archives and checks the root caller/test bytes. All259
previous fixtures are unchanged;263 files now include the new332-byte index
and three lossless parts, totaling180606280 bytes. They restore the complete
1406196370-byte raw document with identical bytes, JSON and SHA. The transport
stream itself is147701120 bytes, SHA256
`1e9b6dc9cc673472bd014c9c71d262415a4c8a643de80647dbc24a0517d2ab7e`.
Fixture DEFLATE only transports research data and does not change game replay
compression or source expected bytes. The shipping codec remains separate.

Final packaged release acceptance passed two tests in76.698s (whole41.629s,
rollback35.068s), build0.34s, without raw environment overrides. All663 package
files and the new fixture archive match the recursively verified input
composition. Protected14 pending Native files,3 pending source files,6 foreign
files and all886/887/887/890/890 frozen watcher/catalog inputs remain unchanged.
All source and SwiftPM jobs for this study are terminal; NTSDNative linked.
No application window or device was exercised. The separate full catalog5 is
still live, last observed5 completed Objects; no whole catalog return is claimed.

The [machine report](../evidence/lib-selection-commands.json) pins the source,
finite manifest, read-only static evidence, full audits, Native composition,
transport and jobs. See the [producer](../../tools/oracle_lib_selection_commands.py),
[independent auditor](../../tools/verify_lib_selection_commands.py) and
[whole-call comparator](../../native/Tests/NTSDCoreTests/OriginalLibSelectionCommandsTests.swift).
The previous source/adapter failures remain in their original immutable reports;
this study needed no source, Native or verifier correction and had no refusal.

After both source producers were confirmed terminal,194 complete atomic prefix
files were compared byte-for-byte with the prior parent and stored as APFS
clones. All1157412946 logical bytes, paths, hashes, modes and mtimes survive;
only physical extent sharing changed. Free space measured7035580416->8193622016
bytes during the2.233s operation. The separate live catalog5 traces/inputs and
its6GiB reserve were not changed. This adds no source/native compatibility claim.

Next: remaining mode consumers, own initialized library/menu/prelude join and
the outer loop/application connection. Full match, Windows, device, all content,
network and clean-Mac completion remain open.
