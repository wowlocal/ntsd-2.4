# Tournament bracket and seat assignment — finite acceptance

This study continues [Tournament setup](LIB_TOURNAMENT_SETUP.md) after Start
at4338c3. It recovers menu26 computer simulations, bracket geometry and round
advance, menu27 human seat assignment/cancellation, menu28 result consumption,
and menu29 winner output/return. Actual match preparation starts at434349 and
remains the next required dependency: Tournament has its own HP/team/arena/
replay rules. Stopping before434349 is not a returned or completed match.

The reference is the pinned original NTSD2.4 EXE, bundled lib.dll and VC80 CRT,
with raw original DAT/head/small BMP and menu DIB resources. The controlled
Unicorn2.1.4 environment uses CW023f and declared COM/GDI/device/file responses.
Disassembly, original instruction execution, memory reads/stores and helper
returns are needed to recover branch order, RNG streams, retained input history,
string and drawing operands, live ownership and the real ret12/outer ret4.
This is not Windows/device/own initialized library-catalog evidence.

Before capture, freeze a finite manifest containing fresh setup-to-bracket
chains for all computers and human assignment; natural wait and Jump advance;
confirmation cancellation/reassignment and held Attack; both ordinary RNG
and ID/10==5 outcomes; declared menu28 results and menu29 human/computer output.
Controlled bracket/result entry states are explicitly inputs, not previous
expected after-state or claims of a played match. Only declared buttons/history
and the three outer call ABI words may change between retained calls.
Initial previous-Attack zero is a declared released-input boundary. The actual
419a60 local-input producer copies current CD..D3 to C6..CC;431c70 menu reset
does not reset this history. Any per-call history stimulus must be explicit.
File-backed44d120..44d317 geometry must be verified against original EXE bytes.

Acceptance requires every finite call's full World/400Actors/globals/catalog/
resource bytes and masks, ordered events, semantic caller locals, original
helper returns/stack balance, and exact end boundary. Rebuild Native catalog
and geometry from owned provenance; never import unknown backing or expected
post-state. Execute enabled Start music and winner stop; no fake success or
disabled sound. Preserve source failures separately and do not retry a safety
refusal. Freeze completed source cases atomically; never restart a live capture
for silence. Compare Native rollback after partial bracket/assignment/music
work, retain setup and relevant menu regressions, then losslessly package and
independently verify all bytes/JSON/SHA and unchanged prior fixtures before
publication. Freeze the accepted package; isolate it from pending work.

This plan excludes arbitrary corrupted pointers, protection structures and
fault continuation. Missing reconstructed match-preparation operands stay an
explicit boundary. Full tick/match, Team Tournament/War, application integration,
input/device timing, actual Windows and clean-Mac verification remain open.
