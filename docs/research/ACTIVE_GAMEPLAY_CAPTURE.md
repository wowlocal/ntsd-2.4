# Forty-eight own loaded calls with active keyboard input

Both pristine EXE/VC80 controls match Native for all48 planned calls after the
entire accepted16-call neutral parent. Each comparison checks6484388 records/
27160941109 bytes with masks,1549 state checkpoints and49308/51996 ordered
active-call events;13366/13750 new body/output helper returns. These totals
retain the full parent and the first-call rollback trial where applicable.
The17-return intermediate proof is separate in
[the finite plan and preparation history](ACTIVE_GAMEPLAY_PLAN.md).

**New fidelity boundary:** the actual PE entry loads bundled `lib.dll` before
CRT startup; that library changes game code. It is not loaded in these controls.
Its SHA256 is `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
The captures remain useful pristine controls, but they do not establish the
DLL-enabled application's startup or gameplay. Recovering those changes now
precedes any full-match/application equivalence claim. The installation and
first native text dependency are recorded in [LIB_RUNTIME](LIB_RUNTIME.md).
Native must implement
the recovered behavior without loading the DLL at runtime.
The read-only [patch-overlap audit](ACTIVE_LIB_IMPACT.md) finds five installed
sites in these pristine active-call inventories; it does not compare DLL outcomes.

The reference is the pinned original NTSD2.4 distribution, EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`
and VC80 SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`,
on initialized Unicorn2.1.4 without bundled lib.dll. The source rebuilds its
controlled startup, resource loading,
menus, selection, launch and the complete neutral parent on one retained CPU.
It then supplies only acquired keyboard transitions through the live own
bindings. No Actor, phase, health, position, RNG or caller-stack word is injected.
Normal machine-frame, memory, FPU and helper observations are needed to recover
action order, transient object lifetime, packet changes and sound/output order.
Platform/resource responses and synthetic allocation backing remain declared
research boundaries; this is not Windows or host/device behavior.

## Input and observed behavior

The48 acquisition frames request left/right movement for16 calls, moving
attacks for8, release for4, Naruto defend/Sasuke attack for8, jump for both for4,
opposite depth directions for4, then release for4. A requested button does not
guarantee that its action can begin immediately in the current game state.
The source reads the actual loaded keyboard bindings on every call. Native
derives virtual keys from its own loaded controls and uses the fixed semantic
schedule. It never uses expected Actor commands to drive gameplay.

Both sources produce the same gameplay trajectory. The replay and elapsed
counters450b8c/450bbc advance18..65;450b80 remains the recording-enabled flag1.
There are20 actual keyboard-byte transitions and20 changed recording buffers.
Phase-one calls and the released phase-zero packets leave recording bytes
unchanged. Recording remains live; this sequence does not exercise its final
writer or destruction.

On call17, Naruto's opoint constructs slot50. The original later processes
that slot in the same lifecycle pass, requests its catalog sound and removes
it before the whole call returns. Slot1/X357/index109 precedes slot50/X370/
index366. The final output drains both queued plays in order. Across48 calls,
there are51 primitive RNG events, one lifecycle constructor, seven catalog
sound requests and seven queued plays. Those are research COM requests, not
audio-device measurements.

The inherited constructor memset adapter is used exactly once on this path.
Its entry/return, saved registers, actual Actor+f0 destination and400 bytes
are recorded separately. Those bytes were already zero and remain zero;
the adapter's4450a0 address is excluded from actual stage instruction coverage.
There is no claim that the CRT memset body executed.

| Final field | Naruto | Sasuke |
| --- | ---: | ---: |
|Position X/Y/Z|354 /0 /497|364 /-78 /525|
|Frame|0|212|
|HP|500|500|
|MP|221|221|

Only slots0/1 remain active after every complete return. HP never changes in
this trajectory. Sasuke is still airborne at its end; the series does not
demonstrate a damaging hit, a round result or a complete match. Final RNG
fields450bcc/450c34 are107/68, and all acquired keys are released.

## Compared storage and evidence boundaries

Each complete return adds all137 Object records, including their inline Frame
tables and name tails, plus130 owned weapon-string allocations. These bytes
do not change in the48-call source trajectory, but every full returned snapshot
now observes them. Known bitmap/string identities are normalized from native
resource ownership only.95 Objects have undefined portrait-pointer words;
the comparator preserves their complete bytes/masks without inventing a pointer.

The existing World/400-Actor records, globals, replay allocations, resource
lifetimes and14586 mutable Frame-heap allocations remain in scope. Full Frame
heap/Object/string snapshots occur at complete returns and acquisition
boundaries; the eighteen middle body boundaries retain their previous scope.
This is not a snapshot of every allocation after every original instruction.

Both sources preserve55568 FPU checkpoints, including all15102 neutral-parent
checkpoints and the complete28590-entry retained16-active-call history. The
new48-call portion contains40466 checkpoints:843 per call plus two more on
call17 for the transient live slot. All source CW values stay023f. This source
FPU history is separate from native arithmetic comparison; it does not assert
native process-wide FPSW/tag equivalence or hardware x87 behavior.

Each source records102259 caller-stack accesses from59 PCs. The native call
retains its semantic round result and justified target; unknown scratch and
formatter storage remain unknown. No source stack snapshot initializes Native.
Broad observed-original-address inventories5355/5363 include inherited hook
boundaries and unexecuted stage stops; do not label those inventories as
per-instruction execution coverage.

The declared bitmap backing still produces1824/3360 undefined bitmap-read
events: camera768 each, World drawing96 each and HUD960/2496. Those research
allocation values remain explicit; they are not recovered Windows bitmap
allocation contents or device pixels.

Each active native comparison first reconstructs the entire accepted parent,
then keeps its own state between acquired inputs and complete loaded calls.
The first new call is also tried with a failure at the final dispatcher-write
observer, after real changed replay bytes and movement. The call must roll
back its complete owned state while retaining the already acquired keyboard
input. It cannot undo external effects submitted by callbacks; callers must
buffer those effects until the whole transaction commits.

## Immutable artifacts and reproduction

Source: [oracle_active_gameplay.py](../../tools/oracle_active_gameplay.py).
Independent source checks: [verify_active_gameplay_source.py](../../tools/verify_active_gameplay_source.py).
Native acceptance: [accept_active_gameplay.py](../../tools/accept_active_gameplay.py).
Full packaging/pin checks: [verify_active_gameplay_artifacts.py](../../tools/verify_active_gameplay_artifacts.py).
Trajectory summary: [summarize_active_gameplay.py](../../tools/summarize_active_gameplay.py).

Raw primary51835713 bytes, SHA256
`316e4bf9380afb878998ea87264eec5fb9b1ea02defb3af76e86c7145d506d67`;
control52421599 bytes, SHA256
`e7eb88127ac989323ee40622c347dde8c1920871fd767f33c3a80167ce198e7a`.
Each has657 complete component manifests and3905/3906 binary blobs. Independent
validation checks every manifest/blob, full global writes, key acquisition,
normal returns and retained parents. Source/error/checkpoint files from all
three terminal preparation failures remain preserved with hashes; source
expected bytes were never changed to obtain a native match.

The two lossless fixtures are8569152/8585744 bytes, SHA256
`3b074ff960538fde6e13b1b65bda4f833b22bbc9cafcef990de12f71aaed511c` and
`d3abb578f36ac74716fa6e71eb12091c6f0eed5adb02f1b59a550275c73fc0b3`.
All196 prepublication pins remain unchanged;198 at publication. Complete
restored raw bytes plus newline, JSON, all7811 blobs,1314 component manifests
and ten codec vendor hashes verify independently. Fixture deflation is research
transport and does not replace the game's replay codec.

The strict48 native package exports accepted4298b7e plus the same six active
comparison files that passed the earlier integrated timer/neutral/pause suite.
The final preparation build completes179.43s. Two raw release tests pass
159.390s/build169.00s. The raw invocation rebuilt after the already completed
inventory build; source hashes remained identical and no live build was
restarted. The exact SwiftPM invalidation cause was not audited. Exact process,
source/package pins and results are in `build/research/active-gameplay-work.json`.
The final two packaged release tests pass153.680s/build166.35s without raw
overrides. Both source captures and all owned SwiftPM jobs are terminal;
NTSDNative linked, but no application window or device was exercised.
The package contains510 pinned files, excluding the then-concurrent bundled
library implementation. The six active comparison files are identical to the
earlier integrated package; its six timer/neutral/pause tests had already
passed160.603s/build175.89s. These independent package scopes are not a test of
the full library-enabled join.

The exact completed capture producer is preserved at
`build/research/active-gameplay-completed-source.py`, SHA256
`a4aa7689d9dd654a2bffca78ef17294c79cc49829e15598aeb4d54b73612b0a6`.
After both source processes completed, only a comment identifying the pristine
library boundary was added to the oracle. Its serialized scope string and all
raw corpus/report bytes remain unchanged. The explicit unloaded-library domain
in the published metadata supplements those immutable capture-time descriptions.
The subsequent library milestone preserves these198 pins and adds two separate
fixtures; final independent verification checks all200 current pins.

The actual bundled-library startup,43e9a0 dispatcher, timed application loop
and static World458b00 join remain separate. The app still uses the earlier
Practice engine. Full-match,
general-engine window integration, latency/audio, Windows and clean-Mac checks
remain requirements of the full game objective.
