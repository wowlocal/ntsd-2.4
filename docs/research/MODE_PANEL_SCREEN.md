# Mode screen with the complete enabled panel

The subsequent [library menu continuation](LIB_MENU_CONTINUATION.md) now
compares54 controlled actual422ab8/ret4 returns and two playback continuations,
including network/overlay/volume/present and whole rollback. Its declared
menu-tail entry skips the earlier tick body; own initialized composition remains open.

OriginalModeScreen.advanceWithLibraryPanel joins the accepted whole423b00 panel
to431d10, its bundled-library text replacement, selection, help and cursor tail.
The private screen routine passes current staged resources to the panel after
selection may release the background. Globals, allocations, local strings and
retained text DC commit together. The previous callback-based APIs continue to
share the same screen rules.

All26 controlled ret16 returns and two playback continuations match native;
four panel boundaries verify explicit enclosing rollback. The
[finite plan](MODE_PANEL_SCREEN_PLAN.md) covers all32 source calls.
The existing [library screen](LIB_MODE_SCREEN.md), [panel](MENU_PANEL_DRAW.md)
and [mode selection](MODE_SELECTION.md) corpora remain unchanged. This study
composes their controlled inputs; own initialized catalog/menu, the outer timer
loop and the app connection remain open.

## Reference and declared inputs

Pinned NTSD EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`
and bundled lib.dll SHA256
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`
execute under Unicorn2.1.4. The accepted loader relocates the library to36000000
and executes its installer, retaining13 patches and4000/20000 allocations.
VirtualProtect/copy responses are controlled loader observations, not Windows
protection measurements. No EXE, DLL or emulator enters native shipping runtime.

Instruction, stack, register, read and store traces recover cross-helper order,
resource lifetime and string provenance. Whole bitmap/clip/fill/input/sound/
release/key-name/text children execute. COM/GDI/timer/Sleep/Shell/free/quit are
recorded adapters; no host device, URL, quit or delay is exercised. Normal cookie
checks remain. The panel observer includes its cold tail after the shared ret;
the outer screen executes its actual prologue and ret16. EntrySP1000f000 becomes
bodySP1000e8e8; normal return is30000000/SP1000f014 with saved registers andCW023f
checked. Playback stops before43249c with the screen still pending.

Nine controlled Actor records and eight World seat references retain the prior
mode-screen domain. Five declared500-frame bitmap records supply existing
background/menu/guide/cursor resources; two replay allocations remain owned.
A sixth separate794x550/count11 panel atlas is at2700a020 or2700c020. It starts
with A5 or a deterministic ramp and false masks; its header and13 rectangle
slots are initialized, with11 actual panel rectangles. Both native and the
independent source verifier reconstruct all six bitmap records from these input
declarations. This is neither original raster-pixel validation nor proof that
the own loader initialized these resources. No expected after-state or private
source stack supplies native operands.

Each of two controls has16 predeclared cases: nominal,28 help labels, main link,
banner18 below its bottom edge, notice hover/blink, left/right navigation, timer
reset, confirmation/background release, release followed by notice click, early
outer link before a panel banner, negative GetDC followed by notice, playback,
zero timer range and no selectable row. No branch-completion claim follows from
this composition corpus; the standalone studies retain their wider coverage.

## Ordered composition and provenance

Confirmation releases the current background before the panel requests its
timer. The following native bitmap callbacks see that background as dead and
its surface word cleared, while the independent panel remains live. A notice
click can then request a second confirmation sound, Sleep300 and Shell. The
screen's earlier external link consumes held before a later panel banner checks
it: exactly one Shell request occurs. Panel effects precede help labels and the
cursor, and the final previous-button store sees the panel's cleared held word.
Playback exits before any panel request. Negative GetDC retains the previous
library DC and still continues through the panel and help/cursor behavior.

Native local strings start as1,796 zero/undefined bytes. Defined bytes and masks
are compared; unread private bytes stay zero/unknown. The only instruction reads
with false source global write masks are the pinned EXE cookie44eea4:32 prologue
reads and26 return-check reads. Its value is independently verified from EXE
data. No unknown observed stack/atlas operand is supplied as native input.
For every100-byte fill request, size/color fields and the full mask compare;
the92 undefined padding bytes retain separate source and native backing.

The independent source audit verifies all32 atomic parts,110,256 instruction
reads/176,154 bytes,490 API string reads/7,290 bytes,27,942 stores/102,836 bytes,
704 final regions/6,137,856 bytes and masks,1,218 helper returns and131 blobs/
4,786,976 decoded bytes. Original instruction bytes match1,396 EXE and51 DLL
starts. These boundary-inclusive counts are distinct from native comparison.

The28 selected successful continuations contain4,352 ordered events and1,146
helper returns:340 Blts,198 text requests with136 successful GDI sequences,
112 key labels,26 whole panels,16 sound requests/48 sound methods, four frees,
six timer requests and ten each Sleep/Shell requests. Their original instruction
starts are1,391 EXE plus51 DLL. Native compares globals, all allocation storage/
liveness, read-only World/Actors, defined local storage and semantic retained DC.
Full source stack/library reconstruction remains separate from native private ABI.

Two calls stop before zero DIV423be8; two stop before the ninth ineligible
navigation scan423ed0, after eight actual attempts. Native throws the existing
panel errors and rolls back the entire enclosing screen, including earlier text
DC/local/global work. These four are explicit rejections, not matched full
returns, executed divide faults or completed endless loops.

Four additional native-only observer failures test the enclosing transaction:
panel timer after background free, panel Shell after free and sound,27th help
label after panel drawing, and the final cursor Blt. All state owners must retain
their entry values and the caller's external event buffer must remain uncommitted.
These injected observer errors are distinct from source behavior.

## Acceptance boundary

Build inputs derive from accepteda2a66cc plus only the changed mode-screen file
and new tests. Existing251 fixtures and all pending/frozen/foreign work remain
unchanged. Source/native/package jobs and hashes are recorded in the
[evidence](../evidence/mode-panel-screen.json). Raw acceptance passes12 release
tests in26.920s/build224.09s, including all ten retained tests and the new two
tests in0.283s. NTSDNative links; no app window is exercised. Source capture and
native comparison pass without source, expected-byte or game-rule corrections.
The existing unrelated trailing-closure compiler warning remains in its log.
Packaged acceptance passes two tests in0.230s/build0.33s without raw overrides.
All643 packaged/archive files verify;640 accepted files remain unchanged in
the raw642-file input package. Raw15,893,078 bytes pack losslessly to1,531,944
bytes; complete bytes, JSON, SHA and all131 blobs verify. The new252nd fixture
preserves all251 older pins. Both source jobs and all SwiftPM jobs are terminal.

Next preserve this composition in the own initialized menu and join the actual
outer timer loop. The guarded catalog capture and its frozen watcher remain
independent live work, never restarted for silence. Whole catalog/menu, app
input/animation/audio, Windows/full match/all original content/network/clean-Mac
acceptance remain open.
