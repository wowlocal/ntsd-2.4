# Observed startup correction1 — test imports and unchanged25 checks

2026-09-26, HEAD1164fb8. Continue the preserved failed validation, not a new scope.
Read WORKFLOW, [validation](APPLICATION_OBSERVED_STARTUP_VALIDATION.md), its frozen
plan and the observed-startup implementation plan. Full native NTSD and the first
Naruto/Sasuke District match retain their original criteria; this corrects the test
compilation gate for required whole-startup external observations.

Clone the exact2254-file original implementation candidate and verify the failed
validation copy against it. Change only the import header of
native/Tests/NTSDCoreTests/OriginalApplicationObservedStartupTests.swift:
add `import Foundation` and replace `import NTSDReference` with
`@testable import NTSDReferenceChecks`, matching the prior test's explicit imports.
Keep every test body/assertion, Core, source bytes/masks/expected and all25 selected
methods/limits exact. Record before/after pins and reversible patch. Prior compiler
failure, artifacts, two launcher errors and unexecuted finalizer remain immutable.
This is correction round1; no claim that additional compilation errors are excluded.
Stop at any new failure and diagnose it separately; original three-round bound stays.

Use a new task application-observed-startup-correction1-20260926 under the same X5
parent01a0dc49-738f-7972-8fb0-e98fb2f34408. Reuse the prior guarded monitor/queue/
package/archive routines; task identity changes only. The fresh release/jobs2/
Swift5+enable-testing/native-SwiftPM build and all25 complete tests are mandatory.
Exact199Core/61Reference/277test source inventory and1686 package files checked.
No original/emulator/capture/affected refused operation. Actual Mac clock IO only
inside the selected whole-caller test at returned permits; other responses stay
declared controls. Original EXE/DLL and dirty-input provenance are inherited from
pinned implementation/validation manifests, never inferred from HEAD alone.

All prior limits unchanged: build3600s/12GiB RSS, four new600s/8GiB; exact21 old
method limits (including four300s/4GiB), queue14400s, preparation1800s, closure3600s.
Writable APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,90GiB external/9GiB internal
before clone; keep40/6GiB reserves plus17GiB source commitment. Allow28GiB physical,
stop26GiB observed decrease,150GiB logical,metadata16MiB/root2MiB. T7 authorized
separately. Revalidate PID/start/command/cwd/job; no duplicate job or silence restart.

Separate build, package-byte, whole comparison, preservation/archive and review gates.
Full artifacts and PAX metadata verified before publication. Check root1034,
prior2249, original+failed2254 and corrected2254, source55; only the two import edits
differ. Preserve all failures and stop on first nonpass. Update study/CURRENT_WORK/
map and commit the checked increment. Author review is not independent review;
independent review, root promotion, physical window/resources/callbacks/input/audio,
prepared nonthrowing/aggregate boundaries, initialized loading, Windows/clean-Mac/
full match/game remain open. Source59727 terminal34 Objects, full137 and incidents
remain open. EXE envelope not recalculated. After a passing comparison, continue
concrete physical backend integration using the retained whole-caller owner.
