# Host loading presence assertion, round3

**2026-09-22: fresh build/package checks and34 methods passed; method35
hit its resident guard and seven later methods were not started.**
Native comparison remains incomplete. Independent comparator/contract review, root promotion and actual application
acceptance remain open.

The [frozen plan](APPLICATION_HOST_LOADING_CORRECTION3_PLAN.md) follows the
[round2 sampled diagnosis](APPLICATION_HOST_LOADING_COMPLETION.md) and retains
all earlier assertion/guard outcomes. Root author performs implementation and
verification; this does not provide independent review. No original game,
Windows/device experiment or refused operation runs in this task. See the
[publication](../evidence/application-host-loading-correction3.json),
[patch](../evidence/application-host-loading-correction3.patch),
[closure](../evidence/application-host-loading-correction3-close.json) and
[documentation receipt](../evidence/application-host-loading-correction3-docs.json).

## Analysis and implementation

Base HEAD is `69f7a824c72f4be1ddf25d8fd3654324764a631d`. The actual1037-file baseline
is the frozen round1 candidate, including its relevant dirty inputs. The only
change replaces `XCTAssertNotNil(alternate)` with `XCTAssertTrue(alternate != nil)`
at the retained-child presence checkpoint in HostLoadingTests. The existence
predicate remains; XCTest receives Bool for reporting. The adjacent nil check,
expected different-ticket error and all state/platform/payload/owner comparisons
remain byte-identical. No lifecycle cleanup, case splitting, expected/mask or Core
change was made. The prior full fresh/cached operation-wrapper check is retained.

This correction is grounded in the one-second stack sample, which observed
recursive XCTest formatting at that checkpoint. It does not retrospectively
establish a pass or attribute the entire earlier runtime to formatting. Both
the8GiB RSS termination and600s timeout remain their original incomplete outcomes.

Preparation PID39397 completed exit0 in6.557s. All1037 regular files were cloned
and verified by body/mode/membership and distinct inode; only the declared test
file differs afterward. Applying the exact one-line patch to a separate original
copy reproduces the corrected file byte for byte. Root1034, all487 fixture/runtime
files and55 source code pins are preserved. The runner remains identical to the
prior fresh-build runner except for task identity, including its monitor loop
and three process classification predicates.

## Validation

Build PID39530 completed exit0 in295.738s, sampled peak tree RSS6,231,621,632 bytes.
The fresh output graph contains191 Core,61 ReferenceChecks and267 Tests original
sources plus two generated accessors. All objects/modules passed freshness and
flag checks. The62,536,136-byte binary and385 fixture/102 runtime package files
passed full byte/membership checks. There were no compiler errors.

Test commands and complete/partial results are preserved in
`build/research/application-host-loading-correction3-20260922`. The full42-method
selection is unchanged. Fresh Swift6.4 release/WMO targets use Swift5/macOS14,
two jobs and testability enabled. The same600s per-method limit applies; only
method28 has the already declared12GiB RSS ceiling, all others8GiB. No time or
memory limit was enlarged for this code correction. Complete named methods,
three zero-failure suite summaries and actual exit0 are required.

All first34 methods passed with complete named results and actual exit0:25 prior
host regressions, all four new HostLoading methods and all five LoadedMenu methods.
The presence-corrected ticket method28 passed in97.395s with sampled RSS9,586,753,536
bytes. It still exceeds8GiB, so its separately declared12GiB allowance remains
necessary for this observed run; the Bool correction did not establish an8GiB bound.
Fresh/cached loading, late retry and six packet-exhaustion controls also passed.

Method35 `OriginalApplicationLoadedCycleTests/testActualBootstrapCyclesKeepCachedOwnersAndReachHeldSelectionBoundary`
(PID46267) hit its8GiB resident guard at18.722s: sampled8,712,486,912 bytes,
122,552,320 above the bound. The monitor revalidated identity/cwd before SIGTERM,
reaped-15 and found no remaining processes. The log contains a method start only,
with no assertion error or complete result. Methods36–42 never started. Queue
elapsed time was707.438s. This leaves34 verified passes and eight unresolved methods.
No method was skipped or relabeled, and no automatic repeat followed the guard.

Static inspection of the guarded test shows retained first/current owners through
both backings and whole input/menu/outer checks, and no `XCTAssertNotNil` anywhere
in its file. It does not locate the RSS peak or prove its cause. The earlier
formatting sample is not evidence about this different method. The concrete
resource-contract issue is that8GiB has not sufficed for all whole multi-owner
regressions. That bound is an experiment constraint, not an original game rule.

## Preservation and open gates

The new archive verifier reuses exact file bytes/mode/mtime/membership checks,
explicitly distinguishes source traversal directories from the two archive
ancestor directories, and verifies exact nanosecond PAX metadata times. Package,
archive and Native comparison gates remain separate. The24GiB observed allowance
includes concurrent source growth and keeps original40/6GiB reserves. Source59727
is revalidated independently and never restarted by these corrections.

The fourth selection input boundary is an injected stop, not another returned
iteration. Controlled Native composition does not replace whole Catalog53 source,
real providers/timing/window/input/audio, later typed child retention, independent
review, root promotion or full-game/clean-Mac acceptance. Existing safety
dependencies remain open. This is the last of the three declared correction
rounds. The failure and static/capacity diagnosis are preserved; the next task
must use the following revised continuation contract, not an automatic fourth
candidate correction: same frozen binary/candidate,34 retained passes, whole
methods35–42,12GiB RSS for the three LoadedCycle methods35–37 and8GiB for the five
initialized methods38–42, unchanged600s limit and stop on first nonpass. Recheck
24GiB host capacity and source/space commitments. No code, expected/mask or method
body change is proposed. This keeps the original guard immutable and requires
all eight complete results before the42-method gate can pass.
