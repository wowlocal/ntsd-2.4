# Display observation optional count — correction1 and exact34 validation

2026-09-26, HEAD9c7bcf5. Continue the implemented2261-file display candidate and
preserved first compile failure. Base manifest
c6077a1eeaec431860dc58930ac8eed593faba350870bc597cf256383bc5905f.
Build20523 terminal1/absent before tests; failure-finalizer25731 terminal0/absent,
4339-file artifact and27-member metadata archives verified. No original, Native
test or blocked operation is repeated. Use WORKFLOW and the implementation/
[validation plan](APPLICATION_MAC_DISPLAY_VALIDATION_PLAN.md).

## Exact correction and consumer

The compiler requires Observation.knownPixels:Int; `s?.storage.map { ... } ?? 0`
leaves that expression optional. On a separate complete clone, change only
OriginalMacDisplayBackend.observation: explicitly unwrap optional storage, count
its known bytes with the same Array/filter/count expression, otherwise assign0
to an explicitly typed Int. This is a diagnostic result type correction. No pixel
write/read/format, allocation, reference, request or Core algorithm changes. Every
test body, original expected byte, mask, fixture and runtime resource remains exact.
Preserve both failed candidates and the original syntax-only implementation.

The required result is the same whole-startup display allocation and physical clear/
AppKit view readback toward the first Naruto/Sasuke District match and full game.
Run exactly the same34 methods and every saved limit, including all29 earlier
methods and the420-case clear comparison. Windowed WinMain must still perform no
clear/query-format. Native format/logical screen/reference policies and all unknown
Windows/device boundaries remain exactly as declared, not broadened by correction.

1. Revalidate X5 and terminal jobs; clone all2261 files by bodies/modes/nsmtimes and
   distinct inodes. Apply only the explicit observation-count replacement and pin
   the one-file patch. Verify every other file, especially all tests, unchanged.
2. Reuse failed task's guarded build/queue/package tools with task-name change only.
   Preserve monitor/result predicates, all34 methods/limits and target membership.
3. Fresh release build-tests/jobs2/enable-testing/disable-index-store, package check
   of199Core/61Reference/5MacPlatform/279tests and1686 exact resource files, then34
   complete methods with exact saved one-method pass and no guards/signals/residuals.
4. Stop at first nonpass and preserve it before diagnosis. At most three correction
   rounds; this is round1. No automatic rerun, comparator change or relaxed mask/color.
5. Verify root1034/prior2257/original2261/failed2261/corrected2261/source55, patch
   roundtrip and full regular artifact/PAX archives. Publish, update handoff, commit.

## Bounds and remaining gates

Task application-mac-display-correction1-20260926 under X5 parent
01a0dc49-738f-7972-8fb0-e98fb2f34408. Writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,90GiB external/9GiB internal before preparation;
40/6GiB reserves plus17GiB source commitment.28GiB physical, stop26GiB observed
decrease,150GiB logical,metadata16MiB/root2MiB. Prep<=1800s, build<=3600s/RSS12GiB,
queue<=14400s, each of34 saved time/RSS limits unchanged; finalization<=3600s,
poll1s/RSS2s/logical15s. Revalidate PID/start/command/cwd/job before actions.

Only task-owned files/alias, preparation adapter, plan/study/evidence and own
navigation change. Root Native/source/expected/masks/frozen producers/plans/incidents
remain protected; T7 permission does not move pinned APFS paths. Independent review
unavailable, author review is not independent. No original/emulator/capture/refused
operation runs. Source59727 stays terminal34 Objects, not137. General blit/presentation/
conversion/callback/fullscreen/providers/input/audio/loading/root promotion/Windows/
visual/clean-Mac/full match/game remain open; NTSDApp still Practice. EXE envelope
not recalculated. After success choose the next concrete rendering dependency.
