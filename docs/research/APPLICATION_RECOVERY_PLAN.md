# Whole application surface recovery43e890

Recover43e860..43e88b and whole43e890..43e8d4, including actual401ae0/401a80,
43bdd0 and all window/DirectDraw children. Pinned original NTSD2.4 EXE and bundled
lib.dll execute under Unicorn2.1.4 after the accepted installer parent. Actual
instruction, stack/global and object-release observations recover which restore
result matters, exact8876024c handling, flags and ignored recreation failure.
COM/Win32 responses and helper-local backing are declared boundaries, not actual
Windows device/loader/callback delivery. No control/protective storage corruption.
Native game must use Swift rules and owned resources, without DLL/emulation.

Finite acceptance: both display modes, primary present/absent, draw present/
absent with a valid back surface; five first-restore results and eight second-
restore result boundaries (including8876024b/c/d). Avoid duplicate unused first
responses when the primary is absent. Execute whole recreation for negative
non-sentinel back results. Cross ordinary failures of each observed recreation
API in representative windowed/fullscreen and fallback routes. Two whole43bec0
initializations followed by repeated recovery calls must retain their own
surfaces/globals/releases; preserve caller stack between those direct calls.
A separate failed creation followed by another recovery may encounter a null
back surface: retain the original ordinary-resource failure boundary separately
from native explicit rejection/rollback, never call it a successful match.

Compare complete bounded globals/masks, ordered requests and stores, object
lifetime, all helper-entry structures, actual helper and whole returns, callee
registers/SP/CW and byte-checked executed PCs. Native shares display destruction
with the accepted lifecycle callback and reuses whole43bdd0. Late failures after
release/recreation/show/flag clear must roll back all staged owned state and
buffered effects. Missing local backing stays explicit. Earlier lifecycle,
window initialization, art setup and message-loop tests remain unchanged.

Retain237 immutable prior pins, atomic cases and terminal errors. Use isolated
committedca2abac native export with owned overlays, excluding six foreign files.
Publish only after raw comparison and independently verify bytes/JSON/SHA/blobs/
vendor hashes, then packaged comparison. Full43e9a0's actual negative-result
reachability, own WinMain/CRT/NLS/private-stack join, reentrant callbacks, library
transforms, real window/devices, matches/all content and cleanMac remain open.
