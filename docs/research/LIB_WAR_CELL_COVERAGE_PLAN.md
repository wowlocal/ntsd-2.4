# War editable-cell coverage supplement

The completed574-call [setup corpus](LIB_WAR_SETUP_PLAN.md) remains immutable.
Full audit2 reconstructed every captured call, then failed its final44-cell
coverage assertion. Only seven distinct cells received adjustment writes.
The preserved `lib-war-source2-coverage-gap1.json` shows the cause: at the
438bbb consumer, simultaneous Right/Defense becomes Right only; simultaneous
Attack/Jump/Defense becomes Attack only. The existing menu input arbitration
has already selected one action before War consumes the flags. Four frame
phases and all thirty preset/strength combinations remain separate observations.
The44-cell requirement is not reduced to seven. Source audit3 records missing
cells explicitly; reconstruction of the574 observations is not full acceptance.

Recover editing each of the22 active and22 reserve cells through ordinary,
separate navigation and Defense presses. References remain the pinned EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
and VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Use controlled Unicorn2.1.4/CW023f, original resources and the same declared
allocator/COM/GDI/music responses. Execute41bc90, the declared4229cc menu tail,
installed library, whole438b40, frame helpers, ret1c and outer ret4. This is no
Windows/device, earlier CPU-selection reachability or played-battle claim.

The finite supplement has192 calls. Its first two calls independently rebuild
the primary constructor/resource parent and declared ready bridge, reproducing
the first two completed source2 cases byte for byte before new actions proceed.
The new sequence starts at actual row6/cursor2/side0. Two Down edges reach row1;
two Left edges reach cursor0. Each edge has a separate press and release call.
For each cell, a Defense edge edits it, then a Right edge moves to the next cell.
Rows1/2 each traverse twelve cells; rows3/4 each traverse ten. A Down edge moves
between rows. All44 edits carry a declared expected address, checked against
actual writes after the completed case is atomically retained. No cursor, troop,
private-stack or expected output value is injected to force coverage. The only
ready bridge remains the explicitly declared input from the earlier study.

Low-level reads/stores, checkpoints and helper/REP/ownership traces distinguish
input priority from a missing troop update and prove that every cell belongs to
the intended row and team. Acceptance requires all192 actual outer returns,
191 War returns, the401 constructor parent, two owned War resources, exact
parent reproduction, all44 adjustment destinations, and complete record/mask,
read/store/event/helper reconstruction. Compare the same retained chain with
independently produced Native state. Keep the original574-case comparison and
its eight source-coupled rollback trials as separate required evidence.

Do not rerun or replace completed capture2. The supplement has a new manifest,
producer/input pins, raw document, atomic parts and logs. Preserve any failed
coverage assertion or source fault; do not continue an actual source fault.
Use X5 for large files and retain the6GiB storage reserve. Full setup acceptance
still requires combined coverage, Native comparison, lossless packaging and
retained dispatcher checks. Preparation43a21f, War gameplay43a860, the own
catalog/library/outer loop/app, full match/game and device/Windows checks stay open.
