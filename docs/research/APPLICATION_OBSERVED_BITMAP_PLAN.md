# Observed bitmap requests through the whole first-menu Host transaction

2026-09-26, HEAD878f7fd. Previous turn: progress, native bitmap/DC provider and
all48 checks published. This card connects that provider to the complete first
resource/settings/background/body/menu/dispatcher/timer caller, advancing the
startup/loading path to the Naruto/Sasuke District match. It does not substitute
another renderer or source capture for the required caller integration.

## Contract, evidence and finite scope

Reuse OriginalRequestExchange and the existing Host/Bootstrap/MenuSession.
Move the bitmap request's type-only declaration into Core, preserving the public
Mac alias. Add one per-iteration coordinator and a value delivery cursor/history
on the prepared platform. Physical work runs outside Core after beginService;
the final cursor check runs after the last fallible publication copy, immediately
before either commit or pending-loading retention. Retained contexts own receipt
resources after coordinator/backend death and after later iterations. Empty
iterations must not accumulate empty receipt histories. Pin the Host commit
sequence inside its transaction lock to reject intervening external commits.

Keep the existing array-based bitmap path unchanged. The new callback path
requires empty bitmap response arrays, updates the same staged provenance owner,
and returns actual observed writes unchanged. For the currently supported Native
provider, GetObject writes only offsets4..<20 and description only8..<16; validate
their extents and equality to the Native-owned image/surface fields. Reject other
write domains, conflicting bytes and partial/unknown required fields explicitly;
never promote ignored type/pointer/private bytes to known zeros. The declaration
path continues rejecting captured writes. No old expected, mask, comparator or
fixture is changed. Late validation failure publishes neither Core nor platform.

Mac service keeps its old exchange entry and adds the coordinator entry. Required
message/debug requests may be served once by an explicit external diagnostic
consumer; absence of that consumer remains a boundary. Diagnostic test consumers
are controlled replies, not claims that an actual Windows/macOS dialog appeared.
No renderer may deliver the already-served bitmap effects again from the committed
batch or its overlapping graphics projection.

Read-only evidence: APPLICATION_BOOTSTRAP, APPLICATION_HOST_TRANSACTION_VALIDATION,
APPLICATION_HOST_DELIVERY_CONTEXT_VALIDATION, APPLICATION_OBSERVED_STARTUP,
BITMAP_SURFACE_LOADING, APPLICATION_SURFACE_COLORS, APPLICATION_GRAPHICS_OWNERS,
and archive1968–2025/2053–2140. Original EXE SHA256:
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c.
No original/emulator/Windows execution or historical producer/auditor restart.
The48/50/12 source caller comparisons retain their existing declared boundaries.

Five new methods cover: whole first menu on the physical startup display with
actual bitmap replies and complete retained state/effect/resource checks;
late failure and retry without repeated physical work; missing-resource message/
debug order and rollback; observed output extent/byte/mask consistency and atomic
registry rejection; stale Host/foreign/cancelled protocol and context/history
lifetimes. New physical tests use original input bytes, explicit non-bitmap
controls and disjoint test-owned logical wrapper allocations; no source after-state
or private allocator addresses become input. Compare full physical RGB/masks to
the accepted source colors; compare whole Core state to an independent prepared
replay of those actual API replies, preserving the distinction from Windows.

Retain all48 previous methods,5 HostSession,2 Bootstrap,3 Graphics and6 delivery
context methods:69 total. Exact names frozen before execution. Inherit each old
method's limits from bitmap or delivery-context validation; the3 expensive loaded
context methods retain1800s/16GiB. New methods600s/8GiB. Stop at first nonpass;
at most3 correction rounds before diagnosis, never alter old failures to pass.

## Ownership, execution and delivery bounds

Actual baseline2265-file candidate manifest SHA256
944cd8c8a83ace502c7c950d22aec925ffcc827e883b2df438b0c319b38ce843.
Modify five Core files and the Mac service; add two Core files and one test:
2268 files,201Core/61Reference/7MacPlatform/282tests,1686 unchanged resources.
Root1034, prior2257, all2265 base files, source55 and immutable instruction archive
remain protected. Only task-owned candidate/alias/adapters/templates, this plan,
study/evidence and own navigation insertions may change. Independent reviewer
unavailable; author analysis is not review. Existing safety incidents remain open.

Task application-observed-bitmap-20260926 under X5 parent
01a0dc49-738f-7972-8fb0-e98fb2f34408. Recheck writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548,90GiB external/9GiB internal preflight;
retain40/6GiB reserves plus17GiB source commitment. Physical28GiB, stop observed
decrease26GiB, logical150GiB, metadata16MiB/root2MiB. Prep1800s, build3600s/RSS12GiB,
queue14400s, finalization3600s. Reuse guarded runner/queue/package/archive tools.
Use the corrected PID/start-time protection from the previous test monitor, not
its obsolete permanent numeric exclusion. Revalidate every job before action.
The old build-monitor gap remains historical; this is a new changed-code build.

Acceptance separates fresh build, exact package bytes/membership, all69 outcomes,
protected inputs, patch and regular/PAX archives, and independent review. Commit
coherent checked increments with open gates stated. Whole actual front rendering,
general Blt, callback/rectangle provenance and exact zero presentation rectangles,
other IO/diagnostic UI/input/audio/Windows/clean-Mac/full match/full game remain
open. NTSDApp remains Practice. EXE envelope is not recalculated.
