# Independent active-body comparison on owned application state

Status: in progress. Parent a5a7a7d accepted the finite input endpoints and
retained48-call preflight, while the entire active body remains unaccepted.
Consumer: the complete Bootstrap game tick after real own keyboard acquisition,
on the path to a full Naruto/Sasuke District match and the full native game.
This plan extends [the active card](APPLICATION_ACTIVE_GAMEPLAY_PLAN.md).

## Scope and reference

Compare all19 stages of both immutable48-call schedules using independent
calculations on current own storage; source-first calculation must reproduce
saved stage endpoints before application comparison. First implement the
control/history/Frame/RNG projection, then physics and the remaining stages.
Intermediate stage results do not close the whole retained-body contract.
The previous neutral comparator and accepted acquisition checks stay strict.

Reference remains the original clean NTSD distribution, EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
VC80 c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
installed lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba.
Read saved active captures, declared source instruction/store/helper evidence,
ACTOR_INPUT/ACTOR_CONTROL/WORLD_CONTROL/CONTROL_PRECISION and the installed
library contract. No original game/emulator execution or historical producer/
auditor rerun is planned. Swift executes only Native tests. Saved Unicorn
CW023f mappings and platform responses remain distinct from Windows/device.

The Native preflight reached additional Sasuke frames66..69 and242. These are
observations of Native behavior, not expected results. New formulas must read
actual retained DAT/header/Frame/RNG values, reject unsupported independent
branches explicitly, and use the existing source/control-library evidence.
Pristine active controls do not establish installed-library branch outcomes.

## Required comparison

| Boundary | Required evidence |
| --- | --- |
| Control | Full World/400 Actor/global bytes+masks, buffers/history/combos, current Frame reads, movement and sequential frame transitions, own RNG values and ordered observations; installed hook predicates explicit |
| Physics/lifetime | Full numeric/store ordering, world pass/caller effects, alias/coherence, subsequent consumers; actual transient-slot lifetime cannot be imported from source |
| Remaining body | Contact/hit/links/cpoints/camera/drawing/impulses/lifecycle/commands/HUD/notices/recording/layout/output, all current Object/Frame/BG/resource owners and masks, complete effects/journal |
| Whole application | Both48 retained returns after the existing17 neutral parent; input survives rejected gameplay, whole owner rollback, same-ticket retry and required prior regressions |

Source snapshots enter the comparison model only. Core receives its own retained
application state and declared platform replies, never an expected after-state.
A byte endpoint match is not a per-instruction store proof; typed stores and
non-front/helper/RNG events retain separate checks and declared gaps. No store
may be excluded merely because Native disagrees. No unknown bytes become zero.

## Ownership, execution and preservation

Root owns new OriginalApplicationActiveBody*.swift comparison tests, a bounded
optional observer hook in OriginalApplicationActiveGameplayTests.swift, this
plan/result/evidence and CURRENT_WORK/RESEARCH_MAP links. Core changes require
separate demonstrated gaps and a plan addendum. Existing Source/StateProjection
neutral readers, fixtures, expected, masks, resources and history stay immutable.
The existing read-only war_contract_review may own new numbered reviews only.
Independent review and machine comparison are separate gates.

The actual clean991-file parent is pinned in
build/research/application-owned-active-body-native-20260914/initial.json.
New large IO uses that task's X5 area only after UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548/free-space validation. Reserves remain40GiB
X5/6GiB internal; initial task maximum40GiB,3600seconds per Native run, three
initial candidates before diagnosis/revised bounds. Reuse the established
freeze/job/verification/archive tools with concrete pins; no new capture engine.
Select the new comparison methods plus both existing active methods when their
shared helper changes; unchanged regressions are inherited only by exact pins.
Do not restart a live or completed job. Preserve failure logs and frozen inputs.

Existing safety incidents remain open under WORKFLOW. This continuation does
not reroute/retry an affected operation; record any new refusal and stop that
operation. Full active body, paused paths, damaging outcomes/round/full match,
app integration, device raster/audio/input, Windows/clean-Mac and the full game
remain open. EXE envelope is not recalculated by this comparison work.

## First finite control candidate

The initial saved-data formula reproduced960 Actor checkpoints and96 complete
control pool/global endpoints. It checks resulting masks but did not assert
definedness on every operand read. The Swift candidate uses known record reads,
checks all10 checkpoint PCs, final41e634/SP1000e9bc and ordered RNG helper
entry417170/return413641 joins. Full helper ABI and instruction-store comparison
remain open. Source stage records omit middle Object/Frame storage; acquired
records supply the contract inputs, not proof of no interior writes.

Only first two distinct slots are active, current control states0/1/3/4/7;
combo-transfer, strict Frame-transfer, run-entry and airborne-attack branches
remain explicit unsupported comparison boundaries. Frame selection is sequential
and fresh before installed-library routing. Double arithmetic is limited to
zero or magnitude[1e-100,10000), with inputs/results guarded; the established
extended-exponent/subnormal Core contract is unchanged. Negative trials cover
small/nonfinite operands, live alias, unknown causal input and progress3.

Candidate1 selects OriginalApplication(ActiveBody|ActiveGameplay)Tests/: two new
source-first control methods and two retained active input/body preflight methods.
All994 Native files will be frozen;990 previous files remain exact. The145
unchanged inherited methods are not rerun. Publication accepts only control
endpoints/owners/RNG and existing preflight, leaving18 other stage comparators
and the full application/game open.

Candidate1 built successfully but its two new source-first methods rejected
the assertion that every non-state component reference is unchanged. Saved
call17/29 RNG updates also appear in early.globals, a current-state mirror; all
other early fields remain equal in both48 schedules. The separate correction
record preserves this comparator failure. Candidate2 compares that mirror to
the same calculated globals and explicitly retains all early owner fields.
Core and source expected remain unchanged.
