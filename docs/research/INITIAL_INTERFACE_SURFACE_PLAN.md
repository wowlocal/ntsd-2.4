# Controlled initial interface with whole surface loading

**Accepted controlled study: [INITIAL_INTERFACE_SURFACE](INITIAL_INTERFACE_SURFACE.md).**
This independent dependency follows the [own loading prefix](APPLICATION_LOADING_PREFIX.md); it does not replace
that still-running capture or claim an own postcatalog return.

Recover the loading-time UI caller41c2f5..41c581 with its whole43ee50 constructor,
43ed10 image/surface loader and4013d0 copy child. The older
[initial-interface corpus](INITIAL_INTERFACE.md) stops43ed10 at declared device
results. The [whole bitmap loader](BITMAP_SURFACE_LOADING.md) has separate
controls and own front-menu composition. This study closes their controlled UI
composition boundary, including dimensions produced before surface failure.

Reference: pinned original NTSD2.4 EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
MSVCR80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
and all ten original embedded UI DIBs. Unicorn2.1.4 executes original World,
400Actor/bootstrap, UI/bitmap/copy instructions and actual CRT memset. Normal
constructor cookie checks execute unchanged. Instruction addresses, complete
stores and operand reads establish ordering and the ownership of retained
wrapper dimensions/private API fields; ABI returns establish callee completion.

This is a controlled caller: catalog[0], Object+90, outer selector/frame,
allocator backing and Win32/GDI/COM responses are declared inputs. Native must
construct its own World/Actor/bitmap state; no expected after-state or private
stack backing is imported. The pool and UI run continuously without a CPU or
state replacement between them. These are not actual Windows/DirectDraw/device
calls, an own catalog/loading chain, or a full41bc90 return.41c581 is unexecuted.
No control/protective storage corruption, bypass, exploitation, unrelated target,
external network or source-fault continuation is part of this experiment.

The disjoint declared controlled World/catalog/Object arena74800000 and400Actor
arena75000000 are checked against this VM's complete mappings before allocation.
They are unrelated to current live catalog VMs. Bitmap records use50000020;
all initial full bytes and false masks survive. Only established World/catalog/
Actor/surface pointers may bind native ordinals. Unknown address-looking bytes
remain unchanged. The original ten DIB contents are immutable.

Finite initial set: A5 nominal; ramp/reversed allocation nominal; all/alternating
NULL bitmap allocations; first/last absent image; first CreateSurface=-1;
last CreateSurface=+1; last SetColorKey=-1; last GetDC=-1. NULL allocation skips
construction but stores0; other failures retain the original cleanup/order.
No intentional pointer/cookie damage is requested. A single nominal producer
probe first validates the combined observer; completed outputs use unique names.
Do not restart the three live application catalog captures for this study.

Acceptance requires:

- All ten source calls reach41c581 with10 global pointer stores then44d05c=0,
  exact nonvolatile registers/ret12 for every bitmap constructor, and400+8 Actor
  constructors from the declared first Object. Keep distinct API/allocator inputs.
- Independently replay every retained write into all allocated records, globals
  and stack; verify full bytes/masks and original DIB/source hashes. Compare each
  retained state and API request at its event boundary, including unknown fields.
- Compose native OriginalWorldBootstrap and OriginalInitialInterfaceLoading with
  OriginalBitmapConstructor.constructWithSurfaceLoading. Compare full World,
  all400Actors, all allocated bitmap records/masks, globals after each store,
  exact requests/results and resource ownership/cleanup. Preserve dimensions
  after CreateSurface failure and ignore numeric outcomes as the original does.
- Verify late native observer failures roll back the enclosing candidate,
  including globals, loader and graphics; these are native-only rejection
  checks, not successful source executions. The completed controlled pool is a
  separate compared input; whole own pending-loading rollback remains open.
  Reuse unchanged accepted parent and
  helper controls and keep every prior fixture pin.

Keep any unexpected helper, unknown provenance or source memory fault explicit;
do not replace it with success. Record failure details before diagnosing an
ordinary harness error; stop automatic retries after any safety refusal. Source
execution, native comparison, transport verification and eventual own/window/
Windows evidence must remain separate. Work/jobs:
`build/research/initial-interface-surface-work.json`.
