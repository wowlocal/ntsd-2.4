# Whole application art setup43e8e0

Recover the complete helper43e8e0..43e934 used by43e9a0 when44dce4==1.
The pinned NTSD2.4 EXE (SHA2563f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c)
runs in Unicorn2.1.4 at declared COM/OutputDebugStringA boundaries. This study
is development tooling, not a DLL/emulator in the native game or a Windows
surface/pixel measurement. Actual instructions and stack/global stores are
needed to establish live target reads, local retention, request order and the
signed HRESULT decision. No protective/control storage is modified.

Finite acceptance:225 whole controlled calls cross five query HRESULTs, five
clear HRESULTs, three output extents and three ordinary local backings. Six
further calls reuse their own previous globals and local output without reset.
All source calls execute whole401250. Compare full50088 globals,32-byte query
and100-byte clear records and masks, ordered requests/stores, whole return,
stack/callee preservation and actual instruction starts. Query output writes
and changes to valid surface references are declared callback effects. Partial
or absent output is retained but never read by this original caller. Numeric
query/debug failures are ignored. Only the clear HRESULT chooses the original
diagnostic string and result0/1. Native exceptions after query, clear, debug
and final observation must roll back owned value state; platform effects must
be buffered until the encompassing operation commits.

Preserve236 prior fixture pins. Execute source before native acceptance; retain
terminal errors and atomic completed cases. Build from isolated committed
6b69cc3 native files plus owned overlays, excluding six foreign transform files.
Publish lossless evidence only after raw comparison, independently verify all
bytes/JSON/SHA/blobs/vendor pins, then compare packaged evidence. This helper
does not close full43e9a0,43e890 recovery, the initialized stack provenance,
mode/editor/worker dependencies, CRT/NLS or the app/window/full-content goal.
