# Enabled menu panel drawing and input

OriginalMenuPanelDrawing implements whole423b00, including the cold blink tail
4242b3..4242d7 that jumps back before the shared4242b2 return. All1,008 controlled
whole returns match; four unavailable-result boundaries are explicit native
rollback rejections. See the [finite plan](MENU_PANEL_DRAW_PLAN.md) and
[evidence](../evidence/menu-panel-draw.json). This is the panel dependency of the
[installed-library mode screen](LIB_MODE_SCREEN.md); their enclosing enabled
composition and own initialized resource chain remain open.

## Reference, environment and finite domain

The pinned Windows distribution supplies NTSD EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`
and lib.dll SHA256
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Capstone5.0.7 supplies the static inventory; Unicorn2.1.4 executes the original
instructions in the controlled harness. Instruction, memory, stack and register
observations recover animation/timer arithmetic, mouse aliases, bitmap order and
the provenance of consumed fields. This is compatibility research, with no EXE,
DLL or emulation in the native runtime and no Windows/device measurement.

Each fresh research VM runs the actual bundled DLL installer: relocated base
36000000, allocations4000/20000 and13 patches. Declared VirtualProtect and copy
responses are synthetic loader boundaries. The observed panel/helper instruction
bytes do not overlap these patches; this body executes no DLL instructions.
Bitmap43f010, clip43ef70, fill415160 and confirmation-sound401a30 instructions
execute. Timer, COM, Sleep and ShellExecute are recorded adapters; no host URL,
audio device or wall-clock delay is exercised.

Entry rootSP1000f000 contains the stop, previous-button pointer and bitmap target.
The real return reaches30000000/SP1000f004; saved nonvolatile registers, helper
returns and CW023f are checked. Native exposes semantic minimum coordinates,
not the private x86 return-register/stack ABI. The adjacent4242e0 timer loop is
a separate function and is not claimed by this study.

The finite manifest precedes capture:501 cases per backing control, including
64 retained calls each. Gates, eight rotating rows, timer maxima/wrap, animation
thresholds, main/arrow hitbox edges,24 banner rows, version/blink gates, mouse
aliases and distinct drawing targets are declared in the plan. The first1,002
cases remain unchanged. A separate ten-case extension closes the observed
maximum-update, hover-delay-reset and forward-navigation-wrap gaps. It does not
claim all branch outcomes or all possible numeric inputs.

Five declared bitmap records start A5 or a deterministic byte ramp with false
masks. Thirteen rectangle slots per record and the header are initialized;
the panel replaces its first11 rectangles and uses794x550/count11. Panel/guide
addresses swap in the second control. Unused fields remain unknown, and an
independent verifier reconstructs every input byte/mask from this declaration.
These are controlled atlas inputs, not an own resource-loader result. Nine Actor
records, World and two replay records remain unchanged and unused by this body.
Retained calls continue actual preceding globals, applying only declared stimuli.
No private expected stack bytes enter the native implementation.

## Recovered rules

With44d780=-1, the panel finds the greatest positive end time among active links,
requests unsigned milliseconds and takes unsigned remainder by that maximum.
It resets selection/progress before storing the remainder. Other calls select
the first active signed interval containing the timer; this function does not
increment44d780 on every call. Progress, slide positions and durations preserve
32-bit wrap. Hover may hold or reset progress. Navigation changes44d780 to each
attempted row's start before checking eligibility, with eight-row wrapping.

The main link has no upper X bound. The right arrow also has no upper X bound;
both arrows use the first banner width at453f70 for their hover-delay rule.
Banner rows18..23 omit the upper Y bound. Minimum coordinates begin at999;
the badge is requested only when both fall below999. Notice progress wraps,
resets at60 and selects frames around30; its hover test has no lower X/Y bounds
and may request the same frame twice. Bitmap target is the argument, while
border fill uses live global455608.

A link click requires held==1 and the live previous word==0. It clears held
before confirmation sound, then requests Sleep300 and Shell(open,URL). Previous
word aliases to held are retained. Arrow clicks clear held without the link
sound/sleep/shell sequence. Bitmap/fill/sound numeric failures retain the original
ignored-result behavior. Unknown92-byte fill padding is zero/undefined natively;
the source keeps its actual stack backing. Only the defined size/color fields
are byte-compared, alongside the complete padding mask.

## Evidence and failure boundaries

Across all1,012 cases, the independent source audit verifies273,994 instruction
reads/995,608 bytes,268 API string reads/4,470 bytes,219,660 stores/878,640 bytes,
21,252 final regions/185,997,504 bytes and masks, and11,150 helper returns.
All1,012 atomic parts and1,856 blobs/85,210,944 decoded bytes verify.
No observed read consumes unknown bytes; unread private storage remains unknown.
731 original EXE instruction starts include536 of538 panel instructions. The
only missing static starts are skipped alignment424028 and42402f. Actual code
coverage is distinct from branch-outcome, initialized-game and Windows coverage.

The1,008 normal returns contain47,136 ordered events and11,146 helper returns:
4,638 bitmap requests,32,052 bitmap reads,4,638 clip requests,4,224 Blts,
52 timer requests,728 border fills,134 sound requests/402 sound methods and
134 each Sleep/Shell requests. Their730 original instruction starts are
separate from the boundary-inclusive counts above. Native compares full globals
and read-only nonstack records:20,160 records/119,202,048 bytes and masks, plus
defined semantic minima and ordered effects. Source full-stack reconstruction
is independently verified, not claimed as native private-stack equivalence.

Two controls have no positive timer range and stop before the actual zero DIV
at423be8. Two stop at the ninth backward-scan head423ed0, after all eight actual
attempts find no eligible row. Zero and `?` are accepted by the separately
recovered content parser, so these unavailable results need no control-pointer
corruption. Native throws zeroTimerRange/noSelectableRow and rolls back. These
are not successful full returns, executed divide faults or completed endless
loops. The pinned distribution lacks the ad0/ad1 files; successful-data controls
do not establish that its ordinary missing-file startup enables the panel.

Five native-only observer failure points verify rollback and an uncommitted
caller event buffer: initial timer, fourth border fill, sound method, Shell and
final notice Blt. The timer failure precedes the first state mutation; the other
points test later work. External effects remain the caller's responsibility to
buffer until its whole enclosing operation commits.

## Acceptance and remaining composition

The isolated build starts from accepted8d2cc05 plus the two new Swift files;
all638 accepted native files remain byte-identical. Initial compilation failed
on four Swift throwing short-circuit expressions. Moving `try` before the whole
conditions fixes compilation; the earlier input archive/log remains preserved.
No source expected byte or recovered game rule changed.

Raw release acceptance passes10 tests in5.270s/build220.99s, including all eight
retained panel-bitmap, panel-update, startup-panel and installed-screen tests.
The new two tests take1.808s. NTSDNative links; no app window is exercised.
Packaged acceptance passes two tests in1.529s/build0.32s without raw overrides;
all641 archived inputs are verified and recorded in the evidence.
All250 old fixtures remain unchanged. New raw86,099,991 bytes pack losslessly to
12,890,016 bytes; complete bytes, JSON, SHA and every blob are verified.

All panel source and SwiftPM jobs are terminal. Pending14 native catalog files,
three source files, six foreign files and all four frozen watcher manifests
remain unchanged. The own guarded catalog capture and its watcher continue;
they are never restarted for log silence. Next connect the panel to its actual
mode-screen continuation using live resource ownership, then its own initialized
menu chain. Whole own catalog/menu, full match, app input/animation/audio,
Windows/network/all-content and clean-Mac acceptance remain open.
