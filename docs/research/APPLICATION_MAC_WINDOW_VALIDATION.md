# Physical AppKit window through whole startup — all29 methods pass

Fresh compilation, package verification and all29 selected methods passed on the
unchanged2257-file candidate. Four new methods exercised real AppKit windows through
whole startup and the same Host;25 unchanged methods retained the original controlled
comparisons. This validates the declared native window service, not a complete
graphics backend, Windows fidelity or a playable NTSDApp.

[Finite validation plan](APPLICATION_MAC_WINDOW_VALIDATION_PLAN.md),
[implementation](APPLICATION_MAC_WINDOW.md). HEADae25280 is the input revision;
the actual candidate includes the previously preserved accumulated Native work.
No original game, emulator, capture or affected refused operation was executed.

## Actual macOS observations

The complete startup produced window token3, title `Little Fighter 2`, frame
794×582 points and client area794×550 points, backing scale2, visible state true
and window number4651. Those are actual AppKit property observations on this Mac,
not measured Windows values or a visual comparison. The window was explicitly
closed by the test. Original-driven client dimensions match the published state.

All11 physical window requests are retained in their complete committed operation
order: four separately sampled metrics, icon/cursor/class, one create, update and
two show calls. The produced window identity reaches later request bindings and
the published HWND. Remaining API/device responses are declared controls; no
DirectDraw/surface/clipper/pixel success is supplied by the physical window service.

The geometry method creates a second distinct window and verifies the160×120
increase in both frame and client sizes, ownership and preparation rejection,
unsupported requests and explicit destruction. The permit method rejects foreign,
duplicate and cancelled physical starts before any backend operation, while retaining
late answer/failure behavior. The late-publication method keeps the first window
visible after Native rollback, retries without a second create and retains its
lease through the returned context after coordinator death and explicit close.
Automatic visible-window closure on final lease release remains a separate gap.

The native points/decorations/center mapping stays an explicit host policy. These
tests do not prove Windows DPI, placement, class/menu behavior, callbacks, focus,
fullscreen, rendered pixels, input/audio, visual fidelity or clean-Mac behavior.
NTSDApp still launches Practice; candidate promotion and integration remain open.

## Build, comparisons and preservation

Preparation64247 completed with2257 regular distinct-inode clone files. Fresh
build64879 completed in295.567 seconds, sampled peak tree RSS6,606,323,712 bytes,
with no guard, signal or residual process. Existing compiler warnings remain in
the log; no warning-free build is claimed. Package verification checked199Core,
61Reference,2MacPlatform and278test sources, generated accessors and fresh outputs.
All1686 resources (385fixtures/1301runtime files) match their declared bytes.
The65,277,224-byte test binary links AppKit and includes the new module.

Queue75740 completed all29 methods in161.211 seconds, terminal0 and absent.
Every result has the exact method pass, three one-test/zero-failure summaries,
Selected-tests pass and no guard/signal/residual. All25 previous bodies and limits
remain unchanged. Whole35 cases still distinguish23 complete comparisons,5 source
stops and7 provenance rejections, with650 once-served window responses. Retained
window/WinMain/Host comparisons, rollback and lifetime controls all passed.

Monitor identity/reap code and saved-result predicate are unchanged; only the phase
domain and new target/source membership were extended. An observation-summary
reader initially expected a path inside phase-relative pins and raised `KeyError:
'path'`. The exact error and corrected path resolution are preserved. No Native
method, assertion, expected value or comparator was changed or rerun for that error.

Author review is recorded but is not independent review. Original expected bytes,
masks, root1034, prior2254, candidate2257 and source55 pins remain protected.
Artifacts live in build/research/application-mac-window-validation-20260926 on X5;
the final publication separately records full regular-file/PAX archive verification.
Source59727 remains terminal at34 Objects, not a complete137-Object acceptance.
Existing safety incidents remain open. EXE envelope was not recalculated.

## Next concrete dependency

NEXT: native display/surface acquisition and startup clear for the remaining window
requests, consumed by this same exchange and actual window identity. Use the
existing graphics-owner and window-initialization contracts for descriptor masks,
aliases, releases and caller order; establish host format/backing provenance before
implementing conversion. Require complete startup and late rollback with physical
resources, retaining original comparisons and every unsupported boundary. This
removes the next physical drawing dependency toward the first full match; it must
produce owned resources and observable output rather than another inventory.

Windows format/palette/text/clipper behavior, synchronous callbacks/fullscreen,
other providers and aggregate audio, initialized loading, independent review/root
promotion, Windows/visual/device/clean-Mac/full match and full game remain open.

## Verified closure

Finalizer81466 terminal0/absent. Full4932-file artifact and
147-member PAX metadata archives verified by bodies/modes/nsmtimes/
membership, with distinct regular clone inodes. Root/prior/candidate/source pins
remain exact. Candidate manifest SHA256 `f1bdd040998f5d4254277c31d92474206c9d43dccb05ae536643d090279bf92f`;
test binary SHA256 `3a6135f1544cd083cf281bb1daba4407c31e7f96ed26fb4a3f0fb7d440a959ad`.
[Publication](../evidence/application-mac-window-validation.json),
[closure](../evidence/application-mac-window-validation-close.json). Task frozen;
comparison/archives passed, independent review and the listed integration/device
gates remain open.
