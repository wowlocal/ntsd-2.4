# Host gameplay candidate — bounded implementation plan

2026-09-22. Follow [WORKFLOW](WORKFLOW.md), [TASK_TEMPLATE](TASK_TEMPLATE.md),
[CURRENT_WORK](CURRENT_WORK.md) and the
[gameplay preflight](APPLICATION_HOST_GAMEPLAY_PREFLIGHT.md). Base HEAD
`3e4f0d9a485568f55307d4d129aa01b297db682f`. Previous turn made progress: the
preflight contract, six saved-envelope inventories and exact method selection
were verified, committed and pushed. This phase implements the missing actual
host input/body retention toward the first Naruto/Sasuke District match and the
complete native game. It does not redefine that goal as a test suite.

## Inputs and behavior

Clone all 1039 actual files from the frozen application-host-match-correction1-
20260922 candidate, with the subsequent 53-method completion publication. Root
1034 remains pinned by live source59727. Preserve all prior candidate, compiler,
RSS and metadata-overrun evidence. Pin current manifests, preflight files, source55,
archive and actual dirty root inputs; HEAD alone is insufficient.

Only the original distribution is the behavioral reference: EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Reuse the pinned preflight's source readers, neutral17/active48/paused14 schedules,
installed-library comparisons and provider contracts. Their synthetic mappings,
API responses, keyboard writes, CW/ABI and unknown-storage boundaries remain
explicit; they are not Windows/device observations or an installed whole-game
trajectory. No new reference decoding, original execution or source capture.

Implement a common host LoadedOutcome with returned menu, Start and gameplay
Ready. Existing menu APIs adapt to it. One actual acquisition selects the branch.
Retain the current Ready.loading ticket (owner/revision/attempt), all Ready
owners/journals/resources and an independent platform copy. Resume only this
private Ready through existing GameplaySession normal/paused bodies; retain its
returned child/platform before the existing final outer tail publishes once.
Failures retry input, body or tail at the appropriate retained stage without
repeating completed work. Preserve class-copy, alias, installed-library, wrong-
stage, foreign/sibling/stale/consumed ticket, reentry and prior-batch contracts.
Core gameplay rules, numeric operation order and child implementations do not
change. No expected after-state, unknown backing or source ABI enters Core.

## Finite implementation and comparisons

Exactly 17 candidate Swift paths may change: Core OriginalApplicationHostSession;
Tests OriginalApplicationLoadedTestDriver, OriginalApplicationGameplayProjectionTests,
OriginalApplicationActiveGameplayTests, OriginalApplicationPausedGameplayTests,
the eleven Active{Body,Physics,Contacts,Graphics,Lifecycle,Commands,HUD,Notices,
Recording,Layout,Output}Tests wrappers, and new OriginalApplicationHostGameplayTests.
All names have .swift suffix in their existing target directories.

Thread an explicit optional driver through complete existing sequence/comparison
bodies, preserving Bootstrap defaults. Actual host chains begin with existing
loading/character/selection/launch parents, use current bindings and Host.step
acquisition, retain all17/48/14 returns and whole-state/effect/journal comparisons.
Do not seed a host from a Bootstrap snapshot or weaken any expected/mask/assertion.
Transport-only changes may route final failures through the actual host; keep
the same failure points and independent expected calculations. Freeze all63
existing +6 proposed method names from the preflight observations (69 total).
Required new controls cover preparation/provider/local/replay/round/observer,
normal and paused body/graphics/sound/dispatcher/commit, final time/Sleep/commit,
stage-specific retries, exact tickets, reentry, platform-copy and earlier batches.
The six proposed methods and complete scope are enumerated in the preflight.

Independent review is unavailable; author inspection cannot close that gate.
This implementation phase ends at syntax, method inventory, preserved-comparison
inspection, patch round-trip and archive readback. Fresh compilation, all69 Native
methods, package byte verification and build archives require their own next
bounded validation plan. No old binary pass validates changed Core. Retain prior53
method limits and declare new limits before execution, informed by whole-parent
RSS/time evidence. Do not run XCTest in this phase.

## Storage, jobs and mutation ownership

Task `/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-gameplay-candidate-20260922`,
alias `build/research/application-host-gameplay-candidate-20260922`.
Verify APFS UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`, at least61GiB external/
7GiB internal before dependent IO. Original40GiB/6GiB reserves and source17GiB
gross commitment remain. Allow4GiB observed decrease including concurrent source,
stop implementation at3.5GiB and retain0.5GiB for closure. Use APFS clones with
distinct regular inodes; no build/cache copy. No deleting evidence or lowering
reserves. Maximum7200s implementation phase; syntax process300s, 4GiB RSS.

Reuse preparation/pinning/job/parse/patch/archive procedures. Root author owns
this plan, APPLICATION_HOST_GAMEPLAY_CANDIDATE.md, matching
docs/evidence/application-host-gameplay-candidate* outputs, preparation and
finalization helpers, and own CURRENT_WORK/RESEARCH_MAP insertions. Candidate17
paths above are the only code mutations. Root Native, source55, fixtures/resources,
expected/masks, old plans/results and unrelated pending files remain unchanged.
Commands: `python3 tools/prepare_application_host_gameplay_candidate.py`;
record the exact installed `swiftc -frontend -parse` command/inputs/minimal env
in the task parse config and job; then
`python3 tools/finalize_application_host_gameplay_candidate.py --task <task>`.
No compile/link/runtime/device IO, installs, provider changes or promotion.

Revalidate source PID/start/full command/cwd/job before action; never restart or
signal it for silence. Record owned preparation/parse/finalizer identities and
terminal outcomes. Preserve a failed draft and diagnostic before correction;
three rounds before contract diagnosis. Refusal handling follows WORKFLOW:
record exact available error/IDs/time/model, stop affected retries and retain
the dependency. Existing round-result/replay-writer, Actor+7b4, War resource-fault
and Windows ISO incidents remain open; unknown triggers stay unknown. This phase
does not run original or Native result-file provider operations or clear incidents.

## Increment acceptance

Verify syntax only, all69 definitions, all unchanged file bodies/modes/membership,
protected inputs, complete retained comparison statements, reviewable patch
round-trip and changed-file/metadata archive readback. Archive failure evidence
as well as success. Update study/navigation and commit the coherent checked
increment with hooks intact before another independent task. Keep uncompiled/
untested status explicit. Independent review, source catalog completion, root
promotion, installed whole-application equivalence, native app/window/input/audio,
Windows/devices, first complete match and full game remain open.
