# Tournament preparation with the installed library

The controlled continuation now implements434349..4347c5 and rejoins the real
432ab0ret12/outer422ab8ret4. The finite corpus has98 returns:77 retained menu
calls and21 whole preparations. The fresh human chain keeps its own constructor
outputs through78 menu calls;20 additional declared confirmation entries cover
17 ordinary arenas, special99, both random-arena outcomes, human/human and
human/computer assignments, round0/2/4/default names, retained HP/state and network
flags. These are controlled preparation calls, not played fights or a complete
initialized library catalog. The preceding [bracket corpus](LIB_TOURNAMENT_BRACKET.md)
remains immutable and separate.

[The finite plan](LIB_TOURNAMENT_PREPARATION_PLAN.md) declares the original pinned
NTSD EXE/lib.dll/VC80, DAT/BMP/DIB and accepted loaded-catalog dependency. Original
instructions execute in Unicorn2.1.4 at CW023f, with controlled allocator,
GetLocalTime and COM/GDI responses. Memory/read/store/mask/FPU/stack/helper traces
recover operand provenance, lifetime, order and actual returns. This is neither
Windows/device behavior nor runtime use of the original binaries.

## Constructor and replay provenance

The old controlled bracket entries leave most Actor fields unknown. Recording
reads18 Actors whether active or inactive, so importing those bytes would invent
native initialization. Every new parent instead executes419e40 and all400
4061d0 constructors before its first menu call. Native independently constructs
the same declared A5/ramp allocations, retaining the known World/Object owners.
All8421 constructor results and their masks compare. No caller stack is imported.

The immutable loaded-catalog fixture supplies the controlled source's101 BG
records and137 Object+90 operands. Its file SHA is
`5fcd6e364a7761fcad4dc5af9792acf18e20b7bd9ff6ea89a66955c48a2af05e`.
Native rebuilds the entire137-object catalog through its normal raw-resource
loader. Only established bitmap/Object/World references are rebound. Names,
configuration, RNG and replay metadata have explicit raw-file, constructor or
controlled-input provenance. The fresh menu's enabled Start music remains live.

An initial98-call capture exposed the still-unknown first byte of44eed0 in the20
controlled confirmation entries. Native recording and the independent read audit
both rejected43d974's read. Read-only examination established the original PE's
52 zero-initialized bytes at44eed0..44ef03. A fresh98-call capture declares that
producer before execution. Both raw corpora remain intact. All2,788,586 compared
records retain identical actual bytes; events, instructions, writes, helper
returns, input values and outputs are unchanged. Only127,686 repeated mask bits
gain the recovered PE provenance. This is not acceptance of unknown storage.

## Recovered order

CPU candidates inspect the first eight activity flags, then bind slot10+seat and
4512f8+seat*4. The scan is preserved literally. Preparation changes menu to0,
gets local time, runs actual VC80 date formatting, and appends `_1on1_` with
`Prelminar`, `SemiFinal`, `Final`, or no round suffix. The spelling `Prelminar`
is original. Year padding uses spaces; other widths are minimum zero padding.

Random arena uses FD with count−2 and converts count−3 to99. Active first20
Actors are reconstructed, retaining Object ownership and copying Object+90.
MP is500; all three HP fields come from the bracket's retained HP;33c retains
the bracket state; team is participant+10. FE selects X and FF selects Z with
the original signed division and wrapping order. Inactive first20 Actors keep
their earlier constructor state. Later inactive slots20..399 are reconstructed.

After clearing the ten command words in their original order, preparation calls
all17 arena release helpers and loads the selected ordinary arena through actual
bitmap/CRT/platform helpers. It clears450bbc, sets44d034, resets input, then
executes whole43d2c0. It does not add the VS caller's450bdc reset. The native
recording API builds all0x630e18 bytes;21 buffers total136,325,112 bytes. Both
playback and recording ownership remain explicit; the old recording is freed
before calloc, and playback is retained.

At4347ba twelve cdecl argument bytes are still pending. Its `[esp+1c]` reads
rootSP10, the menu pointer. The source stack/read audit confirms this value;
it is not the arena index or a pointer fault. Both ret12 and ret4 execute.

## Comparison and limits

The independent audit reconstructs2409 complete points,2,578,709 records and
7,872,738,990 bytes/masks from2,460,998 stores. It checks425,430 instruction reads,
3622 API reads,22,508 helper returns,147 preparation/FPU points and all152,703
recording reads with known provenance. FD executes2 times; FE and FF execute42
times each. All2981 blobs and every completed atomic source part verify.
The4084 instruction starts include constructor parents and menu/preparation
children; this is not every possible branch outcome or a CPU benchmark.

The corpus starts with ordinary arena layers unloaded. Its release calls thus
exercise the zero-sentinel path. An already loaded arena requires the supplied
release/free continuation; Native rejects its absence and rolls back, rather
than silently omitting platform requests. Dynamic comparison of nonempty arena
release in this whole Tournament caller remains open. Callback/device ownership
is separate from the already accepted storage-level40c0e0 helper contract.

Raw98-case comparison and four late failures passed on Native6. Native7 passed
three tests in79.407s after a245.86s build; packaged three tests passed72.734s
without a raw override. All687 Native package/archive files verify;275 prior
fixtures remain unchanged,278 total. Three new transport files contain92,803,102
bytes and restore988,830,043 original raw bytes with identical JSON and SHA.
Native game compression remains unchanged; transport only packs research data. The failures
cover a bitmap operation, replay allocation, completed recording and the outer
return observer. Final acceptance also verifies arena/bitmap ownership rollback
and rejects an unconnected live arena release. Package results and immutable
artifact pins are recorded in [the evidence](../evidence/lib-tournament-preparation.json)
and `build/research/lib-tournament-preparation/lib-tournament-preparation-work.json`.

Earlier diagnostics are retained: a constructor stop sentinel suppressed the
terminal observer in probe1; native1 referenced an unaccepted pending API;
native3 needed two explicit `self` references in the comparator. Native4 and
audit1 rejected the unknown music operand described above. Native5/audit2 were
started before final source publication and failed to open that file; only after
the source job became terminal were the dependent checks restarted. None was
a source memory fault, safety refusal, changed expected output or successful
whole-preparation comparison.

The current work does not prove a played match, full library startup, window
integration, input/audio latency, actual Windows behavior or clean-Mac operation.
Team Tournament, War, the initialized catalog/application join and those runtime
checks remain required for the full native-game goal.
