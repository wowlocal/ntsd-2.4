# Enabled menu panel drawing, timer and mouse behavior

Recover whole423b00, including the cold tail4242b3..4242d7 jumping back before
the shared4242b2 return, the enabled bitmap panel called by the mode screen
and other menu bodies. Its bitmap loading/content/update dependencies already
have separate native studies; its display/timer/mouse body remains open after
LIB_MODE_SCREEN. The previous goal turn made progress: accepted8d2cc05 adds the
whole controlled library mode screen, immutable250th fixture and passing native
comparisons. The own full installed-library catalog remains live and untouched.

Reference is the pinned NTSD EXE and bundled lib.dll, with VC80 only if an actual
child requires it. First inspect the exact original body and dependencies with
Capstone5.0.7. Then execute valid controlled inputs in Unicorn2.1.4, retaining
the actual installer when a patched helper is reached. No Windows/device claim
follows from disassembly or emulation. All executable execution is research
tooling, absent from the native shipping runtime.

Instruction/store/read/stack/register traces recover timer wrapping, selection,
bitmap request order, live global/mouse aliases and the source ownership needed
for a reusable native panel. Bitmap records and strings must have declared
provenance, including the ordinary missing/null-surface gates. No arbitrary
pointer corruption, private expected-state import or protection modification
outside the established research loader is introduced. Shell/COM/timer/sound
responses are controlled observations, never host URL or device actions.

Finite acceptance will follow the static branch inventory: all ordinary panel
gates, each distinct panel state, timer threshold neighbors and wrap, every
rectangle/hitbox edge, held/previous mouse conditions, and ordered click/release
effects. Use two backing/address controls and retained calls for live state.
Only valid owned atlas/header/string inputs are supplied. Keep actual unknown
or unavailable operands as explicit boundaries and preserve original errors;
do not continue faults or alter expected bytes. Publish the exact finite case
list before capture, preserving each completed case atomically.

The native implementation must share the existing bitmap/sound helpers, stage
globals and caller mouse state, and buffer external effects until success.
Compare full bytes/masks and ordered events, actual complete helper/parent
returns, and late rollback. Retain existing panel bitmap/update/startup and
library mode-screen comparisons. Then connect this full panel to its real menu
caller; a standalone panel does not itself complete that enclosing call.

Build from accepted8d2cc05 plus owned changes in isolation. Keep250 fixtures,
14 pending native catalog files,3 pending source files,6 foreign files and all
frozen watcher inputs unchanged. Source11636 and watcher15664 must be revalidated
before action and never restarted for silence. Full own initialization/menu,
app/window/input latency/audio/Windows/full-match/all-content/network/clean-Mac
goal remains active beyond this dependency.

The initial contiguous disassembly stopped at the first ret and exposed the
direct424214->4242b3 branch. The extra41 bytes contain blink selection and jump
back to42422b; retain them in the whole function. The adjacent4242e0 timer-loop
function is separate. Static analysis finds unsigned timeGetTime modulo a
positive maximum end time, signed interval/duration/animation comparisons,
eight rotating links,24 rectangular links (rows18..23 ignore the bottom edge),
navigation arrows and a60-step update-notice blink. No GDI text call occurs;
this panel's children are bitmap, fill, confirmation sound, Sleep and Shell.

The content parser accepts numeric zero and `?` links; only -99 is rejected.
Therefore an enabled controlled panel can lack a positive timer range without
control-pointer damage. Record that dependency before423be8's zero division,
and a full navigation scan with no eligible row as a nontermination boundary;
do not deliberately execute the divide fault or continue an endless loop.
These are explicit native rollback rejections, never successful full returns.
The pinned distribution lacks ad0/ad1 content/bitmap, so these successful-data
controls do not claim that its ordinary missing-file startup enables the panel.

The finite manifest `build/research/menu-panel-draw-finite-inputs.json` now has
1002 calls (501 per direct/A5 and reverse/ramp control). It enumerates25 initial
timer divisions with positive maxima,96 row/animation controls,36 main-link
hover/click controls,72 arrow controls, two previous-word aliases,144 individual
banner edges, five minimum-coordinate controls, six duration boundaries,
40 notice-blink/hover controls, four version gates, overlapping banners,
independent bitmap/fill targets, two explicit unavailable-result boundaries,
three ordinary entry gates and64 retained calls. No branch completion claim is
made from that inventory alone; dynamic PCs and helper returns remain separate.
The panel uses a declared794x550 atlas with all11 recovered panel rectangles;
the two auxiliary bitmap inputs retain the valid deterministic guide rectangles
from LIB_MODE_SCREEN. Their actual resource-loader composition remains later.

Before the finite capture, preserve the successful first nominal probe and its
producer. Make the second backing control meaningful for the panel itself:
reverse its panel/guide bitmap addresses, initialize13 rectangle slots per atlas, overwrite the11 panel rectangles/count,
and leave unused fields A5/ramp with false masks. All consumed atlas
fields remain defined. The original nominal probe is not relabelled as having
these revised entry inputs; a fresh nominal probe precedes the finite capture.

The first1002 captures are terminal and immutable. Actual coverage leaves14
ordinary instructions plus two skipped alignment instructions. Add exactly five
cases per backing control: all eight increasing active end times; hovered main
link after its duration; hovered left/right arrows after duration; forward wrap
from selected row7. This closes the concrete missing maximum/hold/wrap paths,
not an open-ended numeric expansion. Retain every original1002 case and append
these10 in a separate extension; final finite domain is1012 calls.
