# Library menu continuation through the actual match return

OriginalModeMenuContinuation stages the selected mode screen, whole enabled
panel and subsequent output in one native operation. OriginalMenuReturn now
shares its pristine/library paths, including network text, overlay, volume and
presentation. The library route retains its text DC through every caller.
Globals, World, bitmap/replay ownership, music/resource owners, local strings
and the caller's event buffer commit only after the final observer succeeds.

All54 controlled returns through actual422ab8/ret4 and two playback
continuations match. Four panel boundaries verify explicit native rollback;
they are not full successful matches. The [finite plan](LIB_MENU_CONTINUATION_PLAN.md)
declares the scope before capture. This extends the accepted
[whole panel screen](MODE_PANEL_SCREEN.md), [library screen](LIB_MODE_SCREEN.md)
and pristine [menu return](MENU_RETURN.md), preserving their original corpora.

## Reference and entry boundary

Pinned NTSD EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
lib.dll SHA256
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`
and VC80 CRT SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
execute under Unicorn2.1.4. The accepted library installer executes with its13
patches, relocations and4000/20000 allocations. Its VirtualProtect/copy results
are controlled loader responses, not Windows memory-protection measurements.

The actual41bc90 prologue runs fromSP1000f000 and creates its normal aligned
frame, cookies and SEH chain. At41bcd0/SP1000e9bc the harness declares EBX as
World22000020 and continues at4229cc. The intervening phase/input/gameplay body
is skipped explicitly; this is not an initialized whole tick. Target26006000
is a declared caller input at retainedSP+68, established before the prologue.
No private cookie, return address or unwind state is synthesized to force a
return. Initial FS-chain word12345678 is the declared research chain boundary.

The continuation executes actual429730, the cached resource gate,431d10 and
423b00, then screen ret16, menu42e0f9/ret12, network notice,4028a0/402810 overlay,
401f30 volume,43e940 presentation and422ab8/ret4. The ordinary return is
30000000/SP1000f008. The harness asserts restored saved registers, original FS
chain, balanced helper frames andCW023f. It stops before the outer424746 caller;
its later held clear and early return are not exercised here. Playback stops
before43249c/SP1000d7dc with menu and screen still pending.

The initial static inventory ends its common-epilogue range before the complete
three-byte42e0f9 instruction. It is retained as a partial inventory. The fresh
dynamic corpus records and independently verifies both actual ret instructions;
no complete static coverage claim is made.

COM/GDI/query-volume/timer/Sleep/Shell/free/quit/PTD are declared adapters.
Actual CRT formatting instructions run using the established C-locale/PTD
attachment: the pinned image and actual _initptd result are copied from a
separate controlled CRT CPU, with imports rebound to research responses.
Full CRT startup, host APIs, audio devices and Windows execution remain separate.
No EXE, DLL or emulator enters the native shipping runtime.

## Controlled inputs and ordered behavior

Both backing controls retain nine Actor records, eight World seat references,
five initialized500-frame bitmap records, a sparse794x550/count11 panel atlas,
and two replay allocations. The six atlas inputs are independently reconstructed
from declarations by native tests and the source verifier. Initial globals and
opaque controlled Actor bytes are input snapshots; expected after-state and
private source stack never supply native operands. Local strings start as1,796
zero/undefined bytes. Their defined bytes and masks match at the actual screen
return, before later overlay formatting reuses that source stack lifetime.
The92 undefined fill-padding bytes remain separate source/native backing.

Current/previous menu10 and load flag0 exercise the ordinary cached gate. No
music or resource child executes in this new corpus. Native uses the accepted
[whole startup transaction](CHARACTER_MENU_MUSIC_SURFACE.md); joining freshly
loaded resources to this entire output caller remains an own initialized
composition dependency. Other character-menu bodies and the menu0 timer-reset
branch retain their existing studies; this corpus does not newly cover them.
Native explicitly rejects an unavailable selected menu body.

The predeclared60 cases are30 per backing control: nominal; six signed network
countdown/byte gates and a negative GetDC network path; eight overlay type,
expiry and blocking combinations; three volume edge/input cases; failed query
and failed volume-read responses; three additional presentation modes; mode
confirmation/background release; panel click;28 help labels; playback; zero
timer range; and no selectable navigation row. Existing standalone studies
retain their wider branch coverage.

Network text now follows the installed library's transparent-background/DC
rules and only decrements the countdown when both signed gates pass. Overlay
formatting and volume methods precede presentation. Failed query/read/SetVolume
and presentation HRESULTs preserve the source's continuation rules. Panel and
screen input effects remain live for following callers; confirmation releases
the background before the panel. Native drawing receives the current staged
resource ownership. The old pristine menu-return API uses the same caller order
and retains its original early-return behavior and comparison corpus.

## Audit and native comparison

The independent read-only verifier reconstructs all60 atomic parts and the
nominal probe without editing any expected byte. It checks60,558 stores/
222,702 bytes,221,140 instruction reads/379,904 bytes and606 API reads/
12,974 bytes. It verifies all1,500 final regions/82,287,600 bytes plus masks,
and276 intermediate points containing6,900 regions/378,522,960 bytes plus
masks. The250 content-addressed blobs contain13,247,520 decoded bytes.
All2,330 observed helper returns preserve the declared ABI. Actual instruction
bytes match1,563 EXE,51 library and407 CRT starts. These are observed starts,
not a claim of every branch or complete CPU/Windows behavior.

The only false-mask instruction reads are570 reads of pinned EXE cookie44eea4.
Its original bytes are independently checked; the masks describe global write
coverage. No observed unknown stack/atlas operand is imported. Unread/private
storage remains unknown. Full source CRT/stack/SEH reconstruction is separate
from native semantic game-state comparison.

The56 native-matched continuations retain8,098 ordered events and2,258 helper
returns, with1,509 EXE+51 library+407 CRT starts. They include668 Blts,
254 text requests/246 complete GDI sequences,22 actual CRT formats,10 audio
queries/eight volume reads,54 whole panels,56 key labels, six sound requests/
18 sound methods, two frees and two each panel timer/Sleep/Shell requests.
Native compares complete globals, all owned allocation bytes/masks/liveness,
read-only World/Actors, screen local storage and retained library DC, including
screen/menu/pre-return checkpoints. Cached music/resource owners remain empty.

Two cases stop before zero DIV423be8. Two stop before the ninth ineligible
navigation scan423ed0, after eight attempts. Native throws the existing panel
errors and rolls back the entire menu continuation. These are explicit
rejections, not executed divide faults or completed endless loops.
Five separate native-only failures occur after screen/background release,
network text, the fifth volume method, presentation and the final pre-return
checkpoint. Every state owner and caller event buffer retains its entry value.

## Acceptance and remaining work

The isolated package derives from accepted4147a33 plus three owned native
files;642 accepted files remain unchanged. Raw acceptance passes11 release
tests in52.626s/build227.45s: two new tests in0.771s and nine retained menu
return/cycle/library-loading/panel-screen/music tests. NTSDNative links.
Packaged acceptance passes the two new tests in0.644s/build0.33s without raw
overrides. Existing unrelated compiler warnings remain; source capture,
independent audit and native comparison need no expected or game-rule correction.
All source and SwiftPM jobs for this study are terminal.

Raw34,301,378 bytes pack losslessly to3,693,452 bytes; full bytes/JSON/SHA and
all250 blobs verify. The253rd fixture preserves all252 old fixture pins.
All646 packaged files verify against a reconstructible composition of the
unchanged pinned base archive, three-file native overlay and one-file fixture
overlay. Reusing the immutable base archive avoids a redundant925MB copy while
retaining all tested bytes. [Evidence](../evidence/lib-menu-continuation.json)
records exact jobs, hashes and boundaries.

Pending/foreign files and all four frozen watcher inputs remain unchanged.
The guarded full-catalog capture and its watcher are independent live work,
never restarted for silence.4242e0 is already accepted
[loading progress](LIB_LOADING.md); the [application timer](APPLICATION_TIMER.md)
and message-loop helpers also have existing controlled contracts. The remaining
work is their own initialized composition and app integration, not redoing these
helpers. Full catalog/menu, application window/input/animation/audio, Windows,
full Naruto/Sasuke District match, all original content/network and clean-Mac
acceptance remain open. This milestone exercises no app window or device.
