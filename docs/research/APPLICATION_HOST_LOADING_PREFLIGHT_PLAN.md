# Host loading continuation preflight — frozen plan, 2026-09-22

Follow [WORKFLOW](WORKFLOW.md) and the completed
[host validation](APPLICATION_HOST_TRANSACTION_VALIDATION.md). The game question
is how the production host can retain its actual loading entry, run existing
child owners, and commit the enclosing menu iteration once with staged platform
state. This removes the host driver's current loading stop on the path to an
interactive first match; it does not close the full game or actual device gates.

Base HEAD `12d80fcb4da9eb68088e7be97aa61113519474c5`. Read the actual validated
1036-file host candidate, live-pinned root1034 baseline, relevant Core/tests,
loaded-menu/cycle/bootstrap/input/loading studies and direct dependencies. Pin
all consulted inputs before deriving the final contract. Consult the immutable
instruction archive for owners, masks, numeric and process constraints.
Reference EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
DLL/CRT and original resources remain those in the host validation plan.

This is read-only code and saved-evidence analysis. No original execution,
historical producer/auditor rerun, Native build/test, device IO, dependency install
or refused operation. Do not edit closed candidates, expected data, masks, live
producer pins or root Native. The running Catalog53 source is independent;
revalidate PID/start/full command/cwd/job before any process action, and never
restart or signal it for silence. Existing safety incidents remain open.

Finite questions and acceptance outputs:

1. Trace the exact current PendingLoading and platform from HostSession.step to
   first loading/input/menu children and Bootstrap.finishLoadedMenu; separately
   trace repeated loaded cycles and their retained owners.
2. Identify the final timer/sleep, alias, revision, owner and journal publication
   contract, including rollback and ticket rejection. Distinguish Core guarantees
   from host assumptions, and inspect the saved tests that establish each claim.
3. Inspect at most three relevant saved fixture envelopes as data, checking full
   declared payload hashes before selecting exact parent/return observations.
   No streaming trace or catalog replay; bound each expanded envelope to256MiB
   and cumulative retained decoded input to512MiB. Existing comparison results
   remain their historical evidence level, not a new source or Native match.
4. Produce one concrete next implementation boundary: host API/owners, finite
   successful and late-failure cases, whole-caller comparisons and prerequisites.
   Record any missing ticket, response acquisition or loaded-owner contract
   instead of inferring it from success fixtures. Do not broaden into another
   application readiness inventory or implement a device backend here.

Use existing manifests, receipts and plain file reads. Mutable root paths are
this plan, APPLICATION_HOST_LOADING_PREFLIGHT.md, the matching
docs/evidence/application-host-loading-preflight*.json receipts, and only the new
CURRENT_WORK/RESEARCH_MAP navigation paragraphs. Keep new metadata below512KiB;
no large files or new external task are needed. Any later build has its own X5
mount/reserve/budget check. Preserve original40GiB external/6GiB internal reserves.

Root author performs analysis; independent contract review is unavailable and
stays open. Verify consulted pins, root1034/source55 preservation, JSON/local
links, bounded output size and exact staged scope. Commit this coherent checked
preflight before implementation, with existing hooks enabled and unrelated
pending work preserved. Analysis is complete only when the four finite outputs
above identify an actionable host continuation; no game/device acceptance claim.
