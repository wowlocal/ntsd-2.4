# Own application settings continuation

Continue the fresh BITMAP_SURFACE_LOADING WinMain/message-loop/dispatcher/front
resource parents from actual427089/SP1000ea74 through whole423480, its original
VC80 fscanf/fgets/feof children, return42708e and flag store427092 to42709b.
This recovers the initial menu's control/profile settings and their update order.
Use the pinned NTSD EXE, VC80 and original data/control.txt on Unicorn2.1.4;
keep the same CPU, stack, PTD, earlier CRT state and resource ownership. No
replacement CRT snapshot, scratch fill, expected child output or register reset.

Finite acceptance: all seven accepted own resource parents that reached settings,
then fresh default parents with raw control bytes, chunks1/7, trailing LF,
empty input, absent profile tail,99/198-byte info, backtick names, a missing file
and negative fclose. Each case is a fresh chain, never a forced retry after a
failed helper. Fopen/fclose and translated _read bytes remain declared platform
inputs; Windows opening/text translation and read-error continuations remain
open. All original resource/file bytes and prior240 fixture pins stay immutable.

Trace actual instruction bytes, helper entry/return/SP/nonvolatile registers,
all globals and scratch bytes/masks, ordered CRT and caller events, stack writes
and every caller scratch read. Establish the scratch lifetime from preceding
parent writes without importing its private values into Native. Native starts
its private scratch unknown, requires produced bytes before semantic string
reads, and compares every owned byte and mask plus complete globals/resources.
Full original private bytes remain evidence, even where not compared natively.

Compare ready returns separately from the original missing-FILE stop before
4234db/fscanf. Preserve source cookies and ordinary SEH; no control/protective
storage corruption, bypass, arbitrary fault continuation or external target.
Verify late failures at scan/gets/EOF/close/helper-return/flag-store and the next
screen boundary roll back the enclosing pending message-loop iteration while
retaining committed startup, callback, MSG, counter, baseline and ownership.

Run raw and packaged isolated native acceptance using committed7c48e9b plus
owned overlays. Independently verify full bytes/JSON/SHA/blobs/parents/PCs and
vendor pins; keep six foreign worktree files untouched. Actual CRT initialization
and NLS remain an earlier open dependency. This stage does not claim an original
Windows run, full World/dispatcher return, app window/device/latency, full match,
all content or clean-Mac completion. Next is the actual42709b screen consumer.
