# Fresh host transaction validation — frozen plan, 2026-09-22

Validate the [isolated candidate](APPLICATION_HOST_TRANSACTION_CANDIDATE.md) under
[WORKFLOW](WORKFLOW.md) and the current handoff. The product question is whether
the persistent application owner preserves the recovered whole startup/menu/input
contracts while staging host state and publishing committed batches once.
Actual host/device providers and the complete game remain separate open work.

Base HEAD `14cad6bd9a6b14e86a04bd39d9de64b7b3faf6e5`; actual input is the candidate's
1036-file manifest and its declared 25-method selection, not a HEAD-only checkout.
Original reference EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Saved controlled source evidence supplies all expectations; no original code,
Windows/device experiment, producer rerun or refused operation is executed here.

## Inputs, commands and finite cases

Verify the frozen candidate and its archive/publication, the old 1034 root inputs,
55 live producer pins, 385 fixtures and 102 runtime resources. APFS-clone candidate
regular files into a new task. Do not edit the closed candidate or root Native.
Use the installed Swift6.4 toolchain/SDK, Swift5 mode and macOS14 package target.
Run fresh release `swift-build --build-system native --build-tests --jobs 2`, with
task-owned scratch/cache/config/security paths and index store disabled. The
installed CLI documents this supported, deprecated build-system option. It avoids
relocating the previous swiftbuild cache; it does not alter recovered game rules.
Record exact compiler command, generated sources, outputs, version and logs.

Build Core and its dependent targets from the cloned sources. Verify new linked
test product, all declared resource bytes and actual bundle layout before running
tests; do not assume the old swiftbuild bundle layout. Run each of the 25 frozen
methods in its own XCTest process with a minimal environment and no raw-fixture
overrides. Verify actual exit and complete named method/suite result, not exit0
alone. Keep full parent state/mask/owner/event/RNG/operation/graphics comparisons.
The five new host methods cover 48 primary attempts, 17 late first-menu controls,
same-prepared-input final-hook retries, 50 input chains, six startup late controls,
copy independence, reentry, handoff and pending retention. Twenty old regressions
remain selected. No whole-catalog or gameplay test is added to inflate coverage.

Compilation errors/mismatches/guards are distinct results. Stop the dependent
queue on its first failure. Preserve the exact candidate, logs and failed product
before any correction; at most three identified correction rounds. A correction
requires a separate frozen candidate/manifest and declared recompilation, never
an edited expected value or a skipped mismatch. Do not restart a live/terminal
build for silence or missing observation.

## Process ownership and storage

Task directory:
`/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-transaction-validation-20260922`;
alias `build/research/application-host-transaction-validation-20260922`.
Verify APFS UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`, mounted volume and free space.
Use a 24GiB observed-decrease allowance for a fresh all-target build, regular
resource copies, potential retained failures and archive. The earlier 12GiB was
prospective, not an accepted measured build size. Current free space exceeds
170GB; start requires at least 40+17+24GiB external and 7GiB internal. Keep original
40GiB/6GiB reserves and the concurrent source's gross17GiB budget explicit.
Runtime stop at22GiB decrease; reserve2GiB for terminal/archive metadata and
account for concurrent source growth. Do not delete evidence or lower reserves.

Reuse the previous validation runner's monitoring logic and job/config format in
a separately identified task runner, replacing catalog-specific build gates with
this candidate's gates. Sample descendant-tree RSS (including separate PGIDs),
space and process state. Limits: build3600s, one method600s, total test selection
3600s, build tree12GiB RSS, individual test8GiB RSS, task logical150GiB (sparse CAS
is distinct from observed disk decrease). Poll at1s, RSS at2s, logical size at15s.
Minimal environment: HOME/USER/LOGNAME/LANG/LC_ALL/PATH/TMPDIR only. Do not invoke
XCTest help with the inherited environment.

Revalidate PID/start/full command/cwd/job before signals. Compiler children may
exec another installed compiler at the same PID/start/cwd; record that exact
transition only for declared toolchain executables. Do not adopt a reused PID or
unrelated process. Preserve the existing narrow `(xctest)` terminal observation
handling; it is not an exit status or signal authorization. Guard termination is
revalidated SIGTERM to owned live descendants and leader only; preserve overshoot,
missing observations and unconfirmed terminal status. Never signal source PID59727.
The source's historical HEAD is launch provenance, not a continuing HEAD lock.

## Publication and gates

Root mutable paths: this plan; APPLICATION_HOST_TRANSACTION_VALIDATION.md;
docs/evidence/application-host-transaction-validation*; task preparation/runner/
verification helpers derived from existing tools; own CURRENT_WORK/RESEARCH_MAP
hunks. Candidate/build/test outputs belong to the new X5 task. Existing source,
expected/masks/plans/producers, closed task trees and root Native remain protected.
Owner: root author. No independent reviewer is available; preserve that open gate.

Full source/candidate/resource identity, Native results, archive identity,
independent review and actual application/device acceptance are separate gates.
Verify archive bodies/modes/membership, job outcomes and absence before closure.
Publish failures without acceptance if the boundary is not passed. Commit each
coherent checked result with only its files/navigation hunks; existing hooks stay
enabled and unrelated pending work remains intact. This plan does not authorize
root promotion or claim a playable application. All safety incidents remain open.
