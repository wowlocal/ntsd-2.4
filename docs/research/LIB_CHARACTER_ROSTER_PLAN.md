# Installed-library character selection with a retained catalog

Continue [FRESH_CHARACTER_MENU](FRESH_CHARACTER_MENU.md) and the accepted
[roster operand provenance](CHARACTER_ROSTER_INPUT_PLAN.md). Recover selection
from Random to Naruto/Sasuke, live Actor Object bindings, wrap/Random behavior,
team choice, ready/countdown and ordered portrait/text/sound/output across
successive menu calls. The finite manifest has two independently initialized
sequences with declared A5 versus ramp/reversed Actor/menu-resource backing.

Reference: pinned NTSD EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
bundled lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
137 original data/data.txt entries, their DATs and42 portrait BMPs,11 embedded
menu DIBs. Controlled Unicorn2.1.4/CW023f executes the actual41bc90 prologue,
then declared4229cc menu tail through429730 and actual422ab8ret4 on each call.
Earlier tick/input acquisition and outer4246b0 are explicitly outside this
entry contract. COM/GDI/allocator/IO/time responses are research boundaries.

Create declared registry/Object/portrait entry views from raw original files,
using only the fields independently recovered by the preceding audit. These
views are not a returned own initialized library catalog. Pointer bindings are
declared opaque addresses; unknown fields retain their bytes and masks. Map
only disjoint controlled arenas after checking the emulator's live mappings.
Native must normally rebuild the accepted full catalog from its declared raw
inputs; never read source after-state or comparison-only addresses to create
its names, ID/type, bitmap storage or ownership.

The first call creates its own11menu bitmaps. Later calls preserve all prior
World/400Actor/globals/catalog/resources/DC, and only supply declared button
changes plus ordinary caller ABI words/registers. Preserve prior stack storage
and masks: do not clear it on the repeated calls or import it into Native.
Full read/store/stack/return traces establish that later operands come from
declared inputs or preceding writes, including live retained Object when the
selection indicator becomes Random. Normal cookie/SEH checks execute unchanged.
No malformed pointers, protection corruption, memory-fault continuation or
automatic retry of a refused operation is part of this study.

Finite acceptance: both sequences must finish every declared return and retain
Naruto17/Sasuke21, chosen teams and ready/countdown according to original
execution. Include press/hold/release, both roster directions, Random retaining
the previous Object, end-of-roster skipping noncharacters, and countdown jump.
Independently verify every source byte/mask/read/write/checkpoint/helper return,
exact instruction bytes and atomic prefix. Compare native full World/Actor/
globals on every body checkpoint, complete ordered events, all owned new/prior
bitmap records, name/portrait operands and retained DC between calls. A late
failure after a selected portrait and after present must roll back the entire
current call while retaining the previous successful call.

Preserve all255 old fixtures, frozen/pending/foreign files and the live full
catalog producer. Use isolated native inputs derived from accepted commit89636b4
and complete recursive archive composition. Do not repeat already-passing old
checks unless new code or evidence requires them. Source/native success is not
Windows pixels/audio/input-latency, an initialized library application, a full
match, all content/network or clean-Mac acceptance; those remain open.
