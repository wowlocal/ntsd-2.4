# Owned loaded menu and enclosing iteration

Status: Native loaded-menu and enclosing-iteration join verified; full card open. This continues the open
[loaded-return card](APPLICATION_LOADED_RETURN_PLAN.md) after the accepted
[current input join](APPLICATION_LOADED_INPUT.md), commit `c53d409`.

The game behavior is the first owned menu after loading: keep actual WinMain
music, construct eleven fresh menu bitmaps, enter the installed-library mode
screen and panel, return through loading, and finish the suspended outer
iteration once. The pinned NTSD EXE/resources and unchanged saved MENU_STARTUP,
FRESH_LIBRARY_MENU, MENU_RETURN and outer-loop fixtures supply the contracts.
This stage reads saved evidence and executes Native Swift only. It does not
execute original code, historical producers/auditors or refused operations.
Earlier source faults and three safety incidents remain open.

## Ownership and completion

`OriginalApplicationLoadedMenuSession` consumes the actual returned input child.
It retains current World/400 Actor/global records, catalog, interface, music,
resource registry, bitmap inputs and graphics. Shared menu startup, mode-screen,
panel, menu return and dispatcher implementations supply behavior. Expected
snapshots are used only by tests; they are never Native inputs.

The menu package contains eleven original DIB payloads, with a pinned manifest
and strict regular-file composition. Independent decoded source-color data
contains 582,632 known pixels across 1,388 rows from 1,290,322 DIB bytes. The
SwiftPM bundle and application asset packager include this package. The package
reader's temporary ordinary-app test checks delivery and owned snapshots;
this does not establish a real application launch or device raster output.

New bitmap allocations reserve complete logical ranges against current state,
Actors, catalog/file buffers, interface, all retained WAV buffers and music.
The eleven final records include the last SPARK patch. Source canonical surface
flags are translated to current surface tokens only when adopting records into
the live memory registry. API structures and colors come from owned DIB inputs.

PendingCatalog now retains its actual PendingLoading parent. The returned menu
therefore reaches the same suspended loop ticket. `finishLoadedMenu` checks the
originating Session UUID and revision before any outer operation, resumes only
the remaining timer/sleep tail, and updates counter aliases. State, loop and
value-owned environment install together after the final observer succeeds.
A foreign, stale or already consumed result is rejected. Copies of a Session
remain independent value owners; a host must not publish results from both.

The operation journal includes preceding loading/input work and appended menu
and outer operations. Graphics commands are another view of the same work,
not a second effect batch. Menu-return time is requested lazily only at its
actual timer branch. It does not reuse the panel/background clock.

## Comparisons and limits

Two own Native runs retain the accepted full catalog/pool/input parent and use
independent A5 and reversed/ramp bitmap allocations. For each backing, unchanged
saved MENU_STARTUP resources supply all fifteen checkpoints, 99 checkpoint
bitmap records and eleven final records, including SPARK's earlier unknown
height. Full records and masks are compared with the declared surface mapping.
Retained historical test suites continue their own source comparisons.

The appended graphics journal is projected independently from terminal API
operations and pinned pixel goldens. Commands, resource identities and final
graphics owners are compared. This projection covers declared success replies;
it is not general graphics-error or Windows-device acceptance.

The actual originating outer parent retains baseline 123456923, interval 33 and
counter 7 from saved case 47. The declared late Native reply 123457923 completes
without Sleep and advances counter to 8. Full final State matches the pending
menu except its counter alias. An earlier declared reply exercises Sleep(5)
and rollback. Nine menu failure points, outer time/sleep/final-observer failures,
retry, foreign/stale/repeated completion, allocation overlaps and NULL SPARK
rejection retain prior owners and journals. NULL rejection is not a source-fault
match.

The actual retained parent already has a zero current background pointer. Saved
application-menu-input case47 shows the prior Release/free and pointer clear;
the old wrapper survives as dead storage. The mode screen therefore also loads
MENU_BACK9 from declared clock12345: eleven menu constructors plus one background,
204 bitmap API requests per backing. Its new allocation, source colors and
whole graphics are compared. A separate explicit-zero test writes the same
already-zero input and adds no distinct original branch coverage.

Fresh backgrounds live in current memory and Snapshot.backgrounds;
State.earlyScreen remains historical. A later ordinary prefix consumer needs
an explicit join to current background owners. Latest music similarly lives in
the returned Snapshot rather than being written into historical startup data.

Resource checkpoint tests compare full bitmap records/masks and the selected
load flag, not complete source globals. Menu checkpoint callbacks are global-only;
the final result is a coherent full State. This does not prove
simultaneous mirrored state at every internal API callback. Other menu branches,
uncached music, playback, panel IO, recovery and gameplay require their own
ready dependencies. Unsupported requests remain explicit errors.

## Validation and continuation

All 43 bundled release methods passed on frozen candidate3 in 281.445s,
build 318.05s. NTSDNative linked. All 956 root/frozen Native files, 383 current
fixtures (381 old unchanged) and 471 packaged files passed byte/mode checks.
The complete Native archive was read back and verified. Failed candidates1/2 and their logs remain immutable. Candidate2 corrected the
new provider to return zero for the file-image attempt before its embedded-resource
reply. Candidate3 corrects the new allocation-count assertion and verifies that
the actual parent already has a zero background pointer, requiring a twelfth
bitmap. Independent saved API and Release/free/clear evidence establish both
test corrections. Core and source expected bytes never changed. The work package at `build/research/application-loaded-menu-native-20260913/` retains
plans/amendments, input pins, contract/code/test reviews and exact run metadata.
The data-only raw-DEFLATE wrapper inspection error is retained separately;
correcting the reader did not change any source bytes or expected results.

Next, connect the committed loaded result and its current music/background/menu
owners to the following ordinary iteration and application consumer. Whole
original application correspondence, backend publication, real window/input/
audio, first full match, all content/modes/networking and clean-Mac acceptance
remain open. This finite Native join does not complete the full goal.
