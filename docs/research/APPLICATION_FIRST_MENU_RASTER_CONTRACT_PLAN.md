# Whole first-menu raster contract and unresolved pixel inputs

2026-09-26, HEADcff421e. Previous goal turn was progress: Native window geometry
passed all76 methods and full archives. Those jobs are terminal0 and absent.
This is the bounded contract step explicitly named by CURRENT_WORK. Its immediate
consumer is physical first-menu rendering on the existing Mac window/display/
bitmap owners, toward Naruto/Sasuke District and the complete native game.

## Question, reference and allowed operation

Which exact first-menu requests can be rendered from owned known colors, and
which require missing original Windows/API/font/pixel observations? Inspect the
already accepted graphics reference (224 stages/110 composed chains), its40 source-
color lifetimes and existing DIB RGB/mask payload. Identify complete case0 command
order, all source rectangles/flags/color-key requests, known/unknown sampled source
pixels and installed-library text requirements. Inventory the complete finite
reference too, so case0 does not become a restriction on the common renderer.

Reference is the original NTSD EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c and bundled lib.dll
28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba. Use existing
APPLICATION_GRAPHICS_OWNERS, APPLICATION_SURFACE_COLORS, APPLICATION_DIB_PIXELS,
BITMAP_DRAWING, FRONT_SCREEN_BODY and LIB_RUNTIME evidence. Archive975–984,
1968–2025,3706–3725,3889–3916 remain binding. The first menu uses installed
SetBkMode1, not pristine SetBkColor; retain exact text bytes and lengths.

Operation: Python data-only reading, hashing and joining saved request identities,
resource generations, source masks and declared numeric replies. No original EXE/
DLL, emulator, historical producer/auditor, Native test, app or device executes.
No Windows behavior is inferred from control replies or Native resource success.
No expected pixels, masks, source backing or old result is changed. Existing
safety incidents stay open; this operation does not retry their affected paths.

## Finite output and acceptance

- Pin accepted publications and exact reference/fixture bytes; validate existing
  stage/chain cardinalities and command lengths before deriving new observations.
- Preserve all224 stage identities and110 chain memberships, full command order
  for the first menu, exact resource generations and requested color-key results.
  Report unique and composed counts separately; do not sum overlapping scopes.
- For bitmap Blt, report original signed rectangles, equal/unequal extents,
  mirroring flags, null source, source bounds and available source-color identity.
  Count sampled source mask bytes only for a positive in-bounds rectangle; an
  inverted/zero/out-of-bounds request is a separate API dependency, not filtered
  out or transformed into a valid rectangle. No raster or stretching is simulated.
- Independently cross-check rectangular mask totals by row slices and flattened
  coordinate traversal; verify every consumed RGB/mask payload range/hash and
  retain source values even where mask=false. Unknown bytes never become known0.
- For first-menu text retain exact raw strings/lengths/coordinates/colors,
  SetBkMode1 and acquisition/release results. Identify missing font/glyph/charset/
  codepage/DPI/default-DC inputs explicitly; do not choose a macOS substitute.
- Preserve historical zero destination rectangles and NULL source rectangles.
  Cite actual Native WM_MOVE geometry separately; do not rewrite reference rows.
- Produce a concrete common-renderer dependency table and next implementation or
  necessary reference-observation task. This is contract evidence, not a first
  rendered frame or full-game acceptance. Separate reviewer unavailable; author
  consistency checks do not close independent review.

Allowed new files: this plan, APPLICATION_FIRST_MENU_RASTER_CONTRACT.md,
application-first-menu-raster-contract evidence JSON, own inspect/finalize tools,
and own navigation blocks. Native implementation/tests/resources remain unchanged.
Use existing manifests, hashing, job and PAX archive conventions, no execution
framework. At most3 tool-correction rounds, preserving nonpass outputs/producers.

Task application-first-menu-raster-contract-20260926 on X5 under
01a0dc49-738f-7972-8fb0-e98fb2f34408. Verify mounted writable APFS UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548, keep40/6GiB reserves plus17GiB source
commitment. No new build; task physical256MiB/logical384MiB, report16MiB/root1MiB,
execution600s/finalization600s. Reuse accepted input bodies by APFS clones where
needed for archive preservation; full names/bytes/modes/nsmtimes verified.
Protect root1034/prior2257/baseline2272/source55 and immutable AGENTS archive.
Commit this checked contract increment before beginning an independent build.
