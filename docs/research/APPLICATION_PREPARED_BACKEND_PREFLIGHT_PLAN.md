# Prepared backend — bounded dependency preflight

2026-09-26; base HEADd2b19dd. Retained-context validation continues in its
unchanged queue5193:40/82 saved results checked at the last checkpoint. Its
candidate/build/queue are immutable and must not be duplicated. This independent
static preflight uses the compiled2242-file candidate provisionally; it does not
accept the remaining comparisons or change their next action (same-queue terminal
outcome, diagnosis if needed, and archive closure). Candidate validation stays open.

## Immediate game/application question

The retained batches now own their resource data, but NTSDApp still runs practice.
Can its real backend obtain the replies Core consumes without inventing success
or deferring observable work past a source-required boundary? Determine the first
complete application-side exchange that must be implemented before wiring the
actual startup/loading/menu path. This advances a real native app and ultimately
the full Naruto/Sasuke District match; it is not another isolated game rule.

Reuse the Host/app preflight's complete input/operation inventory rather than
recounting it. Inspect only these six dependency questions:

1. Exactly where startup/iteration/loaded providers return values to Core, and
   where the current Host permits preparation, commits state and hands off work.
2. Which startup/window/audio calls consume a backend result immediately, including
   ordinary failure branches. Preserve numeric replies and owned resource IDs.
3. Where loading progress/presentation occurs within the catalog child relative
   to final Host publication. Separate source call order from unobserved pixels,
   device completion and elapsed time; do not infer Windows visuals from a trace.
4. Event retrieval and time observations: which decisions remain owned by the
   recovered loop and what an actual host must supply at the request boundary.
5. What Native rollback proves versus what a visible window, playback, queue
   removal or file write could undo; identify cancellation/partial delivery and
   retry dependencies explicitly, without promising physical exactly-once IO.
6. Select one concrete next implementation contract/whole-caller comparison, or
   the exact missing evidence that prevents it. Do not implement a success-only
   fake backend, copy the test provider into runtime, or choose an unrelated helper.

## Evidence and bounded operations

Use only the pinned original NTSD distribution as behavioral reference: EXE
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
Read existing studies, instruction archive constraints and Native source. No
original/emulator/capture/auditor, refused operation, build, XCTest, live device,
fixture decoding or new OS API experiment. Existing controlled replies are not
actual Windows/host observations. Findings are static facts/design inferences;
independent review is unavailable and remains open.

Actual Native input is application-host-delivery-context-validation-20260926/
candidate1, manifestab4c0eb67b1c6b25abd4690124e116465dad709db807acd1bd2636c41350f430.
Pin the exact consulted files and line references. Initial15 Core files:
OriginalApplicationHostSession, OriginalApplicationStartupPlatform,
OriginalApplicationBootstrap, OriginalApplicationMenuSession,
OriginalApplicationLoadingSession, OriginalApplicationCatalogSession,
OriginalApplicationLoadedMenuSession, OriginalApplicationMessageLoop,
OriginalLibLoadingProgress, OriginalWinMainStartup, OriginalWindowInitialization,
OriginalInitialLoadingCommon, OriginalMenuPresentation, OriginalMenuSoundStartup
and OriginalWaveLoader (all .swift). Up to nine directly referenced additional
Native files may be selected/pinned before reading; total<=24, aggregate<=8MiB.
Read at most16 relevant study/instruction/evidence documents, aggregate<=8MiB;
reuse already read WORKFLOW/CURRENT_WORK/RESEARCH_MAP/template/CONTINUE_GOAL and
Host/app/transaction/retained-context plans. Preserve the archive unchanged.

Task/alias application-prepared-backend-preflight-20260926 under
/Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/.
Before IO verify writable APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548 and current
free space; retain40GiB external/6GiB internal plus17GiB source commitment and
the concurrent validation's remaining28GiB allowance. Require90GiB external and
9GiB internal. This analysis allowance64MiB, logical64MiB, stop56MiB task bytes;
metadata archive<=16MiB, root additions<=1MiB, elapsed<=3600s. No large candidate
clone or asset/traces copy; T7 and previous storage paths remain unchanged.

Mutable only this new task/alias, this plan, one result study, evidence and own
navigation additions. Do not modify runtime/candidate/tests, previous tools/plans,
live producers or expected values. Reuse existing manifest/pin/PAX procedures;
a checklist is not a new execution framework. One author owns these paths.

## Acceptance and handoff

Produce a source-pinned table of request/reply/publication dependencies, a clear
feasibility conclusion with unknowns, and one finite next contract tied to an
actual whole startup/loading/menu consumer. Proposed host design is not a new
original rule or implementation acceptance. If the old transaction boundary
cannot establish an application behavior, record the gap instead of silently
relaxing fidelity or changing a frozen test/plan. Keep current validation open.

Verify consulted pins, preserve a complete small metadata archive by all names,
bodies/modes/nsmtimes, update the handoff and commit checked analysis. No EXE
envelope recalculation. Existing safety incidents, source59727's terminal34-Object
publication/full137/transport gaps, independent review, actual input/window/audio,
Windows/clean-Mac, complete match and full game remain open. Revalidate running
queue/child identities; silence never authorizes a restart or replacement.
