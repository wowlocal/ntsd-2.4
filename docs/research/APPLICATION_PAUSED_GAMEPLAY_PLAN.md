# Application pause, single-step and resume plan

Status: accepted finite composition; see [results](APPLICATION_PAUSED_GAMEPLAY.md).
Frozen candidate1/candidate2 plans and review findings remain immutable.
Base `06569929b92deeff35234bbf098136845f0438b3`, clean actual inputs.
Consumer: the recovered application's current Bootstrap ticket after the
complete own first+16 neutral gameplay calls. This extends the path toward the
first Naruto/Sasuke District match; the full game goal remains unchanged.

## Evidence and declared boundary

Use [the reviewed frontier](APPLICATION_EQUIVALENCE_FRONTIER.md),
[paused gameplay](PAUSED_GAMEPLAY.md), [paused HUD](PAUSED_HUD.md),
[own neutral composition](APPLICATION_GAMEPLAY_EFFECTS.md) and
[active output](APPLICATION_ACTIVE_OUTPUT.md), including their direct contracts.
The frontier's inventory2 checks all28 saved calls, input latches, counter holds,
render checkpoints, source return boundaries and raw/transport hashes.
Original EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`;
installed lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`.
Saved source uses the original declared Unicorn/COM/GDI boundaries, not actual
Windows or device observations. No new original execution or historical job.
Open [safety incidents](../evidence/codex-safety-incidents-2026-09-12.json)
remain open; this independent Native continuation does not retry those operations.

Two immutable `original-paused-gameplay[‐control].json` fixtures supply14 calls
each, after their neutral parent, with8 paused and6 unpaused continuations.
Whole returned Frame storage is available; whole Object/objectStrings and middle
Frame snapshots are absent. Own catalog provenance remains checked separately.
All28 calls explicitly retain non-playback `450b84==0`; enabled paused indicators
and playback-information remain open. `Menu.Snapshot.local` is not
`Caller.formatter`: the latter and paused stageDefeated stay nil/private, with no
source stack import or claim of comparison for an unobserved caller field.
Own initialization/selection/library installation differs from the pristine
source parent. Acceptance must remain finite current-input composition, not a
single identical installed source startup-to-match trajectory or full ABI/FPU.
The selected old paused regression retains its source/helper ABI/FPU checks;
the new application projection does not add full Native helper ABI observation.
It compares all available stage records and aliases, the paused body/output
global-store suffix and observed background store PCs. Unobserved intermediate
background store order remains distinct from endpoint equality.

## Cases and implementation

| Boundary | Required behavior and comparison |
| --- | --- |
| Own input acquisition | Declare WM_KEYDOWN at calls1(F1=112),5(F2=113),11(F1), and WM_KEYUP at2,6,12. Other calls deliver no messages. These six Native messages are distinct from three changed source keyboard bytes. Compare full MSG, loop counter, key table and recognizers from current state through Bootstrap/WndProc. |
| Loaded input | Independently derive phase and cached latches; clear both ten-byte command and playback buffers; preserve paused local-input skip and five versus six checkpoints; compare round/replay/storage owners and outer KeyScan/diagnostic effects. |
| Paused body | Continue the actual PendingContinuation without repeating input/round. Draw direct background, World, preserving HUD, PAUSE at360/288, indicators and output. No active simulation/camera on cached pause. |
| Library and events | Shared paused caller retains installed text/DC and hit/transform owners; text reaches both layout and output. Compare six checkpoints, complete state/masks, resource aliases and chronological graphics/audio journal. |
| Transition/resume | Unpaused calls1,2,7,8,13,14 retain all19 current-input stage comparisons. Replay/elapsed18,19,19,19,19,19,20,21,21,21,21,21,22,23. In this notice1 domain, notice timer holds on cached pause while text still draws; other notices retain their distinct contracts, including notice3's separate timer increment. |
| Whole return | Current output, pending inner return and original Bootstrap ticket remain one transaction. Compare retained owners after each of28 returned calls. |
| Failure/retry | On an unpaused and paused call, fail late output/dispatcher, inner commit and outer tail/commit; compare unchanged application, environment and ticket, then same-session retry. Keep already committed key acquisition. |

Independent calculations use current input and source-supported scalar rules;
no source after-state is imported into Native. Do not broaden an active-only
comparison by deleting its guard. Source checks precede own comparisons and
include all source records/events declared available, with explicit exclusions.
Reuse existing neutral, input, drawing, journal and graphics comparisons where
their contracts actually apply. External effects remain value-owned until commit.

## Ownership, storage and gates

Root owns `OriginalPausedGameplay.swift`,
`OriginalApplicationGameplaySession.swift`, new
`OriginalApplicationPaused*` test/support files, this plan, the final study/evidence
and navigation. Existing projection helpers may receive separately reviewed
extensions only when their consumers and retained guards are explicit.
No fixture/resource/reference expected bytes, archive or incident edits.
`war_contract_review` owns numbered read-only review reports in the task directory;
author self-review is not independent. Review candidate before Native execution.

Work: `build/research/application-owned-paused-gameplay-native-20260914/` on
verified X5 UUID`3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`; initial1.json records
actual free space and previous-turn progress. Preserve40GiB external/6GiB
internal reserve. New task budget40GiB,3 candidate rounds,3600seconds per Native
run,28 new calls plus complete retained parents and declared rollback attempts.
Freeze actual Native files and selected method names before execution; use the
existing task manifest/job/candidate/audit pattern, not a new execution framework.

Selected regressions: both old OriginalPausedGameplayTests, both current
OriginalApplicationActiveOutputTests (all19 active contracts), both own neutral
sequences through the new tests' required parent, and relevant shared installed
surface-text/paused-HUD methods. Resolve exact names and pins before freezing.
Compile/tests, source package byte verification, archive verification and
independent review are separate gates. Preserve all failed attempts. Do not
restart an accepted historical capture or run for silence.

Stop this card only after both finite schedules, current snapshots/effects,
rollback/retry, selected regressions, preserved inputs, review and publication
pass; otherwise record the actual unresolved gate. Full app integration,
damaging fight/match, other modes/content/AI/playback/network, real Windows/device
and clean-Mac acceptance stay open. EXE envelope is not recalculated by this card.
