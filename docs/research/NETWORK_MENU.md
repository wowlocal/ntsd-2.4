# Network choice, host waiting and client input

`OriginalNetworkMenu` implements the complete selector1/2/3 bodies at
427ca7..42873e/4287de. The reference comparison carries native World, globals,
resource ownership, hostname bytes and library DC through the actual enclosing
caller and presentation/epilogue. This is accepted controlled compatibility
research, not production menu wiring, Windows networking or a full application.

The pinned EXE, bundled lib.dll and VC80 run on Unicorn2.1.4. The actual DLL
installer executes before the World constructor and fresh menu resources. Its
13 patches, relocated instruction bytes and original files are verified. Low
level tracing is needed to recover the retained caller arguments, exact input
scan, mutable strings, output order and resource/network cleanup. API/COM/file/
keyboard responses and allocation backing remain declared research inputs.
No EXE, DLL, emulator or executable-memory patching enters the native runtime.

## Compared paths

Both primary and reverse-allocation/ramp controls independently reconstruct the
whole fresh parent. Each then executes944 caller starts:938 network bodies,
943 complete caller returns and one actual41bc90 loading-entry boundary. The
first idle/network-choice/client sequence produces its own empty hostname,
typed character, Enter state and deferred connection. It does not inject an
expected stack word or fabricate EBP19 at a later UI entry.

The later finite matrix explicitly supplies selectors, input flags, timer values
and network results. It is not944 exclusively user-reachable own transitions.
Three resource controls clear the background slot at the declared UI boundary;
the preceding prefix is not claimed to have produced that absence. Their
already owned old allocations remain in the inventory. Five sound objects are
bound explicitly for volume keys; own audio startup remains open.

| Comparison | Primary | Reverse/ramp |
|---|---:|---:|
| Complete UI bodies and actual tails |938|938|
| Ordered UI events |60632|67198|
| Network API requests |87|87|
| Source helper returns within UI |12355|13293|
| Complete ascending300-key scans |695|695|
| Actual instruction starts, EXE/DLL/CRT |1469/51/446|1474/51/446|
| Defined caller-local bytes written, summed |34469|34469|
| Atomic parts including fresh parent |945|945|
| Storage blobs |2061|2060|

These instruction counts include UI children, not all instructions of every
outer phase. Stopped42873e/4287de and allocator/resource/PTD/API adapters are
excluded from actual-instruction counts. They are not all branch outcomes.
The two different bitmap backings legitimately change negative-picture helper
reads/clips/Blts; full bytes and masks remain unchanged in the source evidence.
Neither backing pattern proves an actual Windows allocation's uninitialized
contents. Parent and tail comparison retain their existing helper contracts.

Three additional17-start corpora each compare12 complete UI bodies, then
explicitly reject one own partial-receive read with full rollback. Their source
calls still return using retained private stack bytes. Do not call these three
successful native matches. They contribute36 additional successful UI bodies;
these five corpora contain1912 UI matches and three separate own rejections.

Two additional sound corpora each execute20 whole caller returns and16 UI
bodies. They explicitly bind both the device flag and five sound objects before
subsequent calls. Each matches27 real COM sound-method requests, including
HRESULT-1 and failed GetDC controls. These add32 successful UI bodies; the
accepted total is1944 UI matches plus three separate own rejections. The first
five published fixtures are unchanged. Device initialization and audible output
are still separate from this controlled enabled-device comparison.

## Recovered behavior

Header/background draws precede address text, input and actions. The installed
401290 route uses `OriginalLibSurfaceText`, retains its own DC and requests
SetBkMode1. The pristine body/presentation APIs remain available for their
unchanged controls. Required text/local bytes are generated by native routines,
not copied from captured after-state.

All rectangles include both endpoints. The matrix tests each edge, its immediate
outside neighbor and the interior with held/previous pairs0/0,1/0,2/0,1/1.

| Action | X | Y |
|---|---|---|
| Forum |368..790|449..536|
| Choose host |260..547|274..300|
| Choose client |260..547|305..330|
| Cancel choice |260..547|336..361|
| Server Back |322..472|361..386|
| Client Connect |239..389|357..382|
| Client Back |411..561|357..382|

A click requires previous0 and held1. Forum requests original sound, Sleep300
and ShellExecute of the original URL; it does not clear held. Host/client/cancel
retain the original bitmap, sound and global-write order. Cancel performs sound,
menu0, offset-1, real background destruction and402d70. Enabled exit notification
and sendto failure are compared, including all newly written256-byte helper
buffer bytes; error-1 takes its original early close without clear/cleanup.

Menu1→3 writes index4511dc=0 and only the first hostname NUL. The recovered
storage is51 bytes at World+7d8; this is not an inferred whole World allocation
size. Before that producer all51 bytes remain unknown. Subsequent appends,
terminators and Backspace writes carry their own provenance.

Menu3 scans keys0..<300 in ascending order, accepting only flag100. It calls
[MENU_CHARACTER](MENU_CHARACTER.md), consumes117 only on the original paths and
retains unconsumed keys at capacity. Backspace is handled separately even when
AL is zero. Hostname lengths0/1/49/50, simultaneous keys, retained capacity flags,
both Shift paths and varying successive Caps responses are checked. Key16 aliases
the Shift flag; the tests preserve that alias. Client Back can change menu yet
still run the remaining key scan during the same frame.

Enter sets pending1, consumes Enter and draws Connecting before presentation.
The actual client attempt occurs on a later tick. Pending2 skips editable output
but does not attempt a connection. [NETWORK_CLIENT](NETWORK_CLIENT.md) supplies
socket, lookup/fallback, connect, greeting, packet, names and RNG rules. Complete
error tails, fallback success, ignored numeric failures with independently
supplied bytes and a zero-byte RNG receive all compare. Socket/connect failures
continue at the actual4287de epilogue instead of being redirected to presentation.

Server waiting sets bit2 while preserving other bits. Timer comparison uses
unsigned elapsed time>150; phase increment wraps signed32 before remainder14.
The matrix includes first-use flags, elapsed0/149/150/151, UInt32 wrap and phases
INT_MIN/-1/0/1/3/13/14/INT_MAX. A positive dot count still draws after Back.
Connected byte must equal1: byte2 does not transition World. Byte1 produces
World1, the next whole call produces World2, and the following actual call stops
at41bc90. No loading worker or match is inferred from that boundary.

The three background controls execute actual423840/43ee50: successful loading,
missing resource and failed color key. The latter two retain diagnostics and
release behavior. Native allocation ownership survives subsequent caller
composition; freed backgrounds are not resurrected by the prefix cache.

## Storage and failure boundary

Every UI compares full World prefix, globals, hostname and owned resource bytes/
masks, ordered events and DC. Its real presentation or epilogue also compares.
The source retains all1024 before/after caller-local bytes and an actual write
mask. Native compares newly written bytes and uses recovered World/target
arguments; it does not adopt unwritten private caller-stack backing.

The three own partial-receive rejections occur at local+270, +f4 and +114, each
relative to callerSP+14: missing greeting, missing flag bytes and missing name
bytes. The independent audit observes14/52/44 source reads without native
provenance in those cases. All successful final UI calls have zero such reads.
The source's use of prior private stack contents remains evidence and an open
compatibility dependency. The broader explicitly supplied local-storage domain
of the retained392 client cases is unchanged.

Seven late failures per main corpus and four per partial corpus preserve all
supplied World/globals/hostname/local/DC/resource states. They reach late text,
hostname mutation, network observers, background release and new background
allocation before throwing. External effects must be buffered until the whole
enclosing operation commits. This does not undo an already sent real packet.
Production caller transaction/routing and initialized listener/peer joins remain
separate work; the reference callback is not the current app runtime.

Actual source UI and tail CW stays0. This early-menu environment has not executed
CRT floating-point startup and is distinct from the earlier CW023f gameplay
chain. No native process-wide FPU-state or Windows CPU claim is made.

## Evidence and verification

[Primary evidence](../evidence/network-menu.json),
[reverse/ramp evidence](../evidence/network-menu-control.json),
[partial greeting](../evidence/network-menu-partial-greeting.json),
[partial flags](../evidence/network-menu-partial-flags.json) and
[partial names](../evidence/network-menu-partial-names.json),
[enabled sound](../evidence/network-menu-sound.json) and
[enabled sound control](../evidence/network-menu-sound-control.json) pin raw paths,
producer SHA, lossless fixtures, blobs, instruction bytes and declared limits.
The producer is [oracle_network_menu.py](../../tools/oracle_network_menu.py);
[verify_network_menu.py](../../tools/verify_network_menu.py) independently
reconstructs loader bytes, source writes, masks and raw/packed JSON.
[accept_network_menu.py](../../tools/accept_network_menu.py) requires terminal
raw acceptance before publishing fixture bytes.

Raw14 release tests passed18.195s/build48.80s. The first five packaged corpora
passed23 tests22.745s/build49.08s. The supplemental enabled-sound raw comparison
passed23 tests9.930s/build0.27s. Final packaged verification covers all seven
corpora without raw overrides, including both menu/sound controls, all three
own rejections and retained character, library text, client/exit, body,
completion and loop tests:23 tests23.957s/build50.08s. Seven new fixtures preserve all218 prior pins, giving
225 total; the intermediate223 pins remain verified. Full raw/packed bytes/JSON/SHA, every blob,10 vendor files and the
isolated570-file native export are independently checked. All owned source and
SwiftPM jobs are terminal. NTSDNative links; no app window or device was tested
at this milestone.

Completed probes and observer failures remain in
`build/research/network-menu-work.json`. The first added return observer expected
a callback that an earlier stop hook prevents; its corrected contract checks
actual PC/SP/saved registers/SEH. A key122 probe exposed absent audio bindings,
not an observed Windows crash. Bitmap error APIs required dispatch through their
actual rebound MessageBox/Release addresses. The independent verifier initially
assumed the other gameplay chain's CW023f; actual0 was retained without changing
source or native results. No completed source process was restarted for silence,
no previous fixture was edited, and no safeguard was bypassed.

Full CRT/NLS/WinMain initialization, asynchronous listener/peer composition,
unknown partial-buffer provenance, remaining lib hooks, native menu/runtime
wiring, window/input/animation/audio/latency, Windows, full match/content and
clean-Mac acceptance remain open. The full game goal is not complete.
