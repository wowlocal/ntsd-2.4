# Team Tournament bracket and four participants

Continue mode3 after [setup](LIB_TEAM_TOURNAMENT_SETUP.md) at435a58. Recover
menu126 pair progression, computer winner/HP calculation, menu127 human and
computer seat assignment/cancellation, menu128 result consumption and menu129
winner output. Complete ordinary calls return through436fb5ret12/422ab8ret4.
The two human/mixed chains stop BEFORE436747, after CPU placement and its sounds,
but before menu0/GetLocalTime, battle Actor/arena/replay preparation. This is an
explicit next dependency, not a returned match or disabled preparation.

References are the pinned NTSD2.4 EXE, bundled lib.dll, VC80 CRT, original DAT/
head/small BMP and menu DIB resources. Controlled Unicorn2.1.4/CW023f executes
actual instructions with declared API/COM/GDI responses and atlas geometry.
Disassembly, memory reads/stores, stack/register and helper return traces are
needed to distinguish pair indices from catalog ordinals, preserve live
assignments, RNG/HP order and original text/music/sound output. Native uses its
own resource loaders, state, RNG and immutable game constants. Reference EXE/
DLL execution is research tooling; this does not establish Windows/device or
own complete catalog/application equivalence.

The frozen377-call manifest has three fresh menu120 setup chains: eight humans,
two humans plus six computers, and eight computers. Preserve every previous
call's World/400Actor/globals/catalog/resource/library/stack storage and masks;
only declared button/history bytes and three ordinary ABI words change after
returns. Previous-Attack CA is explicitly the released/held419a60 input-producer
output;431c70 itself does not reset it. Cover natural timer25/26..80 progression,
Jump advance, occupied-seat rejection, held Attack, four human assignments,
confirmation cancel/reassignment, paired CPU slots and winner wait/dismissal.
These are new declared entry/input chains; the earlier373 setup corpus and its
expected bytes remain immutable and are separately rerun as a native regression.

Add48 controlled menu126 entries: every16 placements of original ID/10==5 among
four fighters, at each ordinary difficulty0/1/2. Pair-preserving order, teams,
positive HP and valid original catalog operands remain explicit input fields.
No-special pairs use105; two special teams use106; one special team wins without
that choice draw. Both winning fighters use separate107/108 draws, with original
range15+18*difficulty. Independently derive the choice and each live HP update.
Add three controlled menu128 outcomes1/2/-1 with four supplied current HP/maxHP/
state/team scalars, including two defeated fighters. These are post-fight
producer-boundary inputs, not claims of played battles. Cover three winner timer
entries19/20/49 with nonidentity pair order and a held-Attack assignment case.
Do not extend to arbitrary damaged pointers/protection or invalid RNG ranges.

Original immutable layout44d120..44d317 contains126 words/504 bytes. Verify it
against original EXE, initialize native global storage from recovered constants,
and compare live reads; do not copy unknown stack or expected after-state.
Source research views still declare resource geometry and original file-backed
short-label NUL. Unknown small-bitmap frame-count masks remain unknown.
The source manifest's initial Python KeyError for an absent optional actorWords
field is preserved with its producer before correction. It ran no original
instructions and produced no expected result. No source or old fixture changed.

Acceptance: every finite call's full World/400Actor/globals/catalog/resource/
library bytes/masks, ordered output and music events, semantic locals and end
boundary; independent full store/read/checkpoint/instruction/ordinary ABI audit;
all48 CPU choices and both HP draws, four participant result stores/halving/team
promotion and tie RNG; all completed atomic cases and raw/packed JSON/SHA.
Current-call rollback must survive late Start music, human assignment, second
HP RNG and final mixed CPU placement while preserving earlier calls. Compare
retained373 Team setup and250 Tournament bracket on the same Core. Preserve285
fixtures, all accepted archives and14 pending Native/3 pending source/6 foreign
files. Use a new isolated copy of accepted699-file Team setup package.

New large data/builds use task-owned X5 through build/research/lib-team-tournament-bracket.
Revalidate independent catalog5 Python78863; never restart for silence or reduce
its reserve. Preserve errors/refusals and stop retries of any safety rejection.
Full Team preparation, War, own catalog/library/outer loop/app integration,
full match/game/network, window/input/audio/Windows/clean-Mac remain open.
