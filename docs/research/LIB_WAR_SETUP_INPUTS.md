# War source input correction after the first resource return

The original [plan](LIB_WAR_SETUP_PLAN.md) and manifests1/2 remain immutable
records of the initial design. Probe1 stopped at an unregistered literal `--`
sprintf format; the observer was extended to this actual original format.
Probe2 completed four whole calls. Its snapshots then showed that real fresh
menu loading429b21 cleared all eight declared ready statuses and selected
ordinals. Those calls are valid unready War observations, not the promised
Human/CPU label or participant coverage. No Native acceptance follows.

The running capture1 was revalidated as uv26975/Python26976 and interrupted
with SIGINT at2026-09-11T17:01:00.159313+00:00 for this concrete input-provenance
gap, not silence. It ended130;145 atomic cases and any interrupted temporary
file remain evidence. No source failure continuation or safety refusal occurred.
The first producer, pins, logs and stop reason stay in build/research/lib-war.

Manifest3 adds an actual menu3/mode4 resource-parent return before each ready
entry:574 calls,412 primary/162 control,573 whole returns and one BEFORE43a21f.
Only after that real return does a declared bridge set menu200/selection3,
the eight selected/status/Random scalars and each retained Actor's Object/team.
No private stack or expected bytes are imported. Both raw and Native sides
must independently apply this declared input. All subsequent button programs
and thirty popup confirmations remain as planned. Their two ready bridges do
not establish earlier mode4 character/CPU selection reachability.

Acceptance additionally checks that all eight ready statuses, selected Object
bindings and teams survive into the first438b40 consumer. The actual constructor
and resource-parent before/after states, all intermediate records and masks,
and both13-bitmap ownership sets must compare. The old plan's572-call count
and claim of ready inputs before fresh loading are superseded only as stated
here; all original expected observations remain untouched.
