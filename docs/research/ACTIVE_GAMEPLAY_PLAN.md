# Active keyboard trajectory after the own neutral sequence

The finite plan below and its preparation history are retained. Both full48
comparisons are now complete in [ACTIVE_GAMEPLAY_CAPTURE](ACTIVE_GAMEPLAY_CAPTURE.md).
These are pristine EXE/VC80 controls without bundled lib.dll; the actual entry
loads that library and changes game code, as recovered in [LIB_RUNTIME](LIB_RUNTIME.md).
Their matches do not establish the library-enabled application or a full match.
The original plan continued commit c65f9a8: both initialized variants had
matched sixteen successive loaded calls. Each fresh chain retains its own CPU
and game state through that entire accepted parent.

The next trajectory supplies48 acquired keyboard frames. The original control
bindings select the virtual keys; no Actor command, phase, RNG, location,
health or source stack value is injected. Frames1..16 hold Naruto left/Sasuke
right;17..24 retain those directions and add attack;25..28 release;29..36 hold
Naruto defend/Sasuke attack;37..40 press jump for both;41..44 hold Naruto up/
Sasuke down;45..48 release. These are deterministic research acquisition
responses before4246b0, not measured OS device events or wall-clock timing.

The reference is the original distribution in
`downloads/NTSD_2.4_2.0a_clean/NTSD 2.4_2.0a`: EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`
and VC80 SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
under Unicorn2.1.4, with the inherited initialized CW023f context. Low-level
observations recover command consumption, action order, moving/attacking
interactions, packet mutations, effects, scratch lifetimes and normal returns.
COM/GDI numeric responses and loaded resource bindings remain the accepted
platform boundaries. Sound stays enabled. Native runtime contains no EXE/DLL
execution, emulator, browser or Wine.

The source tool is [oracle_active_gameplay.py](../../tools/oracle_active_gameplay.py).
It verifies the complete old parent bytes and hashes before applying keys.
Its parent checkpoints have new filenames, preserving the previous captures.
New complete returns are atomically checkpointed; an actual failure retains
its PC/SP, in-progress helper/effect context and all prior completed calls.
Unrecovered AI/object-input children stop before old controlled adapters. A
partial call is not a return, and an unsupported native continuation is not a
match. Preserve any safety refusal and stop retries of the rejected operation.

Both raw before/after keyboard bytes and semantic button requests are recorded.
Bindings are reread from live own globals every call, and overlapping bindings
share one acquired key state. Source code runs only after acquisition; the
native operation must use the same boundary, retaining acquired input if a
later loaded-call transaction fails. Keyboard changes are distinct from source
instruction global-write traces.

Extend complete return snapshots to all137 Object headers, inline Frame tables
and name tails, plus the referenced weapon-string allocations. The existing
World/Actor, mutable Frame heap, backgrounds, bitmaps, replay/resource ownership
and FPU/normal-frame comparisons remain. Intermediate body snapshots retain
their previously declared scope. Known control/physics sound helpers execute
original instructions; their helper ABI and primitive effects are recorded.
Other primitive effects retain actual helper context for native comparison.
No expected state, helper return or unknown caller word may drive Native.

Acceptance requires both48-call source trajectories or the first explicitly
retained dependency; only whole native matches can be published as accepted
fixtures. Compare all acquired-key effects, input boundaries, body stages,
complete return bytes/masks, ordered events and source FPU/stack/return evidence.
A late observer must roll back the entire loaded call after changed commands
and replay data where the trajectory actually exercises those changes. Preserve
all190 continuous-milestone pins and any concurrently accepted independent pins;
verify full raw/packed JSON/bytes/SHA and every component/blob before publication.

Full-match completion, paused/menu/epilogue routing, the timed43e9a0 outer loop,
application engine integration, real Windows/device/input-latency evidence and
clean-Mac packaging remain parts of the full objective. This trajectory is a
step toward them, not a replacement completion criterion.

## Preparation evidence, before source acceptance

The initial two fresh jobs ended with a harness `TypeError` before active
input: the new `acquire(step)` method unintentionally replaced the inherited
character-menu `acquire(changes)`. Both exit codes were1; source sessions24480/
89498 and PIDs96584/96595 were confirmed terminal before correction. Original
logs, the exact harness SHA256
`7e648c3e23994d5a409c1f758d8b08e8dd33e856bedd632edb53b74b66295c35`
and failure evidence are retained under
`build/research/active-gameplay-attempt1/`. Renaming only the new method to
`acquire_active_keys` preserves the menu implementation. The corrected method
inventory was checked for other unintended inherited-method collisions.

The first native build rejected access to the internal catalog. The comparison
now reads a public, immutable `loadedObjects` view of the own catalog. An
isolated native-only export of5a0916a plus the six owned files built in176.04s;
both retained16-call comparisons passed67.356s/build0.23s with their exact old
counts and bytes. No reference fixture or native gameplay rule was changed.
This export excludes concurrent, unfinished pause composition.

A temporary overlay in that isolated export also predicted all48 active calls
from independently reconstructed native state, without active source snapshots.
It returned48 times, kept only slots0/1 active and both HP500, ending with
frames0/212 and MP221. These are **native predictions, not original matches**.
The first preview filter matched no tests; the corrected filter ran the intended
primary test. The preview files and hashes are retained in `build/research`.
The temporary overlay was removed, and all494 isolated-source file hashes
were verified equal to the pre-preview export before any active acceptance.

`build/research/active-gameplay-work.json` records current jobs and exact logs.
The independent [application timer audit](APPLICATION_TIMER_PLAN.md) is a plan
for the later outer-loop join; it does not advance this active source capture.

## Retained16-call active prefix and the constructor observer correction

Both second-attempt sources completed16 active calls, then the lifecycle
observer rejected4450a0 on call17 during opoint construction of slot50. The
existing constructor memset adapter had already returned to406447/SP1000e9a4;
the pending4061d0 caller returned to41fd9d. This was a harness instruction-range
assertion, not an original memory fault. Both jobs were confirmed terminal1
before any correction. All14 source/log/checkpoint/work files,83322918 bytes,
are preserved under `build/research/active-gameplay-attempt2/` with hashes.
The incomplete17th call is not a native match.

The third attempt retains the same adapter, records its before/after bytes,
arguments and preserved registers, and excludes the already-handled4450a0
thunk from actual instruction coverage. It never calls the adapter twice.
Independent checks bind its zero-filled400 bytes to the actual constructor's
Actor+f0, return406447 and stack relation. Fresh calls1..16 must reproduce the
retained second-attempt cases exactly, apart from the new empty boundary list.
The new source hash and live process/session identities are in the work file.

The retained16-return prefix was compared independently using an isolated
comparison-count overlay; production code and every source byte were unchanged.
Its first native trial found an undefined Object+6fc portrait-pointer read in
the new comparator.95 Objects leave this field undefined. As in the accepted
catalog comparator, pointer binding now runs only for fully defined words;
complete raw bytes and masks still compare. No pointer is invented or zeroed.

Both corrected prefix tests pass96.416s/build69.71s. Each checks3551332 records/
13465981813 bytes+masks,813 state checkpoints and28590 source FPU checkpoints,
including the whole old parent. New active events16416/17312 and helpers4410/
4538 remain separate from the old16-call counts. All3357/3358 source blobs and
their complete manifests independently verify. Eight phase-zero calls change
recording bytes; intervening phase-one calls do not. The first late dispatcher
observer verifies rollback after changed replay input and preserves the acquired
keyboard state. Both finish frames6/7,HP500/MP211 at positions378/504 and353/519.

This prefix was intermediate, not an accepted48-call fixture. No active fixture
was published at that point. The isolated count overlay was removed and all494 canonical
48-call source hashes restored, retaining only the defined-pointer correction.
`build/research/active-gameplay-prefix-native/result.json` holds exact source/
test pins and the separate full-call counts. The later whole48-call milestone
supersedes this intermediate boundary; the full game goal remains open.

The third source attempt completed the first active machine return but stopped
on its new retained-prefix equality assertion: live undefined-read entries are
Python tuples, whereas their JSON representation decodes as lists. The first
case was not retained by that older failure handler. Both sessions62401/70157
and PIDs6316/6315 were terminal1 before correction;14 files/52671568 bytes are
preserved in `build/research/active-gameplay-attempt3/`. No original fault or
safety refusal occurred. A local reproducer shows the tuple/list difference,
equal canonical JSON bytes and rejection after changing an instruction address.
The new check compares exact canonical JSON, and whole returns are checkpointed
before verification so a future mismatch retains every actual observed field.
Attempt4 must still reproduce all16 retained cases before continuing.

The canonical48-call isolated package rebuilt in104.55s after removal of the
prefix overlay. The independently accepted pause milestone30ce08e now includes
the application timer milestonef09d23e; a fresh native-only export of that HEAD
plus the six active comparison files is used to check integration with both
accepted continuations. Source/package hashes, process identities and results
remain in `build/research/active-gameplay-work.json` and the integrated pins.
All six integrated retained release tests pass160.603s/build175.89s: two timer,
two neutral and two paused comparisons. Their prior counts remain unchanged.
The export contains502 pinned files. Its first preparation used a newer
tarfile keyword unsupported by the host Python; a second member check omitted
the root directory's slashless name. Both preparations stopped before any build;
the corrected export validates each tracked member before extraction. No game,
expected data or running source process was changed by those corrections.

Attempt4 reproduced the entire neutral parent and all16 retained active
cases. The independent verifier also checks their complete28590-entry FPU
history against immutable attempt2 bytes. Both fresh sources passed call17,
the original constructor observer boundary, before continuing toward48. Exact atomic
17-return checkpoints were copied without slicing into
`build/research/active-gameplay-prefix17-native/`; all components/blobs, globals,
normal returns and the single constructor memset boundary independently verify.

That call constructs slot50 from slot0's opoint and processes it later in the
same lifecycle pass; only slots0/1 remain active at the final return. Catalog
sound requests are slot1/X357/index109 then slot50/X370/index366, followed by
two queued play requests. Both main Actors retain HP500, frame61 and MP211.
The constructor's400 memset bytes were already zero before the adapter and
remain zero afterwards; do not describe them as400 changed bytes or actual
CRT execution. Its correct destination is the actual new Actor+f0, with saved
registers and406447 return preserved. The call adds845 FPU checkpoints, ending
at29435; primary/control body/output helper counts are284/292.

A separate17-call count overlay in the older isolated5a0916a package prepares
an intermediate native comparison of this newly reached constructor path.
It changes only comparison guards, not game algorithms or source values; no
partial fixture is published. The main/integrated package remains strict48.
The preparation build passed106.97s. Exact jobs and source pins are in the work
file; this intermediate preparation does not yet establish the full48 match.
Both17-call native comparisons pass96.758s/build0.24s, each3642990 records/
13893949291 bytes+masks/836 state checkpoints and29435 FPU checkpoints.
New active events17444/18396 and helpers4694/4830 include the exact constructor,
live-slot processing and two sound plays. The older isolated source overlay was
removed and all494 canonical hashes restored; its last built binary remains
the intermediate17-call test. Final48 acceptance used a separate4298b7e export
with the same six active comparison files. The exact intermediate proof is
`build/research/active-gameplay-prefix17-native/result.json`.

Both full sources completed48 returns with exit0; the strict48 native raw
tests passed159.390s/build169.00s and packaged tests153.680s/build166.35s.
Only the two complete48 fixtures were published. Their immutable source bytes,
all7811 blobs/1314 manifests and prior196 pins verify independently. Full
results, bounded claims and the newly discovered bundled-library dependency
are in [ACTIVE_GAMEPLAY_CAPTURE](ACTIVE_GAMEPLAY_CAPTURE.md).
