# Complete the guarded catalog after recovering trace storage

The guarded primary candidate4 ended normally at its research storage boundary,
not the game's return:2026-09-11T08:48:08UTC, exit0, `researchStorageLimit` at
7817836c, reserved6442450944bytes. It retained24 completed Object returns, a
25th pending Object and151 registered WAV records. The175841575-byte output
SHA949a7a632e366543eac32a515ca28b5dd3df5cd4b19edd91bb8e6e7ad7cdb3a5
and all trace parts remain intact. Its20-Object native/audit acceptance stays
separate; neither24 returns nor the complete catalog are newly accepted.

The next finite operation recovers storage and then performs a fresh original
capture from the same declared parent. The behavior being recovered remains
the full own catalog4122f0 return to41c018:137 registry children and all observed
file/DAT/Object/Frame/bitmap/WAV/loading-text operations, order and ownership.
References remain the pinned NTSD EXE, bundled lib.dll, VC80 and original files
under controlled Unicorn2.1.4. COM/GDI/allocator/IO responses remain declared
research boundaries. Native application/Windows/device/full-game evidence is
not implied by this development-only execution.

All four old source processes were verified terminal before storage changes.
Their167474 packed trace files total21163496646bytes;125292 duplicate copies
represent15817825009bytes. Full byte comparisons before/after APFS cloning
preserve every path, SHA, length, mode and mtime; all36 index files stay unchanged.
The operation changes physical storage only, without recompression or removing
any logical reference file. Initial preflight selected nonexistent per-job
filenames for the three old candidates; their authoritative terminal records
are in application-catalog-work.json. That error occurred before any clone and
is retained separately. The corrected operation finished successfully and free
space rose from5933752320 to22000947200bytes. Concurrent filesystem activity
means the measured increase is not a byte-exact physical-allocation claim.

Candidate5 may start only after this completion, current process revalidation,
unchanged source/helper/resource pins and at least20GiB actual free space. Use
the exact unchanged guarded producer SHA69b07f75bac554d13874d73767456f298d2940946be2207538da1934b49a9ce4,
the same original parent and unchanged20000-chunk/20-Object checkpoint/6GiB
reserve settings. Use new output, trace directory and job records. This is a
fresh execution after an observed resource limit was resolved; it is not a
resumption of the old CPU, a retry for silence or bypass of a protection check.
All old source observations and expected results stay immutable.

Acceptance still requires whole original return, complete immutable trace/
intermediate/read audits, full native ownership/state/event comparison and
rollback. Preserve any partial output or new dependency; an exit0 at a research
limit is not a successful whole return. No fabricated helper/CRT success,
private expected-state import, memory/protection corruption, fault continuation
or automatic retry of a platform safety refusal is authorized. The existing
guard and the full catalog dependency remain in force.
