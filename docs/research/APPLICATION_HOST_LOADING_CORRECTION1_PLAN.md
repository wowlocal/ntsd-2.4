# Host loading assertion correction — round1, 2026-09-22

This separate round follows the preserved [failed validation](APPLICATION_HOST_LOADING_VALIDATION.md)
and [WORKFLOW](WORKFLOW.md). Game question and42-method selection remain unchanged:
retain actual loading owners through one host commit, with whole-parent comparisons
and rollback. The immediate defect is the newly added operation-prefix assertion:
fresh loading carries three preceding wrappers; cached input has a direct menu
operation. Keep every type, effect payload, defined mask and order in comparison.
This is an author comparator correction; independent review remains open.

Base HEAD `03afa4ed7ca343b185e9fb26487cbda782c18010`. Actual input is the same
frozen1037-file candidate, with relevant dirty baseline, pinned by the prior task.
Original EXE SHA256 `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
No new original execution, actual Windows/device experiment or refused operation.
Saved controlled source records and existing Native compositional limits remain.

## Correction and cases

Clone the original candidate into a fresh task, preserving every body/mode and
membership. Save the before manifest and a separate exact patch. Change only
`native/Tests/NTSDCoreTests/OriginalApplicationHostLoadingTests.swift`: replace
the shared prefix expectation with a branch on actual `context.cycle` presence.
Fresh expects Input.preceding(Pool.preceding(Catalog.preceding(Loading.menu(effect))));
cached expects Input.menu(effect). Keep full equality, all cases and all other
comparators unchanged. No Core, fixture, expected after-state, mask or old test
file is edited. Pin the corrected manifest before any build or test.

Evidence: previous test26.log and its two assertions, plus existing operation
construction in Loading:44, Catalog:141, Pool:73, Input:72 and LoadedCycle:31,
all pinned in the prior failure-diagnosis. This is a distinguishable new comparator
version, not retroactive acceptance of the original failed method. Review is open.

Build Core/dependent targets fresh with installed Swift6.4, Swift5/macOS14 release,
whole-module compilation, native SwiftPM, jobs2 and `-Xswiftc -enable-testing`.
No prior cache is imported. Reuse the existing preparation, package check, queue,
monitor and archive procedures with new task names and input gates. Verify source/
output membership, flags, all487 packaged resources and exact named XCTest results.
Run the same42 methods:25 previous host regressions, four HostLoading, five
LoadedMenu, three LoadedCycle, five initialized menu/loading. No raw override.
The first25 are rerun because the selected test module has changed. A linked app,
finite composition and fourth injected input boundary do not establish a match.

Stop the queue on the first failed build/method/guard. Preserve all products and
results before further changes. This is correction round1 of at most3; any new
round needs specific evidence and a distinct candidate/config. Never skip a
mismatch or edit an old expected result. No success gate from exit0 alone.

## Storage, processes and publication

New task `/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-loading-correction1-20260922`,
alias `build/research/application-host-loading-correction1-20260922`. Require APFS
UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`,81GiB external/7GiB internal at start.
Allow24GiB observed decrease including concurrent source growth, stop at22GiB,
retain2GiB terminal/archive margin,150GiB logical bound and original40/6GiB
reserves plus the existing source17GiB gross allowance. No deletion/reserve cut.

Build3600s/12GiB sampled tree RSS; each method600s/8GiB; all42 selection3600s.
Poll1s/RSS2s/logical15s. Minimal HOME/USER/LOGNAME/LANG/LC_ALL/PATH/TMPDIR environment.
Reuse unchanged process ownership/monitor loop, compiler transition and narrow
terminal-xctest predicates. Revalidate PID/start/command/cwd/job before any guard
signal; never signal or restart source59727. Short process RSS0 means no sample.
Historical source HEAD remains provenance. Existing safety incidents stay open.

Root mutable paths: this plan, APPLICATION_HOST_LOADING_CORRECTION1.md,
docs/evidence/application-host-loading-correction1*, tools preparation/runner/
package/queue/finalizer names ending loading_validation2.py, loading_package2.py
or loading_tests2.py, and own CURRENT_WORK/RESEARCH_MAP paragraphs. New task owns
its candidate/build/results/archive. Frozen tasks/root Native/baseline/resources/
source producers and archive instructions remain protected. Root author owns
edits; no independent reviewer is available. Separate Native comparison, package,
archive and review gates. Publish failure if any remains; verify full archive
bytes/modes/mtimes/membership and task process terminal/absence before closure.
Commit each coherent checked increment with existing hooks. Root promotion, later
typed child retention, actual providers/devices, whole Catalog53 and game stay open.
