# Independent active physics and caller comparison

Status: investigation/implementation. Parent1ea223e accepts finite active control
endpoints; the other18 stages remain open under [the whole active body plan](APPLICATION_ACTIVE_BODY_PLAN.md).
Consumer: the own physics stage after current control, then contact/lifecycle and
the full Bootstrap tick on the path to the complete match and native game.

## Question and bounded evidence

Reconstruct both saved48-call physics passes41e634..41eed1 independently, including
whole40e490, retained state, live-slot order and death/reversion/spawn predicates.
Then compare those equations on actual own control output without giving Core an
expected state. Use the full original DAT/header/Frame and current own RNG/owners.
Any newly required reachable branch extends the plan explicitly; it is not
excluded because it disagrees with Native. Later post-draw creation/deletion is a
separate stage and cannot be inferred from a physics endpoint.

Read the immutable active fixtures, ACTOR_PHYSICS/WORLD_PHYSICS,
ARITHMETIC_PRECISION/INITIALIZED_GAMEPLAY and relevant archive constraints.
Original baseline EXE SHA2563f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c;
VC80 c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d;
installed lib.dll28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba.
Saved Unicorn mappings/API replies, CW023f and legacy conversion remain a declared
controlled reference, not actual Windows/device observations. No original code,
emulator, completed producer or historical auditor is executed by this card.

## Required comparison and limits

- All96 source physics World/400 Actor/global endpoints and masks, every saved
  Actor checkpoint with PC/slot order, full primitive events and relevant helper
  identities/return boundaries; current acquired Object/Frame inputs retain their
  provenance. Middle source heap snapshots may be absent and stay a declared gap.
- Own96 physics checkpoints from preceding own control, complete match/storage,
  current memory/graphics/audio/library/CRT ownership and caller journal/local
  record; exact numeric results and operation order under53-bit precision.
- Lifetimes/aliases are checked against recovered caller predicates; no source
  activation, after-state, constructor backing or expected frame is injected.
- Retain both accepted control comparisons and whole input/body preflight with
  rejection/rollback/retry checks. Negative comparator trials must discriminate
  unknown causal inputs and unsupported finite numerical or caller branches.
- Full instruction-store or helper ABI proof is claimed only for observations
  actually captured and compared. PC inventory is not a store trace.

## Ownership and execution

Root owns new OriginalApplicationActivePhysics*.swift comparison tests, a bounded
optional observer in OriginalApplicationActiveBodyTests.swift, this plan/result/
evidence and CURRENT_WORK/RESEARCH_MAP links. Existing control/source utilities,
neutral comparators, Core, expected fixtures/resources and history stay immutable.
A Core change needs a demonstrated discrepancy and separate plan amendment.
The existing independent reviewer owns only new numbered read-only review reports.

Initial clean994 files are pinned in
`build/research/application-owned-active-physics-native-20260914/initial.json`.
Freeze the actual changed files before each Native run. Select two new physics
methods and two accepted active-control methods when their helper changes;
inherited regressions require exact unchanged pins. Reuse existing freeze/job/
verification/archive scripts adapted to this card, not a new execution framework.

New large IO uses the task-owned X5 directory only after UUID
3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548 and free-space validation. Keep40GiB X5 and6GiB
internal reserves, task bound40GiB,3600seconds per Native run, initially three
candidates before diagnosis/revised bounds. Verify PID/start/command/cwd/job before
process actions; never rerun a live or completed job for silence. Preserve all
frozen failures, logs and restoration deltas. Shipping fixtures remain local.

Source-first comparison, Native regressions, independent contract review, package
byte/mode verification and both archive readbacks are separate acceptance gates.
Full active body, later transient lifetime, damaging outcomes/paused/round/match,
app integration, device/Windows/clean-Mac and full game remain open until tested.
Existing safety incidents stay open; no retry or rerouting of affected operations.
EXE envelope is not recalculated in this card.

## Candidate1 finite path and source result

Both saved48 schedules have two distinct live fighters, states0/1/3/4/7,
freeze/carried/cpoint zero and reversion ID−1. Only Sasuke calls42..48 are
airborne; there is no physics Frame transition, landing, death, spawn or reverse
transformation. The finite formula keeps these caller gates explicit; it does
not accept unobserved lifetime or landing effects. Later slot50 creation/deletion
is still required in its actual stage.

The separate saved-data Python formula reproduced96 complete pool/mask endpoints
and192 Actor returns with known operand reads (40877/variant),448 verified blobs
per variant and no primitive events. The Swift candidate separately checks all
40e490 helper identities/returns and source checkpoints at41e657/SP1000e9bc.
It compares current own positions and velocities rather than source deltas.
All arithmetic inputs/results use the accepted finite normal53-bit guard; the
legacy/SSE2 coordinate conversions agree within this bounded magnitude range,
which does not establish Windows conversion-mode provenance.

Freeze997 Native files (three added, one prior helper changed;993 old unchanged).
Select OriginalApplication(ActivePhysics|ActiveBody)Tests/: two new physics and
two accepted control methods.147 unselected inherited methods retain exact pins;
there are151 unique retained methods if all four selected methods pass. Eight
new negative comparator trials cover unknown velocity, live alias, unsupported
reversion and landing; accepted control/input/body rejection checks remain.
