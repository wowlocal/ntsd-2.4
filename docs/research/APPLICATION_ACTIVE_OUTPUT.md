# Active output and return from current owned layout

Accepted finite output/return comparison for both retained48-call schedules.
[Plan](APPLICATION_ACTIVE_OUTPUT_PLAN.md), [evidence](../evidence/application-active-output.json).
This closes the last stage comparison on these schedules and composes all19
accepted stage contracts through the current own PendingReturn. Full original
application equivalence, a complete match and the full game remain open.

## Analysis

The saved source goes from422994 through mode label41b130, notice/volume4028a0,
presentation43e940 and queued sound419e60, then the actual422ab8 and428805 returns
to declared STOP30000000/SP1000f42c. Both48-call variants have37825 output events,
583 global stores,7 plays and7975 helper returns each. All globals and masks are
reconstructed from the saved stores and also compared to independent rules.
There are3648 bitmap draws/clips/Blts,25536 bitmap reads,48 formats and83 methods
per variant. Normal frame restoration, known helper return sites and actual ret4
instruction presence are checked from the saved observations. Eight output FPU
checkpoints per call preserve CW023f and FPSW0 or4000; these are source observations,
not a new Native ABI/FPU or instruction-store comparison.

All96 layout/output joins are exact. The seven output component references are
contained in the enclosing after-state; objects, objectStrings and frameHeap are
additional enclosing references. Their absence at the middle boundary stays
explicit. The saved inventory resolves all referenced blobs and checks hashes;
no missing backing is filled from an enclosing expected snapshot.

## Implementation

Three new test-only files derive expected behavior from each current source or
own input. They reuse the accepted independent bitmap/text and graphics-owner
models, without calling Core output handlers for expected results. Literal label
stores, four font passes, notice bytes/timer, presentation, fixed400 catalog and
80 builtin sound queues, dispatcher clear and the complete journal are computed.
Right/left reads and wrapped sum precede positive flag clearing; the clear precedes
sum testing and sound requests. Wrapped pan/volume, fresh master-volume gating,
shared buffers and catalog-before-builtin order remain explicit.

Own sound words must resolve to current startup/catalog/common sound owners.
Observation-only sound events expand exactly once into terminal journal methods.
Each output checkpoint compares complete records/masks, memory/replay and resource
owners, menu-local storage and the ordered graphics/audio journal. Full returned
graphics are checked against the complete earlier-stage prefix plus current output.
All19 checkpoints run in their actual order with inherited independent endpoint
comparisons; the enclosing return equals the output snapshot and dispatcher result.
The older preflight helper alone still reports bodyDifferentialAccepted=false;
the outer composition is a distinct finite claim, not a rewrite of that history.

Controls cover wrapped values, nonpositive master, alias buffers, both queue
extents, ignored numeric device failures, disabled device with unknown backing,
inactive unknown channel words and nonpositive sum with an unread unknown buffer.
Event reordering and bitmap defined-mask changes are rejected. Eight new late
rollback checks across both schedules stop during drawing, after dispatcher
observation, during the second sound slot, or beforeCommit. Four same-session
retries reproduce full return state, owners, journal and graphics. External device
operations must remain buffered until commit; storage rollback cannot undo IO.

## Validation

Seven release methods passed in285.671s/build365.32s:
two new Output, two retained Layout, and controlled GameplayOutput/QueuedSound/
ModeLabel. Controlled corpora retain170/968/148 cases,119700/8314/181730 events,
10968 output and10156 label Blts,5776 sound method requests and three late rollbacks.
165 other method identities are inherited by unchanged pins, not rerun;172 unique.
All1024 current/frozen Native files and485 packaged files were verified. All1021
old Native files,383 fixtures and102 resources are unchanged. Native and evidence
archives receive independent full body/mode readback; publication review is linked
in evidence. Core, original assets and source expected bytes/masks were unchanged.

Native job1 is terminal in `build/research/application-owned-active-output-native-20260914/`;
do not restart it. No original, emulator, historical producer/capture/auditor or
previous terminal test was executed. The original plan's regression-count draft
and saved planning-reader failure remain preserved. Independent review corrected
165 inherited plus7 direct methods before freezing or running this candidate.
Publication review also caught navigation prevalidation and finite-composition
flag-gate omissions before execution; corrected scripts and prior drafts are saved.

## Remaining boundary

The source uses the pristine EXE and declared synthetic responses; the current
own chain uses its installed-library model. Comparisons are made against each
current input. This finite composition does not establish cross-environment full
original application equivalence. Missing middle Object/Frame evidence, formatter
checkpoint/lifetime observability, full helper ABI/FPU/instruction stores, broader
active command/refill/healing/state1700 branches and real devices stay explicit.

NEXT: use saved application/active48 evidence to document the remaining full
application equivalence dependencies, identifying which current source/own input
and library differences have established contracts and which still lack evidence.
Choose the first bounded caller comparison whose dependencies are ready; do not
fill missing snapshots or rerun completed producers. Full match/game, native
window/input/audio integration, Windows/device/clean-Mac and previous safety
incidents remain open. EXE envelope was not recalculated.
