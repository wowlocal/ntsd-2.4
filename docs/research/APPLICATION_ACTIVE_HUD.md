# Active HUD from owned commands output

Both finite48-call HUD comparisons passed. The frozen
[plan](APPLICATION_ACTIVE_HUD_PLAN.md) extends the accepted
[commands](APPLICATION_ACTIVE_COMMANDS.md) through the complete HUD endpoint,
not the full active tick. The next consumer is post-HUD notices, followed by
recording/layout/output, a complete Naruto/Sasuke District match and the full game.

## Current-input contract

The comparison covers both retained48-call schedules at caller421a15..421a2d,
SP1000e9bc, including41ae60 and bitmap/clip/rectangle children. Its source model
starts from saved commands output; its owned model separately starts from the
actual application commands checkpoint. Expected after-state is never installed
in the game. Production Core, DAT, resource files and saved fixtures are unchanged.

The new ActiveHUDProjection uses the existing record and independent bitmap
expansion utilities. It reads current activity, World mapping, Object+728,
HP/recoverableHP/MP, healing timers and team. It does not require neutral Frame
state, grounded Actors, a particular Object ID or source frame numbers.
Eight cells prefer primarycell, otherwise fallbackcell+10; nonzero activity
qualifies and aliases are not deduplicated. Current records supply portrait
ordinals, resource tokens and destinationglobal455608.

Command flags450bc0 and450bb8 become known zero before drawing. Signed wrapped
31*value/125 widths retain negative values and are independent of maxHP.
HP<=0 skips all bars; otherwise the original healing predicate and MP backdrop
apply. The finite bitmap expansion uses the observed794x550 viewport, including
negative-picture fallthrough, original read order and unknown bitmap masks.
These controls do not establish actual Windows backing or pixel colors.
The HUD path emits bitmap-based marks; unexpected extra text/effect events would
fail the complete ordered event comparison.

Source inventory2 independently derives all high-level requests from before
records and current owner order. Primary has5952 events, including960 unknown
reads; control has8640 events and2496 unknown reads. Both source schedules have
HP/recoverable500, MP206..221, timers0, teams10/11 and participants0/1.
All96 source before/after component references are equal, while the recorded
192 command stores explicitly establish their order and width. Owned equality
is checked against computed writes, not assumed from source invariance.

Source helper order/arguments/results, draw/rectangle/HUD receivers and stack extents are
compared using computed draw/read/clip/Blt streams and declared alternating
HRESULT0/1. Clip receiver/saved-register values are not compared; saved arrays
are shape checked. A full Native private ABI
comparison is not claimed. Active trace records the callerSP68 read421a15;
it does not include the pushed word's421a19 write. That separate observation
belongs to the controlled [WORLD_HUD](WORLD_HUD.md) corpus. The callee does not
read the argument and Native never imports it as the target.
Source CW023f/tagffff remain unchanged; FPSW is0 oncall41 and4000 on the other47
calls of each variant. These source checks do not establish Native FPU state.

## Composition and controls

The own comparison retains the complete earlier Bootstrap/input/control/physics/
contacts/graphics/lifecycle/commands sequence. It compares every Actor/World/global
record and mask, current catalog/resources/music/allocations, caller local record
and chronological journal. The graphics owner is independently compared against
the command prefix derived for HUD; the parent also checks the complete returned
command extent. Later-stage event positions remain a separate semantic dependency.

Comparator controls cover unknown causal Actor fields, defining previously
unknown command flags, negative MP widths, maxHP irrelevance, dead-Actor MP
non-read, fallback activity, duplicate aliases and undefined bitmap read masks.
They do not create new source reachability evidence. On each first own call,
seventh-HUD-Blt, HUD-checkpoint and precommit failures must restore entry and
buffered environment; one retry in the same session must recover the complete
return. Existing parent lifecycle and whole application rollback checks remain.

## Verification and preserved limitations

Task evidence: `build/research/application-owned-active-hud-native-20260914/`.
Initial clean parent is0c1e6d4344e9c13a0de58108119281d0b1f67b5f.
Candidate3 pins1012 Native files;1008 prior files remain byte/mode identical.
Only CommandsTests gains optional callback forwarding; three new HUD files
supply the comparison. All383 fixtures and102 resources remain unchanged.

Candidate1 was rejected before Native execution: its HUD-family observation
case lacked a guard against events outside the commands/HUD interval. Candidate2
adds that guard; the first frozen code, manifest, plan and review remain preserved.

Candidate2 built successfully in358.19s and completed both48-call source and own
HUD comparisons, but its two negative Object-token controls failed because
XCTUnwrap records a thrown argument evaluation before XCTAssertThrowsError handles
it. Native job2 is terminal exit1 and not accepted. Candidate3 reads that causal
field before the nonthrowing dictionary unwrap. The failed code/job/logs remain
immutable; no source expected, production rule or compared field was changed.

Selected8 release methods: two HUD, two Commands and four WorldHUD, including
1753 controlled active cases,1789 paused cases and two late resource/observer
failures.151 unaffected method identities are inherited by exact accepted pins;
with8 directly selected methods this yields159 unique retained methods. This is
not a sum of branch coverage or independent full-game validations.
Candidate3 passed all8 methods in270.723s after354.74s build. Commands control/
primary took77.542/56.492s; HUD control/primary60.900/60.923s; controlled active/
paused7.361/7.503s and the two late failures0.001s each. Both owned variants have
5952 events/960 undefined reads, separately derived from their own current
records; the saved control source's8640/2496 are not copied into owned output.
Six new late rollback checks and two same-session retries passed. Parent late
lifecycle/application checks also remain exercised by the whole chain.

`verification3.json` verifies all1012 current/frozen Native bodies and modes,
1008 unchanged prior files,383 fixtures,102 resources and485 actual package files.
Native archive has1012 members and4,442,378,240bytes, SHA256
`f98c7a83a27c22ac19114cba0a1a4fd719e933d40d1aee03689d5551c37ec820`;
every member body and mode was read back. The evidence archive separately retains
both rejected candidates' four changed code bodies, manifests, plans, reviews,
Native2 failure logs and restoration recipes tied to the verified Native3 archive.
The initial archive draft's missing old-body/manifest/review linkage was corrected
before execution; both drafts and the identified preservation gap are retained.

The raw successful log remains immutable. Its diagnostic view separates31 fully
identified XCTest metadata spans to parse192 duplicated parent observations;
no gameplay record is replaced or inferred. Successful Native3 is terminal,
PID78687, started2026-09-14T03:34:44.695324Z and ended03:45:12.566097Z,
exit0. It is not restarted for publication. Detailed gates and independent
reviews are linked by [evidence](../evidence/application-active-hud.json).

Reader1 failed by comparing raw catalog wrapper addresses with normalized saved
event ordinals. The failed reader/log and diagnostic are preserved; reader2 maps
addresses through the current before-state bitmap list, as in the accepted
active graphics reader. Source expected data were unchanged. The independent
reviewer's truncated diagnostic output and indentation error are likewise retained.

No original/emulator/historical producer/capture/auditor was executed. Old safety
incidents stay open. Actual pixels/Windows/device behavior, broader gameplay,
full active tick, complete match/game, app/window/input/audio and clean-Mac delivery
remain open. The EXE envelope has not been recalculated.
