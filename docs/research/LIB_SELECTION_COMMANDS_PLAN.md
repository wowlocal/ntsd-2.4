# Retained VS reselection and random character commands

Extend [LIB_SELECTION_STAGE](LIB_SELECTION_STAGE.md) through the ordinary
settings options1 (reselect) and2 (reroll), then return through the complete
library character menu. Recover their call-site counter values, live selected
roster and shared RNG consumption, latch/hold/release order, subsequent human
and computer selection, text/DC and whole-call rollback.

Reference artifacts are the same pinned NTSD EXE
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
bundled lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
and original DAT/BMP/DIB inputs. Read-only disassembly identifies the source
branches and stack operands before executing a finite ordinary-button manifest.
Controlled Unicorn2.1.4/CW023f execution uses the accepted declared initial
World/400Actor/catalog-name/portrait/arena views, verified CRT-derived RNG tables
and API/COM/GDI boundaries. It runs the actual41bc90 prologue and4229cc
continuation through fresh/cached429730 and422ab8ret4; the earlier tick body,
full library initialization, Windows and devices remain separate.

Retain every preceding call's complete state, owners, library DC and source
stack. Only the next ordinary button bytes and three declared ABI words change
between calls. The finite manifest must be saved before capture. It includes
the existing count/CPU/team/settings route followed by repeated reroll with
held input, reselection and a new human/CPU ready route. Both controlled teams
and CRT seeds are retained. No expected after-state, unknown scratch or source
pointer becomes a Native input. Actual tagEA RNG and candidate lists must run;
the existing shared continuation is reused only after verifying the full caller.

Acceptance requires every declared return or explicit match-prelude boundary,
full World/400Actor/globals/owned resource bytes and masks, ordered events,
caller counters, helper returns and independent replay of all stores/reads and
checkpoints. Compare the unchanged prior selection corpus after shared Core
changes. Inject a late error after a reroll has changed earlier seats and after
reselection state writes; verify full current-call rollback while preserving
prior calls. Preserve all259 old fixtures and frozen/pending/foreign files.
Use a new isolated Native package based on the accepted658-file composition.

Empty candidate backing, other modes and match preparation are not silently
accepted or skipped. No deliberate pointer/protection corruption, arbitrary
post-fault execution or safeguard retry is part of this study. Preserve actual
errors/refusals and stop retries of a refused operation. The long catalog5
process is independent: revalidate its real handles and keep its immutable
inputs and6GiB storage reserve unchanged. This study cannot establish a full
match, application/device output, Windows or clean-Mac/all-content completion.

The saved finite manifest contains338 calls (170 primary/168 control). Both
reproduce the accepted sequence through the second CPU's ready release, then
reroll twice (including held input), select option1, hold/release reselect,
rejoin/reconfirm both humans, accelerate their countdown, reconfirm two CPUs,
reroll again and follow the original arena/music/Start suffix.336 actual ret4
returns and two unreturned42cf8a boundaries are required. The whole preceding
prefix must reproduce the immutable capture3 parent, not only its final state.

Static42df7c..42e0b0 shows the reroll writes SP20 as a World Actor-table cursor
and SP34 as a byte offset. Their final values are World+1b4 and32, even when a
seat is not random. Native retains the semantic World-relative cursor, not a
source address. Reselection changes no tracked caller counters. Static evidence
is separate from the fresh dynamic and Native comparisons.
