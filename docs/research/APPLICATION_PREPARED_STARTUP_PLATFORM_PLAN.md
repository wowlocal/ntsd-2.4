# Production prepared startup platform

2026-09-26; base HEAD b12ebbd. Previous goal turn was progress: the unchanged
2247-file Host inspection candidate passed all eight methods and full archive gates.
Continue that actual candidate, manifest SHA256
3a3595560f2a8f82c637c356401cc946330eb6eec2f6f7cbda14dbdd4c9b1242;
do not start from root1034 or a HEAD-only checkout. Prior jobs are terminal/absent.

## Game result and implementation contract

The recovered whole startup and Host currently depend on an XCTest-only platform
which combines replies with reference comparison. Implement a production Core
platform for the same coordinator/Host: immutable bundled original inputs plus
explicit prepared nonwindow observations, owned consumption positions, staged
panel output and retained opaque resource owners. This removes the test adapter
from the runtime construction path toward the Naruto/Sasuke District match.
Physical device producers remain explicit follow-up dependencies, not fabricated
success defaults. This is a Native ownership/adapter contract, not a new EXE rule.

Reuse the existing provider inventory and window exchange. No new execution
framework, source capture, general IO exchange or duplicated startup algorithm.
The new class conforms to OriginalApplicationWindowStartupPlatform. Its fresh
stagedCopy has independent value positions/output/cursor storage. Immutable package
bytes and immutable prepared values may share backing; opaque owners are retained
only and never invoked or mutated during copying/Core calls. Window replies use
the existing cursor, including its request suspension and complete-byte matching.

Every nonwindow response is supplied explicitly at its existing boundary. Use
typed value entries for clock/critical section/COM, panel write/close/allocation/
bitmap/device, calendar clock/zone/allocation/name conversion, music/cursor/joystick
and WAV aggregate inputs. Match request arguments before consuming an entry; missing
or mismatched inputs throw without advancing that position or appending output.
Expose consumption validation for the enclosing beforeCommit callback. Preserve
numeric failures, nil outputs, absent versus empty file inputs and raw response
bytes. Do not call host IO, Foundation calendar/codepage or live device APIs in Core.

Files come from OriginalApplicationStartupInputs with explicit prepared overrides
for alternate/error input cases; unsupported names remain errors. No fixture or
expected state reader is part of the production platform. Allocation backing and
logical identities are declared Native reservations passed to the existing owned
children; do not import private source ABI or derive allocations from after-state.
Music helper/format notifications have ignored return values and no physical API
response; preserve their caller order while requiring prepared replies for terminal
requests. Sound/WAV aggregate controls are still the existing aggregate contract,
not a newly established per-call live mixer. Retain resource owners through Host
publication and delayed delivery; Native rollback does not undo physical work.

## Evidence and finite acceptance

Only original NTSD supplies behavioral evidence: EXE
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c;
lib28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba;
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d.
Read WORKFLOW/CURRENT_WORK/TASK_TEMPLATE, prepared-backend and Host app preflights,
Host inspection/startup, WINMAIN_STARTUP, STARTUP_PANEL/OUTPUT, calendar/music/WAV
and their direct Native consumers. Archive constraints1174–1235,1650–1831 and
relevant storage/ownership rules remain binding. No original/Windows/device runs.

Two new files only: OriginalApplicationPreparedStartupPlatform.swift and
OriginalApplicationPreparedStartupPlatformTests.swift. All old algorithms,
comparators, tests, masks, expected bytes and packages remain exact. A test wrapper
may send actual arguments/stores/observations to the unchanged reference adapter;
only the production platform's replies feed Core. The audit object is test-only,
is independently copied and never supplies Native state. Prepared values derive
from declared spec/API inputs; saved allocation backing is the separately recorded
pre-allocation input, not saved allocation bytes/masks. Source event request names
bind controlled file/device inputs, not expected after-state.

Three new whole methods, followed by all eight unchanged inspection/startup methods:

1. All35 WinMain parents through the actual new platform/coordinator/Host, preserving
   23 complete/5 source-stop/7 provenance outcomes, full globals/write order/masks,
   owned calendar/music/panel/WAV results, full operations/graphics,650 window replies
   and retained publication state. Do not count stops/rejections as matches.
2. Six late failures/retries on the same Host, preparation/publication copy and
   independent staged positions/output, retained resource lifetime after coordinator
   and Host destruction. Previously served window replies are not served again.
3. Finite missing/mismatched/nonconsumed prepared-input controls, all response
   families, absent/empty/unsupported file distinction, before/after handoff and
   rollback. Bounds and response matching must reject before position/output change.

New methods are selected before edits; comparator and original test bodies remain
unchanged. Author review is not independent review; none is available in this
session. Syntax is an implementation gate only. Fresh build/package/all11 complete
methods and independent review are separate later gates on the frozen candidate.
Preserve any failure and diagnose before a separately identified correction; at most
three implementation rounds before contract review. No automatic source rerun.

## Task ownership and storage

Task/alias application-prepared-startup-platform-20260926 under
/Volumes/X5/ntsd-2.4-research/01a0dc49-738f-7972-8fb0-e98fb2f34408/.
Verify writable APFS UUID3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548 and at least65GiB
external/9GiB internal before cloning. Preserve40/6GiB reserves plus17GiB source
commitment. Implementation allowance4GiB physical, stop3584MiB observed decrease,
16GiB logical,7200s,metadata16MiB,root2MiB. T7 remains separately authorized;
these APFS-dependent clones stay on X5. Fresh build/test gets its own bounded task.

Reuse the existing preparation/syntax/patch-roundtrip/APFS/PAX closure procedures.
Verify root1034/base2247/source55, old test files and new candidate membership.
Archive full regular candidate and all implementation metadata; retain modes and
nanosecond mtimes. Revalidate PID/start/command/cwd/job before process action.
Only the task, two new candidate files, task tools, plan/study/evidence and own
navigation additions change. Existing producers/plans/archives stay frozen.

Publish syntax, implementation, preservation and archive separately from compilation,
comparison, independent review and application/device acceptance, then commit.
Source59727 remains terminal at34 Objects; full137/transport/provenance, existing
safety incidents, physical backend/callbacks/loading messages/fills/audio, Windows/
clean-Mac/full match and complete game remain open. EXE envelope not recalculated.
