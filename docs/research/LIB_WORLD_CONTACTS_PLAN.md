# Bundled library contact collection — finite acceptance plan

Recover the game's candidate-contact filtering at installed4176ac→10001807
and4177b9→100011b9, inside whole417400/ret12 and419380/ret4 with its fusion
tail and caller41eed8..41eefb. This stage collects contacts; damage and the
initialized library-enabled gameplay join remain separate.

References: pinned NTSD2.4 EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, bundled
lib.dll SHA256`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
and immutable [WORLD_CONTACTS](WORLD_CONTACTS.md). Unicorn2.1.4 executes the
actual DLL installer using the committed research loader at36000000, declared
76HIGHLOW relocations and successful allocation/protection/copy API responses.
Those responses are not actual Windows loader or permission evidence. Native
does not load DLLs, patch executable memory or emulate instructions.

Low-level execution is needed because the hooks resume inside a live pair
loop: attacker slot is root entrySP+4 (=currentSP+44 after0x30 local bytes/four pushes),
defender is entrySP+8. The state20 hook uses the attacker's current70 frame,
preserves EDX kind and resumes inside the old effect check. The kind802–807/809
group reads/computes a category comparison but does not branch on it. Recover
these continuations and data provenance rather than infer rules from names.

Acceptance fixed before source capture:

- Reexecute all7925 accepted declared inputs through the installed library;
  compare their complete original outcomes separately with immutable controls.
- A finite matrix of every new kind and adjacent unhandled values, all original
  type categories plus signed/outside-category controls, invulnerability and
  nearest/multiple selection. State20 covers team equality/zero/difference,
  effect0/21/22, eligible kinds, defender types and state10/13 bypasses, with
  state3/18/21 controls. Additional whole callers exercise both directions,
  multiple ITR/BDY, slot aliases and late slots; no arbitrary pointer corruption.
- Check actual hook entry/continuation registers, stack/ret and saved registers,
  CW037f/empty terminal FPU stack, whole400Actors/World bytes and masks, full
  globals, ordered real RNG/reconstruction events and immutable Object/box data.
  Retain full expected bytes as lossless blobs and actual instruction inventories.
- Native shared contact pass has an explicit library selection and an atomic
  public wrapper. Preserve pristine behavior. A late RNG-observer error must
  roll back staged World/Actor/global changes; unknown data remains rejected.
- Raw and packaged release comparisons plus retained pristine contact tests;
  independent transport/blob/SHA/prior-fixture and vendor checks. Publish only
  terminal successful jobs and separate limitations. Source faults or safety
  refusals, if any, stay distinct and are not retried automatically.

Synthetic storage derives from original constructors and declared four-Object
inputs. A read-only original DAT/catalog census will distinguish real kind/state
data from synthetic controls. Neither corpus proves every branch outcome,
natural runtime reachability, damage, a full match, Windows or device behavior.
