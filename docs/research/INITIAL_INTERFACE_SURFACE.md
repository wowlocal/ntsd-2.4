# Initial loading UI with whole bitmap surface helpers

**Ten controlled source/native matches; standalone packaged acceptance passed.**
Ten controlled original World/pool/UI chains reach41c581 with the whole bitmap
constructor, image/surface loader and copy helper. Native compares all ten
through `OriginalInitialInterfaceLoading.loadWithSurfaceLoading`. The pending
[own application loading](APPLICATION_LOADING_PREFIX.md) is a separate running
study: these inputs supply catalog[0]/Object+90 and the outer caller context.
This is not an own catalog continuation, full41bc90 return or window test.

The [finite plan](INITIAL_INTERFACE_SURFACE_PLAN.md) declares the reference,
controlled environment, purpose of instruction/memory observations and acceptance
boundaries. Earlier [initial-interface](INITIAL_INTERFACE.md) and
[bitmap-surface](BITMAP_SURFACE_LOADING.md) corpora remain unchanged. The former
retains its explicit43ed10 device-result API; the new route composes the whole
helpers without replacing those old expected results.

## Executed original behavior

Pinned original EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
MSVCR80 SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
and all ten original embedded UI DIBs. Unicorn2.1.4 executes World419e40,
400-slot allocation/initialization and staging41c052..41c2f5, UI41c2f5..41c581,
43ee50/43ed10/4013d0 and actual CRT memset. No executable/DLL is used by the
native shipping implementation. API, allocator and outer caller responses are
controlled research inputs, not Windows or physical device behavior.

After the actual World constructor, selector2, catalog[0]/Object+90 and the
outer caller frame are explicit inputs. The pool and UI then execute continuously
on the same CPU and stack. Allocations are disjoint from that VM's prior mappings;
World/catalog/Object use74800000 and Actor records75000020 with500 stride.
Two nominal patterns include reversed physical allocation order. Actor allocation
is nonnull in this finite domain; NULL Actor allocations remain outside it.

| Control | Original result at41c581 |
| --- | --- |
| Nominal A5 and ramp/reversed | All ten wrappers/surfaces are produced |
| All/alternating NULL bitmap allocations | Skip the corresponding constructor, store0, continue |
| First/last image absent | Both image attempts return0; report required-resource failure and retain wrapper |
| First CreateSurface=-1 | Retain the previously produced width/height, report failure, keep image undeleted |
| Last CreateSurface=+1 | Also fail the exact-zero test; declared returned surface remains unused/unreleased |
| Last SetColorKey=-1 | Message/debug, Release request, clear surface word; dimensions and wrapper survive |
| Last GetDC=-1 | Skip StretchBlt/ReleaseDC; request DeleteDC and DeleteObject; constructor still sets color key |

Each path stores all ten global bitmap pointers before clearing44d05c. A failed
image or surface operation still returns its wrapper; a null allocation stores0.
Previous global pointers are replaced without Release on this caller path.
After the pool, all400Actor and World records remain unchanged through UI.
Source ESP finishes1000f000, EDI12345678 comes from the declared outer word;
41c581 is unexecuted and is not an epilogue. Captured initial/before-pool/
before-interface/final control words are0; this controlled CW is not own startup
037f, a host FPU measurement, or an assertion about actual Windows flags.

There are85 real bitmap constructors/loaders,83 CreateSurface requests and81
copy calls. All1,406 platform requests,100 bitmap allocations and85 constructor
entries compare in order;400Actor allocation requests and the declared selector
bring the full source event list to5,601. Full helper witnesses number8,514:
4,080Actor initializations,10World,4,173memset,85constructors,85loaders,81copies.
Normal bitmap cookie checks and actual ret12/nonvolatile registers execute.
Actor406489 sets EAX=fffffc18, which survives its4064cc return; this helper does
not return `this`. No private C++ ABI equivalence is claimed natively.

Executed original PC union is733EXE+35CRT=768. The pool executes155 of156 static
starts; its NULL-Actor `xor edi,edi` at41c0b9 is absent. All162 UI caller starts
execute across the finite cases. This is instruction-start coverage, not every
branch outcome of every helper, CPU/device measurement or full-game evidence.

## Native ownership and comparison

[OriginalInitialInterfaceSurfaceLoading.swift](../../native/Sources/NTSDCore/OriginalInitialInterfaceSurfaceLoading.swift)
stages the loader, globals and a value-semantic platform context. It calls the
same interface caller with its own allocation backing/device/path, then the
recovered whole image/copy constructor. Thrown dependency/observer errors retain
all three inputs; callbacks must buffer external effects. Four native-only
failures at the tenth CreateSurface, DeleteObject, SetColorKey and final global
store verify rollback after earlier successful resources. They are not extra
successful original calls or a proof of whole pending41bc90 rollback.

The older callback/default routes are retained in
[OriginalInitialInterfaceLoading.swift](../../native/Sources/NTSDCore/OriginalInitialInterfaceLoading.swift).
They compare the same13 earlier source cases, not26 independent executions.
The small shared `checkedConstruction` validator checks required path, extent
and canonical surface binding; it does not discard dimensions when the surface
word is0. The new native path never invokes the older image-result providers.

The comparison constructs its own World/Actor records from declared allocator
patterns and actual native rules. Supplied initial globals and catalog word are
controlled inputs, not an own loaded catalog. Original DIB bytes produce native
BITMAP responses; native CreateSurface descriptors produce subsequent surface
queries. Captured API numeric results and handle tokens are declared responses.
No expected World/Actor/wrapper after-state or private stack bytes are imported.

All4,095 full records/4,925,440 bytes plus masks compare:10World,4,000Actor and
85bitmap records. Only established World/catalog/Actor/surface pointers bind
ordinals. Other opaque bytes remain unchanged. The12,036 private API bytes whose
ownership is unknown stay0/false natively and retain original bytes separately;
they are not converted into known native padding. Resource dictionaries record
specified API results and cleanup requests, not actual Windows destruction.

## Evidence and validation

[oracle_initial_interface_surface.py](../../tools/oracle_initial_interface_surface.py)
keeps complete raw source fields and ten atomic parts. The nominal single-case
probe exactly reproduces the first final case and all its blobs. The independent
[verifier](../../tools/verify_initial_interface_surface.py) checks every one of203
blobs,10 DIBs and768 instruction bytes against pinned source artifacts. It replays
all1,164,929 writes/4,142,637 written bytes, validates1,536 retained state/API
global snapshots and4,095 full records/masks, and merges56,762 reads/214,529 bytes
with preceding writes. No observed read byte is undefined in that captured
history; this does not establish ownership of unread/private API padding.

An initial verifier assertion incorrectly required Actor EAX==this. Original
406489..4064cc and all4,080 captured returns establishfffffc18; only the verifier
was corrected. A packaging helper initially used a zlib API unavailable in host
Python3.9; `compressobj(wbits=-15)` fixed transport compatibility. Both errors and
their records remain in `build/research/initial-interface-surface-work.json`.
Neither was a source/native mismatch or a safety refusal; source data and game
rules were not changed to make comparisons pass.

Raw comparison12release tests passed5.028s/build210.34s, including the new two
tests1.513s, earlier interface, bitmap-surface and bootstrap tests. Native
NTSDNative linked; no window was exercised by that job. A separate minimal
package is based on4545435 with only six code/test deltas and one fixture,
excluding all unfinished catalog changes and six foreign files. Its619 other
base files are unchanged. Final packaged12tests passed5.348s/build221.07s without raw overrides.
NTSDNative linked; all source/native jobs belonging to this controlled study
are terminal. The three independent own catalog captures remain live.

Raw96,729,868bytes SHA256
`703086bd4b03017ca5aebfed49af80472cc0e1ee960f60c259adb031b81356dd`;
lossless packed9,086,144bytes SHA256
`7f739a9a72c7fc66c740a5dae8afaa4364219b5c11ce24d2962593b3c2fa7df3`.
All original fields, expected bytes, masks, reads and writes survive the complete
byte/JSON roundtrip. Packing is research transport, unrelated to game codecs.
The ten atomic parts,625 raw-build input files and626 standalone-build input
files have full independent SHA verification. All246 older fixtures remain
unchanged. [Evidence report](../evidence/initial-interface-surface.json).

```sh
uv run tools/oracle_initial_interface_surface.py --output build/original/initial-interface-surface.json
uv run tools/verify_initial_interface_surface.py build/original/initial-interface-surface.json --output build/research/initial-interface-surface-fresh-verification.json
swift test --package-path native -c release --filter 'OriginalInitialInterfaceSurfaceTests|OriginalInitialInterfaceTests|OriginalBitmapSurfaceLoadingTests|OriginalBootstrapTests'
```

Own catalog return, own pool/UI continuation, full41bc90/application loop,
Windows/NLS/workers, actual window/devices, complete Naruto/Sasuke District match,
all content/network/replays and clean-Mac acceptance remain open. The three
application catalog source captures continue independently; this controlled
milestone does not change or restart them.

The [pool/UI composition](INITIAL_POOL_INTERFACE.md) now stages both Actor
allocation/construction and this complete UI loader in one native operation.
Its new constructor-return comparisons replay the same immutable source writes;
no fixture or expected byte changed. This adds a controlled native composition,
not the still-pending own application-catalog continuation.
