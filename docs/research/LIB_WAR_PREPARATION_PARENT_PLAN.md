# War preparation: parent and first-return preflight

The [806-call setup milestone](LIB_WAR_SETUP.md) is accepted at
`b6fe14e0c201258a23239d3b44ae3e4826d0cc65`. Recover the first complete War
Start preparation after43a21f: World activation clearing, arena selection and
layer ownership, human/CPU Actor reconstruction, music/input and replay
initialization, then the actual439ecd ret1c and422ab8 ret4. Existing
[preparation inspection](LIB_WAR_PREPARATION_STATIC.md) is static evidence only;
its references to unavailable X5 describe the earlier interruption.

This finite preflight has22 calls: eleven primary and eleven control. It is a
new continuation study, not a restart or replacement of completed source2,
cell or multiple-seat corpora. It must establish original parent provenance
and the actual complete preparation path before the larger preparation matrix
and Native composition are frozen. It does not accept all War preparation.

## Artifacts, environment and observations

References remain the original NTSD EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, and VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Use controlled Unicorn2.1.4/CW023f, C locale, original DAT/BMP/DIB resources,
and normal declared allocator/GetLocalTime/COM/GDI/music responses. Execute
actual41bc90 followed by the declared4229cc tail and installed library.
This is no Windows/device, complete app initialization or played-battle claim.

Full instruction/read/store/mask, stack/register/FPU, helper-return and resource
traces are necessary to recover intermediate writes, pending-push destinations,
CPU slot+10 versus human slot, stream123/125/127 ordering and replay ownership.
Do not replace intermediate floating stores with final positions in the source
trace. Native may not import private stack or unknown resource backing.

The source harness must allocate arena wrappers separately from its two already
owned War wrappers and retain the shared synthetic image/surface/DC counters.
These are declared emulated allocation/device responses, not Windows heap or
COM identity measurements. Observe original loads/releases and accesses through
these bindings. Do not overwrite a live wrapper to simplify allocation reuse.

## Exact input sequence

Each chain begins with the original resource-parent return and declared ready
bridge from the accepted setup producer. Primary cases0/1 must reproduce old
source2 cases0/1; control cases11/12 must reproduce old412/413. Compare every
complete call and constructor byte/mask. Only the control constructor metadata
`firstCase` annotation moves412 to11 in a comparison copy. Source expected
records and the four old case files remain unchanged.

From actual menu200/row6/cursor2/side0, supply the following separate edges to
human seat0, retaining all World/Actor/global/resource/stack history:

1. Attack press/release enters menu202 at option2.
2. Attack press/release requests and completes live Random selection.
3. Up press/release moves to option1.
4. Up press/release moves to option0.
5. Attack press takes Start through43a21f and the whole outer return.

Together with the two parent calls these are eleven calls per chain. The
primary retains two humans and six Random CPUs; control retains eight Random
humans. Random must finish before Start, preserving actual selected Object
provenance. No troop, cursor, input-global action flag or preparation after-state
is supplied to force a path. The existing ready bridge remains declared input;
earlier mode4 character/CPU selection reachability stays open.

Both Start calls use declared local time `[2026,9,5,11,12,34,56,789]` and normal
successful allocation/COM responses. This preflight does not intentionally
create source faults or vary protected/control-pointer backing. A later finite
matrix will cover required ordinary resource failures and other preparation
inputs with separate evidence.

## Finite acceptance and preservation

Require22 complete outer returns,20 War returns and two full preparations.
Verify eight participant reconstructions per preparation, distinct human/CPU
slots, Object/team preservation, controller fields, troop multipliers and all
intermediate numeric writes. Check that the team pass follows the real400-byte
activation clear. Record actual arena choices/streams rather than asserting an
unobserved result. Verify whole layer ownership, date/`_Battle` replay names,
44d034-before450bbc order, resume music, input reset and whole43d2c0 execution.
Start is no longer an unexecuted stop in these two calls.

Audit every original record/mask/read/store/checkpoint and helper return,
including the complete recording buffer and private source-only stack. Verify
all executed instruction bytes against the pinned artifacts. No claim follows
from counts alone, decoded-but-unexecuted starts or an explicit Native rejection.
Require exact first-two-parent reproduction for each chain before accepting
its continuation. Freeze the22-entry manifest and producer hashes before
execution and atomically save each completed case before proceeding.

Use a new task-owned X5 directory and retain the6GiB reserve. Preserve all316
accepted fixtures,745 Native package files and23 unrelated pending files. Do
not restart completed setup806 or catalog5, replace old expected observations,
continue a source fault or retry a safety refusal. Retain errors/time/session
and revalidate actual process handles before subsequent actions.

A successful preflight establishes two controlled original preparation returns.
Whole Native preparation, the finite arena/participant/recording matrix, late
rollback and packaging remain separate requirements. War43a860 gameplay,
own catalog/library/outer loop/app, full match/content/network and
window/input/audio/Windows/clean-Mac checks remain open.
