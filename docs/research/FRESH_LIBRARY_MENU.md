# Fresh resources through the installed-library menu return

OriginalModeMenuContinuation now exposes staged music, resource and startup
checkpoints within its existing transaction. The complete fresh path is directly
compared on one original CPU: music, graph-log formatting, eleven bitmap
constructors, installed-library screen/panel, network/overlay/volume/presentation
and actual422ab8/ret4.22 controlled returns and two playback continuations match;
two NULL-SPARK boundaries are explicit native rollback rejections.

This joins [fresh music/bitmap startup](CHARACTER_MENU_MUSIC_SURFACE.md) to the
previous [cached library menu return](LIB_MENU_CONTINUATION.md). Both prior
corpora remain unchanged. The [finite plan](FRESH_LIBRARY_MENU_PLAN.md) declares
26 cases before capture. These are controlled fresh menu entries, not an own
catalog or complete initialized tick.

## Reference, inputs and execution boundary

Pinned NTSD EXE SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
lib.dll SHA256
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
VC80 SHA256
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
and all11 original embedded menu DIBs are the reference. Unicorn2.1.4 executes
original instructions. The accepted library installer, CRT/PTD attachment and
COM/GDI/image/allocator/time adapters retain their declared research scope;
this is not Windows startup, device output or pixel evidence.

Actual41bc90 creates its normal aligned frame, cookies and SEH chain from
SP1000f000. At41bcd0/SP1000e9bc, declared World22000020 and target26006000
supply the4229cc continuation. Earlier phase/input/gameplay instructions remain
skipped. Actual429730 then sees currentMenu10/previousMenu0/loadFlag1 and executes
402020 and its music children, actual VC80 sprintf,43ee50/43ed10/4013d0 and
actual CRT memset for the eleven constructors. No successful helper is stubbed.
The inherited constructor-only memset adapter is explicitly inapplicable;
4450a0's original import thunk and pinned CRT instructions execute instead.

The mode screen and complete panel follow on the same CPU and stack. Normal
returns traverse screen ret16,42e0f9/ret12 and422ab8/ret4, restoring
SP1000f008, saved registers, the declared original FS-chain word12345678 and
CW023f. The outer424746 caller and its held clear remain unexecuted. Playback
stops before43249c with the screen and menu still pending. NULL-SPARK stops
before429b21's write through zero; it is not an executed null-write fault.
No protection structure or control pointer is damaged or bypassed.

Both controls retain the prior declared World/nine Actors, common five bitmap
records, sparse panel atlas and two replay allocations. Fresh menu wrappers use
guarded A5 or byte-ramp allocator inputs, with reversed wrapper addresses for
the second control. The source maps their backing before execution but marks it
not live until each actual allocation response. Native allocates these through
its own callbacks; it does not import them into the common presentation owner.
The optional26-byte wide music allocation has the same explicit lifetime.

Per control,13 cases cover nominal, confirmation/background release,
network+overlay, volume-up, help, panel click, playback, positive final surface
result, missing RFACE with the next failed description, failed graph creation,
failed rendering, NULL wide allocation and NULL SPARK. Wider standalone helper
and screen domains remain in their accepted studies. New menu resource records
remain independently owned; this selected mode-screen path does not consume
those new wrapper records as draw operands. Other character-menu bodies and
fresh own catalog composition remain separate work.

## Stack lifetime and native comparison

Fresh graph-log/bitmap helpers leave defined bytes in the address span later
used for the mode screen's local storage. Complete historical source masks
remain intact.22 compared paths end with695 historically defined bytes in that
span; the failed-graph pair has553. Only108 bytes per path are written by the
current screen. The remaining587 or445 bytes belong to previous helper
lifetimes and are unread by this screen.

An independent ordered write/read audit verifies all64,510 instruction reads/
65,728 bytes and312 API string reads/5,124 bytes inside the screen local span.
Every consumed byte was written during that screen invocation. No previous
helper local is needed as a native operand. The native comparison therefore
checks all108 current byte values and their exact current write coverage,
retaining zero/unknown native storage elsewhere. It separately preserves and
verifies the full source historical bytes and masks; it neither replaces them
nor claims private stack ABI equivalence. There is no expected-state import.

Native music and bitmap scratch retain their own graph-log and subsequent
GetObject/description dimensions through loading, as established by the prior
startup study. Embedded DIB dimensions and successful description outputs are
independently reconstructed from original bytes and preceding descriptor
requests. Current-call API masks and whole-stack historical masks remain distinct.
The92 undefined fill-padding bytes likewise keep separate native/source backing.

All24 matching continuations compare9,184 ordered events, including5,592 startup
events,264 fresh bitmap records/2,116,224 bytes plus masks and20 music allocations.
Their2,188 helper returns cover2,218 EXE+51 library+439 CRT instruction starts.
Startup includes616 music events,264 allocation/constructor pairs,528 image
requests and522 GetObject requests. The following screen/output includes278
Blts,134 complete text/GDI sequences,56 key labels,22 panels and four overlay
formats. Source requests and native owners are compared in their actual order.
Numeric API failures preserve the original continuation behavior.

The whole26-case source audit checks80,348 stores/346,674 bytes,113,986
instruction reads/247,772 bytes and1,634 API reads/13,620 bytes. All962 final
regions/37,951,212 bytes plus masks and550 points/20,350 regions/802,814,100
bytes plus masks reconstruct from the declared initial state and writes.
All645 blobs/29,638,724 decoded bytes and26 atomic parts verify, together with
the completed nominal probe. The full source corpus contains2,280 helper
returns and2,219 EXE+51 library+439 CRT starts. Only854 false global write-mask
reads occur, all at pinned EXE cookie44eea4. No every-branch/CPU/device claim follows.

Two NULL-SPARK cases verify rollback after music and ten completed bitmap
constructors; they are not successful returns. Seven additional native-only
observer failures occur after music, after all fresh resources, after screen
background release, at network text, the fifth volume method, presentation and
the pre-return checkpoint. Globals, World, common resources, fresh bitmap/music
owners, local strings, library DC and the caller event buffer retain entry values.

## Corrections, validation and remaining work

The first source probe stopped with a Python AttributeError in the inherited
constructor-only memset adapter at4450a0. Its source revision, timestamp,
trace and exact error remain preserved. Observing the actual thunk/CRT path
produced a successful second probe and then all26 cases. No game memory fault,
safety refusal, source byte edit or fault continuation occurred.

The first native run passed all11 retained tests but failed the two new tests
with eight assertions, including two propagated unexpected errors. The copied
test counted12 future allocator buffers as existing common owners (20 vs8)
and conflated earlier helper masks with the current screen's local writes.
Correcting only that comparator, supported by the independent lifetime audit,
passed the new two tests. No Core game rule or expected source byte/mask changed.
The read-only verifier also had two preserved variable-shadowing mistakes:
a final atomic-part path lookup error and an invalid raw-size/hash report using
the last DIB. Both reports/revisions remain; the corrected full audit verifies
the actual33,313,873-byte corpus and its original SHA.

Initial release build230.07s linkedNTSDNative; its13-test run54.106s retained
all11 old passes. The corrected new two tests pass4.667s/build77.29s. Packaged
acceptance passes both in4.476s/build0.28s without a raw override. No app window
or device was exercised. All owned source/native jobs are terminal. Unrelated
existing compiler warnings remain in their original logs.

Raw33,313,873 bytes pack losslessly to4,445,832 bytes. Full bytes/JSON/SHA and
all blobs verify. The254th fixture preserves all253 prior pins. All648 final
package files agree with the independently reconstructed recursive archive
composition, including both initial/corrected test revisions. The raw package
retains645 accepted files and adds one test while modifying one observer API.
See [evidence](../evidence/fresh-library-menu.json) for jobs, hashes and limits.

The separate guarded catalog capture has reached its20-Object checkpoint.
Its frozen native child passes in12.772s:20 Objects,4,995 Frames,194 wrappers,
94 registered WAVs and every observed child prefix. All three independent source
final/snapshot/read audits are still live. The full source capture continues;
no full catalog return or native publication follows from this checkpoint.
Pending/foreign work and all frozen watcher inputs are unchanged.

Next join the own initialized catalog/menu and remaining selected bodies,
then the outer loop and app integration. Full Naruto/Sasuke District match,
application input/animation/audio, Windows, all original content/network and
clean-Mac acceptance remain open. The full native-game goal is unchanged.

Свежий библиотечный вход в человеческий экран и actualret4 отдельно сравнен
в [FRESH_CHARACTER_MENU](FRESH_CHARACTER_MENU.md). Он использует Random-only
контролируемые места и не заменяет собственную последовательность выбора.
