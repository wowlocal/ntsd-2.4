# Retained Stage selection with installed library text

Status: all279 original/Native calls and the independent audit match.
Raw and packaged acceptance passed.

The [bounded plan](LIB_STAGE_MODE_PLAN.md) follows
[the accepted VS caller](LIB_SELECTION_COMMANDS.md). The pinned original NTSD
EXE, bundled lib.dll and VC80 execute only in controlled Unicorn2.1.4 research
tooling at CW023f. Native extends the common character/selection caller and
retains its own resource owners and library DC. No EXE/DLL/emulator enters the
shipping runtime.

Two retained sequences contain279 calls:277 actual422ab8ret4 returns and two
stops BEFORE42cf8a. They begin with fresh11 bitmap owners, raw-derived137 Object/
42 portrait/arena operand views and independently CRT-derived RNG tables.
Primary uses two CPUs; control uses none. Naruto17/Sasuke21 stay selected.
The actual41bc90 prologue and declared4229cc continuation do not execute the
earlier tick body or establish whole own library initialization.

Stage initialization assigns team1 to the first20 Actors. Human character
confirmation immediately reaches status3. Count minimum0 remains legal with
one team. Count wrap, confirmation, cancel to count, and held/released input
retain their order. A CPU character confirmation writes12 then13, clears its
local confirmation, records the Random flag and advances to the next CPU.
Cancel to a previous CPU restores status11, reopening character selection.
The final CPU consumes actual RNG tagd8; zero CPUs consume tagd7. Shared
settings Random consumes tagEA. Each candidate list excludes all eight live
selected ordinals, visits type0/signed ID<30 from ordinal1, and observes every
preceding assignment immediately. Inactive Random seats still participate.

The Stage final-CPU loop retains its Actor-table cursor in callerSP20, ending
at World+1b4; VS uses SP38 at the corresponding boundary. Native derives its
own semantic World-relative cursor. Neither source addresses nor unknown stack
bytes become Native input. Count0 and settings retain the remaining original
caller counter behavior.

Settings first preserve the common Random-arena global write, then floor the
signed Stage value to trunc(stage/10)*10 when its remainder is nonzero. Declared
entry values7/59 become0/50; their full own startup provenance is not claimed.
Frames23/22 overlay the common options. For stage/10 below5, actual42cd5d/42cd6a
produce a digit and NUL at callerSP83c. Otherwise42cd90..42cdad copy the pinned
nine-byte `Survival` literal. Digit text uses x194; Survival uses x174, both y91.
Native produces those bytes from its Stage value without importing local storage.
Option3 advances10 and wraps exactly60 to0. Both sequences traverse all six labels.

Difficulty cycles through its existing four settings. Stage music left or
right toggles ON/OFF while retaining path storage; OFF still stops/seeks music
on subsequent calls, and ignored HRESULTs remain ignored. Library text shares
the preceding DC. Reselect resets all eight statuses/latches, clears selected
ordinals only where the Random flag equals1, and retains Actor Object bindings.
Both chains rejoin/reconfirm, revisit CPU count/readiness, reroll and reach Start.

The source completed0 in71.780s with unchanged inputs. Exact1192975961 raw bytes
have SHA256 `ff9ac413051c58a079430518b381703784665eef9da8cc7f69e8d81d5486a93b`.
All279 atomic completed calls remain retained. No source capture fault or
refusal occurred. The first independent auditor failed while reading a sixth
helper argument: the string pointer was already stored separately, so the
five-word array places y at4 and x at3. Its code/log/error remain immutable.
The corrected auditor also distinguishes d7's candidate at eventStart from
d8/EA's candidate immediately before eventStart. No source byte or game rule
was changed for that correction.

The independent auditor verifies333846 stores/1314811 bytes,616042 instruction
reads/2410432 bytes and9152 API reads/75050 bytes. All6443 checkpoints contain
4935338 records/14302751270 bytes;213714 final records contain619349310 bytes.
Every byte/mask/liveness transition,11827 helper returns,20955 caller counter
words and3004 blobs/156190600 decoded bytes verifies.3551 observed instruction
starts comprise3060 EXE,51 library and440 CRT; this is not all-branch coverage.
5252 undefined bitmap reads retain their original bytes/masks. Remaining false
write-mask reads have pinned EXE-cookie provenance. No unknown observed caller
stack storage is supplied to Native.

All44 live candidate lists verify independently:12 tagd7,10 tagd8 and22 tagEA.
The130 Stage local strings are fully produced by actual stores in the same call:
56 digit1, four each2/3/4, five digit5 and57 Survival. Both zero-CPU confirmations
and both reselection commands verify, and all held inputs consume no RNG.

Raw release acceptance passed six tests in218.308s after a253.24s build:
new Stage whole38.438s and rollback33.668s; unchanged VS command82.655s and
selection63.548s suites also pass. No Core/comparator correction was needed.
An existing unrelated trailing-closure warning remains unchanged. The first
injected Native failure throws on the third d8 return after two assignments;
the second throws after Survival text output. Both preserve the entire current
call's entry World/400Actor/globals, resources/music, library DC and buffered
environment, including all preceding successful calls and supplied buttons.
These are Native observer-failure tests, not original game faults.

The two fresh initial calls also compare against the immutable earlier Stage
parent across14472 shared state records each and331/394 ordered events. Only
declared catalog bindings/global/RNG input differences are permitted, and each
must remain unchanged through every initial-call checkpoint. The complete
parent calls are not identical and are not counted as identical reproductions.

Independent recursive archive verification checks all664 raw Native input files
against the accepted663-file base and the two Core/new comparator overlay.
All263 previous fixtures are unchanged;267 files now include a308-byte index
and three lossless parts totaling154668128 bytes. Complete restored bytes, JSON,
lengths and SHA match the1192975961-byte source. The raw transport DEFLATE stream
is125592681 bytes, SHA256
`09859c809c8b5f72e44bf100d858ce5f1eacc9530d8b2634b11cb5f8081b64f1`.
This DEFLATE is fixture transport only, not game replay compression.

Final packaged release acceptance passes two tests in66.947s after a0.36s build:
whole36.813s, rollback30.135s, without raw overrides. All668 package files and the
new fixture archive verify. Protected14 pending Native/3 pending source/6 foreign
files and all886/887/887/890/890 frozen watcher/catalog input sets are unchanged.
All Stage study source/SwiftPM/audit jobs are terminal. NTSDNative linked; no
window/device/Windows evidence follows. The separate catalog5 remains live,
last observed9 Objects. No source or Native game/comparator correction was
needed; the preserved auditor argument/event-index correction is described above.

Large development captures and the isolated package use the task-owned X5
folder through build/research/lib-stage-mode. Committed fixtures remain regular,
self-contained repository files; the app acquires no X5 runtime dependency.
The separate catalog5 remains live and must be revalidated by actual process
identity. It was not restarted or altered by this study.

This comparison covers the finite ordinary-button sequences, not every branch
outcome. Modes2/3/4, match preparation, own full library initialization and outer
loop/app integration remain open. Full match, input/audio/window/Windows,
all-content/network and clean-Mac checks remain part of the full goal.

See the [producer](../../tools/oracle_lib_stage_mode.py),
[independent auditor](../../tools/verify_lib_stage_mode.py), and
[Native whole-call comparator](../../native/Tests/NTSDCoreTests/OriginalLibStageModeTests.swift).

The [machine evidence](../evidence/lib-stage-mode.json) pins captures, plans,
audits, immutable overlays, packaged acceptance and the preserved auditor error.
