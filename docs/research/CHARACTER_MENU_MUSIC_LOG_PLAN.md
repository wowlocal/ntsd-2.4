# Music log storage consumed by the first menu bitmap

The initial14 continuous controlled calls all reached429e5a. In the first
GetObject failure,43ed79/43ed94 read known width/height bytes5453445c/67726170
from the preceding actual sprintf output. The log starts at1000dee4, exactly
the later loader's24-byte BITMAP record; bytes4..11 are consumed. The first
failed copy descriptor still reads eight initial unknown bytes at1000dde8/ec.
Keep these cases separate. Last-writer evidence is in the immutable raw capture
and build/research/character-menu-music-surface-log-provenance.json.

Extend the same pinned NTSD/VC80/DIB/Unicorn continuous menu-entry study by seven
ordinary controls: failed first GetObject after negative RenderFile, NULL wide
allocation, failed graph creation, cached main track, and music disabled; then
the same GetObject failure with empty and40-byte declared directories. The two
directory cases remain far below the existing260-byte format buffer boundary.
The native string producer and loader's subsequent successful GetObject outputs
must own their own retained bytes. Keep partial write masks and reject a field
when any byte has no recovered native producer. Source platform replies remain
declared inputs, not proof of what Windows accepts for unusually large sizes.

The original14-case producer is preserved verbatim in the initial-source copy.
The producer's only input generalization makes the existing declared directory
configurable; its default stays identical. All existing expected bytes/captures
remain immutable, and no live catalog producer or imported helper is changed.
No source/protection corruption, fault continuation, bypass or unrelated target.
Unexpected errors retain evidence and stop. Acceptance combines all14 prior
cases and7new controls under separately stated match/unknown-operand contracts,
independent full-byte read/write provenance and native rollback. All full-game,
own catalog/menu return, app and Windows/device requirements remain open.
