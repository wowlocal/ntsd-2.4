# Continuous music and bitmap initialization at character-menu entry

Recover the real4229cc caller,429730 prologue, enabled402020 music and all
4297ae..429e5a bitmap initialization on one controlled CPU. The separate music
and whole-surface studies establish the children; this study verifies their
continuous order, shared globals, owned allocations and retained stack lifetime.
It supports the full native menu entry required by the original game. It does
not substitute for the still-running own installed-lib catalog/loading chain,
menu dispatch/return, an app window, Windows or actual audio/graphics devices.

Reference: pinned NTSD EXE, VC80, bgm/main.wma name and11 embedded DIBs;
Unicorn2.1.4 and the retained MusicPlayback/BitmapSurface platform adapters.
Execute the actual CRT sprintf and bitmap memset. Retain the declared controlled
PTD, COM/image/file/allocator responses and numeric errors. No Windows file is
written. Instruction/stack/register/allocation observations recover request
order, helper returns, formatting, cleanup, bytes/masks and storage producers.

The static original prologue consumes0xaa4 bytes. Start actual4229cc with
rootSP1000eab4 and its declared target atroot+68; actual pushes/call/prologue
produce menu bodySP1000e000. EBX is an opaque World token; original code copies
it without dereferencing it on this prefix. The actual prologue writes SEH and
cookie fields normally; observe them and the full mapped stack, without altering
those structures. Initial FPU CW037f is a declared research state, not a host or
Windows measurement. The menu remains unreturned at429e5a.

Finite14 cases: nominal; ramp/reverse allocations; previous-menu10; current-menu0;
music disabled; cached main track; negative graph creation with zero output;
negative RenderFile; NULL wide-path allocation; positive last CreateSurface;
failed SPARK description after missing RFACE; failed first description; failed
first GetObject; and bitmap-load flag0 with music still enabled. Ordinary platform
failure inputs are distinct from actual Windows results. The last operand cases
may expose earlier music/CRT stack producers: preserve observations, recover
their provenance before accepting a native result, and keep unresolved native
dependencies explicit. No private expected stack bytes may become native inputs.

Take an immutable nominal probe before the finite capture. Retain full snapshots
before/after music and every existing resource checkpoint, all allocation masks,
events, helpers and instructions. Independent write/read replay must cover stack,
SEH, globals, music buffers, bitmap buffers and any actual CRT storage changes.
Every normal guard/cookie check remains enabled. Stop before429b21 on NULL-SPARK,
and preserve any unexpected fault/dependency with exact error and available
observations; no automatic retry or source-fault continuation. No intentional
control/protection corruption, exploit, bypass or unrelated system operation.

Native exposes one staged music/full-bitmap operation, sharing the existing
music-before-previous-menu-write sequence. Use only native-produced globals,
audio allocations, resource storage and platform output fields. Verify complete
source/native states and declared API order, plus late errors after music,
bitmap construction, SPARK and final commit. Keep ready matches, NULL boundaries
and rejected unavailable operands separate. Retain the27 surface cases and both
historical startup/resource/repeated-menu corpora without expected edits.

Build accepted43bf56d plus explicitly owned files in isolation. Preserve248
fixtures,14pending catalog files,6foreign files,3pending source files, the live
corrected primary and890frozen watcher pins. Add accepted evidence only after
comparison and lossless packaging. Full own catalog/loading/menu/renderer,
Windows/devices/full-match/all-content/network/clean-Mac goals remain open.
