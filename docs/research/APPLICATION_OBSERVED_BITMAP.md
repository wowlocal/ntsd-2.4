# Observed bitmap replies through the whole first-menu Host

2026-09-26. The implementation parses and Core/MacPlatform compile, but the test
package build failed before XCTest. All69 selected methods remain unstarted.
This is not accepted whole-menu rendering or a playable game. [Finite plan](APPLICATION_OBSERVED_BITMAP_PLAN.md).

The immediate consumer is the existing Host/Bootstrap/MenuSession first-menu
iteration after physical macOS window/display startup. Connecting its bitmap
requests to the concrete provider removes another prepared-device boundary on
the startup/loading path to the Naruto/Sasuke District match. The Practice entry,
general raster delivery and the full game remain separate open requirements.

## Contract and implementation

OriginalBitmapRequestExchange shares the accepted request/permit/cursor algorithm.
OriginalApplicationObservedBitmapIteration suspends and retries the same whole
Host iteration; servicing occurs only after the Core stack has unwound. Host
checks its captured commit sequence while holding its transaction lock, so a
coordinator cannot silently continue against a different committed application.
The new final validation hook runs after the fallible delivery-context copy and
immediately before publication, or before retaining a pending-loading operation.

PreparedStartupPlatform owns its bitmap cursor and prior nonempty cursor history
by value. Receipt resources survive subsequent empty iterations and retained
batch contexts; empty iterations do not grow that history. The cursor is part of
the staged platform, so an attempted iteration cannot advance the committed
platform. Whole external side effects cannot be rolled back: their actual replies
and owners are retained for a retry instead of being performed twice.

The original prepared response-array path is unchanged. An explicit callback
path requires empty arrays and an owned bitmap provenance registry. Observed
GetObject writes are restricted to offsets4..<20; surface description writes to
8..<16. Bytes must agree with their actual owned image/surface. The original
registry still applies its numeric and ownership semantics, but its generated
structure projection is discarded: Core receives the observed response unchanged.
Partial observations remain partial, and required unknown reads are still errors.
Private BITMAP pointers/type and descriptor tails are never imported or zeroed.

The Mac service retains its old exchange entry and exposes the new coordinator
entry. Required message/debug requests need an explicit diagnostic consumer;
physical diagnostic UI is not implemented by this card. Unsupported preflight
requests do not start an external operation. Once service begins, failures retain
owners and the exchange's indeterminate outcome instead of inventing an HRESULT.
Already-served bitmap effects must not be delivered again through the overlapping
graphics projection of the committed batch.

Author analysis checks the changed call chain, publication order, extent arithmetic
and copy ownership. This is not independent review. No source rule, numeric literal,
old comparator, old test body, expected result, fixture or mask changes. The
behavioral reference remains the original EXE SHA256
3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c and the saved
[bootstrap](APPLICATION_BOOTSTRAP.md), [bitmap](BITMAP_SURFACE_LOADING.md),
[source-color](APPLICATION_SURFACE_COLORS.md) and ownership studies. No original,
emulator, Windows, historical capture or refused operation is executed.

## Finite validation boundary

Five new methods exercise complete first-menu physical bitmap acquisition, late
failure/retry, missing-resource diagnostics, observed output validation, stale and
foreign protocol attempts, and retained context/resource histories. Other API
inputs are explicit existing source case0 controls; the MSG window field is bound
to the actual Native window. Wrapper storage uses a disjoint test-owned logical
arena. Neither original after-state nor private source allocator identity is input.

A separate prepared replay of actual bitmap API values compares the whole Core
session: canonical bytes/masks, retained allocations/wrappers, text, settings, RNG,
graphics/resource registry and loop/timer state. This checks composition of the
two Native input paths; it is not a Windows differential observation. Physical
pixels and masks are independently compared with the accepted source-color owner.
Existing source caller comparisons remain in the retained regression selection.

All48 earlier methods plus5 HostSession,2 Bootstrap,3 Graphics and6 delivery-context
methods remain selected unchanged. Three expensive delivery-context methods keep
their1800s/16GiB limits; all other limits are inherited or declared in the plan.
A first nonpass stops the queue, preserves its evidence and leaves remaining
methods unstarted. A later correction must be a separately identified candidate.

## Inputs and open gates

Task: `build/research/application-observed-bitmap-20260926` on verified APFS X5.
Preparation20828 completed with exit0 in11.510s. The complete2265-file bitmap
baseline was cloned with distinct regular inodes; six files changed and three
were added, producing2268 files. All nine changed/new Swift files parse. Monitor
adaptation retains the corrected PID/start-time lifetime guard and unchanged
result predicates; six lifetime controls and the69-method phase domain pass.
Root1034, prior2257, baseline2265 and source55 remain protected.

Fresh build, package identity, all69 method results, complete archives and
independent review are separate gates. Root promotion, complete front raster,
general Blt, callbacks/rectangle provenance, exact zero presentation rectangles,
other IO/diagnostic UI/input/audio, initialized loading, Windows/clean-Mac,
full match and full game remain open. Source59727 stays at the recorded34-Object
boundary, not137. NTSDApp still uses Practice. EXE envelope is not recalculated.

## Preserved first build failure

Build21610 exited1 after152.265s; monitor reaped it with no guard, signal or
remaining process. The13 compiler diagnostics all concern the new test helper:
two zero-argument `run()` calls resolve to inherited `XCTestCase.run()`, returning
Void, rather than `run(late: Bool = false)`. The resulting member/type errors are
not Native comparison failures. The explicit `run(late:true)` call has no such
diagnostic. Core/MacPlatform compiled; no package gate or selected test passed.
Sampled process-tree peak was2,928,967,680 bytes. The original complete log and
failed2268-file candidate remain immutable.

Next: a separate exact-clone correction replacing those two helper calls with
`run(late:false)`, preserving production bytes, all test assertions, all69 exact
methods and original limits. Then fresh build/package/comparison. This corrects
test overload selection; it does not change the original contract or expected
values. Failure archival and publication are recorded below when verified.

Finalizer28739 finished with exit0 in57.864s. The task is
frozen: 4634 regular candidate/partial-release artifact files and
32 PAX metadata members verify by body/mode/nsmtime/membership.
The nine-file patch roundtrip and all protected inputs passed. No current study
process remains. [Publication](../evidence/application-observed-bitmap.json),
[closure](../evidence/application-observed-bitmap-close.json),
[patch](../evidence/application-observed-bitmap.patch). Candidate manifest:
`abd082fc1b711d4f004391b8fa9000b04cba589a2f895c0acccac293eced6b74`.
