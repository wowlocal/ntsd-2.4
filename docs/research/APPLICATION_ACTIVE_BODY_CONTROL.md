# Active control comparison on owned application state

Status: finite control endpoints and owners verified. This is the first
bounded result of [the active body plan](APPLICATION_ACTIVE_BODY_PLAN.md), after
[accepted input and body preflight](APPLICATION_ACTIVE_GAMEPLAY_INPUT.md).
The remaining18 body stages and complete active tick remain unaccepted.

## Independent calculation and comparison

The comparison-only control model reads current own World/Actor/Object/Frame and
RNG records. It never supplies a predicted after-state to Core or Bootstrap.
Before comparing Native, each method reconstructs all48 saved source control
endpoints and five checkpoints per active Actor. Complete bytes and defined
masks are compared, including the unchanged World and398 other Actor slots.
The source reader checks the10 checkpoint PCs per call, final41e634/SP1000e9bc,
ordered random effects, parent Actor and417170 helper returns to413641.

The finite path covers signed buffer decay, ordered edge/history writes, nine
combo recognizers, strict signed Frame-input priorities, ordinary movement,
sequential attack/jump/defend selection, current Frame velocities and live RNG.
Only distinct slots0/1 are active. Original Object IDs2/11 and states0/1/3/4/7
bound this comparison; they do not introduce restrictions in the game engine.
Nonzero hit_d/hit_j fields are read and their predicates checked. A fresh Frame
read after earlier transitions checks the installed-library routing boundary;
final Native observations alone would not establish that condition.

RNG is calculated on the actual retained state. Source results are not universal
expected values: the own Sasuke selection can choose65 where the saved source
chooses60. Subsequent lifecycle frames belong to later, still-open comparisons.
Host53-bit arithmetic is used only for guarded zero or magnitude[1e-100,10000)
operands/results. General extended-exponent/subnormal semantics remain in the
unchanged Core contract and [precision study](CONTROL_PRECISION.md).

At each own control checkpoint the test compares the full pool/globals and
complete match, current memory/resource owners, music, graphics, library/CRT
state, retained caller journal and unknown local record. Expected storage is a
separate test value. The existing whole48-call Bootstrap/input preflight retains
its rejection, rollback and same-ticket retry checks.

## Declared gaps

Source control stage snapshots omit middle Object/Frame heap records. Acquired
records supply the control inputs; whole-call retention does not prove absence
of interior writes. The Python saved-data inventory checks resulting masks but
not every operand's definedness; Swift known reads enforce its actual operands.
Endpoint/checkpoint agreement and selected RNG joins do not establish the full
helper ABI, all instruction stores, or an observed Native hook checkpoint.

Combo transfers, successful Frame transfers, run entry and airborne attacks
remain explicit boundaries of this finite comparison. The other18 active body
stages, paused paths, damaging outcomes, complete match/game, app/device and
Windows/clean-Mac acceptance remain open. Existing safety incidents remain open;
no rejected operation, original execution or historical capture/auditor is run.

## Validation and preservation

Four release methods passed in138.078s after a382.17s build. The new
methods reproduced960 source Actor checkpoints and96 full control endpoints,
then compared96 own control checkpoints and six own RNG events. Twelve negative
comparison trials reject unsupported numeric inputs, aliases, unknown causal
bytes and combo-transfer progress. Both existing active methods were rerun;
the same48-call schedules are repeated by the new methods, not new coverage.
The145 inherited methods are retained by exact pins, not rerun (149 unique
retained methods including the four selected here).

All994 Native files were checked against the frozen candidate;990 prior files
are unchanged. All383 fixtures/102 resources and485 packaged files retain exact
bytes/modes. The Native archive and evidence archive were read back completely.
The first source comparator failure is preserved: early.globals is a current
RNG-state mirror, and now compares to predicted globals rather than startup
immutability. All other early owner fields remain fully compared. The source
contract and four changed Swift files received independent review;
the final publication review is linked from the evidence record. Source fixtures,
Core and historical evidence remain unchanged. Research output lives in
`build/research/application-owned-active-body-native-20260914/` on verified X5.
Code-volume estimates are not recalculated by this card.

[Machine evidence](../evidence/application-active-body-control.json) records exact
commands, terminal job, file/archive hashes, reviews and remaining gates.
