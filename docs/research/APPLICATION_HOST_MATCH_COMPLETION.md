# Typed-host selection complete on the unchanged correction1 binary

**2026-09-22: all53 selected Native methods passed on one verified binary.**
This combines25 preserved correction1 passes with28 new whole-method results.
The three new host tests compare actual loading/character/selection parents,
retained Start/platform, launch and the enclosing application return, including
retry and ticket rejection. First gameplay input is inspected and rolled back
at its declared stop; retained gameplay and a complete tick/match/game remain open.

The [frozen plan](APPLICATION_HOST_MATCH_COMPLETION_PLAN.md) follows the
[preserved method26 guard](APPLICATION_HOST_MATCH_CORRECTION1.md). Base HEAD
`91e7a1bb8e568b6e828bbbb38671242918210da5`. This task made no Native, test, expected,
mask, resource or candidate change and performed no build/relink. It used the
frozen1039-file correction1 candidate and62,637,720-byte binary in place, with
the original candidate cwd and new task-only commands/logs/jobs/TMPDIR.
Root Native remains the protected1034-file tree; no promotion occurred.

[Publication](../evidence/application-host-match-completion.json),
[closure](../evidence/application-host-match-completion-close.json) and
[documentation checks](../evidence/application-host-match-completion-docs.json)
identify the exact inputs, all retained/new results, resource contract and
preservation checks. External task:
`build/research/application-host-match-completion-20260922`.

## Executed comparison

Preparation66887 exited0 in8.056s, verifying candidate/package bytes and all25
prior complete named results. The installed24GiB physical RAM, X5 UUID and
space commitments were checked. All28 generated commands retain their original
selectors, binary, cwd, timeouts and sampling intervals. Only method26's resident
ceiling changes from8 to12GiB; the environment changes only its task-owned TMPDIR.
The monitoring loop is unchanged apart from verified artifact-root arguments;
its ownership/transition predicates and the complete XCTest pass criterion remain
byte-identical to the preceding validated forms.
Positive/negative cwd/phase checks are synthetic classifications, not new OS
transition observations or original-game execution.

Queue66967 exited0 in1233.194s. All28 methods26–53 completed, with991.000s summed
test-process time. Each result has the exact named pass, three complete
one-test/zero-failure summaries, Selected-tests pass, exit0 and no guard.
The maximum sampled resident set is9,893,609,472 bytes at method53.
Method26 passed in98.269s with sampled peak7,374,979,072 bytes under its12GiB
ceiling. This is below the old8GiB ceiling as well; the increase in allowance
does not establish the cause of the previous peak or prove additional memory
was consumed. The previous8,894,627,840-byte guard/exit-15 remains immutable.

The full50-method regression selection includes actual host loading/cached
cycles, initialized loading, whole character and countdown/District/Start
comparisons, owned launch with late failures and both direct launch variants.
The following three tests add the declared enclosing host contract:

| Method | Verified boundary | Process result |
| --- | --- | --- |
|51 `testActualHostStartLaunchAndNextInputRetainWholeOwners`|Both backings use actual host parents; Start remains private, launch prepares the returned child, final outer commit publishes the complete batch, then first input is compared inside the host context and deliberately rolled back.|PID71695,114.633s,8,133,885,952 sampled resident bytes, exit0.|
|52 `testStartLaunchAndOuterTailFailuresRetainHostStages`|Four Start-preparation failures,17 launch failures and time/Sleep/commit failures preserve the appropriate child/platform and earlier batch; successful retry commits once and drains ordered batches.|PID72304,59.431s,7,082,213,376 sampled resident bytes, exit0.|
|53 `testMatchPreludeRejectsDifferentTicketsAndReentry`|Actual foreign and same-revision sibling parents/launch results, stale and consumed tickets, wrong retained stage and reentry are checked without replacing the host's child.|PID72630,86.002s,9,893,609,472 sampled resident bytes, exit0.|

These controls compare whole retained state and existing child outputs; they
do not establish natural reachability of every injected failure. Provider
clock, resource, sound and input responses remain declared Native test values,
not Windows/device observations. The next-input recording/checksum/tick state
is observed before the injected stop and is not installed as retained gameplay.

## Preservation and next dependency

Finalization checks root1034, candidate1039, all487 packaged resources,55 live
source pins, exact old/new results and every body/mode/nanosecond mtime/member
of the previous2486-file/156-directory artifact archive. Only new metadata is
archived here; no duplicate large candidate/build archive is created.
Finalizer73241 exited0 in93.508s and is absent. The132-member metadata archive
is983,040 bytes and passed exact body/mode/nanosecond PAX mtime/membership
readback. The prior2486-file archive remains intact. Observed external decrease
414,429,184 bytes, including concurrent source growth, stays within4GiB;
the task is frozen. Original40GiB external/6GiB internal reserves and44GiB/7GiB
runtime floors remain. Source59727 identity/cwd was revalidated18:52:53 UTC at
3338 chunks/22 Objects, still running and incomplete.

Next bounded task: read-only preflight of the actual host's next gameplay-entry/
input child and its following consumer, using existing saved evidence and the
newly compared ownership boundary. Identify the required retained state, aliases,
platform effects and whole-caller rollback/comparison before extending the API.
Keep the incomplete whole initialized catalog source as a separate dependency;
these tests do not publish or substitute for it.

Independent contract/comparator review remains unavailable. Native comparison,
package and archive are separate from review, root promotion and actual app
delivery. Actual timing/providers, window/input/audio, Windows and clean-Mac
checks, retained gameplay, full matches, all content/modes and the complete
native game remain open, as do the existing safety incidents. No refused path
was retried. The original EXE remains pinned at
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`;
no original execution occurred and the EXE envelope was not recalculated.
