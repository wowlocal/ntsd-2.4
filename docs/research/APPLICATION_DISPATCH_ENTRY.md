# Own dispatcher and first static World entry

Eight fresh own startup/message-loop chains now continue the actual43e9a0
entry through graphics preparation and into4246b0 using static World458b00.
Native independently rebuilds that same prefix and reaches the existing front
resource consumer's first allocation request,8016bytes. The original stops at
4450ac/SP1000ea6c before that allocation executes or returns. **No dispatcher,
World, resource allocation, frame or match has returned in this study.**

The [finite plan](APPLICATION_DISPATCH_ENTRY_PLAN.md) extends
[APPLICATION_MESSAGE_LOOP](APPLICATION_MESSAGE_LOOP.md) and uses the accepted
[APPLICATION_ART_SETUP](APPLICATION_ART_SETUP.md) and
[front-resource consumer](FRONT_MENU_RESOURCES.md). The full174-event own
WinMain parent reproduces unchanged. The normal preceding loop is identical to
its accepted required-dispatch case; a second loop uses an actual minimize
callback. Both loops and every source case retain complete original evidence.

## Reference and execution boundary

The reference is the pinned NTSD2.4 EXE, SHA256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
its VC80 dependencies and original resources from the accepted WinMain parent,
on Unicorn2.1.4. Each new case uses one CPU and continuous stack, registers,
globals and owned platform handles from that startup. No expected child state,
stack word, allocation result or World alias is inserted between stages.
Win32/COM responses and resource delivery remain declared research boundaries.
This does not resolve earlier CRT/NLS execution or actual Windows delivery.

Instruction/stack tracing is needed to establish which World and surface the
application actually passes, and whether earlier stack bytes are reused by the
platform records. The ordinary dispatcher and World SEH/cookie prologues execute.
Their two FS:[0] stores per case are preserved; no protective/control storage
is corrupted, no check is bypassed and no epilogue return is claimed.

The finite controls are normal restore and minimize callbacks, isolated negative
pixel-format/first-clear/second-clear HRESULTs, two signed-extreme combinations,
and minimize with all three negative results. A first probe failed while freezing
the completed parent: deepcopy traversed an Events list's VM owner and attempted
Uc construction without arch/mode. JSON data-only freezing fixed that observer
bookkeeping; the failed script/log and successful probe remain retained. No
source input, native rule or expected result changed.

## Actual order and storage

At43e9a0 the caller's incoming argument is1 after restore or0 after minimize.
It is not a surface pointer and the dispatcher does not read it. Graphics use
own startup primary31002000 through455634 and back31003000 through455608.

1. Service-key scan writes450bec=0 then4593a4=0 on this own path.
2. Because44dce4 is1, whole43e8e0 queries the primary, clears the live back
   surface black, and emits the original success/failure debug string. Query
   errors are ignored; a negative first clear selects helper result0.
3. Regardless of that helper result, the caller writes458440=1,44dce4=2 and
   4593a0=0, then clears the live back surface again. Its HRESULT is ignored.
4. Mode0 calls4246b0 with actual ECX458b00, target31003000 and returnPC43ecbf,
   SP1000ee98. All2008 World bytes are the independently recovered PE zero-fill.
5. The phase0 branch computes1-4511f8 and writes1 at42476b. Own44d068 is1;
   CALL42477f requests allocation0x1f50 and reaches4450ac with returnPC424784.

This establishes the selected own static-World binding. It does not execute
initializer446300 anew or prove its order in the CRT initializer table. The
older controlled World22000020 remains a separate contract. Other dispatcher
modes, live mode changes after the pending World return, worker/editor/loading
routes and actual dispatcher ret remain open.

The source executes135 distinct EXE instruction starts:56/236 dispatcher,
26/26 art helper,17/17 clear and36 World-prefix starts. There are no new DLL
instructions;4450ac is an unexecuted stop. All bytes match the pinned PE, and
all13 installed lib patch spans are disjoint from the new continuation and
preceding loop instructions. This does not complete other library hooks.
The eight prefixes retain40 events,48 global stores and24 completed helper
returns; all400704 global bytes and masks compare. CW stays037f throughout
the new source prefix; this is not a host FPU claim.

## Stack evidence and native ownership

Every case preserves2304 source stack bytes/masks. An independent verifier
replays the declared initial caller arguments, complete startup stores, preceding
loop stores and all new stores. It reconstructs all18432 final stack bytes,
all24 helper-entry records and their earlier-write masks. Those32/100/100-byte
records happen to have earlier producers throughout; some retained bytes come
from startup resource parsing. Native does not acquire ownership of private
compiler stack records merely because the research trace can identify writes.

The platform contract compares owned size32 for the query, and size100/color0
for each clear, plus complete masks, targets, flags, strings, HRESULTs and order.
Across eight cases,160 owned structure bytes compare;1696 opaque bytes remain
unknown natively and are asserted zero with false masks. All1856 source request
bytes remain immutable evidence. This is not full native structure-byte equality.

The new source trace contains456 CPU stack stores and five successful API
query outputs of32bytes. Per-helper raw masks count original CPU stores only:
query size4bytes, each clear size/color8bytes. API output belongs separately to
stackStores and the lifetime knownStack mask. The read-only verifier initially
conflated these masks and treated debug as a structured request; it was corrected
to their actual contracts without editing source or native results.

## Native composition and rollback

[OriginalApplicationDispatchEntry](../../native/Sources/NTSDCore/OriginalApplicationDispatchEntry.swift)
shares service-key, art and clear implementations and stages the full50088-byte
global record. Own loop outer storage overlays independently verified PE backing.
It returns an explicit mode0 GameEntry; other modes report a required dependency.
This API boundary precedes4246b0 and is not a whole-dispatcher return.

The [comparison](../../native/Tests/NTSDCoreTests/OriginalApplicationDispatchEntryTests.swift)
then enters OriginalFrontMenuResources using its own World bytes and globals.
Its own toggle and first allocate(index0) request match, before allocation output
is supplied. The required dependency throws, rolling back the entire pending
loop iteration while retaining completed startup and the preceding callback.
Full globals, outer storage, local/replay ownership, MSG, counter and timer
baseline are checked. Source partial progress is retained separately.

Six additional native failures occur at query, first clear, debug, second clear,
front toggle observer and required-allocation observer. Each preserves that same
committed parent. External effects must remain buffered until a whole iteration
commits. An unavailable dependency with rollback is not successful allocation.

## Verification and next continuation

Raw9 release tests passed4.514s/build202.16s. Final packaged9 tests passed
4.441s/build0.30s without a raw override, including retained message-loop,
art and both front-resource corpora. NTSDNative linked; this composer is not
wired into the Practice app. CUA again reported
`Native apps: Error: Sky Computer Use native pipe startup failed`;
no window or input action occurred and no device or latency claim follows.

The raw corpus3075646bytes has SHA256
`f8017f13e12a7a7bd833f08c2248d5423c8ce21a7e3da0a16be35ec7e57823b0`.
The lossless fixture2453703bytes has SHA256
`2860df5bb6e87bb13152793206b7867898e2c0783b456899876b04f2c1df1d75`.
Full raw/packed bytes, JSON/SHA,151 blobs, eight atomic cases,238 unchanged prior
fixture pins and10 vendor hashes verify;239 fixtures now exist. The isolated
604-file committed6fb8ce4 native export plus three new overlays contains607
files and excludes the six unchanged foreign transform files. All owned source
and SwiftPM jobs are terminal; completed captures must not be restarted.

Reproduction tools are [source](../../tools/oracle_application_dispatch_entry.py),
[read-only verifier](../../tools/verify_application_dispatch_entry.py) and
[acceptance](../../tools/accept_application_dispatch_entry.py).
The report is [application-dispatch-entry.json](../evidence/application-dispatch-entry.json);
job/failed-probe details are in`build/research/application-dispatch-entry-work.json`.
Next continue the actual allocation and43ee50/front-resource loading with own
resource/allocation provenance, then World/menu and remaining dispatcher paths.
Earlier CRT/NLS, private backing, installed library transforms, worker/application
integration, real Windows/window/input/audio, full matches/all content and clean
macOS acceptance remain open. The full game goal is still active.
