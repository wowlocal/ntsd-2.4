# Native continuation after catalog loading

Connect the actual native common-loading prefix, completed catalog/sound owners
and current globals to41c052..41c581 pool/UI construction. Retain the original
pause result and both10-byte command buffers. Current globals must come from
the caller's own catalog execution; never restore an earlier cache/global snapshot.

Reference: unchanged INITIAL_LOADING/INITIAL_LOADING_CONTROL full controlled
NTSD EXE chains and INITIAL_INTERFACE_SURFACE's ten controlled Unicorn chains.
Pinned EXE3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c,
VC80c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d,
and original DAT/BMP/WAV resources. No new original execution or source changes.
The older full loading corpus retains its explicitly supplied bitmap-device
boundary/frozen progress clock; it does not prove the pending installed-library
own application chain or the complete device helpers inside that old capture.

The pool/UI source reads Object+90 after each staging Actor constructor. Use the
retained read/write/helper order to verify80 such reads across the ten cases,
and defer the native provider until those consumers. Expected records remain
comparison operands, never native inputs. No private stack, host allocation,
hardware FPU, Windows/device or source NULL-Actor equivalence is asserted.

Finite acceptance: both existing full loading cases must retain all original
catalog/audio/pool/UI state and events through the new continuation; all retained
pool/UI cases must preserve full constructor/final bytes and masks plus late-read
order. Native-only full-surface completion and a final observer failure exercise
owned resource retention and rollback using a genuinely native loaded catalog.
Missing UI images in that native-only control are declared platform responses,
not a substituted application renderer or a claimed new source full-loading case.

The three full own catalog captures and frozen40-checkpoint package remain
untouched. Full own catalog return, pool/UI,41bc90, application window, full match,
all content, Windows/devices and clean-Mac acceptance remain open.
