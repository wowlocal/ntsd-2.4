# Startup menu-information reader

Recover whole43c4a0..43c685/ret, called at43cf94 before content/panel/default
selection. The pinned original NTSD2.4 EXE and VC80 DLL, executed in Unicorn2.1.4,
are the behavioral reference. Fopen/fclose and translated byte _read responses,
C locale/PTD/locks are declared controlled boundaries. No real game files are
modified, no network request is made, and this is not full WinMain/Windows.

Instruction execution is needed to recover four-token input, fixed-width date
conversions, retained index/period, local initialization and early failure order.
The function compares local <end> even after fopen failure. Track byte provenance;
preserve source unknown reads and separately reject them natively with rollback.
Do not inject private stack bytes, manufacture overflows or alter cookie/control
storage. Tokens in this study fit the actual observed52-byte local spans. The
absence of a width in %s remains an open longer-input boundary, not a native
truncation rule or a claim that52 is a recovered C array declaration.

Finite acceptance:

1. Execute the original adinfo file; now/dont_update and six-part dates; each
   sentinel/partial/invalid date component; signed index/period with retained
   values after conversion failure; missing/empty/short input and bad end marker.
   Supply ordinary read/close failures and chunk1/7/4096. Use two unknown local
   backing patterns and independently declared global initial states.
2. Execute actual VC80 fscanf/sscanf/sprintf. Record full globals/masks, all184
   local bytes/masks, ordered parent stores, CRT returns/arguments and actual
   instruction starts, balanced stack, nonvolatile registers and suppliedCW.
   Compare defined local bytes and every mask; unknown bytes stay in source only.
3. Native shares recovered integer conversion, preserves source partial writes,
   compares byte markers in access order, returns actual boolean, and rolls back
   on unknown reads and late observers. Keep rejected cases separate from whole
   matches. Connect an own successful reader state to the existing cache writer
   and feed its emitted bytes back to a fresh reader, with actual source execution
   for the same connection where feasible; no expected after-state import.
4. Run raw native tests in an isolated committed export, preserving all228 old
   fixture pins and six foreign files. Publish only after raw acceptance;
   independently verify every source/packed byte, JSON/SHA/blob, code inventory,
   old fixture/vendor hashes and terminal jobs; rerun packaged comparisons.

All bundled-library patches must be checked for disjointness with studied bodies.
Full CRT/NLS, earlier window/local provenance, content/panel/startup composition,
application input/audio, full match/content/Windows and clean-Mac remain open.

Finite acceptance is complete in [MENU_INFO_READING](MENU_INFO_READING.md):
194 whole main calls, two unknown-local rejections and16 own read/write/read
chains. Both raw and packaged7-test comparisons pass. Whole43cf94..43cfb4,
WinMain/application/Windows and the full game goal remain open.
