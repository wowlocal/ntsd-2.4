# Host loading validation — frozen plan, 2026-09-22

Validate the exact [loading candidate](APPLICATION_HOST_LOADING_CANDIDATE.md)
under [WORKFLOW](WORKFLOW.md). The game question is whether the persistent host
retains real loading owners and commits one complete loaded iteration, including
retry/rollback and existing source comparisons. This advances application loading
toward the first interactive match; full-game and actual-device gates stay open.

Base HEAD `452898f02159f1ff08263a7b0e1c221a7ef8b2e9`. Actual input is the frozen
1037-file candidate manifest, not a HEAD-only checkout. Reference EXE SHA-256
`3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`, lib.dll
`28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`, VC80
`c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`.
Existing saved controlled source records supply expectations. No original code,
historical capture/auditor, actual device or refused operation runs in this task.

## Commands and finite cases

Verify candidate publication/closure/archive and manifest; preserve root1034,
55 live source code pins,385 fixtures and102 runtime resources. Clone regular
candidate files into the new task and verify full body/mode/membership/distinct
inodes. No cache from a previous task is imported.

Build fresh Core and dependent targets with the installed Swift toolchain,
Swift5/macOS14 release target, `swift-build --build-system native --build-tests
-c release --jobs 2 --disable-index-store -Xswiftc -enable-testing`, with task
scratch/cache/config/security and minimal environment. The supported deprecated
native build system and release testability flag were checked against the
installed CLI in the preceding validation. Record current version, exact command,
compiler source/output graph, generated accessors, package bytes and binary.

Run all42 frozen methods, one named XCTest method per process, with packaged
resources and no raw-fixture override. Selection:25 previous host regressions,
four new HostLoading methods, five LoadedMenu, three LoadedCycle and five
InitializedMenu/InitializedLoading. Whole parent state/masks/resource owners,
numeric observations, ordered requests, operations and graphics remain compared.
Record actual exit and complete named method/suite result; exit0 alone is not a
pass. Repeated/overlapping corpus cases are not new source coverage. The fourth
input boundary is not a returned iteration, and the full Native catalog retains
its declared controls rather than proving the running whole Catalog53 source.

Stop dependent work on the first failed build/method/guard. Preserve the exact
candidate, logs, partial products and terminal status before a correction.
At most three separately identified correction rounds, with distinct candidate
and amended command/input evidence. Never edit expected/masks, skip a mismatch
or overwrite a failed candidate to accept a result. Independent review of any
contract/comparator change remains a separate gate.

## Storage and process ownership

Task `/Volumes/X5/ntsd-2.4-research/01a0c7b2-a956-7760-950e-ecf7dceaaa20/application-host-loading-validation-20260922`,
alias `build/research/application-host-loading-validation-20260922`. Verify APFS
UUID `3A4F5FA6-DC86-4C6E-87E2-EAC2C2D72548`, mount and current free space. Allow24GiB
observed decrease for fresh products, retained failures and verified archive,
including concurrent source growth. Start requires81GiB external/7GiB internal;
stop at22GiB decrease with2GiB terminal/archive margin. Retain original40GiB/6GiB
reserves and the source's17GiB gross allowance. Do not delete evidence or lower
reserves. Task logical bound150GiB; sparse CAS is distinct from physical decrease.

Reuse the previous host runner's ownership, descendant RSS, space, guard and
reap loop. Only new task/input gates and42-method phase range change. Limits:
build3600s/12GiB sampled tree RSS; each method600s/8GiB; selection3600s.
Poll1s, RSS2s, logical size15s. Short processes may finish before an RSS sample;
recorded0 then means no sample. Minimal environment is HOME/USER/LOGNAME/LANG/
LC_ALL/PATH/TMPDIR. Never run XCTest help with the inherited environment.

Before signals verify PID/start/full command/cwd/job. Preserve the existing
allow-listed same-identity compiler exec transitions and narrow `(xctest)` terminal
observation handling; neither authorizes adopting a different process. Revalidated
SIGTERM only to this task's owned live processes on its declared guard. Preserve
monitor errors and incomplete observations. Never signal source PID59727, rerun a
completed capture/build for silence or modify a live producer. Historical source
HEAD is launch provenance, not a continuing HEAD lock.

## Publication

Root mutable paths: this plan, APPLICATION_HOST_LOADING_VALIDATION.md,
docs/evidence/application-host-loading-validation*, task preparation/runner/
package/test/finalization helpers and own CURRENT_WORK/RESEARCH_MAP paragraphs.
All build/candidate/test products belong to the new X5 task. Closed tasks, baseline,
root Native, immutable instruction archive and all expected inputs stay protected.

Owner is root author; no independent reviewer is available. Separate Native
comparison, package bytes, archive identity, review and real app/device acceptance.
Verify complete archive bodies/modes/membership and process terminal/absence
before closing the task. Publish a failure honestly if acceptance is not reached.
Commit each coherent checked increment with its exact scope and existing hooks;
preserve unrelated pending work. Root promotion, actual providers/devices, later
typed child retention, whole Catalog53 comparison, existing incidents and full
game acceptance remain open.
