# Whole retained Stage selection with installed library text

Recover the complete ordinary Stage/mode1 character menu through actual returns
and the match-prelude boundary: fixed human/computer team1, zero and two CPU
counts, wrap/hold/release, CPU cancel to count and previous character, immediate
character readiness, live d7/d8/EA randomization, Stage1..5/Survival, difficulty,
Music ON/OFF in both directions, reselection and a second ready/Start route.

Use the pinned original NTSD EXE `3f7ac67c5890ef979ee24a6dae5528056e7f631725c292cf9cb0a928ebeff71c`,
lib.dll `28d4f1b07992e058840bdac04d8ba44d6f037a248e29d962712bf44bcf90baba`,
VC80 `c3ac989c8489a23bb96400b1856f5325ffc67e844f04651ea5d61bc20a991c6d`
and raw original DAT/BMP/DIBs in controlled Unicorn2.1.4/CW023f. Actual41bc90
prologue establishes the caller frame; declared4229cc resumes the menu tail.
The complete429730 and output/422ab8ret4 run, or stop BEFORE42cf8a match prelude.
Earlier tick, whole library startup, Windows and device behavior remain open.

Read-only disassembly recovers pending pushes, local string producers and the
mode-specific branches. Full store/read/helper/ABI/ownership traces are needed
to recover within-call ordering and retained state. Initial catalog operands
and CRT-seeded RNG tables are the already declared raw-derived inputs. Stage7
and59 represent declared carried values inside the ordinary stage range; their
full own provenance is not claimed. Settings floors each to its tens value.
Every later call retains all prior bytes/masks/owners/DC/source stack and only
changes ordinary button bytes plus three declared ABI words. The finite manifest
is written before execution. No expected after-state or unknown source stack
becomes Native input; short Stage labels are produced from Native stage values.

Acceptance requires all finite returns/boundaries, complete World/400Actor/
globals/owned resource bytes and masks, ordered output and random candidate
lists, all observed caller counters, independent store/read/checkpoint replay,
actual instruction bytes and helper return ABI. Verify fresh Stage initial
calls against the accepted fresh-character parent where inputs are shared;
mode-specific initial declarations are compared explicitly, not called an
identical parent. Both chains must retain Naruto17/Sasuke21 and team1. CPU2
completion uses d8, zero-CPU uses d7, Random commands use EA; held input must not
repeat draws. All six stage settings and both music directions must execute.
Compare the unchanged VS selection and commands corpora after shared Core edits.
Late failure after the third d8 assignment and after a Survival text output
must roll back the entire current Native call and retain preceding calls.

Create a new isolated Native package from accepted663 files; preserve all263 old
fixtures,14 pending Native/3 pending source/6 foreign files and frozen inputs.
Large captures/builds use the task-owned APFS X5 directory via the new canonical
build/research/lib-stage-mode symlink. Shipping fixtures stay self-contained in
the repository. Frozen packages are not modified. Catalog5 remains an independent
live process: revalidate identity, do not restart or lower its6GiB reserve.

Modes2/3/4, empty candidate scratch, enabled special network/playback menu,
match preparation and own library initialization remain open. No deliberate
pointer/protection corruption, arbitrary fault continuation or safeguard retry.
Preserve any actual error/refusal with operation/time/session and stop retries
of a refused operation. This finite study is not full-game/app/Windows/device/
network/clean-Mac completion.
